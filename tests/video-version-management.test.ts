import { test } from '@playwright/test'

test.fixme('VVM-01: Generate new version creates a new job row without resetting existing', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-02: Version list shows all non-archived versions in desc order with v1/v2/v3 labels', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-03: Publishing version X unpublishes all other versions for that SOP', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-04: Worker video tab still shows only the published version', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-05: Archiving moves version to collapsible archived section', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-06: Permanent delete from archived section removes job record', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-07: Label edit saves updated label text', async ({ page }) => {
  // Phase 10 stub
})

test.fixme('VVM-08: Active generation in version list shows live progress stepper', async ({ page }) => {
  // Phase 10 stub
})
