import { test } from '@playwright/test'

test.fixme(
  'SB-UX-05 alert: dispatchEscalationAlert inserts worker_notifications row',
  async () => {}
)

test.fixme('SB-UX-05 lock: NEXT disabled until unlock', async () => {})

test.fixme(
  'SB-UX-05 form: submit closes modal and writes escalation_reports',
  async () => {}
)
