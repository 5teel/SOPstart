import { test } from '@playwright/test'

test.fixme('VGEN-07: Workers see Video tab in SOP detail when a published generated video exists', async ({ page }) => {
  // Phase 8: Video SOP Generation
})

test.fixme('VGEN-08: Video player supports chapter navigation, timestamp jumps, and playback speed control', async ({ page }) => {
  // Phase 8: Video SOP Generation
})
