---
phase: 24-procedure-flow-spatial-node-graph
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/sop/flow/FlowGraphCanvas.tsx
  - src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts
autonomous: true
requirements: [FLOW-01, FLOW-03]
must_haves:
  truths:
    - "A flow graph with authored node positions (any node.position.x !== 0) renders at those exact coordinates, not in auto-layout columns"
    - "A derived graph (all node.x === 0) still renders with the existing depth-layer auto-layout"
    - "The Fit button performs a real viewBox-based zoom-to-fit, not a scroll-to-top-left"
    - "The Export PNG button downloads a PNG of the rendered graph with node colours preserved"
    - "Node colours use the --accent-* CSS-var token set matching FlowTab's TYPE_COLORS (no divergent hardcoded hex)"
  artifacts:
    - path: "src/components/sop/flow/FlowGraphCanvas.tsx"
      provides: "Production renderer: hasExplicitPositions/layoutFromPositions, fitToView, exportPng, unified accent tokens"
      contains: "hasExplicitPositions"
  key_links:
    - from: "FlowGraphCanvas.layout()"
      to: "node.position"
      via: "hasExplicitPositions branch → layoutFromPositions"
      pattern: "layoutFromPositions"
    - from: "FlowGraphCanvas exportPng"
      to: "canvas.toBlob"
      via: "XMLSerializer + getComputedStyle inline"
      pattern: "canvas\\.toBlob"
    - from: "Fit button"
      to: "fitToView"
      via: "onClick handler"
      pattern: "onClick=\\{.*fitToView"
---

<objective>
Productionise the FlowGraphCanvas SVG renderer: (FLOW-01) make `layout()` honour authored node positions when present and fall back to the existing depth-layer auto-layout otherwise; unify the node colour map onto the `--accent-*` CSS-var tokens that already drive FlowTab's list view; (FLOW-03) replace the Fit stub with real viewBox zoom-to-fit and add an Export PNG button that serialises the SVG (with CSS vars inlined) to a downloadable PNG. Zero new dependencies — native Canvas + XMLSerializer only. PREVIEW pill removal happens here too (it lives in this file). Flip the relevant FLOW-01/03 source-contract stubs from Plan 01 to live.

Purpose: Turn the prototype canvas into the production default the desktop Flow view will mount.
Output: Updated FlowGraphCanvas.tsx; flow-graph-canvas.spec.ts FLOW-01/03 assertions live and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-01-SUMMARY.md
@src/components/sop/flow/FlowGraphCanvas.tsx
@src/components/sop/tabs/FlowTab.tsx
@src/lib/offline/photo-compress.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FLOW-01 — honour explicit node positions + unify accent tokens</name>
  <files>src/components/sop/flow/FlowGraphCanvas.tsx</files>
  <read_first>
    - src/components/sop/flow/FlowGraphCanvas.tsx (current prototype — layout() lines 41-85, NODE map lines 17-25, Placed interface lines 33-39, render tree lines 103-191)
    - src/components/sop/tabs/FlowTab.tsx (TYPE_COLORS lines 31-39 — the CANONICAL accent token set FlowGraphCanvas must match: --accent-step #1e40af, --accent-measure #0d9488, --accent-decision #d97706, --accent-escalate #dc2626, --accent-signoff #7c3aed, --accent-inspect #0284c7, --accent-zone #16a34a)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md (§ FlowGraphCanvas — layoutFromPositions spec + colour token unification spec)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-01-SUMMARY.md (confirm schema relaxation landed; explicit graphs now validate in FlowTab)
  </read_first>
  <behavior>
    - hasExplicitPositions(graph) returns true when any node.position.x !== 0, false when all x === 0
    - layout() with an explicit-position graph returns placed coords equal to each node.position (verbatim), width/height = bounding box + NW/NH + PAD*2
    - layout() with an all-zero-x derived graph returns the existing depth-layer auto-layout (unchanged behaviour — existing edges/columns preserved)
    - Node fill/stroke use var(--accent-<type>) values matching FlowTab.TYPE_COLORS; no `#db2777`, `#ea580c`, or other divergent decision/measurement hex remain in the NODE map
  </behavior>
  <action>
    Add a top-level `hasExplicitPositions(graph: FlowGraph): boolean` returning `graph.nodes.some((n) => n.position.x !== 0)`. Add `layoutFromPositions(graph)` returning the same `{ placed: Map<string, Placed>; width: number; height: number }` shape, where each placed entry's x/y equal `node.position.x/y`, `width = Math.max(...xs) + NW + PAD*2`, `height = Math.max(...ys) + NH + PAD*2`. At the top of the existing `layout()`, return `layoutFromPositions(graph)` when `hasExplicitPositions(graph)` is true; otherwise run the existing auto-layout body unchanged (do not delete or alter the longest-path depth layering — it remains the fallback for derived/all-zero-x graphs).
    Replace the hardcoded `NODE` map (lines 17-25) so each type's `fill`/`stroke` use the unified `--accent-*` tokens matching FlowTab.TYPE_COLORS. Use the CSS-var form with hex fallbacks identical to FlowTab: stroke = `var(--accent-step, #1e40af)` etc. For fills, derive a light tint via `color-mix(in srgb, var(--accent-<type>) 12%, transparent)` (matching FlowTab's StepCard tint idiom) OR keep a paper background and rely on the accent stroke + left accent bar — pick the approach that keeps node legibility on the grid-paper bg; the binding constraint is that the stroke/accent equals the FlowTab token and NO divergent hex (`#db2777` pink decision, `#ea580c` orange measurement) remains. Keep the `label` strings in the NODE map. Do not change NW/NH/COLW/ROWH/PAD, the `wrap()` function, or the edge rendering.
    Un-fixme and make live the two FLOW-01/token assertions in flow-graph-canvas.spec.ts that this task satisfies: the `var(--accent-` presence + no-`#db2777` assertion (token unification). Leave FLOW-03 export/fit assertions for Task 2 and FLOW-04 useViewport assertion for Plan 03.
  </action>
  <verify>
    <automated>npx playwright test --project=phase24-unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hasExplicitPositions" src/components/sop/flow/FlowGraphCanvas.tsx` ≥ 2 (definition + call site)
    - `grep -c "layoutFromPositions" src/components/sop/flow/FlowGraphCanvas.tsx` ≥ 2
    - `FlowGraphCanvas.tsx` contains `var(--accent-step` and contains NO `#db2777` and NO `#ea580c`
    - existing `phase24-unit` derivation tests still pass (auto-layout fallback unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Explicit-position graphs render at authored coordinates; derived graphs keep auto-layout; node colours unified to --accent-* tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FLOW-03 — real zoom-to-fit + Export PNG; remove PREVIEW pill</name>
  <files>src/components/sop/flow/FlowGraphCanvas.tsx, src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts</files>
  <read_first>
    - src/components/sop/flow/FlowGraphCanvas.tsx (post-Task-1 state — scrollRef line 104, toolbar lines 110-123, Fit stub onClick scrollTo line 118, PREVIEW pill line 115, svg element line 126)
    - src/lib/offline/photo-compress.ts (canonical canvas pattern: loadImage helper lines ~48-55, canvasToBlob promise wrapper lines ~72-83 — reuse the Promise + Image.onload/onerror shape for exportPng)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md (§ FlowGraphCanvas — fitToView + exportPng implementation spec; inlineCssVars getComputedStyle clone)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md (§ Pattern 2 viewBox fit, § Pattern 3 SVG→PNG export, Pitfall 1 CSS vars don't resolve in serialised SVG)
  </read_first>
  <behavior>
    - fitToView reads layout width/height (already in useMemo) and the scroll container's clientWidth/clientHeight, computes scale = Math.min(cw/gw, ch/gh, 1) (never scales above 100%), and sets the SVG viewBox/width/height accordingly
    - exportPng clones the SVG, inlines getComputedStyle fill/stroke onto the clone (so var(--accent-*) resolve to concrete colours), serialises via XMLSerializer, rasterises to a canvas at devicePixelRatio, and triggers a download of image/png via canvas.toBlob
    - The Fit button onClick references fitToView (not scrollTo); an Export PNG button onClick references exportPng
    - The PREVIEW pill is absent from FlowGraphCanvas.tsx
  </behavior>
  <action>
    Add `svgRef = useRef<SVGSVGElement>(null)` alongside the existing `scrollRef`, and attach it to the `<svg>` element (line 126). Import `useCallback` (add to the existing `react` import). Implement `fitToView` as a `useCallback` depending on `[width, height]`: read `svgRef.current` and `scrollRef.current`; compute `scale = Math.min(container.clientWidth / width, container.clientHeight / height, 1)`; set `svg.setAttribute('viewBox', '0 0 ' + (width/scale) + ' ' + (height/scale))`, and set width/height attributes to the container client dimensions. Implement `exportPng` as an async `useCallback`: clone svgRef.current; walk live vs clone elements in parallel (querySelectorAll('*')) and for each set `cloneEl.style.fill = getComputedStyle(liveEl).fill` and `.stroke` when not 'none' (CSS-var inline per Pitfall 1); serialise the clone with `new XMLSerializer().serializeToString(...)`; create an object URL blob (`image/svg+xml;charset=utf-8`); draw onto an off-screen canvas sized `w*dpr × h*dpr` with `ctx.scale(dpr,dpr)` via an Image onload (reuse photo-compress loadImage idiom); revoke the SVG url; `canvas.toBlob(..., 'image/png')` → create an `<a download="procedure-flow.png">`, click, revoke. Wire the existing Fit button's onClick to `fitToView` (remove the scrollTo stub). Add an "Export PNG" button in the same toolbar wired to `void exportPng()` using the existing `evidence-btn !min-h-[30px] text-[11px]` class. DELETE the PREVIEW pill `<span>` at line 115 entirely. Do not place any code in <action> prose — describe behaviour; the executor writes the implementation in the file. No new npm imports.
    Un-fixme and make live the FLOW-03 assertions in flow-graph-canvas.spec.ts: fitToView defined + wired to onClick (not scrollTo), exportPng defined + calls canvas.toBlob + wired to an Export button onClick.
  </action>
  <verify>
    <automated>npx playwright test --project=phase24-stubs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `FlowGraphCanvas.tsx` contains `fitToView` wired as `onClick={fitToView}` (or `onClick={() => fitToView()}`) and contains NO `scrollTo(`
    - `FlowGraphCanvas.tsx` contains `exportPng`, `canvas.toBlob`, `XMLSerializer`, and `getComputedStyle`; an Export button references `exportPng` in onClick
    - `grep -c PREVIEW src/components/sop/flow/FlowGraphCanvas.tsx` returns 0
    - `flow-graph-canvas.spec.ts` FLOW-03 assertions are live (no longer test.fixme) and pass under `phase24-stubs`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Fit performs viewBox zoom-to-fit; Export PNG downloads a colour-correct PNG; PREVIEW pill gone from the canvas; FLOW-03 contract tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered SVG → exported PNG (client-only) | pure client-side rasterisation; no network, no untrusted input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information Disclosure | exportPng (SVG→canvas→PNG) | accept | Renders only the SOP's own flow graph the user is already authorised to view; no cross-origin image is drawn (canvas not tainted); output is a local download, no exfiltration path |
| T-24-04 | Denial of Service | layoutFromPositions on adversarial positions | accept | Positions already validated by FlowGraphSchema (numbers) + 256KB graph cap at write time; bounding-box math is O(n) over a capped node set |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs (native Canvas/XMLSerializer + existing deps only) — slopcheck N/A |
</threat_model>

<verification>
- `npx playwright test --project=phase24-unit` green (auto-layout fallback preserved)
- `npx playwright test --project=phase24-stubs` green (FLOW-01 token + FLOW-03 fit/export assertions live)
- `npx tsc --noEmit` green
- PREVIEW absent from FlowGraphCanvas.tsx (no-preview-pill guard advances; full green when Plan 03 removes the other two)
</verification>

<success_criteria>
- Explicit positions honoured; derived auto-layout intact
- Fit = real viewBox zoom-to-fit; Export PNG downloads colour-correct PNG offline
- Node colours unified to --accent-* tokens
- No new dependencies; bundle impact deferred to Plan 03's gate check
</success_criteria>

<output>
Create `.planning/phases/24-procedure-flow-spatial-node-graph/24-02-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols (in src/components/sop/flow/FlowGraphCanvas.tsx):
- `hasExplicitPositions(graph)` (function) — discriminant: any node.position.x !== 0
- `layoutFromPositions(graph)` (function) — honours authored coordinates verbatim
- `fitToView` (useCallback) — real viewBox-based zoom-to-fit (replaces scrollTo stub)
- `exportPng` (async useCallback) — SVG → CSS-var-inlined clone → canvas → PNG download
- `svgRef` (ref) — attached to the rendered `<svg>` for fit/export

Modified:
- `NODE` colour map — unified onto `var(--accent-*)` tokens matching FlowTab.TYPE_COLORS (divergent `#db2777`/`#ea580c` removed)
- `layout()` — branches to layoutFromPositions when explicit positions present; auto-layout fallback otherwise
- PREVIEW pill removed from the canvas toolbar
- `flow-graph-canvas.spec.ts` — FLOW-01 token + FLOW-03 fit/export assertions flipped live

New UI affordance: "Export PNG" toolbar button alongside "Fit"
</artifacts_produced>
