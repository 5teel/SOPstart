---
phase: 24-procedure-flow-spatial-node-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - src/lib/validators/flow-graph.ts
  - src/lib/validators/__tests__/flow-graph-schema.spec.ts
  - src/lib/sop/__tests__/flow-graph-derivation.test.ts
  - tests/lint/no-preview-pill.spec.ts
  - src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts
  - .planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md
autonomous: true
requirements: [FLOW-02, FLOW-05]
must_haves:
  truths:
    - "A registered playwright project (phase24-stubs) runs the new source-contract + lint specs"
    - "FlowGraphSchema accepts non-UUID node ids and edge from/to (z.string().min(1)) while stepId still requires a UUID"
    - "The FLOW-05 round-trip risk (FlowGraphField reachability + autosave clobber) is recorded with code-confirmed findings before any FLOW-05 implementation"
    - "FLOW-02 derivation coverage is audited; InspectBlock/ZoneBlock and cross-section branch cases are tested"
  artifacts:
    - path: "src/lib/validators/flow-graph.ts"
      provides: "Relaxed FlowGraphSchema (min(1) ids, uuid stepId)"
      contains: "z.string().min(1)"
    - path: "src/lib/validators/__tests__/flow-graph-schema.spec.ts"
      provides: "Schema relaxation contract tests"
    - path: "src/lib/sop/__tests__/flow-graph-derivation.test.ts"
      provides: "Extended FLOW-02 coverage (inspect/zone/cross-section)"
    - path: "tests/lint/no-preview-pill.spec.ts"
      provides: "Lint guard asserting PREVIEW absent from flow components (vacuous-pass at plan-01 head)"
    - path: "src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts"
      provides: "Source-contract stubs for FLOW-01/03/04 wiring (test.fixme until Plan 02/03)"
    - path: "playwright.config.ts"
      provides: "phase24-stubs project registration"
      contains: "phase24-stubs"
    - path: ".planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md"
      provides: "FLOW-05 reachability + autosave findings"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/lint/no-preview-pill.spec.ts"
      via: "phase24-stubs testMatch regex"
      pattern: "no-preview-pill"
    - from: "src/lib/validators/flow-graph.ts"
      to: "FlowTab + updateSopFlowGraph"
      via: "FlowGraphSchema.safeParse"
      pattern: "z\\.string\\(\\)\\.min\\(1\\)"
---

<objective>
Lay the Wave-0 test surface and the two zero-risk foundations the renderer/wiring plans (24-02, 24-03) depend on: (1) relax FlowGraphSchema so derived non-UUID node ids validate (FLOW-05 schema half); (2) register a `phase24-stubs` playwright project and create the new spec/lint files so later plans turn fixmes green instead of inventing test infrastructure; (3) audit FLOW-02 derivation coverage and close the two known gaps (InspectBlock/ZoneBlock, cross-section branch); (4) record the FLOW-05 round-trip investigation (FlowGraphField reachability in the 21.6 builder + autosave clobber risk) as code-confirmed findings so 24-03 implements against facts, not assumptions.

Purpose: Establish the Nyquist test floor and de-risk FLOW-05 before any UI change touches the bundle-gated `/sops/[sopId]` route.
Output: Relaxed schema, registered test project, 3 new spec files + 1 extended test file, 1 investigation doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax FlowGraphSchema + schema contract tests</name>
  <files>src/lib/validators/flow-graph.ts, src/lib/validators/__tests__/flow-graph-schema.spec.ts</files>
  <read_first>
    - src/lib/validators/flow-graph.ts (the 22-line schema being edited — current state)
    - src/lib/sop/flow-graph.ts (confirm non-step node id assignment: props.junctionId || props.id || `${section.id}:${index}` — none guaranteed UUID)
    - src/components/sop/tabs/FlowTab.tsx (confirm stepMap keys on step.id via node.id, lines 210-231 — the relaxation must not break this)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md (§ src/lib/validators/flow-graph.ts — exact 3-line change spec)
  </read_first>
  <action>
    In src/lib/validators/flow-graph.ts, change exactly three field validators: node `id` from `z.string().uuid()` to `z.string().min(1)` (line 6); edge `from` from `z.string().uuid()` to `z.string().min(1)` (line 13); edge `to` from `z.string().uuid()` to `z.string().min(1)` (line 14). Leave `stepId: z.string().uuid().optional()` UNCHANGED (line 10) — it always references sop_steps.id. Add a one-line comment on the id field citing the rationale: derived non-step nodes use junctionId/props.id which are not guaranteed UUIDs (FLOW-05). Do NOT change `version`, `type`, `label`, `position`, `blockProps`, `kind`, or edge `label`.
    Create src/lib/validators/__tests__/flow-graph-schema.spec.ts importing `FlowGraphSchema` from `../flow-graph`. Assert: (a) a graph with a node id of `'section-abc:0'` (non-UUID, min length ≥1) parses successfully; (b) an edge with `from`/`to` set to non-UUID strings parses successfully; (c) a node with `id: ''` (empty string) FAILS parse; (d) a node with a non-UUID `stepId` (e.g. `'not-a-uuid'`) FAILS parse while a valid UUID stepId passes. Use `@playwright/test` `test`/`expect` (match the existing flow-graph-derivation.test.ts import style). Do NOT register this file's project here — Task 3 owns playwright.config.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validators/flow-graph.ts` contains `z.string().min(1)` for node id and edge from/to, and `grep -c "z.string().uuid()" src/lib/validators/flow-graph.ts` returns 1 (only stepId remains uuid)
    - `flow-graph-schema.spec.ts` exists and contains four assertions: non-UUID id passes, non-UUID edge endpoints pass, empty-string id fails, non-UUID stepId fails
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Schema relaxed to the FLOW-05 contract; schema spec exists (will run once Task 3 registers the project).</done>
</task>

<task type="auto">
  <name>Task 2: Audit + extend FLOW-02 derivation coverage</name>
  <files>src/lib/sop/__tests__/flow-graph-derivation.test.ts</files>
  <read_first>
    - src/lib/sop/__tests__/flow-graph-derivation.test.ts (existing 216-line suite — the U()/step()/sop() helper structure to reuse; do NOT add helper files)
    - src/lib/sop/flow-graph.ts (PUCK_TYPE_TO_NODE map for inspect/zone; DecisionBlock.options nextStepId branch logic; edge.label 60-char truncation)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md (§ FLOW-02 Coverage Analysis — the 3 missing axes: inspect/zone, edge-label truncation, cross-section branch target)
  </read_first>
  <action>
    Append (do not restructure) new test cases to the existing flow-graph-derivation.test.ts using the file's existing `U()` uuid factory, `step()` helper, and `sop()` wrapper. Add: (a) a test that an InspectBlock and a ZoneBlock in layout_data.content become nodes with `type: 'inspect'` and `type: 'zone'` respectively; (b) a test that a DecisionBlock option label longer than 60 chars produces an edge whose `label` is ≤60 chars (truncation behaviour as currently implemented in flow-graph.ts — assert against the actual cap, do not change source); (c) a test that a DecisionBlock in section A with an option resolving to a `nextStepId` that is a step UUID in section B produces a branch edge whose `to` equals that section-B step id. If any of these reveals a real derivation bug, STOP and surface it as a finding in the summary rather than editing flow-graph.ts in this plan (FLOW-02 is verify-only per CONTEXT). All existing tests must remain unchanged and passing.
  </action>
  <verify>
    <automated>npx playwright test --project=phase24-unit</automated>
  </verify>
  <acceptance_criteria>
    - `flow-graph-derivation.test.ts` contains new tests matching `/inspect|zone/i`, an edge-label truncation test, and a cross-section branch-target test
    - `npx playwright test --project=phase24-unit` passes (all original + new cases green)
    - No edits to `src/lib/sop/flow-graph.ts` (FLOW-02 is verify-only — `git diff --stat src/lib/sop/flow-graph.ts` shows no change)
  </acceptance_criteria>
  <done>FLOW-02 coverage axes closed; derivation confirmed shipped and correct (or a bug surfaced as a finding).</done>
</task>

<task type="auto">
  <name>Task 3: Register phase24-stubs project + create stub specs + FLOW-05 investigation doc</name>
  <files>playwright.config.ts, tests/lint/no-preview-pill.spec.ts, src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts, .planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md</files>
  <read_first>
    - playwright.config.ts (existing phase24-unit block ~lines 74-80 and phase21.6-stubs block ~lines 119-126 — copy the stubs registration shape exactly: testDir '.', testMatch regex, CLAUDE.md 2026-05-25 comment)
    - tests/lint/no-bulk-verify-ui.spec.ts (grep-absence lint-guard analog — readFileSync + expect(...).not.toMatch pattern)
    - src/components/sop/flow/FlowGraphCanvas.tsx (current state — fitToView/exportPng do NOT yet exist; PREVIEW pill at line 115; scrollTo stub at line 118)
    - src/components/sop/tabs/FlowTab.tsx (current state — useState('list') at line 190; no useViewport import yet; "Graph (preview)" at line 24)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (lines 73-77 autosave wiring + lines 432-458 sanitizedInitial root.props.flowGraph + line 535 ui rightSideBarVisible:false)
    - src/hooks/useBuilderAutosave.ts (confirm it writes only layout_data per section_id, never root.props.flowGraph)
    - src/lib/builder/puck-config.tsx (lines 203-215 — flowGraph is a root.fields custom field rendered in the right sidebar)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md (§ playwright.config.ts, § new test files — exact regex + spec shape)
  </read_first>
  <action>
    1. playwright.config.ts: append a new project after the phase21.6-stubs block: name `phase24-stubs`, `testDir: '.'`, `testMatch: /(no-preview-pill|flow-graph-canvas|flow-graph-schema)\.(test|spec)\.ts$/`, with a comment citing CLAUDE.md 2026-05-25 (unregistered specs never run). Do NOT modify the existing phase24-unit project. Validate after editing with `npx playwright test --list --project=phase24-stubs` and confirm all three filenames appear.
    2. tests/lint/no-preview-pill.spec.ts: copy the no-bulk-verify-ui.spec.ts grep-absence shape. Read each of the three files (`src/components/sop/flow/FlowGraphCanvas.tsx`, `src/components/sop/tabs/FlowTab.tsx`, `src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx`) via readFileSync and assert content does NOT match `/PREVIEW/`. Mark this test `test.fixme(...)` for now (PREVIEW still present at plan-01 head — Plans 02/03 remove it and flip these to live). Add a comment that 24-02/24-03 must un-fixme this.
    3. src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts: source-contract stubs using readFileSync + regex against the live files. Write four assertions as `test.fixme(...)` (they go red until 24-02/24-03 land): (a) FlowGraphCanvas.tsx defines `fitToView` AND wires it to a Fit button onClick (assert `onClick={fitToView}` or `onClick={() => fitToView()}` present, NOT the `scrollTo` stub); (b) FlowGraphCanvas.tsx defines `exportPng` and calls `canvas.toBlob` AND an Export button wires `onClick={exportPng}`; (c) FlowTab.tsx imports `useViewport` from `@/hooks/useViewport` AND seeds `useState<'list' | 'graph'>('list')` (SSR-safe initial state) AND has a `useEffect` that sets view to `'graph'` when viewport === 'desktop'; (d) FlowGraphCanvas.tsx node fills reference `var(--accent-` (token unification) and contain NO `#db2777` (the old divergent decision hex). Per the CLAUDE.md 2026-06-05 learning, assert the HANDLER wiring (onClick references the function), not merely that the function name string appears.
    4. 24-FLOW05-INVESTIGATION.md: record the code-confirmed findings (already gathered during planning — re-verify each against the files in read_first and cite line numbers): (i) FlowGraphField is a `root.fields.flowGraph` custom field in puck-config.tsx that Puck renders in the RIGHT sidebar; BuilderClient passes `ui={{ rightSideBarVisible: false }}` (line 535), so the field is CURRENTLY UNREACHABLE in the 21.6 builder UI — FLOW-05 implementation (Plan 03) MUST re-surface the editor outside the suppressed sidebar (e.g. a portaled modal/panel like BuilderFlowButton, mounting FlowGraphEditor with initialGraph+sopId directly — it needs no Puck context). (ii) Autosave (useBuilderAutosave) writes ONLY `layout_data` keyed per `section_id`; it never serialises `root.props.flowGraph` to `sops.flow_graph`. `updateSopFlowGraph` (called by FlowGraphEditor.handleSave) is the sole writer of `sops.flow_graph`. Therefore autosave CANNOT clobber a saved flow graph — A2 resolved, no defensive change needed. (iii) hasExplicitPositions discriminant (any node.position.x !== 0) is sound: derived nodes are all x=0; FlowGraphEditor.handleAddNode seeds x=0 but drag snaps x off 0 — the only edge case (single node never dragged) is acceptable. Conclude with a one-line directive for Plan 03: "Re-surface FlowGraphEditor via a builder entry point that does not rely on Puck's suppressed right sidebar."
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase24-stubs</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase24-stubs` lists `no-preview-pill`, `flow-graph-canvas`, and `flow-graph-schema` spec files (per CLAUDE.md 2026-05-25 — confirm discovery, not just file existence)
    - `playwright.config.ts` contains `phase24-stubs` and the testMatch regex `/(no-preview-pill|flow-graph-canvas|flow-graph-schema)\.(test|spec)\.ts$/`; the existing `phase24-unit` block is unchanged
    - `no-preview-pill.spec.ts` and `flow-graph-canvas.spec.ts` exist and use `test.fixme` (not live) with comments instructing 24-02/24-03 to un-fixme
    - `24-FLOW05-INVESTIGATION.md` states FlowGraphField is unreachable under `rightSideBarVisible: false` with a BuilderClient.tsx line citation, states autosave does not clobber flow_graph with a useBuilderAutosave.ts citation, and gives Plan 03 the re-surface directive
    - `npx playwright test --project=phase24-stubs` exits 0 (fixme tests are skipped, schema spec from Task 1 passes)
  </acceptance_criteria>
  <done>Test project registered and discoverable; stub specs in place as fixmes; FLOW-05 implementation de-risked with code-confirmed findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder client → updateSopFlowGraph server action | admin-authored flow graph crosses to the DB; already role-gated + Zod-validated + 256KB capped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | FlowGraphSchema relaxation (id/from/to → min(1)) | mitigate | Relaxation keeps min(1) (non-empty) + 256KB size cap in updateSopFlowGraph; stepId stays uuid; schema spec asserts empty-string id rejected |
| T-24-02 | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new deps, native browser APIs + existing libs only) — slopcheck N/A |
</threat_model>

<verification>
- `npx tsc --noEmit` green
- `npx playwright test --project=phase24-unit` green (FLOW-02 + extended coverage)
- `npx playwright test --project=phase24-stubs` green (schema spec passes; fixme stubs skipped)
- `npx playwright test --list --project=phase24-stubs` shows all 3 stub filenames
</verification>

<success_criteria>
- FlowGraphSchema accepts derived non-UUID ids; stepId still UUID-gated; empty-string id rejected
- phase24-stubs project registered and discoverable
- FLOW-02 coverage gaps (inspect/zone, cross-section, label truncation) tested and green
- FLOW-05 investigation doc records the unreachable-field gap (with line citation) and the no-clobber autosave finding, directing Plan 03
</success_criteria>

<output>
Create `.planning/phases/24-procedure-flow-spatial-node-graph/24-01-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files / symbols:
- `src/lib/validators/__tests__/flow-graph-schema.spec.ts` (file) — schema relaxation contract tests
- `tests/lint/no-preview-pill.spec.ts` (file) — PREVIEW-absence lint guard (test.fixme until Plans 02/03 remove PREVIEW)
- `src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts` (file) — FLOW-01/03/04 source-contract stubs (test.fixme)
- `.planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md` (file) — FLOW-05 reachability + autosave findings
- `phase24-stubs` (playwright project, in playwright.config.ts) — runs no-preview-pill / flow-graph-canvas / flow-graph-schema specs

Modified symbols:
- `FlowGraphSchema` (src/lib/validators/flow-graph.ts) — node `id`, edge `from`/`to` relaxed to `z.string().min(1)`; `stepId` stays `z.string().uuid()`
- `flow-graph-derivation.test.ts` — extended with InspectBlock/ZoneBlock, cross-section branch, and edge-label truncation cases
</artifacts_produced>
