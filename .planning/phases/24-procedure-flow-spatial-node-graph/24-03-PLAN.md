---
phase: 24-procedure-flow-spatial-node-graph
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/components/sop/tabs/FlowTab.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
  - src/lib/journeys/journeys.ts
  - tests/lint/no-preview-pill.spec.ts
  - src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts
  - .planning/phases/24-procedure-flow-spatial-node-graph/24-HUMAN-UAT.md
autonomous: false
requirements: [FLOW-04, FLOW-05]
must_haves:
  truths:
    - "On desktop (≥1024px) the Flow tab defaults to the graph view; on mobile and on SSR it defaults to the list view"
    - "The List/Graph toggle has no '(preview)' label and the PREVIEW pill is absent from all flow surfaces"
    - "The Flow tab default-view behaviour change is reflected in journeys.ts (/pathways) in this commit"
    - "An admin can reach the FlowGraphEditor in the builder (re-surfaced outside the suppressed Puck right sidebar), author positions, save, and see the explicit graph render in the Flow tab"
    - "First-Load-JS for /sops/[sopId]/page stays within 1104 KB ±2 KB"
  artifacts:
    - path: "src/components/sop/tabs/FlowTab.tsx"
      provides: "SSR-safe desktop-default graph view via useViewport; toggle label without preview"
      contains: "useViewport"
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx"
      provides: "Re-surfaced FlowGraphEditor entry point (modal/panel outside Puck sidebar)"
    - path: "src/lib/journeys/journeys.ts"
      provides: "Updated /sops/[sopId] flow-tab detail reflecting graph default"
    - path: ".planning/phases/24-procedure-flow-spatial-node-graph/24-HUMAN-UAT.md"
      provides: "FLOW-05 round-trip + FLOW-03 export + FLOW-04 viewport runbook"
  key_links:
    - from: "FlowTab"
      to: "useViewport"
      via: "useEffect reconcile (SSR-safe seed 'list')"
      pattern: "viewport === 'desktop'"
    - from: "BuilderClient builder UI"
      to: "FlowGraphEditor"
      via: "re-surfaced entry point (portaled panel/modal, no Puck context)"
      pattern: "FlowGraphEditor|FlowGraphField"
---

<objective>
Ship the user-facing behaviour: (FLOW-04) make the Flow tab default to the graph on desktop while keeping list as the mobile/SSR default and an always-available toggle, using the SSR-safe useViewport reconcile pattern; remove the last two PREVIEW occurrences (FlowTab toggle label + BuilderFlowButton modal header); (FLOW-05) re-surface the FlowGraphEditor in the builder so admins can actually author explicit positions — the Plan-01 investigation confirmed the field is unreachable under `rightSideBarVisible: false`. Update journeys.ts for the default-view flow change, run the bundle gate, flip the FLOW-04 source-contract stub and the no-preview-pill guard fully live, and hand off the carried Phase 12.5 round-trip as a real-browser human-UAT runbook (grep-GREEN is not "done" for this per the 2026-06-05 learning).

Purpose: Deliver the production Flow experience and close the carried FLOW-05 UAT gap with a runnable browser checklist.
Output: Desktop-default graph view; FlowGraphEditor reachable; journeys.ts + UAT runbook updated; bundle gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-01-SUMMARY.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md
@.planning/phases/24-procedure-flow-spatial-node-graph/24-02-SUMMARY.md
@src/hooks/useViewport.ts
@src/lib/builder/flow-graph-field.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FLOW-04 — desktop-default graph view (SSR-safe) + drop preview label</name>
  <files>src/components/sop/tabs/FlowTab.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx, src/components/sop/flow/__tests__/flow-graph-canvas.spec.ts, tests/lint/no-preview-pill.spec.ts</files>
  <read_first>
    - src/components/sop/tabs/FlowTab.tsx (ViewToggle line 24 "Graph (preview)"; useState('list') line 190; existing useRef/useMemo/useState import line 2)
    - src/hooks/useViewport.ts (full impl — returns 'mobile'|'desktop', seeds 'mobile', matchMedia 1024px reconcile in useEffect; the SSR-safe analog to copy)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx (PREVIEW pill line 75)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-PATTERNS.md (§ FlowTab — desktop-default state pattern; ViewToggle label update)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md (§ Pattern 4 SSR-safe default; Pitfall 2 hydration mismatch #418)
    - CLAUDE.md (2026-06-08 SSR navigator/window-at-module-load learning — never derive first render from window)
  </read_first>
  <behavior>
    - FlowTab seeds `useState<'list' | 'graph'>('list')` (server + first client render = list, SSR-safe, no hydration mismatch)
    - After hydration, a useEffect upgrades view to 'graph' when useViewport() === 'desktop'; mobile stays 'list'
    - The user can still toggle List/Graph manually in either viewport (manual toggle is not overridden by the effect after initial reconcile)
    - The toggle label reads 'Graph' (no '(preview)'); PREVIEW pill absent from BuilderFlowButton header
  </behavior>
  <action>
    In FlowTab.tsx: add `useEffect` to the existing react import (it currently imports useRef/useMemo/useState only) and `import { useViewport } from '@/hooks/useViewport'`. Keep `const [view, setView] = useState<'list' | 'graph'>('list')` exactly as-is (SSR-safe seed — do NOT seed from window or useViewport's return). Add `const viewport = useViewport()` and a `useEffect(() => { if (viewport === 'desktop') setView('graph') }, [viewport])`. Add a comment citing CLAUDE.md 2026-06-08 (never seed first render from window → hydration #418). Change the ViewToggle label expression at line 24 from `'Graph (preview)'` to `'Graph'`. Do not touch the stepMap/entries useMemo, the explicit-vs-derived resolution block, or the StepCard tree.
    In BuilderFlowButton.tsx: delete the `<span className="pill">PREVIEW</span>` at line 75 (keep the "Procedure flow" h2).
    Flip live the FLOW-04 source-contract assertion in flow-graph-canvas.spec.ts (useViewport imported in FlowTab, initial state 'list', useEffect sets 'graph' on desktop). Flip the `no-preview-pill.spec.ts` guard from test.fixme to a LIVE test now that all three files (FlowGraphCanvas from Plan 02, FlowTab + BuilderFlowButton here) are PREVIEW-free — the guard asserts `/PREVIEW/` absent from all three.
  </action>
  <verify>
    <automated>npx playwright test --project=phase24-stubs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `FlowTab.tsx` contains `useViewport`, retains `useState<'list' | 'graph'>('list')`, and has a `useEffect` with `viewport === 'desktop'` setting view to 'graph'; FlowTab.tsx contains NO `window.innerWidth` and NO useState initialiser reading window
    - `grep -rc PREVIEW src/components/sop/flow/FlowGraphCanvas.tsx src/components/sop/tabs/FlowTab.tsx "src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx"` returns 0 across all three
    - `no-preview-pill.spec.ts` is LIVE (not test.fixme) and passes; flow-graph-canvas.spec.ts FLOW-04 assertion is live and passes
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Desktop defaults to graph, mobile/SSR to list, manual toggle works, no hydration mismatch; all PREVIEW labels removed; lint guard fully live and green.</done>
</task>

<task type="auto">
  <name>Task 2: FLOW-05 — re-surface FlowGraphEditor in the builder + journeys.ts update</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx, src/lib/journeys/journeys.ts</files>
  <read_first>
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-FLOW05-INVESTIGATION.md (the Plan-01 directive: FlowGraphField is unreachable under rightSideBarVisible:false; re-surface FlowGraphEditor via an entry point that needs no Puck context)
    - src/lib/builder/flow-graph-field.tsx (FlowGraphEditor takes initialGraph + sopId props directly and writes via updateSopFlowGraph — it does NOT need Puck context; FlowGraphField is the Puck wrapper that reads value via root.props)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx (the portaled-modal pattern to mirror: a header button + createPortal modal mounting FlowGraphCanvas; use the same shell for the editor)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (header region lines 460-505 where BuilderFlowButton / RerunReviewerButton mount; initialSop.flow_graph available; sopId available; ui rightSideBarVisible:false at line 535)
    - src/lib/validators/flow-graph.ts (relaxed schema from Plan 01 — derived graphs now safeParse, so the editor can seed from initialSop.flow_graph)
    - src/lib/journeys/journeys.ts (line 112 — the /sops/[sopId] 'detail' screen entry whose detail text mentions the flow tab)
    - CLAUDE.md (§ Pathways Map Maintenance — journeys.ts same-commit rule; 2026-06-08 "dispatch from outside Puck" learning for components rendered outside <Puck>)
  </read_first>
  <action>
    Re-surface the FlowGraphEditor so admins can author explicit positions without the suppressed Puck right sidebar. Implement an "Edit flow" entry point in the builder header (alongside the existing BuilderFlowButton "Flow" preview button) that opens a portaled modal/panel mounting `FlowGraphEditor` directly with `initialGraph` (seeded from `FlowGraphSchema.safeParse(initialSop.flow_graph)` → on success the explicit graph, else `deriveFlowGraph(sop)` so the admin starts from the derived layout rather than an empty canvas) and `sopId`. Because FlowGraphEditor calls `updateSopFlowGraph` directly and uses its own internal SVG/toolbar (it does NOT consume Puck context), mount it OUTSIDE `<Puck>` — mirror BuilderFlowButton's createPortal + mounted-guard + Escape-to-close shell (avoid the 2026-06-08 useGetPuck-outside-Puck crash by NOT calling any Puck hook in this entry point). Prefer extending/adding a sibling component file if BuilderFlowButton's single-responsibility is preview-only; reuse its modal shell idiom. The existing FlowGraphField/root.fields wiring in puck-config.tsx may remain (harmless, just unreachable) — do not remove it; the new entry point is the production path. Do NOT change `ui={{ leftSideBarVisible: false, rightSideBarVisible: false }}` (that is a deliberate 21.6 invariant) — re-surface the editor independently of the sidebar.
    Update src/lib/journeys/journeys.ts: amend the `/sops/[sopId]` screen detail (line 112) so the flow-tab description reflects the new default — e.g. note that the flow tab now defaults to a spatial graph on desktop with a List/Graph toggle (list on mobile). Keep the `route` value real (`/sops/[sopId]`). Do not add new routes (none are introduced). This satisfies the CLAUDE.md same-commit pathways rule for a flow-behaviour change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase24-stubs</automated>
  </verify>
  <acceptance_criteria>
    - BuilderClient.tsx (or a sibling builder component it mounts) references `FlowGraphEditor` (or `FlowGraphField`) via a re-surfaced entry point that is NOT inside the suppressed Puck right sidebar, and seeds initialGraph from explicit-or-derived graph
    - The entry point calls no Puck hook (no `useGetPuck`/`usePuck`) — avoids the 2026-06-08 outside-Puck crash; `grep` shows the editor mount uses createPortal/modal shell, not a Puck override
    - `ui={{ leftSideBarVisible: false, rightSideBarVisible: false }}` remains in BuilderClient.tsx (21.6 invariant preserved)
    - `journeys.ts` line for `/sops/[sopId]` detail mentions the graph/list flow-tab default (flow change reflected per CLAUDE.md rule); no new route added
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>FlowGraphEditor is reachable in the builder, seeded from the current graph, persisting via updateSopFlowGraph; pathways map updated for the flow change.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-UAT — FLOW-05 round-trip, FLOW-03 export, FLOW-04 viewport (real browser)</name>
  <files>.planning/phases/24-procedure-flow-spatial-node-graph/24-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-RESEARCH.md (§ FLOW-05 Round-Trip Analysis — the 6-step round-trip checklist; § Manual-Only Verifications in 24-VALIDATION.md)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-VALIDATION.md (§ Manual-Only Verifications table)
    - CLAUDE.md (2026-06-05 source-contract learning — grep-GREEN is not "done"; this carried Phase 12.5 item MUST run in a real authenticated browser)
    - Memory: SOPstart UAT happens on sopstart.com post-deploy (Railway), not localhost
  </read_first>
  <what-built>
    Automated this phase: relaxed schema (24-01), production FlowGraphCanvas with explicit-position layout + viewBox Fit + Export PNG + unified accent tokens (24-02), desktop-default graph view + re-surfaced FlowGraphEditor (24-03). First write the runbook file `24-HUMAN-UAT.md`, then pause for Simon to run it on the deployed app.
  </what-built>
  <action>
    Before pausing, WRITE `.planning/phases/24-procedure-flow-spatial-node-graph/24-HUMAN-UAT.md` containing the numbered checklist below so Simon (or a future session) can execute it post-deploy. Then present the checklist and pause for the resume signal.
  </action>
  <how-to-verify>
    Run on the deployed app (sopstart.com), authenticated as an admin, after this phase deploys:
    1. **FLOW-05 round-trip (carried Phase 12.5 item):** Open any SOP with Decision/Measurement blocks in the admin builder → click the new "Edit flow" entry point → the FlowGraphEditor opens (NOT hidden by the suppressed sidebar) seeded with the derived/explicit graph → add or drag a node to a new position → "Save to SOP" → reload the builder → confirm the position persisted → open `/sops/[sopId]` Flow tab → confirm the graph renders at the AUTHORED positions (not auto-layout depth columns) → toggle List/Graph and confirm both render.
    2. **FLOW-03 export:** On the Flow tab graph (or builder preview), click **Fit** → the whole graph fits the viewport in one click (not a scroll jump). Click **Export PNG** → a PNG downloads → open it → node colours match the on-screen --accent-* tokens (no all-black nodes — confirms CSS-var inlining), labels legible (system-font fallback acceptable for v1 per RESEARCH Open Q2).
    3. **FLOW-04 viewport default:** On a desktop browser ≥1024px wide, open `/sops/[sopId]` → the Flow tab shows the GRAPH by default. On a phone (or ≤1023px), it shows the LIST by default. The List/Graph toggle works both ways. Open the browser console → confirm NO React error #418 / hydration warning on load.
  </how-to-verify>
  <acceptance_criteria>
    - `24-HUMAN-UAT.md` exists with the three numbered scenarios above and is committed
    - (post-deploy, human-confirmed) round-trip persists + renders explicit positions; PNG exports with correct colours; desktop=graph / mobile=list with no hydration warning
  </acceptance_criteria>
  <resume-signal>Type "approved" once the round-trip, export, and viewport checks pass on the deployed app — or describe any issue (e.g. editor still hidden, PNG nodes black, hydration #418) for gap closure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Bundle gate — /sops/[sopId] First-Load-JS within 1104 KB ±2 KB</name>
  <files>(verification only — no source change unless a regression is found)</files>
  <read_first>
    - .bundle-baseline.json (route /sops/[sopId]/page baseline = 1104 KB; ±2 KB forward tolerance; DO NOT hand-edit)
    - scripts/check-bundle-size.ts (the authoritative gate; CLAUDE.md note: npm run build masks postbuild exit code — run the tsx script directly)
    - .planning/phases/24-procedure-flow-spatial-node-graph/24-CONTEXT.md (§ bundle gate constraint — FlowGraphCanvas is statically imported into FlowTab on this route)
  </read_first>
  <what-built>
    The phase added explicit-position layout, viewBox Fit, Export PNG (native Canvas/XMLSerializer), and useViewport wiring into FlowTab/FlowGraphCanvas — all statically imported on the bundle-gated /sops/[sopId] route. This task confirms the added code did not blow the ±2 KB tolerance.
  </what-built>
  <action>
    Run a production build and the bundle gate directly (not via `npm run build` alone, which masks the postbuild exit code per the Phase 15-04 learning). On Windows use: `npm run build; if ($?) { npx tsx scripts/check-bundle-size.ts }`. If the gate reports `/sops/[sopId]/page` within 1104 KB ±2 KB → green, done. If it exceeds tolerance: the added export-PNG/layout code must be trimmed or dynamic-imported (per CONTEXT locked decision: dynamic-import if it pushes past tolerance) — surface the overage and the dynamic-import option as a finding; do NOT re-baseline .bundle-baseline.json to absorb new feature weight (re-baseline is only for intentional global shifts, not to hide this phase's drift).
  </action>
  <how-to-verify>
    1. From `C:\Development\SOPstart` in PowerShell run: `npm run build; if ($?) { npx tsx scripts/check-bundle-size.ts }`
    2. Confirm the script exits 0 and reports `/sops/[sopId]/page` First-Load-JS within 1104 ±2 KB.
  </how-to-verify>
  <acceptance_criteria>
    - `npx tsx scripts/check-bundle-size.ts` exits 0 with `/sops/[sopId]/page` within 1104 KB ±2 KB
    - `.bundle-baseline.json` is unchanged (not re-baselined to absorb this phase's drift)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the bundle gate is green — or paste the overage so the export-PNG path can be dynamic-imported.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder admin → updateSopFlowGraph (re-surfaced editor) | authored positions cross to the DB via the existing role-gated + Zod-validated + 256KB-capped server action — unchanged write path |
| desktop/mobile viewport detection | client-only matchMedia reconcile; no server trust decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Elevation of Privilege | re-surfaced FlowGraphEditor write path | mitigate | Writes still flow only through updateSopFlowGraph (existing admin/safety_manager role gate + FlowGraphSchema validation + 256KB cap); the re-surfaced entry point adds no new write path, only re-exposes the existing one |
| T-24-06 | Tampering | hydration-mismatch via window-derived first render (FLOW-04) | mitigate | useState seeds 'list' (SSR-safe constant); viewport reconciled only in useEffect; flow-graph-canvas.spec asserts initial 'list' + no window read — closes #418 risk |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs (existing deps + native APIs only) — slopcheck N/A |
</threat_model>

<verification>
- `npx playwright test --project=phase24-stubs` green (FLOW-04 + no-preview-pill fully live)
- `npx playwright test --project=phase24-unit` green (no regression)
- `npx tsc --noEmit` green
- `npx tsx scripts/check-bundle-size.ts` green (1104 KB ±2 KB)
- Human-UAT: FLOW-05 round-trip + FLOW-03 export + FLOW-04 viewport pass on deployed app
- `/pathways` → "All screens" shows 0 not-mapped for /sops/[sopId] (no new route; detail text updated)
</verification>

<success_criteria>
- Desktop Flow tab defaults to graph; mobile/SSR to list; toggle works; no hydration #418
- All PREVIEW labels removed across the three flow surfaces
- FlowGraphEditor reachable in builder; round-trip persists authored positions to Flow tab
- journeys.ts reflects the flow-tab default change in this commit
- Bundle gate green within ±2 KB; baseline not re-edited for feature weight
- 24-HUMAN-UAT.md runbook committed for the carried Phase 12.5 item
</success_criteria>

<output>
Create `.planning/phases/24-procedure-flow-spatial-node-graph/24-03-SUMMARY.md` when done
</output>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New files / symbols:
- A re-surfaced builder "Edit flow" entry point mounting `FlowGraphEditor` outside the suppressed Puck right sidebar (new sibling component or extension of BuilderFlowButton.tsx; portaled modal, no Puck hook)
- `.planning/phases/24-procedure-flow-spatial-node-graph/24-HUMAN-UAT.md` (file) — FLOW-05 round-trip + FLOW-03 export + FLOW-04 viewport runbook

Modified symbols:
- `FlowTab` (src/components/sop/tabs/FlowTab.tsx) — `useViewport` import + `useEffect` desktop-default reconcile (SSR-safe seed 'list'); ViewToggle label `'Graph'` (preview dropped)
- `BuilderFlowButton.tsx` — PREVIEW pill removed
- `journeys.ts` — `/sops/[sopId]` detail updated to reflect graph/list flow-tab default
- `no-preview-pill.spec.ts` + `flow-graph-canvas.spec.ts` (FLOW-04) — flipped fully live

Behaviour delivered: desktop Flow tab defaults to graph, mobile/SSR to list; FlowGraphEditor reachable in builder; bundle gate held at 1104 KB ±2 KB
</artifacts_produced>
