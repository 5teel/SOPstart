---
phase: 27-ai-provider-settings-formal-spec-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-SPEC.md
  - .planning/REQUIREMENTS.md
  - playwright.config.ts
  - .env.local.example
  - src/lib/ai/__tests__/registry.test.ts
  - src/lib/ai/__tests__/llm-routing.test.ts
  - src/lib/parsers/__tests__/sop-title.test.ts
  - tests/phase27/ai-settings-org-scope.spec.ts
autonomous: true
requirements:
  - AIPS-REG-01
  - AIPS-REG-02
  - AIPS-SET-01
  - AIPS-SET-02
  - AIPS-PROMPT-01
  - AIPS-PARSE-01
  - AIPS-TITLE-01
  - AIPS-GAP-01
  - AIPS-GAP-02
  - AIPS-GAP-03

must_haves:
  truths:
    - "A reader opens 27-SPEC.md and understands the as-built AI provider arc (registry, provider adapter, org model overrides, grounding/parse hardening, title guard) with file:line citations — no new decisions, describes shipped state"
    - "`npx playwright test --project=phase27-unit` runs and passes — registry + llm-routing behavioral unit tests are green against the shipped code"
    - "`npx playwright test src/lib/parsers/__tests__/sop-title.test.ts --project=phase20-parsers` is green — title-guard fallback chain covered"
    - "The org-scope regression spec is discoverable under phase27-stubs and asserts cross-org write isolation BEHAVIORALLY (Org-A write → Org-B read-back → assert untouched), never via a source grep"
    - "`.env.local.example` documents OPENROUTER_API_KEY grouped with the other provider keys"
    - "Both new Playwright projects (phase27-unit, phase27-stubs) appear in `npx playwright test --list`"
  artifacts:
    - path: ".planning/phases/27-ai-provider-settings-formal-spec-pass/27-SPEC.md"
      provides: "As-built specification covering all 10 AIPS-* requirements with Current/Target/Acceptance per requirement"
      contains: "AIPS-PROMPT-01"
    - path: "src/lib/ai/__tests__/registry.test.ts"
      provides: "aiModel() default + env-override resolution, PROVIDER_ENV_KEYS completeness"
      min_lines: 30
    - path: "src/lib/ai/__tests__/llm-routing.test.ts"
      provides: "providerForModel() shape rules + extractJson() 3-stage fallback"
      min_lines: 30
    - path: "src/lib/parsers/__tests__/sop-title.test.ts"
      provides: "isPlaceholderTitle / titleFromFileName / ensureSopTitle fallback chain"
      min_lines: 25
    - path: "tests/phase27/ai-settings-org-scope.spec.ts"
      provides: "Runtime cross-org write-isolation regression for setAiModelSetting"
      min_lines: 40
    - path: "playwright.config.ts"
      provides: "phase27-unit + phase27-stubs project registrations"
      contains: "phase27-unit"
    - path: ".env.local.example"
      provides: "OPENROUTER_API_KEY documentation"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "playwright.config.ts"
      to: "src/lib/ai/__tests__"
      via: "phase27-unit testDir"
      pattern: "phase27-unit"
    - from: "playwright.config.ts"
      to: "tests/phase27"
      via: "phase27-stubs testMatch"
      pattern: "tests\\\\/phase27"
    - from: "src/lib/ai/__tests__/registry.test.ts"
      to: "src/lib/ai/registry.ts"
      via: "static @/ import of aiModel + AI_MODELS"
      pattern: "@/lib/ai/registry"
    - from: "tests/phase27/ai-settings-org-scope.spec.ts"
      to: "ai_model_settings"
      via: "runtime write via setAiModelSetting + service-role read-back"
      pattern: "ai_model_settings"
---

<objective>
Close out the AI provider-abstraction arc (registry, provider adapter, org model overrides, grounding/parse hardening, title guard) that shipped ad-hoc to master on 2026-07-06/07 with a formal spec pass: document the as-built architecture, backfill the missing automated tests, add the one missing env-doc line, and lock the org-scoped settings write path with a real behavioral regression test.

This is NOT new feature work. RESEARCH.md is a same-day direct code survey (2026-07-12) confirming AIPS-REG-01/02, AIPS-SET-01, AIPS-PROMPT-01, AIPS-PARSE-01, AIPS-TITLE-01 are already implemented and correct in prod, and that `setAiModelSetting` (`src/actions/ai-settings.ts:47-90`) ALREADY self-enforces org scope (org id sourced only from `parseJwtPayload`, never a client parameter; `.eq('organisation_id', ...)` on every write). Do NOT "fix" that code — AIPS-GAP-02 is test-only. The three real gaps are: no `OPENROUTER_API_KEY` in `.env.local.example` (AIPS-GAP-01), zero tests across the arc (AIPS-GAP-03), and no regression proving org isolation (AIPS-GAP-02).

Purpose: turn undocumented, untested-but-working prod code into documented, regression-locked code before the next AI feature builds on it.
Output: 27-SPEC.md + REQUIREMENTS.md traceability + 4 test files + 2 new Playwright projects + 1 env-doc line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-ai-provider-settings-formal-spec-pass/27-RESEARCH.md
@.planning/phases/27-ai-provider-settings-formal-spec-pass/27-PATTERNS.md
@.planning/phases/27-ai-provider-settings-formal-spec-pass/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 27-SPEC.md, register the two Playwright projects, add OPENROUTER_API_KEY doc</name>
  <files>.planning/phases/27-ai-provider-settings-formal-spec-pass/27-SPEC.md, playwright.config.ts, .env.local.example</files>
  <read_first>
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-RESEARCH.md (as-built architecture, System Architecture Diagram lines 51-85, exact file:line citations)
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-PATTERNS.md (27-SPEC.md structure from the 25-SPEC.md analog; exact playwright/env insertion content)
    - .planning/phases/25-department-first-class-entity/25-SPEC.md (SPEC heading order to mirror: title → Created/Ambiguity/Requirements metadata → Goal → Background → Requirements (numbered, Current/Target/Acceptance) → Boundaries In/Out of scope)
    - playwright.config.ts (existing project array; phase23-unit ends line 213, phase20-parsers lines 70-73, phase26/phase26.5 broad-testMatch blocks lines 226-248)
    - .env.local.example (Anthropic block lines 9-10, blank line 11, Shotstack section starts line 12)
    - .planning/REQUIREMENTS.md (AIPS-* rows lines 476-505 — checkbox states + traceability table)
    - src/lib/ai/registry.ts, src/lib/ai/llm.ts, src/lib/ai/org-settings.ts, src/lib/parsers/sop-title.ts, src/actions/ai-settings.ts, src/lib/parsers/sop-parser.ts (as-built code being documented — read to cite accurate signatures/line numbers)
  </read_first>
  <action>
    Write `27-SPEC.md` mirroring the `25-SPEC.md` heading order: `# Phase 27: AI Provider & Settings — Specification`, then a metadata line (`**Created:** 2026-07-12` / `**Requirements:** AIPS-REG-01..02, AIPS-SET-01..02, AIPS-PROMPT-01, AIPS-PARSE-01, AIPS-TITLE-01, AIPS-GAP-01..03`), `## Goal`, `## Background` (as-built context citing `src/lib/ai/registry.ts`, `src/lib/ai/llm.ts`, `src/lib/ai/org-settings.ts`, `src/actions/ai-settings.ts`, `src/lib/parsers/sop-title.ts`, `src/lib/parsers/sop-parser.ts`, migration `00042_ai_model_settings.sql`), a `## Architecture` section reproducing the System Architecture Diagram from 27-RESEARCH.md lines 51-85 (lightly adapted, do not redraw), a `## Requirements` section with one numbered entry per AIPS-* ID each carrying **Current** / **Target** / **Acceptance** sub-bullets (for the 6 already-shipped IDs Current == Target — describe the shipped state, cite the exact file:line from RESEARCH e.g. `registry.ts:206-209`, `llm.ts:42-46`/`118-133`, `ai-settings.ts:47-90`, `sop-title.ts:31-89`, `sop-parser.ts:302-315`, `api/sops/parse/route.ts:73-79`), and a `## Boundaries` section with `**In scope:**` (the 3 gaps + docs) and `**Out of scope:**` (widening ORG_CONFIGURABLE_KEYS beyond the 3 parse use-cases per RESEARCH Open Question 2; VOYAGE_API_KEY doc; model-options.ts behavioral tests per RESEARCH Pitfall 3; re-deriving the historical "13 callsites" ROADMAP prose per RESEARCH Pitfall 4). AIPS-PROMPT-01 and AIPS-PARSE-01 are covered ONLY by this SPEC (no test) — their Acceptance bullets must state the observable as-built behavior (grounding/structure/title rules in SYSTEM_PROMPT; empty-field normalization + idempotent delete-before-reparse + OpenRouter fence/retry) with a one-line pointer to `.autoresearch/` for R&D provenance rather than re-deriving the composite-score metrics. Add a one-line "future contributors" rule per RESEARCH "Don't Hand-Roll": new AI code must call `aiModel()`/`llmText()`/`llmToolCall()`, never an SDK directly or a hardcoded model string.

    In `playwright.config.ts`, add two project objects immediately after the `phase23-unit` entry (line 213): (1) `name: 'phase27-unit'`, `testDir: './src/lib/ai/__tests__'`, `testMatch: /.*\.test\.ts$/` — mirrors phase23-unit exactly; (2) `name: 'phase27-stubs'`, `testDir: '.'`, `testMatch: /tests\/phase27\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }` — mirrors the phase26 broad-testMatch single-registration pattern. Add a comment on each restating the CLAUDE.md 2026-05-25 rule (a spec not in any project regex NEVER runs). Do NOT add a project for `sop-title.test.ts` — it lands in the existing `phase20-parsers` testDir and is picked up by its broad `.test.ts$` match.

    In `.env.local.example`, insert two lines immediately after line 10 (`ANTHROPIC_API_KEY=sk-ant-...`) and before the blank line 11: a comment header reading `## OpenRouter (cross-provider model routing — GLM 5.2 and other OpenRouter-served models)` and an empty-value line `OPENROUTER_API_KEY=`. This groups it with the sibling provider keys (Supabase → OpenAI → Anthropic → OpenRouter → Shotstack → Deepgram). Only OPENROUTER_API_KEY — do NOT add VOYAGE_API_KEY (out of scope).

    Do NOT flip any `.planning/REQUIREMENTS.md` checkboxes in this task — AIPS-GAP-01 is the only requirement this task fully closes on its own; AIPS-GAP-02/GAP-03 are delivered by Task 2/3 and must not be marked complete until those tasks land (see Task 3's action for the deferred checkbox flip).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase27-unit && npx playwright test --list --project=phase27-stubs && grep -q "OPENROUTER_API_KEY" .env.local.example && grep -q "AIPS-PROMPT-01" .planning/phases/27-ai-provider-settings-formal-spec-pass/27-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase27-unit` exits 0 (project resolves; empty file list is fine at this task since test files land in Task 2)
    - `npx playwright test --list --project=phase27-stubs` exits 0
    - `playwright.config.ts` contains `name: 'phase27-unit'` with `testDir: './src/lib/ai/__tests__'` and `name: 'phase27-stubs'` with `testMatch: /tests\/phase27\/.*\.(spec|test)\.ts$/`
    - `.env.local.example` line following `ANTHROPIC_API_KEY=sk-ant-...` is the OpenRouter comment header, followed by `OPENROUTER_API_KEY=`; VOYAGE_API_KEY is NOT present
    - `27-SPEC.md` contains a `## Requirements` section with a numbered entry for every one of the 10 AIPS-* IDs, each with Current/Target/Acceptance bullets; AIPS-PROMPT-01 and AIPS-PARSE-01 entries state observable as-built behavior with file:line citations
    - `27-SPEC.md` `## Boundaries` `**Out of scope:**` explicitly lists: widening ORG_CONFIGURABLE_KEYS, VOYAGE_API_KEY doc, model-options.ts tests
    - `.planning/REQUIREMENTS.md` AIPS-SET-02/GAP-01/GAP-02/GAP-03 checkboxes are `[x]`
  </acceptance_criteria>
  <done>27-SPEC.md documents all 10 AIPS-* IDs as-built; both Playwright projects registered and resolvable; OPENROUTER_API_KEY documented; REQUIREMENTS.md traceability updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill registry, llm-routing, and title-guard unit tests</name>
  <files>src/lib/ai/__tests__/registry.test.ts, src/lib/ai/__tests__/llm-routing.test.ts, src/lib/parsers/__tests__/sop-title.test.ts</files>
  <read_first>
    - src/lib/ai/registry.ts (AI_MODELS keys, aiModel() at :206-209, PROVIDER_ENV_KEYS, AiProvider type, AiModelKey type)
    - src/lib/ai/llm.ts (providerForModel() at :42-46, extractJson() at :118-133)
    - src/lib/parsers/sop-title.ts (isPlaceholderTitle / titleFromFileName / ensureSopTitle at :31-89)
    - src/lib/ai-fields/__tests__/registry.test.ts (analog header comment convention lines 1-27 + test-body convention lines 36-52 — one test() per behavioral contract, static @/ imports, no beforeEach fixtures)
    - src/lib/parsers/__tests__/ (existing sibling .test.ts files — confirm phase20-parsers conventions)
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-RESEARCH.md (Code Examples section — exact assertions to cover)
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-PATTERNS.md (behavioral contracts list lines 71-73, 79)
  </read_first>
  <behavior>
    registry.test.ts (AIPS-REG-01, AIPS-GAP-03):
    - Every key in AI_MODELS resolves via aiModel(key) to its defaultId when the key's envVar is unset
    - Setting process.env[envVar] overrides the default for at least one representative key, then restoring env restores default resolution
    - PROVIDER_ENV_KEYS has an entry for every AiProvider referenced across AI_MODELS (no provider without an env-key mapping)
    llm-routing.test.ts (AIPS-REG-02, AIPS-GAP-03):
    - providerForModel('z-ai/glm-5.2') === 'openrouter'; providerForModel('claude-haiku-4-5-20251001') === 'anthropic'; providerForModel('gpt-4o-2024-08-06') === 'openai'
    - extractJson() returns the same parsed object for all three fixtures: raw JSON string, fenced ```json block, prose-wrapped {...}
    - extractJson() on garbage/non-JSON input returns null (does not throw)
    sop-title.test.ts (AIPS-TITLE-01, AIPS-GAP-03):
    - isPlaceholderTitle returns true for known placeholder strings (e.g. 'Untitled', 'SOP', empty) and false for a real title
    - titleFromFileName strips extension/normalizes a sample filename into a human title
    - ensureSopTitle fallback chain: valid LLM title kept → placeholder falls back to filename-derived → junk-with-no-filename falls back to 'Untitled SOP'
  </behavior>
  <action>
    Create the three test files. Each opens with the analog header comment block from `src/lib/ai-fields/__tests__/registry.test.ts` lines 1-27: cite the requirement IDs covered, name the registered Playwright project + testDir (phase27-unit → ./src/lib/ai/__tests__ for the two AI files; phase20-parsers → ./src/lib/parsers/__tests__ for sop-title), and restate verbatim the CLAUDE.md 2026-04-24 rule that dynamic `import('@/...')` fails in the Playwright Node runner outside a testDir-scoped project — so use STATIC `@/` imports only. Import from `@/lib/ai/registry`, `@/lib/ai/llm`, `@/lib/parsers/sop-title` statically. Use `import { test, expect } from '@playwright/test'`, one `test()` per behavioral contract, named `'AIPS-<ID> [case]: description'`, plain `expect()` assertions, no beforeEach/fixtures. For the registry env-override test, save the pre-existing `process.env[envVar]`, set it, assert, then restore in a finally so no test pollutes sibling tests. Iterate `Object.keys(AI_MODELS)` for the default-resolution assertion so new registry keys are automatically covered. These tests exercise ALREADY-CORRECT shipped code — they must PASS immediately (regression lock-in), not RED-then-implement.
  </action>
  <verify>
    <automated>npx playwright test src/lib/ai/__tests__/registry.test.ts src/lib/ai/__tests__/llm-routing.test.ts --project=phase27-unit && npx playwright test src/lib/parsers/__tests__/sop-title.test.ts --project=phase20-parsers</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase27-unit` lists AND passes registry.test.ts + llm-routing.test.ts (both green, non-zero test count)
    - `npx playwright test src/lib/parsers/__tests__/sop-title.test.ts --project=phase20-parsers` is green
    - All three files use static `@/` imports (no `await import(` anywhere in the three files)
    - registry.test.ts iterates AI_MODELS keys (not a hardcoded subset) for default resolution, and asserts PROVIDER_ENV_KEYS covers every provider in AI_MODELS
    - llm-routing.test.ts asserts all three provider shapes AND extractJson() garbage-input returns null (not throws)
    - sop-title.test.ts covers all three ensureSopTitle fallback rungs (LLM title / filename / 'Untitled SOP')
  </acceptance_criteria>
  <done>Three unit test files green against the shipped code, discoverable in their registered projects, static-import compliant.</done>
</task>

<task type="auto">
  <name>Task 3: Runtime org-scope write-isolation regression for setAiModelSetting</name>
  <files>tests/phase27/ai-settings-org-scope.spec.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/actions/ai-settings.ts (setAiModelSetting at :47-90 — confirm requireAdmin() derives organisationId from JWT claims via parseJwtPayload, NO organisationId parameter; .eq('organisation_id', ctx.organisationId) on upsert AND delete)
    - supabase/migrations/00042_ai_model_settings.sql (ai_model_settings table shape: organisation_id, use_case, model_id; onConflict organisation_id,use_case; RLS = org-scoped SELECT only, no authenticated write policy)
    - tests/integration/departments-rls.spec.ts (analog: header convention lines 1-15; source-contract-that-runs-now block lines 35-134; test.fixme runtime block with fully-documented Steps lines 140-191; magic-link cookie auth reference)
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-PATTERNS.md (org-scope spec section lines 83-115 — the 3 runtime steps + the explicit "NO source-contract grep" prohibition)
    - .planning/phases/27-ai-provider-settings-formal-spec-pass/27-RESEARCH.md (Anti-Patterns: grep-only test is the exact 2026-06-05 GREEN-but-meaningless trap and must not be repeated)
  </read_first>
  <action>
    Create `tests/phase27/ai-settings-org-scope.spec.ts` under a `test.describe('AIPS-SET-02 — ai_model_settings write isolation', ...)`. The PRIMARY deliverable is a REAL runtime write-isolation test — NOT a source grep. Mirror the departments-rls.spec.ts structure: a header comment naming the verified requirement IDs (AIPS-GAP-02, AIPS-SET-02), the source-under-test (`src/actions/ai-settings.ts:47-90`), and the phase27-stubs registration + list-verify command. The runtime test body follows the three PATTERNS steps: (1) authenticate as an Org A admin (magic-link cookie install per the CLAUDE.md 2026-04-24 pattern — `sb-{projectRef}-auth-token` cookie, anon key env var is `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`), call the setAiModelSetting path to write `parse-triage` → a vetted model id; (2) using a service-role/admin client (`SUPABASE_SERVICE_ROLE_KEY`), read `ai_model_settings` rows filtered to Org B's organisation_id and assert none were created or mutated; (3) read back the written row and assert its `organisation_id` equals Org A's id (JWT-derived, not client-suppliable — the function signature takes no organisationId param). Because this exercises live Supabase + a session, follow the departments-rls precedent EXACTLY: write the full behavioral body, and if live-DB/session fixtures are not reachable in the execution sandbox, land the runtime assertions as `test.fixme` with the complete Steps documented inline (as departments-rls lines 140-191 do) — carried as a live-Supabase UAT item consistent with this project's Railway-only-testing posture, and EXPLICITLY call out in the plan SUMMARY.md ("Carried UAT" section) that the org-isolation assertion has not actually executed yet if it lands as fixme, mirroring how departments-rls's fixme items are tracked. You MAY additionally include ONE immediately-running behavioral source assertion that proves the "no attack surface" property (setAiModelSetting's exported signature accepts only (useCase, modelId) with no organisationId parameter) — but this is supplementary, NOT a substitute for the runtime isolation test. Under NO circumstance reduce this spec to a `toContain(".eq('organisation_id'")` grep.

    Once this spec is created and passing (or fixme-carried), flip the AIPS-SET-02, AIPS-GAP-01, AIPS-GAP-02, AIPS-GAP-03 checkboxes in `.planning/REQUIREMENTS.md` from `[ ]` to `[x]` (lines 484, 494, 495, 496) and update the traceability table (lines 502-505) status cells to reflect this phase closing them — this is the last task in the plan, so all three tasks' deliverables exist by this point.
  </action>
  <verify>
    <automated>npx playwright test tests/phase27/ai-settings-org-scope.spec.ts --project=phase27-stubs --list && npx playwright test tests/phase27/ai-settings-org-scope.spec.ts --project=phase27-stubs && grep -q "AIPS-GAP-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The spec is discoverable: `npx playwright test tests/phase27/ai-settings-org-scope.spec.ts --project=phase27-stubs --list` shows it
    - `npx playwright test tests/phase27/ai-settings-org-scope.spec.ts --project=phase27-stubs` exits 0 (green now, or green-with-fixme for the live-DB runtime assertions per the departments-rls precedent)
    - The runtime test body contains the three behavioral steps (Org-A write → Org-B service-role read-back asserting untouched → assert written row's organisation_id === Org A id) — verifiable by reading the file
    - The spec does NOT contain a `toContain(".eq('organisation_id'"` or any equivalent source-string grep standing in for the behavioral assertion
    - If runtime assertions are test.fixme, each carries the full inline Steps documentation (mirrors departments-rls lines 140-191) AND the plan SUMMARY.md's "Carried UAT" section names this item explicitly
    - `.planning/REQUIREMENTS.md` AIPS-SET-02/GAP-01/GAP-02/GAP-03 checkboxes are `[x]` and the v5.0 traceability table reflects Phase 27 closure
  </acceptance_criteria>
  <done>A behavioral (not grep) org-scope regression spec exists, registered under phase27-stubs, proving Org A's setAiModelSetting write never touches Org B's rows — locking in the already-correct self-enforcement. REQUIREMENTS.md traceability flipped only after all three tasks' deliverables exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → `setAiModelSetting` server action | Admin-supplied (useCase, modelId) crosses into a service-role DB write; org identity must NOT come from client input |
| Server action → `ai_model_settings` (service-role) | createAdminClient bypasses RLS — the action itself is the only org-scope gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | `setAiModelSetting` service-role write | mitigate | ALREADY mitigated in code (org id from `parseJwtPayload` JWT claims, never a parameter; `.eq('organisation_id', ctx.organisationId)` on upsert + delete). This phase LOCKS it in with the Task 3 runtime regression — no code change. |
| T-27-02 | Tampering | Arbitrary/expensive model-id injection via `setAiModelSetting` | mitigate | ALREADY mitigated — `AI_MODEL_OPTIONS[key]` vetted-allowlist check before upsert. Documented in 27-SPEC.md Requirements (AIPS-SET-01/V5). No code change. |
| T-27-SC | Tampering | npm installs | accept | No package installs this phase — 100% backfill against already-installed deps (RESEARCH § Standard Stack). No supply-chain surface introduced. |
</threat_model>

<verification>
- `npm run test` (full suite) green — no regression in prior-phase projects
- `npx tsc --noEmit` clean (test files carry no type errors — CLAUDE.md 2026-06-02: next-build scope ≠ tsc scope)
- `npm run build` (next build) clean — final gate; this phase touches `'use server'`-adjacent test code, so a real build confirms no server-action constraint breakage
- Both new projects appear in `npx playwright test --list`
- `27-SPEC.md` covers all 10 AIPS-* IDs; REQUIREMENTS.md traceability reflects closure
</verification>

<success_criteria>
- All 10 AIPS-* requirements documented as-built in 27-SPEC.md with Current/Target/Acceptance
- registry.test.ts + llm-routing.test.ts green under phase27-unit
- sop-title.test.ts green under phase20-parsers
- ai-settings-org-scope.spec.ts is a behavioral runtime isolation test (not a grep), discoverable under phase27-stubs
- OPENROUTER_API_KEY documented in .env.local.example
- No source code changed in the AI provider arc (documentation + tests only) — the shipped code was confirmed correct
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New symbols/files created by this phase (excluded from source-drift verification — they do not exist before this phase):

**New files:**
- `.planning/phases/27-ai-provider-settings-formal-spec-pass/27-SPEC.md`
- `src/lib/ai/__tests__/registry.test.ts`
- `src/lib/ai/__tests__/llm-routing.test.ts`
- `src/lib/parsers/__tests__/sop-title.test.ts`
- `tests/phase27/ai-settings-org-scope.spec.ts`

**New Playwright project names (in playwright.config.ts):**
- `phase27-unit` (testDir `./src/lib/ai/__tests__`, testMatch `/.*\.test\.ts$/`)
- `phase27-stubs` (testDir `.`, testMatch `/tests\/phase27\/.*\.(spec|test)\.ts$/`)

**New env doc key (in .env.local.example):**
- `OPENROUTER_API_KEY` (empty-value documentation line + `## OpenRouter` comment header)

**Modified files (no new source symbols — tests reference EXISTING exports):**
- `playwright.config.ts` (two project entries appended)
- `.env.local.example` (two lines inserted)
- `.planning/REQUIREMENTS.md` (checkbox + traceability updates)

**Existing symbols the tests reference (NOT created here — already shipped):**
- `aiModel`, `AI_MODELS`, `PROVIDER_ENV_KEYS`, `AiProvider`, `AiModelKey` (src/lib/ai/registry.ts)
- `providerForModel`, `extractJson`, `llmText`, `llmToolCall` (src/lib/ai/llm.ts)
- `isPlaceholderTitle`, `titleFromFileName`, `ensureSopTitle` (src/lib/parsers/sop-title.ts)
- `setAiModelSetting`, `getAiSettings` (src/actions/ai-settings.ts)
</artifacts_produced>

<output>
Create `.planning/phases/27-ai-provider-settings-formal-spec-pass/27-01-SUMMARY.md` when done
</output>
