---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 05
type: execute
wave: 3
depends_on: ["33-03"]
files_modified:
  - src/actions/grants.ts
  - src/types/org-model.ts
  - src/lib/org-model/resolve-sop-access.ts
  - src/lib/org-model/__tests__/resolve-sop-access.test.ts
  - tests/phase32/grants-org-isolation.spec.ts
  - tests/phase33/sop-grant-schema.spec.ts
  - tests/phase33/sop-grant-materialization.spec.ts
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "createGrant accepts an XOR collectionId/sopId target; SOP targets are org-verified via a sops guard mirroring the collections guard"
    - "A SOP with ANY direct SOP-target grant (any subject tier) STOPS following its collection's grants — the override lives in materialization only; resolveEffectiveAccess is byte-unchanged"
    - "Revoking the last SOP-target grant makes the SOP re-follow its collection on the next materialize (emergent — no stored overridden flag)"
    - "Override forces sops.all_departments = false (00035 bypass closed); materializeOrgAccess includes SOP-target-bearing SOPs so role-membership revocation propagates"
    - "Live ephemeral-org runtime tests prove override + revoke + org isolation via JUNCTION truth + the D-13 RPC — never raw-select denial (Pitfall 6)"
  artifacts:
    - path: "src/actions/grants.ts"
      provides: "sopId target arm + narrowing-override materialization"
      exports: ["createGrant", "revokeGrant", "listGrants", "materializeSopAccess", "materializeCollectionAccess"]
    - path: "src/lib/org-model/resolve-sop-access.ts"
      provides: "pure override-rule helper (unit-testable, NOT a grants.ts export — 'use server' sync-export trap)"
  key_links:
    - from: "materializeSopAccessForOrg"
      to: "resolveEffectiveAccess"
      via: "called twice — grantsByUnit (collection targets) + sopGrantsByUnit (SOP targets); override branch applied by the caller"
      pattern: "resolveEffectiveAccess"
    - from: "revokeGrant SOP-target branch"
      to: "materializeSopAccessForOrg"
      via: "direct re-materialize of that SOP (re-follow trigger)"
      pattern: "materializeSopAccess"
---

<objective>
Extend grants.ts with the SOP-target arm and the locked narrowing-override rule (SC-3): a grant can point at a single SOP from any subject tier, and a SOP with any direct SOP-target grant stops following its collection — implemented entirely in `materializeSopAccessForOrg` (zero RLS changes; SOP-target grants materialize into the same sop_departments/sop_access_people junctions the shipped 00035/00046 arms read). Ships with REAL live-Supabase runtime tests (SC-4).

Purpose: locked decision 2026-07-19 ("once people are chosen by name for a SOP, it stops following its collection") + pre-plan gate (b) ADOPTED: override trigger = existence of any direct SOP-target grant; re-follow is emergent from revoking the last one. This is the security-critical write path — the phase's core.
Output: grants.ts + types + pure helper + unit tests + 2 phase33 specs live + grants-org-isolation repointed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@src/actions/grants.ts
@src/lib/org-model/resolve-access.ts
@src/types/org-model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SOP-target arm + override materialization in grants.ts</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 1 createGrant Zod change, § Pattern 2 — the override rule + ALL 7 edge cases, § Source-Contract Repoint Inventory grants.ts row)
    - src/actions/grants.ts (current createGrant/revokeGrant/materializeSopAccessForOrg L401-502 incl. the CR-02 guard L406-414 and materializeOrgAccess L332-341)
    - tests/phase32/grants-org-isolation.spec.ts (the function-body extraction regexes + strict guard-ordering pins you must keep or repoint IN THIS COMMIT)
  </read_first>
  <files>src/actions/grants.ts, src/types/org-model.ts, src/lib/org-model/resolve-sop-access.ts, tests/phase32/grants-org-isolation.spec.ts</files>
  <action>
    grants.ts ('use server', all value exports async): (1) CreateGrantInput gains nullable uuid `collectionId` + `sopId` with a second .refine enforcing XOR. (2) Keep the existing collections guard and its pinned literal 'Collection not found in this organisation' with its position strictly BEFORE the access_grants insert; add a mirrored sops guard for SOP targets (from('sops').select on id + organisation_id eq orgId) with literal 'SOP not found in this organisation'. (3) createGrant's 23505 idempotent re-read adds .is('sop_id', null)/.eq('sop_id', …) matching per arm. (4) revokeGrant branches re-materialization: collection-target → materializeCollectionAccessForOrg (keep this call reachable — pinned); SOP-target → materializeSopAccessForOrg directly (this is what makes last-person-removed re-follow work). (5) materializeSopAccessForOrg implements the override: read the SOP's direct SOP-target grants; overridden = any exist (ANY subject tier, not just person). When overridden, ignore collection-derived access entirely; resolve SOP-target inheritance by calling the byte-unchanged resolveEffectiveAccess a second time with sopGrantsByUnit (org/area-subject SOP-target grants inherit down normally — additive WITHIN the SOP-target tier). Dept subjects → sop_departments; role subjects fan to role_members and person subjects → sop_access_people ONLY (the Priya rule carries over verbatim). (6) On override, set sops.all_departments = false (00035 bypass would make the override cosmetic). (7) CR-02 guard update: skip materialization only if no collection AND no SOP-target grants (lets collection-less/draft SOPs be wired by name). (8) materializeOrgAccess also includes SOPs bearing SOP-target grants (one extra access_grants select where sop_id not null) so role-membership changes propagate revocation to overridden SOPs (CR-03 class). (9) Extract the pure override-decision logic (given collection-derived sets + SOP-target sets + overridden flag → final dept/person sets) into src/lib/org-model/resolve-sop-access.ts — a NORMAL module, never a grants.ts export ([2026-06-27] 'use server' sync-export breaks next build only). types/org-model.ts: AccessGrant gains `sopId: string | null`. Repoint tests/phase32/grants-org-isolation.spec.ts in this same commit wherever body-extraction regexes or guard-index assertions no longer match (keep verifySubjectInOrg + guards-before-insert ordering, 'Grant belongs to another organisation', createAdminClient(), the sop_departments/sop_access_people literals).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/phase32/grants-org-isolation.spec.ts --project=phase32 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - createGrant enforces XOR + per-arm org guard BEFORE insert; both target guards' literals present
    - Override implemented in materializeSopAccessForOrg only; `git diff --stat src/lib/org-model/resolve-access.ts` is empty (resolver byte-unchanged)
    - all_departments=false on override; CR-02 guard + materializeOrgAccess inclusion both updated
    - resolve-sop-access.ts has NO 'use server' directive; grants-org-isolation.spec.ts green post-repoint
  </acceptance_criteria>
  <done>SOP-target grants + narrowing override live in the write path; resolver untouched; pins repointed.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests + LIVE ephemeral-org runtime tests (SC-4)</name>
  <read_first>
    - tests/phase32/grants-org-isolation.spec.ts (createEphemeralOrg/createEphemeralAdmin/mintAccessToken/managementSql helpers + afterAll org-delete cascade cleanup — copy verbatim)
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Code Examples ephemeral skeleton, § Pitfall 6 — assert junction truth + sop_in_user_person_grants() RPC, NEVER raw .select() denial: 00003 OR-composes an org-wide read, shipped and deliberate)
  </read_first>
  <files>src/lib/org-model/__tests__/resolve-sop-access.test.ts, tests/phase33/sop-grant-schema.spec.ts, tests/phase33/sop-grant-materialization.spec.ts</files>
  <action>
    Flip all three live. (a) resolve-sop-access.test.ts (phase32-unit project): behavioural cases for the pure helper — override triggers on any-tier direct SOP grant; no SOP grants → collection sets pass through; org/area-subject SOP-target grants inherit down within the SOP tier; last-grant-removed input yields collection sets again (emergent re-follow). (b) sop-grant-schema.spec.ts: source-contract on the createGrant XOR + sopRow guard + revokeGrant branch, plus live pg introspection (Management API) that the XOR constraint + new unique index exist. (c) sop-grant-materialization.spec.ts — LIVE runtime with ephemeral throwaway orgs (no test.fixme, per the [2026-06-15] mandate): seed dept + collection + 2 SOPs + a dept-subject collection grant → materialize → assert BOTH SOPs' sop_departments rows exist; add a person-subject SOP-target grant on SOP-1 → re-materialize → assert SOP-1's sop_departments EMPTY + sop_access_people = {person} + sops.all_departments = false (override), SOP-2 unchanged (still follows the collection); revoke the person grant → assert SOP-1's sop_departments repopulated (re-follow). Cross-org isolation: org-A admin attempting createGrant with org-B's sop_id → rejected, zero rows. Person-visibility asserted via junction rows + the sop_in_user_person_grants() D-13 RPC outcome for the granted member — NEVER via raw-select denial for a non-chosen same-org worker (that would assert something false — Pitfall 6). Cleanup: org delete cascades all fixtures (afterAll). Specs test.skip gracefully when live env vars are absent.
  </action>
  <verify>
    <automated>npx playwright test --project=phase33 --project=phase32-unit 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - No test.fixme remains in sop-grant-materialization.spec.ts for override/revoke/isolation cases
    - Override case asserts sop_departments EMPTY + sop_access_people populated + all_departments=false; sibling SOP unaffected; revoke → re-follow proven live
    - No assertion anywhere claims raw-select denial for a same-org non-grantee
    - Unit + phase33 projects green
  </acceptance_criteria>
  <done>Override semantics + org isolation + revoke propagation proven by live runtime + unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SOP-target grant write | Attacker may pass another org's sop_id (fresh admin-client path — Pitfall 1, recurred 3×). |
| grant → materialized junctions | Junction rows ARE the enforcement surface (00003 OR-composes org-wide raw reads — Pitfall 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Elevation of Privilege | cross-tenant SOP-target grant | mitigate | sops.organisation_id guard before insert + live ephemeral-org rejection test asserting zero rows |
| T-33-05-02 | Elevation of Privilege | retained access after revoke/override | mitigate | revokeGrant SOP branch re-materializes; materializeOrgAccess includes SOP-target SOPs; live test asserts junction rows gone |
| T-33-05-03 | Elevation of Privilege | override bypass via all_departments | mitigate | Force all_departments=false on override + live assertion |
| T-33-05-04 | Tampering | duplicate grants defeating revoke (WR-04 class) | mitigate | 00050 unique index covers sop_id (verified live in 33-03) |
| T-33-05-05 | Repudiation | stale grants-org-isolation guard ([2026-07-13] class) | mitigate | Repoint in the same commit as the grants.ts change; spec runs in Task 1's verify |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds (grants.ts is 'use server' — [2026-06-27])
- phase33 + phase32 + phase32-unit projects green
</verification>

<success_criteria>
SC-3 write path + SC-4 proof shipped: XOR grants, narrowing override in materialization only, resolver byte-unchanged, live junction-truth tests green.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-05-SUMMARY.md` when done.
</output>
