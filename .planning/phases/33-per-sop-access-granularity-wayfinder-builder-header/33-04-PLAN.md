---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 04
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowEditButton.tsx
  - tests/phase30/list-rows.spec.ts
  - tests/phase33/wayfinder-header.spec.ts
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: true
requirements: [SC-6]
must_haves:
  truths:
    - "Builder header is a light bar (white bg, --ink-100 zone dividers) with back / you-are-here / forward zones — no dark #0a0a0b bar remains"
    - "The forward chip carries its lock reason inline as a sentence ('Locked — {N} steps below still need checking')"
    - "ONE 'Tools for this SOP ▾' menu holds all 7 locked items; SopActionsMenu + separate flow buttons no longer render as standalone triggers"
    - "BuilderStageShell component name, handlePublish/approval handlers, hasSourceDoc = showPane, stage keys and 'Edit'/'Check'/'Send to workers' labels are all KEPT (6 spec files pin them)"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx"
      provides: "Wayfinder bar + Tools-for-this-SOP menu (component name kept)"
      contains: "Tools for this SOP"
    - path: "tests/phase33/wayfinder-header.spec.ts"
      provides: "SC-6 source contract, flipped live"
  key_links:
    - from: "Tools menu items"
      to: "/admin/sops/[sopId]/{assign,versions,video,qr}"
      via: "kept hrefs (list-rows regexes pin the paths)"
      pattern: "/assign|/versions|/video|/qr"
    - from: "flow menu rows"
      to: "BuilderFlowButton / BuilderFlowEditButton modals"
      via: "components render inside the menu popover (they already portal their modals)"
      pattern: "BuilderFlowButton"
---

<objective>
Rebuild the builder header as the light-schema Wayfinder bar (SC-6, closes UAT gap G1): back zone ("Back to · SOP library"), here zone (amber "YOU'RE EDITING" tick on a --brand-yellow rule + ellipsized title + v{n}), forward zone (single next-stage chip carrying the lock reason inline), plus a --paper-2 tools row with ONE "Tools for this SOP ▾" menu absorbing SopActionsMenu + BuilderFlowButton + BuilderFlowEditButton + DeleteSopButton. UI-only, independent of the access-model track.

Purpose: locked decisions 2026-07-19 (builder-header README §Decisions 1+2) — winner A restyled light, one self-describing menu with locked labels. Rebuilding only the header JSX inside the existing BuilderStageShell keeps 6 pinned spec files green with zero repoints; only list-rows.spec.ts (old menu labels) must repoint.
Output: Wayfinder bar + single tools menu; list-rows repointed; wayfinder-header.spec live; journeys/uat current.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@sketches/builder-header-orientation/README.md
@sketches/builder-header-orientation/index.html
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wayfinder bar + single Tools menu</name>
  <read_first>
    - sketches/builder-header-orientation/index.html (concept A tab — the look-and-feel contract: zone layout, tick treatment, chip states, tools row)
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 6 — the consolidation map + KEEP list, § Pitfall 5 CSS tokens)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx (the dark header block + SopActionsMenu to replace; handlePublish/approval handlers/hasSourceDoc to KEEP verbatim)
    - src/app/(protected)/admin/sops/builder/[sopId]/{BuilderFlowButton,BuilderFlowEditButton}.tsx (both portal their modals and take sop/sopId props — render them inside the menu popover as rows, or lift only the trigger styling)
  </read_first>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx, src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowButton.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderFlowEditButton.tsx</files>
  <action>
    Replace the dark 48px inline-styled header inside BuilderStageShell with the light Wayfinder bar per the sketch: white bg, --ink-100 hairline zone dividers, --paper-2 tools row; the amber "YOU'RE EDITING" tick (amber text on a --brand-yellow rule) and the green ready-chip are the only colour. KEEP verbatim (pinned by builder-review-flow, publish-stage-approval, sb-auth-builder, sb-builder-infrastructure, scp-source-viewer, scp-parse-pipeline): the BuilderStageShell component name + file path, handlePublish/onPublish + publish URL, approveStep(/requestChanges( handlers, hasSourceDoc = showPane, the ← Library href. BuilderStageStepper: keep the file, the BuilderStage union, stage keys, and display labels 'Edit' / 'Check' / 'Send to workers' (pinned by phase30 plain-language + builder-review-flow); it becomes the here/forward-zone renderer — the forward chip is the single next-stage chip whose gated state reads "Locked — {remaining} steps below still need checking" (SC-6 inline lock reason; use "check" verb in new copy only). Add a third chip state for Phase 29 pending-approval: amber "Waiting for approval — {approver}" mirroring PublishStage's approvalStatus prop (research OQ3 recommendation). The activeStage state machine, handleStageSelect guards, and WR-01 demote effect are untouched. Tools row: ONE "Tools for this SOP ▾" menu (aria-haspopup="menu" / aria-expanded={open} kept) with exactly the 7 locked labels: "Assign this SOP to workers" (→ /assign), "See earlier versions" (→ /versions), "Make a training video" (→ /video), "Print a QR code" (→ /qr), "See the flow diagram" (BuilderFlowButton modal), "Edit the flow diagram" (BuilderFlowEditButton modal), "Delete this draft" (drafts only; keep the <DeleteSopButton sopId={sopId} shape + isDraft={initialSop.status === 'draft'} + {isDraft && ( — regex-pinned). Laziest flow wiring: restyle the two flow components' trigger buttons as menu rows rendered inside the popover — both already portal their modals, no state lifting needed. Keep BuilderFlowButton.tsx at its path (no-preview-pill.spec reads it). OrientationStrip: keep as the Check-stage banner (no test pins it) but dedupe the lock-reason sentence so it isn't stated twice. CSS tokens: only declared tokens (--ink-100, --paper-2, --brand-yellow, --accent-ok) — grep `-- "--token:" src/` before referencing anything else ([2026-07-14]; the no-undefined-css-tokens lint spec gates fallback-less usage).
  </action>
  <verify>
    <automated>npx playwright test tests/builder/builder-review-flow.spec.ts tests/phase30/plain-language.spec.ts tests/phase29/publish-stage-approval.spec.ts tests/lint/no-undefined-css-tokens.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No `#0a0a0b` (or equivalent dark-bar bg) remains in BuilderStageShell's header JSX
    - One menu trigger labelled "Tools for this SOP" with all 7 locked item labels; no standalone SopActionsMenu/flow-button triggers outside it
    - 'Edit' / 'Check' / 'Send to workers' labels + stage keys + BuilderStageShell handlers unchanged (pinned specs green with zero repoints)
    - Lock sentence rendered inside the forward chip; no-undefined-css-tokens spec green
  </acceptance_criteria>
  <done>Wayfinder bar + single tools menu live; all KEEP pins intact.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint list-rows labels, flip wayfinder spec, journeys/uat</name>
  <read_first>
    - tests/phase30/list-rows.spec.ts (the 4 OLD menu-label pins: 'Assign to team', 'Version history', 'Generate video', 'Print QR code' — repoint; href regexes + DeleteSopButton shapes + aria pins — KEEP)
    - tests/phase33/wayfinder-header.spec.ts (Wave-0 stub to flip)
    - CLAUDE.md §Pathways Map Maintenance (D-10 — flow-description changes belong in the same plan)
  </read_first>
  <files>tests/phase30/list-rows.spec.ts, tests/phase33/wayfinder-header.spec.ts, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <action>
    Repoint list-rows.spec.ts: the four old label literals become the new locked labels ("Assign this SOP to workers", "See earlier versions", "Make a training video", "Print a QR code"); keep the href regexes (/admin/sops/${sopId}/assign|versions|video|qr), the DeleteSopButton shape pins, and the aria pins unchanged ([2026-07-13] stale-guard class — same commit as the source change). Flip wayfinder-header.spec.ts live: assert the three zones render from BuilderStageShell source (back link, YOU'RE EDITING tick, forward chip), the inline lock sentence "steps below still need checking", exactly one "Tools for this SOP" menu trigger, all 7 locked labels present, and the absence of the old standalone trigger labels. journeys.ts: no routes change — update the builder-flow step descriptions that mention the stepper/actions menu so /pathways stays truthful; uat/tests.ts: add a phase-33 wayfinder entry (light bar renders, lock reason readable, tools menu complete — screenshot check per [2026-07-14]: green tests prove nothing about visibility). Run the full phase30 + builder + phase29 + phase33 spec set.
  </action>
  <verify>
    <automated>npx playwright test tests/phase33/wayfinder-header.spec.ts tests/phase30/list-rows.spec.ts --project=phase33 --project=phase30 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - list-rows.spec.ts asserts the NEW labels and still asserts hrefs/aria/DeleteSopButton shapes; no old-label literal remains
    - wayfinder-header.spec.ts flipped live and green
    - journeys.ts builder flow descriptions current; uat/tests.ts has the wayfinder review entry
  </acceptance_criteria>
  <done>All pinned specs green post-repoint; SC-6 spec live; pathways/uat current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| header restyle only | No auth/data-path change; publish + approval handlers byte-kept. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Tampering | publish-gate regression via header rebuild | mitigate | handlePublish/assertPublishGates call path untouched; builder-review-flow + publish-stage-approval specs run in-plan |
| T-33-04-02 | Repudiation | stale source-contract pins ([2026-07-13] class) | mitigate | list-rows repoint in the SAME plan/commit as the label change |
| T-33-04-03 | Denial of Service | invisible controls via undefined CSS tokens ([2026-07-14]) | mitigate | Declared-tokens-only rule + no-undefined-css-tokens lint spec + UAT screenshot entry |
</threat_model>

<verification>
- phase30 + phase29 + builder + phase33 wayfinder specs green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
SC-6 shipped: light Wayfinder bar, inline lock reason, one self-describing tools menu; zero stale pins.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-04-SUMMARY.md` when done.
</output>
