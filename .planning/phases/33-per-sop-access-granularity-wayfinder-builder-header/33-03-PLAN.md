---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 03
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - scripts/assert-phase33-sop-target-schema.ts
autonomous: false
requirements: [SC-3, SC-4]
user_setup:
  - service: supabase
    why: "Schema push to the live Postgres — types come from config, so build/tsc pass WITHOUT the push, creating a false-positive verification state"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: ".env.local (present from Phase 13/25/26/32 pushes) — required for non-TTY `npx supabase db push`"
must_haves:
  truths:
    - "access_grants.sop_id column + XOR check + idx_access_grants_sop exist in the live DB (Management API raw SQL, not PostgREST)"
    - "uq_access_grants_subject_target exists live and uq_access_grants_subject_collection is gone"
    - "Every pre-migration access_grants row survives byte-identical with sop_id null"
  artifacts:
    - path: "scripts/assert-phase33-sop-target-schema.ts"
      provides: "the executed --capture/--verify gate (written in 33-02; fix-forward edits allowed here)"
      contains: "to_regclass"
  key_links:
    - from: "scripts/assert-phase33-sop-target-schema.ts"
      to: "live Postgres"
      via: "Management API raw SQL (bypasses PostgREST schema cache)"
      pattern: "pg_indexes"
---

<objective>
[BLOCKING] Apply migration 00050 to the live Supabase database and prove: (a) the sop_id arm + XOR check + both indexes are live, (b) 00049's index is replaced, (c) every existing grant row is byte-identical pre/post. Mirrors the Phase 32 32-03 gate.

Purpose: build + tsc pass WITHOUT the push (types come from config) — a false positive. No dependent grants.ts plan (33-05+) can be truthfully verified until the schema is live.
Output: applied schema + green --verify run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@supabase/migrations/00050_access_grants_sop_target.sql
@scripts/assert-phase33-sop-target-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — capture the live grant snapshot</name>
  <read_first>
    - CLAUDE.md §Learnings [2026-06-15] (PGRST205 staleness), [2026-05-08] (env var names)
  </read_first>
  <files>scripts/assert-phase33-sop-target-schema.ts</files>
  <action>
    Confirm `SUPABASE_ACCESS_TOKEN` + service-role env vars resolve from .env.local, then run `npx tsx scripts/assert-phase33-sop-target-schema.ts --capture` against the live DB (read-only) and confirm it snapshots the current access_grants rows (expect the 00047-seeded + any UAT-wired rows; count printed). Fix-forward any script bug found during the live run — this file is the only editable artifact in this plan.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-phase33-sop-target-schema.ts --capture</automated>
  </verify>
  <acceptance_criteria>
    - `--capture` exits 0 and prints the captured row count
    - Snapshot JSON written to the OS temp dir BEFORE any push
  </acceptance_criteria>
  <done>Pre-migration snapshot captured from the live DB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push 00050, verify equivalence + index swap live</name>
  <read_first>
    - CLAUDE.md §Learnings [2026-06-15] (PGRST205 vs 42P01 — re-run --verify before concluding failure)
  </read_first>
  <files>supabase/migrations/00050_access_grants_sop_target.sql (applied, not edited)</files>
  <action>
    Run automatically, in strict order: (1) `npx supabase db push` with `SUPABASE_ACCESS_TOKEN` set (non-TTY) applying 00050 to the live DB; (2) `npx tsx scripts/assert-phase33-sop-target-schema.ts --verify` asserting sop_id column + XOR constraint + idx_access_grants_sop + uq_access_grants_subject_target present, uq_access_grants_subject_collection absent, and every captured grant row byte-identical with sop_id null. If a probe reports MISSING immediately after push, re-run `--verify` once (PGRST205 stale-cache window) before concluding failure. Blocking checkpoint because it mutates the live production schema.
  </action>
  <what-built>
    Automated push + verify sequence: `db push` applies 00050; `--verify` proves the nullable-arm schema live and pre-existing grants untouched. The checkpoint exists only to confirm the live push succeeded and the equivalence assertion passed on the real DB.
  </what-built>
  <how-to-verify>
    1. Confirm Task 1's `--capture` ran BEFORE `db push`
    2. Confirm `npx supabase db push` reported 00050 applied with no error
    3. Confirm `--verify` printed PASS for: sop_id column, XOR constraint, both new indexes, old index absent, grant-row equivalence
    4. If any probe reports MISSING right after push, re-run `--verify` (PGRST205) before concluding failure
  </how-to-verify>
  <verify>
    <automated>npx tsx scripts/assert-phase33-sop-target-schema.ts --verify</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` applied 00050 to the live DB
    - `--verify` exits 0 on all checks
  </acceptance_criteria>
  <done>Nullable-arm schema live; grant rows untouched; index swap confirmed.</done>
  <resume-signal>Type "approved" once db push succeeded and --verify is green, or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live grant source-of-truth | access_grants drives all materialized worker visibility; a mutated row silently changes who sees what. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | grant-row drift across DDL | mitigate | --capture/--verify byte-equivalence on every pre-existing row |
| T-33-03-02 | Information Disclosure | false-negative probe (PGRST205) | mitigate | Management API raw SQL + one retry after NOTIFY reload |
| T-33-03-03 | Tampering | duplicate-grant window if old index survives | mitigate | --verify asserts old index ABSENT and new index PRESENT by name |
</threat_model>

<verification>
- `npx supabase db push` clean
- `npx tsx scripts/assert-phase33-sop-target-schema.ts --verify` exits 0 against the live DB
</verification>

<success_criteria>
Schema live; grant equivalence + index swap proven. Dependent plans (33-05+) can verify truthfully.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-03-SUMMARY.md` when done.
</output>
