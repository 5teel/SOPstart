---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 08
type: execute
wave: 4
depends_on: ["33-05", "33-06"]
files_modified:
  - src/components/admin/wiring/WiringPatchBay.tsx
  - src/app/(protected)/admin/sops/page.tsx
  - tests/phase32/wire-up-mode.spec.ts
  - tests/phase32/library-filter-deeplink.spec.ts
  - tests/phase33/sop-drilldown.spec.ts
autonomous: true
requirements: [SC-2, SC-3]
must_haves:
  truths:
    - "Collections expand in place to their SOP rows; any SOP is selectable for choose-mode — no ?sop= URL required (closes G2)"
    - "Choosing subjects for a selected SOP writes SOP-target grants (any tier) — the SOP becomes chosen-by-name/overridden; selecting a collection still writes collection grants"
    - "SOP-target wires anchor at the SOP row when its collection is expanded, else aggregate at the collection jack with a count badge (rightEndpoint mirror)"
    - "Overridden SOPs show a chosen-by-name row pill derived client-side from grants (no stored flag)"
  artifacts:
    - path: "src/app/(protected)/admin/sops/page.tsx"
      provides: "sopsByCollection (id, title, status per collection) in the existing access-view Promise.all"
      contains: "sopsByCollection"
    - path: "src/components/admin/wiring/WiringPatchBay.tsx"
      provides: "expandedCollections + SOP rows + generalized choose-mode + rightEndpoint"
      contains: "expandedCollections"
  key_links:
    - from: "SOP row click"
      to: "choose-mode (generalized enterWireUp)"
      via: "selected SOP becomes the wire-up target — WiringNewSop derived from selection, not the pinned URL param"
      pattern: "enterWireUp"
    - from: "handleDone (SOP selected)"
      to: "createGrant with sopId target"
      via: "SOP-target grants per pending subject (33-05 arm)"
      pattern: "sopId"
---

<objective>
Give the access map its missing hierarchy (SC-2, closes G2): collections expand in place to nested SOP rows, any SOP is organically selectable into "choose who sees it" mode, and choosing subjects for a SOP writes SOP-target grants — making it chosen-by-name/overridden (SC-3's UI entry). The pinned `?sop=` path survives as a deep-link nicety only.

Purpose: locked decisions 2026-07-19 (README §Decisions 2 + ROADMAP SC-2) + pre-plan gate (c) ADOPTED: wire density solved by mirroring leftEndpoint with rightEndpoint (SOP wires collapse to the collection jack when unexpanded) on top of the shipped quiet-by-default/focus rules.
Output: drill-down + organic choose-mode + rightEndpoint + overridden pill; wire-up-mode/library-filter pins repointed; sop-drilldown spec live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@sketches/access-hierarchy/README.md
@sketches/access-hierarchy/index.html
@src/components/admin/wiring/WiringPatchBay.tsx
@src/app/(protected)/admin/sops/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collection→SOP drill-down + organic choose-mode + rightEndpoint</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 4 — server data assembly, expandedCollections, enterWireUp generalization, rightEndpoint, overridden pill; § Pattern 2 #4 — no ensureSopCollections precondition for wiring)
    - src/app/(protected)/admin/sops/page.tsx (access-view assembly L193-222 — extend the existing Promise.all, no serial waterfall [2026-07-13])
    - src/components/admin/wiring/WiringPatchBay.tsx (the pinned-SOP nesting JSX from d3fc9f5 to reuse as the child-row pattern; enterWireUp/handleDone/WiringNewSop; WireAgg.count; leftEndpoint from 33-06)
  </read_first>
  <files>src/app/(protected)/admin/sops/page.tsx, src/components/admin/wiring/WiringPatchBay.tsx</files>
  <action>
    page.tsx: extend the access-view assembly to fetch id, title, status per collection (ONE `.in('collection_id', ids)` join read on sop_collections→sops inside the existing Promise.all) and pass `sopsByCollection` down; keep `?sop=` handling as a deep-link that pre-selects/pre-expands, no longer a precondition. WiringPatchBay: add `expandedCollections: Set<string>`; render nested SOP rows reusing the shipped pinned-SOP nesting JSX; clicking any SOP row enters choose-mode — generalize enterWireUp from "the one pinned newSop" to "the selected SOP" (WiringNewSop becomes a derived selection). Grant-target semantics (SC-3): with a SOP selected, handleDone writes ONE SOP-target grant per pending subject (sopId set, collectionId null — any tier; the override trigger is any direct SOP grant); with a collection selected, the existing collection-grant path is unchanged. Remove the "This SOP has no collection — set its category first" dead-end (the 33-05 CR-02 guard change makes collection-less SOPs wireable by name; ensureSopCollections is invoked lazily server-action-side only when needed for display, never as a wiring precondition). rightEndpoint (~10-line mirror of leftEndpoint): a SOP-target wire anchors at the SOP row when its collection is expanded, else at the collection jack with an aggregated WireAgg.count badge. Search (matchIds) extends to SOP titles and auto-expands their collections. Overridden SOPs get a row pill (derive client-side: any grant with sopId === row.id) — pill copy stays neutral in this plan ("chosen by name" final copy confirmed in 33-09's sweep). Keep quiet-by-default/focus-only drawing untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "expandedCollections\|sopsByCollection\|rightEndpoint" src/components/admin/wiring/WiringPatchBay.tsx "src/app/(protected)/admin/sops/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Collections expand to SOP rows; any SOP click enters choose-mode without a ?sop= param
    - SOP-selected handleDone writes sopId-target grants; collection-selected path unchanged; no-collection dead-end gone
    - rightEndpoint collapses SOP wires to the collection jack when unexpanded; SOP-title search auto-expands
    - No new serial await in page.tsx (all reads inside the existing Promise.all)
  </acceptance_criteria>
  <done>G2 closed: organic drill-down + per-SOP choose-mode writing SOP-target grants.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint wire-up-mode + library-filter pins, flip sop-drilldown spec</name>
  <read_first>
    - tests/phase32/wire-up-mode.spec.ts (pins: enterWireUp/handleDone body lines, blastRadiusPeople lines, SelectionStrip prop lines, not.toContain('collectionId: newSop.id') — verify each against the new source)
    - tests/phase32/library-filter-deeplink.spec.ts (openInLibraryHref construction + WiringPatchBayShell mount pins — repoint only if the lines moved)
    - tests/phase33/sop-drilldown.spec.ts (Wave-0 stub)
  </read_first>
  <files>tests/phase32/wire-up-mode.spec.ts, tests/phase32/library-filter-deeplink.spec.ts, tests/phase33/sop-drilldown.spec.ts</files>
  <action>
    Same-commit repoints ([2026-07-13]): update wire-up-mode.spec.ts body-line pins for the generalized enterWireUp/handleDone (the not.toContain('collectionId: newSop.id') CR-01 guard evolves into an assertion that SOP-selected handleDone targets sopId, never a collection id masquerading as one); keep copy-literal pins as-is (copy changes land in 33-09). Check library-filter-deeplink pins against the moved page.tsx lines. Flip sop-drilldown.spec.ts live: source-contract asserts expandedCollections, nested SOP-row rendering, choose-mode reachable from an ordinary SOP row (no ?sop= required), sopId-target handleDone, rightEndpoint collapse rule, and the overridden-pill derivation from grants; browser half honest test.fixme (Railway-only UAT).
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/wire-up-mode.spec.ts tests/phase32/library-filter-deeplink.spec.ts tests/phase33/sop-drilldown.spec.ts --project=phase32 --project=phase33 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three specs green against the new source; no stale-red guard remains
    - sop-drilldown.spec.ts flipped live
  </acceptance_criteria>
  <done>SC-2 spec live; wire-up + deeplink guards repointed, still guarding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOP row click → SOP-target grant write | Client supplies the sop id; grants.ts verifies org membership server-side (33-05 guard). |
| access-view data assembly | Per-collection SOP lists read under session RLS — cross-org ids yield empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08-01 | Elevation of Privilege | forged sop id in choose-mode | mitigate | createGrant's sops org guard (33-05) is the real gate; UI ids are hints only |
| T-33-08-02 | Tampering | choose-mode silently writing collection grants (CR-01 recurrence) | mitigate | Evolved wire-up-mode guard asserts sopId-target writes for SOP selections |
| T-33-08-03 | Denial of Service | person-line spaghetti at 15×20 with SOP rows | mitigate | rightEndpoint collapse + quiet-by-default/focus-only drawing (pre-plan gate c) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- phase32 wire-up-mode + library-filter-deeplink + phase33 sop-drilldown green
</verification>

<success_criteria>
SC-2 shipped and SC-3's UI entry live: drill-down, organic choose-mode, SOP-target grant writes, density-safe wires.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-08-SUMMARY.md` when done.
</output>
