---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00050_access_grants_sop_target.sql
  - scripts/assert-phase33-sop-target-schema.ts
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "access_grants.collection_id is nullable and a nullable sop_id FK to sops (on delete cascade) is defined"
    - "XOR CHECK access_grants_exactly_one_target enforces exactly one of collection_id/sop_id"
    - "00049's uq_access_grants_subject_collection is dropped and replaced by uq_access_grants_subject_target covering sop_id (coalesce-sentinel form)"
    - "assertion script captures/verifies existing access_grants rows byte-identical across the migration"
  artifacts:
    - path: "supabase/migrations/00050_access_grants_sop_target.sql"
      provides: "nullable-arm SOP target + XOR + index swap"
      contains: "access_grants_exactly_one_target"
    - path: "scripts/assert-phase33-sop-target-schema.ts"
      provides: "pre/post access_grants equivalence + schema introspection via Management API raw SQL, PGRST205-tolerant"
      contains: "to_regclass"
  key_links:
    - from: "access_grants.sop_id"
      to: "sops"
      via: "FK on delete cascade (SOP deletion auto-cleans its grants)"
      pattern: "references public.sops"
---

<objective>
Write migration 00050 (FILES ONLY — the [BLOCKING] live `db push` + assertions live in 33-03, mirroring the 32-02/32-03 split): relax `access_grants.collection_id` to nullable, add a nullable `sop_id` FK arm, add the XOR CHECK, and replace 00049's unique index with one covering `sop_id`. Also write the live-verification script that proves existing grant rows survive byte-identical.

Purpose: the nullable-arm shape (research Pattern 1, pre-plan gate (a) ADOPTED) keeps real FK integrity on both target arms and leaves every existing `.eq('collection_id', …)` read naturally blind to SOP-target rows — old code paths degrade safely. The enum/polymorphic alternative was rejected (loses FK integrity, forces day-one discriminator branches).
Output: 00050 + assertion script, unpushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@supabase/migrations/00046_org_model_schema.sql
@supabase/migrations/00049_access_grants_unique_subject_collection.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 00050 nullable-arm migration</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 1 — the EXACT SQL to emit, including the coalesce-to-orgId sentinel index form and the "do NOT set collection_id + sop_id together" rationale)
    - supabase/migrations/00049_access_grants_unique_subject_collection.sql (the sentinel-index precedent being replaced)
  </read_first>
  <files>supabase/migrations/00050_access_grants_sop_target.sql</files>
  <action>
    Single `begin;`/`commit;` migration emitting exactly the research Pattern 1 statements: (1) `alter table public.access_grants alter column collection_id drop not null`; (2) add `sop_id uuid references public.sops(id) on delete cascade` via `add column if not exists`; (3) `add constraint access_grants_exactly_one_target check ((collection_id is null) <> (sop_id is null))` — XOR, never both (a SOP can live in multiple collections via sop_collections; storing a collection alongside would denormalize and go stale); (4) `create index if not exists idx_access_grants_sop on public.access_grants (sop_id)`; (5) `drop index if exists public.uq_access_grants_subject_collection` and recreate as `uq_access_grants_subject_target` on (organisation_id, subject_type, coalesce(subject_id, organisation_id), coalesce(collection_id, organisation_id), coalesce(sop_id, organisation_id)) — 00049's index omits sop_id and PG treats NULLs as distinct, so duplicate SOP-target grants would slip through it (WR-04 class). Pure-additive to rows: no data writes, no policy changes (zero RLS changes this phase — SOP targets materialize into the shipped 00035/00046 junction arms). Add the header comment naming the phase + the XOR rationale.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/00050_access_grants_sop_target.sql | grep -c "access_grants_exactly_one_target\|uq_access_grants_subject_target\|drop not null\|references public.sops"</automated>
  </verify>
  <acceptance_criteria>
    - File contains all 5 statements inside one begin/commit; XOR check uses `<>` between the two null tests
    - New unique index includes `coalesce(sop_id, organisation_id)`; old index dropped by name
    - File contains NO `create policy`, NO `drop policy`, NO insert/update/delete of rows
  </acceptance_criteria>
  <done>00050 defines the nullable-arm SOP target, additive-only, index-swap included.</done>
</task>

<task type="auto">
  <name>Task 2: Write the pre/post equivalence + schema assertion script</name>
  <read_first>
    - scripts/assert-phase32-day-one-equivalence.ts (the --capture/--verify + Management API raw-SQL idiom to copy; confirmed this session it does NOT pin the 00049 index name — no repoint needed there)
    - CLAUDE.md §Learnings [2026-06-15] (PGRST205 vs 42P01), [2026-05-08] (NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY), [Phase 32-03] (tsx scripts wrap async logic in main(), no top-level await)
  </read_first>
  <files>scripts/assert-phase33-sop-target-schema.ts</files>
  <action>
    Standalone tsx script, `main()` wrapper, two argv modes: `--capture` snapshots every `access_grants` row (all columns, ordered by id) to a JSON in the OS temp dir; `--verify` (a) re-reads `access_grants` and asserts every captured row still exists byte-identical with `sop_id` null (pre-existing rows must be untouched by 00050), (b) asserts via Management API raw SQL — NOT the PostgREST client — that: `sop_id` column exists on access_grants (information_schema.columns), `access_grants_exactly_one_target` exists (pg_constraint), `uq_access_grants_subject_target` exists AND `uq_access_grants_subject_collection` does NOT (pg_indexes), `idx_access_grants_sop` exists, and collection_id is nullable. Treat PGRST205 as stale-cache (retry once after `NOTIFY pgrst, 'reload schema'`), never as missing. Print PASS/FAIL per check; exit non-zero on any FAIL. Env var names per [2026-05-08].
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "assert-phase33-sop-target-schema" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Script compiles clean under `npx tsc --noEmit`
    - Existence checks use Management API raw SQL (pg_indexes/pg_constraint/information_schema), not `.from().select()`
    - `--verify` exits non-zero on any missing object, any surviving old index, or any mutated pre-existing grant row
  </acceptance_criteria>
  <done>Script exists, compiles, capture+verify implemented, PGRST205-tolerant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live grant rows | 00050 must not mutate or drop any existing grant (worker visibility derives from them). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Tampering | existing access_grants rows | mitigate | Pure-DDL migration + `--capture`/`--verify` byte-equivalence assertion in 33-03 |
| T-33-02-02 | Tampering | duplicate SOP-target grants defeating revoke (WR-04 class) | mitigate | Replacement unique index covers coalesce(sop_id, organisation_id) |
| T-33-02-03 | Elevation of Privilege | both-targets-set grant widening reads | mitigate | XOR CHECK — exactly one target arm ever set |
</threat_model>

<verification>
- grep gates above pass
- `npx tsc --noEmit` clean
- Actual apply + assertions run in 33-03
</verification>

<success_criteria>
00050 + assertion script written; additive-only; index swap covered; nothing pushed.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-02-SUMMARY.md` when done.
</output>
