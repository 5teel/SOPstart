---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase33/teams-ladder.spec.ts
  - tests/phase33/sop-drilldown.spec.ts
  - tests/phase33/sop-grant-schema.spec.ts
  - tests/phase33/sop-grant-materialization.spec.ts
  - tests/phase33/plain-language-access.spec.ts
  - tests/phase33/wayfinder-header.spec.ts
  - src/lib/org-model/__tests__/resolve-sop-access.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "npx playwright test --list --project=phase33 lists all 6 stub specs (zero discovered = FAIL)"
    - "The phase33 project runs green (stubs test.fixme or vacuous) at phase head"
    - "resolve-sop-access.test.ts is discovered by the existing phase32-unit testDir regex with NO config edit"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase33 project with broad tests/phase33/** testMatch"
      contains: "phase33"
    - path: "tests/phase33/sop-grant-materialization.spec.ts"
      provides: "SC-4 live-runtime stub (flipped LIVE in 33-05, not permanent fixme)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase33/*.spec.ts"
      via: "testMatch regex"
      pattern: "tests\\\\/phase33"
---

<objective>
Wave 0 Nyquist harness for Phase 33: register a `phase33` Playwright project and drop one stub spec per success criterion (SC-1..SC-6) plus the behavioural unit stub for the pure narrowing-override helper. Later plans flip each stub live in the plan that ships the code it tests.

Purpose: CLAUDE.md [2026-05-25] — a spec not in any project regex NEVER runs. Single registration point before any Phase 33 code exists so no later plan touches playwright.config.ts.
Output: `phase33` project + 6 stub specs + 1 unit stub, all discoverable via `--list`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Symbols/files this phase introduces (exclude from source-drift acknowledgement across all Phase 33 plans):
- Migration: `supabase/migrations/00050_access_grants_sop_target.sql` (nullable `sop_id` arm + XOR check + `uq_access_grants_subject_target` replacing 00049's `uq_access_grants_subject_collection`)
- Actions extended: `src/actions/grants.ts` (sopId target arm + narrowing-override materialization), `src/actions/departments.ts` (assignSopDepartments rewired through SOP-target grants)
- Lib: `src/lib/org-model/resolve-sop-access.ts` (NEW pure override-rule helper)
- Components: `src/components/admin/wiring/AccessAnswerPanel.tsx` (NEW); WiringPatchBay/SelectionStrip extended in place
- Builder header: Wayfinder bar inside `BuilderStageShell.tsx` (component name KEPT) + single "Tools for this SOP ▾" menu
- Scripts: `scripts/assert-phase33-sop-target-schema.ts`
- Playwright project: `phase33`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase33 Playwright project</name>
  <read_first>
    - playwright.config.ts (the phase32 project block is the exact analog — broad testMatch, single registration point comment)
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Validation Architecture)
  </read_first>
  <files>playwright.config.ts</files>
  <action>
    Add one project object named `phase33` mirroring the `phase32` block verbatim: `testDir: '.'`, `testMatch: /tests\/phase33\/.*\.(spec|test)\.ts$/`, chromium. Header comment mirrors phase32's (single registration point, CLAUDE.md 2026-05-25 reference, verify command `npx playwright test --list --project=phase33`). Do NOT modify any existing project block.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase33 2>&1 | grep -c "tests/phase33" || true</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains `name: 'phase33'` with the broad testMatch regex
    - `npx playwright test --list --project=phase33` exits 0 and lists the 6 stub files from Task 2 (zero discovered = FAIL, [2026-05-25])
    - git diff shows only an added block
  </acceptance_criteria>
  <done>phase33 project registered; discovers every tests/phase33/*.spec.ts file.</done>
</task>

<task type="auto">
  <name>Task 2: Create 6 stub specs + the override-rule unit stub</name>
  <read_first>
    - tests/phase32/grants-org-isolation.spec.ts (the ephemeral-org helper pattern the SC-4 stub will flip into; also the stub-comment convention "flipped live in NN")
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Phase Requirements → Test Map — exact spec-to-SC mapping)
  </read_first>
  <files>tests/phase33/teams-ladder.spec.ts, tests/phase33/sop-drilldown.spec.ts, tests/phase33/sop-grant-schema.spec.ts, tests/phase33/sop-grant-materialization.spec.ts, tests/phase33/plain-language-access.spec.ts, tests/phase33/wayfinder-header.spec.ts, src/lib/org-model/__tests__/resolve-sop-access.test.ts</files>
  <action>
    Create the 6 phase33 stubs. Source-contract halves may land real from day one where the target file already exists; browser halves are honest `test.fixme` with reasons (phase-32 precedent). Each declares the REAL path constant of the artifact it later asserts on and names its flip plan in a top comment. Mapping: teams-ladder→SC-1 (ladder tiers in WiringPatchBay, flips 33-06), sop-drilldown→SC-2 (collections expand to SOP rows + organic choose-mode, flips 33-08), sop-grant-schema→SC-3 (00050 XOR + createGrant sopId arm + override trigger, flips 33-05), sop-grant-materialization→SC-4 (LIVE ephemeral-org runtime — mark with a comment as [2026-06-15]-mandated real runtime inserts flipped LIVE in 33-05 and extended in 33-07, NOT permanent fixme), plain-language-access→SC-5 (no jargon literals + answer-panel sentences, flips 33-09), wayfinder-header→SC-6 (zones + single tools menu + locked labels, flips 33-04). Also create `src/lib/org-model/__tests__/resolve-sop-access.test.ts` as a behavioural unit stub (test.fixme naming the override cases: trigger on any direct SOP grant, last-person-removed re-follow, org/area SOP-target inheritance) — it auto-registers under the existing phase32-unit testDir regex, no config edit; confirm with `npx playwright test --list --project=phase32-unit`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase33 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist under `tests/phase33/`; unit stub exists under `src/lib/org-model/__tests__/`
    - `npx playwright test --project=phase33` exits 0 (green at phase head)
    - `npx playwright test --list --project=phase32-unit` includes resolve-sop-access.test.ts
    - sop-grant-materialization.spec.ts carries the [2026-06-15] live-runtime mandate comment
  </acceptance_criteria>
  <done>All stubs exist, discoverable, green-at-head.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness only | No runtime surface; config + stub specs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Repudiation | unregistered spec | mitigate | `--list --project=phase33` discoverability gate ([2026-05-25]) |
| T-33-01-SC | Tampering | npm installs | accept | Zero new dependencies this phase (33-RESEARCH Package Legitimacy Audit) — no install task |
</threat_model>

<verification>
- `npx playwright test --list --project=phase33` lists 6 files
- `npx playwright test --project=phase33` green
</verification>

<success_criteria>
phase33 project registered; 6 stubs + unit stub discoverable and green-at-head.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-01-SUMMARY.md` when done.
</output>
