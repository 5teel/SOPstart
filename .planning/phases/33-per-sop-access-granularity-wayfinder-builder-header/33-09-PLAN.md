---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 09
type: execute
wave: 5
depends_on: ["33-04", "33-07", "33-08"]
files_modified:
  - src/components/admin/wiring/SelectionStrip.tsx
  - src/components/admin/wiring/AccessAnswerPanel.tsx
  - src/components/admin/wiring/WiringPatchBay.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
  - tests/phase32/banner-slot-stability.spec.ts
  - tests/phase32/wire-up-mode.spec.ts
  - tests/phase33/plain-language-access.spec.ts
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: true
requirements: [SC-5]
must_haves:
  truths:
    - "No 'grants' / 'wire up' / 'UNWIRED' jargon remains in any user-facing wiring UI copy (SC-5)"
    - "Selecting a SOP/collection renders the plain-language 'Who can see this?' panel; selecting a person/team flips to 'What can they see?'"
    - "An overridden SOP's panel states plainly that removing all named people makes it follow its collection again"
    - "SelectionStrip keeps its 48px fixed slot + structural pins byte-identical (Phase 32 SC-6 pixel-stability contract) — only copy changes"
    - "Phase gate green: full suite + tsc + next build + /pathways 0 not-mapped"
  artifacts:
    - path: "src/components/admin/wiring/AccessAnswerPanel.tsx"
      provides: "B's absorbed 'Who can see this?' / 'What can they see?' detail panel below the bay"
      contains: "Who can see this"
    - path: "src/components/admin/wiring/SelectionStrip.tsx"
      provides: "plain-language strip copy in the unchanged 48px slot"
  key_links:
    - from: "AccessAnswerPanel"
      to: "accessByUnit / grants / peopleIndex memos"
      via: "the bay's EXISTING computed data lifted/passed down — no new fetches, no second resolver"
      pattern: "accessByUnit"
    - from: "PublishStage CTA"
      to: "/admin/sops?view=access&sop="
      via: "kept href, relabelled plainly"
      pattern: "view=access"
---

<objective>
Replace every piece of patch-bay jargon with plain language (SC-5, closes G3) and add the absorbed concept-B answer panel: a component below the bay answering "Who can see this?" for SOPs/collections ("Only 2 people can see this SOP — Dave Hohaia and Priya Sharma, chosen by name") and "What can they see?" for people/teams. Then run the phase gate.

Purpose: locked decisions 2026-07-19 (README §Decisions 3 + SC-5: no grants/wire-up/UNWIRED language anywhere user-facing). Copy is the deliverable; internal identifiers/testids stay (renaming would churn ~40 pinned literals for zero user value — research Anti-Patterns).
Output: plain-language strip + AccessAnswerPanel + jargon sweep + repointed copy pins + journeys/uat + phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@sketches/access-hierarchy/README.md
@sketches/access-hierarchy/index.html
@src/components/admin/wiring/SelectionStrip.tsx
@src/components/admin/wiring/WiringPatchBay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plain-language copy + AccessAnswerPanel</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 5 — copy scope, panel data plumbing, jargon-sweep file list; § Pattern 2 #1 — the re-follow sentence)
    - sketches/access-hierarchy/index.html (concept A tab + B panel — the sentence patterns: people-first counts, chosen-by-name phrasing, green summary treatment)
    - tests/phase32/banner-slot-stability.spec.ts (the structural pins that must survive byte-identical: h-[48px] overflow-hidden, the full className template line, onClick={onDone})
  </read_first>
  <files>src/components/admin/wiring/SelectionStrip.tsx, src/components/admin/wiring/AccessAnswerPanel.tsx, src/components/admin/wiring/WiringPatchBay.tsx, src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx</files>
  <action>
    SelectionStrip: keep the 48px fixed slot EXACTLY as-is structurally (className template line, unconditional mount, onClick={onDone}) and rewrite copy only — "✓ Done wiring" → "Save — done", "via N grants" → people-first sentences ("Visible to 23 people — everyone in Forming and General"), idle onboarding line rewritten plainly. AccessAnswerPanel (NEW, below the bay — not a taller strip): SOP/collection selection → "Who can see this?" sentences (chosen-by-name SOPs name the people and say so; overridden SOPs state "Remove all named people and this SOP follows its collection again"); person/team selection → "What can they see?". Data comes from the bay's existing accessByUnit/grants/peopleIndex memos — lift or pass down, ZERO new fetches and no second inheritance recompute (the ONE-resolver rule; wiring-at-scale pins the resolver call). WiringPatchBay jargon sweep: "NEW · UNWIRED" → plain new-SOP pill, "N grants" counts → people phrasing, bay-hint line, saveError copy ("Wiring failed — … pending grants are kept" → plain retry sentence), finalize the 33-08 overridden pill as "chosen by name". PublishStage: relabel the "Wire up access" CTA plainly (e.g. "Choose who sees this") — href unchanged. Use "check"-verb copy where relevant (OQ2: new copy only, no product-wide sweep). Keep ALL internal identifiers, function names, and testids.
  </action>
  <verify>
    <automated>grep -rn "UNWIRED\|Wire up\|wiring" src/components/admin/wiring/*.tsx | grep -v "//\|WiringPatchBay\|WiringNewSop\|Wiring\w" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - No user-facing string in the wiring components contains "grant"/"wire"/"UNWIRED" (identifiers/testids exempt)
    - AccessAnswerPanel renders both question modes from existing memo data (no new fetch, no resolver duplication)
    - SelectionStrip structural pins byte-identical (banner-slot spec's h-[48px]/className/onDone assertions still pass unmodified)
    - PublishStage CTA relabelled, href intact
  </acceptance_criteria>
  <done>G3 closed: the surface answers "Who can see this?" in plain language everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint copy pins, flip SC-5 spec, journeys/uat, phase gate</name>
  <read_first>
    - tests/phase32/banner-slot-stability.spec.ts (repoint COPY pins only: idle copy halves, Visible-to template, via-N-grant, ✓ Done wiring; keep structural pins untouched)
    - tests/phase32/wire-up-mode.spec.ts (copy-literal pins: NEW · UNWIRED etc. — repoint to the new copy)
    - CLAUDE.md §Pathways Map Maintenance + §Learnings [2026-06-27] (real next build as the phase gate)
    - .planning/phases/32-visual-org-model-library-permissions/32-VERIFICATION.md (human-item 6 — the WR-02 divergence this phase's mechanism answers)
  </read_first>
  <files>tests/phase32/banner-slot-stability.spec.ts, tests/phase32/wire-up-mode.spec.ts, tests/phase33/plain-language-access.spec.ts, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <action>
    Same-commit repoints: banner-slot-stability copy pins → new plain copy (structural pins untouched — they are the SC-6 pixel contract); wire-up-mode copy pins → new copy. Flip plain-language-access.spec.ts live: lint-style assertion that no user-facing JSX string literal in the wiring components matches /grant|wire|UNWIRED/i (identifiers/imports/testids/comments exempt via targeted extraction), plus presence of the "Who can see this?" / "What can they see?" / re-follow sentences. journeys.ts: update the access-map flow step descriptions (drill-down + choose-who-sees-it + answer panel; routes unchanged this phase, so coverage stays 0 not-mapped — verify via tests/phase30/governance-fold.spec.ts). uat/tests.ts: add phase-33 entries — (a) drill-down + choose-by-name flow on sopstart.com, (b) answer-panel sentences read correctly to a non-technical safety manager, (c) the WR-02 closure step: express "Changing Plenum Chamber"'s narrow intent as SOP-target grants via the new UI so the divergence is pinned deliberately (answers UAT Q6 / 32-VERIFICATION human-item 6 instead of leaving it dangling). Phase gate: `npx playwright test --project=phase32 --project=phase32-unit --project=phase33`, full `npm run test` for regressions, `npx tsc --noEmit` ([2026-06-02] full-scope), `npm run build` ([2026-06-27]), pathways 0-not-mapped.
  </action>
  <verify>
    <automated>npx playwright test --project=phase32 --project=phase32-unit --project=phase33 2>&1 | tail -5 && npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - banner-slot + wire-up-mode green post-repoint; structural pixel-contract pins unmodified
    - plain-language-access.spec.ts flipped live and green
    - journeys/uat current incl. the WR-02 closure entry; governance-fold pathways spec green (0 not-mapped)
    - Full suite + tsc + next build clean
  </acceptance_criteria>
  <done>SC-5 shipped; phase gate green; SC-1..SC-6 satisfied across the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy/presentation only | No data-path change; panel reads already-computed org-scoped memo data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09-01 | Information Disclosure | answer panel leaking cross-org names | mitigate | Panel renders ONLY from the bay's existing org-scoped fetches — no new queries |
| T-33-09-02 | Repudiation | stale copy pins ([2026-07-13] class) | mitigate | Same-commit repoints; both phase32 specs run in the gate |
| T-33-09-03 | Tampering | banner-slot reflow regression via copy rewrite | mitigate | Structural pins (h-[48px]/className/onDone) byte-untouched and still asserted |
</threat_model>

<verification>
- Full suite (`npm run test`) + phase32/phase32-unit/phase33 green
- `npx tsc --noEmit` clean; `npm run build` succeeds
- /pathways 0 not-mapped
</verification>

<success_criteria>
Plain-language surface + answer panel shipped; every pinned spec repointed and green; phase gate passed.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-09-SUMMARY.md` when done.
</output>
