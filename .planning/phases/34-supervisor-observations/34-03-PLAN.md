---
phase: 34-supervisor-observations
plan: 03
type: execute
wave: 2
depends_on: [34-01, 34-02]
files_modified:
  - tests/phase34/observation-immutability.spec.ts
  - tests/phase34/observation-cross-org-isolation.spec.ts
autonomous: true
requirements: [OBS-01, OBS-02]
must_haves:
  truths:
    - "Migration 00052 is applied to the live Supabase DB and visible to PostgREST"
    - "A cross-org supervisor cannot write or read another org's observations — proven at runtime (D-13, success criterion 4)"
    - "An authenticated UPDATE/DELETE on an observation row is denied by RLS — proven at runtime (append-only)"
  artifacts:
    - path: "tests/phase34/observation-cross-org-isolation.spec.ts"
      provides: "live two-org isolation proof (fixme → live)"
      contains: "organisation"
    - path: "tests/phase34/observation-immutability.spec.ts"
      provides: "live append-only proof (fixme → live)"
      contains: "update"
  key_links:
    - from: "npx supabase db push"
      to: "sop_observations live table"
      via: "SUPABASE_ACCESS_TOKEN non-interactive push"
      pattern: "sop_observations"
---

<objective>
[BLOCKING] Apply migration 00052 to the live Supabase database, reload the PostgREST schema cache, then flip the two safety-critical Wave-0 stubs live: the success-criterion-4 cross-org write/read isolation proof and the append-only immutability proof. This is the mandatory schema-push gate — build and type checks pass WITHOUT the push (types come from config, not the live DB), creating a false-positive verification state, so this plan exists to make the DB real and prove the two hardest guarantees at runtime.

Purpose: The phase cannot pass verification without the live table, and success criterion 4 (the codebase's recurring service-role write-hole class) can only be proven against a real two-org runtime.
Output: Live `sop_observations` table + two green runtime specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-RESEARCH.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@supabase/migrations/00052_supervisor_observations.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migration 00052 to the live DB + reload PostgREST cache</name>
  <read_first>
    - supabase/migrations/00052_supervisor_observations.sql (the migration being applied)
    - .planning/phases/33-per-sop-access-and-wayfinder/33-03-PLAN.md if present, else the 32-03 blocking-push precedent in ROADMAP (the exact db-push + verify idiom this codebase uses)
  </read_first>
  <files>supabase/migrations/00052_supervisor_observations.sql</files>
  <action>
    Run `npx supabase db push` with `SUPABASE_ACCESS_TOKEN` set in the environment (non-TTY workaround — the token makes the push non-interactive; do NOT hardcode secrets in any committed file). After a successful push, reload the PostgREST schema cache to avoid the PGRST205 stale-cache window (CLAUDE.md 2026-06-15): issue `NOTIFY pgrst, 'reload schema';` via the Supabase Management API SQL endpoint, OR verify table existence directly with `select to_regclass('public.sop_observations')` through the same raw-SQL endpoint (bypasses the PostgREST cache entirely). Confirm both the table and the `organisations.observation_labels` column are live before proceeding. If the push requires an interactive prompt that cannot be suppressed, STOP and surface it — do not fabricate success.
  </action>
  <verify>
    <automated>npx supabase migration list 2>&1 | grep -q "00052" && echo "00052 applied" || echo "PUSH NOT CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list` (or `to_regclass('public.sop_observations')`) confirms 00052 is applied to the remote DB
    - `organisations.observation_labels` column is present on the live table
    - PostgREST schema cache reloaded (NOTIFY issued or existence verified via raw SQL, not just the cached REST client)
  </acceptance_criteria>
  <done>sop_observations + observation_labels are live and PostgREST-visible.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the cross-org isolation + immutability runtime proofs live</name>
  <read_first>
    - tests/phase34/observation-cross-org-isolation.spec.ts, tests/phase34/observation-immutability.spec.ts (the Wave-0 stubs to flip)
    - src/actions/grants.ts and its Phase 32-05 runtime tests (the ephemeral-throwaway-org + magic-link-cookie pattern to reuse for two-org isolation — CLAUDE.md 2026-04-24 magic-link cookie install)
    - .planning/phases/34-supervisor-observations/34-RESEARCH.md (§ "Runtime cross-org isolation test pattern")
  </read_first>
  <files>tests/phase34/observation-cross-org-isolation.spec.ts, tests/phase34/observation-immutability.spec.ts</files>
  <action>
    Convert the two `test.fixme` runtime blocks into live `test(...)` runs (keep the source-contract guards intact).
    - Cross-org (SC-4): spin up two ephemeral throwaway orgs (org A, org B) with the admin client, seed an org-A worker + an org-A SOP, and two authenticated sessions. Assert: (a) an org-B supervisor session inserting a `sop_observations` row with org-A's worker/sop is DENIED by RLS (error or 0 rows — never a 500); (b) an org-B session `select`ing `sop_observations` returns ZERO org-A rows. Tear down both orgs in a `finally`.
    - Immutability: insert one valid observation as an org-A recorder, then attempt an authenticated `update` (change verdict/note) and an authenticated `delete` on that row — assert both are refused by RLS (no rows mutated), confirming append-only at the data layer, not just the UI.
    If no runtime session infra is reachable in this environment (chromium/live-app absent), follow the documented Railway-only-testing fallback: keep the assertions as `test.fixme` with the full live body written and add a source-contract assertion that the migration's policies encode the guarantee — mirror the Phase 27 `test.fixme` carry decision exactly, and note the carry in the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 tests/phase34/observation-cross-org-isolation.spec.ts tests/phase34/observation-immutability.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cross-org spec proves org-B cannot write an org-A observation AND cannot read org-A observations (or is a documented Railway-only `test.fixme` carry with the full body + source-contract backstop)
    - Immutability spec proves authenticated UPDATE and DELETE are RLS-denied
    - `npx playwright test --project=phase34` remains green overall
  </acceptance_criteria>
  <done>Success criterion 4 and append-only immutability are proven (or carried per the Railway-only convention with a source-contract backstop).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → live DB | Green tsc/build without a push is a false-positive; the push closes that gap |
| org A ↔ org B | The runtime proof that the RLS boundary actually holds against a real second tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | EoP / Tampering | cross-org write | mitigate | Live two-org test proves org-B supervisor cannot insert an org-A observation (SC-4) |
| T-34-03-02 | Information Disclosure | cross-org read | mitigate | Live test proves org-B read returns zero org-A rows |
| T-34-03-03 | Repudiation | append-only | mitigate | Live test proves authenticated UPDATE/DELETE denied by RLS |
| T-34-03-04 | Tampering | PGRST205 stale cache | mitigate | NOTIFY reload / to_regclass verify after push (CLAUDE.md 2026-06-15) |
| T-34-03-SC | Tampering | secrets in repo | mitigate | SUPABASE_ACCESS_TOKEN read from env only, never committed |
</threat_model>

<verification>
- `npx supabase migration list` shows 00052 applied
- Cross-org + immutability specs green (or documented Railway-only carry)
- `npx playwright test --project=phase34` green
</verification>

<success_criteria>
The observations table is live and PostgREST-visible; cross-org isolation (SC-4) and append-only immutability are proven at runtime or carried per the project's Railway-only-testing convention with a source-contract backstop.
</success_criteria>

<artifacts_this_phase_produces>
This plan applies (does not create new source symbols) migration 00052 to the live DB and flips two Phase-34 specs live. No new exported symbols. (Phase-wide artifact set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-03-SUMMARY.md` when done.
</output>
