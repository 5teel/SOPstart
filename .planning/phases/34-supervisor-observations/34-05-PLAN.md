---
phase: 34-supervisor-observations
plan: 05
type: execute
wave: 4
depends_on: [34-04]
files_modified:
  - src/components/observations/RecordObservationModal.tsx
  - src/components/observations/VerdictButtons.tsx
  - src/components/observations/ObservationRow.tsx
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "One shared modal records an observation from any entry point: worker (usually pre-filled) → SOP picker (required-first) → two verdict buttons → optional note → save"
    - "The modal states on-face that the record is permanent and visible to the worker (D-06)"
    - "Verdict buttons render the org's renamable labels (D-02) over declared CSS tokens — never the undefined --brand-yellow"
    - "ObservationRow renders one observation (verdict dot + SOP title + observer + date + optional note) for reuse by the person panel and worker profile"
  artifacts:
    - path: "src/components/observations/RecordObservationModal.tsx"
      provides: "shared record modal calling recordObservation"
      contains: "recordObservation"
    - path: "src/components/observations/VerdictButtons.tsx"
      provides: "binary verdict picker (performed_to_sop / needs_support)"
    - path: "src/components/observations/ObservationRow.tsx"
      provides: "single-observation list row renderer"
  key_links:
    - from: "RecordObservationModal"
      to: "recordObservation / listWorkerSopsForPicker"
      via: "server action calls"
      pattern: "recordObservation"
---

<objective>
Build the one shared recording modal both entry points mount, plus the two small presentational primitives it and the read surfaces reuse: `VerdictButtons` (the binary verdict picker) and `ObservationRow` (one observation as a list row). The modal is desktop-first (D-05), states the permanent-record warning on-face (D-06), renders org-renamable verdict labels (D-02), and uses only DECLARED CSS tokens — the sketch's `.btn.yellow`/`--brand-yellow` is undefined in the app and MUST NOT be ported.

Purpose: A single modal component keeps entry A and entry B behaviour identical (CONTEXT Claude's Discretion: they should share one modal).
Output: 3 components under `src/components/observations/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@sketches/supervisor-observations/index.html
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VerdictButtons + ObservationRow primitives</name>
  <read_first>
    - sketches/supervisor-observations/index.html (§03 `.verdict-btn` markup + §01/§04 `.obs-row` markup — the exact visual reference, sections 01-04 approved as-sketched)
    - src/components/activity/CompletionSummaryCard.tsx (copy `formatNZDateTime` and `getInitials` verbatim; reuse the `bg-white border border-[var(--ink-100)] rounded-xl` card idiom)
    - src/styles/blueprint-theme.css (confirm declared tokens: `--accent-ok` #16a34a green, `--accent-decision` #d97706 amber, `--ink-*`; `--brand-yellow` is NOT declared — do not use it)
  </read_first>
  <files>src/components/observations/VerdictButtons.tsx, src/components/observations/ObservationRow.tsx</files>
  <action>
    `VerdictButtons` (`'use client'`): props `{ value: Verdict | null, onChange, labels }` where `labels` is the `{ performed_to_sop, needs_support }` from getObservationLabels. Renders two `verdict-btn`-style buttons side by side. `performed_to_sop` uses `--accent-ok` (green) with a subtitle "Watched full task, consistent with procedure"; `needs_support` uses `--accent-decision` (amber — a coaching flag, NOT a red/disciplinary tone per D-01) with subtitle "Coaching flag — not a disciplinary record". Selected state tints with `color-mix(in srgb, var(--accent-*) 8%, white)`. Import `Verdict` from `@/lib/validators/observations`. Never reference `--brand-yellow`.

    `ObservationRow`: props `{ sopTitle, sopVersion, verdict, note, observerName, createdAt, labels }`. Renders the sketch `.obs-row`: a coloured `obs-dot` (green for performed_to_sop, amber for needs_support), the SOP title + verdict label line, a prose subline "Observed by {observerName} · {formatNZDateTime(createdAt)} · SOP v{sopVersion}", and the note (React JSX auto-escaped — never `dangerouslySetInnerHTML`) when present. Plain `<div>`, no `<Link>` wrapper (nothing to navigate to — avoids the Pitfall-3 stopPropagation class entirely).
  </action>
  <verify>
    <automated>grep -rn -- "--brand-yellow" src/components/observations/ | grep -qc . && echo "FAIL undefined token" || echo "no undefined token"</automated>
  </verify>
  <acceptance_criteria>
    - `VerdictButtons` renders both verdicts using `--accent-ok` and `--accent-decision`; imports `Verdict` type; accepts a `labels` prop
    - `ObservationRow` renders dot + title + observer/date/version + optional note as escaped JSX, no `<Link>`, no `dangerouslySetInnerHTML`
    - `grep -- "--brand-yellow" src/components/observations/` returns zero hits
    - No component references an undeclared CSS custom property
  </acceptance_criteria>
  <done>The two shared presentational primitives exist on the paper/ink system with declared tokens only.</done>
</task>

<task type="auto">
  <name>Task 2: RecordObservationModal (shared, both entry points)</name>
  <read_first>
    - sketches/supervisor-observations/index.html (§03 — the recording modal layout: worker chip (pre-filled) → SOP searchable picker (required-first) → verdict → optional note → SAVE + permanent-record footer line)
    - src/actions/observations.ts (recordObservation, listWorkerSopsForPicker, getObservationLabels signatures)
    - src/components/observations/VerdictButtons.tsx (from Task 1)
  </read_first>
  <files>src/components/observations/RecordObservationModal.tsx</files>
  <action>
    `'use client'` modal, props `{ open, onClose, worker: { id, name, roleLabel? }, presetSopId?, presetCompletionId?, onRecorded? }`. Fields: worker chip (locked/pre-filled, shows PRE-FILLED tag), a searchable SOP picker sourced from `listWorkerSopsForPicker(worker.id)` with the worker's required SOPs listed first (D-06) and the current version shown as "v{n} (current) — stamped", `VerdictButtons` (labels from `getObservationLabels()`, fetched on open), an optional note textarea (max 2000 chars). Primary CTA "SAVE OBSERVATION" uses the ink-900 primary-button idiom (`bg-[var(--ink-900)] text-white`) — matching the sketch's own `.btn.primary` for SAVE and the 30-07 publish-CTA fix — NOT `.btn.yellow`. On save, call `recordObservation({ workerId, sopId, verdict, note, completionId: presetCompletionId })`, disable while pending, surface the returned error inline, and on success call `onRecorded?.()` then `onClose()`. Footer states verbatim: "🔒 Permanent record — cannot be edited or deleted after saving. Visible to {worker.name}." (D-06). Save is disabled until a SOP and a verdict are chosen. No offline queue (desktop-first, D-05 — out of scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "components/observations" | grep -qc . && echo "TS ERROR" || echo "observations components tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - Modal calls `recordObservation`, `listWorkerSopsForPicker`, and `getObservationLabels`
    - SOP picker lists the worker's required SOPs first (D-06)
    - Primary CTA uses `bg-[var(--ink-900)] text-white`, not `--brand-yellow`
    - Footer renders the exact permanent-record + "Visible to {name}" line (D-06)
    - Save disabled until SOP + verdict selected; passes `completionId` through when preset
    - `npx tsc --noEmit` clean for the observations components
  </acceptance_criteria>
  <done>One shared modal records an observation from any entry point with the permanent-record framing on-face.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker-supplied note → render | Free text later shown to the worker; must be escaped on display |
| client modal → server action | The modal only proposes; the server action + RLS enforce authorisation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-01 | Tampering (XSS) | note field render | mitigate | React JSX auto-escaping; no `dangerouslySetInnerHTML` in ObservationRow/modal |
| T-34-05-02 | Tampering | undefined CSS token | mitigate | Only declared tokens used; grep guard for `--brand-yellow`; CI `no-undefined-css-tokens.spec.ts` backstops (CLAUDE.md 2026-07-14) |
| T-34-05-03 | EoP | client-side gate is not the control | accept | The modal is UI only; authorisation is enforced by recordObservation + RLS (34-04/34-02), not by hiding the button |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for src/components/observations/*
- No `--brand-yellow` / undefined token references
- Modal wires recordObservation + picker + labels
</verification>

<success_criteria>
A single shared modal + two reusable primitives deliver the OBS-01 recording UI on the paper/ink system, with the permanent-record framing on-face and zero undefined-token risk.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: `RecordObservationModal`, `VerdictButtons`, `ObservationRow` under `src/components/observations/`. (Phase-wide set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-05-SUMMARY.md` when done.
</output>
