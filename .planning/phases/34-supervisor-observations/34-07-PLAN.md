---
phase: 34-supervisor-observations
plan: 07
type: execute
wave: 5
depends_on: [34-05]
files_modified:
  - src/app/(protected)/activity/SupervisorActivityView.tsx
  - src/components/activity/CompletionSummaryCard.tsx
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "The supervisor /activity view has a Record observation header button that opens the shared modal (manual worker+SOP pick)"
    - "Each completion row has an 'I observed this' action that pre-fills worker + SOP + completion_id and opens the modal (D-03 entry B, D-11 completion link)"
    - "The row action does not navigate to the completion detail page (stopPropagation over the row <Link>)"
  artifacts:
    - path: "src/app/(protected)/activity/SupervisorActivityView.tsx"
      provides: "Record observation header button + shared modal host"
      contains: "RecordObservationModal"
    - path: "src/components/activity/CompletionSummaryCard.tsx"
      provides: "'I observed this' row action"
      contains: "stopPropagation"
  key_links:
    - from: "CompletionSummaryCard 'I observed this'"
      to: "RecordObservationModal (worker+sop+completionId prefilled)"
      via: "onObserve callback lifted to SupervisorActivityView"
      pattern: "onObserve"
---

<objective>
Entry point B (D-03): add a "Record observation" header button to the supervisor `/activity` view, and a per-completion "I observed this" row action that pre-fills worker + SOP + `completion_id` (D-11 free link) and opens the same shared modal. The row action must NOT trigger the row's existing navigation to the completion detail page.

Purpose: The "I just watched that walkthrough" recording moment, cheapest to build (no new surface), reusing the shared modal.
Output: header button + modal host in SupervisorActivityView + row action in CompletionSummaryCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@sketches/supervisor-observations/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 'I observed this' row action on CompletionSummaryCard</name>
  <read_first>
    - src/components/activity/CompletionSummaryCard.tsx (the whole row is one `<Link href={/activity/${id}}>` — a naive inner button navigates on click; Pitfall 3)
    - CLAUDE.md § Phase 04 Learnings (StepPhotoZone `e.stopPropagation()` precedent for the exact same "inner action inside a click target" class)
    - sketches/supervisor-observations/index.html (§02 — the "👁 I OBSERVED THIS" row-action button with `--accent-ok` outline)
  </read_first>
  <files>src/components/activity/CompletionSummaryCard.tsx</files>
  <action>
    Add an optional `onObserve?: (ctx: { workerId, workerName, sopId, sopTitle, completionId }) => void` prop (the card already receives `workerId`, `workerName`, `sopTitle`, `id`; thread `sopId` in via the props from SupervisorActivityView's completion object). Render an "👁 I observed this" button (outline in `--accent-ok`) inside the row. Its onClick MUST call `e.stopPropagation(); e.preventDefault();` before invoking `onObserve(...)` so the parent `<Link>` navigation to `/activity/[id]` does not fire (or restructure so the button is a flex sibling outside the `<Link>` boundary — either avoids the trap; stopPropagation is the smaller diff). Only render the button when `onObserve` is provided (keeps the worker-facing usage of this card unchanged).
  </action>
  <verify>
    <automated>grep -q "stopPropagation" src/components/activity/CompletionSummaryCard.tsx && echo "stopPropagation present"</automated>
  </verify>
  <acceptance_criteria>
    - Card renders an "I observed this" button only when `onObserve` is passed
    - The button's handler calls `e.stopPropagation()` (and `preventDefault()`) before `onObserve`
    - `sopId` is available to build the observe context
    - `npx tsc --noEmit` clean for the card
  </acceptance_criteria>
  <done>The row action fires the observe flow without navigating to the completion detail.</done>
</task>

<task type="auto">
  <name>Task 2: Header button + shared modal host in SupervisorActivityView</name>
  <read_first>
    - src/app/(protected)/activity/SupervisorActivityView.tsx (the client view rendering the completion list; workerMap resolves display names; note admin is redirected away by activity/page.tsx so this surface serves supervisor+safety_manager)
    - src/components/observations/RecordObservationModal.tsx (the shared modal props: worker, presetSopId, presetCompletionId, onRecorded)
  </read_first>
  <files>src/app/(protected)/activity/SupervisorActivityView.tsx</files>
  <action>
    Add a "＋ Record observation" header button (ink-900 primary idiom, next to the "Activity" heading). Hold modal state: `const [observe, setObserve] = useState<{worker, presetSopId?, presetCompletionId?}|null>(null)`. The header button opens the modal with a manual worker pick — since the worker isn't pre-known here, open the modal in a "pick worker + SOP" mode (pass no worker, letting the modal's worker field become selectable from the org members already loaded, OR gate the header button to require selecting a completion first if a full worker-picker is out of scope — prefer the completion-row path as primary and keep the header button opening a worker-selectable modal). Wire each `CompletionSummaryCard`'s `onObserve` to `setObserve({ worker: { id: workerId, name: workerName }, presetSopId: sopId, presetCompletionId: completionId })`. Mount one `<RecordObservationModal open={!!observe} ... onRecorded={...} onClose={() => setObserve(null)} />`. On record, optionally toast/refetch (React Query invalidate is fine but not required).

    Note (ponytail): if the modal's worker field does not yet support free worker selection, the header button may open it scoped to the same completion-row flow via a lightweight worker chooser from `workerOptions` already computed in this view — do not build a new worker-search surface; reuse `workerOptions`/`sopOptions` already in state.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 && grep -q "RecordObservationModal" src/app/\(protected\)/activity/SupervisorActivityView.tsx && echo "modal hosted"</automated>
  </verify>
  <acceptance_criteria>
    - SupervisorActivityView renders a "Record observation" header button and hosts one `RecordObservationModal`
    - Each completion row's `onObserve` pre-fills worker + sopId + completionId (D-11)
    - Header button uses the ink-900 idiom, not `--brand-yellow`
    - phase34 suite green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Entry point B records from the header (manual pick) and from a one-tap completion-row action linked to that completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completion row → observe flow | The row action only pre-fills the modal; recordObservation + RLS enforce authorisation |
| supervisor session → /activity | Surface already gated to supervisor/safety_manager by activity/page.tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07-01 | Tampering | row-action click bleed | mitigate | `e.stopPropagation()`/`preventDefault()` stop the parent `<Link>` navigating (Pitfall 3, Phase 04 precedent) |
| T-34-07-02 | EoP | client-side entry gate | accept | The button visibility is UI-only; recordObservation + RLS are the real authorisation (34-04/34-02) |
| T-34-07-03 | Tampering | undefined CSS token | mitigate | ink-900 / `--accent-ok` declared tokens only; CI no-undefined-css-tokens backstop |
</threat_model>

<verification>
- `grep stopPropagation` present in CompletionSummaryCard
- SupervisorActivityView hosts RecordObservationModal
- phase34 suite green; `npx tsc --noEmit` clean
</verification>

<success_criteria>
Entry point B is live: a header "Record observation" button and a per-completion "I observed this" row action that pre-fills worker+SOP+completion and never navigates away.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: `onObserve` prop on `CompletionSummaryCard`; "Record observation" header button + `RecordObservationModal` host in `SupervisorActivityView`. (Phase-wide set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-07-SUMMARY.md` when done.
</output>
