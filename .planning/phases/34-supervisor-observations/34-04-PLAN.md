---
phase: 34-supervisor-observations
plan: 04
type: execute
wave: 3
depends_on: [34-03]
files_modified:
  - src/actions/observations.ts
  - tests/phase34/record-observation.spec.ts
  - tests/phase34/sop-version-stamp.spec.ts
autonomous: true
requirements: [OBS-01, OBS-03]
must_haves:
  truths:
    - "recordObservation records an observation with a server-resolved SOP version, gated to supervisor/admin/safety_manager, using the SESSION client (no admin client) — RLS is the safety mechanism (D-12)"
    - "listObservationsForPerson (org-scoped) and listObservationsForWorker (self) return observation history for the person panel and worker profile"
    - "listWorkerSopsForPicker returns the org's published SOPs with the worker's required SOPs first (D-06)"
    - "getObservationLabels/setObservationLabels read/write the per-org renamable verdict labels (D-02), canonical values never derived from labels"
  artifacts:
    - path: "src/actions/observations.ts"
      provides: "recordObservation, listObservationsForWorker, listObservationsForPerson, listWorkerSopsForPicker, getObservationLabels, setObservationLabels"
      exports: ["recordObservation", "listObservationsForWorker", "listObservationsForPerson", "listWorkerSopsForPicker", "getObservationLabels", "setObservationLabels"]
  key_links:
    - from: "recordObservation"
      to: "sops.version"
      via: "server-side select before insert (D-10 auto-stamp)"
      pattern: "\\.from\\('sops'\\)\\.select\\('version'\\)"
    - from: "recordObservation"
      to: "sop_observations insert"
      via: "session client (getSessionContext), not createAdminClient"
      pattern: "getSessionContext"
---

<objective>
Ship `src/actions/observations.ts`: the write action (`recordObservation`) plus the read actions the two entry points and the worker profile consume, plus the D-02 org-label read/write. Every path resolves auth via `getSessionContext()` and an inline supervisor|admin|safety_manager role-array check (NOT `requireAdminContext()`, which excludes supervisor). The write uses the SESSION client — the RLS insert policy from 00052 is the safety mechanism (D-12), so no admin client is reached for and there is no cross-org write-hole to self-enforce.

Purpose: The record-and-read backbone every UI in this phase calls. Flips the OBS-01 and OBS-03 runtime stubs live.
Output: `observations.ts` server actions + 2 specs flipped live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-RESEARCH.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@src/lib/validators/observations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: recordObservation + org-label read/write</name>
  <read_first>
    - src/actions/completions.ts (signOffCompletion — copy the role-array check verbatim, widen to supervisor+admin+safety_manager; note its createAdminClient write is the part NOT to copy)
    - src/actions/escalation.ts (authOrg() — the getSessionContext + inline check idiom, session-client write, no admin client anywhere)
    - src/lib/auth/guards.ts (confirm requireAdminContext hardcodes ['admin','safety_manager'] and must NOT be edited — ≥8 callers depend on excluding supervisor)
    - src/lib/validators/observations.ts (RecordObservationSchema)
    - .planning/phases/34-supervisor-observations/34-PATTERNS.md (§ src/actions/observations.ts — the finalized recordObservation body to reproduce)
    - .planning/phases/34-supervisor-observations/34-RESEARCH.md (§ Code Examples — full server-action shape)
  </read_first>
  <files>src/actions/observations.ts</files>
  <action>
    Create the `'use server'` module. `recordObservation(rawInput: unknown)`: safeParse via `RecordObservationSchema`; `getSessionContext()`; reject if `!userId`, if `!role || !['supervisor','admin','safety_manager'].includes(role)` (message: "Only supervisors, admins and safety managers can record observations."), if `!organisationId`. Server-resolve the SOP version — `supabase.from('sops').select('version').eq('id', sopId).single()` (D-10; never trust a client version). Insert into `sop_observations` with the SESSION `supabase` client (D-12 — RLS is the gate; do NOT use createAdminClient): `{ organisation_id: organisationId, sop_id: sopId, sop_version: sop.version, observed_worker_id: workerId, observed_by: userId, verdict, note: note ?? null, completion_id: completionId ?? null }`. Return `{ success: true }` / `{ success: false, error }`. Use `(supabase as any)` for the new table if database.types.ts lacks it, consistent with the departments/blocks precedent.

    Add `getObservationLabels()`: reads `organisations.observation_labels` for the caller's org (session client, org-scoped) and returns `{ performed_to_sop, needs_support }` merged over the platform defaults `{ performed_to_sop: 'Performed to SOP', needs_support: 'Needs support' }` (D-02 — labels are display-only; canonical values are the keys). Add `setObservationLabels({ performed_to_sop, needs_support })`: admin/safety_manager only (inline role check), updates `organisations.observation_labels` for the caller's org, self-enforcing `.eq('id', organisationId)`; if the organisations table has no authenticated admin-update policy, use `createAdminClient()` with the same explicit `.eq('id', organisationId)` self-scope (CLAUDE.md 2026-06-15 self-enforce pattern) — never accept an org id from client input.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 tests/phase34/record-observation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recordObservation` exists, uses `getSessionContext`, checks `['supervisor', 'admin', 'safety_manager']`, and does NOT call `createAdminClient` on the observation insert path
    - It reads `sops.version` server-side and inserts it as `sop_version`
    - `getObservationLabels`/`setObservationLabels` exist; `setObservationLabels` self-enforces `.eq('id', organisationId)` and never reads an org id from input
    - `requireAdminContext()` is not modified
    - record-observation.spec.ts passes (source-contract live)
  </acceptance_criteria>
  <done>The write action and org-label config exist and are RLS-safe with no admin-client write-hole.</done>
</task>

<task type="auto">
  <name>Task 2: Read actions — listObservationsForWorker (self), listObservationsForPerson (org), listWorkerSopsForPicker (D-06)</name>
  <read_first>
    - src/actions/escalation.ts (authOrg() thin read-wrapper to mirror)
    - src/actions/assignments.ts (getUserSopAssignments — the individual+role assignment resolution to mirror for the picker's "required SOPs first")
    - src/actions/org-model.ts (listOrgTree member-name resolution via createAdminClient().auth.admin.listUsers — reuse to resolve observer/worker display names since there is no user_profiles table)
  </read_first>
  <files>src/actions/observations.ts</files>
  <action>
    `listObservationsForWorker(): ` self-scoped read — `getSessionContext()`, then `select` from `sop_observations` where `observed_worker_id = userId`, ordered `created_at desc`, joined with each row's SOP title (via a `sops(title)` select or a follow-up map) and observer display name. Returns rows shaped `{ id, sopId, sopTitle, sopVersion, verdict, note, observerName, createdAt }`. This is what the worker profile (34-08) renders (OBS-02).

    `listObservationsForPerson(workerId: string)`: org-scoped read for the person panel (34-06) — inline supervisor|admin|safety_manager role check, then `select` from `sop_observations` where `observed_worker_id = workerId` (RLS org-scope already confines to the caller's org), same shape as above. Reject non-recorder roles.

    `listWorkerSopsForPicker(workerId: string)`: returns the org's published SOPs as `{ id, title, code, assigned }` sorted assigned-first (D-06 "worker's required SOPs listed first"). Resolve `assigned` by reading the target worker's `sop_assignments` — individual rows where `user_id = workerId` plus role rows matching the worker's `organisation_members.role` (mirror getUserSopAssignments, but for the supplied worker; session client, RLS org-scoped). Do NOT introduce a new "required SOPs" data source — reuse `sop_assignments`, the same table `/sops` reads today (34-RESEARCH Open Question 1). Resolve observer/worker names via the admin `listUsers` email map exactly as `listOrgTree` does.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 tests/phase34/sop-version-stamp.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `listObservationsForWorker` filters on `observed_worker_id = <caller>` and returns SOP title + observer name + verdict + note + sopVersion + createdAt
    - `listObservationsForPerson` is gated to the three recorder roles and returns the same shape for an arbitrary org worker
    - `listWorkerSopsForPicker` returns published org SOPs sorted assigned-first, sourced from `sop_assignments` (no new required-SOPs table)
    - sop-version-stamp.spec.ts passes
  </acceptance_criteria>
  <done>The panel/profile/picker read paths exist and are org- and self-scoped correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | Untrusted input (verdict, note, worker/sop ids, org labels) enters here |
| supervisor role gate | Only supervisor/admin/safety_manager may record or list-for-person; workers may only self-read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | EoP | recordObservation role gate | mitigate | Inline `['supervisor','admin','safety_manager']` check (belt) + RLS insert policy (suspenders); a determined caller hitting Supabase directly still cannot forge a JWT role claim |
| T-34-04-02 | Tampering | client-supplied version | mitigate | `sop_version` is server-resolved from `sops.version`, never accepted from input (D-10) |
| T-34-04-03 | Tampering / cross-tenant | setObservationLabels write | mitigate | org id derived from session, write self-enforces `.eq('id', organisationId)`; if admin-client used, explicit org-scope filter (CLAUDE.md 2026-06-15) |
| T-34-04-04 | Information Disclosure | listObservationsForPerson | mitigate | Session-client read rides `sop_observations_read_org` RLS (org-scoped) + inline recorder-role gate; a worker calling it is rejected before any query |
| T-34-04-05 | Repudiation | no admin-client insert | mitigate | Observation write uses the session client so RLS cannot be bypassed — deliberately no service-role write path (D-12) |
</threat_model>

<verification>
- record-observation.spec.ts + sop-version-stamp.spec.ts green
- `npx tsc --noEmit` clean for observations.ts
- No `createAdminClient` on the observation insert path
</verification>

<success_criteria>
recordObservation + the three reads + org-label config exist, are role- and org-scoped, auto-stamp the SOP version, and use the session client for the write — OBS-01 and OBS-03 runtime stubs are live.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: `src/actions/observations.ts` exporting `recordObservation`, `listObservationsForWorker`, `listObservationsForPerson`, `listWorkerSopsForPicker`, `getObservationLabels`, `setObservationLabels`. (Phase-wide set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-04-SUMMARY.md` when done.
</output>
