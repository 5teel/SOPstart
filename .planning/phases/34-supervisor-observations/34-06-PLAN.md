---
phase: 34-supervisor-observations
plan: 06
type: execute
wave: 5
depends_on: [34-05]
files_modified:
  - src/components/admin/org-model/PersonPanel.tsx
  - src/components/admin/org-model/OrgChartCanvas.tsx
  - src/components/admin/org-model/OrgColumnsBoard.tsx
  - src/components/admin/org-model/TeamViewShell.tsx
  - src/components/admin/observations/ObservationLabelsCard.tsx
  - src/app/(protected)/admin/settings/page.tsx
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "Clicking a named person in the org chart OR the columns view opens a side panel with their info, observation history, and a Record observation CTA (D-03 entry A)"
    - "The Record CTA opens the shared modal with the worker pre-filled — the supervisor only picks the SOP"
    - "Vacancy chips are not clickable (no person to observe)"
    - "Admins can rename the two verdict display labels per org from settings (D-02); canonical values are unchanged"
  artifacts:
    - path: "src/components/admin/org-model/PersonPanel.tsx"
      provides: "side panel: info + observation history + record CTA (Phase 35 training-record growth point)"
      contains: "listObservationsForPerson"
    - path: "src/components/admin/observations/ObservationLabelsCard.tsx"
      provides: "per-org verdict label editor (D-02)"
      contains: "setObservationLabels"
  key_links:
    - from: "OrgChartCanvas / OrgColumnsBoard person-chip onClick"
      to: "TeamViewShell selected-person state"
      via: "onSelectPerson callback prop"
      pattern: "onSelectPerson"
    - from: "PersonPanel Record CTA"
      to: "RecordObservationModal"
      via: "worker-prefilled modal open"
      pattern: "RecordObservationModal"
---

<objective>
Entry point A (D-03): make org-chart and columns person-chips clickable, lift the selection to `TeamViewShell`, and open a `PersonPanel` side panel (person info + observation history + "Record observation" CTA that opens the shared modal with the worker pre-filled). Build this panel as a surface that grows — it is the future home of Phase 35's per-worker training record (CONTEXT D-03). Also ship the D-02 per-org verdict-label editor as a settings card, since renaming is admin-org-level config.

Purpose: The primary "walk the floor" recording entry point, plus the org-level label config it depends on.
Output: PersonPanel + person-click wiring across both org views + label settings card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@sketches/supervisor-observations/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonPanel (info + observation history + record CTA)</name>
  <read_first>
    - sketches/supervisor-observations/index.html (§01 — the person panel: header with name + role/dept, "＋ RECORD OBSERVATION" CTA + "~30 seconds" caption, "Observation history" list)
    - src/app/(protected)/activity/SupervisorActivityView.tsx (the "client shell over server-fetched data" shape to mirror)
    - src/components/observations/ObservationRow.tsx, src/components/observations/RecordObservationModal.tsx (the reused primitives)
    - src/actions/observations.ts (listObservationsForPerson, getObservationLabels)
    - src/types/org-model.ts (OrgPerson — id is null for a vacancy)
  </read_first>
  <files>src/components/admin/org-model/PersonPanel.tsx</files>
  <action>
    `'use client'` slide-over panel, props `{ person: { id, name, roleLabel? } | null, onClose }`. When a person is set: fetch `listObservationsForPerson(person.id)` + `getObservationLabels()` on open (loading skeleton, empty state "No observations yet"). Render: header (name + role/dept line), a "Record observation" primary CTA (ink-900 idiom, NOT `--brand-yellow`) + "~30 seconds · worker pre-filled, just pick the SOP" caption, then an "Observation history" list of `ObservationRow`s (newest first). The CTA opens `RecordObservationModal` with `worker` pre-filled; on `onRecorded`, refetch the history. Structure the panel body as a section list so Phase 35's training record can be appended without a rewrite (CONTEXT D-03 growth-point note). Keep it lean — info + history + CTA only, no derived-state logic this phase.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "org-model/PersonPanel" | grep -qc . && echo "TS ERROR" || echo "PersonPanel tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - PersonPanel fetches `listObservationsForPerson` + `getObservationLabels`, renders `ObservationRow` history + empty state
    - Record CTA opens `RecordObservationModal` with the worker pre-filled and refetches on record
    - CTA uses the ink-900 idiom, not `--brand-yellow`
    - `npx tsc --noEmit` clean for PersonPanel
  </acceptance_criteria>
  <done>The person panel exists, shows history, and records with the worker pre-filled — built to grow into Phase 35's training record.</done>
</task>

<task type="auto">
  <name>Task 2: Wire person-chip clicks in both org views through TeamViewShell</name>
  <read_first>
    - src/components/admin/org-model/OrgChartCanvas.tsx (person-chip render ~line 232-237 — plain `<span>`, no onClick today; grep confirms none)
    - src/components/admin/org-model/OrgColumnsBoard.tsx (person-chip render ~line 104-109 — same, no onClick)
    - src/components/admin/org-model/TeamViewShell.tsx (the client shell that owns view state — the correct place to hold selected-person state, per the Phase 32 "async Server Component can't hold client state" precedent)
  </read_first>
  <files>src/components/admin/org-model/OrgChartCanvas.tsx, src/components/admin/org-model/OrgColumnsBoard.tsx, src/components/admin/org-model/TeamViewShell.tsx</files>
  <action>
    Add an optional `onSelectPerson?: (person: { id: string, name: string, roleLabel?: string }) => void` prop to `OrgChartCanvas` and `OrgColumnsBoard`. In each, add `onClick` to the NAMED person-chip only — gate on `!person.isVacancy && person.id` so vacancy chips stay inert (no person to observe); pass `{ id: person.id, name: person.name, roleLabel: role.name }`. Give the named chip `cursor-pointer` affordance. In `TeamViewShell`, add `const [selectedPerson, setSelectedPerson] = useState<{id,name,roleLabel?}|null>(null)`, pass `onSelectPerson={setSelectedPerson}` to both views, and mount `<PersonPanel person={selectedPerson} onClose={() => setSelectedPerson(null)} />` as a sibling. Do not disturb the existing `onChange`/refetch wiring or the add-affordance ghosts.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "org-model/(OrgChartCanvas|OrgColumnsBoard|TeamViewShell)" | grep -qc . && echo "TS ERROR" || echo "org-model wiring tsc clean"</automated>
  </verify>
  <acceptance_criteria>
    - Both `OrgChartCanvas` and `OrgColumnsBoard` accept `onSelectPerson` and call it only for named (non-vacancy) chips
    - `TeamViewShell` holds `selectedPerson` state and mounts `PersonPanel`
    - Vacancy chips have no onClick
    - `npx tsc --noEmit` clean; phase34 suite still green
  </acceptance_criteria>
  <done>Clicking a named person in either org view opens the panel; vacancies stay inert.</done>
</task>

<task type="auto">
  <name>Task 3: Org verdict-label editor on /admin/settings (D-02)</name>
  <read_first>
    - src/app/(protected)/admin/settings/page.tsx (the settings hub — add a "CONFIG" section beside the existing Approval chains block; server component + AdminNav)
    - src/actions/observations.ts (getObservationLabels, setObservationLabels)
    - src/components/admin/governance/ApprovalChainEditor.tsx (the existing settings-config editor pattern to mirror for shape/placement)
  </read_first>
  <files>src/components/admin/observations/ObservationLabelsCard.tsx, src/app/(protected)/admin/settings/page.tsx</files>
  <action>
    Create `ObservationLabelsCard` (`'use client'`), props `{ initial: { performed_to_sop, needs_support } }`: two labelled text inputs (default placeholders "Performed to SOP" / "Needs support"), a Save button calling `setObservationLabels(...)`, inline success/error, disabled-while-pending. A caption clarifies these are display names only — the competency engine reads fixed canonical values (D-02). In `settings/page.tsx`, fetch `getObservationLabels()` server-side (add to the existing `Promise.all`) and render a new "Observation labels" CONFIG section (mirroring the "Approval chains" block markup) with `<ObservationLabelsCard initial={...} />`.
  </action>
  <verify>
    <automated>grep -rq "setObservationLabels" src/components/admin/observations/ObservationLabelsCard.tsx && echo "labels editor wired"</automated>
  </verify>
  <acceptance_criteria>
    - `ObservationLabelsCard` calls `setObservationLabels` and shows two editable label fields with a "display-only" caption
    - `settings/page.tsx` fetches `getObservationLabels()` and renders the card in a CONFIG section
    - `npx tsc --noEmit` clean for both files
  </acceptance_criteria>
  <done>Admins can rename the two verdict labels per org; canonical values remain fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → settings write | Label rename must stay org-scoped (server action self-enforces) |
| person selection → panel read | Panel reads are org-scoped by RLS + recorder-role gate in listObservationsForPerson |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-01 | Information Disclosure | PersonPanel history | mitigate | Data comes from `listObservationsForPerson` (org-scoped RLS + recorder-role gate, 34-04); panel is admin/team, itself admin-gated |
| T-34-06-02 | Tampering / cross-tenant | label rename | mitigate | `setObservationLabels` self-enforces `.eq('id', organisationId)` from session (34-04) — no client org id |
| T-34-06-03 | Input Validation | vacancy click | mitigate | onClick gated on `!isVacancy && person.id` — a null-id vacancy can never open a record flow |
| T-34-06-04 | Tampering | undefined CSS token | mitigate | ink-900 CTA idiom, no `--brand-yellow`; CI no-undefined-css-tokens backstop |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for all six files
- phase34 suite green
- Clicking a named chip in either view opens PersonPanel; vacancy chips inert (source-contract: onClick gated on `!isVacancy`)
</verification>

<success_criteria>
Entry point A is live: named person → side panel → record with worker pre-filled, in both org views; vacancies inert; admins can rename verdict labels per org.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: `PersonPanel`, `ObservationLabelsCard`; `onSelectPerson` prop added to `OrgChartCanvas`/`OrgColumnsBoard`; `selectedPerson` state in `TeamViewShell`; "Observation labels" section on `/admin/settings`. (Phase-wide set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-06-SUMMARY.md` when done.
</output>
