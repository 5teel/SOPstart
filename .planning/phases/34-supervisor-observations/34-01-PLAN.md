---
phase: 34-supervisor-observations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase34/record-observation.spec.ts
  - tests/phase34/observation-immutability.spec.ts
  - tests/phase34/worker-observation-visibility.spec.ts
  - tests/phase34/sop-version-stamp.spec.ts
  - tests/phase34/observation-cross-org-isolation.spec.ts
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "npx playwright test --list --project=phase34 lists all 5 spec files"
    - "Every phase-34 requirement (OBS-01/02/03) + success criterion 4 has a registered spec"
    - "Stub specs pass green while production code is absent (guarded), and name the plan that flips each live"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase34 project registration (broad testMatch)"
      contains: "tests/phase34"
    - path: "tests/phase34/observation-cross-org-isolation.spec.ts"
      provides: "SC-4 cross-org write/read isolation harness (runtime fixme → live in 34-03)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase34/*.spec.ts"
      via: "phase34 project testMatch regex"
      pattern: "tests\\\\/phase34"
---

<objective>
Stand up the Nyquist Wave-0 test harness for Phase 34 before any production code ships: register a `phase34` Playwright project and drop 5 stub specs — one per phase requirement plus the mandatory cross-org isolation proof (success criterion 4). Every spec passes green now (source-contract assertions guarded by `fs.existsSync`; runtime assertions as `test.fixme`) and is flipped live by a named downstream plan.

Purpose: A spec file not in any project regex NEVER runs (CLAUDE.md 2026-05-25). Registering the harness first guarantees each requirement is gated the moment its production code lands.
Output: `phase34` project in `playwright.config.ts` + 5 spec files under `tests/phase34/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-RESEARCH.md
@.planning/phases/34-supervisor-observations/34-VALIDATION.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase34 Playwright project</name>
  <read_first>
    - playwright.config.ts (see the phase33 project entry, lines ~389-409 — mirror its broad testMatch + comment block verbatim)
  </read_first>
  <files>playwright.config.ts</files>
  <action>
    Append a new project object to the `projects` array, immediately after the `phase33` entry, named `phase34`. Use the identical shape as `phase33`: `testDir: '.'`, `testMatch: /tests\/phase34\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`. Add a leading comment mirroring phase33's block that (a) cites CLAUDE.md 2026-05-25 (an unregistered spec never runs), (b) states the testMatch is DELIBERATELY BROAD so every later plan drops specs into tests/phase34/ with no further config edit, (c) lists the 5 Wave-0 stub filenames and which requirement each covers (record-observation → OBS-01, observation-immutability → OBS-01 append-only, worker-observation-visibility → OBS-02, sop-version-stamp → OBS-03/D-10, observation-cross-org-isolation → success criterion 4), and (d) the verify command `npx playwright test --list --project=phase34`. Do NOT touch any other project entry.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase34 2>&1 | grep -c "tests[\\\\/]phase34"</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains a `name: 'phase34'` project whose testMatch is `/tests\/phase34\/.*\.(spec|test)\.ts$/`
    - `npx playwright test --list --project=phase34` lists exactly the 5 spec files created in Task 2 (zero discovered = FAIL)
    - No existing project entry is modified
  </acceptance_criteria>
  <done>The phase34 project is registered and discovers all 5 stub specs.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 5 Wave-0 stub specs (green-when-absent + fixme runtime)</name>
  <read_first>
    - tests/phase33/teams-ladder.spec.ts (the exact stub idiom to copy: `fs.readFileSync` + `toContain` source-contract, plus a `test.fixme` runtime `describe` block for live-app assertions)
    - .planning/phases/34-supervisor-observations/34-RESEARCH.md (§ "Phase Requirement → Test Map" and § "Runtime cross-org isolation test pattern")
    - .planning/phases/34-supervisor-observations/34-PATTERNS.md (canonical file paths + symbol names each spec will assert against)
  </read_first>
  <files>tests/phase34/record-observation.spec.ts, tests/phase34/observation-immutability.spec.ts, tests/phase34/worker-observation-visibility.spec.ts, tests/phase34/sop-version-stamp.spec.ts, tests/phase34/observation-cross-org-isolation.spec.ts</files>
  <action>
    Write 5 specs. Each must pass green now: wrap every source-contract `toContain` in an `if (fs.existsSync(target))` guard (green when the production file is absent — mirrors the phase22 fs.existsSync stub idiom), and put every live-app/runtime assertion in a `test.fixme(...)` block. Each spec's file header comment must name the downstream plan that flips it live.

    1. `record-observation.spec.ts` (OBS-01, flipped live in 34-04): source-contract guards asserting `src/actions/observations.ts` exports `recordObservation` and contains the supervisor|admin|safety_manager role-array check `['supervisor', 'admin', 'safety_manager']`, and that it uses the session client (contains `getSessionContext(`) and NOT `createAdminClient(` for the insert. Plus a `test.fixme` runtime: a supervisor session records an observation → success; a plain `worker` session is rejected.
    2. `observation-immutability.spec.ts` (OBS-01 append-only, flipped live in 34-03): source-contract on `supabase/migrations/00052_supervisor_observations.sql` — contains `create policy sop_observations_insert_recorder` and does NOT contain any `for update` or `for delete` policy on `sop_observations` (assert absence of `for update` and `for delete` tokens scoped to the observations block). Plus `test.fixme` runtime: an authenticated UPDATE on an observation row is denied by RLS (no rows affected / policy error), not a 500.
    3. `worker-observation-visibility.spec.ts` (OBS-02, flipped live in 34-08): source-contract guard on `src/app/(protected)/profile/page.tsx` referencing an observations section component, and on `src/actions/observations.ts` exporting `listObservationsForWorker`. Plus `test.fixme` runtime: an authenticated worker reads observations where `observed_worker_id = self` and gets their own rows.
    4. `sop-version-stamp.spec.ts` (OBS-03 / D-10, flipped live in 34-04): source-contract guard asserting `recordObservation` server-resolves the SOP version (`.from('sops').select('version')`) and inserts `sop_version` from that read, never from client input. Plus `test.fixme` runtime: insert then assert the row's `sop_version` equals the SOP's current `version`.
    5. `observation-cross-org-isolation.spec.ts` (success criterion 4, flipped live in 34-03): `test.fixme` runtime following the Phase 32-05 ephemeral-org precedent (two throwaway orgs + two sessions) — an org-B supervisor CANNOT insert an observation about an org-A worker (RLS denial), and an org-B session read returns ZERO org-A observation rows. Include a source-contract guard that the migration's `sop_observations_read_org` policy scopes on `current_organisation_id()` OR `observed_worker_id = auth.uid()` (and the worker branch matches only the caller's OWN id — no widened `= any(...)`).

    Use `process.cwd()` + `path.join` for file paths (Windows-safe, per the phase33 spec). Do not import any `@/` module dynamically (fails in the Playwright Node runner — CLAUDE.md 2026-06-24); source-contract via `fs.readFileSync` only.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34</automated>
  </verify>
  <acceptance_criteria>
    - All 5 specs pass green (`npx playwright test --project=phase34` exits 0)
    - Each spec's source-contract assertions are guarded by `fs.existsSync` so they are green while production files are absent
    - `observation-cross-org-isolation.spec.ts` contains a `test.fixme` runtime block spinning up two ephemeral orgs (SC-4)
    - No spec uses a dynamic `import('@/...')`
  </acceptance_criteria>
  <done>5 registered specs pass green and gate OBS-01/02/03 + SC-4, each naming its live-flip plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI | Specs must actually execute; an unregistered spec silently gates nothing (the 2026-05-25 class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Repudiation | phase34 project regex | mitigate | Task 1 verify runs `--list --project=phase34` and greps for the 5 files; zero discovered = FAIL |
| T-34-01-02 | Tampering | green-when-absent stubs | mitigate | fs.existsSync guards keep stubs honest — they assert real symbols the moment the file exists, not a hardcoded pass |
| T-34-01-SC | Tampering | npm installs | accept | No package installs this phase — supply-chain vector N/A (0 new deps per 34-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx playwright test --list --project=phase34` lists all 5 specs
- `npx playwright test --project=phase34` green
</verification>

<success_criteria>
Phase-34 Nyquist harness registered and green; every OBS requirement + SC-4 has a spec that a named downstream plan flips live.
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by Phase 34 (this plan creates the ones marked ★):
- ★ `phase34` Playwright project (playwright.config.ts)
- ★ tests/phase34/record-observation.spec.ts, observation-immutability.spec.ts, worker-observation-visibility.spec.ts, sop-version-stamp.spec.ts, observation-cross-org-isolation.spec.ts
- (later plans) table `sop_observations`; column `organisations.observation_labels`; policies `sop_observations_read_org`, `sop_observations_insert_recorder`; migration `00052_supervisor_observations.sql`; validator `RecordObservationSchema`/`VerdictSchema`; server actions `recordObservation`, `listObservationsForWorker`, `listObservationsForPerson`, `listWorkerSopsForPicker`, `getObservationLabels`, `setObservationLabels`; components `RecordObservationModal`, `VerdictButtons`, `ObservationRow`, `PersonPanel`, `ObservationsSection`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-01-SUMMARY.md` when done.
</output>
