---
phase: 34-supervisor-observations
plan: 08
type: execute
wave: 5
depends_on: [34-05]
files_modified:
  - src/components/profile/ObservationsSection.tsx
  - src/app/(protected)/profile/page.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
  - tests/phase34/worker-observation-visibility.spec.ts
autonomous: true
requirements: [OBS-02, OBS-03]
must_haves:
  truths:
    - "A worker sees every observation about them on their own /profile — verdict, note, observer name, date, SOP version (D-07, OBS-02)"
    - "A plain-language trust banner (NZ Privacy Act framing) ships in the same change as the write UI (D-08)"
    - "The worker has no edit/delete/hide control over their own observations (append-only, D-08)"
    - "journeys.ts + uat/tests.ts document the record + worker-visibility flows"
  artifacts:
    - path: "src/components/profile/ObservationsSection.tsx"
      provides: "worker 'Observations about you' section + trust banner"
      contains: "listObservationsForWorker"
    - path: "src/app/(protected)/profile/page.tsx"
      provides: "profile page renders the observations section"
      contains: "ObservationsSection"
  key_links:
    - from: "ObservationsSection"
      to: "listObservationsForWorker"
      via: "self-scoped server read"
      pattern: "listObservationsForWorker"
---

<objective>
OBS-02: give the worker a full-transparency "Observations about you" section on their own `/profile` — every observation recorded about them (verdict, note, observer, date, SOP version) — with a plain-language trust banner (NZ Privacy Act framing, D-08) shipping in the SAME change as the supervisor write UI. No edit/delete/hide control exists (append-only, D-08). Also update `journeys.ts` and `uat/tests.ts` to document the phase's flows (single owner to avoid parallel-wave write conflicts with 34-06/07).

Purpose: Trust is the whole game — worker visibility is what makes observations coaching evidence, not surveillance.
Output: ObservationsSection + profile wiring + journeys/uat + worker-visibility spec live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
@sketches/supervisor-observations/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: ObservationsSection + trust banner on /profile</name>
  <read_first>
    - src/app/(protected)/profile/page.tsx (server component; `blueprint-frame p-5` section shell + `mono text-xs uppercase` heading; `<OrgSwitcher/>` is the precedent for an additive server-fetched section pulled into its own component)
    - src/components/profile/OrgSwitcher.tsx (the "own component below the static blocks" pattern)
    - sketches/supervisor-observations/index.html (§04 — "Observations about you" header + record count pill + the yellow-tinted trust banner + obs-rows)
    - src/components/observations/ObservationRow.tsx (reused row renderer)
    - src/actions/observations.ts (listObservationsForWorker, getObservationLabels)
  </read_first>
  <files>src/components/profile/ObservationsSection.tsx, src/app/(protected)/profile/page.tsx</files>
  <action>
    Create `ObservationsSection` (server component, mirrors OrgSwitcher's additive pattern): fetch `listObservationsForWorker()` + `getObservationLabels()`. Render a `blueprint-frame` section headed "Observations about you" with a record-count pill, then the trust banner (a tinted callout) with the exact D-08 copy direction: "These are records your supervisors made after watching you work — they're part of your training evidence, and they're yours to see. Nothing here is hidden from you." Then the list of `ObservationRow`s (newest first) including verdict, note, observer name, date, and SOP version (OBS-02 fields). Empty state: "No observations yet." Provide NO edit/delete/dismiss control (append-only, D-08). In `profile/page.tsx`, render `<ObservationsSection />` below the Account section and OrgSwitcher (thin additive edit, keep the file a small list of sections). Use declared tokens only — no `--brand-yellow`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 tests/phase34/worker-observation-visibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ObservationsSection` calls `listObservationsForWorker`, renders trust banner + `ObservationRow` list showing verdict/note/observer/date/version
    - No edit, delete, or hide control on any observation
    - `profile/page.tsx` renders `<ObservationsSection />`
    - worker-observation-visibility.spec.ts passes; no `--brand-yellow`
  </acceptance_criteria>
  <done>The worker sees all observations about them with the trust banner, and cannot alter them.</done>
</task>

<task type="auto">
  <name>Task 2: Flip worker-visibility runtime proof + update journeys.ts + uat/tests.ts</name>
  <read_first>
    - tests/phase34/worker-observation-visibility.spec.ts (the Wave-0 stub to flip live)
    - src/lib/journeys/journeys.ts (the Journey config shape — add flows; keep `route` values real so /pathways coverage + deep-links work; CLAUDE.md Pathways Map Maintenance rule)
    - src/lib/uat/tests.ts (the UAT test config shape — add plain-language click-path checks)
  </read_first>
  <files>tests/phase34/worker-observation-visibility.spec.ts, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <action>
    Flip the worker-visibility runtime block from `test.fixme` to live (or carry as a documented Railway-only `test.fixme` with the full body + source-contract backstop if no live session infra is reachable, per the 34-03 convention): an authenticated worker reads observations about themself and sees their own rows; a worker cannot see another worker's observations.

    Add to `journeys.ts` two flows (no NEW routes are created this phase — Phase 34 augments existing routes, so /pathways "All screens" gains no not-mapped entries; these are flow-documentation additions): (1) "Supervisor records an observation" spanning `/admin/team` (person panel) and `/activity` (row action) → observation saved; (2) "Worker sees observations about them" at `/profile`. Keep `route` values to real existing routes. Add matching plain-language entries to `uat/tests.ts` (layman click-paths + yes/no questions, no internal IDs — per the UAT layman-language convention): e.g. "Open a worker in Team, click Record observation, pick a SOP, choose a verdict, Save — does it appear in their history?" and "As a worker, open Profile — can you see the observations your supervisor recorded, with the note?".
  </action>
  <verify>
    <automated>npx playwright test --project=phase34 && grep -qi "observation" src/lib/journeys/journeys.ts && grep -qi "observation" src/lib/uat/tests.ts && echo "journeys+uat updated"</automated>
  </verify>
  <acceptance_criteria>
    - worker-observation-visibility.spec.ts is live (or a documented Railway-only carry with source-contract backstop)
    - journeys.ts contains the record-observation and worker-visibility flows on real routes
    - uat/tests.ts contains plain-language observation checks
    - phase34 suite green
  </acceptance_criteria>
  <done>OBS-02 is proven and the phase's flows are documented in the living pathways + UAT maps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → own observations | Worker may read rows about themself only; never another worker's |
| worker note render | Supervisor free-text note shown to the worker must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08-01 | Information Disclosure | worker self-read | mitigate | `listObservationsForWorker` filters `observed_worker_id = self`; RLS `observed_worker_id = auth.uid()` branch matches only the caller (34-02/34-04) |
| T-34-08-02 | Tampering (XSS) | note render | mitigate | ObservationRow renders note as escaped JSX; no `dangerouslySetInnerHTML` |
| T-34-08-03 | Repudiation | worker hide/delete | mitigate | No edit/delete/dismiss control offered (D-08); append-only enforced at DB layer |
| T-34-08-04 | Repudiation | stale pathways map | mitigate | journeys.ts + uat/tests.ts updated in this change per the project convention |
</threat_model>

<verification>
- worker-observation-visibility.spec.ts green (or documented carry)
- journeys.ts + uat/tests.ts mention observations on real routes
- `npx tsc --noEmit` clean; no `--brand-yellow`
</verification>

<success_criteria>
The worker sees every observation about them with the trust banner and no ability to alter it; the phase's flows are in the living pathways + UAT maps.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: `ObservationsSection`; profile page renders it; journeys.ts + uat/tests.ts observation flows. (Phase-wide set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-08-SUMMARY.md` when done.
</output>
