---
phase: 34-supervisor-observations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00052_supervisor_observations.sql
  - src/lib/validators/observations.ts
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "sop_observations table exists as an append-only, org-scoped, role-checked standalone record — not anchored to a walkthrough completion (D-09; no UPDATE, no DELETE policy)"
    - "A worker can read observations about themself via the observed_worker_id self-read branch (OBS-02)"
    - "verdict is constrained at the DB layer to the two canonical values"
    - "organisations carries a per-org observation_labels jsonb for renamable display labels (D-02)"
  artifacts:
    - path: "supabase/migrations/00052_supervisor_observations.sql"
      provides: "sop_observations table + RLS + organisations.observation_labels column"
      contains: "create table if not exists public.sop_observations"
    - path: "src/lib/validators/observations.ts"
      provides: "RecordObservationSchema + VerdictSchema Zod contracts"
      exports: ["RecordObservationSchema", "VerdictSchema"]
  key_links:
    - from: "sop_observations RLS insert policy"
      to: "current_organisation_id() + current_user_role() + auth.uid()"
      via: "with check clause"
      pattern: "current_user_role\\(\\) in \\('admin', 'safety_manager', 'supervisor'\\)"
---

<objective>
Create the `sop_observations` append-only table (files only — no live push; 34-03 pushes it) by copy-adapting the proven `sop_review_events` shape from migration 00043, widened to three recorder roles and given a worker self-read branch for OBS-02. Add a per-org `observation_labels` jsonb column for D-02's renamable verdict labels. Ship the matching Zod validator.

Purpose: This is the D-12-mandated append-only/org-scoped/role-checked evidence table — the standalone data layer the whole phase (and Phase 35's classifier) reads from.
Output: Migration `00052_supervisor_observations.sql` + `src/lib/validators/observations.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-RESEARCH.md
@.planning/phases/34-supervisor-observations/34-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 00052 — sop_observations table + RLS + org labels column</name>
  <read_first>
    - supabase/migrations/00043_ownership_review_governance.sql (§ Section 4 sop_review_events — the EXACT append-only RLS shape to copy: org-scoped SELECT, role-checked INSERT with `<actor>_by = auth.uid()`, NO update/delete policy)
    - supabase/migrations/00001_foundation_schema.sql (confirm `current_organisation_id()` and `current_user_role()` helpers exist — do NOT redefine them; confirm the organisations table shape for the additive jsonb column)
    - .planning/phases/34-supervisor-observations/34-PATTERNS.md (§ migration — the finalized table DDL + policy text to reproduce)
  </read_first>
  <files>supabase/migrations/00052_supervisor_observations.sql</files>
  <action>
    Create `public.sop_observations` with columns: `id uuid pk default gen_random_uuid()`, `organisation_id uuid not null references organisations(id) on delete cascade`, `sop_id uuid not null references sops(id) on delete cascade`, `sop_version int not null` (auto-stamped by the server action per D-10), `observed_worker_id uuid not null references auth.users(id) on delete cascade`, `observed_by uuid references auth.users(id) on delete set null`, `verdict text not null check (verdict in ('performed_to_sop', 'needs_support'))` (D-01 canonical values, DB-level defense-in-depth), `note text`, `completion_id uuid references sop_completions(id) on delete set null` (D-11 optional link), `created_at timestamptz not null default now()`. Add indexes on `observed_worker_id`, `organisation_id`, and `sop_id`.

    Enable RLS. Add exactly two policies (copy 00043's idiom):
    - `sop_observations_read_org` FOR SELECT TO authenticated USING `organisation_id = public.current_organisation_id() OR observed_worker_id = auth.uid()`. The `observed_worker_id = auth.uid()` OR-branch is the NEW OBS-02 worker self-read guarantee (no precedent in 00043) — it matches ONLY the caller's own id, so a worker can never read another worker's observations.
    - `sop_observations_insert_recorder` FOR INSERT TO authenticated WITH CHECK `organisation_id = public.current_organisation_id() AND public.current_user_role() in ('admin', 'safety_manager', 'supervisor') AND observed_by = auth.uid()` (D-04 three recorder roles).
    - Add explicit `-- NO UPDATE policy — append-only (D-12)` and `-- NO DELETE policy — append-only (D-12)` comments; add NO such policies.

    Then add the D-02 org-label column: `alter table public.organisations add column if not exists observation_labels jsonb`. Comment it as `{ performed_to_sop: "<label>", needs_support: "<label>" }`, display-only, canonical values never derived from it. No new RLS on organisations — the existing admin-update policy from foundation gates the write (34-04 self-enforces org scope regardless).

    Header comment must cite: models 00043 § sop_review_events; widened to 3 roles (D-04); adds worker self-read branch (OBS-02); D-01 verdict check; D-10 version stamp; D-11 optional completion link; D-12 append-only.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/00052_supervisor_observations.sql | grep -Eic "for +update|for +delete" | grep -qx 0 && echo "NO update/delete policy OK"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `create table if not exists public.sop_observations`
    - Contains `check (verdict in ('performed_to_sop', 'needs_support'))`
    - Contains policy `sop_observations_read_org` with `observed_worker_id = auth.uid()` in the USING clause
    - Contains policy `sop_observations_insert_recorder` with `current_user_role() in ('admin', 'safety_manager', 'supervisor')` and `observed_by = auth.uid()`
    - Contains NO `for update` and NO `for delete` policy on sop_observations (grep of non-comment lines returns 0)
    - Contains `alter table public.organisations add column if not exists observation_labels jsonb`
    - Does NOT redefine `current_organisation_id()` or `current_user_role()`
  </acceptance_criteria>
  <done>Migration 00052 defines the append-only, org-scoped, worker-self-readable observations table + org-label column, ready for live push in 34-03.</done>
</task>

<task type="auto">
  <name>Task 2: RecordObservationSchema Zod validator</name>
  <read_first>
    - src/lib/validators/completions.ts (mirror the file-header doc-comment convention + SignOffSchema shape)
    - .planning/phases/34-supervisor-observations/34-PATTERNS.md (§ src/lib/validators/observations.ts — the final schema to reproduce)
  </read_first>
  <files>src/lib/validators/observations.ts</files>
  <action>
    Export `VerdictSchema = z.enum(['performed_to_sop', 'needs_support'])` and `type Verdict`. Export `RecordObservationSchema = z.object({ workerId: z.string().uuid(), sopId: z.string().uuid(), verdict: VerdictSchema, note: z.string().max(2000).optional(), completionId: z.string().uuid().optional() })` and `type RecordObservationInput`. File-header doc-comment must name the table this feeds (`sop_observations`) and cite migration 00052. The Zod enum mirrors the DB `check` constraint as V5 input validation (defense-in-depth, per 34-RESEARCH Security Domain V5).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "validators/observations" | grep -qc . && echo "TS ERROR" || echo "tsc clean for observations validator"</automated>
  </verify>
  <acceptance_criteria>
    - Exports `VerdictSchema`, `RecordObservationSchema`, and their inferred types
    - `note` is capped at 2000 chars and optional; `completionId` optional; `workerId`/`sopId` are `.uuid()`
    - `npx tsc --noEmit` reports no error in this file
  </acceptance_criteria>
  <done>The observation input contract exists and type-checks, ready for import by 34-04's server action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RLS insert | Untrusted verdict/note/ids cross into the DB; the RLS WITH CHECK + Zod + column `check` are the gates |
| org A ↔ org B | Cross-tenant read/write must be impossible at the data layer |
| worker → own observations | A worker may read rows about themself, never about another worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Tampering / EoP | sop_observations INSERT | mitigate | RLS WITH CHECK binds `organisation_id = current_organisation_id()` + role-array + `observed_by = auth.uid()` — the recurring cross-tenant write-hole class (CLAUDE.md 2026-06-15/26) is closed at the data layer, no service-role write exists |
| T-34-02-02 | Information Disclosure | sop_observations SELECT | mitigate | `observed_worker_id = auth.uid()` OR-branch matches ONLY the caller's own id (never widened to `= any(...)`); org branch scopes everything else |
| T-34-02-03 | Repudiation | append-only contract | mitigate | No UPDATE and no DELETE policy — a future edit/delete requires a new migration, making the trade-off reviewable (verified by Task 1 grep) |
| T-34-02-04 | Tampering | verdict value | mitigate | Postgres `check (verdict in (...))` + Zod enum — two layers reject any non-canonical value |
| T-34-02-SC | Tampering | npm installs | accept | Zero new packages this phase (34-RESEARCH Package Legitimacy Audit) — supply-chain vector N/A |
</threat_model>

<verification>
- Migration grep: no `for update`/`for delete` policy on sop_observations
- Migration contains both RLS policies with the exact org + role + auth.uid() clauses
- `npx tsc --noEmit` clean for the validator
</verification>

<success_criteria>
The append-only observations schema + org-label column + Zod contract exist as files, faithfully modeled on 00043, ready for the [BLOCKING] live push in 34-03.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: migration `00052_supervisor_observations.sql`; table `public.sop_observations`; column `public.organisations.observation_labels`; RLS policies `sop_observations_read_org`, `sop_observations_insert_recorder`; validator module `src/lib/validators/observations.ts` exporting `VerdictSchema`, `RecordObservationSchema`, `Verdict`, `RecordObservationInput`.
(Phase-wide set is enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-02-SUMMARY.md` when done.
</output>
