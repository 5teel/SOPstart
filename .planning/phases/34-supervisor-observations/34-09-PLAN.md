---
phase: 34-supervisor-observations
plan: 09
type: execute
wave: 6
depends_on: [34-06, 34-07, 34-08]
files_modified:
  - .planning/phases/34-supervisor-observations/34-09-SUMMARY.md
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "The full test suite is green on the merged tree (all waves integrated)"
    - "npx tsc --noEmit and a real npm run build both pass (next build catches what tsc alone misses)"
    - "/pathways shows 0 not-mapped for the phase's routes"
    - "OBS-01/02/03 + success criterion 4 each have implementation evidence audited PASS"
  artifacts:
    - path: ".planning/phases/34-supervisor-observations/34-09-SUMMARY.md"
      provides: "phase gate audit + requirement-by-requirement evidence"
  key_links:
    - from: "merged tree"
      to: "phase gate"
      via: "full suite + tsc + next build + requirement audit"
      pattern: "OBS-0"
---

<objective>
Merged-tree phase gate: with all waves integrated, run the full Playwright suite, `npx tsc --noEmit`, and a real `npm run build` (next build — the 2026-06-27 learning: tsc green is necessary, not sufficient; a `'use server'` sync export or route-only break only surfaces here), confirm `/pathways` shows 0 not-mapped for the phase's routes, and audit OBS-01/02/03 + success criterion 4 against shipped code with evidence.

Purpose: Prove the phase is coherent as a whole, not just plan-by-plan — the pattern used by every recent phase's final gate.
Output: `34-09-SUMMARY.md` with the requirement-by-requirement audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-supervisor-observations/34-CONTEXT.md
@.planning/phases/34-supervisor-observations/34-VALIDATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + tsc + next build on the merged tree</name>
  <read_first>
    - playwright.config.ts (project list — full suite entry point)
    - CLAUDE.md § Learnings (2026-06-27 — next build catches `'use server'` sync-export / route-only failures that tsc misses; run npm run build as the final gate)
  </read_first>
  <files>.planning/phases/34-supervisor-observations/34-09-SUMMARY.md</files>
  <action>
    Run `npm run test` (full suite) — record phase34 project result and confirm no regressions in adjacent projects (phase28 governance, phase32/33 org-model, activity/profile). Run `npx tsc --noEmit`. Run `npm run build` (next build) and confirm a clean production build — this is the mandatory final gate (observations.ts is a `'use server'` module; a stray sync export would only break here, and the settings/profile/team route edits are only exercised by next build). Record all three outcomes in the SUMMARY; if any fails, fix-forward on the merged tree before declaring done.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` full suite green (phase34 + no adjacent regressions)
    - `npx tsc --noEmit` clean
    - `npm run build` completes a clean production build
    - Results recorded in 34-09-SUMMARY.md
  </acceptance_criteria>
  <done>The merged tree is green across suite, types, and a real production build.</done>
</task>

<task type="auto">
  <name>Task 2: Requirement audit + pathways coverage</name>
  <read_first>
    - .planning/REQUIREMENTS.md (§ v7.0 — OBS-01/02/03 wording)
    - .planning/ROADMAP.md (§ Phase 34 — success criteria 1-4)
    - src/lib/journeys/journeys.ts (confirm the phase flows are on real routes)
  </read_first>
  <files>.planning/phases/34-supervisor-observations/34-09-SUMMARY.md</files>
  <action>
    Audit each requirement against shipped code with a file/behaviour citation: OBS-01 (recordObservation role gate + append-only migration + both entry points), OBS-02 (worker /profile section + self-read RLS branch + trust banner), OBS-03 (observation feeds the person panel + worker profile + sop_version auto-stamp), and success criterion 4 (cross-org isolation runtime test — live or documented Railway-only carry). Confirm `/pathways` shows 0 not-mapped for any route this phase touched (Phase 34 adds no new routes, so coverage should be unaffected — state that explicitly). Record the audit table + any carried residuals (e.g. Railway-only runtime carries) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -qE "OBS-01|OBS-02|OBS-03" .planning/phases/34-supervisor-observations/34-09-SUMMARY.md && echo "audit present"</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains a per-requirement PASS/evidence table for OBS-01/02/03 + SC-4
    - /pathways coverage confirmed 0 not-mapped for phase routes (or the no-new-routes rationale stated)
    - Any Railway-only runtime carries are documented as residuals
  </acceptance_criteria>
  <done>Every phase-34 requirement is audited PASS with evidence; pathways coverage confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan-by-plan green → phase-coherent | Individual plans passing does not prove the integrated tree builds and behaves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-09-01 | Repudiation | false-positive verification | mitigate | Real `npm run build` gate (2026-06-27) — not just tsc — on the merged tree |
| T-34-09-02 | EoP / Info Disclosure | cross-org regression | mitigate | Full-suite run re-executes the SC-4 cross-org + immutability specs on the integrated tree |
| T-34-09-03 | Repudiation | uncited requirement | mitigate | Per-requirement audit table with file/behaviour evidence in the SUMMARY |
</threat_model>

<verification>
- `npm run test` full suite green
- `npx tsc --noEmit` clean
- `npm run build` clean
- Requirement audit table present in SUMMARY
</verification>

<success_criteria>
The merged Phase 34 tree is green across suite, types, and production build; OBS-01/02/03 + SC-4 are audited PASS with evidence; pathways coverage confirmed.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces the phase gate audit (`34-09-SUMMARY.md`) — no new source symbols. (Phase-wide artifact set enumerated in 34-01-PLAN.)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/34-supervisor-observations/34-09-SUMMARY.md` when done.
</output>
