---
phase: 09-streamlined-file-video-pipeline
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/pipeline-entry.test.ts
  - tests/pipeline-linkage.test.ts
  - tests/pipeline-autoqueue.test.ts
  - tests/pipeline-progress.test.ts
  - tests/pipeline-failure-recovery.test.ts
  - tests/pipeline-review-gate.test.ts
  - playwright.config.ts
autonomous: true
requirements: [PATH-01, PATH-02, PATH-03, PATH-04, PATH-05, PATH-06]
must_haves:
  truths:
    - "Six Playwright test stub files exist, one per PATH requirement"
    - "Each test file contains at least one test.fixme() block naming the PATH requirement"
    - "playwright.config.ts has a phase9-stubs project matching all six new files"
    - "npx playwright test --list prints the phase9-stubs project with ~6 tests"
  artifacts:
    - path: "tests/pipeline-entry.test.ts"
      provides: "PATH-01 stubs"
      contains: "test.fixme"
    - path: "tests/pipeline-linkage.test.ts"
      provides: "PATH-02 stubs"
      contains: "test.fixme"
    - path: "tests/pipeline-autoqueue.test.ts"
      provides: "PATH-03 stubs"
      contains: "test.fixme"
    - path: "tests/pipeline-progress.test.ts"
      provides: "PATH-04 stubs"
      contains: "test.fixme"
    - path: "tests/pipeline-failure-recovery.test.ts"
      provides: "PATH-05 stubs"
      contains: "test.fixme"
    - path: "tests/pipeline-review-gate.test.ts"
      provides: "PATH-06 stubs"
      contains: "test.fixme"
    - path: "playwright.config.ts"
      provides: "phase9-stubs project entry"
      contains: "phase9-stubs"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/pipeline-*.test.ts"
      via: "testMatch regex"
      pattern: "phase9-stubs"
---

<objective>
Create Playwright integration test stubs for all six PATH-XX requirements and
register a phase9-stubs project so the downstream implementation plans have a
place to fill in real assertions.

Purpose: Wave 0 test inventory — mirrors phase2-stubs, phase6-stubs, phase8-stubs
conventions. All stubs use test.fixme() so they list in the inventory without
failing CI.

Output: 6 stub files plus a new project entry in playwright.config.ts.
</objective>

<execution_context>
@C:/Users/Simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-streamlined-file-video-pipeline/09-CONTEXT.md
@.planning/phases/09-streamlined-file-video-pipeline/09-UI-SPEC.md
@playwright.config.ts
@tests/stage-progress.test.ts
</context>

<interfaces>
Existing Playwright stub convention (from tests/stage-progress.test.ts):

```typescript
import { test, expect } from '@playwright/test'

test.describe('<Feature name> (<REQ-ID>)', () => {
  test.fixme('<behavior description>', async ({ page }) => {})
})
```

Existing playwright.config.ts project shape:

```typescript
{
  name: 'phase{N}-stubs',
  testMatch: /regex-matching-test-filenames/,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create 6 PATH requirement stub files</name>
  <files>tests/pipeline-entry.test.ts, tests/pipeline-linkage.test.ts, tests/pipeline-autoqueue.test.ts, tests/pipeline-progress.test.ts, tests/pipeline-failure-recovery.test.ts, tests/pipeline-review-gate.test.ts</files>
  <read_first>
    - tests/stage-progress.test.ts (reference stub format)
    - .planning/phases/09-streamlined-file-video-pipeline/09-CONTEXT.md (decisions that become test assertions later)
    - .planning/phases/09-streamlined-file-video-pipeline/09-UI-SPEC.md (copy strings to assert on)
  </read_first>
  <action>
    Create 6 stub files using `import { test, expect } from '@playwright/test'` and `test.fixme()` blocks. Exact contents:

    **tests/pipeline-entry.test.ts** (PATH-01):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('File-to-video pipeline entry (PATH-01)', () => {
      test.fixme('UploadDropzone shows "Generate video SOP" button next to Browse video', async ({ page }) => {})
      test.fixme('clicking "Generate video SOP" opens the format selection modal', async ({ page }) => {})
      test.fixme('modal lists Narrated slideshow and Screen-recording style options', async ({ page }) => {})
      test.fixme('"Start pipeline" CTA is disabled until a format is selected', async ({ page }) => {})
      test.fixme('confirming format dispatches upload and navigates to /admin/sops/pipeline/[pipelineId]', async ({ page }) => {})
      test.fixme('Discard button dismisses modal without creating a pipeline', async ({ page }) => {})
    })
    ```

    **tests/pipeline-linkage.test.ts** (PATH-02):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Pipeline state linkage (PATH-02)', () => {
      test.fixme('starting a pipeline creates a sop_pipeline_runs row with requested_video_format', async ({ page }) => {})
      test.fixme('the created parse_jobs row references the pipeline_run_id', async ({ page }) => {})
      test.fixme('the created sops row references the pipeline_run_id', async ({ page }) => {})
      test.fixme('video_generation_jobs row created by auto-queue references the same pipeline_run_id', async ({ page }) => {})
      test.fixme('pipeline_run_id is scoped to the uploading user organisation', async ({ page }) => {})
    })
    ```

    **tests/pipeline-autoqueue.test.ts** (PATH-03):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Publish auto-queues video generation (PATH-03)', () => {
      test.fixme('publishing a pipeline-linked SOP enqueues a video_generation_jobs row', async ({ page }) => {})
      test.fixme('enqueued job uses the pipeline format (narrated_slideshow or screen_recording)', async ({ page }) => {})
      test.fixme('enqueued job carries the pipeline_run_id from the parse job', async ({ page }) => {})
      test.fixme('publishing a non-pipeline SOP does NOT auto-queue a video job', async ({ page }) => {})
      test.fixme('publish succeeds even if enqueue path has internal error — SOP publish is not rolled back', async ({ page }) => {})
    })
    ```

    **tests/pipeline-progress.test.ts** (PATH-04):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Unified pipeline progress page (PATH-04)', () => {
      test.fixme('progress page renders 5 named stages: Uploading, Parsing, Review, Generating video, Ready', async ({ page }) => {})
      test.fixme('active stage label uses text-brand-yellow font-semibold with aria-current="step"', async ({ page }) => {})
      test.fixme('Review stage shows orange checkpoint panel with "Review SOP now" CTA that deep-links to /admin/sops/[sopId]/review', async ({ page }) => {})
      test.fixme('Ready stage shows green panel with "Preview and publish video" CTA that deep-links to /admin/sops/[sopId]/video', async ({ page }) => {})
      test.fixme('progress page subscribes to realtime and falls back to 5s polling after 5s', async ({ page }) => {})
      test.fixme('review page shows "Back to pipeline" link when opened from the pipeline flow', async ({ page }) => {})
    })
    ```

    **tests/pipeline-failure-recovery.test.ts** (PATH-05):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Pipeline failure recovery (PATH-05)', () => {
      test.fixme('video generation failure after publish keeps the SOP status=published', async ({ page }) => {})
      test.fixme('progress page renders "Video generation failed" panel with "Go to video panel" CTA', async ({ page }) => {})
      test.fixme('"Go to video panel" navigates to /admin/sops/[sopId]/video where retry button exists', async ({ page }) => {})
      test.fixme('parse failure on pipeline-linked SOP shows existing ParseJobStatus failed state', async ({ page }) => {})
    })
    ```

    **tests/pipeline-review-gate.test.ts** (PATH-06):
    ```typescript
    import { test, expect } from '@playwright/test'

    test.describe('Publish-time review gate preserved (PATH-06)', () => {
      test.fixme('POST /api/sops/[sopId]/publish still counts unapproved sections and returns 400 if any remain', async ({ page }) => {})
      test.fixme('auto-queue path runs ONLY after server-side publish gate passes', async ({ page }) => {})
      test.fixme('skipping review (DB-level publish flip) does NOT trigger the auto-queue server code path', async ({ page }) => {})
    })
    ```

    Each file is independent — no imports from src/. All tests use test.fixme() so they are visible in the inventory but do not run.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase9-stubs 2>&1 | grep -c "fixme" | grep -q "^[0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist under tests/ with names pipeline-entry.test.ts, pipeline-linkage.test.ts, pipeline-autoqueue.test.ts, pipeline-progress.test.ts, pipeline-failure-recovery.test.ts, pipeline-review-gate.test.ts
    - Each file contains exactly one top-level test.describe block
    - Each file uses test.fixme() on every test (no test.skip, no test())
    - Each test.describe title includes the PATH-0N requirement ID
    - Each file imports from '@playwright/test' (not relative imports)
  </acceptance_criteria>
  <done>All 6 stub files created with required structure</done>
</task>

<task type="auto">
  <name>Task 2: Register phase9-stubs project in playwright.config.ts</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (existing project entries for phase2-stubs, phase3-stubs, phase6-stubs, phase8-stubs)
  </read_first>
  <action>
    Add a new project entry to the projects array in playwright.config.ts, placed after the phase8-stubs entry:

    ```typescript
    {
      name: 'phase9-stubs',
      testMatch: /pipeline-entry|pipeline-linkage|pipeline-autoqueue|pipeline-progress|pipeline-failure-recovery|pipeline-review-gate/,
    },
    ```

    Keep existing project entries intact. Do not modify testDir, timeout, retries, or baseURL.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase9-stubs 2>&1 | grep -E "pipeline-(entry|linkage|autoqueue|progress|failure-recovery|review-gate)"</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts contains exactly one 'phase9-stubs' project entry
    - testMatch regex contains all 6 basenames: pipeline-entry, pipeline-linkage, pipeline-autoqueue, pipeline-progress, pipeline-failure-recovery, pipeline-review-gate
    - Running `npx playwright test --list --project=phase9-stubs` lists tests from all 6 files
    - Existing projects (integration, e2e, phase2-stubs, phase3-stubs, phase6-stubs, phase8-stubs) are unchanged
  </acceptance_criteria>
  <done>phase9-stubs project discoverable and lists all 6 stub files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (this plan) | Test stubs are inert — no runtime surface area, no data read/write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | I (Info disclosure) | Test files committed to repo | accept | Stubs contain no secrets, no real org IDs, no fixtures with PII |
| T-09-00-02 | T (Tampering) | playwright.config.ts | accept | Only adds one new project entry; existing projects untouched |
</threat_model>

<verification>
- `npx playwright test --list --project=phase9-stubs` lists ≥22 tests across 6 files
- No existing tests in other projects stop running
</verification>

<success_criteria>
Wave 0 complete: all six PATH-XX requirements have a dedicated test stub file
registered in Playwright. Implementation plans 01–04 fill these in as they land.
</success_criteria>

<output>
After completion, create `.planning/phases/09-streamlined-file-video-pipeline/09-00-SUMMARY.md`
</output>
