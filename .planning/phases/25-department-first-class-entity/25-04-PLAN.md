---
phase: 25-department-first-class-entity
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - src/components/admin/departments/DepartmentCard.tsx
  - src/components/admin/departments/DepartmentGrid.tsx
  - src/components/admin/departments/DepartmentFormModal.tsx
  - src/app/(protected)/admin/departments/page.tsx
  - tests/e2e/admin-departments.spec.ts
  - playwright.config.ts
autonomous: false
requirements: [REQ-1, REQ-5, REQ-6, D-03]
user_setup: []

must_haves:
  truths:
    - "/admin/departments renders one card per department with colour stripe, code, owner line, and live People/SOPs/Blocks counts (REQ-6)"
    - "Create adds a department; archive hides it from active filters without deleting junction history (REQ-6)"
    - "A department with no owner renders the explicit red No owner assigned warning state (REQ-5, D-03)"
    - "The create/edit form sets owner from an org-member selector and a fixed 8-colour swatch picker (REQ-5, V5)"
    - "Page is admin/safety_manager-gated and org-scoped (REQ-1)"
    - "admin-departments.spec.ts appears in `npx playwright test --list` (registered, not orphaned — 2026-05-25 unregistered-spec guard)"
  artifacts:
    - path: "src/app/(protected)/admin/departments/page.tsx"
      provides: "SSR departments route with auth guard + listDepartments + sub-nav"
      contains: "listDepartments"
    - path: "src/components/admin/departments/DepartmentCard.tsx"
      provides: "card with stripe/owner-block/stats + no-owner warning"
      contains: "No owner assigned"
    - path: "src/components/admin/departments/DepartmentGrid.tsx"
      provides: "2-col grid + add-card + create/edit modal wiring + show-archived toggle"
      contains: "NEW DEPARTMENT"
    - path: "tests/e2e/admin-departments.spec.ts"
      provides: "page-load smoke + one card-render assertion for /admin/departments"
      contains: "admin/departments"
  key_links:
    - from: "src/app/(protected)/admin/departments/page.tsx"
      to: "src/actions/departments.ts"
      via: "listDepartments() SSR fetch"
      pattern: "listDepartments"
    - from: "src/components/admin/departments/DepartmentGrid.tsx"
      to: "src/actions/departments.ts"
      via: "createDepartment/updateDepartment/archiveDepartment/setDepartmentOwner"
      pattern: "(create|update|archive)Department"
---

<objective>
Ship Surface 1: the `/admin/departments` management page — a 2-column card grid where each department shows its colour stripe, code, owner line (or the red "No owner assigned" warning), and live People/SOPs/Blocks counts, with create/edit/archive and owner-set via a form modal. Implements REQ-6 and the D-03 owner-accountability visibility (the Visy "nobody owns SOPs" gap surfaced as a feature).

Purpose: This is the organising spine surface — the canonical place to define departments that the block library, team, and wizard all point back to.
Output: 3 components + 1 route + 1 registered e2e smoke spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-UI-SPEC.md
@.planning/phases/25-department-first-class-entity/25-PATTERNS.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
@src/app/(protected)/admin/blocks/page.tsx
@src/types/sop.ts
</context>

<artifacts_this_phase_produces>
- **Components:** `DepartmentCard.tsx`, `DepartmentGrid.tsx`, `DepartmentFormModal.tsx`
- **Route:** `/admin/departments` (`src/app/(protected)/admin/departments/page.tsx`)
- **Test:** `tests/e2e/admin-departments.spec.ts` (created AND registered in playwright.config.ts by Task 3)
- (Consumes: `listDepartments`/`createDepartment`/`updateDepartment`/`archiveDepartment`/`setDepartmentOwner` from Plan 03; `DChip` not required here — owner block uses the `.owner` pattern, not DChip)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: DepartmentCard + DepartmentFormModal</name>
  <read_first>
    - src/components/admin/blocks/BlockListTable.tsx (role-match analog: row token usage + archive button pattern)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 145-254 — full card anatomy: 6px stripe, .cdot, owner block filled + .owner.empty red warning, 3-cell stats, add-card, archived distinction; 234-247 create/edit form fields; 624-652 copywriting)
    - .claude/skills/sketch-findings-SOPstart/SKILL.md (validated tokens — JetBrains Mono, paper/ink palette)
    - src/types/sop.ts (DepartmentWithCounts)
  </read_first>
  <action>
    Create `src/components/admin/departments/DepartmentCard.tsx` taking a `DepartmentWithCounts` + handlers. Render per UI-SPEC: 6px left colour stripe (department.colour), `.inner` 18px padding, top row with 26×26 `.cdot` (colour bg + icon/letter), name (15px/700), code line ("{CODE} · department", 10px uppercase var(--ink-500)), and a `⋯` overflow button (edit/archive). Owner block: filled state (24×24 ink-900 avatar initials, "OWNER" 9px label, "{Name} · {Role}") OR `.owner.empty` no-owner warning (dashed var(--accent-hazard) border, rgba(239,68,68,0.10) red `!` avatar, red "No owner assigned — set one" text) when owner_user_id is null (REQ-5, D-03). Stats row: 3 equal cells People/SOPs/Blocks (17px/700 value, 9px uppercase label). Archived cards render at opacity 0.5 with greyed stripe + "ARCHIVED" label. Create `src/components/admin/departments/DepartmentFormModal.tsx` (reuse the project's existing admin modal pattern) with fields Name (required), Code (≤6 auto-uppercase, required), Colour (fixed 8-swatch radio picker — the UI-SPEC palette, no free input), Icon (optional emoji/char), Owner (searchable org-member selector, optional). Wire create/update via the Plan 03 actions; surface the duplicate-code error copy from UI-SPEC.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/admin/departments/DepartmentCard.tsx','utf8');['No owner assigned','accent-hazard','OWNER','people_count'].forEach(s=>{if(!c.includes(s)){console.error('Card missing',s);process.exit(1)}});const m=fs.readFileSync('src/components/admin/departments/DepartmentFormModal.tsx','utf8');if(!/colou?r/i.test(m)||!/code/i.test(m)){console.error('FormModal missing fields');process.exit(1)};console.log('card+modal OK')" && npx tsc --noEmit 2>&1 | (grep -E "departments/Department(Card|FormModal)" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - DepartmentCard renders stripe + cdot + code + owner block + 3-cell stats per UI-SPEC
    - No-owner case renders the red "No owner assigned — set one" warning (REQ-5, D-03) — grep finds the exact copy + var(--accent-hazard)
    - FormModal presents a fixed 8-swatch colour picker (no free-form input, V5) + member owner selector
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The card (incl. governance-gap warning) and the create/edit form exist to UI-SPEC fidelity.</done>
</task>

<task type="auto">
  <name>Task 2: DepartmentGrid + /admin/departments route + shared sub-nav</name>
  <read_first>
    - src/app/(protected)/admin/blocks/page.tsx (EXACT analog: lines 1-58 SSR auth guard + admin/safety_manager redirect + Promise.all fetch; the existing sub-nav to extend)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 698-750 page + sub-nav skeleton)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 145-156 layout; 247-254 empty state; 575-584 shared sub-nav; 587-594 responsive)
  </read_first>
  <action>
    Create `src/components/admin/departments/DepartmentGrid.tsx` (client) rendering a `grid-template-columns: 1fr 1fr` (single col < 768px) of DepartmentCards + the dashed add-card ("＋" / "NEW DEPARTMENT"), a "Show archived" toggle when archived depts exist, the empty state copy when zero depts, and the DepartmentFormModal open/close state (both the header "＋ New department" button and the add-card open it). Create `src/app/(protected)/admin/departments/page.tsx` (SSR): copy the blocks/page auth guard (redirect non-admin/safety_manager to /dashboard — REQ-1 org+role gate), `await listDepartments()`, render `<main className="max-w-[1000px] ...">` with h1 "Departments" + sub-heading copy + the shared sub-nav (SOPs | Library | Team | Departments, Departments active) + `<DepartmentGrid>`. Add the "Departments" tab to the shared admin sub-nav so it appears on this page (the other pages get it in Plan 05/06). Use the exact page h1/sub-heading/CTA copy from UI-SPEC copywriting.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const g=fs.readFileSync('src/components/admin/departments/DepartmentGrid.tsx','utf8');['NEW DEPARTMENT','DepartmentCard'].forEach(s=>{if(!g.includes(s)){console.error('Grid missing',s);process.exit(1)}});const p=fs.readFileSync('src/app/(protected)/admin/departments/page.tsx','utf8');['listDepartments','dashboard','Departments'].forEach(s=>{if(!p.includes(s)){console.error('page missing',s);process.exit(1)}});console.log('grid+route OK')" && npx tsc --noEmit 2>&1 | (grep -E "admin/departments/(page|.*DepartmentGrid)" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/departments/page.tsx` exists, guards to admin/safety_manager + redirects others to /dashboard (REQ-1), calls listDepartments
    - DepartmentGrid renders cards + add-card + show-archived toggle + empty state
    - Shared sub-nav includes the new "Departments" tab
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The route renders the live department grid with create/edit/archive entry points behind the admin guard.</done>
</task>

<task type="auto">
  <name>Task 3: Create + register the /admin/departments e2e smoke spec</name>
  <read_first>
    - playwright.config.ts (self — the `projects` array; the 2026-05-25 learning: a spec file not matched by ANY project's testMatch regex NEVER runs. There is no phase25 project yet — Plan 02/03 add an integration-style entry for the *.spec.ts tests; this e2e spec lives under tests/e2e and needs its filename matched by a chromium e2e project)
    - tests/e2e/offline-indicator.spec.ts (analog: the existing e2e spec shape — page goto + a render assertion; matches the existing `e2e` project's `/offline-indicator/` regex pattern)
    - scripts/uat-magic-link.mjs (analog: installing an authenticated admin session for the page-load smoke, magic-link cookie per the 2026-04-24 learning)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 145-156 — the card grid markup the assertion targets)
  </read_first>
  <action>
    Create `tests/e2e/admin-departments.spec.ts`: an authenticated (admin magic-link session per scripts/uat-magic-link.mjs) page-load smoke that navigates to `/admin/departments`, asserts the page loads (status 200 / h1 "Departments" visible — NOT the unhandled-route screen), and makes ONE card-render assertion (at least one DepartmentCard renders — e.g. the General department auto-created by the Plan 02 migration is present, asserting the colour stripe + code + a People/SOPs/Blocks stat cell). Keep it a smoke (no create/archive flow — that is the human-verify checkpoint's job). Then REGISTER the spec in `playwright.config.ts`: add a new `phase25-e2e` project (or extend the existing `e2e` project's `testMatch` regex) whose `testMatch` includes `admin-departments`, with `use: { browserName: 'chromium' }` and a project comment citing the 2026-05-25 unregistered-spec learning (mirror the existing per-phase project comments). Do NOT leave it relying only on the global `testMatch: ['**/*.spec.ts']` — per the existing config every spec is gated behind a per-project regex, so an unregistered filename is invisible to `npm run test`. Validate registration: `npx playwright test --list` must show `admin-departments`.
  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('tests/e2e/admin-departments.spec.ts');const c=require('fs').readFileSync('playwright.config.ts','utf8');if(!/admin-departments/.test(c)){console.error('admin-departments not registered in playwright.config.ts');process.exit(1)};console.log('spec file + config entry present')" && npx playwright test --list 2>&1 | (grep -q "admin-departments" && echo "spec registered" || (echo "admin-departments NOT in playwright test --list"; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/admin-departments.spec.ts` exists: authenticated page-load smoke for `/admin/departments` + one card-render assertion (REQ-6)
    - The spec's filename is matched by a project `testMatch` in `playwright.config.ts` (a phase25-e2e project or the extended e2e regex), with a comment citing the 2026-05-25 unregistered-spec learning
    - `npx playwright test --list | grep admin-departments` is non-empty (registration proven, 2026-05-25 guard satisfied)
  </acceptance_criteria>
  <done>The e2e smoke for /admin/departments exists AND is registered in playwright.config.ts so it actually runs under `npm run test`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify /admin/departments on the deployed app</name>
  <action>On the deployed app, sign in as admin and verify /admin/departments renders cards with live counts, the red no-owner warning on ownerless departments (clearing on owner-set), and working create/archive — per the how-to-verify steps below.</action>
  <what-built>Built `/admin/departments`: SSR page + DepartmentGrid + DepartmentCard (with the red no-owner warning) + DepartmentFormModal, plus a registered e2e smoke spec (`tests/e2e/admin-departments.spec.ts`, registered in playwright.config.ts per Task 3).</what-built>
  <how-to-verify>
    1. On sopstart.com (or the deployed preview), sign in as an admin and visit `/admin/departments`.
    2. Confirm: one card per department with colour stripe, code, owner line, and People/SOPs/Blocks counts (counts non-zero after the Plan 02 migration assigned everything to General).
    3. The General department (auto-created by migration) should show a red "No owner assigned — set one" warning until you set an owner.
    4. Click "＋ New department", create one (name, code, a colour swatch, optional icon, optional owner) — it appears as a card.
    5. Set an owner on a card → the red warning clears and the owner line shows "{Name} · {Role}".
    6. Archive a department → it leaves the active grid; "Show archived" reveals it greyed; its junction history is preserved (counts still computed).
  </how-to-verify>
  <acceptance_criteria>
    - Cards render with correct live counts (REQ-6)
    - No-owner warning visible on ownerless departments and clears on owner-set (REQ-5, D-03)
    - Create + archive work; archived hidden from active filters without data loss (REQ-6)
    - `tests/e2e/admin-departments.spec.ts` passes (page smoke) and is listed by `npx playwright test --list`
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/departments | Admin-only surface; non-admins must not reach it |
| form input → createDepartment | Colour/code/owner inputs validated server-side (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | non-org/non-admin reaching another org's departments (T-HIGH) | mitigate | page auth guard redirects non-admin/safety_manager to /dashboard; listDepartments is org-scoped by RLS (REQ-1) |
| T-25-05 | Tampering | owner UI implying edit/approve power (T-MED) | mitigate | D-03: card/form present owner as accountability label only; no edit/approve affordance is gated on ownership in this surface |
| T-25-08 | Input Validation | free-form colour via the form (T-MED) | mitigate | colour is a fixed 8-swatch radio picker bound to the Plan 03 z.enum (V5) |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | no new packages |
</threat_model>

<verification>
- grep + `npx tsc --noEmit` guards on the 3 components + route
- e2e smoke `admin-departments.spec.ts` created AND registered (Task 3 — `npx playwright test --list | grep admin-departments` non-empty), REQ-6 page load
- Human-verify checkpoint on sopstart.com (counts, no-owner warning, create/archive) — per the project's Railway-only/runtime-UAT learnings, runtime rendering is verified in the deployed app, not local dev
</verification>

<success_criteria>
- `/admin/departments` renders cards with counts, owner accountability (incl. red warning), and create/edit/archive (REQ-6, REQ-5)
- Page is admin-gated + org-scoped (REQ-1)
- The e2e smoke spec is registered in playwright.config.ts and appears in `npx playwright test --list`
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-04-SUMMARY.md` when done
</output>
</output>
