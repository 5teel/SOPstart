---
phase: 25-department-first-class-entity
plan: 06
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - src/components/admin/RoleAssignmentTable.tsx
  - src/app/(protected)/admin/team/page.tsx
  - src/app/(protected)/admin/sops/new/blank/WizardClient.tsx
  - src/app/(protected)/admin/sops/new/blank/page.tsx
  - src/app/(protected)/admin/sops/new/ai/PromptClient.tsx
  - src/app/(protected)/admin/sops/new/ai/page.tsx
  - tests/integration/wizard-sop-dept.spec.ts
  - playwright.config.ts
autonomous: false
requirements: [REQ-4, REQ-5, REQ-9, D-03, D-04]
user_setup: []

must_haves:
  truths:
    - "Assigning a member to a department from /admin/team persists a member_departments row; removing one removes them from that filter only (REQ-4)"
    - "A member who owns a department shows the amber ★ Owns {Dept} badge; setting/clearing owner from the team picker drives the departments-page warning (REQ-5, D-03)"
    - "The team page shows a department filter bar + a Departments column with chips + the dashed add button (REQ-4 surface)"
    - "Creating a SOP via the blank wizard with two departments selected persists two sop_departments rows; All departments sets sops.all_departments (REQ-9, D-04)"
    - "The AI-draft SOP path exposes the same department field and writes sop_departments (REQ-9, A3)"
  artifacts:
    - path: "src/components/admin/RoleAssignmentTable.tsx"
      provides: "departments prop + Departments column + member-mode DepartmentPicker + owner badge"
      contains: "DepartmentPicker"
    - path: "src/app/(protected)/admin/sops/new/blank/WizardClient.tsx"
      provides: "department multi-select (localOnly) wired into createSopFromWizard"
      contains: "departmentIds"
    - path: "tests/integration/wizard-sop-dept.spec.ts"
      provides: "wizard writes sop_departments assertion (REQ-9)"
      contains: "sop_departments"
  key_links:
    - from: "src/components/admin/RoleAssignmentTable.tsx"
      to: "src/components/admin/departments/DepartmentPicker.tsx"
      via: "member-mode picker with inline owner-set"
      pattern: "DepartmentPicker"
    - from: "src/app/(protected)/admin/sops/new/blank/WizardClient.tsx"
      to: "src/actions/sops.ts"
      via: "createSopFromWizard({ departmentIds, allDepartments })"
      pattern: "createSopFromWizard"
---

<objective>
Ship Surface 3 and Surface 4: extend `/admin/team` (`RoleAssignmentTable`) with a department filter bar, a Departments column (member-mode `DepartmentPicker` + inline owner-set + amber owner badge), and add the create-SOP department multi-select to BOTH creation paths (the blank wizard and the AI-draft prompt), writing `sop_departments` / `all_departments`. Implements REQ-4, REQ-9, and the team-side of D-03 owner accountability.

Purpose: People and SOPs must reference departments where they are created/managed; this closes the member↔dept and sop↔dept write paths the data model (Plan 01) and actions (Plan 03) enable.
Output: RoleAssignmentTable + team page rework, both create-SOP paths wired, 1 integration spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-RESEARCH.md
@.planning/phases/25-department-first-class-entity/25-PATTERNS.md
@.planning/phases/25-department-first-class-entity/25-UI-SPEC.md
@src/components/admin/RoleAssignmentTable.tsx
@src/types/sop.ts
</context>

<artifacts_this_phase_produces>
- **Modified:** `RoleAssignmentTable.tsx` (departments prop + Departments column + owner badge + member picker), `team/page.tsx` (listDepartments + pass prop), `WizardClient.tsx` + blank `page.tsx` (department field), `PromptClient.tsx` + ai `page.tsx` (department field)
- **Test:** `tests/integration/wizard-sop-dept.spec.ts` (registered in playwright.config.ts)
- (Consumes: `DepartmentPicker` member-mode + `DChip` from Plan 03; `assignMemberDepartments`/`setDepartmentOwner`/`listDepartments` + the wired `createSopFromWizard`/ai-prompt route from Plan 03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RoleAssignmentTable + /admin/team department column, owner badge, filter bar</name>
  <read_first>
    - src/components/admin/RoleAssignmentTable.tsx (self — Props {orgId, inviteCode}; member list rows with role select + remove; the TeamMember type now carries department_ids from Plan 03)
    - src/app/(protected)/admin/team/page.tsx (self — the org/member fetch + RoleAssignmentTable render to extend with departments)
    - src/components/admin/departments/DepartmentPicker.tsx + DChip.tsx (Plan 03 — member-mode picker with owner-set; add chip)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 607-663 RoleAssignmentTable deltas; 805-816 team page deltas)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 375-441 team filter bar / column header / member row / owner badge / member-mode picker with owner-set; 624-652 copy)
  </read_first>
  <action>
    MODIFY `src/components/admin/RoleAssignmentTable.tsx`: add a `departments: Department[]` prop; render a department filter bar above the member list (same contract as the block library, filters visible members to the selected dept); add a Departments column header (230px) after Role; in each member row add the amber `★ Owns {Dept}` owner badge after the name (color #a16207, border var(--accent-signoff), bg rgba(251,191,36,0.16)) for each department the member owns, a `.c-dept` cell rendering a DChip per assigned department (with the ★ owner suffix where this member owns it) plus the dashed add DChip that opens the member-mode DepartmentPicker popover; the picker's inline owner-set calls setDepartmentOwner (D-03). Keep the role select orthogonal/unchanged. Handle the cascade: unchecking a member from a dept they own clears the owner (the picker fires setDepartmentOwner(deptId, null)). MODIFY `src/app/(protected)/admin/team/page.tsx`: `await listDepartments()` and pass `departments` to RoleAssignmentTable; ensure the shared sub-nav includes the Departments tab. Use the team sub-heading + context-line copy from UI-SPEC.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('src/components/admin/RoleAssignmentTable.tsx','utf8');['DepartmentPicker','departments','Owns'].forEach(s=>{if(!r.includes(s)){console.error('RoleAssignmentTable missing',s);process.exit(1)}});const p=fs.readFileSync('src/app/(protected)/admin/team/page.tsx','utf8');if(!/listDepartments/.test(p)){console.error('team page missing listDepartments');process.exit(1)};console.log('team OK')" && (npx tsc --noEmit 2>&1 | (grep -E "RoleAssignmentTable|admin/team/page" && exit 1 || exit 0))</automated>
  </verify>
  <acceptance_criteria>
    - RoleAssignmentTable takes `departments`, renders the filter bar + Departments column + member-mode DepartmentPicker + the amber owner badge (REQ-4, REQ-5, D-03)
    - Inline owner-set calls setDepartmentOwner; unchecking an owned dept clears the owner (cascade to the departments-page warning)
    - team/page.tsx fetches + passes departments; sub-nav has the Departments tab
    - `npx tsc --noEmit` clean for these files
  </acceptance_criteria>
  <done>The team surface assigns members to departments and sets owners inline, with the owner badge rendered.</done>
</task>

<task type="auto">
  <name>Task 2: Create-SOP department field on both creation paths (blank wizard + AI draft)</name>
  <read_first>
    - src/app/(protected)/admin/sops/new/blank/WizardClient.tsx (self — Step 1 title/sopNumber/categoryTag fields + the createSopFromWizard submit call; the WizardClientProps to extend with departments)
    - src/app/(protected)/admin/sops/new/blank/page.tsx (self — the server component that fetches categories + renders WizardClient; add listDepartments)
    - src/app/(protected)/admin/sops/new/ai/PromptClient.tsx (self — Props {categories}; the categoryTag form field + the POST to /api/sops/ai-prompt to extend with departmentIds/allDepartments)
    - src/app/(protected)/admin/sops/new/ai/page.tsx (self — the server component rendering PromptClient; add listDepartments)
    - src/components/admin/departments/DepartmentPicker.tsx (Plan 03 — sop-mode + localOnly for create)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 820-868 WizardClient deltas + the localOnly create-mode note)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 445-495 create-SOP field placement/helper/All-departments)
  </read_first>
  <action>
    MODIFY `src/app/(protected)/admin/sops/new/blank/WizardClient.tsx`: add `departments: Department[]` to props, add `departmentIds`/`allDepartments` local state, render the sop-mode DepartmentPicker with `localOnly` (no server action on toggle — sopId does not exist yet, A4) in Step 1 in place of the old free-text category select (hide the category field; the column is retained read-only in DB), show the helper copy, and thread `{ departmentIds, allDepartments }` into the existing `createSopFromWizard(...)` submit call (the action already writes sop_departments per Plan 03). Update `src/app/(protected)/admin/sops/new/blank/page.tsx` to `await listDepartments()` and pass it. MODIFY `src/app/(protected)/admin/sops/new/ai/PromptClient.tsx` similarly: add a department multi-select (localOnly) and include `departmentIds`/`allDepartments` in the JSON body POSTed to `/api/sops/ai-prompt` (the route writes the junction per Plan 03, A3); update `src/app/(protected)/admin/sops/new/ai/page.tsx` to fetch + pass departments. Then create `tests/integration/wizard-sop-dept.spec.ts`: drive createSopFromWizard with two department ids, assert two sop_departments rows exist for the new sop; drive it with allDepartments=true, assert sops.all_departments=true and zero junction rows (REQ-9, D-04). Register it in playwright.config.ts.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('src/app/(protected)/admin/sops/new/blank/WizardClient.tsx','utf8');['departmentIds','DepartmentPicker','localOnly'].forEach(s=>{if(!w.includes(s)){console.error('WizardClient missing',s);process.exit(1)}});const a=fs.readFileSync('src/app/(protected)/admin/sops/new/ai/PromptClient.tsx','utf8');if(!/department/i.test(a)){console.error('AI path missing department field');process.exit(1)};console.log('wizard+ai OK')" && (npx playwright test --list 2>&1 | (grep -q "wizard-sop-dept" && echo "spec registered" || (echo "wizard-sop-dept NOT registered"; exit 1))) && (npx tsc --noEmit 2>&1 | (grep -E "sops/new/(blank|ai)" && exit 1 || exit 0))</automated>
  </verify>
  <acceptance_criteria>
    - WizardClient renders a localOnly sop-mode DepartmentPicker + helper copy and passes departmentIds/allDepartments to createSopFromWizard (REQ-9, A4)
    - The AI-draft PromptClient exposes the same field and posts departmentIds/allDepartments to /api/sops/ai-prompt (REQ-9, A3)
    - Both blank + ai `page.tsx` fetch listDepartments and pass it
    - `wizard-sop-dept.spec.ts` asserts 2 junction rows for a 2-dept create AND all_departments=true for the org-wide case (D-04); spec is registered
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both SOP creation paths write department junctions; the wizard junction write is covered by a registered integration spec.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify team dept assignment + create-SOP paths</name>
  <action>On the deployed app, verify /admin/team member dept assignment + inline owner-set (badge + departments-page warning cascade) and that both create-SOP paths apply department tags — per the how-to-verify steps below.</action>
  <what-built>Extended `/admin/team` (department filter bar + Departments column + member-mode picker with inline owner-set + amber owner badge) and added the department multi-select to both create-SOP paths (blank wizard + AI draft). Integration spec `wizard-sop-dept.spec.ts` covers the junction write.</what-built>
  <how-to-verify>
    1. On sopstart.com as an admin, visit `/admin/team`.
    2. Assign a member to a department via the dashed add button -> a chip appears; confirm they now appear under that department's filter and not under others.
    3. In the member picker, click "Set owner" on a department -> the member gets the amber "★ Owns {Dept}" badge, and on `/admin/departments` that department's red no-owner warning clears.
    4. Remove the member from the department they own -> the owner clears and `/admin/departments` shows the red warning again.
    5. Visit `/admin/sops/new/blank`, create a SOP with two departments selected -> the SOP is created; confirm (via /admin/departments SOP counts or the builder) it is tagged to both.
    6. Repeat on `/admin/sops/new/ai` (AI draft) -> the department field is present and applied.
  </how-to-verify>
  <acceptance_criteria>
    - Member dept assignment + single-removal filter isolation works (REQ-4)
    - Inline owner-set toggles the amber badge AND the departments-page warning (REQ-5, D-03)
    - Both create-SOP paths write department tags (REQ-9); `wizard-sop-dept.spec.ts` green
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what rendered/behaved incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> /admin/team | Member↔dept + owner writes; org+role enforced server-side |
| admin browser -> create-SOP | departmentIds chosen client-side, validated + written server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Elevation of Privilege | setting a foreign-org member as owner via the team picker (T-MED) | mitigate | setDepartmentOwner (Plan 03) verifies the target is in organisation_members for the same org; the picker only lists org members |
| T-25-05 | Tampering | owner badge implying edit/approve rights (T-MED) | mitigate | D-03: badge + owner-set are accountability labels only; no write/approve gate is added based on ownership in this surface |
| T-25-11 | Tampering | wizard creating a SOP with departments from another org | mitigate | createSopFromWizard writes sop_departments under the caller's org; department ids are org-scoped by RLS so a foreign id cannot be tagged |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase |
</threat_model>

<verification>
- grep + `npx tsc --noEmit` guards on RoleAssignmentTable, team page, both wizard/ai clients + pages
- `wizard-sop-dept` integration spec registered + asserting REQ-9/D-04
- Human-verify checkpoint on sopstart.com (member assign/owner cascade + both create paths) per the project's runtime-UAT learnings
- Runs AFTER Plan 03 (depends_on 25-03) so DepartmentPicker + the wired actions exist
</verification>

<success_criteria>
- `/admin/team` assigns members to departments + sets owners inline with the badge (REQ-4, REQ-5)
- Both create-SOP paths write sop_departments / all_departments (REQ-9, D-04)
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-06-SUMMARY.md` when done
</output>
</output>
