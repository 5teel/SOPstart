---
phase: 25-department-first-class-entity
plan: 05
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - src/components/admin/blocks/BlockListTable.tsx
  - src/components/admin/blocks/BlockPicker.tsx
  - src/components/admin/blocks/SaveToLibraryModal.tsx
  - src/app/(protected)/admin/blocks/page.tsx
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/admin/global-blocks/page.tsx
  - src/app/(protected)/admin/global-blocks/suggestions/page.tsx
  - src/components/sop/CategoryBottomSheet.tsx
  - src/app/(protected)/sops/page.tsx
  - src/lib/auth/platform-admin-guard.ts
  - src/lib/journeys/journeys.ts
  - tests/lint/no-global-blocks-in-journeys.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [REQ-2, REQ-3, REQ-7, D-04]
user_setup: []

must_haves:
  truths:
    - "/admin/blocks filters by department (All + per-dept + counts) and shows per-block department chips incl. the cyan All-departments chip (REQ-2, REQ-7, D-04)"
    - "The /admin/global-blocks and /admin/global-blocks/suggestions routes are deleted and return 404 with zero inbound refs in src (REQ-7)"
    - "No My/Global library scope control remains anywhere in src (REQ-7)"
    - "journeys.ts adds the manage-departments pathway, removes curate-globals + the Platform admin group; /pathways shows 0 not-mapped for /admin/departments and no /admin/global-blocks refs"
    - "The worker SOP library CategoryBottomSheet/CategorySidebar consumer is migrated to department selection (REQ-3 surface follow-through)"
  artifacts:
    - path: "src/app/(protected)/admin/blocks/page.tsx"
      provides: "department filter bar + departmentId listBlocks + departments prop"
      contains: "departmentId"
    - path: "src/lib/journeys/journeys.ts"
      provides: "manage-departments pathway; curate-globals removed"
      contains: "manage-departments"
    - path: "tests/lint/no-global-blocks-in-journeys.spec.ts"
      provides: "source-contract guard: no /admin/global-blocks refs, /admin/departments mapped"
      contains: "global-blocks"
  key_links:
    - from: "src/app/(protected)/admin/blocks/page.tsx"
      to: "src/actions/departments.ts"
      via: "listDepartments() for the filter bar + BlockListTable departments prop"
      pattern: "listDepartments"
    - from: "src/components/admin/blocks/BlockListTable.tsx"
      to: "src/components/admin/departments/DepartmentPicker.tsx"
      via: "Departments picker popover (block mode) + DChip per junction"
      pattern: "DepartmentPicker|DChip"
---

<objective>
Ship Surface 2 and the route-cleanup: rework `/admin/blocks` to a department-organised library (filter bar with counts, per-block DChip chips incl. the cyan "All departments" chip, "Departments" picker popover), DELETE the `/admin/global-blocks` + `/admin/global-blocks/suggestions` routes and repoint every inbound reference, remove the "Suggest for global" UI, migrate the single worker-library `CategoryBottomSheet` consumer to department selection, and update `src/lib/journeys/journeys.ts` (add `manage-departments`, remove `curate-globals` + the Platform-admin group) — guarded by a registered lint spec.

Purpose: REQ-7 replaces the org/global model with a department-tagged library; the dead-route grep-and-repoint and journeys.ts update are mandatory per the project's dead-route + Pathways-Map-Maintenance learnings.
Output: BlockListTable + page rework, 2 route deletions + 4 reference repoints, worker-library migration, journeys.ts edit, 1 lint spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-RESEARCH.md
@.planning/phases/25-department-first-class-entity/25-PATTERNS.md
@.planning/phases/25-department-first-class-entity/25-UI-SPEC.md
@CLAUDE.md
@src/components/admin/blocks/BlockListTable.tsx
@src/lib/journeys/journeys.ts
</context>

<artifacts_this_phase_produces>
- **Modified:** `BlockListTable.tsx` (dept chips + Departments picker), `blocks/page.tsx` (filter bar + departmentId), `BlockPicker.tsx` (drop includeGlobal), `SaveToLibraryModal.tsx` (drop suggest_global radio), `sops/page.tsx` (admin — drop isPlatformAdmin nav link), `platform-admin-guard.ts` (comment), `journeys.ts` (manage-departments add / curate-globals remove)
- **Deleted:** `src/app/(protected)/admin/global-blocks/page.tsx`, `src/app/(protected)/admin/global-blocks/suggestions/page.tsx`
- **Migrated:** `CategoryBottomSheet.tsx` -> department-shaped props; worker `src/app/(protected)/sops/page.tsx` consumer updated
- **Test:** `tests/lint/no-global-blocks-in-journeys.spec.ts` (registered in playwright.config.ts)
- (Consumes: `DepartmentPicker`, `DChip` from Plan 03; `listDepartments`, `listBlocks({departmentId})` from Plan 03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: BlockListTable + /admin/blocks department filter rework</name>
  <read_first>
    - src/components/admin/blocks/BlockListTable.tsx (self — Props categories->departments, the GLOBAL badge to delete, category_tags chip cell to replace with DChip, actions column to add the Departments picker)
    - src/app/(protected)/admin/blocks/page.tsx (self — SCOPE_TABS L22-25 to remove, scope param L46-47 + listBlocks L50-58 to replace with departmentId + listDepartments)
    - src/components/admin/departments/DepartmentPicker.tsx + DChip.tsx (Plan 03 — block-mode picker + chips to consume)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 543-603 BlockListTable deltas; 754-786 page deltas)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 257-371 filter bar / context line / chips / Departments picker / popover; 552-571 filter-button contract)
  </read_first>
  <action>
    MODIFY `src/components/admin/blocks/BlockListTable.tsx`: replace the `categories: BlockCategory[]` prop with `departments: Department[]`; change block items to carry `departmentIds?: string[]` and `allDepartments?: boolean`; build a `deptMap` from departments; rename the Categories column header to Departments; DELETE the GLOBAL badge block; replace the category_tags chip cell with DChip components (all-departments variant when block.allDepartments, else a department DChip per departmentId); add a Departments ghost button in the actions column that opens the block-mode DepartmentPicker popover for that row (state openPickerId). MODIFY `src/app/(protected)/admin/blocks/page.tsx`: remove SCOPE_TABS + the scope param + the includeGlobal/globalOnly listBlocks args; read a `dept` search param; call Promise.all of listBlocks({ departmentId: dept, kindSlug: kind }) and listDepartments(); pass departments to BlockListTable; render a department filter bar above the kind row as server-rendered Link href="?dept=X" buttons (All + per-dept with count badge + colour swatch, active state from the URL param) per the UI-SPEC filter-button contract (44px min height); render the context line copy (All vs dept-active). Use the block-library sub-heading + CTA copy from UI-SPEC; ensure the shared sub-nav now includes the Departments tab.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/components/admin/blocks/BlockListTable.tsx','utf8');if(/GLOBAL/.test(t.replace(/\/\/.*/g,''))){console.error('GLOBAL badge still present');process.exit(1)};['departments','DChip'].forEach(s=>{if(!t.includes(s)){console.error('BlockListTable missing',s);process.exit(1)}});const p=fs.readFileSync('src/app/(protected)/admin/blocks/page.tsx','utf8');if(/SCOPE_TABS|globalOnly|includeGlobal/.test(p)){console.error('scope/global remnants in blocks page');process.exit(1)};['departmentId','listDepartments'].forEach(s=>{if(!p.includes(s)){console.error('blocks page missing',s);process.exit(1)}});console.log('block library OK')" && (npx tsc --noEmit 2>&1 | (grep -E "blocks/(BlockListTable|page)" && exit 1 || exit 0))</automated>
  </verify>
  <acceptance_criteria>
    - No `SCOPE_TABS`/`includeGlobal`/`globalOnly` in blocks/page.tsx; no `GLOBAL` badge in BlockListTable (REQ-7)
    - BlockListTable renders DChip per junction + cyan all-departments chip (REQ-2, D-04) and a Departments picker popover
    - blocks/page.tsx reads `dept` param, fetches via departmentId + listDepartments, renders the filter bar + context line
    - `npx tsc --noEmit` clean for these files
  </acceptance_criteria>
  <done>The block library is department-organised with no org/global scope control.</done>
</task>

<task type="auto">
  <name>Task 2: Delete global-blocks routes + repoint every inbound reference + remove suggest-global UI</name>
  <read_first>
    - .planning/phases/25-department-first-class-entity/25-RESEARCH.md (lines 158-175 — the complete deletion map: 2 files to delete, 5 reference locations across 7 files)
    - src/app/(protected)/admin/sops/page.tsx (self — L51/L54/L116-118 isPlatformAdmin conditional nav link to /admin/global-blocks to remove)
    - src/components/admin/blocks/SaveToLibraryModal.tsx (self — L41/L85/L200-202 suggest_global radio to remove)
    - src/components/admin/blocks/BlockPicker.tsx (self — L53 listBlocks({includeGlobal:true}) to update to a department-agnostic call)
    - src/lib/auth/platform-admin-guard.ts (self — L6/L13 comments referencing /admin/global-blocks)
  </read_first>
  <action>
    DELETE `src/app/(protected)/admin/global-blocks/page.tsx` and `src/app/(protected)/admin/global-blocks/suggestions/page.tsx` (remove the now-empty directory). Repoint every inbound reference from the RESEARCH deletion map: in `src/app/(protected)/admin/sops/page.tsx` remove the isPlatformAdmin detection (L51-54) and the conditional Link to /admin/global-blocks (L116-118), and add the Departments tab to this page's sub-nav (shared nav consistency); in `src/components/admin/blocks/SaveToLibraryModal.tsx` remove the suggest_global scope state + radio option (L41/L85/L200-202), leaving org-only save; in `src/components/admin/blocks/BlockPicker.tsx` change listBlocks({ includeGlobal: true, ... }) (L53) to the department-agnostic listBlocks({ includeContent: true, kindSlug }) (no global flag — RLS scopes to org); update the /admin/global-blocks comments in `src/lib/auth/platform-admin-guard.ts` (the blocks.ts comment is handled in Plan 03). After edits, run a grep proving zero /admin/global-blocks and zero suggest_global references remain in src (the project's dead-route learning: internal Link hrefs are not type-checked).
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process');const out=cp.execSync('rg -n \"/admin/global-blocks|suggest_global\" src || true',{encoding:'utf8'});if(out.trim()){console.error('REMAINING REFS:\n'+out);process.exit(1)};const fs=require('fs');['src/app/(protected)/admin/global-blocks/page.tsx','src/app/(protected)/admin/global-blocks/suggestions/page.tsx'].forEach(f=>{if(fs.existsSync(f)){console.error('route not deleted:',f);process.exit(1)}});console.log('routes deleted, zero refs')" && (npx tsc --noEmit 2>&1 | (grep -E "admin/(sops/page|blocks/(BlockPicker|SaveToLibraryModal))|platform-admin-guard" && exit 1 || exit 0))</automated>
  </verify>
  <acceptance_criteria>
    - Both global-blocks page files are deleted (filesystem check)
    - `rg "/admin/global-blocks|suggest_global" src` returns nothing (REQ-7, dead-route + A5/A6 guard)
    - `admin/sops/page.tsx` no longer renders the isPlatformAdmin global-blocks link; SaveToLibraryModal has no suggest_global radio; BlockPicker no longer passes includeGlobal
    - `npx tsc --noEmit` clean for these files
  </acceptance_criteria>
  <done>The global-blocks surface and suggest-global affordance are fully removed with no dangling refs.</done>
</task>

<task type="auto">
  <name>Task 3: Worker CategoryBottomSheet -> department + journeys.ts + lint guard</name>
  <read_first>
    - src/components/sop/CategoryBottomSheet.tsx (self — CategoryBottomSheetProps + CategorySidebar export to reshape to department props)
    - src/app/(protected)/sops/page.tsx (self — the ONLY consumer: L23 import, L154 CategorySidebar, L179 CategoryBottomSheet)
    - src/lib/journeys/journeys.ts (self — L47 JOURNEY_GROUPS Platform admin entry; curate-globals journey L354-367; manage-team roles step; the Journey interface)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 666-694 CategoryBottomSheet reshape; 887-943 the exact journeys.ts edits)
    - CLAUDE.md (section Pathways Map Maintenance — journeys.ts must change in this phase; /pathways All screens must show 0 not-mapped)
  </read_first>
  <action>
    Reshape `src/components/sop/CategoryBottomSheet.tsx` to department selection: replace the CategoryItem[] props with departments: Department[]; selectedIds: string[]; allDepartments: boolean; onSelect:(ids,all)=>void for both the bottom-sheet and the CategorySidebar exports (rename to DepartmentBottomSheet/DepartmentSidebar or keep the names but change the shape — choose one and update the consumer to match); preserve the slide-up panel structure exactly, add a colour swatch per row + an All departments option at top (cyan style). Update the single consumer `src/app/(protected)/sops/page.tsx` (L23/L154/L179) to pass departments + handle department-based selection (the worker library is gated by sop_departments RLS from Plan 01; this is the in-page department selector). Edit `src/lib/journeys/journeys.ts` per the PATTERNS map: remove the curate-globals journey entirely, remove 'Platform admin' from JOURNEY_GROUPS, extend the manage-team roles step detail to mention departments, and add the manage-departments journey (group 'Library & team', screen step route '/admin/departments'). Create `tests/lint/no-global-blocks-in-journeys.spec.ts`: assert journeys.ts contains /admin/departments, contains no /admin/global-blocks, and that the src tree contains no /admin/global-blocks route reference; register it in `playwright.config.ts` (extend a lint project regex to include no-global-blocks-in-journeys) and validate via npx playwright test --list.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('src/lib/journeys/journeys.ts','utf8');if(/curate-globals|Platform admin|admin\/global-blocks/.test(j)){console.error('journeys.ts still has global-block artifacts');process.exit(1)};if(!j.includes('manage-departments')||!j.includes('/admin/departments')){console.error('journeys.ts missing manage-departments');process.exit(1)};const c=fs.readFileSync('src/components/sop/CategoryBottomSheet.tsx','utf8');if(!/department/i.test(c)){console.error('CategoryBottomSheet not migrated');process.exit(1)};console.log('journeys+sheet OK')" && (npx playwright test --list 2>&1 | (grep -q "no-global-blocks-in-journeys" && echo "lint spec registered" || (echo "lint spec NOT registered"; exit 1))) && (npx tsc --noEmit 2>&1 | (grep -E "journeys.ts|CategoryBottomSheet|protected./sops/page" && exit 1 || exit 0))</automated>
  </verify>
  <acceptance_criteria>
    - journeys.ts has no `curate-globals`/`Platform admin`/`/admin/global-blocks` and DOES have `manage-departments` -> `/admin/departments` (Pathways Map Maintenance)
    - CategoryBottomSheet + CategorySidebar migrated to department props; the worker `sops/page.tsx` consumer updated to match
    - `no-global-blocks-in-journeys` lint spec exists, is registered (appears in `npx playwright test --list`), and asserts the no-global-blocks/has-departments contract (REQ-7 acceptance, 2026-05-25 unregistered-spec guard)
    - `npx tsc --noEmit` clean for journeys.ts/CategoryBottomSheet/worker sops page
  </acceptance_criteria>
  <done>The worker library uses departments, journeys.ts reflects the new + removed routes, and a registered lint guard enforces it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> /admin/blocks | Admin library surface; department tagging writes via block-mode picker |
| deleted route -> dangling Link | A dead internal href degrades to an unhandled 404 (no not-found.tsx) — a silent nav bug |
| worker browser -> /sops | Worker library; department selection is a view filter, RLS is the real gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Tampering | leftover suggest_global UI writing to the dropped table (T-MED) | mitigate | SaveToLibraryModal suggest_global radio removed; grep proves zero suggest_global refs in src (A5/A6); the table was dropped in Plan 01 |
| T-25-09 | Spoofing / Availability | dangling /admin/global-blocks Link surfacing a broken nav (T-MED) | mitigate | every inbound ref repointed/removed; `rg "/admin/global-blocks" src` returns nothing (dead-route learning) |
| T-25-10 | Information Disclosure | worker library showing SOPs across departments after the filter change (T-MED) | mitigate | the in-page selector is a view filter only; actual visibility is the Plan 01 `sops_visible_by_department` RLS gate (verified in Plan 02) — the UI cannot widen RLS |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase |
</threat_model>

<verification>
- grep guards: zero `/admin/global-blocks` + `suggest_global` in src; `GLOBAL` badge + `SCOPE_TABS` gone; journeys.ts has manage-departments and no curate-globals
- `no-global-blocks-in-journeys` registered in playwright.config.ts (`npx playwright test --list`)
- `npx tsc --noEmit` clean across all touched files
- Pathways: `/pathways` All screens shows 0 not-mapped for `/admin/departments` (CLAUDE.md maintenance contract)
- Runs AFTER Plan 03 (depends_on 25-03) so DepartmentPicker/DChip/listDepartments exist
</verification>

<success_criteria>
- `/admin/blocks` is department-organised; org/global scope control fully removed (REQ-7)
- Both global-blocks routes deleted with zero dangling refs
- journeys.ts + worker library reflect the department model; lint guard enforces it
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-05-SUMMARY.md` when done
</output>
