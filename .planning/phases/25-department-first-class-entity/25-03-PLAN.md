---
phase: 25-department-first-class-entity
plan: 03
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - src/actions/departments.ts
  - src/actions/blocks.ts
  - src/actions/sops.ts
  - src/actions/auth.ts
  - src/components/admin/departments/DChip.tsx
  - src/components/admin/departments/DepartmentPicker.tsx
  - tests/integration/member-dept.spec.ts
autonomous: true
requirements: [REQ-2, REQ-3, REQ-4, REQ-5, REQ-9, D-02, D-03, D-04]
user_setup: []

must_haves:
  truths:
    - "An admin can create/edit/archive a department and set its owner; setDepartmentOwner rejects a non-org-member (REQ-5, D-03, T-25-03)"
    - "assignBlockDepartments / assignSopDepartments / assignMemberDepartments persist replace-semantics junction rows (REQ-2/3/4)"
    - "listDepartments returns DepartmentWithCounts with live people/sop/block counts (REQ-6 data source)"
    - "blocks.ts no longer exposes includeGlobal/globalOnly/suggest_global; listBlocks accepts departmentId; block_suggestions actions removed (REQ-7, A5/A6)"
    - "createSopFromWizard + the AI-prompt SOP create write sop_departments rows / all_departments flag (REQ-9, D-04)"
    - "DepartmentPicker mirrors SubTradePicker with colour swatch + All-departments toggle + member-mode owner-set + create-mode local-only variant"
  artifacts:
    - path: "src/actions/departments.ts"
      provides: "department CRUD + listDepartments(counts) + setDepartmentOwner + 3 junction assigners"
      exports: ["listDepartments", "createDepartment", "updateDepartment", "archiveDepartment", "setDepartmentOwner", "assignBlockDepartments", "assignSopDepartments", "assignMemberDepartments"]
    - path: "src/components/admin/departments/DepartmentPicker.tsx"
      provides: "member/block/sop multi-select with colour swatch + All-departments + create-mode localOnly"
      contains: "mode"
    - path: "src/components/admin/departments/DChip.tsx"
      provides: "department / all-departments / add chip variants"
      contains: "all-departments"
  key_links:
    - from: "src/actions/departments.ts"
      to: "public.member_departments"
      via: "replace-semantics delete-then-insert"
      pattern: "member_departments"
    - from: "src/components/admin/departments/DepartmentPicker.tsx"
      to: "src/actions/departments.ts"
      via: "assignMemberDepartments/assignBlockDepartments/assignSopDepartments on toggle (non-create modes)"
      pattern: "assign(Member|Block|Sop)Departments"
    - from: "src/actions/sops.ts"
      to: "public.sop_departments"
      via: "createSopFromWizard post-insert junction write"
      pattern: "sop_departments"
---

<objective>
Build the server-action layer and the two shared client primitives (`DChip`, `DepartmentPicker`) that all three Wave-3 UI surfaces consume. Create `src/actions/departments.ts` (CRUD + counts + owner + three junction assigners), surgically modify `src/actions/blocks.ts` (drop org/global + suggest_global, add departmentId filter), wire `createSopFromWizard` (and the AI-prompt SOP create) to write `sop_departments`, add `department_ids` to the `TeamMember` type, and add the remaining Wave-0 integration scaffold for member↔dept.

Purpose: Wave 3 plans (departments page, block library, team+wizard) must import a stable contract and ready-built pickers — no scavenger hunt. Concentrating the shared pickers here keeps Wave-3 files disjoint and worktree-safe.
Output: One new actions file, three modified actions/types files, two shared components, one integration spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-RESEARCH.md
@.planning/phases/25-department-first-class-entity/25-PATTERNS.md
@.planning/phases/25-department-first-class-entity/25-UI-SPEC.md
@src/actions/blocks.ts
@src/components/admin/SubTradePicker.tsx
</context>

<artifacts_this_phase_produces>
- **Actions:** `src/actions/departments.ts` exports `listDepartments`, `createDepartment`, `updateDepartment`, `archiveDepartment`, `setDepartmentOwner`, `assignBlockDepartments`, `assignSopDepartments`, `assignMemberDepartments`
- **Type field:** `department_ids: string[]` added to `TeamMember` in `src/actions/auth.ts`
- **Components:** `src/components/admin/departments/DChip.tsx`, `src/components/admin/departments/DepartmentPicker.tsx`
- **listBlocks option:** `departmentId` (replaces `includeGlobal`/`globalOnly`)
- **Removed:** `suggest_global` scope from `CreateBlockInput`/`SaveFromSectionInput`; `listBlockSuggestions`/`promoteSuggestion`/`rejectSuggestion`/`requirePlatformAdmin` from `blocks.ts`
- **createSopFromWizard input:** `departmentIds`, `allDepartments`
- **Test:** `tests/integration/member-dept.spec.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/actions/departments.ts — CRUD, counts, owner, junction assigners</name>
  <read_first>
    - src/actions/blocks.ts (EXACT analog: lines 40-55 requireAdmin; CreateBlockInput Zod shape; archiveBlock mutation shape; discriminated-union return pattern — copy verbatim, rename only)
    - src/components/admin/SubTradePicker.tsx (the replace-semantics toggle the assigners back)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 264-352 — the full departments.ts skeleton incl. CreateDepartmentInput colour enum, replace-semantics assigner, listDepartments counts, D-03 owner-must-be-org-member check)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 117-135 the 8 allowed dept colours for the Zod enum)
  </read_first>
  <behavior>
    - createDepartment: valid input → inserts with organisation_id from JWT (never client) → returns {department}; bad colour (not in the 8-hex allow-list) → {error}
    - setDepartmentOwner: owner is an org member → updates owner_user_id; owner NOT in organisation_members for this org → {error} (D-03, T-25-03)
    - assignMemberDepartments(memberId, ids): deletes existing member_departments for memberId, inserts ids → {success}; empty ids → deletes all, no insert
    - assignBlockDepartments / assignSopDepartments(targetId, ids, allDepartments): allDepartments=true → sets parent.all_departments=true and clears junction rows; else clears all_departments + replaces junction rows (D-04)
    - listDepartments: returns DepartmentWithCounts[] with people_count/sop_count/block_count for the caller's org only
  </behavior>
  <action>
    Create `src/actions/departments.ts` with `'use server'`. Copy `requireAdmin()` verbatim from blocks.ts (reads role + organisation_id from JWT claims, never client input — T-25-03). Zod schemas: `CreateDepartmentInput` (name 1-100, code 1-6 toUpperCase, colour z.enum of the 8 UI-SPEC hex values, icon max 4 optional, ownerUserId uuid nullable optional), `UpdateDepartmentInput` (same + id). Export `createDepartment`, `updateDepartment`, `archiveDepartment` (sets archived=true, never deletes — REQ-6), `setDepartmentOwner(departmentId, userId|null)` which MUST verify userId is in organisation_members for the same org before update (D-03), `listDepartments()` returning DepartmentWithCounts[] (counts via aggregate or three count queries scoped to org), and replace-semantics `assignMemberDepartments(memberId, ids)`, `assignBlockDepartments(blockId, ids, allDepartments)`, `assignSopDepartments(sopId, ids, allDepartments)` per the PATTERNS skeleton. Every function returns the discriminated `{...}|{error}` union, never throws. allDepartments handling per D-04: setting it true clears the junction and flips the parent flag.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('src/actions/departments.ts','utf8');['listDepartments','createDepartment','updateDepartment','archiveDepartment','setDepartmentOwner','assignBlockDepartments','assignSopDepartments','assignMemberDepartments','organisation_members'].forEach(s=>{if(!f.includes(s)){console.error('missing',s);process.exit(1)}});if(!/colou?r:\s*z\.enum/.test(f)){console.error('colour must be a z.enum allow-list');process.exit(1)}console.log('departments.ts OK')" && npx tsc --noEmit 2>&1 | (grep -E "actions/departments.ts" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - All 8 exports present; `setDepartmentOwner` references `organisation_members` (D-03 owner-must-be-member, T-25-03)
    - colour validated by a z.enum allow-list (no free-form string → no CSS-injection risk, V5)
    - junction assigners use delete-then-insert replace semantics; allDepartments=true clears junction + sets parent flag (D-04)
    - archiveDepartment sets a flag, issues no DELETE
    - `npx tsc --noEmit` clean for the file
  </acceptance_criteria>
  <done>departments.ts exposes the full CRUD+counts+owner+junction contract Wave 3 imports.</done>
</task>

<task type="auto">
  <name>Task 2: blocks.ts + sops.ts + auth.ts modifications (retire org/global, wire sop_departments)</name>
  <read_first>
    - src/actions/blocks.ts (self — L8 comment, L124 SaveFromSectionInput scope enum, L137/139 includeGlobal/globalOnly options, L416-422 listBlocks defaults, L451-458 the globalOnly/includeGlobal query branch, L548-596 the suggest_global write branch, and listBlockSuggestions/promoteSuggestion/rejectSuggestion/requirePlatformAdmin to remove)
    - src/actions/sops.ts (self — createSopFromWizard schema + the sops insert + sop_sections insert; the post-insert hook point)
    - src/app/api/sops/ai-prompt/route.ts (self — the AI-prompt SOP create path; where the sops row is inserted so departmentIds can be threaded from the request body)
    - src/actions/auth.ts (self — the TeamMember type + getTeamMembersWithEmails)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 356-421 the exact blocks.ts + sops.ts deltas)
  </read_first>
  <action>
    In `src/actions/blocks.ts`: remove `includeGlobal`/`globalOnly` from `ListBlocksOptions` and add `departmentId?: string`; replace the `globalOnly`/`includeGlobal` query branch — when `departmentId` is set, restrict to blocks tagged to that department OR `all_departments = true` (fetch block ids from `block_departments` then OR with the all_departments column; RLS already scopes to org so no org filter needed post-migration); when unset, return all org blocks. Remove `suggest_global` from both `CreateBlockInput.scope` and `SaveFromSectionInput.scope` (leave `'org'` only) and delete the `if (data.scope === 'suggest_global')` branch plus `listBlockSuggestions`, `promoteSuggestion`, `rejectSuggestion`, `requirePlatformAdmin` (T-25 cleanup — these only served the removed global model). Update the L8/L61 comments referencing /admin/global-blocks. In `src/actions/sops.ts`: extend the `createSopFromWizard` Zod schema with `departmentIds: z.array(z.string().uuid()).max(20).optional().default([])` and `allDepartments: z.boolean().optional().default(false)`; after the sops row is inserted, if allDepartments set `sops.all_departments=true`, else insert `sop_departments` rows for each id (REQ-9, D-04). Apply the SAME post-create dept write to the AI-prompt path in `src/app/api/sops/ai-prompt/route.ts` (thread `departmentIds`/`allDepartments` from the request body into the sops insert; A3). In `src/actions/auth.ts`: add `department_ids: string[]` to the `TeamMember` type and populate it in `getTeamMembersWithEmails` (select from member_departments per member).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('src/actions/blocks.ts','utf8');if(/includeGlobal|globalOnly|suggest_global|listBlockSuggestions|requirePlatformAdmin/.test(b)){console.error('blocks.ts still references retired global/suggest API');process.exit(1)};if(!b.includes('departmentId')){console.error('listBlocks missing departmentId');process.exit(1)};const s=fs.readFileSync('src/actions/sops.ts','utf8');if(!/sop_departments/.test(s)||!/allDepartments/.test(s)){console.error('createSopFromWizard not wired to sop_departments');process.exit(1)};const a=fs.readFileSync('src/actions/auth.ts','utf8');if(!/department_ids/.test(a)){console.error('TeamMember missing department_ids');process.exit(1)};console.log('blocks+sops+auth OK')" && npx tsc --noEmit 2>&1 | (grep -E "actions/(blocks|sops|auth).ts|api/sops/ai-prompt" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `grep` finds NO `includeGlobal|globalOnly|suggest_global|listBlockSuggestions|requirePlatformAdmin` in blocks.ts (REQ-7, A5/A6)
    - `listBlocks` accepts `departmentId` and filters by junction OR all_departments
    - `createSopFromWizard` and the AI-prompt route both write `sop_departments` / `all_departments` (REQ-9, A3, D-04)
    - `TeamMember` has `department_ids: string[]`
    - `npx tsc --noEmit` clean across blocks.ts/sops.ts/auth.ts/ai-prompt route
  </acceptance_criteria>
  <done>The org-vs-global + suggest_global API is fully retired; SOP creation writes department junctions on both create paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DChip + DepartmentPicker shared components + member-dept integration spec</name>
  <read_first>
    - src/components/admin/SubTradePicker.tsx (EXACT analog: useTransition optimistic toggle + revert-on-error at lines 72-92; pill button aria-pressed at 115-132 — copy, add swatch)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 424-538 the full DepartmentPicker + DChip skeletons with deltas)
    - .planning/phases/25-department-first-class-entity/25-UI-SPEC.md (lines 498-548 DChip variant visual contract; 332-441 picker popover block-mode + member-mode owner-set; 445-495 create-SOP field + local-only note)
    - src/types/sop.ts (the Department type from Plan 01)
  </read_first>
  <behavior>
    - DChip variant='department' renders swatch+name; variant='all-departments' renders cyan ◇ chip (var(--accent-mcu)); variant='add' renders dashed blue ＋ button with 44px tap target
    - DepartmentPicker mode='member' toggle calls assignMemberDepartments and reverts on error; mode='block'/'sop' toggle calls assignBlock/SopDepartments with allDepartments
    - DepartmentPicker localOnly (create/wizard mode) fires NO server action on toggle — only onChange (A4)
    - block/sop modes expose an "All departments" toggle mutually exclusive with individual ids (D-04)
    - member mode exposes per-assigned-dept "Set owner / ★ Owner" affordance calling setDepartmentOwner (D-03)
  </behavior>
  <action>
    Create `src/components/admin/departments/DChip.tsx` with the three variants per the UI-SPEC visual contract (department: var(--paper-2) bg + var(--ink-300) border + 7px colour swatch; all-departments: rgba(6,182,212,0.06) bg + var(--accent-mcu) border + ◇ prefix, no swatch; add: dashed var(--accent-step), ＋ only, min 44×44 tap target; optional showOwnerStar appends ★). Create `src/components/admin/departments/DepartmentPicker.tsx` mirroring SubTradePicker: props discriminated by mode 'member'|'block'|'sop', accept `departments: Department[]` + `selectedIds: string[]` as props (pre-fetched, not loaded internally), render a colour swatch per pill, copy the useTransition toggle+revert pattern. Add an `allDepartments` state + "All departments" toggle for block/sop modes (mutually exclusive with individual selection, D-04). Add a `localOnly` boolean prop: when true, toggle updates local state + calls onChange only and fires NO server action (the wizard create case, A4); when false, toggle calls the matching assign* server action. For mode='member', render the inline "Set owner"/"★ Owner" affordance per assigned dept calling setDepartmentOwner (D-03). Then create `tests/integration/member-dept.spec.ts` (Wave-0 scaffold): assign a member to Forming+Cleaning via assignMemberDepartments, assert both rows exist; remove one, assert only that filter drops (REQ-4); set the member as Forming owner, remove the member, assert departments.owner_user_id goes NULL (REQ-5, V4) — register it in playwright.config.ts alongside the Plan 02 specs.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/admin/departments/DChip.tsx','utf8');['all-departments','add','accent-mcu'].forEach(s=>{if(!c.includes(s)){console.error('DChip missing',s);process.exit(1)}});const p=fs.readFileSync('src/components/admin/departments/DepartmentPicker.tsx','utf8');['localOnly','allDepartments','assignMemberDepartments','setDepartmentOwner','useTransition'].forEach(s=>{if(!p.includes(s)){console.error('DepartmentPicker missing',s);process.exit(1)}});console.log('components OK')" && npx playwright test --list 2>&1 | (grep -q "member-dept" && echo "spec registered" || (echo "member-dept not registered"; exit 1)) && npx tsc --noEmit 2>&1 | (grep -E "departments/D(Chip|epartmentPicker)" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - DChip renders all three variants; all-departments uses var(--accent-mcu) cyan, add uses dashed var(--accent-step) with 44px min tap target
    - DepartmentPicker supports member/block/sop modes, colour swatch per pill, All-departments toggle (block/sop), member-mode owner-set, and a `localOnly` flag that suppresses server actions on toggle (A4)
    - `member-dept.spec.ts` asserts multi-dept membership, single-removal isolation, and owner→NULL on member removal (REQ-4/5, V4)
    - spec appears in `npx playwright test --list`; `npx tsc --noEmit` clean for both components
  </acceptance_criteria>
  <done>Both shared primitives exist with the full UI-SPEC contract; member↔dept behaviour is covered by a registered integration spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | Department CRUD/owner/junction writes; org + role must be derived server-side from JWT, not trusted from client |
| client → listBlocks/createSop | Filter + create inputs validated by Zod before DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Elevation of Privilege | createDepartment / setDepartmentOwner accepting foreign org or non-member (T-MED→HIGH) | mitigate | requireAdmin reads organisation_id + role from JWT claims (never client); setDepartmentOwner verifies userId ∈ organisation_members for the same org |
| T-25-05 | Tampering | owner treated as authz grant (T-MED) | mitigate | D-03: owner is a label; no action grants edit/approve based on ownership — assign*/CRUD gate on role only, not ownership |
| T-25-07 | Tampering | orphaned `suggest_global` writes after global model removed | mitigate | `suggest_global` scope + the four block-suggestion actions removed from blocks.ts (A5/A6); the dropped table (Plan 01 00037) leaves no write target |
| T-25-08 | Input Validation | free-form department colour → CSS injection | mitigate | colour is a z.enum allow-list of 8 hex values (V5) |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | no new packages this phase |
</threat_model>

<verification>
- grep guards confirm retired-API removal + new contract present (Tasks 1-3 verify blocks)
- `npx tsc --noEmit` clean for all touched files
- `member-dept` spec registered and asserting REQ-4/REQ-5/V4
- This plan must run AFTER Plan 02's [BLOCKING] apply (depends_on 25-02) so the actions query real tables
</verification>

<success_criteria>
- departments.ts exposes the 8-export contract; owner-set is org-member-gated (D-03)
- blocks.ts/sops.ts/auth.ts retired the global API and wired sop_departments on both create paths
- DChip + DepartmentPicker built to the UI-SPEC contract with create-mode localOnly support
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-03-SUMMARY.md` when done
</output>
