---
phase: 25-department-first-class-entity
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - scripts/apply-phase25-migrations.mjs
  - tests/integration/departments-rls.spec.ts
  - tests/integration/sop-dept-visibility.spec.ts
  - playwright.config.ts
autonomous: false
requirements: [REQ-1, REQ-3, REQ-8, D-01, D-02, D-02a, D-03]
user_setup: []

must_haves:
  truths:
    - "After apply, zero blocks have organisation_id IS NULL and no block row was deleted beyond the copied globals (REQ-8, D-01)"
    - "A department created in org A returns 0 rows when selected by an org B session (REQ-1 cross-tenant isolation)"
    - "SELECT on sops returns rows (no 42P17) after the department policy is live (D-02a no-recursion)"
    - "A worker in only Forming sees Forming + all_departments SOPs and NOT a Cleaning-only SOP (REQ-3, D-02 OR composition)"
    - "departments + 3 junction tables + all_departments columns physically exist in the live DB (schema push proven, not type-inferred)"
  artifacts:
    - path: "scripts/apply-phase25-migrations.mjs"
      provides: "service-role migration applier for 00035/00036/00037 against the remote DB"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "tests/integration/departments-rls.spec.ts"
      provides: "cross-tenant isolation + no-recursion assertions (REQ-1, D-02a)"
      contains: "departments"
    - path: "tests/integration/sop-dept-visibility.spec.ts"
      provides: "OR-composed worker visibility assertions (REQ-3, D-02)"
      contains: "all_departments"
  key_links:
    - from: "scripts/apply-phase25-migrations.mjs"
      to: "supabase/migrations/00035_departments_schema.sql"
      via: "reads + executes each migration file body against the live DB"
      pattern: "00035_departments_schema"
    - from: "tests/integration/departments-rls.spec.ts"
      to: "public.departments"
      via: "RLS-scoped SELECT proving org isolation"
      pattern: "departments"
---

<objective>
[BLOCKING] Apply the Phase 25 migrations (00035/00036/00037) to the live Supabase database, then prove the schema invariants that build/typecheck cannot: zero null-org blocks, cross-tenant department isolation, no RLS recursion on `sops`, and OR-composed worker visibility. This plan is the gate between "migration files written" (Plan 01) and "UI built against real tables" (Wave 4).

Purpose: SOPstart's generated types come from config, not the live DB — `npm run build` passes even if the migrations were never applied (a false-positive verification state). This plan forces the push and asserts post-migration reality.
Output: A reusable migration applier script + two integration specs (registered in playwright.config.ts) + a successful live apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-RESEARCH.md
@.planning/phases/25-department-first-class-entity/25-01-PLAN.md
@scripts/uat-session.mjs
@supabase/migrations/00035_departments_schema.sql
@supabase/migrations/00036_departments_data.sql
@supabase/migrations/00037_departments_rls_cleanup.sql
</context>

<artifacts_this_phase_produces>
- **Script:** `scripts/apply-phase25-migrations.mjs` (service-role migration runner)
- **Tests:** `tests/integration/departments-rls.spec.ts`, `tests/integration/sop-dept-visibility.spec.ts`
- **Config:** new playwright project regex entry registering the two specs (and reserving the phase-25 lint spec slot Plan 05 adds)
- (Consumes, does not create: the 00035/00036/00037 tables from Plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migration applier script + Wave-0 RLS/visibility test scaffolds</name>
  <read_first>
    - scripts/uat-session.mjs (analog: .env.local loader + createClient(SERVICE_KEY) + magic-link session install — copy the env-load + service-role-client pattern; note ANON key is NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, NOT ANON_KEY)
    - scripts/uat-magic-link.mjs (analog: installing an RLS-scoped (non-service) session for a specific user to test policies as that user)
    - playwright.config.ts (self — the project regex list; the 2026-05-25 learning: a spec not matched by a project regex NEVER runs)
    - .planning/phases/25-department-first-class-entity/25-RESEARCH.md (lines 651-704 V1/V2/V3 test approaches; lines 728-735 Wave 0 gaps)
  </read_first>
  <action>
    Create `scripts/apply-phase25-migrations.mjs`: load `.env.local` (the uat-session.mjs loader), read `SUPABASE_SERVICE_ROLE_KEY` + `NEXT_PUBLIC_SUPABASE_URL`, read the three migration file bodies in order (00035, 00036, 00037), and execute each against the remote DB. Use the established execution path — if the project exposes an `exec_sql`/SQL RPC use it; otherwise (preferred, dependency-free) open a direct Postgres connection to the remote DB URL with the `pg` driver if present, else print the exact `supabase db push` / `psql` command for manual run and exit non-zero with a clear message. The script must print a post-apply assertion block: `SELECT count(*) FROM blocks WHERE organisation_id IS NULL` (expect 0), `SELECT to_regclass('public.departments')` and the three junctions (expect non-null), and `SELECT 1 FROM sops LIMIT 1` (must not error with 42P17). Then create two integration specs as scaffolds wired to the applied schema: `tests/integration/departments-rls.spec.ts` — using two distinct org sessions (magic-link per uat-magic-link.mjs), assert org B's session returns 0 rows for org A's department id, and an org-A-scoped insert with org B's organisation_id is rejected by RLS (REQ-1, T-25-01/03); `tests/integration/sop-dept-visibility.spec.ts` — seed a Forming dept + a Forming-tagged SOP + a Cleaning-only SOP + an all_departments SOP, assign a worker to Forming only, assert the worker sees Forming + all_departments and NOT Cleaning (REQ-3, D-02), and assert `SELECT * FROM sops LIMIT 1` does not 500 (D-02a, T-25-02). Register BOTH specs in `playwright.config.ts` by adding/extending a project whose testMatch includes `departments-rls` and `sop-dept-visibility` (and reserve the `no-global-blocks-in-journeys` name for Plan 05). Validate registration with `npx playwright test --list` showing both files.
  </action>
  <verify>
    <automated>npx playwright test --list 2>&1 | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const ok=s.includes('departments-rls')&&s.includes('sop-dept-visibility');if(!ok){console.error('specs not registered in playwright.config.ts');process.exit(1)}console.log('specs registered')})" && node -e "require('fs').accessSync('scripts/apply-phase25-migrations.mjs');console.log('applier present')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/apply-phase25-migrations.mjs` exists, loads `.env.local`, uses `SUPABASE_SERVICE_ROLE_KEY`, applies 00035→00036→00037 in order, and prints the post-apply assertion results
    - Both integration specs exist and appear in `npx playwright test --list` (2026-05-25 unregistered-spec guard satisfied)
    - `departments-rls.spec.ts` asserts org-B-cannot-read-org-A-department (REQ-1)
    - `sop-dept-visibility.spec.ts` asserts Forming-worker sees Forming+all_departments, not Cleaning-only, and that `sops` SELECT does not 42P17 (REQ-3, D-02a)
  </acceptance_criteria>
  <done>The applier and two registered integration specs exist and are discoverable by Playwright.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply Phase 25 migrations to the live DB</name>
  <action>Run the applier (or the printed supabase db push fallback) to apply 00035/00036/00037 to the live remote DB, confirm the post-apply assertion block is all-green, then run npm run test:integration and confirm the two new specs pass. This is the [BLOCKING] gate before Wave 4.</action>
  <what-built>Migration applier script `scripts/apply-phase25-migrations.mjs` ready to push 00035/00036/00037 to the live remote Supabase DB. SOPstart's DB is remote; applying requires `SUPABASE_SERVICE_ROLE_KEY` from `.env.local` which the autonomous executor must not assume is loadable non-interactively. This step is [BLOCKING] and must complete BEFORE Wave 4 UI plans and BEFORE verification — build/typecheck pass WITHOUT the push (types come from generated config, not the live DB), so skipping this produces a false-positive green.</what-built>
  <how-to-verify>
    1. From `C:\Development\SOPstart` in PowerShell, run the applier:
       `node scripts/apply-phase25-migrations.mjs`
    2. Confirm the printed post-apply assertions all pass:
       - `blocks WHERE organisation_id IS NULL` = 0
       - `to_regclass('public.departments')`, `block_departments`, `sop_departments`, `member_departments` all non-null
       - `SELECT 1 FROM sops LIMIT 1` returns a row (NO `42P17 infinite recursion` error)
    3. If the applier cannot connect non-interactively, run the printed fallback manually (one line, copy-pasteable):
       `npx supabase db push`  — OR paste each migration file into the Supabase SQL editor in order 00035 → 00036 → 00037.
    4. Then run the integration gate:
       `npm run test:integration`
       and confirm `departments-rls` + `sop-dept-visibility` pass.
  </how-to-verify>
  <acceptance_criteria>
    - All three migrations applied to the live DB (assertion block all-green)
    - `npm run test:integration` passes the two new specs (cross-tenant isolation + OR-visibility + no-recursion)
    - Zero `organisation_id IS NULL` blocks in the live DB (REQ-8, D-01)
  </acceptance_criteria>
  <resume-signal>Type "applied" once the assertions and integration specs are green, or paste the error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applier (service-role) → remote Postgres | Service-role bypasses RLS; mis-applied migration is the highest-impact failure surface |
| test sessions (RLS-scoped) → Postgres | Two-org sessions probe the real isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | cross-tenant department read (T-HIGH) | mitigate | `departments-rls.spec.ts` proves org B cannot read org A's department (REQ-1 acceptance) |
| T-25-02 | Denial of Service / Availability | RLS recursion 42P17 on `sops` (T-HIGH) | mitigate | applier + spec both run `SELECT FROM sops LIMIT 1` post-apply; non-zero/500 fails the gate (D-02a) |
| T-25-04 | Tampering / Data Loss | global→org migration orphaning blocks (T-HIGH) | mitigate | applier asserts `count(*) blocks WHERE organisation_id IS NULL = 0` after 00036 (REQ-8, D-01) |
| T-25-06 | Information Disclosure | service-role key handling in the applier | accept | key read from `.env.local` only (never hardcoded/committed); applier is a local dev script, mirrors existing uat-*.mjs pattern |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | no new packages; `pg` used only if already present, else manual-fallback path |
</threat_model>

<verification>
- `npx playwright test --list` shows both new specs (registration proven)
- Live apply assertion block all-green (human checkpoint)
- `npm run test:integration` green for departments-rls + sop-dept-visibility
- This plan is the [BLOCKING] gate: Wave 4 must NOT start until "applied" is signalled
</verification>

<success_criteria>
- 00035/00036/00037 are live on the remote DB with zero null-org blocks
- Cross-tenant isolation, OR-visibility, and no-recursion proven by passing integration specs
- Both specs registered in playwright.config.ts
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-02-SUMMARY.md` when done
</output>
</output>
