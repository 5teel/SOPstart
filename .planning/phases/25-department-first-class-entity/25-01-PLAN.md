---
phase: 25-department-first-class-entity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00035_departments_schema.sql
  - supabase/migrations/00036_departments_data.sql
  - supabase/migrations/00037_departments_rls_cleanup.sql
  - src/types/sop.ts
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5, REQ-8, D-01, D-02, D-02a, D-03, D-04]
user_setup: []

must_haves:
  truths:
    - "An admin can create an org-scoped department invisible to other orgs (REQ-1, cross-tenant RLS)"
    - "A block can be tagged to multiple departments or marked org-wide via blocks.all_departments (REQ-2, D-04 parity)"
    - "A SOP can be tagged to departments or marked org-wide via sops.all_departments (REQ-3, D-04)"
    - "A member can belong to multiple departments via member_departments (REQ-4)"
    - "departments.owner_user_id references an org member and goes NULL when that member is removed (REQ-5, D-03 ON DELETE SET NULL)"
    - "Worker SOP visibility OR-composes a department arm without recursion (D-02, D-02a, no 42P17)"
    - "Migration auto-creates one General dept per org and assigns all existing SOPs+blocks to it; 65 globals copied per-org with all_departments=true; zero blocks left with organisation_id NULL; category retained read-only (D-01, REQ-8)"
  artifacts:
    - path: "supabase/migrations/00035_departments_schema.sql"
      provides: "departments table + 3 junctions + all_departments columns + SECURITY DEFINER helpers + sops_visible_by_department policy"
      contains: "create table if not exists public.departments"
    - path: "supabase/migrations/00036_departments_data.sql"
      provides: "General-dept-per-org backfill + per-org global block copy + null-org delete"
      contains: "FROM public.organisations"
    - path: "supabase/migrations/00037_departments_rls_cleanup.sql"
      provides: "drop org/global block policies + drop block_suggestions"
      contains: "drop table if exists public.block_suggestions"
    - path: "src/types/sop.ts"
      provides: "Department + DepartmentWithCounts + MemberDepartment types"
      contains: "export interface Department"
  key_links:
    - from: "supabase/migrations/00035_departments_schema.sql"
      to: "public.sop_departments"
      via: "sop_in_user_departments() SECURITY DEFINER helper called by sops_visible_by_department policy"
      pattern: "sop_in_user_departments"
    - from: "supabase/migrations/00036_departments_data.sql"
      to: "public.blocks"
      via: "per-org copy of organisation_id IS NULL globals then DELETE"
      pattern: "organisation_id IS NULL"
---

<objective>
Create the Phase 25 database foundation: the `departments` first-class entity, three many-to-many junction tables (`block_departments`, `sop_departments`, `member_departments`), the `all_departments` flag on both `blocks` and `sops`, the SECURITY-DEFINER RLS helpers and the additive `sops_visible_by_department` SELECT policy, the non-destructive data migration (General dept per org + per-org global copy + null-org delete), the RLS/table cleanup that retires the org-vs-global model, and the TypeScript `Department` types that downstream actions and UI consume.

Purpose: Every other plan in this phase depends on these tables, helpers, and types existing. This is the dependency spine — written here, applied in Plan 02.
Output: Three numbered migration files (00035/00036/00037) + extended `src/types/sop.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-department-first-class-entity/25-SPEC.md
@.planning/phases/25-department-first-class-entity/25-CONTEXT.md
@.planning/phases/25-department-first-class-entity/25-RESEARCH.md
@.planning/phases/25-department-first-class-entity/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following symbols (newly-created — exclude from drift verification):
- **Tables:** `departments`, `block_departments`, `sop_departments`, `member_departments`
- **Columns:** `blocks.all_departments` (boolean), `sops.all_departments` (boolean)
- **RLS helper functions:** `current_user_department_ids()`, `sop_in_user_departments(uuid)`
- **RLS policies:** `departments_org_read`, `departments_admin_insert`, `departments_admin_update`, `departments_admin_delete`, `block_departments_read_all_auth`, `sop_departments_read_all_auth`, `member_departments_self_read`, `sops_visible_by_department`
- **Migration files:** `00035_departments_schema.sql`, `00036_departments_data.sql`, `00037_departments_rls_cleanup.sql`
- **Types:** `Department`, `DepartmentWithCounts`, `MemberDepartment` in `src/types/sop.ts`
- **Dropped:** `block_suggestions` table + its policies (sunset with the org-vs-global model)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration 00035 — departments + junctions + helpers + policies</name>
  <read_first>
    - supabase/migrations/00030_sub_trades.sql (EXACT analog: sub_trades table + users_sub_trades/sops_sub_trades junctions + current_user_sub_trades/sub_trade_id_intersects SECURITY DEFINER helpers + the additive sops_visible_by_sub_trade SELECT policy — copy this skeleton)
    - supabase/migrations/00031_fix_sops_sub_trades_rls_recursion.sql (the using(true)-on-junction fix to replicate so the new junctions never recurse)
    - supabase/migrations/00019_section_kinds_and_blocks.sql (blocks table + current_organisation_id() helper used by org-scoped policies)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 40-174 — the exact table/junction/helper/column/policy DDL skeleton for this file)
  </read_first>
  <action>
    Create `supabase/migrations/00035_departments_schema.sql` wrapped in begin;/commit;. Define: (1) `public.departments` with columns id, organisation_id (FK organisations ON DELETE CASCADE), name, code, colour (default '#3b82f6'), icon (nullable), owner_user_id (FK auth.users ON DELETE SET NULL per D-03/REQ-5), archived (default false), created_at, updated_at, and `unique (organisation_id, code)`. Enable RLS; add `departments_org_read` (using organisation_id = current_organisation_id()), and admin-only insert/update/delete policies gated on organisation_members.role in ('admin','safety_manager') AND organisation_id = current_organisation_id(). (2) Three junctions `block_departments(block_id, department_id)`, `sop_departments(sop_id, department_id)`, `member_departments(member_id, department_id, assigned_at, assigned_by)`, each with a composite PK, RLS enabled, and SELECT policy `using (true)` for block_departments and sop_departments; `member_departments_self_read` allows member_id = auth.uid() OR admin/safety_manager. Per D-02a NEVER reference sops/blocks from a junction SELECT policy. (3) `add column if not exists all_departments boolean not null default false` on both `public.blocks` and `public.sops` (D-04 parity). (4) SECURITY DEFINER helpers `current_user_department_ids()` (returns setof uuid from member_departments where member_id = auth.uid()) and `sop_in_user_departments(p_sop_id uuid)` (exists sop_departments row whose department_id in current_user_department_ids()), both `language sql stable security definer set search_path = public`. (5) Additive third permissive SELECT policy `sops_visible_by_department` on public.sops: using ( sops.all_departments = true OR not exists (select 1 from public.sop_departments sd where sd.sop_id = sops.id) OR sop_in_user_departments(sops.id) ) — this is the D-02 OR arm; do NOT modify the existing base or sops_visible_by_sub_trade policies. Do NOT rename any existing table/column (avoids the 00026 function-body-rot trap).
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('supabase/migrations/00035_departments_schema.sql','utf8'); const need=['create table if not exists public.departments','block_departments','sop_departments','member_departments','all_departments','current_user_department_ids','sop_in_user_departments','sops_visible_by_department','on delete set null']; const body=f.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n'); const miss=need.filter(t=>!body.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(/policy[\s\S]*sop_departments[\s\S]*from public.sops/i.test(body)){console.error('RECURSION: junction policy references sops');process.exit(1)} console.log('00035 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists, wrapped in a single begin;/commit; transaction
    - `departments` table has `owner_user_id ... on delete set null` (REQ-5 ownerless-on-removal)
    - All three junctions present with composite PKs; block_departments + sop_departments SELECT policies are `using (true)` (D-02a — grep finds no junction SELECT policy that references `from public.sops` or `from public.blocks`)
    - `all_departments boolean not null default false` added to BOTH blocks and sops (D-04)
    - `sop_in_user_departments` + `current_user_department_ids` are `security definer set search_path = public`
    - `sops_visible_by_department` is a NEW permissive SELECT policy (does not DROP/ALTER `sops_visible_by_sub_trade`)
  </acceptance_criteria>
  <done>00035 creates departments + 3 junctions + 2 helpers + all_departments columns + the additive dept visibility policy, with no junction→parent recursion.</done>
</task>

<task type="auto">
  <name>Task 2: Data migration 00036 — General dept per org, per-org global copy, null-org delete</name>
  <read_first>
    - supabase/migrations/00023_phase13_nz_global_block_seed.sql (analog: the 65 organisation_id=null seed block structure being migrated; confirms all 65 are null-org)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 178-238 — the exact PL/pgSQL loop: departments FIRST via RETURNING id, then sop/block junctions, then per-org global copy, then DELETE null-org, then idempotency assertion)
    - .planning/phases/25-department-first-class-entity/25-RESEARCH.md (lines 88-94, 588-596 — multi-org copy strategy + Pitfall 2/5 ordering)
  </read_first>
  <action>
    Create `supabase/migrations/00036_departments_data.sql` wrapped in begin;/commit; with a DO $$ ... $$ PL/pgSQL block. For each row in `public.organisations`: (1) INSERT one `General` department (code 'GEN', colour '#3b82f6') ON CONFLICT (organisation_id, code) DO NOTHING and capture its id (SELECT id back if the INSERT conflicted — do not rely solely on RETURNING when ON CONFLICT skips). (2) INSERT `sop_departments` rows linking that org's existing `sops` to the General dept (ON CONFLICT DO NOTHING). (3) INSERT `block_departments` rows linking that org's existing org-owned `blocks` (organisation_id = org.id) to the General dept. (4) For each global block (organisation_id IS NULL), INSERT a per-org copy with organisation_id=org.id, all_departments=true, carrying over kind_slug, name, category_tags, free_text_tags, created_by, current_version_id — do NOT update globals in place (other orgs would lose access). After the org loop: DELETE FROM public.blocks WHERE organisation_id IS NULL. Then RAISE EXCEPTION if `(select count(*) from public.blocks where organisation_id is null) > 0`. Per D-01 do NOT drop or null the `category`/`category_tag` columns — they remain read-only. Add a `RAISE NOTICE` no-op-safe path when zero orgs exist (CI/test). The migration must be idempotent (re-runnable): the ON CONFLICT guards + the null-org DELETE make a second run a no-op.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('supabase/migrations/00036_departments_data.sql','utf8'); const b=f.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n'); const need=['FROM public.organisations','sop_departments','block_departments','all_departments','DELETE FROM public.blocks','RAISE EXCEPTION']; const miss=need.filter(t=>!b.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(/update\s+public\.blocks\s+set\s+organisation_id/i.test(b)){console.error('IN-PLACE global update detected (must copy per-org)');process.exit(1)} if(/drop\s+column.*category/i.test(b)){console.error('category column must be retained (D-01)');process.exit(1)} console.log('00036 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - General dept inserted per org BEFORE any junction rows (Pitfall 5 ordering)
    - Globals are COPIED per-org with all_departments=true, never updated in place (grep finds no `update public.blocks set organisation_id`)
    - `DELETE FROM public.blocks WHERE organisation_id IS NULL` present, followed by a count assertion that RAISES on remaining null-org blocks
    - No `drop column ... category` / `category_tag` (D-01 non-destructive)
    - Zero-org case does not error
  </acceptance_criteria>
  <done>00036 backfills General depts, assigns existing SOPs+blocks, copies globals per-org, deletes null-org rows, and asserts zero orphans — non-destructively.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup migration 00037 + Department types</name>
  <read_first>
    - supabase/migrations/00031_fix_sops_sub_trades_rls_recursion.sql (analog: drop-then-replace policy pattern)
    - supabase/migrations/00022_block_library_phase13.sql (the org/global block policies + block_suggestions table being retired — read its policy names so the DROPs are exact)
    - src/types/sop.ts (self — the existing SubTrade interface is the template for Department)
    - .planning/phases/25-department-first-class-entity/25-PATTERNS.md (lines 242-260 cleanup DDL; lines 947-979 Department type shape)
  </read_first>
  <action>
    Create `supabase/migrations/00037_departments_rls_cleanup.sql` wrapped in begin;/commit;. `drop policy if exists` for the null-org global read policy and the platform-admin global write policy on `public.blocks` (use the exact names from 00019/00022 confirmed by reading those files), `drop policy if exists` the block_suggestions policies, then `drop table if exists public.block_suggestions cascade`. Do NOT drop `is_platform_admin()` / platform-admin guard functions if any surviving policy still references them — only drop the block-suggestions-specific objects (verify no other consumer before dropping). Separately, MODIFY `src/types/sop.ts`: add `Department` (id, organisation_id, name, code, colour, icon: string|null, owner_user_id: string|null, archived: boolean, created_at, updated_at), `DepartmentWithCounts extends Department` (people_count, sop_count, block_count: number), and `MemberDepartment` (member_id, department_id) — mirror the existing `SubTrade` interface style. Do not remove existing types.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('supabase/migrations/00037_departments_rls_cleanup.sql','utf8');if(!/drop table if exists public.block_suggestions/i.test(m)){console.error('missing block_suggestions drop');process.exit(1)};const t=fs.readFileSync('src/types/sop.ts','utf8');['export interface Department','DepartmentWithCounts','people_count','owner_user_id'].forEach(s=>{if(!t.includes(s)){console.error('missing type',s);process.exit(1)}});console.log('00037+types OK')" && npx tsc --noEmit 2>&1 | (grep -E "src/types/sop.ts" && exit 1 || exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - 00037 drops the org/global block policies + block_suggestions table+policies; does NOT drop functions still referenced by surviving policies
    - `src/types/sop.ts` exports `Department`, `DepartmentWithCounts`, `MemberDepartment` with the exact field set above
    - `npx tsc --noEmit` reports no errors originating in `src/types/sop.ts`
  </acceptance_criteria>
  <done>00037 retires the org-vs-global block model + block_suggestions; sop.ts exposes the Department type surface for downstream consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker/admin browser → Supabase (RLS) | Authenticated tenant reads/writes cross into shared Postgres; org isolation enforced by RLS only |
| migration runner → blocks/sops data | Bulk transform can orphan or expose content if mis-sequenced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | `departments` + junctions cross-tenant read (T-HIGH) | mitigate | `departments_org_read` uses `organisation_id = current_organisation_id()`; junctions are non-sensitive UUID pairs gated at the parent; cross-tenant isolation verified in Plan 02 (REQ-1 acceptance: org A dept invisible to org B) |
| T-25-02 | Denial of Service / Availability | `sops_visible_by_department` recursion → 42P17 500-on-all-sops (T-HIGH, the 00030/00031 trap) | mitigate | OR arm calls `sop_in_user_departments()` SECURITY DEFINER helper; all junction SELECT policies are `using(true)`; no junction policy references `sops`/`blocks`; Plan 02 runs `SELECT * FROM sops LIMIT 1` post-apply to prove no recursion (D-02a) |
| T-25-03 | Elevation of Privilege | injecting arbitrary `organisation_id` on department insert | mitigate | `departments_admin_insert` WITH CHECK pins `organisation_id = current_organisation_id()` and requires admin/safety_manager role; client cannot set a foreign org |
| T-25-04 | Tampering / Data Loss | global→org + category migration deleting/orphaning blocks (T-HIGH) | mitigate | D-01 non-destructive: copy globals per-org BEFORE delete; `category` retained read-only; end-of-migration `RAISE EXCEPTION` asserts zero null-org blocks; no block row deleted except the now-copied null-org originals (REQ-8) |
| T-25-05 | Tampering | (privilege confusion) owner mistaken as an authz grant (T-MED) | mitigate | D-03: `owner_user_id` is a label-only column; this plan adds NO owner-based write/approve policy — owner grants zero permissions |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none); nothing to install |
</threat_model>

<verification>
- `node` AST/grep guards on all three migration files pass (Tasks 1-3 verify blocks)
- No junction SELECT policy references a parent table (recursion guard in Task 1 verify)
- `npx tsc --noEmit` clean for `src/types/sop.ts`
- Behavioural DB invariants (zero null-org blocks, cross-tenant isolation, no 42P17) are asserted in Plan 02 AFTER the migrations are applied to the live DB — build/typecheck cannot prove these
</verification>

<success_criteria>
- 00035/00036/00037 exist with the DDL/data/cleanup described, passing the grep guards
- `src/types/sop.ts` exposes Department/DepartmentWithCounts/MemberDepartment
- Migration set is non-destructive (D-01) and recursion-safe (D-02a) by construction
</success_criteria>

<output>
Create `.planning/phases/25-department-first-class-entity/25-01-SUMMARY.md` when done
</output>
