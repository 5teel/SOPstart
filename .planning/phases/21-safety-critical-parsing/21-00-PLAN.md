---
phase: 21-safety-critical-parsing
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/integration/scp-source-viewer.test.ts
  - tests/integration/scp-ai-reviewer.test.ts
  - tests/integration/scp-verify-checklist.test.ts
  - tests/integration/scp-parse-pipeline.test.ts
autonomous: true
requirements:
  - SCP-VIEWER-01
  - SCP-VIEWER-02
  - SCP-VIEWER-03
  - SCP-VIEWER-04
  - SCP-VIEWER-05
  - SCP-AI-01
  - SCP-AI-02
  - SCP-AI-03
  - SCP-AI-04
  - SCP-AI-05
  - SCP-AI-06
  - SCP-AI-07
  - SCP-AI-08
  - SCP-VERIFY-01
  - SCP-VERIFY-02
  - SCP-VERIFY-03
  - SCP-VERIFY-04
  - SCP-VERIFY-05
  - SCP-VERIFY-06
  - SCP-PARSE-01
  - SCP-PARSE-02
  - SCP-PARSE-03
  - SCP-PARSE-04

must_haves:
  truths:
    - "Every one of the 23 SCP requirements has at least one matching test stub"
    - "Test stubs use `test.fixme` so CI green is preserved"
    - "Each stub names the SCP-XX requirement(s) it covers in its describe/test title"
    - "Each stub contains a comment block documenting the acceptance criteria so the executor in Waves 1-4 can flip `fixme` → live by reading the stub alone"
  artifacts:
    - path: "tests/integration/scp-source-viewer.test.ts"
      provides: "Playwright stubs for SCP-VIEWER-01..05"
      min_lines: 50
    - path: "tests/integration/scp-ai-reviewer.test.ts"
      provides: "Playwright stubs for SCP-AI-01..08"
      min_lines: 80
    - path: "tests/integration/scp-verify-checklist.test.ts"
      provides: "Playwright stubs for SCP-VERIFY-01..06"
      min_lines: 60
    - path: "tests/integration/scp-parse-pipeline.test.ts"
      provides: "Playwright stubs for SCP-PARSE-01..04"
      min_lines: 40
  key_links:
    - from: "tests/integration/scp-*.test.ts"
      to: ".planning/REQUIREMENTS.md § v4.0 → Safety-Critical Parsing"
      via: "test title prefix `SCP-XX:`"
      pattern: "test\\.fixme\\(['\"]SCP-"
---

<objective>
Land Playwright integration-test scaffolds for all 23 SCP requirements BEFORE any
production code in Waves 1-4 ships. All stubs are `test.fixme` so CI stays green
during Phase 21 execution. Executors in later waves flip `fixme` → live tests as
each requirement is implemented.

Purpose: Lock down the verification contract before implementation drift. Mirrors
the proven `tests/integration/sb-*.test.ts` pattern from Phases 11-15.

Output: Four test files, one per requirement family.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-safety-critical-parsing/21-CONTEXT.md
@.planning/archive/v3.0/20-conversion-pipeline-v2/20-SPEC.md
@tests/integration/sb-builder-infrastructure.test.ts
@tests/integration/sb-layout-editor.test.ts
@tests/integration/sop-parsing.test.ts

<interfaces>
Existing test-stub pattern (extracted from `tests/integration/sb-builder-infrastructure.test.ts`):

```typescript
import { test, expect } from '@playwright/test'

test.describe('SB-INFRA-03: bundle isolation', () => {
  test.fixme('worker route bundle excludes Puck/Konva/Yjs imports', async ({ page }) => {
    // Acceptance: CI bundle check passes; First Load JS for /sops/[sopId]
    // on mobile within 2 KB of pre-Phase-15 baseline.
    expect(true).toBe(true)
  })
})
```

Stub conventions:
- Filename: `scp-{family}.test.ts` (kebab-case, single hyphen)
- `test.fixme(...)` (NOT `test.skip`) so the linter still type-checks the body
- Each `test.fixme` title MUST start with the SCP-XX ID followed by `: ` then a verb-noun phrase
- Each body contains: `// Acceptance: <criteria from REQUIREMENTS.md and/or 20-SPEC.md>`
- The body must contain at least one `expect()` so when `fixme` is removed the harness wires up correctly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub SCP-VIEWER-01..05 (side-by-side source viewer) + SCP-PARSE-01..04 (Phase 20 contract)</name>
  <files>
    tests/integration/scp-source-viewer.test.ts,
    tests/integration/scp-parse-pipeline.test.ts
  </files>
  <action>
Create two new Playwright test files using the `test.fixme` pattern from `tests/integration/sb-builder-infrastructure.test.ts`.

**`tests/integration/scp-source-viewer.test.ts`** — one `test.describe` block, five `test.fixme` cases:
- `SCP-VIEWER-01: source PDF / DOCX / image rendered alongside parsed blocks in builder`
- `SCP-VIEWER-02: clicking parsed block scrolls source viewer to exact passage`
- `SCP-VIEWER-03: clicking source region highlights corresponding parsed block`
- `SCP-VIEWER-04: source viewer persistent throughout review (not modal, not dismissible)`
- `SCP-VIEWER-05: source viewer works uniformly for DOCX, PDF, image, and video-transcript formats`

Each body block must include `// Acceptance:` comment with the matching 20-SPEC acceptance line (e.g. for VIEWER-02 reference SPEC requirement 4's 200ms p95 target from Spike 002).

**`tests/integration/scp-parse-pipeline.test.ts`** — one `test.describe` block, four `test.fixme` cases:
- `SCP-PARSE-01: photos/diagrams/charts/tables extracted with step-level provenance metadata`
- `SCP-PARSE-02: parsed drafts land directly as Puck layout_data in builder`
- `SCP-PARSE-03: side-by-side source viewer integrated into builder review surface`
- `SCP-PARSE-04: AI reviewer auto-runs as part of parse pipeline`

PARSE-01 body must include `expect(provenance).toMatchObject({ source_type: expect.any(String), region: expect.any(Object) })` skeleton even under fixme so the executor only needs to wire data.

Implementing per D-21-10 (Wave 0 stubs land first, all `test.fixme`).
  </action>
  <verify>
    <automated>npx playwright test tests/integration/scp-source-viewer.test.ts tests/integration/scp-parse-pipeline.test.ts --list 2>&1 | grep -E "SCP-(VIEWER|PARSE)-" | wc -l</automated>
  </verify>
  <done>
- Both files exist
- `--list` returns exactly 9 tests (5 VIEWER + 4 PARSE)
- All tests show `[fixme]` annotation
- `npm run lint` clean on both new files
- `npm test -- --grep "SCP-"` returns 9 skipped (no failures)
  </done>
</task>

<task type="auto">
  <name>Task 2: Stub SCP-AI-01..08 (AI reviewer) + SCP-VERIFY-01..06 (per-block verify gate)</name>
  <files>
    tests/integration/scp-ai-reviewer.test.ts,
    tests/integration/scp-verify-checklist.test.ts
  </files>
  <action>
**`tests/integration/scp-ai-reviewer.test.ts`** — one `test.describe` block, eight `test.fixme` cases:
- `SCP-AI-01: omission check flags safety-critical content dropped from source`
- `SCP-AI-02: anchoring check confirms photos/diagrams attached to right step`
- `SCP-AI-03: step-image alignment check confirms photo depicts its anchored step's action`
- `SCP-AI-04: table fidelity check preserves dosages/torques/temps exactly`
- `SCP-AI-05: terminology consistency check matches org SOP vocabulary`
- `SCP-AI-06: all five jobs auto-run on first parse without admin invocation`
- `SCP-AI-07: admin can manually re-run any job after editing parsed draft`
- `SCP-AI-08: per-parse cost bounded with prompt-caching + per-org spend cap`

For AI-01, AI-02 use Spike 003 fixture references in the comment block (`@.planning/spikes/003-ai-reviewer-omission-anchoring/experiment/fixture/expected-defects.json`).

For AI-08, the acceptance comment must reference both the prompt-cache assertion (`cache_read_input_tokens > 0` on 2nd-onward calls) AND the per-org cap (429 response when org daily cap exhausted) per CONV-09 (5 re-runs/SOP/day) and the per-org daily spend cap.

**`tests/integration/scp-verify-checklist.test.ts`** — one `test.describe` block, six `test.fixme` cases:
- `SCP-VERIFY-01: every block in draft carries verified_by boolean`
- `SCP-VERIFY-02: publish button hard-disabled until 100% of blocks verified`
- `SCP-VERIFY-03: verification timestamps + admin user_id stored immutably for audit`
- `SCP-VERIFY-04: re-editing a block requires re-verification of that specific block only`
- `SCP-VERIFY-05: no bulk-verify or skip-all option exists in UI`
- `SCP-VERIFY-06: verify UI guides admin eye-flow such that they read each block`

VERIFY-02 body must include the canonical Playwright pattern to read `publishButton.isDisabled()` AND the 50/50 → enabled flip from Spike 004.

VERIFY-05 body must `expect(await page.locator('button:has-text("Approve all")').count()).toBe(0)` as the bulk-verify prohibition assertion. This stub is the test that locks D-21-07 in code.
  </action>
  <verify>
    <automated>npx playwright test tests/integration/scp-ai-reviewer.test.ts tests/integration/scp-verify-checklist.test.ts --list 2>&1 | grep -E "SCP-(AI|VERIFY)-" | wc -l</automated>
  </verify>
  <done>
- Both files exist
- `--list` returns exactly 14 tests (8 AI + 6 VERIFY)
- All tests show `[fixme]` annotation
- `npm run lint` clean
- `npm test -- --grep "SCP-"` returns 23 total skipped (5+8+6+4) across all four files
- Combined assertion: `npx playwright test --list 2>&1 | grep -c "SCP-"` returns 23
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → test runner | Stubs must remain non-failing; a flipped-on stub before the feature exists turns CI red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-00-01 | Tampering | `test.fixme` accidentally removed before feature ships | mitigate | CI grep: `grep -r "test.fixme" tests/integration/scp-` → confirm count ≥ 23 until Wave 4 completes |
| T-21-00-02 | Repudiation | Stub doesn't actually map to a SCP requirement | mitigate | Each stub title MUST start `SCP-XX:` ID prefix; lint rule in `npm run lint` config |
</threat_model>

<verification>
- All four test files exist and compile (no TypeScript errors)
- 23 distinct `test.fixme` blocks across the four files, each with `SCP-XX:` title prefix
- `npm test` passes (no failures because all stubs are fixme)
- `npm run lint` passes
- Each stub has at least one `expect()` call so the harness wires up when fixme is removed
</verification>

<success_criteria>
- 23 of 23 SCP requirements have a matching `test.fixme` stub
- CI green (no test failures because everything is fixme)
- Subsequent waves (21-01 through 21-04) have a clear "flip fixme → live" verification path per requirement
</success_criteria>

<output>
After completion, create `.planning/phases/21-safety-critical-parsing/21-00-SUMMARY.md` documenting:
- Final stub count per family
- File paths and line counts
- The exact `npx playwright test --list` output line count
</output>
