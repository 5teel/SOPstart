---
phase: 03-worker-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/(protected)/sops/page.tsx
  - src/app/(protected)/sops/[sopId]/page.tsx
  - src/components/sop/SopLibraryCard.tsx
  - src/components/sop/SopSectionTabs.tsx
  - src/components/sop/SectionContent.tsx
  - src/components/sop/SopSearchInput.tsx
  - src/components/sop/CategoryBottomSheet.tsx
autonomous: true
requirements:
  - WORK-03
  - WORK-04
  - MGMT-02
  - MGMT-03
  - MGMT-04
must_haves:
  truths:
    - "Worker sees all assigned SOPs in a scrollable list on /sops"
    - "Worker can search SOPs by title, number, category, or department with instant filtering"
    - "Worker can filter SOPs by category via bottom sheet (mobile) or sidebar (desktop)"
    - "Worker can view any SOP section directly via tabbed quick-reference mode"
    - "Worker can jump to any section tab without walking through steps first"
    - "Assigned SOPs appear first in the library"
  artifacts:
    - path: "src/app/(protected)/sops/page.tsx"
      provides: "Worker SOP library page with search and category filter"
    - path: "src/app/(protected)/sops/[sopId]/page.tsx"
      provides: "SOP detail page with quick-reference tab bar"
    - path: "src/components/sop/SopLibraryCard.tsx"
      provides: "SOP card in library list with cache status and badges"
    - path: "src/components/sop/SopSectionTabs.tsx"
      provides: "Horizontal scrollable tab bar for section types"
    - path: "src/components/sop/SectionContent.tsx"
      provides: "Renders section content with type-specific styling"
    - path: "src/components/sop/SopSearchInput.tsx"
      provides: "Full-screen search overlay with instant filter"
    - path: "src/components/sop/CategoryBottomSheet.tsx"
      provides: "Mobile bottom sheet / desktop sidebar for category filtering"
  key_links:
    - from: "src/app/(protected)/sops/page.tsx"
      to: "src/hooks/useAssignedSops.ts"
      via: "TanStack Query hook reading from Dexie"
      pattern: "useAssignedSops"
    - from: "src/app/(protected)/sops/page.tsx"
      to: "src/hooks/useSopSync.ts"
      via: "Triggers sync on mount/online"
      pattern: "useSopSync"
    - from: "src/app/(protected)/sops/[sopId]/page.tsx"
      to: "src/hooks/useSopDetail.ts"
      via: "Loads full SOP with sections from Dexie"
      pattern: "useSopDetail"
    - from: "src/components/sop/SopSectionTabs.tsx"
      to: "src/components/sop/SectionContent.tsx"
      via: "Active tab renders corresponding SectionContent"
      pattern: "SectionContent"
---

<objective>
Build the SOP library page (search, category filter, SOP cards) and the SOP detail/quick-reference page (tabbed section navigation). Workers can browse, search, and directly access any SOP section.

Purpose: Workers need to find SOPs quickly and look up specific sections (PPE, Emergency) in seconds during real work scenarios.
Output: 7 files covering the library list, search, category filter, SOP detail, section tabs, and section content rendering.
</objective>

<execution_context>
@C:/Users/Simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-worker-experience/03-CONTEXT.md
@.planning/phases/03-worker-experience/03-UI-SPEC.md
@.planning/phases/03-worker-experience/03-RESEARCH.md
@.planning/phases/03-worker-experience/03-01-SUMMARY.md

<interfaces>
From src/hooks/useAssignedSops.ts (created in Plan 01):
```typescript
export function useAssignedSops(options?: { category?: string; search?: string }): UseQueryResult<CachedSop[]>
```

From src/hooks/useSopDetail.ts (created in Plan 01):
```typescript
export function useSopDetail(sopId: string): UseQueryResult<SopWithSections | undefined>
```

From src/hooks/useSopSync.ts (created in Plan 01):
```typescript
export function useSopSync(): { syncing: boolean; lastSyncResult: { synced: number; errors: string[] } | null }
```

From src/lib/offline/db.ts (created in Plan 01):
```typescript
export const db: Dexie & {
  sops: EntityTable<CachedSop, 'id'>
  sections: EntityTable<SopSection, 'id'>
  steps: EntityTable<SopStep, 'id'>
  images: EntityTable<SopImage, 'id'>
  syncMeta: EntityTable<{ key: string; value: string }, 'key'>
}
```

From src/types/sop.ts:
```typescript
export interface SopSection { id: string; sop_id: string; section_type: string; title: string; content: string | null; sort_order: number; ... }
export interface SopWithSections extends Sop { sop_sections: (SopSection & { sop_steps: SopStep[]; sop_images: SopImage[] })[] }
```

From src/components/layout/BottomTabBar.tsx:
```typescript
// SOPs tab currently links to /dashboard — will need updating to /sops
const tabs = [
  { label: 'SOPs', href: '/dashboard', Icon: DocumentIcon },
  { label: 'Activity', href: '/activity', Icon: ClockIcon },
  { label: 'Profile', href: '/profile', Icon: UserIcon },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SOP library page with cards, search overlay, and category filter</name>
  <files>
    src/app/(protected)/sops/page.tsx,
    src/components/sop/SopLibraryCard.tsx,
    src/components/sop/SopSearchInput.tsx,
    src/components/sop/CategoryBottomSheet.tsx,
    src/components/layout/BottomTabBar.tsx
  </files>
  <read_first>
    src/hooks/useAssignedSops.ts,
    src/hooks/useSopSync.ts,
    src/lib/offline/db.ts,
    src/types/sop.ts,
    src/components/layout/BottomTabBar.tsx,
    .planning/phases/03-worker-experience/03-UI-SPEC.md (lines 113-158 for Page 1, lines 297-327 for C-01, lines 590-689 for C-07/C-08, lines 833-858 for C-14)
  </read_first>
  <action>
**0. Update BottomTabBar SOPs link:**
- Change the SOPs tab `href` from `/dashboard` to `/sops`
- Update the `isActive` check to match `/sops` route prefix

**1. Create `src/components/sop/SopLibraryCard.tsx`:**
- `'use client'` directive
- Props: `{ sop: CachedSop; isCached: boolean }`
- Link wrapper: `<Link href={/sops/${sop.id}}>`
- Base: `flex items-start gap-4 p-4 bg-steel-800 rounded-xl hover:bg-steel-700 active:bg-steel-600 transition-colors cursor-pointer min-h-[88px] border border-transparent hover:border-steel-600`
- Left column (`flex-shrink-0`): lucide `FileText` size={28} `text-steel-400`, below it cache readiness dot:
  - Cached: `w-2.5 h-2.5 rounded-full bg-green-500` title="Available offline"
  - Not cached: `w-2.5 h-2.5 rounded-full bg-steel-600`
- Middle column (`flex-1 min-w-0`):
  - Title: `text-base font-semibold text-steel-100 leading-snug` (truncate with `line-clamp-2`)
  - Category + department: `text-xs text-steel-400 mt-0.5`
  - Badge row (`flex items-center gap-2 mt-2 flex-wrap`):
    - "Assigned" badge: `inline-flex items-center gap-1 px-2 py-0.5 bg-brand-yellow/20 text-brand-yellow text-xs font-semibold rounded`
- Right column (`flex-shrink-0 flex flex-col items-end justify-between self-stretch`):
  - lucide `ChevronRight` size={18} `text-steel-400`
  - Step count: `text-xs text-steel-400 mt-auto`

**2. Create `src/components/sop/SopSearchInput.tsx`:**
- `'use client'` directive
- Props: `{ onSearch: (term: string) => void; onClose: () => void }`
- Full-screen overlay: `fixed inset-0 z-40 bg-steel-900 flex flex-col`
- Transition: `transition-transform duration-200 ease-out` — slides from `translate-y-[-100%]` to `translate-y-0`
- Search input bar: `flex items-center gap-3 px-4 py-3 border-b border-steel-700`
  - lucide `Search` size={20} `text-steel-400`
  - `<input>` auto-focused, `flex-1 bg-transparent text-base text-steel-100 placeholder:text-steel-400 outline-none h-[40px]`, placeholder "Search SOPs..."
  - Cancel button: `text-sm font-medium text-brand-yellow hover:text-amber-400 flex-shrink-0 px-2`
- Results area: `flex-1 overflow-y-auto px-4 py-4 flex flex-col gap-2`
- Input triggers `onSearch` on every keypress (instant filter for Dexie in-memory data)
- No results: empty state with lucide `SearchX` and "Nothing matched"
- Tapping a result: handled by SopLibraryCard's Link — overlay should close on navigation

**3. Create `src/components/sop/CategoryBottomSheet.tsx`:**
- `'use client'` directive
- Props: `{ categories: { name: string; count: number }[]; activeCategory: string | null; onSelect: (category: string | null) => void; open: boolean; onClose: () => void }`
- Mobile bottom sheet (below `lg:` breakpoint):
  - Backdrop: `fixed inset-0 z-30 bg-black/60 backdrop-blur-sm` — tap dismisses
  - Sheet: `fixed bottom-0 left-0 right-0 z-40 bg-steel-800 rounded-t-2xl shadow-2xl max-h-[70vh] overflow-hidden flex flex-col`
  - Handle: `mx-auto mt-3 mb-0 w-10 h-1 bg-steel-600 rounded-full flex-shrink-0`
  - Header: "Filter by category" + Clear button (if filter active, `text-brand-yellow`)
  - Category rows: `flex items-center justify-between px-4 h-[56px] rounded-xl transition-colors cursor-pointer`
    - Active: `bg-brand-yellow/15 border border-brand-yellow/30` with lucide `Check` prefix
    - Default: `hover:bg-steel-700`
    - Category name + count chip
  - "All categories" row at top (clears filter)
- Desktop sidebar (lg+ breakpoint): NOT a sheet, rendered as `w-[240px] flex-shrink-0 sticky top-0 h-screen overflow-y-auto py-6 px-3 border-r border-steel-700 bg-steel-900`
  - Same category rows but h-[44px]

**4. Create `src/app/(protected)/sops/page.tsx`:**
- `'use client'` directive
- Import `useAssignedSops`, `useSopSync`, `SopLibraryCard`, `SopSearchInput`, `CategoryBottomSheet`, `db`
- Trigger sync on mount: `useSopSync()`
- State: `searchOpen` (boolean), `searchTerm` (string), `activeCategory` (string | null), `categorySheetOpen` (boolean)
- Query: `useAssignedSops({ category: activeCategory, search: searchTerm })`
- Compute categories: derive from all SOPs' `category` field (unique values with counts) — use a separate `useQuery` reading `db.sops.orderBy('category').uniqueKeys()` or compute from full list
- Get last sync time from `db.syncMeta.get('lastSync')`
- Layout (mobile):
  - Sticky header (h-[56px]): app name left, search icon right (lucide `Search`, opens overlay)
  - Page heading: "Your SOPs" `text-2xl font-bold text-steel-100 mb-1`
  - Metadata line: `"{count} procedures assigned to your role"` + "Synced {relative time}" `text-xs text-steel-400`
  - Category filter pill: `"All categories"` or active category name, tap opens bottom sheet
  - SOP list: `flex flex-col gap-3`
  - Render `<SopLibraryCard>` for each SOP
  - Check cache status per SOP: query `db.sops.get(sop.id)` — presence means cached (since all data is from Dexie, all are cached)
- Layout (desktop lg+): sidebar + content area per UI-SPEC
- Empty state (no assigned SOPs): lucide `ClipboardList` size={48}, "No SOPs assigned yet", "Your admin hasn't assigned any SOPs to you yet. Give them a nudge."
- Search overlay: conditionally render `<SopSearchInput>` when `searchOpen` is true
  - Results: pass filtered SOPs from `useAssignedSops({ search: searchTerm })`
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx tsc --noEmit src/app/\(protected\)/sops/page.tsx src/components/sop/SopLibraryCard.tsx src/components/sop/SopSearchInput.tsx src/components/sop/CategoryBottomSheet.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - BottomTabBar.tsx SOPs tab href is "/sops" (not "/dashboard")
    - sops/page.tsx imports useAssignedSops and useSopSync
    - sops/page.tsx renders SopLibraryCard for each SOP
    - SopLibraryCard.tsx has min-h-[88px] and renders title, category, cache dot, chevron
    - SopSearchInput.tsx has auto-focus on input and fires onSearch on input change
    - CategoryBottomSheet.tsx renders bottom sheet on mobile (z-40) and sidebar on desktop (w-[240px])
    - Empty state renders when no SOPs are assigned
    - TypeScript compiles without errors for all 4 new files
  </acceptance_criteria>
  <done>SOP library page shows assigned SOPs with search and category filtering, bottom tab bar links to /sops</done>
</task>

<task type="auto">
  <name>Task 2: Create SOP detail page with quick-reference tab bar and section content</name>
  <files>
    src/app/(protected)/sops/[sopId]/page.tsx,
    src/components/sop/SopSectionTabs.tsx,
    src/components/sop/SectionContent.tsx
  </files>
  <read_first>
    src/hooks/useSopDetail.ts,
    src/types/sop.ts,
    src/lib/offline/db.ts,
    src/components/sop/SopImageInline.tsx,
    .planning/phases/03-worker-experience/03-UI-SPEC.md (lines 160-188 for Page 2, lines 330-380 for C-02, lines 765-802 for C-12, lines 805-830 for C-13)
  </read_first>
  <action>
**1. Create `src/components/sop/SopSectionTabs.tsx`:**
- `'use client'` directive
- Props: `{ sections: SopSection[]; activeType: string; onTabChange: (type: string) => void }`
- Container: `flex overflow-x-auto scrollbar-none bg-steel-900 border-b border-steel-700 -mx-0 px-4 gap-0`
- Generate tabs dynamically from the sections array (sorted by sort_order)
- Each tab button: `flex-shrink-0 flex flex-col items-center justify-end px-4 h-[52px] gap-1 relative whitespace-nowrap text-[13px] font-semibold transition-colors`
- Tab colour semantics (based on section_type):
  - hazards: active `text-red-400 border-red-400`
  - ppe: active `text-blue-400 border-blue-400`
  - steps: active `text-brand-yellow border-brand-yellow`
  - emergency: active `text-red-400 border-red-400`
  - other: active `text-steel-100 border-steel-100`
- Inactive: `text-steel-400 hover:text-steel-100`
- Active indicator: `border-b-2` in the section colour
- Tab labels: map section_type to display names: hazards->"Hazards", ppe->"PPE", steps->"Steps", emergency->"Emergency", overview->"Overview", notes->"Notes", default->title-cased
- Tab icon prefix (lucide, 16px, matching text colour): hazards->`AlertTriangle`, ppe->`ShieldCheck`, steps->`ListChecks`, emergency->`Siren`

**2. Create `src/components/sop/SectionContent.tsx`:**
- `'use client'` directive
- Props: `{ section: SopSection & { sop_steps: SopStep[]; sop_images: SopImage[] } }`
- Renders content differently based on `section.section_type`:
- **Hazards and Emergency:** `bg-red-500/10 border border-red-500/30 rounded-xl p-5 mb-4`
  - Header: lucide `AlertTriangle` (hazards) or `Siren` (emergency) size={18} `text-red-400` + title in uppercase
  - Content as bullet list with `text-red-400` bullets, or render `section.content` as text
- **PPE:** `bg-blue-500/10 border border-blue-500/30 rounded-xl p-5 mb-4`
  - Header: lucide `ShieldCheck` `text-blue-400` + title
  - PPE items as chips: `inline-flex items-center gap-1.5 px-3 py-1.5 bg-blue-500/15 text-blue-300 text-sm font-medium rounded-lg border border-blue-500/30 m-1`
  - Parse content by newlines or bullet points to create individual chips
- **Steps (quick reference — read-only):** `flex flex-col gap-3`
  - Each step: `flex items-start gap-4 p-4 bg-steel-800 rounded-xl border border-steel-700`
  - Step number: `text-[13px] font-bold text-steel-400 w-6 flex-shrink-0 pt-0.5 tabular-nums`
  - Step text: `text-base text-steel-100 leading-relaxed flex-1`
  - Inline images for each step via `<SopImageInline>`
- **Info/Other:** `bg-steel-800 border border-steel-700 rounded-xl p-5 mb-4`
  - No colour accent, render content as text

**3. Create `src/app/(protected)/sops/[sopId]/page.tsx`:**
- `'use client'` directive
- Import `useSopDetail`, `SopSectionTabs`, `SectionContent`
- Import lucide: `ArrowLeft`, `Download`, `CheckCircle`
- Get `sopId` from `useParams()`
- Load SOP: `const { data: sop, isLoading } = useSopDetail(sopId as string)`
- State: `activeTab` (string, defaults to 'steps' or first section's section_type)
- Track cache status: since SOP loaded from Dexie, it's cached. Show download icon that shows status.
- Layout:
  - Sticky header (h-[56px]): back button (lucide `ArrowLeft`, navigates to `/sops`), SOP title (truncated), cache icon (lucide `CheckCircle` green if cached, `Download` if not)
  - Sticky tab bar (top-[56px]): `<SopSectionTabs>` with sections derived from `sop.sop_sections`
  - Scroll area: `flex-1 overflow-y-auto px-4 py-6 pb-[80px]`
  - Render `<SectionContent>` for the active tab's section
  - Bottom action bar (ONLY on Steps tab): sticky bottom, "Start Walkthrough" button — `w-full h-[72px] bg-brand-yellow text-steel-900 font-bold text-lg rounded-xl` linking to `/sops/${sopId}/walkthrough`
  - If offline and SOP not cached: show offline state per UI-SPEC I-09
- Loading: skeleton pulse
- Not found: error state with back link
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx tsc --noEmit src/app/\(protected\)/sops/\[sopId\]/page.tsx src/components/sop/SopSectionTabs.tsx src/components/sop/SectionContent.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sops/[sopId]/page.tsx imports useSopDetail and renders SopSectionTabs + SectionContent
    - sops/[sopId]/page.tsx has a "Start Walkthrough" button (h-[72px] bg-brand-yellow) visible only on Steps tab
    - SopSectionTabs.tsx renders tabs with section-specific colours (red for hazards, blue for PPE, yellow for steps)
    - SopSectionTabs.tsx uses h-[52px] tab height and overflow-x-auto
    - SectionContent.tsx renders hazards with bg-red-500/10, PPE with bg-blue-500/10, steps as numbered list
    - SectionContent.tsx renders inline images via SopImageInline
    - TypeScript compiles without errors for all 3 files
  </acceptance_criteria>
  <done>SOP detail page with quick-reference tabs allows direct access to any section, "Start Walkthrough" button links to walkthrough</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run build` completes without errors
- /sops route renders SOP library with assigned SOPs
- /sops/[sopId] route renders tab bar with section content
- Search filters SOPs in real time
- Category filter works via bottom sheet (mobile)
</verification>

<success_criteria>
- SOP library page at /sops shows all assigned SOPs (MGMT-02)
- Search works by title, number, category, department (MGMT-03)
- Category filter available via bottom sheet/sidebar (MGMT-04)
- SOP detail page has tabbed quick-reference for all sections (WORK-03)
- Worker can jump to any section directly via tab tap (WORK-04)
- "Start Walkthrough" button links to walkthrough route
- Empty states render appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-experience/03-03-SUMMARY.md`
</output>
