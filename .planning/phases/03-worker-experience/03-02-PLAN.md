---
phase: 03-worker-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx
  - src/app/(protected)/sops/[sopId]/walkthrough/page.tsx
  - src/components/sop/SafetyAcknowledgement.tsx
  - src/components/sop/WalkthroughList.tsx
  - src/components/sop/StepItem.tsx
  - src/components/sop/StepProgress.tsx
  - src/components/sop/SopImageInline.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-05
  - WORK-06
  - WORK-09
  - WORK-10
must_haves:
  truths:
    - "Worker can walk through an SOP step-by-step with a scrolling list, marking each step complete by tapping"
    - "Worker can navigate back by tapping a completed step to undo it"
    - "Hazard and PPE cards are shown as a mandatory full-screen gate before walkthrough steps appear"
    - "Images display inline below step text with tap-to-zoom via lightbox"
    - "All primary actions (Mark Complete, safety acknowledge) use 72px+ tap targets"
    - "Walkthrough is full-screen without BottomTabBar, optimised for one-handed use"
  artifacts:
    - path: "src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx"
      provides: "Full-screen layout without BottomTabBar"
    - path: "src/app/(protected)/sops/[sopId]/walkthrough/page.tsx"
      provides: "Walkthrough page orchestrating safety gate, step list, and bottom action bar"
    - path: "src/components/sop/SafetyAcknowledgement.tsx"
      provides: "Full-screen hazard/PPE gate with 80px orange acknowledge button"
    - path: "src/components/sop/WalkthroughList.tsx"
      provides: "Scrollable step list with safety summary strip"
    - path: "src/components/sop/StepItem.tsx"
      provides: "Individual step row with 72px+ min height, tap-to-complete"
    - path: "src/components/sop/StepProgress.tsx"
      provides: "Progress bar and Step N of M counter"
    - path: "src/components/sop/SopImageInline.tsx"
      provides: "Inline image with yet-another-react-lightbox zoom"
  key_links:
    - from: "src/app/(protected)/sops/[sopId]/walkthrough/page.tsx"
      to: "src/stores/walkthrough.ts"
      via: "useWalkthroughStore for step completion and safety acknowledgement"
      pattern: "useWalkthroughStore"
    - from: "src/app/(protected)/sops/[sopId]/walkthrough/page.tsx"
      to: "src/hooks/useSopDetail.ts"
      via: "Load SOP data from Dexie"
      pattern: "useSopDetail"
    - from: "src/components/sop/SafetyAcknowledgement.tsx"
      to: "src/stores/walkthrough.ts"
      via: "acknowledgeSafety callback"
      pattern: "acknowledgeSafety"
    - from: "src/components/sop/SopImageInline.tsx"
      to: "yet-another-react-lightbox"
      via: "Dynamic import of Lightbox + Zoom plugin"
      pattern: "yet-another-react-lightbox"
---

<objective>
Build the full-screen SOP walkthrough experience: safety acknowledgement gate, scrolling step list with tap-to-complete, progress indicator, inline images with zoom, and the bottom action bar.

Purpose: This is the core worker interaction — the step-by-step SOP walkthrough that makes safety information impossible to miss and is usable with gloves on a phone.
Output: 7 component/page files delivering the complete walkthrough flow (WORK-01, WORK-02, WORK-05, WORK-06, WORK-09, WORK-10).
</objective>

<execution_context>
@C:/Users/Simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-worker-experience/03-CONTEXT.md
@.planning/phases/03-worker-experience/03-UI-SPEC.md
@.planning/phases/03-worker-experience/03-RESEARCH.md
@.planning/phases/03-worker-experience/03-01-SUMMARY.md

<interfaces>
From src/types/sop.ts:
```typescript
export interface SopWithSections extends Sop {
  sop_sections: (SopSection & { sop_steps: SopStep[]; sop_images: SopImage[] })[]
}
export interface SopStep { id: string; section_id: string; step_number: number; text: string; warning: string | null; caution: string | null; tip: string | null; required_tools: string[] | null; time_estimate_minutes: number | null; ... }
export interface SopImage { id: string; sop_id: string; section_id: string | null; step_id: string | null; storage_path: string; content_type: string; alt_text: string | null; sort_order: number; ... }
export interface SopSection { id: string; sop_id: string; section_type: string; title: string; content: string | null; sort_order: number; ... }
```

From src/hooks/useSopDetail.ts (created in Plan 01):
```typescript
export function useSopDetail(sopId: string): UseQueryResult<SopWithSections | undefined>
```

From src/stores/walkthrough.ts (created in Plan 01):
```typescript
export const useWalkthroughStore = create<WalkthroughState>(...)
// markStepComplete(sopId, stepId), markStepIncomplete(sopId, stepId)
// acknowledgeSafety(sopId), isAcknowledged(sopId)
// getCompletedSteps(sopId): string[]
// resetWalkthrough(sopId)
```

From src/app/(protected)/layout.tsx:
```typescript
// Renders OnlineStatusBanner, InstallPrompt, main, BottomTabBar
// Walkthrough needs its own layout.tsx that does NOT render BottomTabBar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create walkthrough layout, safety acknowledgement gate, and step progress header</name>
  <files>
    src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx,
    src/components/sop/SafetyAcknowledgement.tsx,
    src/components/sop/StepProgress.tsx
  </files>
  <read_first>
    src/app/(protected)/layout.tsx,
    src/stores/walkthrough.ts,
    src/types/sop.ts,
    src/app/globals.css,
    .planning/phases/03-worker-experience/03-UI-SPEC.md (lines 190-550 for Pages 3, C-04, C-05)
  </read_first>
  <action>
**1. Create `src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx`:**
- This is a nested layout that OVERRIDES the parent's BottomTabBar. It renders its own minimal chrome.
- Server component that checks auth (same pattern as parent layout: `createClient()`, `getUser()`, redirect if no user)
- Renders: `<div className="flex flex-col h-screen bg-steel-900 overflow-hidden">{children}</div>`
- Does NOT render `<BottomTabBar />` — this is the key difference from the parent layout
- Does NOT render `<OnlineStatusBanner />` — walkthrough is distraction-free (offline status is irrelevant during active walkthrough since data is from Dexie)
- Exports metadata: `export const metadata = { title: 'SOP Walkthrough' }`

**2. Create `src/components/sop/SafetyAcknowledgement.tsx`:**
- `'use client'` directive
- Props: `{ sopId: string; hazardsSection?: SopSection; ppeSection?: SopSection; emergencySection?: SopSection; onAcknowledge: () => void }`
- Full-screen fixed overlay: `fixed inset-0 z-40 bg-steel-900 flex flex-col overflow-y-auto`
- Inner container: `flex flex-col gap-6 px-4 py-8 pb-[120px] max-w-2xl mx-auto w-full`
- Header: lucide `ShieldAlert` size={40} `text-brand-orange`, heading "Before you start" `text-2xl font-bold text-steel-100`, subheading "Review the hazards and required PPE for this procedure." `text-base text-steel-400`
- Hazards card (if hazardsSection exists):
  - Container: `bg-red-500/10 border border-red-500/40 rounded-xl p-5`
  - Header: lucide `AlertTriangle` size={18} `text-red-400` + "HAZARDS" `text-xs font-bold uppercase tracking-widest text-red-400`
  - Divider: `border-t border-red-500/20 my-3`
  - Content: render section content as bullet list with `text-red-400` bullets
- PPE card (if ppeSection exists):
  - Container: `bg-blue-500/10 border border-blue-500/40 rounded-xl p-5`
  - Header: lucide `ShieldCheck` size={18} `text-blue-400` + "PPE REQUIRED" in blue
  - Content: parse PPE items and render as chips: `inline-flex items-center gap-1.5 px-3 py-1.5 bg-blue-500/15 text-blue-300 text-sm font-medium rounded-lg border border-blue-500/30`
- Emergency card (if emergencySection exists): collapsible with `useState` toggle
  - Collapsed header: tap to expand, lucide `Siren`, `ChevronDown` rotates
  - Expanded: show content below header
- Fixed bottom bar: `fixed bottom-0 left-0 right-0 z-50 bg-steel-900 border-t border-steel-700 px-4 pt-4 pb-[calc(16px+env(safe-area-inset-bottom,0px))]`
  - Button: `w-full h-[80px] bg-brand-orange text-white font-bold text-lg rounded-xl hover:bg-orange-500 active:bg-orange-700 transition-colors` with lucide `ShieldCheck` size={22} prefix
  - Text: "Understood -- Start Procedure"
  - onClick: calls `onAcknowledge()`

**3. Create `src/components/sop/StepProgress.tsx`:**
- `'use client'` directive
- Props: `{ completedCount: number; totalSteps: number }`
- Container: `px-4 pt-3 pb-4 bg-steel-900`
- Progress bar: `h-2 bg-steel-700 rounded-full overflow-hidden mb-2` with inner fill `h-full bg-brand-yellow rounded-full transition-all duration-300` and inline width style `{ width: \`${(completedCount / totalSteps) * 100}%\` }`
- Counter row: `flex items-center justify-between`
  - Left: `"Step {nextIncomplete} of {totalSteps}"` or `"All {totalSteps} steps done"` — `text-sm font-semibold text-steel-100 tabular-nums`
  - Right: percentage `text-sm text-steel-400 tabular-nums` (e.g. "25% done")
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx tsc --noEmit src/app/\(protected\)/sops/\[sopId\]/walkthrough/layout.tsx src/components/sop/SafetyAcknowledgement.tsx src/components/sop/StepProgress.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - walkthrough/layout.tsx does NOT import or render BottomTabBar
    - walkthrough/layout.tsx wraps children in h-screen bg-steel-900
    - SafetyAcknowledgement.tsx renders hazards card with bg-red-500/10 and PPE card with bg-blue-500/10
    - SafetyAcknowledgement.tsx has a button with h-[80px] and bg-brand-orange
    - SafetyAcknowledgement.tsx calls onAcknowledge on button click
    - StepProgress.tsx renders a progress bar with bg-brand-yellow fill and percentage-based width
    - StepProgress.tsx shows "Step N of M" counter
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Walkthrough layout isolates from BottomTabBar, safety gate blocks until acknowledged, progress header tracks completion</done>
</task>

<task type="auto">
  <name>Task 2: Create walkthrough page, step list, step item, and inline image components</name>
  <files>
    src/app/(protected)/sops/[sopId]/walkthrough/page.tsx,
    src/components/sop/WalkthroughList.tsx,
    src/components/sop/StepItem.tsx,
    src/components/sop/SopImageInline.tsx
  </files>
  <read_first>
    src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx,
    src/components/sop/SafetyAcknowledgement.tsx,
    src/components/sop/StepProgress.tsx,
    src/hooks/useSopDetail.ts,
    src/stores/walkthrough.ts,
    src/types/sop.ts,
    .planning/phases/03-worker-experience/03-UI-SPEC.md (lines 190-590 for Page 3, C-03, C-06, C-09, C-11)
  </read_first>
  <action>
**1. Create `src/components/sop/SopImageInline.tsx`:**
- `'use client'` directive
- Import `dynamic` from `next/dynamic`, `useState` from React
- Dynamically import `Lightbox` from `yet-another-react-lightbox` (ssr: false)
- Dynamically import `Zoom` from `yet-another-react-lightbox/plugins/zoom` (ssr: false)
- Import `'yet-another-react-lightbox/styles.css'` at top level
- Props: `{ src: string; alt: string }`
- Inline container: `relative mt-3 rounded-xl overflow-hidden bg-steel-700 cursor-zoom-in max-h-[240px] border border-steel-600`
- Image: `w-full object-contain max-h-[240px]`
- Zoom hint badge: `absolute bottom-2 right-2 flex items-center gap-1 px-2 py-1 bg-steel-900/80 rounded-lg text-xs font-medium text-steel-100` with lucide `ZoomIn` size={12} + "Tap to zoom"
- On tap: `setOpen(true)`
- Render `<Lightbox open={open} close={() => setOpen(false)} slides={[{ src, alt }]} plugins={[Zoom]} />` when `open` is true

**2. Create `src/components/sop/StepItem.tsx`:**
- `'use client'` directive
- Props: `{ step: SopStep; status: 'upcoming' | 'active' | 'completed'; images: SopImage[]; onToggle: () => void }`
- Base container: `flex items-start gap-4 px-4 py-5 border-l-4 transition-all duration-150 min-h-[72px] cursor-pointer` — entire row is tappable via `onClick={onToggle}`
- Active state: `border-l-4 border-brand-yellow bg-steel-800/60`
- Completed state: `border-l-4 border-green-500/40 bg-green-500/5`
- Upcoming state: `border-l-4 border-transparent opacity-80`
- Left column (step number, `flex-shrink-0 w-8 pt-0.5`):
  - Upcoming/active: `text-[13px] font-bold tabular-nums text-steel-400` showing step_number
  - Completed: lucide `CheckCircle2` size={20} `text-green-400`
- Centre column (`flex-1 min-w-0`):
  - Step text: `text-lg font-normal text-steel-100 leading-relaxed`
  - Warning annotation (if step.warning): `inline-flex items-center gap-1 px-2 py-1 bg-brand-orange/20 text-brand-orange text-xs font-bold uppercase tracking-wide rounded border border-brand-orange/30 mt-2` with lucide `AlertTriangle` size={12} prefix
  - Caution annotation (if step.caution): same orange styling as warning
  - Inline images: for each image matching this step, render `<SopImageInline src={image.storage_path} alt={image.alt_text || 'Step image'} />`
- Right column (`flex-shrink-0 min-w-[44px] min-h-[72px] flex items-center justify-center`):
  - Upcoming/active: lucide `Circle` size={28} `text-steel-600 hover:text-brand-yellow`
  - Completed: lucide `CheckCircle2` size={28} `text-green-400`
- Haptic feedback on tap: `if ('vibrate' in navigator) navigator.vibrate(30)`

**3. Create `src/components/sop/WalkthroughList.tsx`:**
- `'use client'` directive
- Props: `{ sopId: string; stepsSection: SopSection & { sop_steps: SopStep[]; sop_images: SopImage[] }; allImages: SopImage[]; hazardsSection?: SopSection; ppeSection?: SopSection }`
- Import `useWalkthroughStore` from `@/stores/walkthrough`
- Get `completedSteps` via `store.getCompletedSteps(sopId)` (returns string[])
- Collapsible safety summary strip (shown after acknowledgement, always at top of scroll area):
  - Container: `flex items-center gap-3 px-4 py-3 bg-brand-orange/10 border border-brand-orange/30 rounded-xl mb-4 cursor-pointer`
  - Left: lucide `ShieldAlert` size={18} `text-brand-orange`
  - Text: "Safety summary" `text-sm font-semibold text-brand-orange` + "Tap to review" `text-xs text-steel-400 ml-1`
  - Right: lucide `ChevronDown` size={16} `text-steel-400` — rotates on expand
  - Expanded: renders hazards + PPE content inline (simplified version of SafetyAcknowledgement cards)
- Steps list: `flex flex-col` — render each step from `stepsSection.sop_steps` sorted by `step_number`
- For each step, determine status:
  - If step.id is in completedSteps → 'completed'
  - If step is the first non-completed step → 'active'
  - Otherwise → 'upcoming'
- Find images for each step: filter `allImages` where `image.step_id === step.id`
- Render `<StepItem step={step} status={status} images={stepImages} onToggle={() => handleToggle(step.id)} />`
- `handleToggle(stepId)`: if completed, call `store.markStepIncomplete(sopId, stepId)`; else call `store.markStepComplete(sopId, stepId)`

**4. Create `src/app/(protected)/sops/[sopId]/walkthrough/page.tsx`:**
- `'use client'` directive
- Import `useSopDetail`, `useWalkthroughStore`, `SafetyAcknowledgement`, `StepProgress`, `WalkthroughList`
- Import `useParams`, `useRouter` from `next/navigation`
- Import lucide icons: `X`, `PartyPopper`, `ArrowLeft`, `ArrowRight`
- Get `sopId` from `useParams()`
- Fetch SOP data: `const { data: sop, isLoading } = useSopDetail(sopId as string)`
- Get walkthrough state from store
- Extract sections by type: find `hazards`, `ppe`, `emergency`, and `steps` sections from `sop.sop_sections` using `section_type` field
- Get steps from the steps section (sorted by step_number)
- Compute `completedSteps`, `totalSteps`, `completedCount`, `allDone`
- Check `isAcknowledged(sopId)` — if false, render `<SafetyAcknowledgement>` gate
- Layout structure:
  1. Fixed top bar (h-[56px]): Exit button (lucide `X`, navigates to `/sops/${sopId}`), SOP title (truncated), compact step count ("3/12")
  2. Progress + counter strip (StepProgress): below top bar
  3. Scrollable area: `flex-1 overflow-y-auto px-4 pb-[160px]` — renders `<WalkthroughList>`
  4. Fixed bottom action area: `fixed bottom-0 left-0 right-0 z-30 bg-steel-900 border-t border-steel-700 px-4 pt-3 pb-[calc(12px+env(safe-area-inset-bottom,0px))]`
     - Primary button: `w-full h-[72px] rounded-xl font-bold text-lg transition-all`
       - Normal: `bg-brand-yellow text-steel-900 hover:bg-amber-400` — "Mark step N complete"
       - All done: `bg-green-500 text-white hover:bg-green-400` — "Finish SOP -- all steps done" with PartyPopper icon
     - Secondary row (`flex items-center justify-between h-[44px]`):
       - Previous: lucide `ArrowLeft` + "Previous" — disabled on first step (`opacity-30 pointer-events-none`)
       - Skip: "Skip this step" + lucide `ArrowRight`
     - Primary button click: marks current active step as complete (or navigates back to `/sops/${sopId}` if all done)
     - Previous button: scrolls to and highlights the previous step (does NOT undo completion)
     - Skip button: advances to next step without marking complete
- Loading state: skeleton with pulse animations
- Error state: "SOP not found" with back link
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx tsc --noEmit src/app/\(protected\)/sops/\[sopId\]/walkthrough/page.tsx src/components/sop/WalkthroughList.tsx src/components/sop/StepItem.tsx src/components/sop/SopImageInline.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - walkthrough/page.tsx imports useSopDetail and useWalkthroughStore
    - walkthrough/page.tsx conditionally renders SafetyAcknowledgement when not acknowledged
    - walkthrough/page.tsx renders StepProgress with completedCount and totalSteps
    - walkthrough/page.tsx has a fixed bottom action area with h-[72px] primary button
    - StepItem.tsx has min-h-[72px] on the container (WORK-09 compliance)
    - StepItem.tsx renders warning/caution annotations with brand-orange styling
    - StepItem.tsx supports all 3 states: upcoming, active, completed
    - SopImageInline.tsx uses dynamic import for yet-another-react-lightbox with ssr: false
    - WalkthroughList.tsx includes collapsible safety summary strip with brand-orange styling
    - TypeScript compiles without errors for all 4 files
  </acceptance_criteria>
  <done>Complete walkthrough flow: safety gate blocks until acknowledged, step list with tap-to-complete, progress tracking, inline image zoom, full-screen glove-friendly layout</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run build` completes without errors
- Walkthrough route is at `/sops/[sopId]/walkthrough` with its own layout
- Safety acknowledgement renders before step list
- Step items have 72px+ minimum tap targets
- SopImageInline uses yet-another-react-lightbox for zoom
</verification>

<success_criteria>
- Worker opens walkthrough and sees mandatory safety gate (hazards + PPE)
- After acknowledging, step list appears with progress bar
- Tapping a step marks it complete with green styling, progress updates
- Tapping a completed step undoes completion (WORK-02)
- Images render inline below step text with tap-to-zoom
- All interactive elements are 72px+ (WORK-09)
- Walkthrough is full-screen without BottomTabBar (WORK-10)
- Warning/caution annotations are prominently displayed in orange/red
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-experience/03-02-SUMMARY.md`
</output>
