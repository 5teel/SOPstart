---
phase: 22-voice-driven-walkthrough
plan: 04
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - src/components/sop/walkthrough/ImmersiveStepCard.tsx
autonomous: true
requirements: [VDW-LIT-01, VDW-LIT-02]
user_setup: []
must_haves:
  truths:
    - "Every immersive step card surfaces a visual: a step photo when sop_images has one for the step, else a block-type icon — the visual area is never blank (D-05)"
    - "A step with no authored photo falls back to its section-type icon (hazard→AlertTriangle, ppe→Shield, emergency→Siren, steps→ListChecks, signoff→ClipboardCheck), defaulting to ListChecks for unknown types (D-06)"
    - "Authoring is NOT gated — no publish requirement is added; the icon fallback is purely render-side (D-06)"
  artifacts:
    - path: "src/components/sop/walkthrough/ImmersiveStepCard.tsx"
      provides: "Always-on visual layer (photo-or-icon per step)"
      contains: "SECTION_TYPE_ICONS"
    - path: "src/components/sop/walkthrough/ImmersiveStepCard.tsx"
      provides: "Step photo render reusing the signed-image component"
      contains: "SopImageInline"
  key_links:
    - from: "src/components/sop/walkthrough/ImmersiveStepCard.tsx"
      to: "sop.sop_sections[n].sop_images"
      via: "filter by step_id === current.id"
      pattern: "sop_images"
    - from: "src/components/sop/walkthrough/ImmersiveStepCard.tsx"
      to: "src/components/sop/SopImageInline.tsx"
      via: "render SopImageInline with raw storage_path src"
      pattern: "SopImageInline"
---

<objective>
Add the always-on visual layer to the immersive worker step card so a low-literacy worker can follow an SOP visually without required reading (VDW-LIT-01, VDW-LIT-02). Every step card surfaces either its authored photo (from `sop_images`) or, when none exists, a semantic block-type icon derived from the section type — never a blank visual area, and never forcing admins to attach a photo per step (D-05, D-06).

Purpose: This is the literacy/visual half of the phase and is fully independent of the voice loop — it touches only `ImmersiveStepCard.tsx`, runs in Wave 1 parallel to the Plan 02 voice infrastructure, and merges cleanly because no other plan modifies this file.

Output: An extended `ImmersiveStepCard` rendering a photo-or-icon visual block alongside the existing step text/warning/caution/tools content. No query changes (sop_images is already fetched by useSopDetail), no schema changes, no authoring changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voice-driven-walkthrough/22-RESEARCH.md
@.planning/phases/22-voice-driven-walkthrough/22-PATTERNS.md
@src/components/sop/walkthrough/ImmersiveStepCard.tsx
@src/components/sop/SectionContent.tsx
@src/types/sop.ts
@.claude/skills/sketch-findings-SOPstart/references/interaction-patterns.md
</context>

<artifacts_this_phase_produces>
- `src/components/sop/walkthrough/ImmersiveStepCard.tsx` — adds `SECTION_TYPE_ICONS` map + a photo-or-icon visual block (EXTEND, no new file)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Photo-or-icon visual layer in ImmersiveStepCard</name>
  <files>src/components/sop/walkthrough/ImmersiveStepCard.tsx</files>
  <read_first>
    - src/components/sop/walkthrough/ImmersiveStepCard.tsx (the full component; the existing content renders at lines 73-98 — warning/caution/tip/required_tools — the visual block is inserted after these and before the evidence grid at line 100; `current` is the active step, `currentId`, and the section lookup at lines 38-40 already finds the owning section by step id)
    - src/components/sop/SectionContent.tsx (lines 83-113: the canonical pattern for mapping `section.sop_images` to renders — `section.sop_images.filter((img) => img.step_id === step.id)` and `<SopImageInline src={img.storage_path} alt={img.alt_text ?? 'Step image'} />`; import path `@/components/sop/SopImageInline`)
    - src/types/sop.ts (SopImage fields lines 107-117: `step_id`, `section_id`, `storage_path`, `alt_text`, `sort_order`; SopWithSections lines 190-195 confirms each section already carries `sop_images: SopImage[]`)
    - 22-PATTERNS.md § ImmersiveStepCard.tsx (lines 368-422 — the SECTION_TYPE_ICONS map, insertion point, paper/ink token classes for the icon badge)
    - .claude/skills/sketch-findings-SOPstart/references/interaction-patterns.md § 3 (mobile immersive walkthrough anatomy — the visual sits in the scrollable body)
  </read_first>
  <behavior>
    - A step that has a matching `sop_images` row (`img.step_id === current.id`) renders that image via `SopImageInline` (which resolves the raw storage_path to a signed URL — reuse, do not re-sign). Multiple matching images render in sort_order.
    - A step with NO matching image renders a block-type icon badge: the section type of the owning section maps through `SECTION_TYPE_ICONS` (`hazard`/`hazards`→AlertTriangle, `ppe`→Shield, `emergency`→Siren, `steps`→ListChecks, `signoff`→ClipboardCheck), case-insensitive, defaulting to ListChecks for any unmapped type. The badge uses paper/ink tokens (`text-[var(--ink-400)]`, mono uppercase type label) per the design system.
    - The visual area is NEVER empty: photo present → photo; photo absent → icon. (VDW-LIT-02: the card is followable with zero reading.)
    - No publish gate, no required-photo enforcement is introduced (D-06) — this is render-only.
    - The existing warning/caution/tip/tools/evidence renders are unchanged.
  </behavior>
  <action>
    Extend `src/components/sop/walkthrough/ImmersiveStepCard.tsx`:
    Import the Lucide icons (`AlertTriangle, Shield, Siren, ListChecks, ClipboardCheck`) and `SopImageInline` from `@/components/sop/SopImageInline`.
    Add a module-level `const SECTION_TYPE_ICONS: Record<string, LucideIcon>` per <behavior>.
    Compute the owning section's `section_type` for `current` (reuse the existing section lookup at lines 38-40 which already matches by step id) and resolve `stepImages = ownerSection.sop_images.filter((img) => img.step_id === current.id)` (guard for sections without sop_images).
    Insert a visual block after the required_tools render (after line ~93) and before the evidence grid (line 100): if `stepImages.length > 0`, map them to `<SopImageInline key={img.id} src={img.storage_path} alt={img.alt_text ?? current.text} />`; else render the icon badge (`const Icon = SECTION_TYPE_ICONS[sectionType.toLowerCase()] ?? ListChecks`) with the mono section-type label, using the paper/ink token classes from PATTERNS.md.
    Do not change the component's Props, the evidence-capture grid, the footer nav, or any data fetching.
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase22-stubs tests/phase22/visual-layer.spec.ts` is GREEN: `ImmersiveStepCard.tsx` contains `SECTION_TYPE_ICONS` AND references `sop_images` AND `SopImageInline`.
    - `npx tsc --noEmit` passes.
    - Source-contract (handler/data wiring, not bare token): the file contains a `.filter(` over `sop_images` keyed on `step_id === current.id` (assert both `sop_images` and `step_id` appear in the same render path) AND a fallback `?? ListChecks` default — i.e. the icon is genuinely derived from section type, not a single static icon. The visual-layer spec must assert the fallback default token (`ListChecks`) is present so a dead "always-the-same-icon" implementation cannot pass.
    - No new Props added to `ImmersiveStepCard` (the spec asserts the Props interface still has exactly its existing fields) and no publish-gate logic introduced.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase22-stubs tests/phase22/visual-layer.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Every immersive step card shows a photo when authored, else a section-type icon; visual area is never blank; no authoring gate added; the visual-layer spec is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage → client render | `sop_images.storage_path` is a private Storage path; rendering requires a signed URL — handled by the existing SopImageInline component (RLS-respecting, reused not reimplemented) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Information Disclosure | rendering another org's step image | accept | Images flow from `useSopDetail` whose query is RLS-scoped to the SOP the worker can already see; SopImageInline signs only paths already returned to this client. No new data path introduced — this plan reuses the same `sop` prop already rendered for text |
| T-22-04-02 | Tampering | a "dead" visual layer that always renders one icon (passes a grep test, fails users) | mitigate | visual-layer spec asserts the `?? ListChecks` fallback default AND the `sop_images`/`step_id` filter both exist (2026-06-05 handler-wiring rule) |
| T-22-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (Lucide + SopImageInline already in the codebase; RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx playwright test --project=phase22-stubs tests/phase22/visual-layer.spec.ts` green.
- `npx tsc --noEmit` clean.
- No Props change; no publish gate added.
</verification>

<success_criteria>
- ImmersiveStepCard surfaces a photo-or-icon visual on every step.
- Icon fallback derived from section type with a ListChecks default.
- No authoring gate, no query change, no new Props.
- visual-layer spec green.
</success_criteria>

<output>
Create `.planning/phases/22-voice-driven-walkthrough/22-04-SUMMARY.md` when done. Note that the `journeys.ts` pathways map needs no change (no new route; the immersive walkthrough screen already exists — the visual is an in-card enhancement). Confirm sop_images was already available via useSopDetail (RESEARCH Assumption A3 resolved: no query change needed).
</output>
