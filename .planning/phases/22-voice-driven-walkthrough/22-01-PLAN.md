---
phase: 22-voice-driven-walkthrough
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase22/intent-classifier.spec.ts
  - tests/phase22/tts-route.spec.ts
  - tests/phase22/stt-keyterms.spec.ts
  - tests/phase22/visual-layer.spec.ts
  - tests/phase22/voice-modal.spec.ts
  - tests/phase22/voice-safety-gate.spec.ts
autonomous: true
requirements: [VDW-LIT-01, VDW-LIT-02, VDW-LIT-03, VDW-VOICE-01, VDW-VOICE-02, VDW-VOICE-03]
must_haves:
  truths:
    - "Running `npx playwright test --project=phase22-stubs --list` shows all 6 phase22 spec files"
    - "Running `npx playwright test --project=phase22-stubs` EXECUTES every source-contract spec (not just lists it) so a module-load crash surfaces as a process error, distinct from a correct red (token-absent) failure"
    - "Every phase-22 requirement has at least one registered spec that will execute (no orphan unregistered specs)"
    - "Stub specs fail or skip cleanly at Wave-0 head (the implementation tokens do not exist yet) and become live as Waves 1-2 ship"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase22-stubs project registering tests/phase22/*.spec.ts"
      contains: "phase22-stubs"
    - path: "tests/phase22/intent-classifier.spec.ts"
      provides: "Unit assertions for classifyIntent (VDW-VOICE-03)"
      contains: "classifyIntent"
    - path: "tests/phase22/voice-safety-gate.spec.ts"
      provides: "D-02 source-contract: voice next routes through handleMarkComplete AND the isAcknowledged-false TTS ack-prompt branch exists in the modal (D-02 negative gate)"
      contains: "handleMarkComplete"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase22/*.spec.ts"
      via: "testMatch regex on phase22-stubs project"
      pattern: "phase22"
---

<objective>
Establish the Phase 22 validation scaffold: register a `phase22-stubs` Playwright project and create the six source-contract / unit stub specs that gate every in-scope requirement. This is the Nyquist Wave-0 plan — no production code, only the test harness that subsequent waves turn green.

Purpose: Per CLAUDE.md learning 2026-05-25, a spec file not matched by any project's `testMatch` regex NEVER runs — so the harness must exist and be registered before implementation begins. Per learnings 2026-06-05 / 2026-06-08, source-contract specs must assert handler WIRING (not mere token presence) for interactive affordances, and the loop needs a real browser run before "done." A spec that is only checked via `--list` can hide a module-load crash (it lists fine but throws on execution), so this plan's verify EXECUTES the full project, not just lists it.

Output: `playwright.config.ts` with a new `phase22-stubs` project + 6 spec files under `tests/phase22/`. The intent-classifier spec runs as a real unit test (the classifier is a pure function shipped in Wave 1); the other five are source-contract file-walk assertions that pass once Waves 1-2 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voice-driven-walkthrough/22-RESEARCH.md
@.planning/phases/22-voice-driven-walkthrough/22-PATTERNS.md
@.planning/phases/22-voice-driven-walkthrough/22-VALIDATION.md
@playwright.config.ts
@tests/lint/no-static-desktop-import.spec.ts
</context>

<artifacts_this_phase_produces>
This plan creates the test harness only. The symbols asserted by these specs are produced by Plans 02-04:
- `tests/phase22/intent-classifier.spec.ts` — asserts `classifyIntent` (Plan 02 `src/lib/voice/intent-classifier.ts`)
- `tests/phase22/tts-route.spec.ts` — asserts `src/app/api/voice/tts/route.ts` (Plan 02)
- `tests/phase22/stt-keyterms.spec.ts` — asserts keyterms extension in `src/lib/voice/deepgram-stream.ts` (Plan 02)
- `tests/phase22/visual-layer.spec.ts` — asserts `ImmersiveStepCard.tsx` icon + image layer (Plan 04)
- `tests/phase22/voice-modal.spec.ts` — asserts `WalkthroughVoiceModal.tsx` STT + TTS + intent wiring (Plan 03)
- `tests/phase22/voice-safety-gate.spec.ts` — asserts the D-02 onVoiceNext → handleMarkComplete chain AND the D-02 negative gate (isAcknowledged-false → TTS ack-prompt branch in the modal) (Plan 03)
- `phase22-stubs` Playwright project (this plan)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register phase22-stubs Playwright project</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (the projects array — add the new project after phase21.6-stubs / phase24-stubs, mirroring their `testDir: '.'` + `testMatch` regex shape; phase15-stubs at lines 55-59 is the closest analog that uses `use: { browserName: 'chromium' }`)
    - PATTERNS.md "tests/phase22/*.spec.ts + playwright.config.ts" section (the registration snippet + CLAUDE.md 2026-05-25 rationale)
  </read_first>
  <action>
    Add a new project object to the `projects` array in `playwright.config.ts`:
    name `phase22-stubs`, `testDir: '.'`, `testMatch: /tests\/phase22\/.*\.(spec|test)\.ts$/`, and `use: { browserName: 'chromium' }` (the source-contract specs are filesystem reads but the project must be discoverable under the same browser config as phase15-stubs so the full `npm run test` run includes it). Do NOT modify any existing project regex. Add a comment block citing CLAUDE.md 2026-05-25 (unregistered specs never run) and instructing the verifier to confirm registration via `npx playwright test --list --project=phase22-stubs`.
  </action>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase22-stubs` exits 0 and lists the spec files placed in `tests/phase22/` by Task 2 (zero discovered = FAIL).
    - `playwright.config.ts` contains the literal string `phase22-stubs` exactly once in the projects array.
    - No existing project's `testMatch` regex is altered (diff touches only the inserted object).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --list --project=phase22-stubs</automated>
  </verify>
  <done>The phase22-stubs project is registered and discovers all tests/phase22 spec files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the six phase22 stub specs</name>
  <files>tests/phase22/intent-classifier.spec.ts, tests/phase22/tts-route.spec.ts, tests/phase22/stt-keyterms.spec.ts, tests/phase22/visual-layer.spec.ts, tests/phase22/voice-modal.spec.ts, tests/phase22/voice-safety-gate.spec.ts</files>
  <read_first>
    - tests/lint/no-static-desktop-import.spec.ts (the source-contract file-walk pattern: `fs.readFileSync` + `expect(text).toContain(...)`, REPO_ROOT/SRC_DIR resolution at lines 1-20 per PATTERNS.md)
    - 22-RESEARCH.md § Validation Architecture (the Req→Test map at lines 572-598) and § "Source-contract tests to write" (PATTERNS.md lines 494-500)
    - 22-CONTEXT.md D-02 (the safety invariant the voice-safety-gate spec encodes — including the negative gate: voice "next" before acknowledgement must NOT advance and must speak an ack prompt)
  </read_first>
  <behavior>
    - intent-classifier spec (LIVE unit, runs against Plan 02 code once it lands): `classifyIntent('done')` returns `'next'`; `classifyIntent('next')` returns `'next'`; `classifyIntent('go back')` returns `'prev'`; `classifyIntent('what is next on the blank side hanger')` returns `'question'` (question-word gate beats the "next" keyword); `classifyIntent('how do I change the hanger')` returns `'question'`. Until Plan 02 ships the module, guard the import so the spec is skipped (test.skip) rather than erroring on a missing module — it auto-activates when the file exists.
    - tts-route spec (source-contract): `src/app/api/voice/tts/route.ts` exists AND contains `supabase.auth.getUser()` (auth gate) AND a TTS model reference resolved from a shared constant (assert it imports from `@/lib/voice/tts-constants` OR references `TTS_MODEL`, NOT a bare hardcoded `'gpt-4o-mini-tts'` string literal in the route body) AND `inFlight` (concurrency cap) AND a 500-char input bound (`max(500)` or `length > 500`).
    - stt-keyterms spec (source-contract): `src/lib/voice/deepgram-stream.ts` contains `keyterms` in the `VoiceStreamOpts` interface AND `params.append('keyterm'` (the per-term URL injection).
    - visual-layer spec (source-contract): `ImmersiveStepCard.tsx` contains `SECTION_TYPE_ICONS` (icon fallback map) AND references `sop_images` (the per-step photo render) AND `SopImageInline` (reuse of the existing signed-image component).
    - voice-modal spec (source-contract, HANDLER WIRING per 2026-06-05): `WalkthroughVoiceModal.tsx` contains `startVoiceStream(` (real STT, not the stub) AND `classifyIntent(` (intent dispatch) AND `onVoiceNext` (progression prop) AND a TTS `speak(` call AND `useTtsPlayback`.
    - voice-safety-gate spec (source-contract, D-02): TWO assertions.
      (a) Positive routing/bypass guard: `WalkthroughVoiceModal.tsx` references `onVoiceNext`; `MobileWalkthrough.tsx` exposes the imperative handle (`useImperativeHandle`) AND its `handleMarkComplete` is the only mutation path (assert `markStepAcknowledged` and `markStepComplete` both appear in `handleMarkComplete`); `WalkthroughSwitcher.tsx` passes `onVoiceNext` to the modal AND uses a `ref` to MobileWalkthrough. Assert the modal does NOT call `markStepComplete` directly (grep `MobileWalkthrough`-only ownership: `WalkthroughVoiceModal.tsx` must NOT contain `markStepComplete` — the bypass guard).
      (b) D-02 NEGATIVE gate (new — complements the runtime UAT for must_have truth 5): `WalkthroughVoiceModal.tsx` contains an `isAcknowledged`-false branch that wires the spoken ack-prompt — assert the file contains `isAcknowledged` AND a TTS speak call referencing an acknowledge-prompt string (grep that an `isAcknowledged` false-branch and the ack-prompt phrase, e.g. `acknowledge`, co-occur with a `speak(` call — the voice "next" before ack must speak "please acknowledge…" and NOT advance). This is the grep-level wiring assertion for VDW D-02's negative path; the runtime proof remains Plan 03 Task 3.
  </behavior>
  <action>
    Create the six spec files under `tests/phase22/` using the `no-static-desktop-import.spec.ts` file-walk pattern (Node `fs`/`path`, `REPO_ROOT = path.resolve(__dirname, '..', '..')`). Use `[\s\S]` not the `/s` regex flag (CLAUDE.md 2026-06-02 TS target). For the five source-contract specs, the asserted tokens are listed in <behavior> — each spec must encode the WIRING assertions (handler chains), not just single-token presence. The voice-safety-gate spec must include BOTH the positive routing/bypass guard AND the D-02 negative-gate assertion (isAcknowledged-false → TTS ack-prompt branch). For `intent-classifier.spec.ts`, import `classifyIntent` from `@/lib/voice/intent-classifier` behind a `fs.existsSync` guard + `test.skip` so it is green-when-absent and live-when-present. Do NOT create the production files — those belong to Plans 02-04. Mark each source-contract spec with a leading comment naming the plan that turns it green.
  </action>
  <acceptance_criteria>
    - All 6 files exist under `tests/phase22/` and are discovered by `--project=phase22-stubs`.
    - `npx playwright test --project=phase22-stubs` runs to completion (EXECUTES, not just `--list`): intent-classifier is skipped (module absent at Wave-0 head); the five source-contract specs may fail (red) because the implementation tokens do not exist yet — this is the expected Nyquist baseline, documented in the SUMMARY. A spec that throws a process/module-load error (vs a clean assertion red) is a DEFECT in the spec and must be fixed before this plan is done.
    - `voice-safety-gate.spec.ts` contains: (i) an assertion that `WalkthroughVoiceModal.tsx` does NOT contain `markStepComplete` (the D-02 bypass guard); (ii) an assertion that `handleMarkComplete` in `MobileWalkthrough.tsx` contains both `markStepAcknowledged` and `markStepComplete`; (iii) the D-02 NEGATIVE-gate assertion that `WalkthroughVoiceModal.tsx` contains an `isAcknowledged`-false branch wiring a `speak(` ack-prompt (the spoken "please acknowledge…" path).
    - No spec uses the `/s` regex flag (grep the new files for `/s` flag usage returns 0).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase22-stubs --list && npx playwright test --project=phase22-stubs && npx playwright test --project=phase22-stubs tests/phase22/intent-classifier.spec.ts</automated>
  </verify>
  <done>Six stub specs exist and are registered; the FULL project run executes every spec (so a module-load crash surfaces as a process error, distinct from a correct token-absent red) — source-contract specs are EXPECTED to exit red (assertion failures) at Wave 0 and a process/module-load error is a spec defect that must be fixed; intent-classifier skips cleanly when the Plan-02 module is absent; the voice-safety-gate spec encodes the bypass guard AND the D-02 negative gate; specs go green as Waves 1-2 ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI gate | A spec that is authored but unregistered silently never runs, creating a false "green" — the integrity risk this plan exists to close (2026-05-25). A spec only checked via `--list` can also hide a module-load crash that never executes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | playwright.config.ts project regex | mitigate | Task 1 acceptance asserts `--list --project=phase22-stubs` discovers ≥1 spec; verifier re-runs the list command |
| T-22-01-02 | Repudiation | source-contract specs asserting only token presence | mitigate | Task 2 <behavior> requires handler-wiring assertions + a negative bypass guard (modal must NOT contain `markStepComplete`) + the D-02 negative-gate assertion (isAcknowledged-false → ack-prompt speak) per 2026-06-05 |
| T-22-01-03 | Repudiation | a spec that lists but throws on execution (module-load crash) hides behind `--list` and never runs | mitigate | Task 2 verify EXECUTES the full `phase22-stubs` project (not just `--list`); a process/module-load error is treated as a spec defect distinct from a correct token-absent red |
| T-22-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH § Package Legitimacy Audit confirms zero new packages this phase |
</threat_model>

<verification>
- `npx playwright test --list --project=phase22-stubs` discovers all 6 specs.
- `npx playwright test --project=phase22-stubs` EXECUTES every spec (full run, not just list) — source-contract specs red at Wave-0 head, intent-classifier skipped; no process/module-load errors.
- `npx playwright test --project=phase22-stubs tests/phase22/intent-classifier.spec.ts` skips cleanly (module absent).
- No existing project regex modified.
</verification>

<success_criteria>
- phase22-stubs project registered in playwright.config.ts.
- 6 stub specs created, all discoverable AND executed by the full project run, encoding the wiring assertions defined in <behavior>.
- voice-safety-gate spec encodes the positive routing/bypass guard AND the D-02 negative gate (isAcknowledged-false ack-prompt).
- intent-classifier spec is green-when-absent / live-when-present.
- SUMMARY documents the expected red baseline for the five source-contract specs (clean assertion reds, not module-load errors).
</success_criteria>

<output>
Create `.planning/phases/22-voice-driven-walkthrough/22-01-SUMMARY.md` when done. Record the Wave-0 red/skip baseline (which specs are red and why, confirming each is a clean token-absent assertion red rather than a process/module-load error) so Plans 02-04 know their go-green targets.
</output>
</content>
</invoke>
