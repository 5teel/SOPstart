---
phase: 22-voice-driven-walkthrough
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - src/components/sop/voice/WalkthroughVoiceModal.tsx
  - src/components/sop/walkthrough/MobileWalkthrough.tsx
  - src/components/sop/walkthrough/WalkthroughSwitcher.tsx
autonomous: false
requirements: [VDW-VOICE-02, VDW-VOICE-03, VDW-LIT-03, VDW-VOICE-01]
user_setup: []
must_haves:
  truths:
    - "Pressing the mic in the modal starts a real Deepgram stream (no longer the 'we simulate' stub) and final transcripts are classified by classifyIntent (VDW-VOICE-01)"
    - "A spoken 'next'/'done' invokes onVoiceNext → MobileWalkthrough.handleMarkComplete(currentStepId), advancing exactly as the tap 'I've done this — Next' button does (VDW-VOICE-03)"
    - "Voice 'next' NEVER mutates the walkthrough store directly — it routes through handleMarkComplete (markStepAcknowledged + markStepComplete), preserving the D-02 safety-ack invariant and the completion audit trail"
    - "After a voice Q&A answer resolves, useTtsPlayback.speak() reads the answer aloud (VDW-VOICE-02); on step entry/advance the new step text is read aloud (VDW-LIT-03)"
    - "Before safety acknowledgement, voice 'next' has nothing to advance and the worker hears 'please acknowledge the safety hazards first' — voice does not get a weaker gate than tap (D-02)"
    - "Every voice affordance has a visible tap equivalent (manual text box + tap Speak/Stop remain); the worker is never stuck if the mic mishears (D-04)"
  artifacts:
    - path: "src/components/sop/walkthrough/MobileWalkthrough.tsx"
      provides: "useImperativeHandle exposing onVoiceNext/onVoicePrev/currentStepText"
      contains: "useImperativeHandle"
    - path: "src/components/sop/voice/WalkthroughVoiceModal.tsx"
      provides: "Real Deepgram STT + intent dispatch + TTS read-back"
      contains: "startVoiceStream"
    - path: "src/components/sop/walkthrough/WalkthroughSwitcher.tsx"
      provides: "ref-wired voice callbacks passed to the modal"
      contains: "onVoiceNext"
  key_links:
    - from: "src/components/sop/voice/WalkthroughVoiceModal.tsx"
      to: "MobileWalkthrough.handleMarkComplete (via onVoiceNext prop)"
      via: "classifyIntent('next') → onVoiceNext()"
      pattern: "onVoiceNext"
    - from: "src/components/sop/walkthrough/WalkthroughSwitcher.tsx"
      to: "MobileWalkthrough imperative handle"
      via: "useRef + forwardRef/useImperativeHandle"
      pattern: "useImperativeHandle|MobileWalkthroughHandle"
    - from: "src/components/sop/voice/WalkthroughVoiceModal.tsx"
      to: "useTtsPlayback"
      via: "speak(answer) on answer + speak(stepText) on step change"
      pattern: "useTtsPlayback"
---

<objective>
Wire the Wave-1 primitives into the live voice loop: replace the stubbed mic in `WalkthroughVoiceModal` with the real Deepgram stream, classify final transcripts with `classifyIntent`, route navigation commands through `MobileWalkthrough.handleMarkComplete` (the D-02 safety-gated path), and read answers + step text aloud with `useTtsPlayback`. This closes VDW-VOICE-01/02/03 and the voice half of VDW-LIT-03.

Purpose: This is the integration plan — it touches the three coupled components (modal + MobileWalkthrough + WalkthroughSwitcher) that together form the voice→state→audio loop. It is sequenced into Wave 2 (after the Plan 02 infra ships) because it consumes `startVoiceStream(keyterms)`, `classifyIntent`, `extractKeyterms`, and `useTtsPlayback`, and because the modal is the shared concern no other plan may touch.

The central wiring challenge (confirmed by source audit): `WalkthroughSwitcher` currently renders `MobileWalkthrough` and `WalkthroughVoiceModal` as SIBLINGS, so the modal has no access to `handleMarkComplete`/`handleStepChange`/`currentStep`. This plan adds a `useImperativeHandle` on `MobileWalkthrough` and a `ref` in `WalkthroughSwitcher` to bridge them — the RESEARCH-preferred additive option (no broader state hoist).

Output: A working push-to-talk voice walkthrough on the mobile immersive surface, with a hard human-verify checkpoint (a real authenticated browser run is mandatory — source-contract specs cannot prove the mic→TTS→advance loop, per CLAUDE.md 2026-06-05 / 2026-06-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voice-driven-walkthrough/22-RESEARCH.md
@.planning/phases/22-voice-driven-walkthrough/22-PATTERNS.md
@.planning/phases/22-voice-driven-walkthrough/22-02-SUMMARY.md
@src/components/sop/voice/WalkthroughVoiceModal.tsx
@src/components/sop/walkthrough/MobileWalkthrough.tsx
@src/components/sop/walkthrough/WalkthroughSwitcher.tsx
@src/lib/voice/deepgram-stream.ts
@src/stores/walkthrough.ts
@.claude/skills/sketch-findings-SOPstart/references/interaction-patterns.md
</context>

<artifacts_this_phase_produces>
- `src/components/sop/walkthrough/MobileWalkthrough.tsx` — `forwardRef` + `useImperativeHandle` exposing `MobileWalkthroughHandle { onVoiceNext(): void; onVoicePrev(): void; currentStepText: string; isAcknowledged: boolean }`
- `src/components/sop/voice/WalkthroughVoiceModal.tsx` — real `startVoiceStream` wiring, `classifyIntent` dispatch, `onVoiceNext`/`onVoicePrev`/`currentStepText` props, `useTtsPlayback` read-back, hidden `<audio ref={audioRef} />`, `'speaking'` modal state, push-to-talk mic + retained tap fallback
- `src/components/sop/walkthrough/WalkthroughSwitcher.tsx` — `useRef<MobileWalkthroughHandle>` passed to MobileWalkthrough + voice callback props passed to the modal
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose MobileWalkthrough voice handle + wire WalkthroughSwitcher ref</name>
  <files>src/components/sop/walkthrough/MobileWalkthrough.tsx, src/components/sop/walkthrough/WalkthroughSwitcher.tsx</files>
  <read_first>
    - src/components/sop/walkthrough/MobileWalkthrough.tsx (handleMarkComplete lines 177-198 — the EXACT path voice must call: markStepAcknowledged + markStepComplete + handleStepChange + completion record; handleStepChange lines 145-161 — the URL-state hot path using setLocalStepId + window.history.replaceState, NOT router.push; the D-02 gate: `acknowledged = walkthroughStore.isAcknowledged(sopId)` line 66, sticky bar gated on `acknowledged` line 431; currentStep at line 98, prevStep line 99)
    - src/components/sop/walkthrough/WalkthroughSwitcher.tsx (the sibling render: MobileWalkthrough line 57 + WalkthroughVoiceModal lines 62-67; modalOpen state line 50; the bundle-isolation contract comment lines 20-26 — the modal stays next/dynamic)
    - 22-PATTERNS.md § WalkthroughSwitcher.tsx (lines 288-316 — Option 1 useImperativeHandle is the additive choice) and § MobileWalkthrough.tsx (lines 319-365 — MobileWalkthroughHandle shape, do-not-modify handleMarkComplete)
    - CLAUDE.md learning 2026-05-13 (router.push on search-param → RSC fetch under Serwist; voice nav MUST use the existing handleStepChange path) and 2026-06-08 (components rendered as siblings cannot share local state — the reason the ref bridge is needed)
  </read_first>
  <behavior>
    - MobileWalkthrough becomes a `forwardRef` component exposing, via `useImperativeHandle`, a stable handle: `onVoiceNext()` calls `currentStep && handleMarkComplete(currentStep.id)` (the SAME callback the tap "I've done this — Next" button at line 489 invokes); `onVoicePrev()` calls `prevStep && handleStepChange(prevStep.id)`; `currentStepText` returns `currentStep?.text ?? ''`; `isAcknowledged` returns `walkthroughStore.isAcknowledged(sopId)`. The handle deps include currentStep/prevStep/acknowledged so it reflects live state.
    - WalkthroughSwitcher creates `const mwRef = useRef<MobileWalkthroughHandle>(null)`, passes it to `<MobileWalkthrough ref={mwRef} sop={sop} />`, and passes voice callbacks to the modal: `onVoiceNext={() => mwRef.current?.onVoiceNext()}`, `onVoicePrev={() => mwRef.current?.onVoicePrev()}`, `currentStepText={mwRef.current?.currentStepText ?? ''}` — but `currentStepText` must be reactive, so read it through a small state mirror updated on modal-open and on each advance (see Task 2 useEffect), OR pass a getter the modal calls. The modal stays `next/dynamic`-imported (bundle isolation, SB-LINE-06) — do not static-import it.
    - The existing tap "I've done this — Next", Prev/Next nav, photo gate, and submit flow are unchanged. Desktop variant (DesktopWalkthrough) is untouched (voice mobile-first per D-07; the ref is null for the desktop branch and the modal no-ops gracefully).
  </behavior>
  <action>
    Convert `MobileWalkthrough` to `forwardRef<MobileWalkthroughHandle, { sop: SopWithSections }>`. Define and export the `MobileWalkthroughHandle` interface (`onVoiceNext`, `onVoicePrev`, `currentStepText`, `isAcknowledged`). Add `useImperativeHandle(ref, () => ({ ... }), [currentStep, prevStep, acknowledged, handleMarkComplete, handleStepChange])` wiring each handle method to the EXISTING functions — do NOT duplicate or inline the store writes; `onVoiceNext` must call `handleMarkComplete`, never `walkthroughStore.markStepComplete` directly. Do not modify handleMarkComplete or handleStepChange bodies.
    In `WalkthroughSwitcher`: add `const mwRef = useRef<MobileWalkthroughHandle>(null)`; pass `ref={mwRef}` to the mobile branch; extend the `<WalkthroughVoiceModal>` render with `onVoiceNext`, `onVoicePrev`, and a reactive `currentStepText` source (a `useState` mirror seeded/updated from `mwRef.current?.currentStepText`, refreshed when the modal opens and whenever the modal signals an advance via an `onAdvance` callback prop — keep this minimal). Keep the modal `next/dynamic`.
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase22-stubs tests/phase22/voice-safety-gate.spec.ts` is GREEN: MobileWalkthrough.tsx contains `useImperativeHandle`; its `handleMarkComplete` contains BOTH `markStepAcknowledged` and `markStepComplete`; WalkthroughSwitcher.tsx passes `onVoiceNext` to the modal AND uses a `ref` to MobileWalkthrough.
    - The voice-safety-gate spec's bypass guard passes: `WalkthroughVoiceModal.tsx` does NOT contain `markStepComplete` (Task 2 must keep the store write exclusively inside MobileWalkthrough).
    - `npx tsc --noEmit` passes (forwardRef typing + handle interface resolve).
    - `npx playwright test --project=phase15-stubs no-static-desktop-import` stays GREEN — WalkthroughVoiceModal is still imported only via next/dynamic in WalkthroughSwitcher (bundle isolation preserved).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase22-stubs tests/phase22/voice-safety-gate.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>MobileWalkthrough exposes a stable voice handle wired to its existing handleMarkComplete/handleStepChange; WalkthroughSwitcher bridges it to the modal via a ref; no store write leaks into the modal; bundle isolation intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real STT + intent dispatch + TTS read-back in WalkthroughVoiceModal</name>
  <files>src/components/sop/voice/WalkthroughVoiceModal.tsx</files>
  <read_first>
    - src/components/sop/voice/WalkthroughVoiceModal.tsx (the FULL component; ModalState type line 35; the stubbed startListening lines 94-99 — the "for now we simulate" comment is what this task replaces; stopAndAsk lines 101-129 — the existing Q&A fetch path that becomes the 'question' branch; the Speak/Stop buttons lines 208-231 — the existing tap affordances that REMAIN as the D-04 fallback)
    - src/lib/voice/deepgram-stream.ts (startVoiceStream signature + StreamHandle: onPartial/onFinal/onError/stop — now accepting keyterms from Plan 02)
    - 22-02-SUMMARY.md (the exact exports + contracts shipped by Plan 02: classifyIntent, extractKeyterms, useTtsPlayback's speak/stop, the TTS route response shape)
    - 22-PATTERNS.md § WalkthroughVoiceModal.tsx (lines 197-285 — new Props, 'speaking' state, Deepgram wiring adapted from VoiceCaptureControl, handleFinalTranscript intent dispatch, TTS-on-answer + TTS-on-step-change useEffect, mic-stop sequencing)
    - 22-RESEARCH.md Pitfalls 1 (iOS audio rerouting — stop mic before TTS), 2 (token fetched per mic press, not per modal mount), 5 (iOS autoplay unlock — synchronous .play() on the gesture), 6 (iOS audio/mp4 format — handled by existing media-recorder, do not modify)
    - .claude/skills/sketch-findings-SOPstart/references/interaction-patterns.md § 1 (voice state machine: idle→listening→transcribing→…; aria-live announcements; mic aria-labels)
  </read_first>
  <behavior>
    - Props extended: `onVoiceNext: () => void`, `onVoicePrev: () => void`, `currentStepText: string` (plus an optional `onAdvance?: () => void` so the switcher can refresh currentStepText, and `isAcknowledged?: boolean` sourced from the handle). ModalState gains `'speaking'`.
    - startListening (push-to-talk, D-03): calls `startVoiceStream({ language: 'en-NZ', keyterms: extractKeyterms(...) })` fetching a fresh token per press (Pitfall 2); wires onPartial→setTranscript, onError→error state. On the StreamHandle's final transcript, runs handleFinalTranscript. The mic button is a large glove-friendly tap-to-toggle control; the existing Speak/Stop buttons + manual text box REMAIN as the always-visible tap fallback (D-04).
    - handleFinalTranscript(text): `const intent = classifyIntent(text)`. If `'next'`/`'done'`: stop mic + `tts.stop()`, then check the ack gate — if NOT acknowledged (`isAcknowledged === false`), `tts.speak('Please acknowledge the safety hazards first')` and do NOT advance; if acknowledged, call `onVoiceNext()` and let the step-change useEffect read the new step aloud; set state idle. If `'prev'`: `onVoicePrev()`. If `'question'`: route to the existing Q&A path (stopAndAsk with the transcript as the question).
    - TTS-on-answer (VDW-VOICE-02): after the Q&A fetch resolves (`setState('answered')`), call `tts.speak(data.answer.slice(0, 500))` and set `'speaking'`.
    - TTS-on-step-entry (VDW-LIT-03): a `useEffect` on `currentStepText` calls `tts.speak(currentStepText)` when it changes (the step text is read aloud on advance). Guard the first mount so it does not double-read.
    - Mic-stop sequencing (Pitfall 1): always `tts.stop()` and ensure the mic StreamHandle.stop() has released tracks before TTS plays (push-to-talk naturally sequences this — TTS fires after button release).
    - Render a hidden `<audio ref={tts.audioRef} />`. iOS autoplay unlock: on the first mic press (a user gesture) call a synchronous `audioRef.current.play()` stub to unlock (Pitfall 5).
    - The modal MUST NOT import or call any walkthrough-store mutation (`markStepComplete`/`markStepAcknowledged`) — all progression goes through `onVoiceNext`/`onVoicePrev` props (D-02 single-source-of-truth; enforced by the voice-safety-gate bypass guard).
  </behavior>
  <action>
    Extend the modal Props with `onVoiceNext`, `onVoicePrev`, `currentStepText`, optional `onAdvance` and `isAcknowledged`. Add `'speaking'` to `ModalState`. Import `startVoiceStream` from `@/lib/voice/deepgram-stream`, `classifyIntent` from `@/lib/voice/intent-classifier`, `extractKeyterms` from `@/lib/voice/extract-keyterms`, `useTtsPlayback` from `./useTtsPlayback`. Replace the stub `startListening` with the real Deepgram wiring (token-per-press, onPartial/onFinal/onError, store the StreamHandle in a ref). Add `handleFinalTranscript` with the intent dispatch + ack-gate behavior above. In the Q&A branch, after `setState('answered')`, add `void tts.speak(data.answer.slice(0,500))`. Add the `currentStepText` read-aloud `useEffect`. Render the hidden `<audio ref={tts.audioRef} />` and add the iOS unlock on first mic press. Keep the manual text box + Speak/Stop tap controls. Do NOT import any walkthrough store mutation here. To read the ack state, accept it via the `isAcknowledged` prop the switcher already exposes (sourced from `mwRef.current?.isAcknowledged`) rather than reading the store in the modal.
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase22-stubs tests/phase22/voice-modal.spec.ts` is GREEN: WalkthroughVoiceModal.tsx contains `startVoiceStream(` AND `classifyIntent(` AND `onVoiceNext` AND a `speak(` call AND `useTtsPlayback`.
    - `npx playwright test --project=phase22-stubs tests/phase22/voice-safety-gate.spec.ts` stays GREEN AND its bypass guard holds: WalkthroughVoiceModal.tsx does NOT contain `markStepComplete` (no direct store mutation; progression only via props).
    - Handler-wiring assertion (2026-06-05): the voice-modal spec asserts the `classifyIntent` 'next'/'done' branch calls `onVoiceNext()` (grep that within the file the next-branch invokes `onVoiceNext` — not merely that the prop name appears in the Props interface). The 'question' branch reaches the existing `/api/voice/query` fetch.
    - `npx tsc --noEmit` passes.
    - `npx playwright test --project=phase15-stubs no-static-desktop-import` stays GREEN (bundle isolation: the new imports load with the modal chunk; the modal is still only dynamic-imported in the switcher).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase22-stubs tests/phase22/voice-modal.spec.ts tests/phase22/voice-safety-gate.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The modal runs a real Deepgram stream, classifies transcripts, routes navigation through onVoiceNext (never the store directly), and reads answers + step text aloud; tap fallbacks remain; bundle isolation intact; voice-modal + voice-safety-gate specs green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — real authenticated voice loop on sopstart.com</name>
  <files>(verification only — no files modified)</files>
  <what-built>
    The full push-to-talk voice loop on the mobile immersive walkthrough: mic → Deepgram STT → intent classification → (a) voice "next"/"done" advances the step through the D-02 safety-gated path, (b) a spoken question is answered and the answer is read back aloud via TTS, and step text is read aloud on entry. Plus the always-on visual layer (Plan 04). Source-contract specs are all green, but per CLAUDE.md 2026-06-05 / 2026-06-08, grep-level tests cannot prove the mic→TTS→advance loop actually works at runtime — a real authenticated browser/device run is mandatory before "done."
  </what-built>
  <how-to-verify>
    Per the project's Railway-only-testing rule, verify on the deployed app (sopstart.com), NOT localhost. After this branch is merged and Railway has deployed:
    1. Install the PWA on an iPhone (home-screen) and open a published SOP's Walkthrough tab.
    2. Tap the mic pill, push-to-talk, and ask a step question (e.g. "how do I change the blank side hanger") — confirm: transcription appears, an answer is shown, AND the answer is read back aloud (VDW-VOICE-02).
    3. On a non-safety step, say "done" or "next" — confirm the walkthrough advances to the next step AND the new step text is read aloud (VDW-VOICE-03, VDW-LIT-03).
    4. On a step that requires safety acknowledgement (before acknowledging), say "next" — confirm it does NOT advance and you hear "please acknowledge the safety hazards first" (D-02).
    5. Confirm every voice action also has a working tap equivalent — tap "I've done this — Next" still advances; the manual text box still answers (D-04).
    6. Confirm each step card shows a photo or a type icon (never blank) — Plan 04 visual layer (VDW-LIT-01/02).
    7. (Field/iOS specifics flagged LOW-confidence in RESEARCH) confirm TTS audio plays in the installed PWA (autoplay unlock) and STT returns non-empty transcripts on iOS (audio/mp4 path). Note ≥90% noise accuracy (VDW-VOICE-01) is a separate field test with representative SOP vocabulary in a noisy environment.
  </how-to-verify>
  <action>BLOCKING human checkpoint — no automated action. Pause for Simon to run the manual UAT on sopstart.com per how-to-verify. Do not mark the phase done until approved.</action>
  <verify>
    <human-check>Voice loop advances steps, reads answers + step text aloud, respects the safety gate, and the visual layer renders on a real authenticated sopstart.com session.</human-check>
  </verify>
  <done>Simon has confirmed (typed "approved") that the runtime voice loop + visual layer work on a real authenticated sopstart.com session, or has logged the specific failures for gap closure.</done>
  <resume-signal>Type "approved" when the voice loop advances steps, reads answers + step text aloud, respects the safety gate, and the visual layer renders — or describe the failures (which step, device, and observed vs expected) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice command → walkthrough state machine | A spoken "next" crosses into the completion/ack state machine — the integrity surface where a weaker gate would let voice bypass the safety acknowledgement |
| modal → MobileWalkthrough store | The modal is a sibling with no direct store access by design; the only write path is the onVoiceNext/onVoicePrev props that invoke handleMarkComplete (the audited path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Elevation of Privilege | voice "next" bypassing the D-02 safety re-acknowledgement | mitigate | Voice "next" routes ONLY through onVoiceNext → handleMarkComplete (markStepAcknowledged + markStepComplete + completion record); the modal contains no store mutation (voice-safety-gate bypass guard asserts `markStepComplete` absent from the modal); before ack, voice "next" no-ops + TTS prompts to acknowledge — same gate as tap |
| T-22-03-02 | Tampering | completion audit trail broken by skipping handleMarkComplete | mitigate | onVoiceNext calls the existing handleMarkComplete which starts/updates the completion record; no alternate advance path is introduced; source-contract asserts handleMarkComplete retains both store writes |
| T-22-03-03 | Repudiation | a "dead" voice-next that greps green but never advances (2026-06-05) | mitigate | voice-modal spec asserts the classifyIntent 'next' branch calls onVoiceNext (handler wiring, not prop presence); Task 3 human-verify proves runtime advance on a real device (2026-06-08) |
| T-22-03-04 | Information Disclosure | voice Q&A leaking another SOP's content | accept | The 'question' branch reuses the existing /api/voice/query route whose single-SOP RLS-scoped fetch + sub-trade gate (Phase 15 SB-LINE-04) is unchanged; no new query path added |
| T-22-03-05 | Denial of Service | TTS read-aloud spamming /api/voice/tts on rapid advances | mitigate | useTtsPlayback.speak stops in-progress audio before each call; the TTS route's per-user in-flight cap (Plan 02, 429) bounds concurrency; TTS is fail-silent so a 429 degrades to no-audio, never blocking the advance |
| T-22-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH § Package Legitimacy Audit: not applicable — all deps already installed); the legitimacy checkpoint is N/A this phase |
</threat_model>

<verification>
- `npx playwright test --project=phase22-stubs tests/phase22/voice-modal.spec.ts tests/phase22/voice-safety-gate.spec.ts` green.
- `npx playwright test --project=phase15-stubs no-static-desktop-import` green (bundle isolation preserved).
- `npx tsc --noEmit` clean.
- `npm run test` green at wave merge (full suite).
- Task 3 human-verify approved on sopstart.com before the phase is "done."
</verification>

<success_criteria>
- Mic runs a real Deepgram stream with SOP keyterms; final transcripts classified.
- Voice "next"/"done" advances via handleMarkComplete (D-02 gate intact, audit trail intact); voice never mutates the store directly.
- Answers + step text read aloud via TTS; tap fallbacks remain (D-04).
- Bundle isolation (SB-LINE-06) preserved; modal still dynamic-only.
- Human UAT approved (real authenticated run — the mandatory non-grep gate).
</success_criteria>

<output>
Create `.planning/phases/22-voice-driven-walkthrough/22-03-SUMMARY.md` when done. Per CLAUDE.md Pathways-Map-Maintenance triggers: the immersive walkthrough screen gains a voice-driven mode — confirm `src/lib/journeys/journeys.ts` reflects the voice-driven walkthrough flow (extend the worker walkthrough Journey to note the voice path; no new route is added, so coverage stays at 0 not-mapped). Record the Task-3 UAT outcome and any iOS audio findings for the multi-language fast-follow phase.
</output>
