---
phase: 36-refresher-cadence-version-currency
plan: 07
type: execute
wave: 5
depends_on: ["36-06"]
files_modified:
  - src/components/admin/competency/StatePill.tsx
  - src/components/admin/competency/TrainingMatrixView.tsx
  - tests/phase36/matrix-chips-and-axis-swap.spec.ts
autonomous: true
requirements: [CMP-03, REF-01, TRN-03]

must_haves:
  truths:
    - "A worker trained on a prior version shows an 'Outdated version' chip BESIDE their existing competency pill — the pill's own label, colour and state are unchanged (D-04)"
    - "A cell whose refresher is due/overdue shows a coaching-toned chip, never red-alarm styling, and never a lock/disabled affordance (CMP-04)"
    - "Matrix rollups read as an APPENDED tally — '4/6 signed off · 2 on outdated version' — with the signed-off/competent counts unchanged (D-05)"
    - "A header toggle transposes the matrix (workers as columns, SOPs as rows) and the fit-driven compaction still works in the transposed orientation"
    - "Every chip uses an already-declared CSS token — no new `var(--x)` is referenced without a declaration"
  artifacts:
    - path: "src/components/admin/competency/StatePill.tsx"
      provides: "outdated-version + refresher-due sibling chips (informational only)"
      contains: "Outdated version"
    - path: "src/components/admin/competency/TrainingMatrixView.tsx"
      provides: "rollup tallies + axis-swap toggle"
      contains: "outdatedCount"
  key_links:
    - from: "src/components/admin/competency/TrainingMatrixView.tsx"
      to: "src/components/admin/competency/StatePill.tsx"
      via: "<StatePill result={cell} /> passing the whole MatrixCell"
      pattern: "StatePill result="
---

<objective>
Surface version-currency and refresher state on the supervisor/admin surfaces: sibling chips on the competency pill, appended rollup tallies, and the folded axis-swap toggle.

Purpose: D-04 is literal — CMP-03 says never reset or orphan, so the pill must not change at all; staleness is an additional chip and an additional number, never a demotion or a subtraction. Adding the chips inside `StatePill` lights up all three consuming surfaces (matrix cells, the per-worker record panel, and the worker profile section) in one change, because every caller already passes the whole state object.
Output: chips, tallies, axis-swap toggle, one source-contract guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.planning/todos/pending/2026-07-26-matrix-axis-swap.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatePill — outdated-version and refresher-due sibling chips</name>
  <files>src/components/admin/competency/StatePill.tsx</files>
  <read_first>
    - src/components/admin/competency/StatePill.tsx (whole file — the `needsSupportFlag` sibling-pill pattern at the end of the render, the accent-var mapping, and the header comment's "purely informational... never a control" framing)
    - src/styles/blueprint-theme.css lines 40-62 and the `.state-pill-support` rule (~line 112-120) — the declared accent tokens and the existing coaching-chip idiom
    - src/lib/competency/matrix.ts (MatrixCell), src/actions/competency.ts (MyCompetencyState, RequiredSopRecord) — the three object shapes callers pass in
  </read_first>
  <action>
    Widen `StatePillProps.result` from `Pick<CompetencyResult, 'state' | 'needsSupportFlag' | 'awaitingSignOff'>` to that same Pick intersected with `Partial<{ isOutdatedVersion: boolean; refresherDueAt: string | null; isRefresherOverdue: boolean }>`. All three new fields are OPTIONAL, so every existing call site keeps compiling and any caller that passes a whole `MatrixCell` / `MyCompetencyState` / `RequiredSopRecord` picks the chips up automatically (all three current call sites — `TrainingMatrixView.tsx`, `TrainingRecordSection.tsx`, `CompetencySection.tsx` — already spread whole objects; verify this by reading them before you finish).

    Render two additional sibling `<span className="pill">` elements after the existing `needsSupportFlag` chip, inside the same `inline-flex items-center gap-1.5` wrapper. Never replace, recolour, mute or relabel the primary pill (D-04).
    - Outdated-version chip: rendered when `isOutdatedVersion` is true. Label `Outdated version`. Use `--accent-voice` (orange, declared at blueprint-theme.css line 47) so it is visually distinct from the amber needs-support chip while staying in the coaching family. Add `title="Trained on a prior version of this SOP — their training record is unchanged"`.
    - Refresher chip: rendered when `refresherDueAt` is non-null. Label `Refresher overdue` when `isRefresherOverdue` is true, otherwise `Refresher due {formatted date}` (NZ locale, `en-NZ`, day/short-month/year — the `formatNZDate` idiom already used in `TrainingRecordSection.tsx`). Use `--accent-decision` (amber) for both states — coaching, not discipline; NEVER `--accent-escalate`/red on any refresher or version chip (CONTEXT § Specifics).

    Style the chips with inline `color`/`borderColor` `var(--accent-...)` exactly as the primary pill does, or with a `color-mix(in srgb, var(--accent-x) 12%, transparent)` background mirroring `.state-pill-support`. Before using ANY token, confirm it is declared: `grep -n -- "--accent-voice:\|--accent-decision:" src/styles/blueprint-theme.css` must return both. Do not introduce a new token (CLAUDE.md 2026-07-14 — an undefined custom property renders wrong and fails nothing).

    Render the chips with `{flag && <span .../>}` JSX guards only. Do NOT introduce an `if (isOutdatedVersion)` branch, a comparison on these fields, a `disabled` attribute, an `onClick`, or a lock icon anywhere in this file — `tests/phase36/no-refresher-gate.spec.ts` forbids it and CMP-04 is the locked north star. Extend the header comment to state that the two new chips are additive, informational, and that the primary pill never changes on their account.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Outdated version" src/components/admin/competency/StatePill.tsx` >= 1
    - `grep -c "Refresher" src/components/admin/competency/StatePill.tsx` >= 2 (due + overdue labels)
    - `grep -c "accent-escalate" src/components/admin/competency/StatePill.tsx` == 0
    - Every `var(--...)` token referenced in this file is declared in `src/styles/blueprint-theme.css` (verify with `grep -n -- "<token>:" src/styles/blueprint-theme.css` for each)
    - `npx playwright test --project=phase36` green (the no-refresher-gate guard passes — no `if`/comparison branch on the new fields)
    - `npx playwright test tests/lint/no-undefined-css-tokens.spec.ts` green (if the project regex covers it, run via its registered project)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The pill renders up to three informational siblings; all three consuming surfaces inherit the chips with no further wiring.</done>
</task>

<task type="auto">
  <name>Task 2: TrainingMatrixView — appended rollup tallies</name>
  <files>src/components/admin/competency/TrainingMatrixView.tsx</files>
  <read_first>
    - src/components/admin/competency/TrainingMatrixView.tsx (whole file — the column-header rollup block `{rollup.signedOffCount}/{rollup.total} signed off … {rollup.needsSupportCount} needs support` and the row rollup block `{rollup.competentCount}/{rollup.total} competent …`)
    - src/lib/competency/matrix.ts (RowRollup/ColRollup as extended in 36-04 — `outdatedCount`, `refresherOverdueCount`)
  </read_first>
  <action>
    Extend both rollup renderings so staleness is APPENDED, never subtracted (D-05). The locked copy pattern from CONTEXT is `4/6 signed off · 2 on outdated version` — keep the existing `{count}/{total} signed off` and `{count}/{total} competent` fragments byte-identical, keep the existing needs-support fragment, then append, each rendered only when its count is > 0:
    - `· {outdatedCount} on outdated version` in `--accent-voice`
    - `· {refresherOverdueCount} refresher overdue` in `--accent-decision`
    Apply the same two appended fragments to BOTH the column-header rollup and the row rollup, using the identical `text-[10px] font-normal` sizing already in place.

    Leave `COMPACT_LEGEND` and `compactAccentVar` alone — compact mode is a single state dot and gains no new colours; the chips are a non-compact affordance. Do not add any control, filter or sort tied to the new counts.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "on outdated version" src/components/admin/competency/TrainingMatrixView.tsx` == 2 (row + column rollup)
    - `grep -c "refresher overdue" src/components/admin/competency/TrainingMatrixView.tsx` == 2
    - The existing `signed off` and `competent` rollup fragments are unchanged (`git diff` shows them as context, not modifications)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both rollup axes carry the outdated and refresher tallies alongside unchanged competency counts.</done>
</task>

<task type="auto">
  <name>Task 3: Axis-swap toggle (folded todo) + source-contract guard</name>
  <files>src/components/admin/competency/TrainingMatrixView.tsx, tests/phase36/matrix-chips-and-axis-swap.spec.ts</files>
  <read_first>
    - src/components/admin/competency/TrainingMatrixView.tsx (the filter row where the toggle lands, the `<table>` render, `cellFor`/`rowRollupFor`/`colRollupFor`, and the `isCompact` calculation `containerWidth > 0 && sops.length * COLUMN_WIDTH_PX > containerWidth` plus its ResizeObserver)
    - .planning/todos/pending/2026-07-26-matrix-axis-swap.md (the folded todo — raised in Phase 35 UAT)
  </read_first>
  <action>
    Add a `transposed` boolean state (default false — SOPs as columns, the current layout) and a small toggle control in the existing filter row, next to the Export CSV button. Label it plainly, e.g. `Swap rows/columns`, with `aria-pressed={transposed}`.

    Implement the transpose as a PRESENTATION-ONLY remap immediately before render — derive `rowItems` / `colItems` from `people`/`sops` (swapped when `transposed`), and look each cell up with the existing `cellFor(personId, sopId)` by passing the ids in the correct order for the current orientation. State derivation is symmetric, so `matrix.cells`, both rollup arrays, and the server fetch are entirely unchanged; do NOT add a server round-trip, a new query, or a second cell-lookup path.

    Fix the compaction calculation to follow the orientation (35 D-07 — a measured-width calc, never a hardcoded column-count threshold): `isCompact` must multiply `COLUMN_WIDTH_PX` by the number of items currently rendered AS COLUMNS (`colItems.length`), not by `sops.length` unconditionally. Confirm the ResizeObserver still drives `containerWidth` for the transposed table.

    Render the correct rollup on each axis after transpose: the sticky first column shows the rollup for whatever entity is now the row (person rollup when normal, SOP rollup when transposed) and the header cells show the other. Cell click still calls `onSelectCell(personId, sopId)` with the arguments in their original semantic order regardless of orientation — a transposed matrix must deep-link to the same PersonPanel record.

    Then create `tests/phase36/matrix-chips-and-axis-swap.spec.ts` with source-contract assertions: `StatePill.tsx` renders both new chips and contains no `disabled`/`onClick`/`if (` branch on the new fields; `TrainingMatrixView.tsx` contains the two appended rollup fragments on both axes, contains a `transposed` state with an `aria-pressed` toggle, and its `isCompact` expression no longer hardcodes `sops.length` (assert the compaction expression references the orientation-aware column array). Use `[\s\S]` not the `/s` flag; normalize `\r\n` before multi-line matches.

    Finally, move `.planning/todos/pending/2026-07-26-matrix-axis-swap.md` to `.planning/todos/done/` (create the directory if the repo convention places done todos elsewhere — check for an existing `.planning/todos/done/` first).
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transposed" src/components/admin/competency/TrainingMatrixView.tsx` >= 3 (state, toggle, render remap)
    - `grep -c "aria-pressed" src/components/admin/competency/TrainingMatrixView.tsx` >= 1
    - The `isCompact` expression does NOT contain the literal `sops.length` (it references the orientation-aware column array instead)
    - `onSelectCell(` is still called with `(personId, sopId)` semantics in both orientations — asserted by the spec
    - `2026-07-26-matrix-axis-swap.md` no longer exists under `.planning/todos/pending/`
    - `npx playwright test --project=phase36` green; `npx tsc --noEmit` exits 0; `npm run build` completes
  </acceptance_criteria>
  <done>The matrix transposes on demand with correct compaction and rollups, and the folded todo is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-derived competency fields → admin browser | Render-only; no new client-side authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07-01 | Elevation of Privilege | new chips becoming gates | mitigate | `tests/phase36/no-refresher-gate.spec.ts` forbids comparison/`if` branches on `isOutdatedVersion`/`isRefresherOverdue` in worker-facing files; this plan's own spec additionally forbids `disabled`/`onClick` on the chips in `StatePill.tsx` |
| T-36-07-02 | Tampering | undeclared CSS custom property rendering a chip invisible | mitigate | Only `--accent-voice` and `--accent-decision` are used, both verified declared in `blueprint-theme.css`; `tests/lint/no-undefined-css-tokens.spec.ts` is the standing CI net (CLAUDE.md 2026-07-14) |
| T-36-07-03 | Information Disclosure | transposed matrix exposing extra data | accept | Transpose is a pure client-side remap of already-fetched, already-org-scoped data — no new fetch, no new field |
</threat_model>

<verification>
- `npx playwright test --project=phase36` green
- `npx tsc --noEmit` clean; `npm run build` clean
- Manual (post-deploy, per 36-VALIDATION Manual-Only): chips visible in light and dark on sopstart.com — a CSS-token bug is invisible to every automated gate
</verification>

<success_criteria>
- Outdated-version and refresher chips render as siblings of an unchanged competency pill
- Both rollup axes append the outdated and refresher-overdue tallies with competency counts untouched
- Axis-swap toggle transposes the matrix with orientation-aware compaction and unchanged deep-linking
- No new CSS token introduced; no gating branch on the new fields
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan introduces the optional `StatePill` chip props and `tests/phase36/matrix-chips-and-axis-swap.spec.ts`.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-07-SUMMARY.md` when done
</output>
