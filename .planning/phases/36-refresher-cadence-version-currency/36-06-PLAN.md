---
phase: 36-refresher-cadence-version-currency
plan: 06
type: execute
wave: 4
depends_on: ["36-05"]
files_modified:
  - src/actions/competency.ts
  - tests/phase36/version-breakdown-panel.spec.ts
autonomous: true
requirements: [TRN-03, REF-01, CMP-03]

must_haves:
  truths:
    - "The training CSV export carries `on_current_version` and `refresher_due_date` for every completion row (D-05/D-07)"
    - "An admin can retrieve, per SOP lineage, how many workers completed each version and who they were, with the current version distinguished (TRN-03/D-09)"
    - "The breakdown read is gated to the versions page's existing roles (admin / safety_manager) and self-enforces the caller's organisation"
  artifacts:
    - path: "src/actions/competency.ts"
      provides: "CSV version/refresher columns + getVersionCompletionBreakdown"
      exports: ["getVersionCompletionBreakdown"]
    - path: "tests/phase36/version-breakdown-panel.spec.ts"
      provides: "TRN-03 action-side source-contract assertions (live)"
  key_links:
    - from: "src/actions/competency.ts exportTrainingCsv"
      to: "src/lib/competency/csv.ts"
      via: "TrainingCsvRow.onCurrentVersion / refresherDueDate through generateTrainingCsv"
      pattern: "onCurrentVersion"
---

<objective>
Finish the server side of Phase 36: fill the two new CSV columns from lineage data, and add the TRN-03 read that powers the per-version completion breakdown panel.

Purpose: the CSV is the audit artifact orgs hand to auditors (D-07), so version-currency and refresher state must be exportable, not just visible on screen. And TRN-03 is the one requirement with no home in the matrix — it belongs on the versions page, next to the supersede history that created the situation.
Output: populated CSV columns + `getVersionCompletionBreakdown` + live TRN-03 action assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.planning/phases/36-refresher-cadence-version-currency/36-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate on_current_version and refresher_due_date in exportTrainingCsv</name>
  <files>src/actions/competency.ts</files>
  <read_first>
    - src/actions/competency.ts `exportTrainingCsv` (its filtered `sop_completions` query, the `sopById` map built from `id, title, sop_number`, and the `rows: TrainingCsvRow[]` mapping)
    - src/actions/competency.ts `resolveLineage` (added in 36-05 — reuse it, do not write a second lineage resolver)
    - src/lib/competency/csv.ts (as modified in 36-04 — the `onCurrentVersion` / `refresherDueDate` fields and the `on_current_version` / `refresher_due_date` headers)
    - src/lib/competency/version-currency.ts, src/lib/competency/refresher.ts
  </read_first>
  <action>
    `exportTrainingCsv` emits ONE ROW PER COMPLETION EVENT (D-14), including completions against superseded versions, so it needs each completion's lineage context rather than a per-worker rollup.

    Changes:
    - Extend the `sops` fetch from `'id, title, sop_number'` to `'id, title, sop_number, version, parent_sop_id, refresher_interval_months'`. Note this fetch is keyed on the DISTINCT `sop_id`s present in the completion result — which after a supersede includes superseded rows. Keep `.eq('organisation_id', orgId)`.
    - Call `resolveLineage` (admin client, `orgId`) over those rows to obtain `canonicalBySopId`, `currentVersionBySopId` and `refresherIntervalBySopId`. Because the completion rows themselves can name superseded SOPs, resolve each row's canonical id first, then read the current version / interval from the canonical entry — falling back to the completion's own sop row when no canonical mapping exists.
    - In the `rows` mapping add:
      `onCurrentVersion: !isOutdatedVersion(c.sop_version, currentVersionForThisLineage)` — i.e. `yes` when the completion was against the current version, `no` when it was against a prior one.
      `refresherDueDate: refresherDueDate(c.submitted_at, intervalForThisLineage)` — the due-date implied by THAT completion event, which is what a per-event audit row should carry; `null` (empty cell) whenever the SOP has no interval (D-02).
    - Change nothing about the existing nine columns, the date-range filters, `nextDayIso`, or the role gate.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35-unit --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "onCurrentVersion" src/actions/competency.ts` >= 1 and `grep -c "refresherDueDate" src/actions/competency.ts` >= 1
    - The `sops` select inside `exportTrainingCsv` includes `version` and `refresher_interval_months`
    - No second lineage query is introduced: `grep -c "parent_sop_id.in." src/actions/competency.ts` is still 1
    - `npx tsc --noEmit` exits 0 and `npm run build` completes
  </acceptance_criteria>
  <done>Every exported completion row states whether it was against the current version and when its refresher falls due.</done>
</task>

<task type="auto">
  <name>Task 2: getVersionCompletionBreakdown (TRN-03) + live panel-action guard</name>
  <files>src/actions/competency.ts, tests/phase36/version-breakdown-panel.spec.ts</files>
  <read_first>
    - src/actions/competency.ts (the `RECORDER_ROLES` gate, `callerOrgId`, `resolveDisplayNames`, and `resolveLineage`)
    - src/actions/versioning.ts (`getVersionHistory` + the `['admin','safety_manager']` role gate used by `uploadNewVersion`/`cloneSopAsDraft` — the versions page's existing access boundary)
    - tests/phase36/version-breakdown-panel.spec.ts (the Wave-0 stub from 36-01 — its guards self-activate; fill in the real assertions here)
    - .planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md (Open Question 1 — resolved: keep the versions page's stricter gate, do NOT loosen to RECORDER_ROLES)
  </read_first>
  <action>
    Add `getVersionCompletionBreakdown(sopId: string)` to `src/actions/competency.ts`, returning `{ breakdown: VersionCompletionBreakdown } | { error: string }` where `VersionCompletionBreakdown` is `{ sopId: string; versions: Array<{ sopId: string; version: number; isCurrent: boolean; completionCount: number; workers: Array<{ userId: string; displayName: string; completedAt: string }> }> }`.

    Rules:
    - Role gate: `['admin', 'safety_manager']` — the gate the versions page already enforces. Do NOT use `RECORDER_ROLES`: adding supervisor access here would silently widen who can see version/approval history, a product decision CONTEXT does not make (RESEARCH Open Question 1). Add a comment saying exactly that, naming the alternative and why it was rejected.
    - Resolve the caller's org via `getSessionContext()` + `callerOrgId()`; then verify the supplied `sopId` belongs to that org (`sops` row fetched with `.eq('organisation_id', orgId)`) BEFORE any further read. Never trust a client-supplied `sopId` alone.
    - Resolve the lineage with `resolveLineage` over that single required row, then fetch `sop_completions` for `.in('sop_id', lineage.allSopIds)` `.eq('organisation_id', orgId)`, plus the lineage's `sops` rows for their `version` values.
    - Group completions by their ACTUAL `sop_id` (this action reports per-version, so it must NOT remap to canonical), count distinct workers per version, and resolve names via the existing `resolveDisplayNames` (email is the display name everywhere in this codebase — do not invent a full-name source).
    - Mark `isCurrent` on the entry whose `version` equals the requested SOP's `version` (the row the requirement junctions point at). Do not derive it from `superseded_by`, which `cloneSopAsDraft`/`performPublish` do not set.
    - Order versions descending by `version`, matching `getVersionHistory`'s ordering on the same page.
    - This is a READ ONLY. It never writes, never gates anything, and is not referenced from any worker-facing file.

    Then fill in `tests/phase36/version-breakdown-panel.spec.ts` with the action-side assertions LIVE (the panel-side assertion added in 36-01 stays `fs.existsSync`-guarded until 36-09 renders it): `getVersionCompletionBreakdown` is exported from `src/actions/competency.ts`; its body contains `'admin'` and `'safety_manager'` and does NOT reference `RECORDER_ROLES`; it contains an `.eq('organisation_id'` org self-enforcement filter; and it contains no `.update(`/`.insert(`/`.delete(` call.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `getVersionCompletionBreakdown` is an `async` export of the `'use server'` module (a sync export breaks `next build` — CLAUDE.md 2026-06-27), proven by `npm run build` completing
    - The spec asserts the `['admin','safety_manager']` gate AND the absence of `RECORDER_ROLES` inside that function body
    - The spec asserts the org self-enforcement filter and the absence of any write call in that function body
    - `npx playwright test --project=phase36` green
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The TRN-03 data source exists, is correctly gated and org-scoped, and is guarded mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin session → `getVersionCompletionBreakdown(sopId)` | Untrusted `sopId` crosses here into an admin-client read |
| exported CSV → Excel/Sheets on an auditor's machine | Field content is interpreted by a spreadsheet engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Information Disclosure | `getVersionCompletionBreakdown` client-supplied `sopId` | mitigate | The SOP row is fetched with `.eq('organisation_id', orgId)` and a miss returns an error BEFORE any completion read; `orgId` re-derived via `callerOrgId()` from `organisation_members` |
| T-36-06-02 | Elevation of Privilege | role gate drift toward `RECORDER_ROLES` | mitigate | Guard spec asserts `'admin'`/`'safety_manager'` present and `RECORDER_ROLES` absent in that function body — the versions page's existing access boundary cannot be widened by accident |
| T-36-06-03 | Tampering | CSV formula injection via new columns | mitigate | Both new values are machine-generated ('yes'/'no', ISO date) AND still routed through `csvField()` in `generateTrainingCsv` (CLAUDE.md 2026-07-24) |
| T-36-06-04 | Information Disclosure | worker identities in the breakdown | accept | Emails of the caller's own org members, shown to admin/safety_manager only — the same data class the training matrix already surfaces to a narrower role set |
</threat_model>

<verification>
- `npx playwright test --project=phase36 --project=phase35-unit` green
- `npx tsc --noEmit` clean; `npm run build` clean
- Still exactly one lineage query in competency.ts
</verification>

<success_criteria>
- CSV rows carry populated `on_current_version` and `refresher_due_date`
- `getVersionCompletionBreakdown` returns per-version completion counts + worker lists with `isCurrent` marked
- Gate is admin/safety_manager, org self-enforced, read-only — all three mechanically asserted
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan introduces `getVersionCompletionBreakdown` and the `VersionCompletionBreakdown` interface.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-06-SUMMARY.md` when done
</output>
