---
phase: 36-refresher-cadence-version-currency
plan: 09
type: execute
wave: 5
depends_on: ["36-06"]
files_modified:
  - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx
  - src/actions/versioning.ts
  - tests/phase36/version-breakdown-panel.spec.ts
autonomous: true
requirements: [TRN-03, REF-01]

must_haves:
  truths:
    - "On the versions page an admin sees, per version, how many workers completed it and who they were, with the current version distinguished (TRN-03/D-09)"
    - "An admin can set or clear this SOP's refresher interval from the same page, and the page reflects the saved value"
    - "The panel and the interval control are admin/safety_manager only — the versions page's existing access boundary is unchanged"
    - "Nothing on this page gates or blocks worker access; the breakdown is a read"
  artifacts:
    - path: "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"
      provides: "TRN-03 completion-breakdown panel + refresher-interval control"
      contains: "getVersionCompletionBreakdown"
  key_links:
    - from: "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"
      to: "src/actions/competency.ts"
      via: "getVersionCompletionBreakdown(sopId) in the existing load effect"
      pattern: "getVersionCompletionBreakdown"
    - from: "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"
      to: "src/actions/governance.ts"
      via: "setRefresherInterval onClick handler (wired, not empty)"
      pattern: "setRefresherInterval"
---

<objective>
Land TRN-03 where it belongs: a per-version completion breakdown on the version-history page, alongside a control for this SOP's refresher interval.

Purpose: the supersede history that creates "trained on an outdated version" already lives on this page (D-09), and the admin who supersedes a SOP is the one who needs to know who is now behind. Putting the refresher-interval control here too means Phase 36 adds ZERO new admin routes — the least-new-surface option CONTEXT asked for.
Output: one panel, one control, live TRN-03 assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.planning/phases/36-refresher-cadence-version-currency/36-06-SUMMARY.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRN-03 completion-breakdown panel</name>
  <files>src/app/(protected)/admin/sops/[sopId]/versions/page.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx (whole file — the `loadVersions` effect that calls `getVersionHistory` then `getApprovalHistory`, the `currentSop` derivation `versions.find(v => v.superseded_by === null) ?? versions[0]`, the version-row grid, the read-only approval-history `<ul>` under each row, and `formatDate`)
    - src/actions/competency.ts `getVersionCompletionBreakdown` + `VersionCompletionBreakdown` (36-06)
  </read_first>
  <action>
    Extend the existing `loadVersions` effect to also call `getVersionCompletionBreakdown(sopId)` after `getVersionHistory` resolves, storing the result in a `breakdown` state (and an error state). Follow the file's existing pattern for `getApprovalHistory`: a single effect, results narrowed with an `'success' in result`-style check, failures surfaced rather than silently swallowed (a returned error must render as a message, never as an empty panel — the 2026-07-20 silent-dead-feature class).

    Render the completion evidence INSIDE each existing version row block, directly beneath the approval-history list, so the page keeps one chronological version list rather than growing a second parallel table:
    - A one-line summary per version: `{completionCount} completed this version`, rendered for every version including zero (`No completions on this version`) so an admin can tell "nobody" from "not loaded".
    - The current version's line carries the existing `Current` badge context; for non-current versions with `completionCount > 0`, append an amber `— trained on an outdated version` note using `--accent-voice`, the same token the matrix chip uses. Confirm the token is declared in `src/styles/blueprint-theme.css` before use (CLAUDE.md 2026-07-14).
    - An expand/collapse control per version (`aria-expanded`, default collapsed) revealing the worker list: display name + completion date via the existing `formatDate` helper.

    Frame the copy as coaching, not discipline (CONTEXT § Specifics): this panel reports who is on which version, it never labels anyone non-compliant and never links to a "force re-walk" action. Add no write control, no assignment control, and no filter.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getVersionCompletionBreakdown" "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"` >= 2 (import + call)
    - The page renders an explicit zero-state string for versions with no completions
    - `grep -c "aria-expanded" "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"` >= 1
    - Every `var(--...)` token newly referenced in this file is declared in `src/styles/blueprint-theme.css`
    - `npx tsc --noEmit` exits 0 and `npm run build` completes
  </acceptance_criteria>
  <done>Each version row reports its completion count and expands to the worker list.</done>
</task>

<task type="auto">
  <name>Task 2: Refresher-interval control on the versions page</name>
  <files>src/app/(protected)/admin/sops/[sopId]/versions/page.tsx, src/actions/versioning.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx (the action-button row holding "Upload New Version" and "Edit into new version", and the inline-confirmation-card idiom used by `showUploadConfirm` / `showCloneConfirm`)
    - src/actions/governance.ts `setRefresherInterval` (36-03 — signature `(sopId, months: number | null)`, returns `{ success: true } | { error: string }`)
    - src/actions/versioning.ts `getVersionHistory` + the `VersionRecord` interface (the page's existing data source — this is where the current interval value comes from)
  </read_first>
  <action>
    First, surface the stored value: add `refresher_interval_months` to `getVersionHistory`'s `sops` select list and to the `VersionRecord` interface as `refresher_interval_months: number | null`. That is a two-line additive change to the page's EXISTING data source — do not add a second fetch for one integer.

    Then add a compact refresher control to the action-button row (after "Edit into new version"), operating on `currentSop.id`:
    - A number `<input type="number" min={1} max={120} step={1}>` seeded from `currentSop.refresher_interval_months` plus a Save button, and a separate "Turn off" affordance that submits `null`.
    - Save calls `setRefresherInterval(currentSop.id, value)` — the handler must be genuinely WIRED to the action (CLAUDE.md 2026-06-05: an empty `onClick` passes every grep-level test while the feature is dead). On success, re-run the existing `loadVersions()` so the input reflects the persisted value; on error, render `result.error` in the page's existing error slot.
    - Client-side `min`/`max` are UX only; the server-side integer 1..120 validation in `setRefresherInterval` and the DB check constraint are the real gates — do not duplicate business rules beyond the input attributes.
    - Label and helper copy must make D-01 and D-02 unambiguous in plain language: this is how often WORKERS re-walk this procedure, it is separate from the document's own review cycle, and leaving it off means no refresher prompts at all for this SOP. Avoid jargon and internal IDs (the /uat plain-language convention).
    - Disable the Save button only while a save is in flight (the file's existing `uploading`/`cloning` disabled idiom). Never disable it based on competency, refresher or version state.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setRefresherInterval" "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"` >= 2 (import + call inside the handler)
    - The Save handler body contains `await setRefresherInterval(` — asserted by the spec, not by eye (no empty handler)
    - `grep -c "refresher_interval_months" src/actions/versioning.ts` == 6 (the 4 copy-forward positions from 36-03 + the getVersionHistory select + the VersionRecord field)
    - `min={1}` and `max={120}` are present on the input
    - A "turn off"/clear path submits `null`
    - `npx tsc --noEmit` exits 0 and `npm run build` completes
  </acceptance_criteria>
  <done>An admin can set, change and clear the SOP's refresher interval without leaving the version-history page.</done>
</task>

<task type="auto">
  <name>Task 3: Activate the TRN-03 panel assertions live</name>
  <files>tests/phase36/version-breakdown-panel.spec.ts</files>
  <read_first>
    - tests/phase36/version-breakdown-panel.spec.ts (the stub from 36-01 with its `fs.existsSync`-guarded panel assertions, plus the action-side assertions filled in by 36-06)
    - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx (as modified in Tasks 1-2)
  </read_first>
  <action>
    Remove the `fs.existsSync` / `test.skip` guards on the panel-side assertions (the target file now exists and renders the panel) and make them live:
    - the versions page imports and CALLS `getVersionCompletionBreakdown`
    - the versions page imports and CALLS `setRefresherInterval` from `@/actions/governance` inside a handler (assert `await setRefresherInterval(` appears, proving the handler is wired — not just that the identifier is imported)
    - the page renders the per-version completion summary and an `aria-expanded` worker-list control
    - the page contains no `disabled={` expression referencing a competency/refresher/version-currency field
    Keep the action-side assertions from 36-06 unchanged. Use `[\s\S]` not `/s`, and normalize `\r\n` → `\n` before matching multi-line literals (CLAUDE.md 2026-06-02 and 2026-07-18).
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.skip" tests/phase36/version-breakdown-panel.spec.ts` == 0
    - `npx playwright test --project=phase36` green with every assertion in this file running live (confirm the reported test count for this file went up versus the previous run)
    - The wiring assertion is `await setRefresherInterval(`, not a bare import check
  </acceptance_criteria>
  <done>TRN-03 is mechanically guarded end-to-end: action gate, org scope, panel render, and wired control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → `setRefresherInterval` | Untrusted `months`/`sopId` cross here |
| admin browser → `getVersionCompletionBreakdown` | Untrusted `sopId` crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09-01 | Tampering | client-side `min`/`max` on the interval input | mitigate | Server-side integer 1..120 validation in `setRefresherInterval` (36-03) plus the DB check constraint (36-02) are the enforcing gates; the input attributes are UX only |
| T-36-09-02 | Elevation of Privilege | panel/control reachable by supervisors | mitigate | Both the page's existing route guard and `getVersionCompletionBreakdown`'s `['admin','safety_manager']` gate apply; the guard spec asserts the action gate has not been loosened to `RECORDER_ROLES` |
| T-36-09-03 | Repudiation | dead control (empty onClick) | mitigate | Spec asserts `await setRefresherInterval(` inside the handler body (CLAUDE.md 2026-06-05 dead-feature class) |
| T-36-09-04 | Information Disclosure | worker completion history on an admin page | accept | Org-scoped, admin/safety_manager only, same data class as the training matrix; the action re-verifies the SOP belongs to the caller's organisation |
</threat_model>

<verification>
- `npx playwright test --project=phase36` green with no skipped assertions in version-breakdown-panel.spec.ts
- `npx tsc --noEmit` clean; `npm run build` clean
- Manual (post-deploy): set an interval, reload, confirm the value persists; expand a superseded version and confirm the worker list renders
</verification>

<success_criteria>
- Per-version completion counts + expandable worker lists render on the versions page with the current version distinguished
- Refresher interval is settable and clearable from the same page, wired to the server action, reflecting the persisted value
- Existing role boundary unchanged; assertions live
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan adds `VersionRecord.refresher_interval_months` and the versions-page panel/control.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-09-SUMMARY.md` when done
</output>
