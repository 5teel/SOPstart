---
phase: 36-refresher-cadence-version-currency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase36/no-refresher-gate.spec.ts
  - tests/phase36/version-currency-lineage.spec.ts
  - tests/phase36/version-breakdown-panel.spec.ts
  - .planning/phases/36-refresher-cadence-version-currency/36-VALIDATION.md
autonomous: true
requirements: [CMP-03, TRN-03, REF-01, REF-02]

must_haves:
  truths:
    - "A `phase36` Playwright project exists and `npx playwright test --list --project=phase36` lists every spec file under tests/phase36/"
    - "The CMP-04-style gate guard runs LIVE from Wave 0 against the worker-facing files and passes (no refresher/version gate exists yet, and none may be introduced later)"
    - "The CMP-03 lineage runtime probe and TRN-03 panel spec exist as self-activating stubs that turn live in plans 36-09 / 36-10"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase36 project registration (broad tests/phase36/** testMatch)"
      contains: "phase36"
    - path: "tests/phase36/no-refresher-gate.spec.ts"
      provides: "REF-01 / CMP-04 informational-only guard (live from Wave 0)"
      min_lines: 40
    - path: "tests/phase36/version-currency-lineage.spec.ts"
      provides: "CMP-03 orphaning-scenario probe stub (activated in 36-10)"
    - path: "tests/phase36/version-breakdown-panel.spec.ts"
      provides: "TRN-03 panel/action source-contract stub (activated in 36-09)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase36/"
      via: "project testMatch regex"
      pattern: "tests\\\\?/phase36"
---

<objective>
Wave 0 for Phase 36: register the `phase36` Playwright project and land the three spec files that gate this phase, so no later plan can ship a green-that-isn't.

Purpose: CLAUDE.md 2026-05-25 — a spec file not covered by a project `testMatch` regex NEVER runs. Every later plan in this phase drops or activates assertions inside `tests/phase36/`; that directory must be registered exactly once, in Wave 0.
Output: `phase36` project + 3 spec files (one live guard, two self-activating stubs) + a filled Validation map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase36 Playwright project</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (read the `phase34` and `phase35` project blocks at the end of the `projects` array — copy their shape verbatim)
    - .planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md (Pitfall 6)
  </read_first>
  <action>
    Append a `phase36` project block to the `projects` array in playwright.config.ts, immediately after the existing `phase35` / `phase35-unit` blocks. Use `name: 'phase36'`, `testDir: '.'`, `testMatch: /tests\/phase36\/.*\.(spec|test)\.ts$/`, and the same `use: { ...devices['Desktop Chrome'] }` shape the `phase35` block uses.

    Carry the same DELIBERATELY BROAD comment the phase34/phase35 blocks carry: this is the phase's ONE registration point, so plans 36-05/36-09/36-10 can add or activate specs under tests/phase36/ with zero further config edits.

    Do NOT add a `phase36-unit` project. The pure-function unit tests for this phase land in `src/lib/competency/__tests__/`, which the EXISTING `phase35-unit` project (testDir `./src/lib/competency/__tests__`, testMatch `/.*\.test\.ts$/`) already covers — a second project pointed at the same directory would double-run them.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase36` exits 0 and lists all 3 files created in Task 2 (no "no tests found")
    - `grep -c "phase36" playwright.config.ts` returns >= 1 after filtering comment lines: `grep -v '^\s*[/*]' playwright.config.ts | grep -c "phase36"` >= 1
    - `grep -c "phase36-unit" playwright.config.ts` returns 0
    - `npx playwright test --list --project=phase35-unit` still lists classify/csv/matrix tests (no regression to the shared unit project)
  </acceptance_criteria>
  <done>The phase36 project is registered and discovers every tests/phase36/ spec.</done>
</task>

<task type="auto">
  <name>Task 2: Land the three phase-36 spec files (1 live guard + 2 self-activating stubs)</name>
  <files>tests/phase36/no-refresher-gate.spec.ts, tests/phase36/version-currency-lineage.spec.ts, tests/phase36/version-breakdown-panel.spec.ts, .planning/phases/36-refresher-cadence-version-currency/36-VALIDATION.md</files>
  <read_first>
    - tests/phase35/no-competency-gate.spec.ts (the GATE_PATTERN idiom + the fs.existsSync/test.skip green-when-absent pattern — copy both verbatim)
    - tests/phase35/competency-rls-probe.spec.ts (the ephemeral-org + minted-session live-probe harness the CMP-03 lineage probe copies)
    - src/components/profile/CompetencySection.tsx, src/components/sop/SopLibraryCard.tsx, src/app/(protected)/sops/page.tsx (the worker-facing files the gate guard targets)
  </read_first>
  <action>
    Create three specs under `tests/phase36/`.

    (a) `no-refresher-gate.spec.ts` — LIVE from Wave 0 (REF-01, CMP-04 north star, D28-07 precedent). Fork `tests/phase35/no-competency-gate.spec.ts`. Define `GATE_PATTERN` as a regex forbidding any control-flow branch on the new derived fields — cover the identifiers `isOutdatedVersion`, `refresherDueAt`, `isRefresherOverdue`, `isRefresherDue`, `refresher_interval_months` used in a comparison (`[<>=!]`) or an `if (...)` condition. Include the same "GATE_PATTERN self-check" describe block phase35 uses (assert the regex MATCHES a synthetic string like `if (isRefresherOverdue)`) so the regex can never rot into an inert always-passing test. Target these files: `src/components/sop/tabs/ReadTab.tsx`, `src/app/(protected)/sops/[sopId]/page.tsx`, `src/components/profile/CompetencySection.tsx`, `src/components/sop/SopLibraryCard.tsx`, `src/app/(protected)/sops/page.tsx`. All five exist today, so every assertion runs live now and stays live as later plans add chips to them.

    IMPORTANT: rendering a chip is `{isRefresherDue && <span .../>}` — a JSX conditional RENDER, not a gate. Write GATE_PATTERN so that a bare `{x && <` render guard does NOT match, but an `if (x)` branch or a `x ===`/`x !==`/`x >` comparison DOES. State this distinction in the file header comment so a later executor does not "fix" the regex into uselessness.

    (b) `version-currency-lineage.spec.ts` — CMP-03 orphaning probe (RESEARCH Pitfall 1: the single most important scenario in this phase). Structure it as a runtime probe modelled on `tests/phase35/competency-rls-probe.spec.ts`: ephemeral org + minted sessions, worker completes SOP v1, admin supersedes to v2, then assert the worker's v1 completion still surfaces as evidence under v2's read (state is NOT `not_started`) and is flagged `isOutdatedVersion: true`. Land it as `test.fixme` (or guarded by an env-credential check that skips when `SUPABASE_SERVICE_ROLE_KEY` is absent) so Wave 0 stays green; plan 36-10 activates it. Add a file-header comment naming 36-10 as the activating plan.

    (c) `version-breakdown-panel.spec.ts` — TRN-03. Source-contract assertions guarded with `fs.existsSync` + `test.skip` (phase22 green-when-absent precedent) on: `getVersionCompletionBreakdown` exported from `src/actions/competency.ts`, its role gate being the versions-page gate `['admin', 'safety_manager']` (NOT `RECORDER_ROLES` — RESEARCH Open Question 1 resolves to the stricter existing gate), and the panel being rendered by `src/app/(protected)/admin/sops/[sopId]/versions/page.tsx`. The guards self-activate when 36-06 and 36-09 create the symbols.

    Then update `.planning/phases/36-refresher-cadence-version-currency/36-VALIDATION.md`: set frontmatter `wave_0_complete: true` and `nyquist_compliant: true`, and replace the placeholder row in the Per-Task Verification Map with one row per requirement naming the spec + command that covers it (CMP-03 → version-currency-lineage.spec.ts; TRN-03 → version-breakdown-panel.spec.ts; REF-01 → no-refresher-gate.spec.ts; REF-02 → `npx playwright test --project=phase35-unit` for the pure refresher math).
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase36` is green (live guard passes; the two stubs report as skipped/fixme, not failed)
    - `npx playwright test --list --project=phase36` lists all 3 files
    - The GATE_PATTERN self-check test exists and passes: `grep -c "self-check" tests/phase36/no-refresher-gate.spec.ts` >= 1
    - A synthetic JSX render guard does NOT trip the regex — the spec contains an explicit assertion that `"{isRefresherDue && <span"` does NOT match GATE_PATTERN
    - `grep -v '^\s*\*' .planning/phases/36-refresher-cadence-version-currency/36-VALIDATION.md | grep -c "nyquist_compliant: true"` == 1
  </acceptance_criteria>
  <done>Three specs exist, the project runs them, the live guard is green, and VALIDATION.md records the requirement→spec map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Supabase | The CMP-03 lineage probe (when activated in 36-10) mints real sessions against the live project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | `tests/phase36/no-refresher-gate.spec.ts` GATE_PATTERN | mitigate | Mandatory self-check test asserting the regex matches a synthetic gate string — an inert regex is a guard that stopped guarding (CLAUDE.md 2026-07-13) |
| T-36-01-02 | Repudiation | Unregistered spec files | mitigate | Single broad `tests/phase36/**` project registration + `--list` verification in acceptance criteria (CLAUDE.md 2026-05-25) |
| T-36-01-SC | Tampering | npm/pip/cargo installs | mitigate | No packages introduced by this phase (RESEARCH § Package Legitimacy Audit) — nothing to verify |
</threat_model>

<verification>
- `npx playwright test --list --project=phase36` lists 3 files
- `npx playwright test --project=phase36` green
- `npx playwright test --project=phase35 --project=phase35-unit` still green (no regression to the forked-from suites)
</verification>

<success_criteria>
- phase36 project registered once, discovers tests/phase36/**
- no-refresher-gate.spec.ts runs LIVE and passes against 5 worker-facing files
- Two self-activating stubs in place for CMP-03 and TRN-03
- 36-VALIDATION.md `nyquist_compliant: true` with a filled requirement→spec map
</success_criteria>

## Artifacts this phase produces

Symbols created by Phase 36 (excluded from source-drift verification):

| Artifact | Kind | Introduced by |
|----------|------|---------------|
| `phase36` Playwright project | config | 36-01 |
| `tests/phase36/no-refresher-gate.spec.ts` | file | 36-01 |
| `tests/phase36/version-currency-lineage.spec.ts` | file | 36-01 |
| `tests/phase36/version-breakdown-panel.spec.ts` | file | 36-01 |
| `sops.refresher_interval_months` | DB column | 36-02 |
| `supabase/migrations/00055_sops_refresher_interval.sql` | file | 36-02 |
| `src/lib/competency/version-currency.ts` / `isOutdatedVersion` | file / function | 36-02 |
| `src/lib/competency/refresher.ts` / `refresherDueDate`, `isRefresherOverdue` | file / functions | 36-02 |
| `setRefresherInterval` | server action (`src/actions/governance.ts`) | 36-03 |
| `MatrixSop.currentVersion`, `MatrixSop.refresherIntervalMonths`, `BuildMatrixInput.nowIso` | interface fields | 36-04 |
| `MatrixCell.isOutdatedVersion`, `.refresherDueAt`, `.isRefresherOverdue` | interface fields | 36-04 |
| `RowRollup.outdatedCount`, `.refresherOverdueCount`, `ColRollup.outdatedCount`, `.refresherOverdueCount` | interface fields | 36-04 |
| `TrainingCsvRow.onCurrentVersion`, `.refresherDueDate`; CSV headers `on_current_version`, `refresher_due_date` | interface fields / columns | 36-04 |
| `resolveLineage` (module-private helper in `src/actions/competency.ts`) | function | 36-05 |
| `MyCompetencyState.isOutdatedVersion/.refresherDueAt/.isRefresherOverdue`, `RequiredSopRecord.isOutdatedVersion/.refresherDueAt/.isRefresherOverdue` | interface fields | 36-05 |
| `getVersionCompletionBreakdown`, `VersionCompletionBreakdown` | server action / interface | 36-06 |
| `StatePillProps.result.isOutdatedVersion/.refresherDueAt/.isRefresherOverdue` | optional props | 36-07 |
| `SopLibraryCardProps.isRefresherDue`, `.isRefresherOverdue` | optional props | 36-08 |

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-01-SUMMARY.md` when done
</output>
