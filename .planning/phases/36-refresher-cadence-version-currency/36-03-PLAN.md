---
phase: 36-refresher-cadence-version-currency
plan: 03
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - scripts/apply-phase36-migration.mjs
  - src/actions/versioning.ts
  - src/actions/governance.ts
  - tests/phase36/refresher-interval-write.spec.ts
autonomous: true
requirements: [REF-01, REF-02]

must_haves:
  truths:
    - "`sops.refresher_interval_months` exists on the LIVE remote database and PostgREST can see it"
    - "An admin can set or clear a per-SOP refresher interval; a non-admin cannot; an out-of-range value is rejected server-side"
    - "A refresher interval set on v1 survives BOTH supersede paths — `uploadNewVersion` and `cloneSopAsDraft` — and appears on the new version row (RESEARCH Pitfall 2: two copy-forward sites, not one)"
  artifacts:
    - path: "scripts/apply-phase36-migration.mjs"
      provides: "non-interactive db push + PGRST reload + cache-bypassing column assertion"
      contains: "00055_sops_refresher_interval"
    - path: "src/actions/governance.ts"
      provides: "setRefresherInterval admin write action"
      exports: ["setRefresherInterval"]
    - path: "tests/phase36/refresher-interval-write.spec.ts"
      provides: "source-contract guard for the write action + BOTH copy-forward sites"
  key_links:
    - from: "src/actions/versioning.ts"
      to: "sops.refresher_interval_months"
      via: "explicit field in BOTH insert payloads and BOTH preceding select() lists"
      pattern: "refresher_interval_months"
---

<objective>
Apply migration 00055 to the live database, add the admin write action for the per-SOP refresher interval, and close RESEARCH Pitfall 2 by carrying the interval forward through BOTH version-supersede paths.

Purpose: `next build` and `tsc` both pass without the push (types are hand-extended, not generated from the live DB) — so without this [BLOCKING] task the phase reaches verification in a false-positive state. And an interval that silently vanishes the first time an admin clicks "Edit into new version" makes the whole refresher feature look broken.
Output: live column, `setRefresherInterval`, two copy-forward sites, one source-contract guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.planning/phases/36-refresher-cadence-version-currency/36-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration 00055 to the live database and prove the column exists</name>
  <files>scripts/apply-phase36-migration.mjs</files>
  <read_first>
    - scripts/apply-phase34-gap-migration.mjs (the exact non-interactive push + Management-API assertion harness to copy-adapt — including its .env.local loader and its `to_regclass`-style cache-bypassing assertion)
    - supabase/migrations/00055_sops_refresher_interval.sql (created in 36-02)
    - .planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md (Pitfall on PostgREST schema-cache staleness is inherited from CLAUDE.md 2026-06-15)
  </read_first>
  <action>
    Copy-adapt `scripts/apply-phase34-gap-migration.mjs` to `scripts/apply-phase36-migration.mjs`, repointing `MIGRATION_FILE` at `supabase/migrations/00055_sops_refresher_interval.sql`. Keep its .env.local loader, its `npx supabase db push` invocation using `SUPABASE_ACCESS_TOKEN` for non-interactive auth, and its Management-API SQL path (`POST https://api.supabase.com/v1/projects/{ref}/database/query`) for assertions.

    Replace the phase-34 assertions with these three, all executed as raw SQL through the Management API so they bypass the PostgREST schema cache entirely:
    1. Column exists: query `information_schema.columns` for `table_schema='public' AND table_name='sops' AND column_name='refresher_interval_months'` and assert exactly one row with `data_type='integer'` and `is_nullable='YES'`.
    2. Check constraint is live: assert an INSERT/UPDATE of `refresher_interval_months = 0` and `= 121` is REJECTED (run it inside a transaction that rolls back, or probe `pg_constraint` for the named constraint on the `sops` relation) — a constraint that exists in the file but not in the DB is the exact false-positive this task exists to prevent.
    3. Then issue `NOTIFY pgrst, 'reload schema';` through the same Management API call so PostgREST picks the column up immediately instead of within its ~1 minute auto-reload window (CLAUDE.md 2026-06-15: assertions running instantly after push hit the stale window and report PGRST205 "not found in the schema cache" for a table/column that genuinely exists).

    Run the script. If `npx supabase db push` demands an interactive prompt that `SUPABASE_ACCESS_TOKEN` does not suppress (e.g. a DB password prompt), fall back to executing the migration SQL body directly through the same Management API `database/query` endpoint — the phase-25/29/34 scripts already establish that fallback. Record which path was used in the SUMMARY.

    Distinguish error codes in the output: `PGRST205` ("in the schema cache") means stale cache, NOT a missing column; `42P01`/`42703` mean genuinely absent. Do not report a pass on any non-zero exit.
  </action>
  <verify>
    <automated>node scripts/apply-phase36-migration.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/apply-phase36-migration.mjs` exits 0 and prints an explicit PASS line for each of the three assertions
    - The information_schema probe returns `integer` / `is_nullable = YES` for `public.sops.refresher_interval_months`
    - The out-of-range probe (0 and 121) is REJECTED by the database, not accepted
    - The script prints the `NOTIFY pgrst` reload confirmation
  </acceptance_criteria>
  <done>The column and its constraint are live on the remote database and visible to PostgREST.</done>
</task>

<task type="auto">
  <name>Task 2: Carry refresher_interval_months forward through BOTH supersede paths</name>
  <files>src/actions/versioning.ts</files>
  <read_first>
    - src/actions/versioning.ts lines 28-80 (`uploadNewVersion`: the `select('id, version, parent_sop_id, organisation_id, source_file_type')` fetch and the explicit-field-list `insert({...})` payload)
    - src/actions/versioning.ts lines 290-335 (`cloneSopAsDraft`: the `select('id, version, parent_sop_id, organisation_id, title, source_file_name, source_file_type, source_file_path')` fetch and its explicit-field-list `insert({...})` payload)
    - .planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md (Pitfall 2 — both insert sites enumerate fields by name, so a new column is NOT carried automatically)
  </read_first>
  <action>
    Both supersede paths build their new `sops` row from an EXPLICIT field list, never a spread — so a new column silently drops unless added by name in FOUR places (two `select()` lists and two `insert()` payloads).

    1. `uploadNewVersion`: add `refresher_interval_months` to the `oldSop` select list, and `refresher_interval_months: oldSop.refresher_interval_months ?? null` to the `admin.from('sops').insert({...})` payload.
    2. `cloneSopAsDraft`: add `refresher_interval_months` to the `sourceSop` select list, and `refresher_interval_months: sourceSop.refresher_interval_months ?? null` to its `admin.from('sops').insert({...})` payload.

    Add a short comment at each site naming Phase 36 / REF-01 and stating WHY: the refresher interval is the worker's re-walkthrough clock and must survive supersede (D-01), and this file's inserts are explicit field lists so any future per-SOP column must be added here too.

    Change nothing else in either function — the version/lineage math, org-scope assertions, storage paths, parse-job creation and `superseded_by` update all stay byte-identical.

    Note for the record (do not code around it): `cloneSopAsDraft` does not itself set `superseded_by`, and `performPublish` in `src/lib/governance/publish-core.ts` does not either — so version currency must NEVER be derived from `superseded_by`. It is derived from the monotonic `version` integer of the SOP row that the requirement junctions actually point at (handled in 36-04/36-05).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refresher_interval_months" src/actions/versioning.ts` == 4 (two selects + two insert payloads)
    - Both occurrences inside `insert({` payloads read from the previously-fetched source row, not from a literal or a parameter
    - `npx tsc --noEmit` exits 0
    - `npx playwright test --project=phase23 --project=phase35` (or whichever projects cover versioning source contracts) show no new failures; at minimum `npx playwright test --project=phase35-unit` stays green
  </acceptance_criteria>
  <done>An interval set on v1 is present on the row created by either supersede path.</done>
</task>

<task type="auto">
  <name>Task 3: setRefresherInterval admin write action + source-contract guard</name>
  <files>src/actions/governance.ts, tests/phase36/refresher-interval-write.spec.ts</files>
  <read_first>
    - src/actions/governance.ts (the `setSopOwner` function ~lines 104-147: `requireAdmin()` → validation → PLAIN session client `createClient()` → `.update(...).eq('id', sopId).select('id')` → `updated.length === 0` means RLS filtered, return 'SOP not found'; and `setReviewCadence` ~lines 214-247 for the integer 1..120 validation wording)
    - src/actions/versioning.ts (modified in Task 2 — confirms the column name spelling)
    - tests/phase36/no-refresher-gate.spec.ts (created in 36-01 — the file-header/registration convention for specs in this directory)
  </read_first>
  <action>
    Add `setRefresherInterval(sopId: string, months: number | null): Promise<{ success: true } | { error: string }>` to `src/actions/governance.ts`, mirroring `setSopOwner` exactly:
    - reject empty `sopId`
    - `const ctx = await requireAdmin(); if ('error' in ctx) return { error: ctx.error }`
    - validate `months`: accept `null` (D-02 — clearing the interval turns the refresher OFF for that SOP, producing no due-date, no chip, no rollup contribution), otherwise require `Number.isInteger(months) && months >= 1 && months <= 120`, returning the message `'months must be an integer between 1 and 120'` (identical wording to `setReviewCadence`)
    - write with the PLAIN session client `await createClient()` — NOT `createAdminClient()`. `sops` has the `admins_can_update_sops` policy (org + admin/safety_manager), so RLS is the real gate here; using the service-role client would trade an RLS gate for a cross-tenant write hole (CLAUDE.md 2026-06-15 recurring class). Update `{ refresher_interval_months: months, updated_at: new Date().toISOString() }` `.eq('id', sopId).select('id')`.
    - on error: `console.error('[setRefresherInterval] update error', error)` then return `{ error: error.message }`
    - on zero returned rows: return `{ error: 'SOP not found' }` — 0 rows means RLS filtered the row (wrong org or insufficient role), which must never read as silent success (LR-01 precedent in `setSopOwner`).
    Add the function to the module's export list in the file-header doc comment, matching how `setSopOwner`/`setReviewCadence`/`requireAdmin` are documented there.

    Then create `tests/phase36/refresher-interval-write.spec.ts` — source-contract assertions over the two files this plan touched:
    - `src/actions/governance.ts` contains `export async function setRefresherInterval`, calls `requireAdmin(`, references `'months must be an integer between 1 and 120'`, uses `createClient(` and NOT `createAdminClient(` inside the function body, and contains the zero-rows `'SOP not found'` branch.
    - `src/actions/versioning.ts` contains `refresher_interval_months` at least 4 times, and specifically inside BOTH insert payloads (assert the substring appears within the body slices of `uploadNewVersion` and `cloneSopAsDraft` separately, not merely somewhere in the file — Pitfall 2 is precisely "you added one of two sites").
    Use `[\s\S]` rather than the regex `/s` flag (CLAUDE.md 2026-06-02 TS-target learning), and strip `\r` before matching multi-line literals (CLAUDE.md 2026-07-18 worktree CRLF learning).
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase36` green, including the new refresher-interval-write spec
    - The spec asserts the copy-forward inside BOTH function bodies separately (two distinct assertions, not one file-level grep)
    - `grep -c "createAdminClient" <setRefresherInterval body>` == 0 — verified by the spec, not by eye
    - `npx tsc --noEmit` exits 0 and `npm run build` completes (a sync export added to a `'use server'` file breaks `next build` — CLAUDE.md 2026-06-27; `setRefresherInterval` must be `async`)
  </acceptance_criteria>
  <done>Admins can set/clear a per-SOP refresher interval; the guard proves both copy-forward sites and the session-client posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `setRefresherInterval` server action | Untrusted `sopId` and `months` cross here |
| CI/dev machine → live Supabase | The migration script mutates production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Elevation of Privilege | `setRefresherInterval` | mitigate | `requireAdmin()` gate BEFORE any DB touch, plus the PLAIN session client so `admins_can_update_sops` RLS (org + admin/safety_manager) is the enforcing backstop — never `createAdminClient()` (CLAUDE.md 2026-06-15/26 recurring service-role-write class) |
| T-36-03-02 | Tampering | client-supplied `months` | mitigate | Server-side integer 1..120 validation before the write, backed by the DB check constraint from 36-02 — client `<input min/max>` is UX only |
| T-36-03-03 | Spoofing | client-supplied `sopId` targeting another org | mitigate | No org parameter is accepted; the session client's RLS scopes the UPDATE, and 0 returned rows is surfaced as `'SOP not found'` rather than silent success |
| T-36-03-04 | Repudiation | false-positive verification from an unpushed migration | mitigate | [BLOCKING] Task 1 asserts the column AND its constraint through cache-bypassing raw SQL before any downstream plan reads the column |
| T-36-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages introduced by this phase (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `node scripts/apply-phase36-migration.mjs` → all assertions PASS
- `npx tsc --noEmit` clean; `npm run build` clean
- `npx playwright test --project=phase36` green
</verification>

<success_criteria>
- Live DB carries `sops.refresher_interval_months` (integer, nullable, 1..120 constrained) and PostgREST sees it
- `setRefresherInterval` exists, is async, admin-gated, session-client-written, range-validated
- `refresher_interval_months` appears in all four copy-forward positions in versioning.ts
- Source-contract spec proves both copy-forward sites independently
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan introduces `scripts/apply-phase36-migration.mjs`, `setRefresherInterval` (in `src/actions/governance.ts`), and `tests/phase36/refresher-interval-write.spec.ts`.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-03-SUMMARY.md` when done
</output>
