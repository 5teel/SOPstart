---
phase: 36-refresher-cadence-version-currency
plan: 10
type: execute
wave: 6
depends_on: ["36-07", "36-08", "36-09"]
files_modified:
  - tests/phase36/no-refresher-gate.spec.ts
  - tests/phase36/version-currency-lineage.spec.ts
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: true
requirements: [CMP-03, TRN-03, REF-01, REF-02]

must_haves:
  truths:
    - "A worker who completed v1 of a superseded SOP still shows their earned competency state — proven against the LIVE database, not just by source inspection (CMP-03, RESEARCH Pitfall 1)"
    - "The informational-only guard covers every surface this phase touched and fails if any of them ever gains a gating branch (CMP-04 / REF-01 success criterion 4)"
    - "`/pathways` shows 0 not-mapped screens for this phase's surfaces, and `/uat` carries plain-language tests for the new behaviour"
    - "Full suite, tsc, next build and the bundle gate are all green before the phase is declared done"
  artifacts:
    - path: "tests/phase36/version-currency-lineage.spec.ts"
      provides: "LIVE supersede/orphaning runtime proof for CMP-03"
    - path: "tests/phase36/no-refresher-gate.spec.ts"
      provides: "extended informational-only guard across all Phase 36 surfaces"
    - path: "src/lib/journeys/journeys.ts"
      provides: "pathways map updated for the version-breakdown panel + refresher chips"
    - path: "src/lib/uat/tests.ts"
      provides: "plain-language UAT tests for version-currency and refresher cadence"
  key_links:
    - from: "tests/phase36/version-currency-lineage.spec.ts"
      to: "live Supabase (ephemeral org)"
      via: "minted sessions + real supersede, mirroring tests/phase35/competency-rls-probe.spec.ts"
      pattern: "createClient"
---

<objective>
Close the phase: activate the CMP-03 runtime proof against the live database, widen the informational-only guard to every surface this phase touched, update the two living source-of-truth maps, and run the full gate.

Purpose: this codebase's most expensive recurring failure is a phase that ships all-green on source-contract tests while the actual behaviour is broken or dangerous (2026-06-05, 2026-06-08, 2026-06-24, 2026-07-20). The orphaning scenario is exactly the kind of bug a grep-level test cannot see, so it gets a real runtime probe. And `journeys.ts` / `uat/tests.ts` are project-mandated same-change artifacts.
Output: live probe, extended guard, updated maps, green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.planning/phases/36-refresher-cadence-version-currency/36-07-SUMMARY.md
@.planning/phases/36-refresher-cadence-version-currency/36-08-SUMMARY.md
@.planning/phases/36-refresher-cadence-version-currency/36-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate the CMP-03 supersede/orphaning runtime probe</name>
  <files>tests/phase36/version-currency-lineage.spec.ts</files>
  <read_first>
    - tests/phase36/version-currency-lineage.spec.ts (the Wave-0 stub from 36-01)
    - tests/phase35/competency-rls-probe.spec.ts (the live harness to copy: ephemeral orgs, service-role setup, minted sessions, teardown)
    - scripts/uat-seed-competency.mjs and scripts/uat-seed-cleanup.mjs (existing seed/teardown helpers for competency fixtures)
    - src/actions/competency.ts (as widened in 36-05 — the behaviour under test)
  </read_first>
  <action>
    Turn the stub into a live runtime probe. Scenario (RESEARCH Pitfall 1, the single most important case in this phase):
    1. Create an ephemeral organisation with a department, one worker member and one admin member.
    2. Create and publish SOP v1, make it required for the worker (via the department junction the matrix actually reads — `sop_departments`, never re-derived from `access_grants`, MTX-02).
    3. Insert a `sop_completions` row for the worker against v1.
    4. Supersede: create the v2 row with `parent_sop_id` = v1's lineage root and `version = 2`, publish it, and materialize its requirement junctions the way the real publish path does.
    5. Assert, reading as the admin/supervisor: the worker's cell for the CURRENT SOP is NOT `not_started` (their v1 evidence survived the supersede — CMP-03) AND the cell carries `isOutdatedVersion: true`.
    6. Assert the negative control: a worker whose completion IS against v2 has `isOutdatedVersion: false`.
    7. Assert refresher lineage behaviour (RESEARCH Assumption A3): with an interval set, a worker whose only completion is a RECENT v1 completion is NOT instantly refresher-overdue the moment v2 publishes.
    8. Tear the ephemeral org down in an `afterAll`, whether or not assertions passed.

    Gate the probe on credentials being present (`NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` from `.env.local`) with an explicit `test.skip(!creds, 'live credentials absent')` — but RUN IT in this task and record the live PASS output in the SUMMARY. A probe that is written and never executed is the Phase 34 failure mode (an org-wide disclosure hole shipped because a probe existed for one role only).

    If the probe FAILS, that is a real Phase 36 bug in the lineage widening — fix `src/actions/competency.ts`, do not weaken the assertion.
  </action>
  <verify>
    <automated>npx playwright test tests/phase36/version-currency-lineage.spec.ts --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - The probe RUNS (not skipped) in this task's execution and reports PASS for all four assertions in steps 5-7
    - `grep -c "not_started" tests/phase36/version-currency-lineage.spec.ts` >= 1 (the orphaning assertion is explicit)
    - The spec contains both a positive (outdated true) and a negative (outdated false) case
    - Teardown runs in an `afterAll` and the ephemeral org is gone afterwards (verify with a follow-up query or the cleanup script's own output)
  </acceptance_criteria>
  <done>CMP-03 is proven against the live database, in both the positive and negative direction.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the informational-only guard to every Phase 36 surface</name>
  <files>tests/phase36/no-refresher-gate.spec.ts</files>
  <read_first>
    - tests/phase36/no-refresher-gate.spec.ts (from 36-01 — GATE_PATTERN, the self-check block, the five target files)
    - The files this phase actually changed: src/components/admin/competency/StatePill.tsx, src/components/admin/competency/TrainingMatrixView.tsx, src/components/admin/competency/TrainingRecordSection.tsx, src/components/profile/CompetencySection.tsx, src/components/sop/SopLibraryCard.tsx, src/app/(protected)/sops/page.tsx, src/app/(protected)/admin/sops/[sopId]/versions/page.tsx
  </read_first>
  <action>
    Widen the guard's target list to every file this phase touched that can render to a worker or influence a worker path, adding at minimum: `StatePill.tsx`, `TrainingRecordSection.tsx`, and `TrainingMatrixView.tsx` (a supervisor surface, but the same north star applies — nothing here may disable or lock a worker path).

    Add a second, stricter assertion class alongside GATE_PATTERN: for the chip-rendering files, assert there is NO `disabled=` expression and NO `onClick` handler attached to any element whose surrounding text includes the new chip labels — i.e. the chips are passive. Keep this precise enough not to trip on the matrix's legitimate pre-existing cell button and the page's legitimate pre-existing controls; scope the assertion to the chip elements (e.g. by slicing the file around `Outdated version` / `Refresher` / `data-refresher-due-badge`).

    Re-confirm the JSX-render-guard exemption still holds after widening: the spec must contain the explicit assertion that `"{isRefresherDue && <span"` does NOT match GATE_PATTERN, and that `"if (isRefresherOverdue)"` DOES. Without both halves the regex can rot in either direction.

    Do not weaken any existing assertion. If a real gating branch was introduced by 36-07/36-08/36-09, fix the component, not the guard.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - The guard targets >= 8 files, including all three competency components and both worker library files
    - Both halves of the self-check (positive match on `if (...)`, negative match on `{x && <span`) are present and passing
    - `npx playwright test --project=phase36` green
  </acceptance_criteria>
  <done>Any future gating branch on version-currency or refresher state fails CI.</done>
</task>

<task type="auto">
  <name>Task 3: Update journeys.ts + uat/tests.ts, then run the full phase gate</name>
  <files>src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts (the version-management journey around the `/admin/sops/[sopId]/versions` screen entry, and the profile/library screen entries the chips now appear on)
    - src/lib/uat/tests.ts (the `p35-training-matrix` / `p35-cell-click-record` / `p35-export-csv` entries — the plain-language format and id convention to mirror)
    - CLAUDE.md § Pathways Map Maintenance (the same-change rule) and the /uat plain-language convention
    - .planning/phases/36-refresher-cadence-version-currency/36-VALIDATION.md (Manual-Only Verifications row)
  </read_first>
  <action>
    Phase 36 adds NO new routes, so no new `screen` nodes are required — but three existing screens change what they do, and `journeys.ts` is the living source of truth for that:
    - `/admin/sops/[sopId]/versions` — extend its `detail` to mention the per-version completion breakdown (who completed the current version vs a prior one) and the per-SOP refresher interval control.
    - the worker profile screen carrying "Your training" — mention the outdated-version and refresher chips.
    - the worker SOP library screen — mention the refresher-due chip on Your SOPs cards.
    Keep every `route` value real so `/pathways` coverage and deep-links keep working.

    Then add UAT entries to `src/lib/uat/tests.ts` mirroring the phase-35 entries' shape and plain language (click-paths and yes/no questions, no jargon, no internal IDs):
    - `p36-outdated-version` — supersede a SOP, then look at the training matrix / a worker's record: "Could you tell at a glance who is still trained on the older version?" and "Did their existing training record stay intact (nothing was reset or removed)?"
    - `p36-refresher-cadence` — set a refresher interval on a SOP, then look at the worker's SOP list and the matrix: "Was it clear how often workers need to re-walk this procedure?", "Did the reminder appear where you'd expect?", "Did it ever stop anyone from opening the SOP?"
    - `p36-version-breakdown` — on the version history page: "Could you see how many people completed each version?", "Was the current version obvious?"

    Then run the FULL phase gate and record every result in the SUMMARY:
    1. `npx tsc --noEmit`
    2. `npm run build` (a `next build` is mandatory — it enforces constraints `tsc` alone does not, CLAUDE.md 2026-06-27)
    3. `npm run test` (full suite; note any pre-existing failures in older phase stubs separately from Phase 36 results)
    4. the bundle-size check
    5. `/pathways` "All screens" coverage: confirm 0 not-mapped for this phase's surfaces (grep `journeys.ts` for each touched route if the page cannot be opened headlessly)
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refresher" src/lib/journeys/journeys.ts` >= 1 and the versions-screen `detail` mentions the completion breakdown
    - `grep -c "p36-" src/lib/uat/tests.ts` >= 3
    - `npx tsc --noEmit` exits 0
    - `npm run build` completes successfully
    - `npm run test` shows all phase36 / phase35 / phase35-unit projects green, with any unrelated pre-existing failures explicitly enumerated in the SUMMARY
    - Bundle gate passes or is explicitly re-baselined with before/after KB recorded
  </acceptance_criteria>
  <done>The living maps reflect the phase, and every automated gate is green and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Supabase | The runtime probe creates and destroys a real ephemeral organisation with real sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-01 | Repudiation | source-contract-only verification hiding a real orphaning bug | mitigate | The CMP-03 probe is a LIVE runtime scenario with positive AND negative controls, executed in-task with its PASS output recorded (the Phase 34 one-probe-is-not-coverage lesson) |
| T-36-10-02 | Tampering | leftover probe fixtures polluting the live database | mitigate | `afterAll` teardown of the ephemeral organisation, verified after the run; cleanup script referenced in the SUMMARY if any residue remains |
| T-36-10-03 | Elevation of Privilege | a future gating branch on refresher/version state | mitigate | Extended guard across >= 8 files with a two-sided regex self-check, running in the registered `phase36` project |
| T-36-10-04 | Information Disclosure | probe credentials in the repo | mitigate | Credentials read from `.env.local` at runtime only; the spec skips explicitly when absent and never inlines a key |
</threat_model>

<verification>
- `npx playwright test --project=phase36` green, with the lineage probe RUN (not skipped) at least once and its output recorded
- `npx tsc --noEmit` clean; `npm run build` clean; `npm run test` green for phase36/phase35/phase35-unit
- `/pathways` 0 not-mapped for touched surfaces
</verification>

<success_criteria>
- CMP-03 orphaning scenario proven live, both directions
- Informational-only guard extended to every Phase 36 surface with a two-sided self-check
- journeys.ts and uat/tests.ts updated in the same change as the surfaces they describe
- Full gate green and recorded, including `next build` and the bundle check
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan produces no new source symbols — it activates guards and updates the two living maps.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-10-SUMMARY.md` when done
</output>
