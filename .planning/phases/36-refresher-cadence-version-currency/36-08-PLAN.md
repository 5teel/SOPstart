---
phase: 36-refresher-cadence-version-currency
plan: 08
type: execute
wave: 5
depends_on: ["36-06"]
files_modified:
  - src/components/sop/SopLibraryCard.tsx
  - src/app/(protected)/sops/page.tsx
  - tests/phase36/worker-library-chip.spec.ts
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "A worker sees a 'Refresher due' chip on that SOP's card in Your SOPs, where they would act on it (D-08)"
    - "A SOP with no refresher interval shows no chip at all — zero noise from day one (D-02)"
    - "The chip never gates, disables, hides, reorders or blocks the card's link to the SOP (CMP-04)"
    - "The worker route's First Load JS stays inside the existing bundle gate"
  artifacts:
    - path: "src/components/sop/SopLibraryCard.tsx"
      provides: "informational refresher-due badge beside the existing Updated badge"
      contains: "data-refresher-due-badge"
    - path: "src/app/(protected)/sops/page.tsx"
      provides: "client-side refresher due computation from last completion + per-SOP interval"
      contains: "refresherDueDate"
  key_links:
    - from: "src/app/(protected)/sops/page.tsx"
      to: "src/lib/competency/refresher.ts"
      via: "refresherDueDate + isRefresherOverdue over the existing lastCompletionMap"
      pattern: "refresherDueDate"
---

<objective>
Give workers the one refresher signal they need, in the one place they would act on it: a chip on the SOP's card in Your SOPs.

Purpose: D-08 puts the worker-facing refresher signal on the profile "Your training" rows (which 36-07 already delivers through `StatePill`) AND on the worker's library card. This plan does the card. No dashboard nudges, no notifications, no forced re-walk — the chip is a sibling of the existing "Updated" badge, which is itself an informational-only precedent from Phase 23 (D-09: "badge is informational only, no onClick re-walk").
Output: an optional badge prop, its parent-page computation, and a guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-refresher-cadence-version-currency/36-CONTEXT.md
@.planning/phases/36-refresher-cadence-version-currency/36-RESEARCH.md
@.planning/phases/36-refresher-cadence-version-currency/36-PATTERNS.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SopLibraryCard — refresher-due badge</name>
  <files>src/components/sop/SopLibraryCard.tsx</files>
  <read_first>
    - src/components/sop/SopLibraryCard.tsx (whole file — the `hasNewerVersion?: boolean` prop with its "informational only — no forced re-walk" doc block, and the `data-updated-badge` span it renders inside the badge row)
    - src/styles/blueprint-theme.css (confirm `--accent-decision` is declared before referencing it)
  </read_first>
  <action>
    Add two optional props mirroring the existing `hasNewerVersion` shape: `isRefresherDue?: boolean` and `isRefresherOverdue?: boolean`, both defaulting to `false`, both computed by the parent page. Document them with the same framing the `hasNewerVersion` block uses: Phase 36 REF-01 / D-08, informational only, no forced re-walk, no gating (CMP-04).

    In the existing badge row (the `flex items-center gap-2 mt-2 flex-wrap` div, after the `hasNewerVersion` badge), render `{isRefresherDue && <span data-refresher-due-badge="true" ...>}` with the label `Refresher overdue` when `isRefresherOverdue` is true and `Refresher due` otherwise. Style it to match the sibling badges: `inline-flex items-center gap-1 px-2 py-0.5 text-xs font-semibold rounded` with `bg-[var(--accent-decision)]/10 text-[var(--accent-decision)]` — amber, coaching-toned, never red (CONTEXT § Specifics). Add a `title` explaining it in worker language, e.g. `"Time for a refresher walkthrough of this SOP"`.

    Change nothing else: the card stays a single `<Link href={/sops/${sop.id}}>`, is never disabled, never conditionally hidden, and the chip adds no `onClick`. Use only the `{flag && <span>}` JSX render guard — no `if` branch, no comparison on the new props (`tests/phase36/no-refresher-gate.spec.ts` targets this file).
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-refresher-due-badge" src/components/sop/SopLibraryCard.tsx` == 1
    - `grep -c "disabled" src/components/sop/SopLibraryCard.tsx` == 0
    - `--accent-decision` is confirmed declared in `src/styles/blueprint-theme.css`
    - `npx playwright test --project=phase36` green (no-refresher-gate guard passes on this file)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The card can render an informational refresher badge; no behaviour changes.</done>
</task>

<task type="auto">
  <name>Task 2: Compute the worker's refresher state in the library page + guard + bundle check</name>
  <files>src/app/(protected)/sops/page.tsx, tests/phase36/worker-library-chip.spec.ts</files>
  <read_first>
    - src/app/(protected)/sops/page.tsx (the `YourSopsSection` component: its existing `lastCompletionMap` useQuery over `sop_completions` — already RLS-scoped to the current user — and the `hasNewerVersion(sop)` helper + its `<SopLibraryCard ... hasNewerVersion={...} />` call site)
    - src/lib/competency/refresher.ts (36-02: `refresherDueDate`, `isRefresherOverdue` — pure, client-importable)
    - scripts/check-bundle-size.ts (the standing worker-bundle gate and how to run it)
  </read_first>
  <action>
    In `YourSopsSection`, add one small TanStack query alongside the existing `lastCompletionMap` query: select `id, refresher_interval_months` from `sops` via the browser Supabase client (RLS scopes it to SOPs the worker can see — `org_members_can_view_sops`), reduced to `Record<sopId, number | null>`. Use the same `staleTime` order of magnitude as the neighbouring queries. Do NOT call a server action here and do NOT fetch competency state — the library card needs one boolean, not a classifier result.

    Add a `refresherState(sop)` helper next to the existing `hasNewerVersion(sop)` helper that returns `{ isRefresherDue: boolean; isRefresherOverdue: boolean }`:
    - `const due = refresherDueDate(lastCompletionMap[sop.id] ?? null, intervalMap[sop.id] ?? null)`
    - `isRefresherDue = due !== null && new Date().toISOString() >= due` (a due-or-overdue chip; per CONTEXT the exact "due soon" window is Claude's discretion — keep it simple: the chip appears at/after the due date, no amber-before-due window, which keeps zero noise as the default)
    - `isRefresherOverdue = isRefresherOverdue(due, new Date().toISOString())`
    Pass both into `<SopLibraryCard ... />`. Compute `now` ONCE per render pass, not per card. Never read `navigator`/`Date` at module load — this file is a client component and the values are used inside render, which is fine, but do not hoist a `const NOW = new Date()` to module scope (CLAUDE.md 2026-06-08 hydration-mismatch class: derive nothing stable-looking from a moving value at module load).

    Note the interval map covers only SOPs the worker can read; a missing entry yields `null` → no chip, which is the correct D-02 default.

    Then create `tests/phase36/worker-library-chip.spec.ts` asserting: `SopLibraryCard.tsx` declares `isRefresherDue`/`isRefresherOverdue` optional props and renders `data-refresher-due-badge`; `sops/page.tsx` imports `refresherDueDate` from `@/lib/competency/refresher` and passes both props at the `<SopLibraryCard` call site (assert the prop names appear in the same JSX element, not merely somewhere in the file — the CLAUDE.md 2026-06-05 dead-feature class); and neither file contains a gating branch on the new fields.

    Finally run the bundle gate. `refresher.ts` + `cadences.ts` are a few lines of pure date math, so the delta should be ~0 KB; if the gate reports a delta outside tolerance, re-baseline via the existing capture script and record the before/after numbers in the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test --project=phase36</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refresherDueDate" "src/app/(protected)/sops/page.tsx"` >= 1
    - The `<SopLibraryCard` element in `YourSopsSection` passes both `isRefresherDue` and `isRefresherOverdue`
    - `npx playwright test --project=phase36` green including `worker-library-chip.spec.ts`
    - `npx tsc --noEmit` exits 0 and `npm run build` completes
    - `npx tsx scripts/check-bundle-size.ts` (or the project's documented invocation) passes, or the SUMMARY records an explicit re-baseline with before/after KB
  </acceptance_criteria>
  <done>Workers see a refresher chip exactly where they would act, computed from data the page already had, with the bundle gate flat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker browser → `sops` / `sop_completions` via the browser Supabase client | RLS is the only gate; both reads are self/org-scoped by existing policies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08-01 | Information Disclosure | new client-side `sops` interval query | accept | `org_members_can_view_sops` already scopes `sops` SELECT to the caller's organisation, and the query returns only an id and an admin-set integer — no PII, no cross-tenant surface |
| T-36-08-02 | Elevation of Privilege | refresher chip becoming an access gate | mitigate | JSX render guards only; `tests/phase36/no-refresher-gate.spec.ts` covers both `SopLibraryCard.tsx` and `sops/page.tsx`, and the card remains an unconditional `<Link>` |
| T-36-08-03 | Denial of Service | extra worker-route query on every library render | accept | One indexed `select('id, refresher_interval_months')` with TanStack staleTime caching, alongside two queries the page already issues |
</threat_model>

<verification>
- `npx playwright test --project=phase36` green
- `npx tsc --noEmit` clean; `npm run build` clean
- Bundle gate passes or is explicitly re-baselined
- Manual (post-deploy): chip visible on a phone-width viewport on sopstart.com
</verification>

<success_criteria>
- Refresher chip renders on Your SOPs cards for SOPs with an interval and a past-due last completion
- No chip at all when the interval is unset
- Card link, ordering and enablement unchanged
- Worker bundle gate flat (or re-baselined with recorded numbers)
</success_criteria>

## Artifacts this phase produces

See `36-01-PLAN.md` § "Artifacts this phase produces". This plan introduces `SopLibraryCardProps.isRefresherDue` / `.isRefresherOverdue` and `tests/phase36/worker-library-chip.spec.ts`.

<output>
Create `.planning/phases/36-refresher-cadence-version-currency/36-08-SUMMARY.md` when done
</output>
