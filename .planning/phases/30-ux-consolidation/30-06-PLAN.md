---
phase: 30-ux-consolidation
plan: 06
type: execute
wave: 6
depends_on: [30-01]
files_modified:
  - src/components/sop/SopTabNav.tsx
  - src/components/sop/tabs/ReadTab.tsx
  - src/components/sop/tabs/index.ts
  - src/components/sop/tabs/ModelTab.tsx
  - src/components/sop/tabs/WalkthroughTab.tsx
  - src/app/(protected)/sops/[sopId]/page.tsx
  - src/app/(protected)/sops/[sopId]/walkthrough/page.tsx
  - src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx
  - src/app/(protected)/sops/page.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase28/library-and-worker.spec.ts
  - tests/phase30/tab-merge.spec.ts
  - tests/phase30/dead-weight.spec.ts
  - .bundle-baseline.json
autonomous: true
requirements: [UX-05, UX-08, UX-04]
must_haves:
  truths:
    - "Worker SOP detail has exactly 3 tabs: Read · Walk it · Flow (SOP_TABS === ['read','walk','flow'])"
    - "The merged Read tab renders PPE once and equipment once (single isPpeSection usage); it keeps the phase28-guarded 'Current as of' caption and never gates on review_due_at/owner (D28-07)"
    - "Legacy ?tab= params still land: overview|tools|hazards|model → read, walkthrough → walk"
    - "ModelTab, WalkthroughTab, and the /sops/[sopId]/walkthrough route+layout are deleted; the worker 'Create SOP' tab is gone; the worker dept filter is fixed or removed (no placebo)"
    - "Worker /sops/[sopId] First Load JS is ≤ baseline + 2 KB (merge should reduce it); DesktopWalkthrough + WalkthroughVoiceModal stay as separate dynamic chunks"
  artifacts:
    - path: "src/components/sop/tabs/ReadTab.tsx"
      provides: "Merged Overview+Tools+Hazards brief, PPE/equipment rendered once"
    - path: "src/components/sop/SopTabNav.tsx"
      provides: "3-tab registry + legacy-param mapping choke point"
      exports: ["SOP_TABS", "SopTabId", "isSopTabId", "useActiveTab", "SopTabNav"]
  key_links:
    - from: "src/components/sop/SopTabNav.tsx useActiveTab"
      to: "read|walk|flow"
      via: "legacy param map applied before the guard"
      pattern: "overview|tools|hazards|model|walkthrough"
    - from: "src/app/(protected)/sops/[sopId]/page.tsx"
      to: "ReadTab | WalkthroughSwitcher | FlowTab"
      via: "render switch on the 3 tabs; walkthrough mounts only via WalkthroughSwitcher (bundle trap)"
      pattern: "ReadTab|WalkthroughSwitcher|FlowTab"
---

<objective>
UX-05 worker 6 tabs → 3 (Read · Walk it · Flow) with PPE+equipment rendered once, plus the coupled UX-08 worker deletions (ModelTab, WalkthroughTab shim, /sops/[sopId]/walkthrough route+layout, no-op dept filter) and the UX-04 worker "Create SOP" tab removal — all on the same worker surface. Legacy `?tab=` params map to the new tabs so QR/bookmarks still land. Re-baseline the worker bundle after the intentional chunk shift.

Purpose: The worker path is the product's heart — three clear tabs, no dead Model tab, no placebo filter, no orphan walkthrough route, and no worker-side create affordance (admins reach create via admin nav).
Output: 3-tab worker detail, merged Read tab, deletions done, bundle re-baselined, phase28 worker spec repointed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
@src/components/sop/SopTabNav.tsx
@src/components/sop/tabs/index.ts
</context>

<artifacts_produced>
- `SopTabNav.tsx`: `SOP_TABS = ['read','walk','flow']`; `TAB_DEFS` = Read / Walk it / Flow; legacy-param mapping inside `useActiveTab`/`isSopTabId` (overview|tools|hazards|model→read, walkthrough→walk) — old params accepted forever.
- `ReadTab.tsx` (NEW): merged Overview+Tools+Hazards; ONE `isPpeSection`; PPE + equipment rendered once; keeps the "Current as of" caption; no review/owner gate.
- DELETED: `ModelTab.tsx`, `WalkthroughTab.tsx`, `/sops/[sopId]/walkthrough/{page,layout}.tsx`; their exports removed from `tabs/index.ts`.
- `sops/[sopId]/page.tsx` render switch → Read/Walk/Flow (walkthrough via `WalkthroughSwitcher` only).
- `sops/page.tsx`: worker "Create SOP" tab removed; dept filter fixed (client `sop_departments` fetch+filter) OR removed — executor verifies the SELECT policy first (decision #3).
- Regenerated `.bundle-baseline.json` (via `scripts/capture-bundle-baseline.ts`, never hand-edited); repointed `tests/phase28/library-and-worker.spec.ts` (OverviewTab→ReadTab; drop the deleted-walkthrough no-gate assertion).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: SopTabNav 6→3 + legacy param map + merged ReadTab</name>
  <files>src/components/sop/SopTabNav.tsx, src/components/sop/tabs/ReadTab.tsx</files>
  <read_first>
    - src/components/sop/SopTabNav.tsx (current SOP_TABS/TAB_DEFS/isSopTabId/useActiveTab/handleChange — the choke point to modify)
    - 30-PATTERNS.md § "src/components/sop/tabs/ReadTab.tsx" (the verbatim `isPpeSection` copy-pasted in ToolsTab:9-13 + HazardsTab:17-21 — collapse to ONE) + § "Tab registry"
    - src/components/sop/tabs/OverviewTab.tsx, ToolsTab.tsx, HazardsTab.tsx (render sections to concatenate; OverviewTab "Current as of" caption line ~55; equipment renders in Overview AND Tools — render once)
    - 30-RESEARCH.md § "Current Wiring 5" (tab sizes, PPE duplication, merged Read must keep caption + D28-07 no-gate) + § "Bundle Gate Mechanics" (Read must NOT statically import components/sop/walkthrough/*)
  </read_first>
  <action>
    Shrink `SOP_TABS` to `['read','walk','flow']` and `TAB_DEFS` to Read / Walk it / Flow. Add a legacy map applied BEFORE `isSopTabId` in `useActiveTab` (and normalise in `SopTabNav`): `overview|tools|hazards|model → read`, `walkthrough → walk`; keep the existing `router.push('?tab=…', { scroll:false })` handler. Build `ReadTab.tsx` by concatenating Overview + Tools + Hazards render sections with ONE `isPpeSection` and equipment rendered once; keep the "Current as of" caption; add NO review_due_at/owner gate. Do NOT statically import anything from `components/sop/walkthrough/` (bundle trap).
  </action>
  <acceptance_criteria>
    - `grep -n "SOP_TABS = " src/components/sop/SopTabNav.tsx` shows exactly `['read', 'walk', 'flow']`.
    - `useActiveTab`/`isSopTabId` map all 6 legacy values (unit-style assertion in tab-merge spec covers overview/tools/hazards/model→read, walkthrough→walk).
    - `grep -c "function isPpeSection" src/components/sop/tabs/ReadTab.tsx` == 1; ReadTab contains the "Current as of" caption; `grep -n "review_due_at\|review_due\|owner" src/components/sop/tabs/ReadTab.tsx` finds no worker-facing gate.
    - `grep -rn "components/sop/walkthrough" src/components/sop/tabs/ReadTab.tsx` returns 0 (no static walkthrough import).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>3-tab registry with legacy mapping; ReadTab merges the three briefs with PPE/equipment once and the caption preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Model/Walkthrough tabs + walkthrough route, rewire render switch, worker create tab + dept filter</name>
  <files>src/components/sop/tabs/index.ts, src/components/sop/tabs/ModelTab.tsx, src/components/sop/tabs/WalkthroughTab.tsx, src/app/(protected)/sops/[sopId]/page.tsx, src/app/(protected)/sops/[sopId]/walkthrough/page.tsx, src/app/(protected)/sops/[sopId]/walkthrough/layout.tsx, src/app/(protected)/sops/page.tsx, tests/phase28/library-and-worker.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 7" (ModelTab/WalkthroughTab importers = tabs/index.ts + render switch; walkthrough route only reached via its own redirect + journeys + phase28 spec) + § "Current Wiring 5" (render switch sops/[sopId]/page.tsx:92-97; WalkthroughSwitcher mounts desktop+voice) + § "Dead-Href Sweep B" (walkthrough journeys 78,138,149,160,298; no code hrefs)
    - 30-RESEARCH.md Open Question 3 + orchestrator decision #3 (dept filter: verify `sop_departments` SELECT policy — `using(true)` per 2026-06-15 learning — fix ~20 ln if it supports a worker client read, else remove the sidebar/sheet/pill)
    - src/app/(protected)/sops/page.tsx (no-op filter lines 93-101 `return true`; "Create SOP" tab ~144-152)
    - tests/phase28/library-and-worker.spec.ts (OverviewTab "Current as of"/no-gate assertions → repoint to ReadTab; walkthrough/page.tsx no-gate assertion → drop, route deleted)
    - CLAUDE.md learning 2026-06-08 (grep dead hrefs after route removal) + 29-01 precedent (repoint spec in the same plan that deletes the file)
  </read_first>
  <action>
    Delete `ModelTab.tsx` + `WalkthroughTab.tsx` and their `tabs/index.ts` exports. Delete `/sops/[sopId]/walkthrough/page.tsx` + `layout.tsx`. Update the render switch in `sops/[sopId]/page.tsx` to Read/Walk/Flow (Walk mounts `WalkthroughSwitcher`; Flow=FlowTab). Grep `/walkthrough` across `src` for dead hrefs and repoint any to `?tab=walk`. Remove the worker "Create SOP" tab from `sops/page.tsx`. For the dept filter: FIRST verify the `sop_departments` SELECT policy allows a worker client read — if yes, replace the `return true` no-op with a real client-side `sop_departments` fetch keyed on assigned SOP ids and filter; if no, remove the DepartmentSidebar/BottomSheet/pill + its state entirely. Repoint `tests/phase28/library-and-worker.spec.ts`: OverviewTab assertions → ReadTab; drop the walkthrough-page no-gate assertion (route gone) — the D28-07 no-gate invariant is now asserted on ReadTab.
  </action>
  <acceptance_criteria>
    - `[ ! -f src/components/sop/tabs/ModelTab.tsx ] && [ ! -f src/components/sop/tabs/WalkthroughTab.tsx ]` true; neither exported from `tabs/index.ts`.
    - `[ ! -d "src/app/(protected)/sops/[sopId]/walkthrough" ]` true (route+layout gone).
    - `grep -rn "/walkthrough" src` shows no dead href (only `?tab=walk` param usage or none).
    - `sops/page.tsx` has no "Create SOP" tab (grep absent) and no `return true` placebo filter (grep the no-op gone).
    - `npx playwright test --project=phase28 --project=phase30` exits 0 (library-and-worker repointed and green).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase28 --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>Model/Walkthrough tabs + walkthrough route deleted; render switch on 3 tabs; worker create tab gone; dept filter honest; phase28 worker spec repointed green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-baseline bundle + journeys + tab-merge spec live</name>
  <files>.bundle-baseline.json, src/lib/journeys/journeys.ts, tests/phase30/tab-merge.spec.ts, tests/phase30/dead-weight.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Bundle Gate Mechanics" (postbuild `check-bundle-size.ts`; two hard contracts — First Load JS ≤ baseline+2KB at 1057 KB, and DesktopWalkthrough+WalkthroughVoiceModal separate dynamic chunks; re-capture via `capture-bundle-baseline.ts`, commit, never hand-edit) + § "journeys.ts Impact" (walkthrough entries 78,138,149,160,298; tab-list description line 133)
    - tests/phase30/tab-merge.spec.ts + dead-weight.spec.ts (stubs from 30-01 — flip live)
    - CLAUDE.md STATE decisions [Phase 15-04] (re-baseline is legitimate for an intentional chunk shift; justify in SUMMARY)
  </read_first>
  <action>
    Run `npm run build`; if the intentional tab-merge shift moved chunks, regenerate `.bundle-baseline.json` via `npx tsx scripts/capture-bundle-baseline.ts` and commit it (document the delta + justification in SUMMARY; the merge should REDUCE First Load JS). Update journeys.ts: repoint the `/sops/[sopId]/walkthrough` entries to `/sops/[sopId]` with the Walk tab, and fix the tab-list description (line 133) from the 6-tab list to `read, walk, flow`. Flip `tab-merge.spec.ts` live (SOP_TABS===3, all 6 legacy params map, ModelTab absent, PPE once, bundle+chunk gates) and extend `dead-weight.spec.ts` with the ModelTab/WalkthroughTab/walkthrough-route deletions + journeys carries no removed routes.
  </action>
  <acceptance_criteria>
    - `npm run build` succeeds AND the postbuild bundle gate passes (First Load JS ≤ baseline+2 KB; both walkthrough chunks present).
    - `.bundle-baseline.json` regenerated via the capture script if chunks shifted (SUMMARY documents the delta) — not hand-edited.
    - journeys.ts contains no `/sops/[sopId]/walkthrough` route and the tab-list description reads `read, walk, flow`.
    - `npx playwright test tests/phase30/tab-merge.spec.ts tests/phase30/dead-weight.spec.ts --project=phase30` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test tests/phase30/tab-merge.spec.ts tests/phase30/dead-weight.spec.ts --project=phase30</automated>
  </verify>
  <done>Bundle re-baselined and gate green; journeys reflects the 3-tab worker route; tab-merge + dead-weight specs live-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker client → sop_departments read | if the dept filter is FIXED, a worker client read of sop_departments crosses RLS |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | Information disclosure | worker dept-filter client read | mitigate | Executor VERIFIES the `sop_departments` SELECT policy permits the worker read before adding the fetch; if the policy does not support it, REMOVE the filter rather than bypass RLS (decision #3) |
| T-30-06-02 | Elevation | merged Read tab exposing review/owner state | mitigate | ReadTab adds NO review_due_at/owner gate (D28-07 worker invariant); asserted by grep in Task 1 + the repointed phase28 spec |
| T-30-06-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `npm run build` clean incl. bundle gate.
- `npx playwright test --project=phase30 --project=phase28` green.
- `grep -rn "/walkthrough" src` shows no dead href; `[ ! -d "src/app/(protected)/sops/[sopId]/walkthrough" ]`.
</verification>

<success_criteria>
UX-05/UX-08/UX-04(worker): 3-tab worker detail, merged Read with PPE/equipment once + caption + no gate, legacy params land, Model/Walkthrough/route deleted, worker create tab gone, dept filter honest, bundle re-baselined, phase28 worker spec repointed.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-06-SUMMARY.md` when done.
</output>
