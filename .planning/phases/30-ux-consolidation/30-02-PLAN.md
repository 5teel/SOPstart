---
phase: 30-ux-consolidation
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/lib/auth/role-home.ts
  - src/app/(protected)/dashboard/page.tsx
  - src/app/(protected)/pending/page.tsx
  - src/app/not-found.tsx
  - src/actions/auth.ts
  - src/lib/supabase/middleware.ts
  - src/app/(auth)/login/roster/page.tsx
  - src/app/(protected)/activity/page.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/journeys/roles.ts
  - tests/phase30/role-homes.spec.ts
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "Each role lands on its real home: worker → /sops, supervisor + safety_manager → /activity, admin → /admin/sops; pending (no role) → /pending"
    - "The /dashboard route survives ONLY as a redirect-only shim (role → roleHome); AdminDashboard tiles + inline PendingDashboard UI are deleted"
    - "roleHome(role) is the single source of the role→home mapping, importable by middleware/auth/guards (lives in src/lib/, not a 'use server' file)"
    - "A stale link to a removed URL renders a navigable not-found page, not the bare browser error"
  artifacts:
    - path: "src/lib/auth/role-home.ts"
      provides: "roleHome(role) mapping — the one place role→home is decided"
      exports: ["roleHome"]
    - path: "src/app/(protected)/pending/page.tsx"
      provides: "Relocated PendingDashboard JSX for users with no org role"
    - path: "src/app/not-found.tsx"
      provides: "App-level not-found boundary (RESEARCH Pitfall 1)"
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Redirect-only shim (role dispatch), no UI"
  key_links:
    - from: "src/lib/supabase/middleware.ts"
      to: "roleHome / /dashboard shim"
      via: "authed user on auth route redirects to the role-dispatch"
      pattern: "redirect\\('/dashboard'\\)|roleHome"
    - from: "src/actions/auth.ts"
      to: "role-home dispatch"
      via: "post sign-in / join / accept-invite redirect"
      pattern: "redirect\\("
---

<objective>
UX-01 One home per role. Extract `roleHome(role)` into `src/lib/auth/role-home.ts`; reduce `/dashboard` to a redirect-only shim (delete the AdminDashboard tile grid and the inline PendingDashboard UI); relocate the pending-user JSX to a minimal `/pending` page; add an app-level `not-found.tsx`. Per the orchestrator's locked decision #5, internal admin guards may KEEP `redirect('/dashboard')` (the shim forwards them correctly) — this plan bounds the sweep to the role-dispatch + nav landing points; TopHeader nav-item removal is handled in 30-04.

Purpose: Users must never see a duplicate launcher or land nowhere sensible. One decision function, one shim, one pending home.
Output: roleHome helper, dashboard shim, /pending, not-found.tsx, journeys/roles landing points updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
@src/app/(protected)/dashboard/page.tsx
@src/lib/supabase/middleware.ts
@src/actions/auth.ts
</context>

<artifacts_produced>
- `roleHome(role)` in `src/lib/auth/role-home.ts` — maps `worker→/sops`, `supervisor→/activity`, `safety_manager→/activity`, `admin→/admin/sops`, absent/unknown → `/pending` (safe default, RESEARCH Assumption A1).
- `/pending/page.tsx` — relocated `PendingDashboard` blueprint-frame JSX (verbatim from dashboard/page.tsx:90-101).
- `src/app/not-found.tsx` — minimal navigable fallback linking to the viewer's role home (or `/`).
- `dashboard/page.tsx` reduced to: read role → `redirect(roleHome(role))`; `!role → redirect('/pending')`. AdminDashboard + DashTile grid deleted (DashTile is lifted into the 30-05 method picker).
- journeys.ts + roles.ts `landsOn` values point at the real homes.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: roleHome helper + dashboard shim + /pending + not-found.tsx</name>
  <files>src/lib/auth/role-home.ts, src/app/(protected)/dashboard/page.tsx, src/app/(protected)/pending/page.tsx, src/app/not-found.tsx</files>
  <read_first>
    - src/app/(protected)/dashboard/page.tsx (role switch lines 22-26; AdminDashboard tiles 47-73; PendingDashboard JSX 90-101 — the exact markup to relocate)
    - 30-PATTERNS.md § "Role-home redirects (UX-01)" (the role switch to move) + § "/pending stub"
    - 30-RESEARCH.md § "Current Wiring 1" (pending users land nowhere; safe default `/sops` when claim absent) + Pitfall 1 (not-found.tsx) + Pitfall 3 ('use server' sync exports)
    - CLAUDE.md learning 2026-06-27 (roleHome must NOT live in a 'use server' file)
  </read_first>
  <action>
    Create `roleHome(role: string | null | undefined): string` in `src/lib/auth/role-home.ts` (plain module, NOT 'use server'): worker→'/sops', supervisor→'/activity', safety_manager→'/activity', admin→'/admin/sops', else→'/pending'. Reduce `dashboard/page.tsx` to a server shim: resolve role (existing member lookup), `redirect(roleHome(role))`; delete `AdminDashboard`, the 6 `DashTile`s, and the inline `PendingDashboard`. Create `/pending/page.tsx` with the relocated PendingDashboard JSX verbatim (blueprint-frame + pill tokens). Create `src/app/not-found.tsx` — a small server component with a "This page moved" message + a Link to `/` (CSS-var tokens only, no hardcoded hex).
  </action>
  <acceptance_criteria>
    - `role-home.ts` exports `roleHome`; `grep -n "'use server'" src/lib/auth/role-home.ts` returns 0 hits.
    - `dashboard/page.tsx` contains `roleHome(` and a `redirect(` call and NO `DashTile`/`AdminDashboard` JSX (grep both absent).
    - `/pending/page.tsx` and `src/app/not-found.tsx` exist.
    - `npm run build` succeeds (proves no 'use server' sync-export break and the not-found boundary compiles).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>roleHome centralised; dashboard is a redirect shim; /pending + not-found exist; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint login/middleware/activity dispatch + journeys/roles landing points</name>
  <files>src/actions/auth.ts, src/lib/supabase/middleware.ts, src/app/(auth)/login/roster/page.tsx, src/app/(protected)/activity/page.tsx, src/lib/journeys/journeys.ts, src/lib/journeys/roles.ts, tests/phase30/role-homes.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Dead-Href Sweep Inventory A" (the 35 /dashboard references + which are guards vs nav vs landing) + § "Current Wiring 1" (middleware line 41, auth.ts 111/177/291, roster 46, activity 30)
    - src/lib/journeys/journeys.ts (entries 62,98,117,131,224) and src/lib/journeys/roles.ts (landsOn 49,100)
    - CLAUDE.md learning 2026-06-26 (parse JWT claim via parseJwtPayload, never atob) — the roster/middleware role read
    - Orchestrator locked decision #5 (guards may KEEP redirecting to /dashboard which forwards; only nav/landing must be direct)
  </read_first>
  <action>
    Keep `/dashboard` as the shim target where a role is not cheaply known (middleware line 41 may stay pointing at the shim; the shim dispatches). Where a role IS in hand, redirect via `roleHome(role)`: `roster/page.tsx:46` and `activity/page.tsx:30` (admin fallthrough → `/admin/sops`). `actions/auth.ts` (111/177/291) may keep `redirect('/dashboard')` (shim forwards) OR use `roleHome` if the role is resolved at that point — choose the smaller correct diff and note it. Update `journeys.ts` + `roles.ts` `landsOn` so no landing entry claims a role lands on `/dashboard` — each points at its real home; add the `/pending` and `not-found` screens if the map enumerates them. Flip the `role-homes.spec.ts` stub to live: assert `roleHome` mapping for all 5 cases and that the dispatch/landing points reference the real homes.
  </action>
  <acceptance_criteria>
    - `roster/page.tsx` and `activity/page.tsx` admin-fallthrough reference `roleHome(` or a real home, not a bare `/dashboard` landing.
    - `grep -rn "'/dashboard'" src/lib/journeys` returns 0 hits (journeys/roles no longer land any role on /dashboard).
    - `npx playwright test tests/phase30/role-homes.spec.ts --project=phase30` exits 0 (stub now live).
    - JWT role reads use `parseJwtPayload`, not `atob` (grep any new claim read).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/role-homes.spec.ts --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>Landing points route via roleHome; journeys/roles carry no /dashboard landings; role-homes spec live-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| unauthenticated → authed route | middleware decides redirect, NOT access — redirect is never an access grant |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | Elevation | middleware role-home redirect | mitigate | Redirect target is a landing decision only; every admin page keeps its own `['admin','safety_manager']` guard (unchanged this plan). No middleware access grant added |
| T-30-02-02 | Spoofing | JWT role claim read | mitigate | Role read via shared `parseJwtPayload` (Base64URL-safe), never raw `atob`; absent claim → `/pending` safe default (A1) |
| T-30-02-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `npm run build` clean (no 'use server' sync-export break).
- `npx playwright test --project=phase30 --project=phase26.5 --project=phase25-e2e` green (agent-dashboard + admin-departments specs still assert guards `redirect('/dashboard')` — guards unchanged, so they stay green).
- `grep -rn "'/dashboard'" src/lib/journeys` == 0.
</verification>

<success_criteria>
UX-01 satisfied: one roleHome function, dashboard is a redirect-only shim, /pending + not-found exist, no journeys/roles landing points at /dashboard. Admin guards unchanged (decision #5) so phase26.5/phase25 guard specs stay green.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-02-SUMMARY.md` when done.
</output>
