---
phase: 30-ux-consolidation
plan: 08
type: execute
wave: 8
depends_on: [30-01, 30-02, 30-03, 30-04, 30-05, 30-06, 30-07]
files_modified:
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/admin/governance/page.tsx
  - src/app/(protected)/admin/settings/page.tsx
  - src/components/admin/governance/GovernanceFilterChips.tsx
  - src/components/admin/governance/GovernanceWidget.tsx
  - src/components/admin/sops/LibraryReviewCell.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase28/governance-queue.spec.ts
  - tests/phase28/library-and-worker.spec.ts
  - tests/phase29/approval-chain-editor.spec.ts
  - tests/phase29/queue-approve-action.spec.ts
  - tests/phase30/governance-fold.spec.ts
  - tests/phase30/list-rows.spec.ts
autonomous: true
requirements: [UX-03, UX-06]
must_haves:
  truths:
    - "/admin/sops hosts the 'Needs attention' governance view: flag chips (Overdue / Due soon / Unowned / Stale role / Awaiting approval) + the GovernanceQueueRow list, reusing the existing rows verbatim"
    - "One-click Approve for the matching next approver works from the queue rows (approveStep + isCallerNextApprover gate preserved verbatim) AND still from the builder PublishStage; awaiting-approval count + deep-link live on the /admin/sops header chips (APR-03/APR-04)"
    - "Admin SOP list rows are one line: title · status chip · one flag chip · owner; SopDepartmentEditor + LibraryReviewCell + the 5 icon actions are gone from the row; click opens the builder"
    - "GovernanceWidget + LibraryReviewCell are deleted as separate surfaces; /admin/governance is a redirect shim to /admin/sops?view=attention (preserving ?filter=X → the new filter param); the approval-chain editor lives in /admin/settings"
    - "The existing 'Needs attention' STATUS_TAB (value=failed) is renamed 'Parse issues'; journeys /pathways shows 0 not-mapped; full phase28/29 regression + tsc + next build + bundle gate green"
  artifacts:
    - path: "src/app/(protected)/admin/sops/page.tsx"
      provides: "Single SOP surface: one-line rows + folded needs-attention governance view + header flag chips"
    - path: "src/app/(protected)/admin/governance/page.tsx"
      provides: "Redirect shim → /admin/sops?view=attention (maps ?filter=X)"
  key_links:
    - from: "admin/sops needs-attention view"
      to: "approveStep(row.id) via GovernanceQueueRow"
      via: "row action, gated by row.flags.includes('awaiting_approval') && row.isCallerNextApprover — moved verbatim"
      pattern: "approveStep\\(|isCallerNextApprover"
    - from: "admin/settings/page.tsx"
      to: "ApprovalChainPanel"
      via: "editor relocated here from the governance page"
      pattern: "ApprovalChainPanel|ApprovalChainEditor"
---

<objective>
UX-03 governance fold + UX-06 one-line rows + final gate. Fold the governance queue into `/admin/sops` as the "Needs attention" view (flag chips + the existing GovernanceQueueRow list reused verbatim — approveStep + isCallerNextApprover gate preserved), rename the colliding STATUS_TAB `failed`→"Parse issues" (decision #4), make `/admin/governance` a redirect shim to `/admin/sops?view=attention` mapping `?filter=X` (decision #1), relocate the approval-chain editor to `/admin/settings`, delete GovernanceWidget + LibraryReviewCell, and simplify the SOP list rows to one line. Repoint all phase28/29 specs the moves touch. Close the phase with the merged-tree gate: full phase28+29 regression, `npx tsc --noEmit`, `npm run build` (bundle gate), and /pathways 0 not-mapped.

Purpose: One list answers "which SOPs need attention" — the three governance surfaces collapse to one, rows go one-line, and the whole phase is proven green.
Output: folded governance, one-line rows, redirect shim, editor in settings, all specs repointed, phase gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
@src/app/(protected)/admin/governance/page.tsx
</context>

<artifacts_produced>
- `admin/sops/page.tsx`: needs-attention view (`?view=attention`) rendering `GovernanceFilterChips` + `GovernanceQueueRow` list (reused verbatim); header flag chips with counts (Overdue/Due soon/Unowned/Stale role/Awaiting approval) incl. awaiting-approval count + deep-link; STATUS_TAB `failed` label renamed "Parse issues"; one-line rows (title · status chip · one flag chip · owner); SopDepartmentEditor + LibraryReviewCell + 5 icon actions removed from rows.
- `admin/governance/page.tsx`: redirect shim → `/admin/sops?view=attention` (maps legacy `?filter=X`).
- `admin/settings/page.tsx`: hosts the relocated `ApprovalChainPanel`/`ApprovalChainEditor`.
- `GovernanceFilterChips.tsx`: hrefs repointed to the folded view.
- DELETED: `src/components/admin/governance/GovernanceWidget.tsx`, `src/components/admin/sops/LibraryReviewCell.tsx`.
- Repointed specs: phase28 governance-queue + library-and-worker; phase29 approval-chain-editor + queue-approve-action.
- journeys.ts final: /admin/governance → shim, editor under settings, 0 not-mapped.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fold governance into /admin/sops (needs-attention view) + shim + relocate editor</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/app/(protected)/admin/governance/page.tsx, src/app/(protected)/admin/settings/page.tsx, src/components/admin/governance/GovernanceFilterChips.tsx</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 3" (governance/page.tsx assembles listGovernanceQueue + GovernanceFilterChips + GovernanceQueueRow + ApprovalChainEditor; GovernanceWidget deep-links; the "Needs attention" STATUS_TAB collision at page.tsx:24) + Open Question 1 (locked: shim to ?view=attention) + § "Preserved verbatim (HARD constraint)"
    - 30-PATTERNS.md § "governance fold + one-line rows" (STATUS_TABS idiom admin/sops/page.tsx:174-195; GovernanceFilterChips 22-35; the GovernanceQueueRow approve handler 49-59/84-92 to move VERBATIM; FLAG_STYLE/FLAG_LABEL 11-25)
    - orchestrator decisions #1 (shim + ?filter mapping; approve wiring moves verbatim) + #4 (rename failed tab → "Parse issues"; new view owns "Needs attention")
    - src/app/(protected)/admin/governance/page.tsx + src/components/admin/governance/ApprovalChainPanel.tsx (the editor to relocate)
    - CLAUDE.md learning 2026-06-05 (assert approveStep handler wiring, not token presence)
  </read_first>
  <action>
    In `admin/sops/page.tsx` add a `?view=attention` needs-attention view: call `listGovernanceQueue()` (as the widget does today — server component, same cost), render `GovernanceFilterChips` + the `GovernanceQueueRow` list MOVED VERBATIM (do not reimplement approveStep/isCallerNextApprover). Add header flag chips with counts including awaiting-approval + its deep-link (HARD constraint). Rename the existing STATUS_TAB `failed` label to "Parse issues" so the new view owns "Needs attention". Repoint `GovernanceFilterChips` hrefs `/admin/governance?filter=` → `/admin/sops?view=attention&…`. Convert `admin/governance/page.tsx` to a redirect shim → `/admin/sops?view=attention`, mapping legacy `?filter=X` to the new filter param (keep the admin guard before redirect). Relocate `ApprovalChainPanel` (+ its category/member data assembly) into `admin/settings/page.tsx`. Do NOT touch `src/actions/governance.ts` or `src/actions/approvals.ts`.
  </action>
  <acceptance_criteria>
    - `admin/sops/page.tsx` contains `listGovernanceQueue(`, `GovernanceQueueRow`, `approveStep`/`isCallerNextApprover` (verbatim move), and the awaiting-approval header chip + deep-link.
    - The `failed` STATUS_TAB renders label "Parse issues" (grep); the folded view renders label "Needs attention".
    - `admin/governance/page.tsx` is a redirect shim (`redirect('/admin/sops?view=attention` present; no queue/editor render) that maps `?filter=`.
    - `admin/settings/page.tsx` renders `ApprovalChainPanel`/`ApprovalChainEditor`.
    - `src/actions/governance.ts` and `src/actions/approvals.ts` are byte-unchanged (`git diff --quiet` on both).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Governance folded into /admin/sops with approve wiring verbatim; governance route is a shim; editor in settings; server actions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: One-line rows + delete GovernanceWidget/LibraryReviewCell + repoint phase28/29 specs</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/components/admin/governance/GovernanceWidget.tsx, src/components/admin/sops/LibraryReviewCell.tsx, tests/phase28/governance-queue.spec.ts, tests/phase28/library-and-worker.spec.ts, tests/phase29/approval-chain-editor.spec.ts, tests/phase29/queue-approve-action.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 6" (row anatomy admin/sops/page.tsx:239-366: SopDepartmentEditor 289-296, LibraryReviewCell 297-301, 5 icon actions 306-350; keep title+StatusBadge+one flag chip+owner; owner map built at 88-94; row already links to builder line 242) + § "Test Repointing Inventory" (exact per-spec reads)
    - 30-PATTERNS.md § "One-line row (UX-06)" (strip list) + § "Test repointing (phase28/29 source-contract specs)"
    - tests/phase28/governance-queue.spec.ts, tests/phase28/library-and-worker.spec.ts, tests/phase29/approval-chain-editor.spec.ts, tests/phase29/queue-approve-action.spec.ts (the path constants + assertions to repoint — GovernanceWidget lives at src/components/admin/governance/GovernanceWidget.tsx, LibraryReviewCell at src/components/admin/sops/LibraryReviewCell.tsx)
    - CLAUDE.md 29-01 precedent (repoint the spec in the SAME plan that moves/deletes the code)
  </read_first>
  <action>
    Simplify each SOP list row to one line: title · StatusBadge · ONE flag chip · owner label (owner map already built). Remove `SopDepartmentEditor` (reachable via the builder Assign action from 30-07), `LibraryReviewCell`, and the 5 icon-only action Links from the row. Keep the row click → `/admin/sops/builder/${sop.id}`. Delete `src/components/admin/governance/GovernanceWidget.tsx` + `src/components/admin/sops/LibraryReviewCell.tsx` (their counts/actions now live in the folded view). Repoint the 4 specs: governance-queue → page path stays but assert the folded surface + journeys mapping; library-and-worker → LibraryReviewCell/GovernanceWidget assertions move to the merged surface (OverviewTab already repointed to ReadTab in 30-06); approval-chain-editor → editor now in settings + assert NO /admin/governance/approval-chains route; queue-approve-action → Widget assertions → header chips, QueueRow/FilterChips survive (assert approveStep handler wiring). Grep dead hrefs for `/admin/governance` and repoint per shim.
  </action>
  <acceptance_criteria>
    - Row region contains no `SopDepartmentEditor`, no `LibraryReviewCell`, and no icon-only `evidence-btn` action Links (grep all absent in the row block); row still links to `/admin/sops/builder/`.
    - `[ ! -f src/components/admin/governance/GovernanceWidget.tsx ] && [ ! -f src/components/admin/sops/LibraryReviewCell.tsx ]` true; `grep -rn "GovernanceWidget\|LibraryReviewCell" src` returns 0.
    - `npx playwright test --project=phase28 --project=phase29` exits 0 (all repointed specs green).
    - `npx playwright test tests/phase30/list-rows.spec.ts --project=phase30` exits 0 (row-half assertions now live).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase28 --project=phase29 tests/phase30/list-rows.spec.ts --project=phase30</automated>
  </verify>
  <done>Rows one-line; GovernanceWidget/LibraryReviewCell deleted; phase28/29 specs repointed green; list-rows spec live.</done>
</task>

<task type="auto">
  <name>Task 3: journeys final sweep + governance-fold spec + merged-tree phase gate</name>
  <files>src/lib/journeys/journeys.ts, tests/phase30/governance-fold.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "journeys.ts Impact" (all changed routes; /pathways "All screens" 0 not-mapped) + § "Validation Architecture" (full suite command + phase gate) + § "Common Pitfalls" 4 (npm run build is the real gate)
    - 30-PATTERNS.md § "journeys.ts same-change rule" + § "Dead-href sweep after every route removal"
    - CLAUDE.md Pathways Map Maintenance rule (0 not-mapped) + learning 2026-06-27 (npm run build final gate) + 2026-06-08 (dead hrefs)
    - tests/phase30/governance-fold.spec.ts (stub from 30-01 — flip live)
  </read_first>
  <action>
    Final journeys.ts sweep: /admin/governance → shim entry, approval editor under settings, needs-attention as a view of /admin/sops; ensure every route added/removed across the phase (method picker, /admin/settings, /pending, folded governance, deleted walkthrough/Model) is reflected and NO removed route remains referenced. Flip `governance-fold.spec.ts` live: needs-attention view renders QueueRow/chips, approveStep wired in QueueRow AND PublishStage, awaiting count + deep-link present, /admin/governance is a shim. Run the merged-tree gate: full phase28+29+30 regression, `npx tsc --noEmit`, `npm run build` (postbuild bundle gate), and confirm /pathways 0 not-mapped (journeys route-coverage assertion in the spec). Run the dead-href greps for `/dashboard` (nav surfaces), `/admin/governance`, `/walkthrough`.
  </action>
  <acceptance_criteria>
    - journeys.ts references no deleted route (grep `/sops/[sopId]/walkthrough`, `/admin/governance?filter`, ModelTab route == 0) and maps method picker + /admin/settings + /pending + the folded governance view; /pathways coverage assertion shows 0 not-mapped.
    - `npm run test` (FULL suite — all Playwright projects incl. integration, e2e, phase26.5, phase21-stubs) exits with no NEW failures vs the Wave-1 baseline recorded in 30-01 (pre-existing fixme/known-fail entries stay as baselined). tests/integration/departments-rls.spec.ts specifically: if its runtime /dashboard navigation fails against the shim, repoint it to a real role-home route in this plan (add to files_modified).
    - `npx tsc --noEmit` clean AND `npm run build` succeeds with the postbuild bundle gate passing.
    - Dead-href greps: `/admin/governance?filter` and `/walkthrough` return 0 dead hits; `/dashboard` returns 0 hits in nav surfaces (guards→shim allowed per decision #5).
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run test</automated>
  </verify>
  <done>journeys 0 not-mapped; governance-fold spec live; full regression + tsc + build + bundle gate green — phase closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| non-approver → Approve action | GovernanceQueueRow's approve is gated server-side (stepMatchesCaller) + client (isCallerNextApprover); the fold must preserve both |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08-01 | Elevation | folded Approve exposing action to non-matching approvers | mitigate | GovernanceQueueRow moved VERBATIM — approveStep + `row.flags.includes('awaiting_approval') && row.isCallerNextApprover` gate unchanged; server-side stepMatchesCaller in approvals.ts is byte-unchanged (git diff --quiet asserted). APR-03/APR-04 preserved |
| T-30-08-02 | Information disclosure | needs-attention view leaking cross-org governance rows | mitigate | View calls the same `listGovernanceQueue()` the widget calls today (org-scoped) — no new query path; governance.ts untouched |
| T-30-08-03 | Elevation | /admin/governance shim / settings editor | mitigate | Shim keeps the admin guard before redirect; settings page keeps its `['admin','safety_manager']` guard; ApprovalChainPanel relocation is a mount move, not a permission change |
| T-30-08-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `npm run build` clean incl. bundle gate.
- `npm run test` (full suite) — no NEW failures vs the 30-01 baseline; departments-rls.spec.ts green (repointed if the shim breaks it).
- `git diff --quiet src/actions/governance.ts src/actions/approvals.ts` (spine untouched).
- /pathways 0 not-mapped; dead-href greps empty for removed routes.
</verification>

<success_criteria>
UX-03 + UX-06 (rows): governance folded into /admin/sops as the needs-attention view with approve preserved verbatim (APR-03/04), GovernanceWidget/LibraryReviewCell deleted, editor in settings, /admin/governance a shim, rows one-line, STATUS_TAB renamed. Phase gate green: full regression + tsc + build + bundle + /pathways 0 not-mapped.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-08-SUMMARY.md` when done.
</output>
