---
phase: 26-sop-builder-redesign
plan: 04
type: execute
wave: 3
depends_on: ["26-03", "26-01"]
files_modified:
  - src/lib/builder/content-ops.ts
  - src/components/admin/builder-v2/EditableDocument.tsx
  - src/components/admin/builder-v2/BlockEditShell.tsx
  - src/components/admin/builder-v2/InlineText.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
  - tests/phase26/autosave-rewire.spec.ts
  - tests/phase26/props-roundtrip.spec.ts
autonomous: true
requirements: [R1, R2, R7]
user_setup: []
must_haves:
  truths:
    - "Editing a block's text on the admin canvas writes a draftLayouts row to Dexie via the EXISTING useBuilderAutosave, and the edit persists after reload (P11)"
    - "The edit canvas renders the SAME worker block components with hover edit affordances layered on (R2 edit == worker render)"
    - "Blocks reorder via keyboard and pointer (dnd-kit vertical sortable); reorder persists"
    - "Every reducer op round-trips ALL props keys losslessly — junctionId + block_provenance + unknown agent keys survive an edit round-trip (P15/R7 hook, D-02)"
  artifacts:
    - path: "src/lib/builder/content-ops.ts"
      provides: "pure insert/update/delete/reorder/stampProvenance helpers (NOT 'use server')"
      contains: "stampProvenance"
    - path: "src/components/admin/builder-v2/EditableDocument.tsx"
      provides: "controlled content[] host + dnd-kit context; feeds useBuilderAutosave"
      contains: "useBuilderAutosave"
    - path: "src/components/admin/builder-v2/BlockEditShell.tsx"
      provides: "per-block hover tools, grip, data-block-id, worker component wrap"
      contains: "data-block-id"
  key_links:
    - from: "src/components/admin/builder-v2/EditableDocument.tsx"
      to: "src/hooks/useBuilderAutosave.ts"
      via: "reducer effect calls handleChange({content, root})"
      pattern: "useBuilderAutosave"
    - from: "src/lib/builder/content-ops.ts"
      to: "layout_data props"
      via: "spread-merge update preserves junctionId/block_provenance"
      pattern: "block_provenance"
---

<objective>
Build the bespoke EDIT canvas over the same `layout_data` (D-01, R2). The admin now edits the exact worker components (`BlockEditShell` wraps each `src/components/sop/blocks/*` component with hover tools + `contentEditable` text), and all edits flow through a controlled `content[]` reducer whose changes feed the UNCHANGED `useBuilderAutosave` (P11 RE-WIRE — no new persistence path). This makes the "one surface" real: all three on-ramps (R1) already reach `BuilderClient`; we swap `<Puck>` for `<EditableDocument>`.

The reducer is the R7/D-02 contract hook: every op MUST spread-merge props (`{...prev, ...changed}`) and round-trip ALL unknown keys so `junctionId`, `block_provenance`, and future 26.5 agent metadata never drop (RESEARCH Pitfall 7). Agent-layer SURFACING stays out of scope (D-02) — only the lossless round-trip contract lands here.

Purpose: edit == worker render, autosaving through the existing offline path, contract-preserving.
Output: `content-ops.ts` reducer, `EditableDocument`, `BlockEditShell`, `InlineText`, mounted in `BuilderClient` replacing Puck.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
@src/hooks/useBuilderAutosave.ts
</context>

<artifacts>
This plan produces:
- `src/lib/builder/content-ops.ts` — `insertBlock`, `updateBlockProps`, `deleteBlock`, `reorderBlocks`, `duplicateBlock`, `stampProvenance` (pure, non-'use server')
- `src/components/admin/builder-v2/EditableDocument.tsx` — controlled content host + DndContext + reducer→autosave effect
- `src/components/admin/builder-v2/BlockEditShell.tsx` — hover tools (dup/delete/grip), `data-block-id`, worker-component wrap
- `src/components/admin/builder-v2/InlineText.tsx` — uncontrolled contentEditable, commit-on-blur
- `BuilderClient.tsx` swap: `<Puck onChange>` → `<EditableDocument>`
- `tests/phase26/autosave-rewire.spec.ts`, `tests/phase26/props-roundtrip.spec.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure content[] reducer helpers (lossless props round-trip)</name>
  <files>src/lib/builder/content-ops.ts, tests/phase26/props-roundtrip.spec.ts</files>
  <read_first>
    - src/lib/validators/blocks.ts (layout_data item shape `{type, props:{id, junctionId?, block_provenance?, ...fields}}`)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § EditableDocument (Pitfall 7 spread-merge rule) + § Frozen-contract stamping
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Pattern 2 + Pitfall 3 (pure helpers NOT in 'use server')
  </read_first>
  <behavior>
    - `insertBlock(content, type, afterIndex, defaults)` splices `{type, props:{id: uuid(), ...defaults}}`.
    - `updateBlockProps(content, id, changedFields)` returns content with that item's props = `{...prevProps, ...changedFields}` — NEVER reconstructed; `junctionId`, `block_provenance`, and any unknown key survive.
    - `deleteBlock`, `duplicateBlock` (new id, deep-copied props incl. unknown keys), `reorderBlocks(content, from, to)`.
    - `stampProvenance(props, region, runId, ver)` merges a `block_provenance` without dropping existing props.
  </behavior>
  <action>Write `src/lib/builder/content-ops.ts` as pure functions (NO `'use server'`, lives in `src/lib/builder/` per CLAUDE.md 2026-06-27 — a sync helper in an action file breaks `next build`). Implement the reducer ops above; use `crypto.randomUUID()` for new ids. Write `tests/phase26/props-roundtrip.spec.ts` (unit): create an item carrying `junctionId`, `block_provenance`, and a synthetic unknown key `__agent`, run `updateBlockProps` changing only `text`, and assert all three metadata keys survive byte-identical (P15/R7 lossless-round-trip contract, D-02 hook). Also assert `duplicateBlock` copies unknown keys and assigns a fresh id.</action>
  <acceptance_criteria>
    - props-roundtrip spec: after `updateBlockProps`, `junctionId`, `block_provenance`, `__agent` all unchanged; only `text` differs.
    - `content-ops.ts` is NOT under `src/actions/` and has no `'use server'` directive.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "props-roundtrip" && npx tsc --noEmit</automated>
  </verify>
  <done>Reducer helpers pure; lossless props round-trip proven behaviourally (R7/D-02 hook).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditableDocument + BlockEditShell + InlineText; re-wire autosave (P11)</name>
  <files>src/components/admin/builder-v2/EditableDocument.tsx, src/components/admin/builder-v2/BlockEditShell.tsx, src/components/admin/builder-v2/InlineText.tsx, tests/phase26/autosave-rewire.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (L77 `handleChange = useBuilderAutosave(activeSectionId, sopId)`; L533 `<Puck onChange={handleChange}>`)
    - src/hooks/useBuilderAutosave.ts (L14–36 — accepts any `{content, root}`, debounces 750ms → Dexie draftLayouts; RESEARCH A4)
    - src/lib/builder/block-registry.tsx (BLOCK_COMPONENTS + stripMeta from 26-03)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Block Edit-Shell Contract (hover affordances, data-block-id, Pattern A InlineText rules)
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Pitfall 4 (uncontrolled contentEditable)
  </read_first>
  <behavior>
    - `EditableDocument` holds the active section's `content[]` in local state, renders each item as `<BlockEditShell>` wrapping `BLOCK_COMPONENTS[type]` (same component as worker), and on any `content` change calls `handleChange({content, root})` once (the existing hook) — debounced Dexie write.
    - `InlineText` is uncontrolled: sets defaultValue/innerText once, reads `textContent` on blur, commits via `updateBlockProps` (never re-writes the node from state while focused; never innerHTML — XSS).
    - `BlockEditShell` renders `data-block-id={item.props.id}`, a drag grip, and duplicate/delete tools (opacity 0 → 1 on hover, so worker read mode shows none).
    - Editing text → a Dexie `draftLayouts` row for that section; reload restores the edited text.
  </behavior>
  <action>Build `EditableDocument.tsx` ('use client', hydration-clean): controlled `content[]` seeded from the section's `layout_data`, dispatching `content-ops` helpers; a single `useEffect([content])` calls the existing `useBuilderAutosave` `handleChange({content, root})`. Build `BlockEditShell.tsx` wrapping each worker component with the UI-SPEC hover affordances + `data-block-id`. Build `InlineText.tsx` as uncontrolled contentEditable (Pattern A). In `BuilderClient.tsx`, replace the `<Puck onChange={handleChange}>` region with `<EditableDocument section={activeSection} sopId={sopId} />`, keeping `handleChange`/`useBuilderAutosave` wiring intact (selection-sync, overlays, verify UI are RE-WIRED in a later wave — mount minimally here). Do NOT use `router.push` for active-section/selection state — local `useState` + `history.replaceState` only (CLAUDE.md 2026-05-13). Write `tests/phase26/autosave-rewire.spec.ts` (behavioural, chromium): edit a block's text, assert a `db.draftLayouts` row is written for the section, reload the component, assert the edited text persists — the P11 parity proof (NOT a grep for `useBuilderAutosave`).</action>
  <acceptance_criteria>
    - autosave-rewire spec: text edit → draftLayouts row written → reload persists (behavioural).
    - `BlockEditShell` renders `data-block-id` on every block (required for later selection-sync reverse binding).
    - No `router.push`/searchParam write on the edit path (`grep -n "router.push" src/components/admin/builder-v2/` returns nothing).
    - `npx tsc --noEmit` green; `npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "autosave-rewire" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Edit canvas renders worker components with edit affordances; edits autosave via the existing hook and persist (P11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dnd-kit vertical sortable reorder (keyboard + pointer)</name>
  <files>src/components/admin/builder-v2/EditableDocument.tsx, src/components/admin/builder-v2/BlockEditShell.tsx</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Code Examples → dnd-kit vertical sortable
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Block Edit-Shell Contract (grip, restrictToVerticalAxis, keyboard reorder from dnd-kit)
  </read_first>
  <behavior>
    - Blocks reorder via drag on the grip AND via keyboard (dnd-kit gives keyboard sensor for free); reorder is vertical-axis only (`restrictToVerticalAxis`) — blocks reflow, never free-drag (Konva diagram is the only freeform surface).
    - `onDragEnd` dispatches `reorderBlocks` (arrayMove), which round-trips props losslessly (Task 1), then autosaves.
  </behavior>
  <action>Wrap `EditableDocument`'s block list in `<DndContext collisionDetection={closestCenter} modifiers={[restrictToVerticalAxis]}>` + `<SortableContext items={content.map(c=>c.props.id)} strategy={verticalListSortingStrategy}>`; `BlockEditShell` uses `useSortable({id})` on the grip handle. `onDragEnd` → `dispatch(reorderBlocks(content, from, to))` → autosave. Keep `@dnd-kit/*` imported ONLY from `builder-v2/` (admin-only; never from a shared/worker layout).</action>
  <acceptance_criteria>
    - Reorder spec (behavioural, chromium): dragging block B above block A reorders `content[]` and persists (draftLayouts reflects the new order).
    - `restrictToVerticalAxis` applied; no free-drag.
    - dnd-kit imported only under `src/components/admin/builder-v2/` (`grep -rl "@dnd-kit" src | grep -v builder-v2` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "reorder" && npx tsc --noEmit</automated>
  </verify>
  <done>Keyboard + pointer vertical reorder works, persists, and stays admin-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin contentEditable → reducer → layout_data | Admin-authored text/props cross into stored layout_data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | XSS via contentEditable text | mitigate | Commit `textContent` (never innerHTML) to the reducer; user text never rendered as raw HTML |
| T-26-04-02 | Tampering | dropped junctionId/block_provenance on edit | mitigate | Reducer spread-merges props (`{...prev,...changed}`); props-roundtrip parity test enforces (Pitfall 7) |
| T-26-04-03 | Info disclosure | admin editor code in worker bundle | mitigate | `builder-v2/` + dnd-kit imported only from admin routes; grep guard in acceptance |
</threat_model>

<verification>
- autosave-rewire + props-roundtrip + reorder specs green (behavioural).
- `npm run build` green (bundle gate holds; edit code not in worker path).
- No `router.push` on the edit path.
</verification>

<success_criteria>
Edit canvas == worker render (R2); edits autosave through the existing Dexie path and persist (P11); reorder works keyboard+pointer; props round-trip losslessly (R7/D-02 hook); one surface reached by all on-ramps (R1).
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-04-SUMMARY.md` when done
</output>
