---
phase: 26-sop-builder-redesign
plan: 07
type: execute
wave: 5
depends_on: ["26-06"]
files_modified:
  - src/components/admin/builder-v2/fields/FieldPanel.tsx
  - src/components/admin/builder-v2/fields/ArrayFieldEditor.tsx
  - src/components/admin/builder-v2/BlockEditShell.tsx
  - tests/phase26/field-panel-reachability.spec.ts
autonomous: true
requirements: [R2]
user_setup: []
must_haves:
  truths:
    - "The Pattern-C anchored field panel opens from the block's tools row and edits array + multi-field config for every block that has one"
    - "PPE items, Decision options (min 2), Inspect items, Escalate recipients, Measurement min/max/target, and Model fields are all reachable and write valid layout_data"
    - "For every one of the 18 registered block types, the count of Puck-editable fields that are UNREACHABLE in the bespoke editor is exactly 0 (per-block parity test)"
  artifacts:
    - path: "src/components/admin/builder-v2/fields/FieldPanel.tsx"
      provides: "Pattern C anchored popover shell (reuses inserter .pk chrome)"
      contains: "FieldPanel"
    - path: "src/components/admin/builder-v2/fields/ArrayFieldEditor.tsx"
      provides: "add/remove/reorder array-row editor (dnd-kit rows)"
      contains: "ArrayFieldEditor"
  key_links:
    - from: "src/components/admin/builder-v2/fields/FieldPanel.tsx"
      to: "src/lib/validators/blocks.ts"
      via: "panel validates rows against the block's *PropsSchema"
      pattern: "PropsSchema|BlockContentSchema"
---

<objective>
Complete the P14 RE-IMPLEMENT (D-01) with **Pattern C** — the anchored field panel for array lists and multi-field numeric config — and prove the hard rule: **no field editable under Puck is unreachable** (SPEC acceptance), block by block, with a behavioural parity test per type (CLAUDE.md 2026-06-05 — presence tests are insufficient).

Pattern C covers the fields the inline patterns can't: PPECard `items[]`, Decision `options[]{label,nextStepId?,isEscalation?}` (min 2), Inspect `items[]{label,requirePhoto}`, Escalate `recipients[]` + `escalationMode`, Measurement `tolerance{min,max,target}` explicit bounds, and ModelBlock's panel-only fields. Rows are add/remove/reorder (dnd-kit), each validated by the block's `*PropsSchema` Zod. The panel reuses the inserter's `.pk` chrome so there is ONE popover visual language.

Purpose: every structured field reachable and valid; parity proven per block.
Output: `FieldPanel` (Pattern C), `ArrayFieldEditor`, shell "⚙ edit fields" trigger, and 18 per-block reachability parity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@src/lib/validators/blocks.ts
</context>

<artifacts>
This plan produces:
- `src/components/admin/builder-v2/fields/FieldPanel.tsx` — Pattern C anchored popover
- `src/components/admin/builder-v2/fields/ArrayFieldEditor.tsx` — dnd-kit add/remove/reorder rows
- `BlockEditShell` "⚙ edit fields" tool wiring
- `tests/phase26/field-panel-reachability.spec.ts` — per-block 0-unreachable parity
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: FieldPanel (Pattern C) + ArrayFieldEditor</name>
  <files>src/components/admin/builder-v2/fields/FieldPanel.tsx, src/components/admin/builder-v2/fields/ArrayFieldEditor.tsx, src/components/admin/builder-v2/BlockEditShell.tsx</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Field-Editor Contract → Pattern C (anchored ~300px, .pk chrome, drag-reorder rows, Esc/click-away, autosave-on-change) + the per-block C-field list
    - src/lib/validators/blocks.ts (L39–112 region — Measurement/Decision/Inspect/Escalate/SignOff/Zone/VoiceNote schemas; L181–191 PhotoGrid) for exact row field shapes + min counts
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § fields/ (Ponytail: minimum inputs matching Zod, react-hook-form only for real array fields)
  </read_first>
  <behavior>
    - `FieldPanel` opens anchored to the block tools row on a "⚙ edit fields" click; Esc/click-away closes; each change autosaves via the reducer.
    - `ArrayFieldEditor` renders one row per array item with add/remove and dnd-kit reorder; enforces min counts (Decision options min 2 — Zod) and per-row fields (e.g. Decision row = label text + isEscalation toggle + optional nextStepId).
    - All commits validated by the block's `*PropsSchema`; invalid rows blocked with an inline message, not silently dropped.
  </behavior>
  <action>Build `FieldPanel.tsx` reusing the inserter `.pk` chrome (1.5px ink-900 border, 10px radius, 300px, mono header). Build `ArrayFieldEditor.tsx` (dnd-kit sortable rows; use react-hook-form for the array fields where it reduces state boilerplate — Decision/Inspect/PPE/Escalate — per RESEARCH Ponytail). In `BlockEditShell`, add the "⚙ edit fields" tool that opens `FieldPanel` for any block whose FIELD_MAP has a Pattern-C field; render the block's C-fields (PPE items, Decision options, Inspect items, Escalate recipients+mode, Measurement min/max/target, Model assetUrl/hotspots/defaultLayers). Validate against the block `*PropsSchema` before commit.</action>
  <acceptance_criteria>
    - Behavioural: adding a Decision option row writes it to `options[]`; removing below 2 is blocked (Zod min); reordering PPE items persists order.
    - FieldPanel closes on Esc/click-away and autosaves on change.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "field-panel" && npx tsc --noEmit</automated>
  </verify>
  <done>Pattern C panel + array editor work for every C-field block; Zod-validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-block reachability parity — 0 unreachable fields, all 18 types</name>
  <files>tests/phase26/field-panel-reachability.spec.ts</files>
  <read_first>
    - src/components/admin/builder-v2/fields/field-map.ts (the declared field→pattern set per block)
    - src/lib/builder/puck-config.tsx (each block's `fields:` map — the Puck-editable ground truth)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Per-block mapping (unreachable count MUST be 0)
  </read_first>
  <behavior>
    - For each of the 18 registered block types, every field the Puck `fields:` map exposed is editable in the bespoke editor (via A/B/C/D/E) and a value written through the editor round-trips into valid `layout_data`.
    - The unreachable count per block === 0.
  </behavior>
  <action>Write `tests/phase26/field-panel-reachability.spec.ts` as a data-driven behavioural suite: for each block type, (1) assert FIELD_MAP field keys ⊇ the puck-config `fields:` keys (no field missing from the bespoke map), and (2) for a representative field of each pattern present on that block, render the block in the edit shell, drive the corresponding control (InlineText/EnumChip/InlineToken/FieldPanel), and assert the value lands in `layout_data` validated by `BlockContentSchema`/`*PropsSchema`. This is the P14 behavioural parity gate (not a grep) — the acceptance is `unreachable === 0` for all 18.</action>
  <acceptance_criteria>
    - Suite iterates all 18 registered types; each reports 0 unreachable fields.
    - At least one field per pattern (A/B/C/D/E-stub) is driven and asserted to write valid layout_data.
    - `npx tsc --noEmit` green; `npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "reachability" && npm run build</automated>
  </verify>
  <done>All 18 blocks have 0 unreachable fields, proven behaviourally (P14 parity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin field-panel input → layout_data | Array/config field values cross into stored layout_data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | invalid array rows (e.g. <2 Decision options) persisted | mitigate | Zod min/shape enforced before commit; invalid rows blocked with inline message |
| T-26-07-02 | Tampering | XSS via row label text | mitigate | textContent commit; never innerHTML |
</threat_model>

<verification>
- field-panel behavioural spec green.
- reachability suite: 0 unreachable for all 18 blocks.
- `npm run build` green.
</verification>

<success_criteria>
All 16+ structured field editors bespoke (P14 RE-IMPLEMENT complete, D-01); no Puck-editable field unreachable, proven per block behaviourally.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-07-SUMMARY.md` when done
</output>
