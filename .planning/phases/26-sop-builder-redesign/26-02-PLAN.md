---
phase: 26-sop-builder-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase26/README.md
  - tests/phase26/convert-golden-path.spec.ts
  - scripts/capture-convert-golden.ts
  - tests/phase26/fixtures/convert-golden.json
autonomous: true
requirements: [R6]
user_setup: []
must_haves:
  truths:
    - "A phase26 Playwright project exists and discovers every tests/phase26/*.spec.ts file"
    - "A pre-phase convert golden fixture captures layout_data + junctions + block_provenance for a known DOCX BEFORE any editor change"
    - "The convert-parity spec passes at W0 head (nothing has changed yet)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase26 project with a broad tests/phase26 testMatch regex"
      contains: "phase26"
    - path: "tests/phase26/fixtures/convert-golden.json"
      provides: "byte-baseline of parse output for R6 regression"
    - path: "tests/phase26/convert-golden-path.spec.ts"
      provides: "R6 byte-equivalence regression spec"
  key_links:
    - from: "tests/phase26/convert-golden-path.spec.ts"
      to: "tests/phase26/fixtures/convert-golden.json"
      via: "deep-equal assertion"
      pattern: "convert-golden"
---

<objective>
Stand up the Nyquist test harness for the whole phase and freeze the R6 regression baseline BEFORE any editor change touches the pipeline. Two things must exist before Wave 2 rewrites the renderer:
1. A `phase26` Playwright project registered with a broad `tests/phase26/**` regex, so every later spec runs (CLAUDE.md 2026-05-25: an unregistered spec NEVER runs). Using one broad regex means later plans add specs under `tests/phase26/` WITHOUT re-editing `playwright.config.ts`.
2. A **pre-phase convert golden fixture** — a known DOCX run through the UNCHANGED parse pipeline, snapshotting the resulting `layout_data` + `sop_section_blocks` junctions + `block_provenance` — the byte-equivalence baseline R6 asserts against after the full-bespoke swap (D-01).

Purpose: guarantee the FROZEN contract (D-01) is provably unchanged at phase end.
Output: phase26 test project + golden fixture + a convert-parity spec that is green at W0 head.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sop-builder-redesign/26-VALIDATION.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
</context>

<artifacts>
This plan produces:
- Playwright project `phase26` (testMatch `tests/phase26/**`)
- `scripts/capture-convert-golden.ts` — captures the pre-phase parse snapshot
- `tests/phase26/fixtures/convert-golden.json` — the R6 baseline
- `tests/phase26/convert-golden-path.spec.ts` — R6 byte-equivalence regression spec
- `tests/phase26/README.md` — how the phase26 harness + golden fixture work
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase26 Playwright project (broad regex)</name>
  <files>playwright.config.ts, tests/phase26/README.md</files>
  <read_first>
    - playwright.config.ts (existing per-phase project pattern; e.g. phase23-stubs uses `testMatch: /tests\/phase23\/.*\.(spec|test)\.ts$/`)
    - .planning/phases/26-sop-builder-redesign/26-VALIDATION.md § Test Infrastructure
  </read_first>
  <action>Add a `phase26` project to the `projects` array in `playwright.config.ts` with `testDir: '.'` and `testMatch: /tests\/phase26\/.*\.(spec|test)\.ts$/` and `use: { browserName: 'chromium' }` (mirror the phase23-stubs entry). Deliberately broad so every subsequent plan drops specs into `tests/phase26/` with NO further config edits — this is the single registration point per CLAUDE.md 2026-05-25. Add `tests/phase26/README.md` documenting: the project name, the run command `npx playwright test --project=phase26`, and the rule that any new spec must live under `tests/phase26/`. Verify discovery with `npx playwright test --list --project=phase26`.</action>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase26` runs without "no tests found" config error.
    - Adding a file under `tests/phase26/` is discovered by the project (verify by listing after Task 2 creates the spec).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --list --project=phase26</automated>
  </verify>
  <done>phase26 project registered; broad tests/phase26 regex; discovery confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the pre-phase convert golden fixture + parity spec</name>
  <files>scripts/capture-convert-golden.ts, tests/phase26/fixtures/convert-golden.json, tests/phase26/convert-golden-path.spec.ts</files>
  <read_first>
    - src/app/api/sops/parse/route.ts (the convert entry — orchestrates extract → gpt-parser → parsed-sop-to-layout-data → materializeJunctionsForLayout)
    - src/lib/parsers/parsed-sop-to-layout-data.ts (layout_data + block_provenance shape)
    - src/lib/parsers/materialize-junctions or materializeJunctionsForLayout() (sop_section_blocks rows)
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Validation Architecture (Wave 0 Gaps: golden fixture)
  </read_first>
  <action>Write `scripts/capture-convert-golden.ts`: pick a small known DOCX already in the repo test corpus (or add a minimal one under `tests/phase26/fixtures/`), run it through the SAME parse path the convert on-ramp uses, and serialize a canonical snapshot — for each section: `layout_data.content[]` (types + props including `id` normalized to a stable placeholder, `junctionId`, `block_provenance`), the materialized `sop_section_blocks` rows (kind + verified flags + provenance), and section order. Normalize only non-deterministic ids/timestamps to stable tokens so the diff is meaningful; keep everything else byte-exact. Write the snapshot to `tests/phase26/fixtures/convert-golden.json`. Then write `tests/phase26/convert-golden-path.spec.ts` that re-runs the capture and deep-equals it against the committed fixture. **CRITICAL: run this against current, pre-editor-change code so the fixture is a true pre-phase baseline** — this is why it lives in Wave 1 before the renderer swap. The spec is green now; it becomes the R6 regression guard after the bespoke swap.</action>
  <acceptance_criteria>
    - `tests/phase26/fixtures/convert-golden.json` exists and is non-empty (contains layout_data content, junction rows, provenance).
    - `npx playwright test --project=phase26 -g "golden"` passes at current HEAD.
    - Re-running the capture script twice produces byte-identical output (id/timestamp normalization is deterministic).
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/capture-convert-golden.ts --check && npx playwright test --project=phase26 -g "golden"</automated>
  </verify>
  <done>Golden fixture captured pre-phase; parity spec green at W0 head; deterministic re-capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo | Test-only code; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | golden fixture drift | accept | Fixture is committed + regenerated by a deterministic script; drift surfaces as a failing spec (that IS the intended signal) |
</threat_model>

<verification>
- `npx playwright test --list --project=phase26` discovers the new spec.
- `npx playwright test --project=phase26 -g "golden"` green at W0 head.
</verification>

<success_criteria>
phase26 project registered (broad regex); pre-phase convert golden fixture captured and parity-asserted green before any editor change.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-02-SUMMARY.md` when done
</output>
