---
phase: 26-sop-builder-redesign
plan: 09
type: execute
wave: 6
depends_on: ["26-05", "26-07"]
files_modified:
  - src/components/admin/builder-v2/visual/VisualBlock.tsx
  - src/components/admin/builder-v2/visual/MediaGrid.tsx
  - src/lib/builder/block-registry.tsx
  - src/lib/validators/blocks.ts
  - src/actions/introspection.ts
  - src/lib/builder/block-type-labels.ts
  - scripts/contract-check.ts
  - tests/phase26/visual-block.spec.ts
autonomous: true
requirements: [R5, R7]
user_setup: []
must_haves:
  truths:
    - "Inserting a Visual block offers photo / diagram / video; each stored item carries its medium tag (visual:photo|diagram|video)"
    - "The Visual block is registered in all THREE contract places (block-registry renderer + BLOCK_REGISTRY + BlockContentSchema) plus the label map, and scripts/contract-check passes"
    - "Legacy PhotoBlock/PhotoGridBlock/StepWithPhotosBlock photos RENDER THROUGH the Visual media component while their layout_data kind stays unchanged (convert golden-path byte-equivalence preserved — A3)"
    - "Each item's medium tag is exposed to /api/schema (agent-layer contract hook — D-02, no surfacing)"
  artifacts:
    - path: "src/components/admin/builder-v2/visual/VisualBlock.tsx"
      provides: "unified Visual block (mixed media, medium-tagged)"
      contains: "VisualBlock"
    - path: "src/lib/validators/blocks.ts"
      provides: "VisualBlockContentSchema (kind:'visual', items[]{medium,src,alt,caption,annotationId?})"
      contains: "visual"
  key_links:
    - from: "src/lib/builder/block-registry.tsx"
      to: "src/components/admin/builder-v2/visual/VisualBlock.tsx"
      via: "BLOCK_COMPONENTS.VisualBlock"
      pattern: "VisualBlock"
    - from: "src/actions/introspection.ts"
      to: "/api/schema"
      via: "BLOCK_REGISTRY.VisualBlock with medium tags"
      pattern: "VisualBlock"
---

<objective>
Add the unified **Visual** block (R5, D-03): one block holding mixed media (photo · diagram · video), each item medium-tagged `visual:photo|diagram|video`. This is the 5-edit contract — renderer (`block-registry`) + `BLOCK_REGISTRY` + Zod (`BlockContentSchema`) + label map + `contract-check` — plus a Pattern-E media grid with a medium sub-picker.

Two constraints shape it:
- **Convert-safety (A3):** legacy `PhotoBlock`/`PhotoGridBlock`/`StepWithPhotosBlock` photos RENDER THROUGH the Visual media component while their `layout_data` `kind` stays UNCHANGED — this gives the unified UI without rewriting layout_data at parse time, preserving R6 golden-path byte-equivalence. Do NOT transform photo kinds.
- **Agent hook (R7/D-02):** each item's medium tag is surfaced to `/api/schema` via `BLOCK_REGISTRY` — the contract the 26.5 agent layer consumes. NO agent-layer surfacing here.

Purpose: unified, tagged Visual media; contract-complete; convert-safe.
Output: `VisualBlock`, `MediaGrid`, `VisualBlockContentSchema`, 3-place + label + contract-check edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@src/lib/validators/blocks.ts
@src/actions/introspection.ts
@scripts/contract-check.ts
</context>

<artifacts>
This plan produces:
- `src/components/admin/builder-v2/visual/VisualBlock.tsx` — the Visual block component (worker+edit render)
- `src/components/admin/builder-v2/visual/MediaGrid.tsx` — Pattern E media grid + medium sub-picker
- `VisualBlockContentSchema` (kind `visual`) in `src/lib/validators/blocks.ts`
- `VisualBlock` entry in `BLOCK_REGISTRY` (`introspection.ts`) with medium tags
- `VisualBlock` in `block-registry` `BLOCK_COMPONENTS`/`BLOCK_DEFAULTS`, `BLOCK_TYPE_LABELS`, and `contract-check` handling
- `tests/phase26/visual-block.spec.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VisualBlock 3-place contract + label + contract-check (5-edit)</name>
  <files>src/lib/validators/blocks.ts, src/actions/introspection.ts, src/lib/builder/block-type-labels.ts, src/lib/builder/block-registry.tsx, scripts/contract-check.ts</files>
  <read_first>
    - src/lib/validators/blocks.ts (L181–214 — PhotoGrid schemas + the `BlockContentSchema` discriminatedUnion array to extend)
    - src/actions/introspection.ts (L71 BLOCK_REGISTRY entry shape `{schema, description, example}`)
    - src/lib/builder/block-type-labels.ts (L36 label map + L61 SLUG_TO_KEY)
    - scripts/contract-check.ts (EXCLUDED sets + the nameToKind map — VisualBlock IS junction-stored so needs a BlockContentSchema kind, unlike layout-only blocks)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § VisualBlock 3-place
  </read_first>
  <behavior>
    - `VisualBlockContentSchema = z.object({ kind: z.literal('visual'), items: z.array(z.object({ medium: z.enum(['photo','diagram','video']), src: z.string().nullable(), alt, caption, annotationId: z.string().uuid().optional() })) })` in the discriminated union.
    - `BLOCK_REGISTRY.VisualBlock` exposes the schema + example with medium tags (the /api/schema surfacing — R7 hook).
    - `block-registry` maps `VisualBlock → VisualBlock component` + defaults; label map + SLUG_TO_KEY include it; `contract-check` passes with VisualBlock in all three places.
  </behavior>
  <action>Add `VisualBlockContentSchema` (kind `visual`) to the `BlockContentSchema` discriminatedUnion in `blocks.ts`. Add `VisualBlock` to `BLOCK_REGISTRY` (`introspection.ts`) with the schema + a medium-tagged example (so `/api/schema` advertises the `visual:{medium}` tags — R7/D-02 contract hook, surfacing deferred). Add `VisualBlock` to `BLOCK_TYPE_LABELS` + `SLUG_TO_KEY` (P16 label source). Add `VisualBlock` to `block-registry` `BLOCK_COMPONENTS`/`BLOCK_DEFAULTS`. Update `scripts/contract-check.ts` so VisualBlock is required in all three places (its kind `visual` is NOT excluded — it is junction-stored). Run the prebuild contract-check to confirm the 5-edit is complete.</action>
  <acceptance_criteria>
    - `npx tsx scripts/contract-check.ts` exits 0 with VisualBlock in registry + BLOCK_REGISTRY + BlockContentSchema.
    - `GET /api/schema` (or the introspection unit) returns VisualBlock with medium enum tags.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/contract-check.ts && npx playwright test --project=phase26 -g "visual-block" && npx tsc --noEmit</automated>
  </verify>
  <done>VisualBlock contract-complete across all 5 edit sites; medium tags on /api/schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VisualBlock + MediaGrid (Pattern E) + render legacy photo kinds through it</name>
  <files>src/components/admin/builder-v2/visual/VisualBlock.tsx, src/components/admin/builder-v2/visual/MediaGrid.tsx, tests/phase26/visual-block.spec.ts</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Visual Block + Konva → Visual block on the canvas (Pattern E media grid, medium tag pills, ＋ add media sub-picker) + § Field-Editor Contract Pattern E
    - src/components/sop/blocks/PhotoGridBlock.tsx + StepWithPhotosBlock.tsx (media-grid render idioms to reuse)
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Visual+Konva → A3 convert-safety (render legacy kinds THROUGH Visual; do NOT transform layout_data)
  </read_first>
  <behavior>
    - `VisualBlock` renders items in a grid; each item shows its medium tag pill (photo cyan / diagram blue / video pink); `＋ add media` opens a medium sub-picker (photo upload / diagram / video). Diagram items carry `annotationId` (Konva editor opens in 26-11).
    - `MediaGrid` is the shared Pattern-E control used by VisualBlock AND by legacy photo blocks — `PhotoBlock`/`PhotoGridBlock`/`StepWithPhotosBlock` render their photos THROUGH `MediaGrid`/VisualBlock component, but their stored `layout_data` `kind` is unchanged.
    - Inserted Visual items carry the correct `medium` tag; provenance (`block_provenance`) is preserved on converted images (P4).
  </behavior>
  <action>Build `VisualBlock.tsx` (used by both worker read + admin edit — same component, R2) and `MediaGrid.tsx` (Pattern E). Route the render of `PhotoBlock`/`PhotoGridBlock`/`StepWithPhotosBlock` through the Visual media component (adapter mapping their photo arrays → Visual items with `medium:'photo'`) WITHOUT changing their `layout_data` kind — A3 convert-safety (this preserves the 26-02 golden fixture). Write `tests/phase26/visual-block.spec.ts` (behavioural): insert a Visual block, add a photo + a video, assert each stored item carries the right medium tag; render a converted `PhotoBlock` (with block_provenance) through Visual and assert the provenance survives and the layout_data kind is still `photo` (not rewritten).</action>
  <acceptance_criteria>
    - Behavioural: Visual items carry correct medium tags; sub-picker offers photo/diagram/video.
    - Legacy photo block rendered through Visual keeps its `layout_data` kind AND its `block_provenance` (A3 + P4).
    - Convert golden-path spec (26-02) still green (no layout_data drift).
    - `npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "visual-block|golden" && npm run build</automated>
  </verify>
  <done>Unified Visual media (tagged, mixed); legacy photos render through it without layout_data drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin media input → layout_data / /api/schema | Media items + medium tags cross into stored layout_data and the AI schema surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09-01 | Tampering | invalid medium/item persisted | mitigate | `VisualBlockContentSchema` validates items (medium enum, uuid annotationId) before write |
| T-26-09-02 | Info disclosure | agent metadata over-exposed via /api/schema | accept | /api/schema advertises block/medium tags only (contract hook, D-02); no per-SOP memory/PII in this layer |
| T-26-09-03 | Tampering | convert golden-path drift from photo-kind rewrite | mitigate | Render legacy kinds through Visual WITHOUT changing layout_data (A3); golden spec guards |
</threat_model>

<verification>
- contract-check green with VisualBlock in all 3 places.
- visual-block behavioural spec + convert golden spec green.
- `npm run build` green.
</verification>

<success_criteria>
Unified Visual block (R5) contract-complete + medium-tagged + convert-safe; medium tags on /api/schema as the R7/D-02 contract hook (no surfacing).
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-09-SUMMARY.md` when done
</output>
