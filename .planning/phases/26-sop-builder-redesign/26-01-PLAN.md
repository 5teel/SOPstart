---
phase: 26-sop-builder-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [R2, R5]
user_setup: []
must_haves:
  truths:
    - "The five bespoke-editor dependencies are installed and resolve at import"
    - "No dependency runs a network/filesystem postinstall"
    - "tsc is green after install (peer/version compatible with React 19.2.4 / Next 16)"
  artifacts:
    - path: "package.json"
      provides: "@dnd-kit/core@6.3.1, @dnd-kit/sortable@10.0.0, @dnd-kit/modifiers@9.0.0, konva@10.3.0, react-konva@19.2.5 in dependencies"
      contains: "react-konva"
  key_links:
    - from: "package.json"
      to: "node_modules/react-konva"
      via: "npm install"
      pattern: "react-konva"
---

<objective>
Gate and install the five NEW dependencies the full-bespoke editor (D-01) and the absorbed Konva annotation (D-03) require: `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/modifiers` (block reorder), `konva`, `react-konva` (diagram annotation — admin-only, dynamic-imported). All five are `[ASSUMED]` in the 26-RESEARCH Package Legitimacy Audit (slopcheck unavailable), so a blocking human-verify checkpoint precedes the install.

This is the v5.0-opener (D-04) foundation install; it enables the reorder path (R2) and the Visual/Konva path (R5) without touching the worker bundle (nothing imports these until later waves).

Purpose: unblock Waves 3+ (dnd-kit) and Waves 7+ (konva) with vetted, version-pinned deps.
Output: five packages in `package.json`, lockfile updated, tsc green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
@.planning/phases/26-sop-builder-redesign/26-CONTEXT.md
</context>

<artifacts>
This plan produces (new symbols / state):
- `package.json` dependencies: `@dnd-kit/core@6.3.1`, `@dnd-kit/sortable@10.0.0`, `@dnd-kit/modifiers@9.0.0`, `konva@10.3.0`, `react-konva@19.2.5`
- Updated `package-lock.json`
</artifacts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy for the five [ASSUMED] deps</name>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Package Legitimacy Audit (the [ASSUMED] table + peer-dep sanity)
  </read_first>
  <what-built>
    Nothing built yet — this is the pre-install legitimacy gate. RESEARCH tagged all five `[ASSUMED]` because slopcheck could not run in this environment; registry existence + peer deps were verified via `npm view` only.
  </what-built>
  <action>Pause before any install. Present the five [ASSUMED] packages to the human for legitimacy confirmation per How-to-verify (registry page + empty postinstall). Do NOT run `npm install` until the human approves; this checkpoint is never auto-approvable (workflow.auto_advance ignored per the package-legitimacy gate).</action>
  <how-to-verify>
    1. Confirm each package on its registry page (all MIT, high-download, established):
       - npmjs.com/package/@dnd-kit/core (6.3.1)
       - npmjs.com/package/@dnd-kit/sortable (10.0.0)
       - npmjs.com/package/@dnd-kit/modifiers (9.0.0)
       - npmjs.com/package/konva (10.3.0)
       - npmjs.com/package/react-konva (19.2.5 — peer react ^19.2.0, project is 19.2.4)
    2. Confirm none declares a network/filesystem `postinstall` (run `npm view <pkg> scripts.postinstall` — expect empty for all five).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all five packages are legitimate and postinstall-free, OR names a substitute.
    - This checkpoint is NOT auto-approvable (workflow.auto_advance ignored) per the package-legitimacy gate.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name a substitute package.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the five pinned dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current deps + engines: node >=20; do NOT move konva/dnd-kit into optionalDependencies — they are cross-platform, unlike the Windows-only Tailwind/lightningcss deps)
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Standard Stack → Installation
  </read_first>
  <action>Run `npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0 @dnd-kit/modifiers@9.0.0 konva@10.3.0 react-konva@19.2.5` from the repo root. These are regular `dependencies` (cross-platform; the [2026-04-04] Windows-only-optional-dep rule does NOT apply). Do NOT import any of them from application code in this plan — konva/react-konva must stay out of every bundle until the admin-only dynamic import lands in the Konva wave. After install, re-verify `npm view <pkg> scripts.postinstall` printed nothing for all five, then run `npx tsc --noEmit` to confirm no peer/type break with React 19.2.4.</action>
  <acceptance_criteria>
    - `package.json` lists all five at the pinned versions under `dependencies`.
    - `node -e "require.resolve('react-konva'); require.resolve('@dnd-kit/sortable')"` exits 0.
    - `npx tsc --noEmit` exits 0.
    - No application file imports konva/react-konva yet (`grep -rl "react-konva" src/ || true` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>node -e "['@dnd-kit/core','@dnd-kit/sortable','@dnd-kit/modifiers','konva','react-konva'].forEach(p=>require.resolve(p))" && npx tsc --noEmit</automated>
  </verify>
  <done>All five deps resolve; tsc green; nothing imports konva in src/ yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build machine | Untrusted third-party code enters the supply chain at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-SC | Tampering | npm install of 5 [ASSUMED] packages | mitigate | Blocking human-verify checkpoint (Task 1) before install; version-pinned; postinstall confirmed empty; never auto-approvable |
</threat_model>

<verification>
- `npx tsc --noEmit` green.
- No worker-bundle impact yet (nothing imports the new deps).
</verification>

<success_criteria>
Five pinned deps installed, legitimacy human-verified, tsc green, no code imports them yet.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-01-SUMMARY.md` when done
</output>
