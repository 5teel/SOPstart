---
phase: 26-sop-builder-redesign
plan: 06
type: execute
wave: 4
depends_on: ["26-04"]
files_modified:
  - src/components/admin/builder-v2/fields/field-map.ts
  - src/components/admin/builder-v2/fields/EnumChip.tsx
  - src/components/admin/builder-v2/fields/InlineToken.tsx
  - src/components/admin/builder-v2/BlockEditShell.tsx
  - tests/phase26/field-inline-patterns.spec.ts
autonomous: true
requirements: [R2]
user_setup: []
must_haves:
  truths:
    - "Every contentEditable text/textarea field (Pattern A) is editable inline on the block and commits valid layout_data"
    - "Every single-enum field (Pattern B) renders as a chip that cycles (≤3 opts) or opens a tiny menu (>3), writing the enum value"
    - "Every inline numeric/range token (Pattern D) is type-in-place and writes the number/tolerance"
    - "field-map declares, per block type, which field uses which of the 5 patterns (A/B/C/D/E) — the reachability source of truth"
  artifacts:
    - path: "src/components/admin/builder-v2/fields/field-map.ts"
      provides: "per-block field→pattern declaration driving the shell + parity tests"
      contains: "field-map"
    - path: "src/components/admin/builder-v2/fields/EnumChip.tsx"
      provides: "Pattern B enum chip (cycle / anchored menu)"
      contains: "EnumChip"
    - path: "src/components/admin/builder-v2/fields/InlineToken.tsx"
      provides: "Pattern D inline editable numeric/range token"
      contains: "InlineToken"
  key_links:
    - from: "src/components/admin/builder-v2/BlockEditShell.tsx"
      to: "src/components/admin/builder-v2/fields/field-map.ts"
      via: "shell renders field controls per block's pattern map"
      pattern: "field-map"
---

<objective>
Begin the P14 RE-IMPLEMENT (all field editors bespoke — D-01, no Puck popover). This wave delivers the three INLINE patterns and the field-map that drives all reachability:
- **Pattern A** (inline contentEditable) — already have `InlineText` from 26-04; wire it per block.
- **Pattern B** (enum chip) — `EnumChip`: ≤3 options cycle-on-click (sketch `cycleSev`), >3 open a small anchored enum menu; colour follows the block's semantic accent.
- **Pattern D** (inline token) — `InlineToken`: dashed-underline type-in-place span for numeric/range values (unit, tolerance `8.0–8.4`, step number, maxDurationSec).

The `field-map.ts` declares per-block-type which field uses which of the 5 patterns (A/B/C/D/E) — the single source the shell renders from AND the reachability checklist the parity tests (26-07) assert against. Field shapes are frozen by each block's `*PropsSchema` Zod (`src/lib/validators/blocks.ts`); reuse them as validation. The rule (SPEC acceptance): **no field editable under Puck may become unreachable.**

Purpose: inline-editable fields for the simple/common types, plus the pattern registry.
Output: `field-map.ts`, `EnumChip`, `InlineToken`, shell wiring for Pattern A/B/D blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@src/lib/validators/blocks.ts
@src/lib/builder/puck-config.tsx
</context>

<artifacts>
This plan produces:
- `src/components/admin/builder-v2/fields/field-map.ts` — `FIELD_MAP: Record<blockType, { field: string; pattern: 'A'|'B'|'C'|'D'|'E'; enum?: string[] }[]>`
- `src/components/admin/builder-v2/fields/EnumChip.tsx` (Pattern B)
- `src/components/admin/builder-v2/fields/InlineToken.tsx` (Pattern D)
- `BlockEditShell` wiring for Pattern A/B/D fields
- `tests/phase26/field-inline-patterns.spec.ts`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: field-map — per-block field→pattern registry (reachability source of truth)</name>
  <files>src/components/admin/builder-v2/fields/field-map.ts</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Field-Editor Contract → the "Per-block mapping (all 18 registered types)" table (the authoritative field→pattern list)
    - src/lib/builder/puck-config.tsx (per-block `fields:` maps — the exact Puck-editable field set = reachability checklist)
    - src/lib/validators/blocks.ts (each block's `*ContentSchema` — the field shapes/enums)
  </read_first>
  <action>Create `field-map.ts` transcribing the UI-SPEC per-block mapping table into `FIELD_MAP`: for each of the 18 registered block types, list every Puck-editable field with its assigned pattern (A/B/C/D/E) and, for enums, the option list from the Zod schema. This file is the single reachability source: the shell renders from it and the 26-07 parity tests assert `unreachable count === 0` per block against it. Include EVERY field the current Puck `fields` map exposes — cross-check each block's `fields:` object so nothing is dropped (SPEC acceptance: no Puck-editable field becomes unreachable).</action>
  <acceptance_criteria>
    - `FIELD_MAP` has an entry for all 18 registered block types.
    - For each block, the set of fields in FIELD_MAP === the set of keys in that block's puck-config `fields:` map (a unit test compares the two key sets).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "field-map" && npx tsc --noEmit</automated>
  </verify>
  <done>field-map covers every Puck-editable field of all 18 blocks; parity with puck-config fields verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EnumChip (B) + InlineToken (D) primitives + Pattern-A/B/D shell wiring</name>
  <files>src/components/admin/builder-v2/fields/EnumChip.tsx, src/components/admin/builder-v2/fields/InlineToken.tsx, src/components/admin/builder-v2/BlockEditShell.tsx, tests/phase26/field-inline-patterns.spec.ts</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Field-Editor Contract (Pattern B chip: cycle ≤3 / menu >3, semantic accent; Pattern D token: dashed underline, mono) + § Color (semantic accent per block)
    - src/components/admin/builder-v2/InlineText.tsx (Pattern A, from 26-04 — the commit-on-blur idiom to mirror)
    - src/components/admin/builder-v2/fields/field-map.ts (Task 1)
  </read_first>
  <behavior>
    - `EnumChip`: renders the current enum value as a pill; ≤3 options → click cycles to next; >3 options → click opens a small anchored menu; selecting commits `updateBlockProps(id, {field: value})`. Chip colour = block semantic accent.
    - `InlineToken`: dashed-underline editable span (mono); type-in-place, commits a validated number/range on blur; rejects non-numeric (keeps prior value).
    - Shell renders, per FIELD_MAP, Pattern-A fields via `InlineText`, Pattern-B via `EnumChip`, Pattern-D via `InlineToken`. (Pattern C/E deferred to 26-07/26-09.)
  </behavior>
  <action>Build `EnumChip.tsx` and `InlineToken.tsx` per the UI-SPEC. In `BlockEditShell.tsx`, iterate the block's FIELD_MAP entries and render Pattern A → `InlineText`, B → `EnumChip`, D → `InlineToken`; each commits through `content-ops.updateBlockProps` (lossless merge). Validate committed values against the block's `*PropsSchema` before commit (reuse the Zod schema; on invalid, keep prior value). Ponytail: these are minimum inputs matching the Zod schema — NOT a form framework. Write `tests/phase26/field-inline-patterns.spec.ts` (behavioural): for a Hazard block, cycle the severity `EnumChip` critical→warning→notice and assert `layout_data` writes the enum; for a Measurement block, type a unit into the `InlineToken` and assert it writes; for a Callout, edit title+body inline (A) and assert both persist.</action>
  <acceptance_criteria>
    - field-inline-patterns spec: enum cycle writes value; inline token writes number/range; Pattern-A dual fields persist — all as valid layout_data.
    - Invalid token input keeps prior value (Zod-guarded).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "field-inline-patterns" && npx tsc --noEmit</automated>
  </verify>
  <done>Pattern A/B/D fields inline-editable + Zod-validated; commit through the lossless reducer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin field input → layout_data | Field values cross into stored layout_data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Tampering | invalid enum/number injected via field control | mitigate | Each commit validated against the block's `*PropsSchema` Zod before write; invalid → prior value kept |
| T-26-06-02 | Tampering | XSS via inline field text | mitigate | textContent commit (never innerHTML) — inherited from InlineText (26-04) |
</threat_model>

<verification>
- field-map key-set parity with puck-config fields (all 18 blocks).
- field-inline-patterns behavioural spec green.
- `npx tsc --noEmit` green.
</verification>

<success_criteria>
Pattern A/B/D field editors bespoke and inline (P14 partial, D-01); field-map is the reachability source of truth covering all 18 blocks; no field dropped.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-06-SUMMARY.md` when done
</output>
