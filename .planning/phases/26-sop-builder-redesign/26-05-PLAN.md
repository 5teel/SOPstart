---
phase: 26-sop-builder-redesign
plan: 05
type: execute
wave: 3
depends_on: ["26-03", "26-01"]
files_modified:
  - next.config.ts
  - supabase/migrations/00039_sop_image_annotations.sql
  - src/types/database.types.ts
  - src/components/admin/builder-v2/visual/AnnotationEditor.tsx
  - src/components/admin/builder-v2/visual/AnnotationEditorLoader.tsx
  - scripts/check-bundle-size.ts
  - tests/phase26/konva-worker-isolation.spec.ts
autonomous: false
requirements: [R5, R8]
user_setup: []
must_haves:
  truths:
    - "react-konva renders a Stage in a Next.js 16 admin route via dynamic({ssr:false}) with 'canvas' externalized (the day-1 spike passes)"
    - "sop_image_annotations exists in the live DB: append-only, org-scoped RLS read, NO authenticated write, NO cross-table reference to public.sops (42P17-safe)"
    - "Konva + react-konva are absent from the /sops/[sopId] (worker) First Load JS manifest"
  artifacts:
    - path: "supabase/migrations/00039_sop_image_annotations.sql"
      provides: "annotation scene store (scene jsonb, natural dims, baked path, org_id, RLS)"
      contains: "sop_image_annotations"
    - path: "src/components/admin/builder-v2/visual/AnnotationEditorLoader.tsx"
      provides: "dynamic({ssr:false}) admin-only loader — Konva never in worker bundle"
      contains: "ssr: false"
    - path: "next.config.ts"
      provides: "'canvas' in serverExternalPackages"
      contains: "canvas"
  key_links:
    - from: "src/components/admin/builder-v2/visual/AnnotationEditorLoader.tsx"
      to: "src/components/admin/builder-v2/visual/AnnotationEditor.tsx"
      via: "dynamic import ssr:false"
      pattern: "dynamic"
---

<objective>
Lay the Konva foundation for the absorbed Phase 17 diagram annotation (D-03) and de-risk the one MEDIUM-confidence assumption up front: **does react-konva render in Next.js 16** (RESEARCH A2 — verified on Next 15 only; day-1 spike required). This wave runs in parallel with the edit-canvas wave (disjoint files) so the Konva risk surfaces early, not at wave 8.

Three foundation pieces: (1) `'canvas'` externalization + a dynamic `{ssr:false}` `AnnotationEditor` that mounts a real `<Stage>` on an admin route (the spike); (2) migration `00039_sop_image_annotations` — the scene store, copying the 00038 append-only + org-scoped-RLS + NO-cross-table pattern (42P17-safe, service-role writes only); (3) the HARD constraint gate — **workers NEVER download Konva** (D-03): extend the bundle check + a no-static-import lint so Konva can never enter `/sops/[sopId]` (R8 bundle isolation).

Purpose: prove Konva works in-stack + create its data + fence it out of the worker bundle before building primitives.
Output: canvas externalization, dynamic AnnotationEditor spike, `00039` migration (pushed), Konva-worker-isolation gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
@.planning/research/v3.0-image-annotation.md
@next.config.ts
</context>

<artifacts>
This plan produces:
- `next.config.ts` with `'canvas'` appended to `serverExternalPackages`
- `supabase/migrations/00039_sop_image_annotations.sql` — table + RLS + indexes
- `src/types/database.types.ts` extension for `sop_image_annotations`
- `src/components/admin/builder-v2/visual/AnnotationEditor.tsx` — 'use client' Konva `<Stage>` spike shell
- `src/components/admin/builder-v2/visual/AnnotationEditorLoader.tsx` — `dynamic({ssr:false})` admin-only loader
- `scripts/check-bundle-size.ts` extension asserting Konva absent from worker manifest
- `tests/phase26/konva-worker-isolation.spec.ts` — no-static-import lint (registered under phase26)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Konva-in-Next-16 spike — canvas externalization + dynamic Stage</name>
  <files>next.config.ts, src/components/admin/builder-v2/visual/AnnotationEditor.tsx, src/components/admin/builder-v2/visual/AnnotationEditorLoader.tsx</files>
  <read_first>
    - next.config.ts (current `serverExternalPackages: ['officeparser','file-type','sharp','@anthropic-ai/sdk','ffmpeg-static']` — append 'canvas')
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Pitfall 5 (canvas module + StrictMode leaks) + § Code Examples → Konva admin-only dynamic import
    - .planning/research/v3.0-image-annotation.md (Konva Stage/Layer setup, ssr:false pattern)
  </read_first>
  <action>Append `'canvas'` to `serverExternalPackages` in `next.config.ts`. Create `AnnotationEditor.tsx` ('use client', starts with the directive): a minimal `<Stage><Layer><Rect/></Layer></Stage>` from `react-konva`, sized to a passed natural width/height, with `stage.destroy()` in the effect cleanup (StrictMode leak guard, Pitfall 5). Create `AnnotationEditorLoader.tsx` that `dynamic(() => import('./AnnotationEditor'), { ssr:false, loading: () => <div>Loading annotator…</div> })` and is imported ONLY from admin builder-v2 code. This is the spike: a real Stage must mount and `next build` must succeed. **If `next build` fails on the `canvas` module or react-konva under Next 16 --webpack, STOP and escalate the RESEARCH Open-Q1 fallback chain (Excalidraw → custom SVG) — do not force a workaround.**</action>
  <acceptance_criteria>
    - `npm run build` succeeds with `'canvas'` externalized and react-konva in the graph.
    - A smoke render (behavioural, chromium): the loader mounts and a `<canvas>` element appears (Stage rendered) with no console error.
    - `AnnotationEditor.tsx` cleans up (`stage.destroy()` on unmount).
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test --project=phase26 -g "konva.*spike|annotation.*mount"</automated>
  </verify>
  <done>react-konva renders in Next 16 via dynamic ssr:false; canvas externalized; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 00039 sop_image_annotations (append-only, org-scoped, 42P17-safe)</name>
  <files>supabase/migrations/00039_sop_image_annotations.sql, src/types/database.types.ts</files>
  <read_first>
    - supabase/migrations/00038_phase23_schema.sql (L48–80 — the append-only + RLS pattern to copy EXACTLY; `current_organisation_id()` helper)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § 00039 migration entry
    - CLAUDE.md learning 2026-05-13 (RLS recursion — NO cross-table reference to public.sops)
  </read_first>
  <action>Write `00039_sop_image_annotations.sql`: table `sop_image_annotations` with `id uuid pk default gen_random_uuid()`, `sop_image_id uuid` (FK to sop_images), `scene jsonb not null`, `natural_width int`, `natural_height int`, `baked_storage_path text`, `baked_at timestamptz`, `organisation_id uuid not null` (FK organisations), `created_at`/`updated_at`. Index on `organisation_id` and `sop_image_id`. Enable RLS; add ONLY a `for select to authenticated using (organisation_id = public.current_organisation_id())` policy. Add NO authenticated INSERT/UPDATE/DELETE (writes go through `createAdminClient()` in the annotations server action — 26-13 — which self-enforces org-scope, per CLAUDE.md 2026-06-15). CRITICAL: the RLS predicate uses only `current_organisation_id()` (a SECURITY DEFINER helper) — NO `exists(select … from public.sops)` cross-table reference (42P17 recursion — CLAUDE.md 2026-05-13). Extend `src/types/database.types.ts` with the `sop_image_annotations` row/insert/update types (manual extension — type regen unavailable, consistent with prior phases).</action>
  <acceptance_criteria>
    - Migration file present; RLS enabled with exactly one SELECT policy; no authenticated write policy; no cross-table `public.sops` reference in any policy.
    - `database.types.ts` includes `sop_image_annotations`.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/00039_sop_image_annotations.sql','utf8'); if(!/enable row level security/i.test(s)||/from\s+public\.sops/i.test(s)) process.exit(1)" && npx tsc --noEmit</automated>
  </verify>
  <done>00039 written: append-only, org-scoped SELECT-only RLS, 42P17-safe; types extended.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Apply schema push + Konva-worker-isolation gate</name>
  <files>scripts/check-bundle-size.ts, tests/phase26/konva-worker-isolation.spec.ts</files>
  <read_first>
    - scripts/check-bundle-size.ts (how it inspects the /sops/[sopId] manifest / findSymbolInBuildOutput; the phase15 chunk-existence pattern)
    - tests/phase15 no-static-desktop-import spec (the lint-guard shape to mirror for a no-static-Konva-import guard)
    - CLAUDE.md learnings 2026-05-08 (SUPABASE_ACCESS_TOKEN non-TTY push) + 2026-05-25 (register the lint spec in a project regex — it lands under phase26)
  </read_first>
  <action>**[BLOCKING] Schema push:** run `supabase db push` to apply 00039 to the live DB (non-TTY: `SUPABASE_ACCESS_TOKEN` from `.env.local`, as prior phases auto-completed). This is required BEFORE verification — build/types pass without it (types come from config, not the live DB) creating a false positive. If the token is absent, STOP and surface a human checkpoint to run the push. Then extend `scripts/check-bundle-size.ts` to assert `konva`/`react-konva` symbols are ABSENT from the `/sops/[sopId]/page` First Load manifest (mirroring the existing chunk-existence logic). Add `tests/phase26/konva-worker-isolation.spec.ts` — a source-lint mirroring `no-static-desktop-import`: assert no file OUTSIDE `src/components/admin/builder-v2/visual/` statically imports `konva`/`react-konva`/`AnnotationEditor` (only the `dynamic()` loader may reference it). This spec runs under the phase26 project (already registered in 26-02 — no config edit).</action>
  <acceptance_criteria>
    - `sop_image_annotations` exists in the live DB (query returns the table / RLS present).
    - `npm run build` green with the extended bundle check asserting Konva absent from the worker manifest.
    - konva-worker-isolation spec passes (no static Konva import outside `visual/`).
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test --project=phase26 -g "konva-worker-isolation"</automated>
  </verify>
  <done>00039 pushed to live DB; worker bundle proven Konva-free by gate + lint (D-03 hard constraint, R8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → sop_image_annotations | Annotation scene data (org-scoped) — writes deferred to service-role action (26-13) |
| admin Konva bundle → worker route | Konva must never cross into the worker tier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Elevation | RLS recursion (42P17) from cross-table policy | mitigate | Policy uses only `current_organisation_id()` SECURITY DEFINER helper; NO reference to public.sops (CLAUDE.md 2026-05-13) |
| T-26-05-02 | Tampering | authenticated direct write to annotations table | mitigate | No authenticated write policy; writes only via service-role action with self-enforced org-scope (26-13) |
| T-26-05-03 | DoS-adjacent | Konva leaking into worker bundle | mitigate | serverExternalPackages + dynamic ssr:false + bundle gate + no-static-import lint (D-03) |
</threat_model>

<verification>
- `npm run build` green with Konva-absent worker-manifest assertion.
- 00039 applied to live DB (schema push confirmed).
- konva-worker-isolation lint green.
</verification>

<success_criteria>
Konva proven in Next 16 (admin, dynamic); annotation scene store exists in the live DB (append-only, org-scoped, 42P17-safe); workers provably never download Konva (R5/D-03/R8).
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-05-SUMMARY.md` when done
</output>
