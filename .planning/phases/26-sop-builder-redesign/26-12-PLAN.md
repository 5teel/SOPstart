---
phase: 26-sop-builder-redesign
plan: 12
type: execute
wave: 8
depends_on: ["26-10"]
files_modified:
  - src/components/admin/builder-v2/BlockEditShell.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
  - tests/phase26/selection-sync.spec.ts
  - tests/phase26/ai-overlay.spec.ts
  - tests/phase26/verify-gate.spec.ts
autonomous: true
requirements: [R8]
user_setup: []
must_haves:
  truths:
    - "Selecting a block on the bespoke canvas fires setActiveProvenance(region, junctionId) and highlights its source region; clicking a source region focuses the block (P12 RE-WIRE)"
    - "A block with an open AI flag renders the badge + inline ReviewerFlagsPanel; a verified block renders no flag (P13 RE-IMPLEMENT, components reused)"
    - "A Heading block whose text starts 'Unanchored figures…' shows the Reference-images chip (P9)"
    - "Leaving any block unverified makes the publish POST return 400 unverified_blocks; the no-bulk-verify lint guard still passes (P8 KEEP server, RE-IMPLEMENT UI)"
  artifacts:
    - path: "src/components/admin/builder-v2/BlockEditShell.tsx"
      provides: "selection-sync fire on focus, AI-flag overlay, orphan chip, per-block verify affordance"
      contains: "setActiveProvenance"
  key_links:
    - from: "src/components/admin/builder-v2/BlockEditShell.tsx"
      to: "useSelectionSync (setActiveProvenance / registerBlockClickHandler)"
      via: "on focus + data-block-id reverse binding"
      pattern: "setActiveProvenance"
    - from: "src/components/admin/builder-v2/BlockEditShell.tsx"
      to: "sop_section_blocks junction rows (componentIdToJunction map)"
      via: "ReviewerFlagsPanel / verify affordance keyed by junctionId"
      pattern: "componentIdToJunction"
---

<objective>
Re-earn the three UI bindings Puck's `componentOverlay` gave for free (D-01 full-bespoke), plus the verify UI — all keyed off the SAME `componentIdToJunction` map already built in `BuilderClient` (portable `useMemo`). Every item here is a Preservation-Checklist RE-WIRE/RE-IMPLEMENT and gets a BEHAVIOURAL parity test (CLAUDE.md 2026-06-05 — presence tests are exactly the blind spot that shipped dead features before):
- **P12 (RE-WIRE):** `useSelectionSync` context UNCHANGED; the caller moves — `BlockEditShell` fires `onItemSelected({componentId, junctionId})` on focus → `setActiveProvenance`; reverse via `registerBlockClickHandler` querying `[data-block-id]`.
- **P13 (RE-IMPLEMENT, thin):** reuse `PuckItemBadgeOverlay` + `ReviewerFlagsPanel` AS-IS inside the shell, keyed by junctionId.
- **P9 (RE-IMPLEMENT, thin):** the "Reference images" chip on `Unanchored figures…` heading blocks.
- **P8 (KEEP server · RE-IMPLEMENT UI):** re-mount `VerifyChecklistGate` + a per-block verify affordance; the server publish gate (400 `unverified_blocks`) is untouched and authoritative; the no-bulk-verify lint guard still passes (R8).

Purpose: full parity with Puck's overlay/selection/verify surface, proven behaviourally.
Output: re-wired `BlockEditShell`/`BuilderClient`; selection-sync + AI-overlay + verify-gate parity specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
@src/components/admin/source-viewer/useSelectionSync.tsx
</context>

<artifacts>
This plan produces:
- Re-wired `BlockEditShell` (selection fire, AI-flag overlay, orphan chip, per-block verify)
- `BuilderClient` mounting `VerifyChecklistGate` + passing `componentIdToJunction`/selection callbacks to `EditableDocument`
- `tests/phase26/selection-sync.spec.ts`, `ai-overlay.spec.ts`, `verify-gate.spec.ts` (behavioural parity)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: P12 selection-sync re-wire (canvas ↔ source, behavioural)</name>
  <files>src/components/admin/builder-v2/BlockEditShell.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx, tests/phase26/selection-sync.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (L251–313 selection wiring; L280 `setActiveProvenance(region, junctionId)`; L289–313 `registerBlockClickHandler` → `[data-puck-item-id]` reverse binding to re-point at `[data-block-id]`; L203 `componentIdToJunction`)
    - src/components/admin/source-viewer/useSelectionSync.tsx (context — UNCHANGED; only the caller moves)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § BlockEditShell (P12) + § Code Examples → Selection re-wire
  </read_first>
  <behavior>
    - Focusing/selecting a block fires `onItemSelected({componentId: props.id, junctionId})`, which resolves the region from `junctionMap` and calls `setActiveProvenance(region, junctionId)` → the source region highlights + scrolls.
    - Clicking a source region calls the registered handler → `document.querySelector('[data-block-id="…"]')` focuses the block.
    - Non-convert SOPs show none of this.
  </behavior>
  <action>Move the selection wiring from the (removed) Puck `componentOverlay` into `BlockEditShell`: fire `onItemSelected` on block focus using `props.id` + `props.junctionId`; pass the `setActiveProvenance` resolver + `registerBlockClickHandler` from `BuilderClient` (reuse the existing `junctionMap`/`componentIdToJunction` useMemo). Re-point the reverse handler's selector from `[data-puck-item-id]` to `[data-block-id]` (the shell already renders `data-block-id` from 26-04). `useSelectionSync` context stays untouched. Write `tests/phase26/selection-sync.spec.ts` (behavioural): select a converted block, assert `setActiveProvenance` fired with the correct region+junctionId (spy) and the source region gets the active highlight; click a source region, assert the matching `[data-block-id]` block receives focus.</action>
  <acceptance_criteria>
    - selection-sync spec: block select → setActiveProvenance(region, junctionId) fired; source click → block focused (behavioural, not a grep).
    - `useSelectionSync.tsx` unchanged (git diff empty for that file).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "selection-sync" && npx tsc --noEmit</automated>
  </verify>
  <done>Canvas↔source selection-sync re-wired to the bespoke shell; context unchanged; parity proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P13 AI-flag overlay + P9 orphan-image chip (reused components)</name>
  <files>src/components/admin/builder-v2/BlockEditShell.tsx, tests/phase26/ai-overlay.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (L410–414 inline `<ReviewerFlagsPanel sopId blockId={junctionId} />`; L203 `componentIdToJunction`; L225 `componentIdToProps` for the Unanchored-figures check)
    - src/lib/builder/puck-config.tsx `createPuckOverrides.componentOverlay` (L970–1063 — the badge/overlay logic to relocate; `PuckItemBadgeOverlay` component to reuse)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Review Overlays (badge in header, one panel expanded at a time, purple --ai) + orphan chip
  </read_first>
  <behavior>
    - A block with an open AI flag renders the `PuckItemBadgeOverlay` count badge in its header + an expandable inline `ReviewerFlagsPanel` (keyed by junctionId); a verified/clean block renders no flag. Only one flags panel expanded at a time.
    - A Heading block whose `props.text` starts `Unanchored figures…` renders the dashed "Reference images" chip (P9).
  </behavior>
  <action>Render `PuckItemBadgeOverlay` + `ReviewerFlagsPanel` INSIDE `BlockEditShell`, keyed off the portable `componentIdToJunction` map (reuse both components AS-IS — thin re-implement). Add the `startsWith('Unanchored figures')` chip on Heading blocks (per `componentIdToProps`). Badges sit inside the block header (never floating — z-order per UI-SPEC). Write `tests/phase26/ai-overlay.spec.ts` (behavioural): render a block with an open reviewer flag → assert badge + panel render; render a verified block → assert NO flag; render an `Unanchored figures…` heading → assert the chip appears. Not "the file contains ReviewerFlagsPanel."</action>
  <acceptance_criteria>
    - ai-overlay spec: open-flag block → badge + panel; verified block → none; orphan heading → chip (behavioural).
    - Only one flags panel expands at a time.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "ai-overlay" && npx tsc --noEmit</automated>
  </verify>
  <done>AI-flag overlay + orphan chip re-implemented (components reused); parity proven behaviourally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: P8 per-block verify UI + publish-gate regression (server KEEP)</name>
  <files>src/components/admin/builder-v2/BlockEditShell.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx, tests/phase26/verify-gate.spec.ts</files>
  <read_first>
    - src/app/api/sops/[sopId]/publish/route.ts (server 400 `unverified_blocks` — KEEP untouched; the authoritative gate)
    - src/hooks/useVerifyChecklist + VerifyChecklistGate (the verify UI + action to re-mount)
    - tests/phase15 no-bulk-verify-ui.spec.ts (the R8 lint guard — must still pass: no bulk-verify affordance anywhere in src/)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Review Overlays (per-block verify chip; NO bulk-verify affordance)
  </read_first>
  <behavior>
    - Each block has an individual verify affordance writing through the existing verify action; there is NO bulk-verify affordance anywhere (R8 lint guard).
    - Leaving any block unverified → publish POST returns 400 `unverified_blocks`; the UI only disables the Continue→Publish button.
    - The server publish route is unchanged.
  </behavior>
  <action>Re-mount `VerifyChecklistGate` in the bespoke shell and add a per-block verify affordance that writes through the EXISTING verify action (server gate untouched — P8 KEEP server). Ensure the UI never offers a bulk-verify control (the no-bulk-verify lint guard from Phase 21 must stay green — R8). Write `tests/phase26/verify-gate.spec.ts` (behavioural/integration): with one block's `verified_by_admin_id IS NULL`, POST publish → assert 400 `unverified_blocks`; verify all blocks → assert publish succeeds. Reuse/extend the existing publish-gate spec rather than duplicating the server assertion.</action>
  <acceptance_criteria>
    - verify-gate spec: unverified → publish 400 `unverified_blocks`; all-verified → publish OK (behavioural).
    - no-bulk-verify lint guard still green (no bulk affordance introduced).
    - publish route file unchanged (git diff empty).
    - `npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "verify-gate" && npx playwright test -g "no-bulk-verify" && npm run build</automated>
  </verify>
  <done>Per-block verify UI re-implemented; server gate KEEP + 400 behaviour proven; no-bulk-verify intact (R8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify affordance → publish gate | Per-block verify state gates publish (server-authoritative) |
| reviewer flags (server-fetched) → overlay | Flag data crosses into the admin canvas read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-12-01 | Elevation | client bypasses verify gate to publish | mitigate | Server publish route (400 `unverified_blocks`) is authoritative and unchanged (P8 KEEP); UI only disables the button |
| T-26-12-02 | Tampering | bulk-verify affordance reintroduced | mitigate | no-bulk-verify lint guard re-run in acceptance (R8) |
| T-26-12-03 | Info disclosure | reviewer flags rendered for another org | mitigate | Flags keyed off junction rows already org-scoped server-side; overlay read-only |
</threat_model>

<verification>
- selection-sync + ai-overlay + verify-gate behavioural specs green.
- no-bulk-verify lint + publish route unchanged.
- `npm run build` green.
</verification>

<success_criteria>
P12/P13/P9/P8 re-earned in the bespoke canvas with behavioural parity (D-01, CLAUDE.md 2026-06-05); server gate untouched; no-bulk-verify intact (R8).
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-12-SUMMARY.md` when done
</output>
