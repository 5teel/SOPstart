---
phase: 26-sop-builder-redesign
plan: 08
type: execute
wave: 6
depends_on: ["26-07"]
files_modified:
  - src/components/admin/builder-v2/inserter/InserterMenu.tsx
  - src/components/admin/builder-v2/inserter/inserter-model.ts
  - src/components/admin/builder-v2/inserter/ReuseTier.tsx
  - src/components/admin/builder-v2/EditableDocument.tsx
  - src/components/admin/builder-v2/BlockEditShell.tsx
  - tests/phase26/inserter.spec.ts
autonomous: true
requirements: [R3]
user_setup: []
must_haves:
  truths:
    - "Opening ＋ in a Hazards section yields a different 'Fits here' list than opening it in a Steps section (context-aware by section render-family)"
    - "The menu is keyboard-operable: ↑↓ move highlight, ↵ insert-or-drill, esc close, and type-to-filter narrows the list"
    - "The Reuse tier is department-scoped with an all-org toggle, reusing the existing Phase 13 BlockPicker/addBlockToSection path (not rebuilt)"
    - "＋ dividers appear between every block and at section end; inserting places the block at the cursor and autosaves"
  artifacts:
    - path: "src/components/admin/builder-v2/inserter/inserter-model.ts"
      provides: "LANE (section→fits-here) / SMART (prev→predicted) / GROUPS (catalog) ported from the sketch"
      contains: "LANE"
    - path: "src/components/admin/builder-v2/inserter/InserterMenu.tsx"
      provides: "paged command menu (home/All/Reuse/AI), keyboard nav, type-to-filter"
      contains: "InserterMenu"
  key_links:
    - from: "src/components/admin/builder-v2/inserter/InserterMenu.tsx"
      to: "src/lib/builder/content-ops.ts"
      via: "onInsert → insertBlock at cursor → autosave"
      pattern: "insertBlock"
    - from: "src/components/admin/builder-v2/inserter/ReuseTier.tsx"
      to: "BlockPicker / addBlockToSection"
      via: "department-scoped library reuse (Phase 13 path)"
      pattern: "addBlockToSection"
---

<objective>
Deliver the tiered, context-aware inserter (R3) validated in the sketch. The `＋` becomes a paged command menu: a short home (search + one smart row + a "Fits here" list keyed by section render-family + drill rows for More/Reuse/AI), type-to-filter, and full keyboard nav. The interaction model + data (`LANE`/`SMART`/`GROUPS`/`LIB`) are already encoded in `sketches/sop-builder-redesign/index.html` — port them verbatim (~120 lines, no headless-menu library per RESEARCH).

The Reuse tier is NOT rebuilt: it wraps the existing Phase 13 `BlockPicker` + `addBlockToSection` (department-scoped, junction-linked, already wired in `BuilderClient`), with the all-org toggle. Insert places a block at the cursor via `content-ops.insertBlock` and autosaves through the existing path.

Purpose: fast, context-aware block insertion — the fix for "insertion is dumb."
Output: `inserter-model.ts`, `InserterMenu`, `ReuseTier`, ＋ dividers in the document/shell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@src/components/admin/sops/AddMenu.tsx
</context>

<artifacts>
This plan produces:
- `src/components/admin/builder-v2/inserter/inserter-model.ts` — `LANE`, `SMART`, `GROUPS` (ported from the sketch), keyed to `SECTION_RENDER_FAMILIES`
- `src/components/admin/builder-v2/inserter/InserterMenu.tsx` — paged command menu + keyboard nav + type-to-filter
- `src/components/admin/builder-v2/inserter/ReuseTier.tsx` — dept-scoped BlockPicker wrapper + all-org toggle
- ＋ divider affordances in `EditableDocument`/`BlockEditShell`
- `tests/phase26/inserter.spec.ts`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Port the inserter model + paged menu (context + keyboard)</name>
  <files>src/components/admin/builder-v2/inserter/inserter-model.ts, src/components/admin/builder-v2/inserter/InserterMenu.tsx</files>
  <read_first>
    - sketches/sop-builder-redesign/index.html (`LANE`/`SMART`/`GROUPS`/`LIB`, `renderPicker`/`filterPicker`/keydown — the exact model + keyboard behaviour to port)
    - src/actions/introspection.ts (L60 `SECTION_RENDER_FAMILIES`: hazard|ppe|steps|content|signoff|emergency|custom — the LANE keys)
    - src/components/admin/sops/AddMenu.tsx (L94–269 humanised grouped menu + `getPillStyle` L32, `humanizeBlockType` L200, `onInsert(componentType)` L20–27 contract to reuse)
    - src/lib/builder/block-type-labels.ts (`humanizeBlockType` — single label source, P16)
  </read_first>
  <behavior>
    - `LANE[sectionRenderFamily] → blockType[]` drives "Fits here"; opening ＋ in `hazard` vs `steps` yields different lists.
    - `SMART[prevBlockType] → {predictedType, why}` drives the single smart row (and the ghost source in 26-10).
    - Keyboard: ↑↓ move `.hi` highlight, ↵ insert-or-drill, esc close; typing filters the visible rows (type-to-filter). Home page stays short; deeper tiers (More block types / Reuse / Describe with AI) are one ↵ away.
    - All labels via `humanizeBlockType` (never raw PascalCase — P16).
  </behavior>
  <action>Create `inserter-model.ts` transcribing `LANE`/`SMART`/`GROUPS` from the sketch, LANE keyed to `SECTION_RENDER_FAMILIES`. Build `InserterMenu.tsx` ('use client'): home page (search input + smart row + Fits-here LANE list + drill rows), keyboard handler (↑↓/↵/esc), type-to-filter; `onInsert(type)` calls the parent's insert-at-cursor callback. Reuse `AddMenu`'s pill styling + `humanizeBlockType`. No headless-menu library — port the sketch's ~120-line handler. Local state only; NO `router.push` (CLAUDE.md 2026-05-13).</action>
  <acceptance_criteria>
    - Behavioural: `LANE` for a `hazard` section ≠ `LANE` for a `steps` section (different Fits-here rows).
    - Keyboard: ↑↓ moves highlight, ↵ inserts the highlighted type, esc closes; typing filters.
    - Every rendered block label comes from `humanizeBlockType` (no raw PascalCase in the menu DOM).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "inserter" && npx tsc --noEmit</automated>
  </verify>
  <done>Context-aware paged inserter with keyboard nav + type-to-filter; humanised labels.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse tier (dept-scoped BlockPicker) + ＋ dividers wired to insert</name>
  <files>src/components/admin/builder-v2/inserter/ReuseTier.tsx, src/components/admin/builder-v2/EditableDocument.tsx, src/components/admin/builder-v2/BlockEditShell.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (L24–26, L329–351 `handleLibraryAdd` — existing BlockPicker + `addBlockToSection` wiring to reuse; L203 `componentIdToJunction`)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Inserter + Ghosts (Reuse page = dept-scope segmented toggle over BlockPicker) + § Block Edit-Shell Contract (＋ dividers `.adddiv`/`.addbig`)
  </read_first>
  <behavior>
    - The Reuse drill page renders the existing `BlockPicker` scoped to the SOP's department (`category_tag` + org) with a "Forming dept / All departments" segmented toggle; selecting a library block routes through the existing `addBlockToSection` junction path (NOT rebuilt).
    - ＋ dividers render between every block and at section end; clicking opens `InserterMenu` at that cursor index; insert → `content-ops.insertBlock(content, type, afterIndex, BLOCK_DEFAULTS[type])` → autosave.
  </behavior>
  <action>Build `ReuseTier.tsx` wrapping the existing `BlockPicker` with the dept-scope toggle, delegating selection to the existing `addBlockToSection`/`handleLibraryAdd` path. Add ＋ divider affordances in `EditableDocument` (between blocks) and section-end (`＋ Add step or block`) that open `InserterMenu` at the cursor and, on insert, dispatch `insertBlock` (using `BLOCK_DEFAULTS`) then autosave. Keep department scope sourced from `initialSop.category_tag` + org exactly as `BlockPicker` consumes today.</action>
  <acceptance_criteria>
    - Behavioural: Reuse tier lists dept-scoped library blocks; toggling to "All departments" broadens the list; selecting one calls `addBlockToSection` (junction created).
    - Inserting from a ＋ divider adds the block at the correct index and autosaves (draftLayouts reflects it).
    - `npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "inserter|reuse" && npm run build</automated>
  </verify>
  <done>Reuse tier reuses the Phase 13 library path (dept-scoped + all-org toggle); ＋ dividers insert-at-cursor + autosave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library reuse → junction write | Reusing a library block writes a sop_section_blocks junction (existing org-scoped path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08-01 | Elevation | cross-org library block reuse | mitigate | Reuse the existing `addBlockToSection` path, which is already org-scoped (Phase 13); no new write path introduced |
| T-26-08-02 | Tampering | inserting an unregistered block type | mitigate | Insert only from LANE/GROUPS which reference registered types; unknown types still guarded by sanitizeLayoutContent at render |
</threat_model>

<verification>
- inserter behavioural spec green (context-vary + keyboard + reuse).
- `npm run build` green.
</verification>

<success_criteria>
Tiered, context-aware, keyboard-operable inserter (R3); Reuse reuses the Phase 13 dept-scoped path; ＋ dividers insert-at-cursor with autosave.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-08-SUMMARY.md` when done
</output>
