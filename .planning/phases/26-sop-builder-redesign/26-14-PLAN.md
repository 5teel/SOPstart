---
phase: 26-sop-builder-redesign
plan: 14
type: execute
wave: 10
depends_on: ["26-12", "26-13"]
files_modified:
  - tests/phase26/convert-golden-path.spec.ts
  - tests/phase26/spine-regression.spec.ts
  - package.json
  - src/lib/builder/puck-config.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: true
requirements: [R1, R6, R8]
user_setup: []
must_haves:
  truths:
    - "The convert on-ramp produces layout_data + sop_section_blocks junctions + block_provenance byte-equivalent to the pre-phase golden fixture (R6)"
    - "Publish gate (400 unverified_blocks), junctionId stamping, no-bulk-verify lint, and append-only worker records all still pass (R8)"
    - "@puckeditor/core is removed from package.json and no src file imports it — both render paths and all field editors are bespoke (D-01 end-state)"
    - "journeys.ts reflects the unified create/convert/edit builder surface + Visual annotation flow; /pathways shows 0 not-mapped for the phase's routes"
  artifacts:
    - path: "tests/phase26/convert-golden-path.spec.ts"
      provides: "R6 byte-equivalence regression (now asserting post-phase == pre-phase fixture)"
      contains: "convert-golden"
    - path: "package.json"
      provides: "@puckeditor/core removed"
      contains: "dependencies"
  key_links:
    - from: "tests/phase26/convert-golden-path.spec.ts"
      to: "tests/phase26/fixtures/convert-golden.json"
      via: "post-phase deep-equal to the pre-phase baseline"
      pattern: "convert-golden"
---

<objective>
Close Phase 26 (v5.0 opener — D-04): prove the FROZEN contract survived the full-bespoke swap (D-01), sweep the R8 regression surface, remove Puck entirely, and keep the pathways map honest.

- **R6 golden-path parity:** re-run the 26-02 capture against post-phase code and assert byte-equivalence to the frozen `convert-golden.json` (layout_data + junctions + provenance). This is the hard proof that convert → AI-review → verify → publish is untouched.
- **R8 regression sweep:** publish gate (400 `unverified_blocks`), junctionId stamping, no-bulk-verify lint, append-only worker records — all still green.
- **Full-bespoke cleanup (D-01 end-state / Deferred "migration cleanup"):** remove `@puckeditor/core` — but ONLY after grep confirms no `src/` file imports it (RESEARCH Q4: keep the dep until both render paths + all 16 field editors are Puck-free + parity-tested; all now landed W2–W9). Retire the dead `puck-config.tsx` field/render definitions.
- **Pathways map (CLAUDE.md Pathways Map Maintenance):** update `journeys.ts` for the unified builder surface + Visual annotation flow; confirm `/pathways` "All screens" shows 0 not-mapped for this phase's routes; add/refresh a `uat/tests.ts` entry for the redesigned builder.

Purpose: verified frozen-contract preservation + clean Puck-free end-state + accurate maps.
Output: golden-path + spine-regression green, Puck removed, journeys/uat updated, real `npm run build` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
@.planning/phases/26-sop-builder-redesign/26-SPEC.md
@tests/phase26/convert-golden-path.spec.ts
</context>

<artifacts>
This plan produces:
- Post-phase-passing `tests/phase26/convert-golden-path.spec.ts` (R6 regression)
- `tests/phase26/spine-regression.spec.ts` (R8 sweep)
- `@puckeditor/core` removed from `package.json`; dead Puck definitions retired from `puck-config.tsx`
- Updated `src/lib/journeys/journeys.ts` + `src/lib/uat/tests.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert golden-path byte-equivalence + R8 regression sweep</name>
  <files>tests/phase26/convert-golden-path.spec.ts, tests/phase26/spine-regression.spec.ts</files>
  <read_first>
    - tests/phase26/convert-golden-path.spec.ts + tests/phase26/fixtures/convert-golden.json (26-02 — the pre-phase baseline)
    - src/app/api/sops/[sopId]/publish/route.ts (400 `unverified_blocks` — must still hold)
    - tests/phase15 no-bulk-verify-ui.spec.ts + the junction stamping path (materializeJunctionsForLayout / props.junctionId)
    - .planning/phases/26-sop-builder-redesign/26-SPEC.md § Acceptance Criteria (R6/R8 lines)
  </read_first>
  <behavior>
    - Running the convert capture against post-phase code deep-equals the frozen `convert-golden.json` (no drift in layout_data, junctions, or provenance).
    - Publish gate still 400s on unverified; junctionId still stamped on converted blocks; no-bulk-verify lint green; append-only worker completion records unaffected.
  </behavior>
  <action>Confirm `tests/phase26/convert-golden-path.spec.ts` now runs against the fully-bespoke code and deep-equals the pre-phase fixture (if the capture normalization needs any adjustment for determinism, fix the script — NOT the fixture; a real drift must fail). Write `tests/phase26/spine-regression.spec.ts` aggregating the R8 invariants: (a) unverified block → publish 400 `unverified_blocks`; (b) a converted block round-tripped through the editor still carries `props.junctionId` + `block_provenance`; (c) no-bulk-verify lint passes; (d) worker completion records remain append-only (reuse the existing Phase 4 assertion). This is the phase's frozen-contract proof.</action>
  <acceptance_criteria>
    - convert-golden-path spec: post-phase output byte-equivalent to the pre-phase fixture.
    - spine-regression spec: all four R8 invariants green.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "convert-golden|spine-regression" && npx playwright test -g "no-bulk-verify"</automated>
  </verify>
  <done>Convert byte-equivalence proven (R6); R8 invariants all green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove @puckeditor/core (full-bespoke end-state)</name>
  <files>package.json, src/lib/builder/puck-config.tsx</files>
  <read_first>
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Open Questions Q4 (remove the dep only after both render paths + all field editors are Puck-free)
    - src/lib/builder/puck-config.tsx (what still lives here after the renderer moved to block-registry — dead Puck `fields`/`render` definitions to retire; keep anything still imported)
    - scripts/contract-check.ts (already repointed to block-registry in 26-03 — confirm it no longer needs puck-config)
  </read_first>
  <action>First run `grep -rn "@puckeditor/core" src` — it MUST return zero hits (both render paths bespoke since 26-03, all field editors bespoke since 26-06/26-07). If any hit remains, STOP and fix that consumer before removing the dep. Then remove `@puckeditor/core` from `package.json` dependencies and retire the now-dead Puck `fields`/`render`/`components` definitions in `puck-config.tsx` (keep any still-referenced exports like the block prop types if imported elsewhere; delete the Puck-config object + `<Puck>`/`<Render>` config). Run a real `npm run build` (next build + prebuild contract-check on block-registry + postbuild bundle gate) — the final authoritative gate (CLAUDE.md 2026-06-27: tsc green is necessary, not sufficient).</action>
  <acceptance_criteria>
    - `grep -rn "@puckeditor/core" src` returns nothing; `@puckeditor/core` absent from `package.json`.
    - `npm run build` green (contract-check + bundle gate included).
    - The re-captured bundle baseline (26-03) still holds (no worker regression).
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "@puckeditor/core" src && npm run build</automated>
  </verify>
  <done>Puck fully removed; bespoke end-state; full build green.</done>
</task>

<task type="auto">
  <name>Task 3: Update pathways map + UAT config; final phase gate</name>
  <files>src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts (the /pathways single source of truth; the "All screens" panel flags any route no pathway covers)
    - CLAUDE.md § Pathways Map Maintenance (update alongside flow changes; 0 not-mapped for the phase's routes)
    - src/lib/uat/tests.ts (add a builder-redesign UAT entry — R2 edit==worker parity + Konva annotation, the VALIDATION manual items)
  </read_first>
  <action>Update `journeys.ts` so the unified create/convert/edit builder surface + Visual annotation flow are represented (extend/add the relevant `Journey`; keep `route` values real so coverage + deep-links work) — the builder route path is unchanged but its flow is materially redesigned. Confirm `/pathways` "All screens" shows 0 not-mapped for this phase's routes (grep each route in journeys.ts, or open the page). Add a `uat/tests.ts` entry covering the two VALIDATION manual-only items (R2 edit==worker visual parity; Konva annotate/re-edit/palm-reject → baked PNG on worker read). Then run the full suite + a real `npm run build` as the phase gate.</action>
  <acceptance_criteria>
    - journeys.ts covers the builder surface + Visual annotation flow; 0 not-mapped for this phase's routes.
    - uat/tests.ts has the builder-redesign entry (R2 parity + Konva).
    - `npm run test` full suite green + `npm run build` green (phase gate).
  </acceptance_criteria>
  <verify>
    <automated>npm run test && npm run build</automated>
  </verify>
  <done>Pathways + UAT maps updated; full suite + real build green — phase gate passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| convert pipeline → frozen contract | Regression here would silently break the parse/review/publish spine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-14-01 | Tampering | silent layout_data/junction drift from the bespoke swap | mitigate | Byte-equivalence golden-path regression vs the pre-phase fixture; drift fails the gate |
| T-26-14-02 | Elevation | publish/verify gate weakened during re-wire | mitigate | spine-regression asserts the server 400 gate + no-bulk-verify still hold (R8) |
| T-26-14-03 | Tampering | dangling Puck import after dep removal breaks build | mitigate | grep-zero guard before removal + real `npm run build` gate |
</threat_model>

<verification>
- convert-golden-path (byte-equivalent) + spine-regression (R8) green.
- `@puckeditor/core` gone; `npm run build` green.
- Full `npm run test` green; journeys/uat updated; /pathways 0 not-mapped.
</verification>

<success_criteria>
Frozen contract preserved byte-for-byte (R6); no safety/autosave/offline/gate regression (R8); Puck fully removed (D-01 end-state); convert on-ramp identical (R1); pathways + UAT maps current; phase gate (full suite + real build) green.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-14-SUMMARY.md` when done
</output>
