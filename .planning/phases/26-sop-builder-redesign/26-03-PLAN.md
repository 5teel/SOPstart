---
phase: 26-sop-builder-redesign
plan: 03
type: execute
wave: 2
depends_on: ["26-02"]
files_modified:
  - src/lib/builder/block-registry.tsx
  - src/lib/builder/sanitize-layout.ts
  - src/components/sop/LayoutRenderer.tsx
  - scripts/contract-check.ts
  - scripts/capture-bundle-baseline.ts
  - .bundle-baseline.json
  - tests/phase26/render-parity.spec.ts
  - tests/phase26/contract-check-target.spec.ts
autonomous: true
requirements: [R2, R6]
user_setup: []
must_haves:
  truths:
    - "The worker read path renders every block type identically to before, with NO @puckeditor/core in the worker bundle"
    - "An unknown block type renders UnsupportedBlockPlaceholder in the read path (P17 preserved)"
    - "scripts/contract-check.ts validates the NEW block-registry, not the dead puck-config file"
    - "The /sops/[sopId] First Load JS baseline is re-captured Puck-free and the bundle gate is green"
  artifacts:
    - path: "src/lib/builder/block-registry.tsx"
      provides: "BLOCK_COMPONENTS (type→component) + BLOCK_DEFAULTS — the renderer 'place' off Puck (D-01)"
      contains: "BLOCK_COMPONENTS"
    - path: "src/lib/builder/sanitize-layout.ts"
      provides: "sanitizeLayoutContent + UnsupportedBlockPlaceholder relocated (P17)"
      contains: "UnsupportedBlockPlaceholder"
    - path: "src/components/sop/LayoutRenderer.tsx"
      provides: "bespoke type→component switch, mode read; drops <Render>"
      contains: "BLOCK_COMPONENTS"
  key_links:
    - from: "src/components/sop/LayoutRenderer.tsx"
      to: "src/lib/builder/block-registry.tsx"
      via: "import BLOCK_COMPONENTS"
      pattern: "BLOCK_COMPONENTS"
    - from: "scripts/contract-check.ts"
      to: "src/lib/builder/block-registry.tsx"
      via: "read registry file for 3-place check"
      pattern: "block-registry"
---

<objective>
Replace Puck as the RENDER engine (D-01) on the worker read path — the safest first move because it removes `@puckeditor/core` from `/sops/[sopId]` while proving "edit == worker render" (R2) becomes literally true (one renderer, `mode` differs later). `layout_data` is Puck-agnostic JSON (`{content:[{type,props}]}`), and the 18 block components in `src/components/sop/blocks/*` already accept exactly those props — so the swap is a `type→component` switch, not a rewrite.

This also closes two W0 contract gaps: repoint `scripts/contract-check.ts` (prebuild 3-place gate) off the soon-dead `puck-config.tsx` onto the new registry (RESEARCH Pitfall 1 — otherwise the gate silently checks a dead file), and re-capture `.bundle-baseline.json` now that Puck leaves the worker path.

Purpose: prove the frozen contract holds at the render boundary and shrink the worker bundle.
Output: `block-registry.tsx`, `sanitize-layout.ts`, Puck-free `LayoutRenderer`, repointed contract-check, re-captured bundle baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@.planning/phases/26-sop-builder-redesign/26-RESEARCH.md
@src/components/sop/LayoutRenderer.tsx
@src/lib/builder/puck-config.tsx
@scripts/contract-check.ts
</context>

<artifacts>
This plan produces:
- `src/lib/builder/block-registry.tsx` — `BLOCK_COMPONENTS` (18-entry type→component map), `BLOCK_DEFAULTS`, `stripMeta(props)` helper
- `src/lib/builder/sanitize-layout.ts` — relocated `sanitizeLayoutContent`, `UnsupportedBlockPlaceholder`
- Rewritten `src/components/sop/LayoutRenderer.tsx` (Puck-free, mode-read switch)
- Repointed `scripts/contract-check.ts` (reads `block-registry.tsx`)
- Re-captured `.bundle-baseline.json` (Puck-free worker baseline)
- `tests/phase26/render-parity.spec.ts`, `tests/phase26/contract-check-target.spec.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract block-registry + sanitize-layout off puck-config</name>
  <files>src/lib/builder/block-registry.tsx, src/lib/builder/sanitize-layout.ts</files>
  <read_first>
    - src/lib/builder/puck-config.tsx (the `components:` map L216–795 for the 18 type→component + defaultProps; `UnsupportedBlockPlaceholder` L82; `sanitizeLayoutContent` L106; per-block `SafeRender` guard L226–233)
    - src/components/sop/blocks/index.ts (barrel exports all 18 components — the source of BLOCK_COMPONENTS)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § block-registry / sanitize-layout entries
  </read_first>
  <behavior>
    - `BLOCK_COMPONENTS` has exactly one entry per registered block type (all 18: TextBlock, HeadingBlock, CalloutBlock, StepBlock, HazardCardBlock, PPECardBlock, MeasurementBlock, DecisionBlock, EscalateBlock, SignOffBlock, ZoneBlock, InspectBlock, VoiceNoteBlock, StepWithPhotosBlock, PhotoBlock, PhotoGridBlock, ModelBlock — plus the placeholder handled separately).
    - `sanitizeLayoutContent(items)` rewrites any item whose `type` is not a key of BLOCK_COMPONENTS to `{ type:'UnsupportedBlockPlaceholder', props }` — identical semantics to the puck-config version, but the known-type check now uses BLOCK_COMPONENTS.
    - `stripMeta(props)` drops `id`, `junctionId`, `block_provenance` (they are not component props).
  </behavior>
  <action>Create `src/lib/builder/block-registry.tsx`: import the barrel `* as Blocks from '@/components/sop/blocks'`; export `BLOCK_COMPONENTS` mapping each registered type string to its component, `BLOCK_DEFAULTS` distilled from each puck entry's `defaultProps`, and `stripMeta(props)`. Move `UnsupportedBlockPlaceholder` and `sanitizeLayoutContent` VERBATIM from `puck-config.tsx` into `src/lib/builder/sanitize-layout.ts` (P17 relocate), swapping the known-type membership check from the Puck components map to `BLOCK_COMPONENTS`. Keep `puck-config.tsx` intact for now (the Puck field editors are removed in the cleanup wave — RESEARCH Q4: keep `@puckeditor/core` installed until every field editor + both render paths are Puck-free).</action>
  <acceptance_criteria>
    - `BLOCK_COMPONENTS` key count === the puck-config components count minus placeholder (source-contract + a unit assertion counting keys).
    - `sanitizeLayoutContent([{type:'Nonexistent',props:{id:'x'}}])[0].type === 'UnsupportedBlockPlaceholder'` (unit).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "sanitize|registry" && npx tsc --noEmit</automated>
  </verify>
  <done>Registry + sanitize-layout extracted; unknown-type → placeholder preserved; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite LayoutRenderer to the bespoke switch (mode read, Puck-free)</name>
  <files>src/components/sop/LayoutRenderer.tsx, tests/phase26/render-parity.spec.ts</files>
  <read_first>
    - src/components/sop/LayoutRenderer.tsx (itself L1–59 — KEEP the version gate L25–34, `LayoutDataSchema.safeParse` L36, warn-once flags L9, sanitize call L49–54; replace only the `<Render>` at L58)
    - src/lib/builder/block-registry.tsx (from Task 1)
    - .planning/phases/26-sop-builder-redesign/26-PATTERNS.md § LayoutRenderer REWRITE
  </read_first>
  <behavior>
    - For valid `layout_data`, the renderer maps each sanitized item to `BLOCK_COMPONENTS[type]` spreading `stripMeta(props)`, keyed by `props.id`; unknown types render `UnsupportedBlockPlaceholder`.
    - Unsupported layout version → fallback (unchanged). Parse failure → fallback (unchanged). Warn-once flags preserved.
    - No import of `@puckeditor/core` remains in this file or its import graph on the worker path.
  </behavior>
  <action>Rewrite `LayoutRenderer.tsx`: drop `import { Render } from '@puckeditor/core'` and the `<Render config={puckConfig} data=… />`; keep the version-support gate, `LayoutDataSchema.safeParse`, warn-once flags, and the `sanitizeLayoutContent` call exactly. Map `sanitized.content.map(it => C ? <C key={it.props.id} {...stripMeta(it.props)} /> : <UnsupportedBlockPlaceholder type={it.type} />)`. Keep the file `'use client'`, hydration-clean (no navigator/Date at module load — CLAUDE.md 2026-06-08). Write `tests/phase26/render-parity.spec.ts`: for a representative `layout_data` (one of each block kind), assert the DOM produced by LayoutRenderer contains each block's signature markup (the SAME components), and that an injected unknown type renders the placeholder — the behavioural R2 read-parity proof.</action>
  <acceptance_criteria>
    - `grep -rn "@puckeditor/core" src/components/sop/LayoutRenderer.tsx` returns nothing.
    - render-parity spec: every block kind renders its component; unknown type → placeholder.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "render-parity" && npx tsc --noEmit</automated>
  </verify>
  <done>LayoutRenderer is Puck-free, renders identical components, preserves placeholder + fallbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint contract-check + re-capture Puck-free bundle baseline</name>
  <files>scripts/contract-check.ts, scripts/capture-bundle-baseline.ts, .bundle-baseline.json, tests/phase26/contract-check-target.spec.ts</files>
  <read_first>
    - scripts/contract-check.ts (L27 `PUCK_CONFIG` path; L113–117 `extractPuckComponentKeys` greps `components:\s*{`; the 3-place enforcement in `main()`)
    - scripts/check-bundle-size.ts + scripts/capture-bundle-baseline.ts (how the /sops/[sopId] First Load JS baseline is derived — webpack manifest handling)
    - .planning/phases/26-sop-builder-redesign/26-RESEARCH.md § Pitfall 1 + bundle re-capture
  </read_first>
  <action>Repoint `scripts/contract-check.ts`: change `PUCK_CONFIG` to read `src/lib/builder/block-registry.tsx` and adapt `extractPuckComponentKeys` to extract keys from the `BLOCK_COMPONENTS` object literal (the `export const BLOCK_COMPONENTS = { … } as const` map) instead of `components: {`. Keep the BLOCK_REGISTRY + BlockContentSchema places and the EXCLUDED sets unchanged. Add `tests/phase26/contract-check-target.spec.ts` asserting the script's registry path constant equals the live `block-registry.tsx` path (Pitfall 1 guard). Then re-run `npm run build` so Puck is confirmed gone from the worker path, and re-capture `.bundle-baseline.json` via `scripts/capture-bundle-baseline.ts` for `/sops/[sopId]/page` (expect a DECREASE — Puck left the worker bundle); record the new number in the baseline. The bundle gate is baseline+2KB max, so a decrease is safe but the baseline MUST be re-captured to reflect the new floor.</action>
  <acceptance_criteria>
    - `npx tsx scripts/contract-check.ts` exits 0 reading `block-registry.tsx` (prints the 18-block set).
    - contract-check-target spec passes (script targets the live registry path).
    - `.bundle-baseline.json` updated with the new (lower) `/sops/[sopId]` First Load JS; `npm run build` green (prebuild contract-check + postbuild bundle-size both pass).
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/contract-check.ts && npx playwright test --project=phase26 -g "contract-check-target" && npm run build</automated>
  </verify>
  <done>contract-check reads the new registry; bundle baseline re-captured Puck-free; full build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored layout_data → worker render | Malformed/unknown block types cross into the render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | unknown/malformed block type in layout_data | mitigate | `LayoutDataSchema.safeParse` + `sanitizeLayoutContent` → `UnsupportedBlockPlaceholder`; per-block guard preserved (P17) |
| T-26-03-02 | Tampering | silent contract-gate bypass after renderer move | mitigate | Repoint contract-check to block-registry + a spec asserting the target file matches the live registry (Pitfall 1) |
| T-26-03-03 | DoS-adjacent | worker bundle regression | mitigate | Re-capture bundle baseline Puck-free; postbuild bundle gate (baseline+2KB) enforces |
</threat_model>

<verification>
- `npm run build` green (prebuild contract-check on the new registry + postbuild bundle gate on the re-captured baseline).
- render-parity + contract-check-target specs green.
- No `@puckeditor/core` in the worker LayoutRenderer import graph.
</verification>

<success_criteria>
Worker read path is Puck-free and renders identical components (R2); unknown-block safety preserved (P17); 3-place gate reads the live registry; bundle baseline re-captured (decrease).
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-03-SUMMARY.md` when done
</output>
