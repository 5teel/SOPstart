---
phase: 26-sop-builder-redesign
plan: 11
type: execute
wave: 7
depends_on: ["26-09"]
files_modified:
  - src/components/admin/builder-v2/visual/AnnotationEditor.tsx
  - src/components/admin/builder-v2/visual/annotation-tools.ts
  - src/components/admin/builder-v2/visual/DiagramHotspotBlock.tsx
  - tests/phase26/annotation-primitives.spec.ts
autonomous: false
requirements: [R5]
user_setup: []
must_haves:
  truths:
    - "The annotation editor draws Arrow / Rect / Ellipse / Text / numbered-callout / freehand onto a diagram at its natural dimensions, selects/transforms shapes, and supports undo/redo"
    - "The scene serializes to a Konva JSON scene graph re-openable for editing (non-destructive; image URL stays OUT of the scene)"
    - "Stylus 'pen only' palm rejection filters non-pen pointers while pen mode is active"
    - "DiagramHotspotBlock places numbered callouts at freeform x/y in natural-image space (the only freeform-positioning surface)"
  artifacts:
    - path: "src/components/admin/builder-v2/visual/AnnotationEditor.tsx"
      provides: "Konva editor: primitives + Transformer + undo/redo + stylus (admin-only, dynamic)"
      contains: "Konva"
    - path: "src/components/admin/builder-v2/visual/DiagramHotspotBlock.tsx"
      provides: "numbered callouts at freeform x/y (natural-image space)"
      contains: "DiagramHotspotBlock"
  key_links:
    - from: "src/components/admin/builder-v2/visual/AnnotationEditor.tsx"
      to: "sop_image_annotations.scene"
      via: "scene JSON snapshot (save deferred to 26-13)"
      pattern: "scene"
---

<objective>
Build the absorbed Phase 17 annotation primitives (D-03, slice 2) inside the Visual/diagram medium — Konva-based, admin-only, dynamic-imported (the foundation + spike landed in 26-05). Draw Arrow / Rectangle / Ellipse / Text / numbered-callout / freehand; select+transform via `Konva.Transformer`; undo/redo via scene-JSON snapshots; stylus palm-rejection (`pointerType==='pen'` filter). Scenes are non-destructive Konva JSON (the image URL stays OUT of the scene — coordinates in natural-image space for stability).

`DiagramHotspotBlock` folds in here: numbered callouts at freeform x/y in natural-image space — the ONLY freeform-positioning exception in the otherwise block-reflow builder.

Konva UX (draw / re-edit / palm-reject) is device-dependent, so a human-verify checkpoint confirms the interaction after the automated shape/undo/serialize coverage.

Purpose: full diagram annotation capability, non-destructive + re-editable.
Output: `AnnotationEditor` (primitives + transformer + undo/redo + stylus), `DiagramHotspotBlock`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/26-sop-builder-redesign/26-UI-SPEC.md
@.planning/phases/26-sop-builder-redesign/26-PATTERNS.md
@.planning/research/v3.0-image-annotation.md
</context>

<artifacts>
This plan produces:
- `src/components/admin/builder-v2/visual/AnnotationEditor.tsx` — full Konva editor (upgrades the 26-05 spike shell)
- `src/components/admin/builder-v2/visual/annotation-tools.ts` — primitive factories + scene snapshot/undo-redo helpers (pure)
- `src/components/admin/builder-v2/visual/DiagramHotspotBlock.tsx` — freeform numbered-callout block
- `tests/phase26/annotation-primitives.spec.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Primitives + Transformer + undo/redo + stylus palm-rejection</name>
  <files>src/components/admin/builder-v2/visual/AnnotationEditor.tsx, src/components/admin/builder-v2/visual/annotation-tools.ts, tests/phase26/annotation-primitives.spec.ts</files>
  <read_first>
    - .planning/research/v3.0-image-annotation.md (Konva selection, Transformer, undo/redo via scene snapshots, stylus/palm rejection, dual-store — carry verbatim, do NOT re-derive)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § Visual Block + Konva Annotation Editor (toolbar primitives → Konva mapping; controls; stroke colours = semantic accents; text via textarea overlay)
    - src/components/admin/builder-v2/visual/AnnotationEditor.tsx (the 26-05 spike shell to upgrade)
  </read_first>
  <behavior>
    - Each tool creates its Konva node: Arrow (`Konva.Arrow`), Rect (`Konva.Rect`), Ellipse (`Konva.Ellipse`), Text (`Konva.Text` edited via `<textarea>` overlay), numbered callout (`Konva.Label`, auto-increment), freehand (`Konva.Line`, tension).
    - Click a shape → `Konva.Transformer` handles (resize/rotate); Delete key removes; drag moves.
    - Undo/redo restores prior scene snapshots (scene-JSON stack).
    - Scene serializes WITHOUT the image URL; coordinates in natural-image space.
    - Pen-only toggle filters `pointerType !== 'pen'` while active (palm rejection).
  </behavior>
  <action>Upgrade `AnnotationEditor.tsx` from the spike shell to the full editor per the Phase 17 research + UI-SPEC. Put pure logic (primitive factories, scene snapshot/serialize, undo/redo stack) in `annotation-tools.ts` (unit-testable without a DOM). Stroke colours default to semantic accents (brand-yellow primary, hazard red for warnings) — not arbitrary. `stage.destroy()` on unmount (Pitfall 5). Write `tests/phase26/annotation-primitives.spec.ts`: unit-test `annotation-tools` (create each primitive → scene JSON contains it; undo removes the last; redo restores; serialize omits image URL), and a behavioural chromium test that adds a shape and re-opens the scene to confirm it round-trips. Konva/react-konva stay imported ONLY here (admin-only) — the 26-05 isolation lint continues to guard.</action>
  <acceptance_criteria>
    - Unit: each primitive appears in the scene JSON; undo/redo work; serialized scene has NO image URL.
    - Behavioural: draw a rect, serialize, re-open → rect present (non-destructive round-trip).
    - Konva still absent from the worker manifest (26-05 lint + bundle gate green).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "annotation-primitives|konva-worker-isolation" && npx tsc --noEmit</automated>
  </verify>
  <done>All six primitives + transformer + undo/redo + stylus + non-destructive scene; Konva admin-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiagramHotspotBlock — freeform numbered callouts (natural-image space)</name>
  <files>src/components/admin/builder-v2/visual/DiagramHotspotBlock.tsx, tests/phase26/annotation-primitives.spec.ts</files>
  <read_first>
    - .planning/ROADMAP.md §Phase 17 criterion 3 (DiagramHotspotBlock — numbered callouts at freeform x/y; the ONLY freeform exception)
    - .planning/phases/26-sop-builder-redesign/26-UI-SPEC.md § DiagramHotspotBlock (natural-image-space callouts, folds into Visual/diagram medium)
    - src/lib/validators/blocks.ts (VisualBlockContentSchema from 26-09 — annotationId linkage)
  </read_first>
  <behavior>
    - Admin drops a machine diagram into a DiagramHotspotBlock and places numbered callouts at freeform x/y in natural-image space; callouts persist in the annotation scene (linked via `annotationId`).
    - This is the single freeform-positioning surface — every other block reflows.
  </behavior>
  <action>Build `DiagramHotspotBlock.tsx` folding into the Visual/diagram medium: numbered-callout placement at freeform x/y in natural-image coordinates, backed by the annotation scene (`annotationId` from the Visual item). Extend the primitives spec to assert callouts persist at their natural-space coordinates across a re-open (coordinate stability). Keep it admin-only/dynamic (Konva isolation preserved).</action>
  <acceptance_criteria>
    - Behavioural: placing two numbered callouts persists their x/y in natural-image space; re-open preserves positions and numbering.
    - DiagramHotspotBlock references its annotation via `annotationId`.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase26 -g "hotspot|annotation-primitives" && npx tsc --noEmit</automated>
  </verify>
  <done>Freeform numbered callouts in natural-image space; the only freeform surface; annotationId-linked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Konva annotation UX (draw / re-edit / palm-reject)</name>
  <what-built>
    The full Konva annotation editor (primitives, transformer, undo/redo, stylus palm-rejection) + DiagramHotspotBlock, admin-only and dynamic-imported. Automated tests cover primitive creation, scene serialize/round-trip, and hotspot coordinate stability — but stylus/pointer behaviour and the draw feel need a real device (VALIDATION manual-only item).
  </what-built>
  <action>After the automated coverage is green, pause and present the on-device verification steps below to the human (this is the VALIDATION manual-only Konva UX item — draw/select/undo/palm-reject/re-edit cannot be proven headless). Do not mark the plan done until the human approves or files issues.</action>
  <how-to-verify>
    1. On sopstart.com, open a SOP in the builder, add a Visual block, add a diagram item, open the annotation editor.
    2. Draw an arrow, a rectangle, a numbered callout, and a freehand stroke; select + resize one; undo/redo.
    3. Toggle "Pen only" and confirm palm/finger input is rejected while a pen draws (iPad if available).
    4. Close, re-open the same diagram — confirm the exact annotations reload (non-destructive).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms draw / select-transform / undo-redo / palm-reject / re-edit all behave, or files specific issues.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin annotation input → scene JSON | Annotation geometry/text authored client-side (saved via service-role action in 26-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11-01 | Tampering | XSS via annotation text | mitigate | Konva text is canvas-rendered (no DOM injection); textarea overlay commits plain text |
| T-26-11-02 | DoS-adjacent | Konva leaking to worker | mitigate | Editor stays admin-only/dynamic; 26-05 isolation lint + bundle gate re-run in acceptance |
</threat_model>

<verification>
- annotation-primitives unit + behavioural specs green; hotspot coordinate stability green.
- konva-worker-isolation lint still green.
- Human checkpoint approves the device UX.
</verification>

<success_criteria>
Full non-destructive diagram annotation (R5/D-03 slice 2) + DiagramHotspotBlock; admin-only Konva; UX human-verified.
</success_criteria>

<output>
Create `.planning/phases/26-sop-builder-redesign/26-11-SUMMARY.md` when done
</output>
