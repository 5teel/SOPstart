---
phase: 37-assessor-governance
plan: 07
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/app/(protected)/activity/[completionId]/page.tsx
  - src/app/(protected)/admin/sops/[sopId]/video/page.tsx
  - src/actions/observations.ts
  - tests/phase37/gap-org-guards.spec.ts
autonomous: true
requirements: [ASR-01]

must_haves:
  truths:
    - "An authenticated user from organisation A cannot view organisation B's completion detail — no worker id, no step data, no signed photo URL, no sign-off history (CR-01)"
    - "The assessor predicate on the completion-detail page is evaluated against the SESSION organisation, never the fetched row's organisation_id (CR-01)"
    - "An admin/safety_manager from organisation A cannot view organisation B's SOP video-versions page (rule-5 sibling of CR-01, found by same-pattern grep)"
    - "Only supervisors/admins/safety managers can fan out assessment-request notifications — a plain worker calling requestAssessorReview is refused before any admin-client write (WR-02)"
    - "An observation row can only reference a completion that belongs to the caller's organisation AND to the observed worker (WR-05)"
    - "Every page under src/app/(protected) that reads with createAdminClient() compares against the session organisationId — enforced mechanically, so the next one added cannot ship without a guard"
  artifacts:
    - path: "src/app/(protected)/activity/[completionId]/page.tsx"
      provides: "org-scope guard before any photo/predicate work; session-org-derived predicate call"
      contains: "data.organisation_id !== organisationId"
    - path: "src/app/(protected)/admin/sops/[sopId]/video/page.tsx"
      provides: "org-scope guard on the admin-client SOP fetch"
      contains: "sop.organisation_id !== organisationId"
    - path: "src/actions/observations.ts"
      provides: "role-gated requestAssessorReview; org+worker-validated completionId on recordObservation"
      exports: ["recordObservation", "requestAssessorReview"]
    - path: "tests/phase37/gap-org-guards.spec.ts"
      provides: "positional + systemic regression guards for the three holes"
  key_links:
    - from: "src/app/(protected)/activity/[completionId]/page.tsx"
      to: "src/lib/competency/assessor.ts"
      via: "isSignedOffAssessor called with the session organisationId, after the org guard"
      pattern: "isSignedOffAssessor\\(userId, data\\.sop_id, admin, organisationId\\)"
    - from: "src/actions/observations.ts"
      to: "sop_completions"
      via: "admin-client existence check filtered on organisation_id AND worker_id before insert"
      pattern: "\\.eq\\('worker_id', workerId\\)"
---

<objective>
Close the two cross-org authorization holes on surfaces this phase extended (CR-01 and its same-pattern sibling), plus the two write-path validation gaps in `observations.ts` (WR-02, WR-05), and leave behind a systemic guard so the next unguarded admin-client page fetch fails a test instead of shipping.

Purpose: CR-01 is the exact service-role self-enforcement class CLAUDE.md flags in 2026-06-15, 2026-06-26, 2026-07-05 and 2026-07-20 — and this phase made it marginally worse by widening `isSupervisor` to `admin` and feeding the attacker-controlled `data.organisation_id` into the predicate. The 2026-07-20 learning is explicit that one probe per policy is not coverage; the same applies here — a per-file guard is not coverage, so this plan also adds the directory-wide assertion.
Output: guarded completion-detail page, guarded video-versions page, gated + validated observation write path, and `tests/phase37/gap-org-guards.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-assessor-governance/37-REVIEW.md
@.planning/phases/37-assessor-governance/37-VERIFICATION.md
@.planning/phases/37-assessor-governance/37-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Org-scope guards on both admin-client page fetches (CR-01 + rule-5 sibling)</name>
  <files>src/app/(protected)/activity/[completionId]/page.tsx, src/app/(protected)/admin/sops/[sopId]/video/page.tsx</files>
  <read_first>
    - src/app/(protected)/activity/[completionId]/page.tsx (the WHOLE file — 161 lines: the `getSessionContext()` destructure at line 39, the admin fetch at 46-62, the cast at 68, the worker check at 71-73, the `Promise.all` at 93-118 including the `createSignedUrl` loop and the `isSignedOffAssessor(userId, data.sop_id, admin, data.organisation_id)` call at line 117)
    - src/app/(protected)/admin/sops/[sopId]/video/page.tsx lines 12-58 (the session destructure at 22, the role gate at 26-28, the admin `sops` fetch at 33-37, the `!sop || sop.status !== 'published'` redirect at 39-41, and the two `video_generation_jobs` queries at 44-57 keyed on `sop_id`)
    - src/actions/versioning.ts lines 598-626 (`getSopVersionForDiff` — the CANONICAL correct shape in this codebase: select `organisation_id`, fetch by id with the admin client, then `if (!organisationId || sop.organisation_id !== organisationId)` before any downstream work. Mirror this shape exactly rather than inventing a new one.)
    - src/app/(protected)/admin/sops/pipeline/[pipelineId]/page.tsx lines 38-60 (second correct precedent — the page-level form of the same guard)
  </read_first>
  <action>
    **`src/app/(protected)/activity/[completionId]/page.tsx` — three edits, in this order.**

    1. Line 39: add `organisationId` to the destructure — `const { supabase, userId, role, organisationId } = await getSessionContext()`. Leave the two existing `redirect` guards on `userId`/`role` unchanged.

    2. Immediately AFTER the cast `const data = rawData as unknown as RawCompletionData` (line 68) and BEFORE the existing worker check at line 71, insert:

        `if (!organisationId || data.organisation_id !== organisationId) redirect('/activity')`

       Position is load-bearing and must not drift: it has to run before the `Promise.all` at line 93, because that block mints 1-hour presigned photo URLs for every completion photo. A guard placed after it would leak the URLs it is meant to protect. Comment it as the CR-01 fix and name the class (service-role self-enforcement — CLAUDE.md 2026-06-15 / 2026-07-20). Keep the existing `role === 'worker' && data.worker_id !== userId` check exactly as-is; it handles the same-org peer-worker case and is orthogonal.

    3. Line 117: change the predicate's fourth argument from `data.organisation_id` to `organisationId`. The row's org is attacker-influenced (it is whatever org the supplied UUID happens to belong to); the session org is the only trustworthy value. After edit 2 the two are provably equal, so this is defence-in-depth against the guard being moved or deleted later — say so in the comment.

    Change nothing else in the file: the `RawCompletionData` interface, the `sops`/`completion_photos`/`completion_sign_offs` joins, `isSupervisor`, `canOverride`, and every prop passed to `CompletionDetailClient` stay byte-identical.

    **`src/app/(protected)/admin/sops/[sopId]/video/page.tsx` — the same hole, found by a same-pattern grep (CLAUDE.md rule 5: a hole found in one place is checked everywhere before it is called fixed).** This page fetches a SOP by id with the admin client and gates only on `role`, so an admin/safety_manager of ANY organisation can read another org's SOP title/status and enumerate its video-generation jobs.

    - Line 22: add `organisationId` to the destructure.
    - Line 35: add `organisation_id` to the `.select(...)` list.
    - Line 39: widen the redirect condition to `if (!sop || !organisationId || sop.organisation_id !== organisationId || sop.status !== 'published')`. Keep the redirect target `/admin/sops` and the published check unchanged. The two `video_generation_jobs` queries below are keyed on `sop_id`, which this guard has now proven in-org — no further change needed there.

    Do NOT touch `src/app/(protected)/admin/sops/pipeline/[pipelineId]/page.tsx`, `src/app/(protected)/admin/ai-settings/page.tsx`, or `src/actions/versioning.ts` — all three were checked during the sweep and already carry a correct guard.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data.organisation_id !== organisationId" "src/app/(protected)/activity/[completionId]/page.tsx"` == 1
    - In that file, the index of `data.organisation_id !== organisationId` is LESS than the index of `createSignedUrl` and LESS than the index of `isSignedOffAssessor(` (assert positionally, not by presence)
    - `grep -c "data.organisation_id" "src/app/(protected)/activity/[completionId]/page.tsx"` == 1 — the ONLY remaining use is the guard itself; the predicate call no longer references it
    - The file contains the literal `isSignedOffAssessor(userId, data.sop_id, admin, organisationId)`
    - `grep -c "sop.organisation_id !== organisationId" "src/app/(protected)/admin/sops/[sopId]/video/page.tsx"` == 1, and that file's `.select(` list for the `sops` table contains `organisation_id`
    - `npx tsc --noEmit` exits 0 AND `npm run build` completes
  </acceptance_criteria>
  <done>Neither page can be used to read another organisation's data, and the completion page's assessor predicate is evaluated against the session org.</done>
</task>

<task type="auto">
  <name>Task 2: Role-gate requestAssessorReview (WR-02) and validate the observation completionId (WR-05)</name>
  <files>src/actions/observations.ts</files>
  <read_first>
    - src/actions/observations.ts (the WHOLE file — 462 lines: `recordObservation` at 37-107 including the sop lookup at 54-56, the gate at 58-87 and the insert at 89-100; the `RECORDER_ROLES` const at line 171; the sibling gates in `listObservationsForPerson` (240) and `listWorkerSopsForPicker` (269) which are the shape to copy; and `requestAssessorReview` at 367-422 including its existing org-scoped SOP check at 378-384)
    - src/lib/validators/observations.ts (`RecordObservationSchema` — `completionId` is an unconstrained optional UUID, which is why the action must validate it)
    - supabase/migrations/00057_restore_sop_observations_cross_org_guard.sql (the live insert policy — `sop_observation_refs_in_org(sop_id, observed_worker_id, organisation_id)` deliberately covers sop_id and observed_worker_id but NOT completion_id, which is the gap this task closes in the action layer)
  </read_first>
  <action>
    **WR-02 — role gate on `requestAssessorReview` (line 367).** Every sibling action in this file gates on `RECORDER_ROLES` or the admin pair; this one checks only `userId` + `organisationId` and then uses the ADMIN client to insert one `worker_notifications` row per admin/safety_manager. As shipped, any authenticated worker can POST-invoke it (server actions are endpoints) and spam every admin's notification badge, one request per published SOP.

    - Add `role` to the `getSessionContext()` destructure at line 370.
    - Immediately after the `userId` check and BEFORE the `organisationId` check, insert:

        `if (!role || !RECORDER_ROLES.includes(role)) return { success: false, error: 'Only supervisors, admins and safety managers can request assessment.' }`

      Use the exact `RECORDER_ROLES` const already declared at line 171 — do not re-declare or inline a fresh array. Match the error-string style of the sibling gate at line 48. The gate must precede the `createAdminClient()` call at line 374, so no service-role work happens for an unauthorized caller.

    **WR-05 — validate `completionId` before it is stamped onto a permanent audit row (line 89-100).** `sop_observations` is append-only, so a foreign or unrelated `completion_id` is unfixable once written.

    - In `recordObservation`, after the SOP lookup at lines 54-56 and BEFORE the ASR-01 gate block at line 58 (so an invalid reference is rejected without spending a predicate read), insert a guarded existence check that runs only when `completionId` is present:

      Query `sop_completions` with `createAdminClient()`, selecting `id`, filtered on `.eq('id', completionId)`, `.eq('organisation_id', organisationId)` and `.eq('worker_id', workerId)`, using `.maybeSingle()`. If no row comes back, return `{ success: false, error: 'Completion not found.' }`.

      Use the admin client, not the session client: `sop_completions` RLS returns a supervisor only their assigned workers' rows, so a session read would falsely reject legitimate recorders (the 2026-07-20 "RLS silently EMPTIES a same-org read a server action legitimately needs" class, inverted into a false deny). Both `organisationId` and `workerId` used as filters are already session-derived / schema-validated at this point, so the admin client self-enforces its own scope — state this in the comment, matching the rationale comments already present at lines 65-74 and 289-294.

      `workerId` is the observed worker from `parsed.data` (destructured at line 52) — binding the completion to the SAME worker the observation is about is the whole point; an in-org completion belonging to a different worker is still wrong evidence.

    Change nothing else: the insert payload, the gate, the error codes, and every other action in the file stay byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - In the `requestAssessorReview` body (sliced from `export async function requestAssessorReview` to `export interface AssessmentRequest`), `RECORDER_ROLES.includes(role)` appears and its index is LESS than the index of `createAdminClient(` in the same slice
    - `grep -c "const RECORDER_ROLES" src/actions/observations.ts` == 1 — the const was reused, not duplicated
    - In the `recordObservation` body (sliced from `export async function recordObservation` to `getObservationLabels`), the literal `.eq('worker_id', workerId)` appears, and its index is LESS than the index of `from('sop_observations').insert(`
    - That same slice contains `.eq('organisation_id', organisationId)` positioned before the insert, and the string `Completion not found.`
    - The completionId-validation block is positioned BEFORE `verdict === 'performed_to_sop'` (index comparison) — invalid references are rejected without a predicate read
    - `npx tsc --noEmit` exits 0 AND `npm run build` completes (`observations.ts` is a `'use server'` module — every value export must stay async, CLAUDE.md 2026-06-27)
  </acceptance_criteria>
  <done>Notification fan-out is restricted to recorder roles, and a permanent observation row can only cite a completion belonging to the same org and the same observed worker.</done>
</task>

<task type="auto">
  <name>Task 3: Regression spec — positional guards plus a directory-wide admin-client sweep</name>
  <files>tests/phase37/gap-org-guards.spec.ts</files>
  <read_first>
    - tests/phase37/assessor-gate.spec.ts (the phase's source-contract idiom: `readFileSync` + `.replace(/\r\n/g, '\n')`, index-comparison assertions for wiring POSITION, slice-scoped windows for per-function assertions, and the registration header comment naming the project regex)
    - tests/phase34/observation-cross-org-isolation.spec.ts (the phase-34 cross-org guard this phase already regressed once — read to avoid duplicating what it already covers, and to match its naming of the T-34-03-01 threat)
    - playwright.config.ts lines 483-505 (the `phase37` project: `testDir: '.'`, `testMatch: /tests\/phase37\/.*\.(spec|test)\.ts$/` — deliberately broad, so a new file in `tests/phase37/` needs NO config edit; CLAUDE.md 2026-05-25 requires proving discovery anyway)
    - All three files modified in Tasks 1-2
  </read_first>
  <action>
    Create `tests/phase37/gap-org-guards.spec.ts` with a header comment naming the closure source (37-VERIFICATION.md gaps CR-01, WR-02, WR-05 plus the rule-5 sibling) and the project regex it registers under.

    Four describe blocks, all source-contract over `readFileSync` with `\r\n` stripped:

    1. **CR-01 — completion detail page.** Assert `organisationId` is destructured from `getSessionContext()`; assert the guard literal `data.organisation_id !== organisationId` is present exactly once; assert its index is less than the index of `createSignedUrl` AND less than the index of `isSignedOffAssessor(`; assert the file contains `isSignedOffAssessor(userId, data.sop_id, admin, organisationId)`; assert `data.organisation_id` occurs exactly once in the whole file (proving the predicate no longer consumes the row's org).

    2. **Rule-5 sibling — video versions page.** Assert `sop.organisation_id !== organisationId` present; assert the `sops` `.select(` slice contains `organisation_id`.

    3. **WR-02 / WR-05 — observations.ts.** Slice `requestAssessorReview` (from its `export async function` to `export interface AssessmentRequest`) and assert `RECORDER_ROLES.includes(role)` appears at an index below `createAdminClient(` within that slice. Slice `recordObservation` (to `export async function getObservationLabels`) and assert `.eq('worker_id', workerId)` appears at an index below `from('sop_observations').insert(`, and below/above per Task 2's ordering that it also precedes `verdict === 'performed_to_sop'`.

    4. **Systemic sweep — every protected page that uses the admin client carries an org comparison.** Walk `src/app/(protected)` with `readdirSync(dir, { recursive: true })`, keep entries ending in `page.tsx`, read each, keep those containing `createAdminClient(`, and assert every one also contains `organisationId`. Assert the collected set is non-empty first (an empty glob passing vacuously is the 2026-05-25 "test that tests nothing" trap) and include the file list in the failure message so the next offender is named, not just counted. This is the guard that makes the fix systemic rather than per-file: today it covers `activity/[completionId]`, `admin/sops/pipeline/[pipelineId]`, `admin/sops/[sopId]/video` and `admin/ai-settings`, and it fails the moment a fifth unguarded one is added.

    Use `[\s\S]` rather than the `/s` regex flag (CLAUDE.md 2026-06-02 — the project's TS target rejects it and `next build`'s typecheck scope differs from root `tsc`).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase37 && npx playwright test --project=phase37</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase37` lists `tests/phase37/gap-org-guards.spec.ts` (zero discovered = the spec is not gating anything, CLAUDE.md 2026-05-25)
    - `npx playwright test --project=phase37` green, zero `test.fixme` in the new file
    - The spec contains at least three index-comparison assertions, not only `toContain` presence checks
    - The systemic sweep asserts the discovered file list is non-empty before asserting over it
    - Reverting either Task 1 edit or either Task 2 edit makes this spec RED — state in the SUMMARY which assertion catches each of the four fixes
    - `npm run test` shows no new failures in other projects
  </acceptance_criteria>
  <done>Each closed hole has an assertion that fails if it reopens, and the org-guard rule is enforced across the whole protected route tree rather than on the two files that happened to be reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-supplied route param → admin-client fetch | `completionId` / `sopId` are attacker-chosen UUIDs; the row they resolve to is attacker-influenced |
| service-role client → `sop_completions` / `sops` / `worker_notifications` | RLS bypassed; org scope is the caller's own responsibility |
| authenticated non-recorder → `requestAssessorReview` server action | Server actions are POST-invokable endpoints for any authenticated session |
| client-supplied `completionId` → append-only `sop_observations` row | Once written, the reference is permanent and unfixable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-07-01 | Information Disclosure | `/activity/[completionId]` admin-client fetch | mitigate | `data.organisation_id !== organisationId` redirect placed before the presigned-URL `Promise.all`, asserted positionally so it cannot be moved below the leak point |
| T-37-07-02 | Elevation of Privilege | assessor predicate fed the row's org instead of the session org | mitigate | Fourth argument changed to session-derived `organisationId`; asserted by an exact-literal match plus an occurrence count on `data.organisation_id` |
| T-37-07-03 | Information Disclosure | `/admin/sops/[sopId]/video` admin-client fetch (rule-5 sibling) | mitigate | Same guard shape as `getSopVersionForDiff`; `organisation_id` added to the select so the comparison is possible at all |
| T-37-07-04 | Denial of Service | `requestAssessorReview` notification fan-out | mitigate | `RECORDER_ROLES` gate positioned before `createAdminClient()`, asserted positionally |
| T-37-07-05 | Repudiation | foreign/unrelated `completion_id` on a permanent audit row | mitigate | Admin-client existence check filtered on both `organisation_id` and `worker_id` before insert; the RLS policy's `sop_observation_refs_in_org` deliberately does not cover completion_id, so the action layer is the only place this can be closed |
| T-37-07-06 | Information Disclosure | the NEXT unguarded admin-client page fetch | mitigate | Directory-wide sweep over `src/app/(protected)/**/page.tsx` — per-file guards are not coverage (2026-07-20) |
| T-37-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages in this closure pass (37-RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx playwright test --list --project=phase37` discovers 7 files including `gap-org-guards.spec.ts`
- `npx playwright test --project=phase37` green
- `npx tsc --noEmit` clean; `npm run build` clean
- `npm run test` shows no new failures elsewhere (notably `tests/phase34/*` and `tests/phase37/assessor-ui-*`)
</verification>

<success_criteria>
- A cross-org completion UUID redirects to `/activity` before any signed photo URL is minted
- A cross-org SOP UUID redirects to `/admin/sops` before any video job is enumerated
- The assessor predicate on the completion page consumes only the session organisation
- A non-recorder role cannot reach the notification fan-out
- An observation's `completion_id` is provably same-org and same-worker before insert
- The org-guard rule is enforced across every protected page that uses the admin client, not just the two that were reviewed
</success_criteria>

## Artifacts this phase produces

Extends `37-01-PLAN.md` § "Artifacts this phase produces". This plan changes no exported symbol signatures. It adds:

| Artifact | Kind | Change |
|----------|------|--------|
| `src/app/(protected)/activity/[completionId]/page.tsx` | route (modified) | org-scope redirect guard; predicate now takes session `organisationId` |
| `src/app/(protected)/admin/sops/[sopId]/video/page.tsx` | route (modified) | `organisation_id` added to select; org-scope redirect guard |
| `recordObservation` (`src/actions/observations.ts`) | server action (modified) | completionId org+worker existence check before insert |
| `requestAssessorReview` (`src/actions/observations.ts`) | server action (modified) | `RECORDER_ROLES` gate before any admin-client work |
| `tests/phase37/gap-org-guards.spec.ts` | spec (new) | positional regression guards + directory-wide admin-client org sweep |

No route added, removed or renamed, and no navigation/redirect *flow* changed for an authorized user — `src/lib/journeys/journeys.ts` and `src/lib/uat/tests.ts` need no edit in this plan (an unauthorized cross-org viewer being bounced is not a pathway).

<output>
Create `.planning/phases/37-assessor-governance/37-07-SUMMARY.md` when done
</output>
