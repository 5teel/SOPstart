---
phase: 37-assessor-governance
plan: 03
type: execute
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - scripts/apply-phase37-migration.mjs
  - src/actions/observations.ts
  - tests/phase37/assessor-gate.spec.ts
autonomous: true
requirements: [ASR-01]

must_haves:
  truths:
    - "Migration 00056's five columns, two CHECK constraints and the re-created insert policy are LIVE on the remote database and visible to PostgREST"
    - "A supervisor who is not signed off on SOP X cannot record a `performed_to_sop` observation about anyone on SOP X (D-03)"
    - "The same supervisor CAN still record a `needs_support` coaching observation on SOP X, and the predicate is never even called on that path (D-04, branch-before-gate)"
    - "An admin or safety_manager who is not signed off can record the advancing observation only by supplying a reason, and the row is stamped `is_assessor_override = true` with that reason (D-05/D-06)"
    - "A blocked supervisor can send a request for someone to assess THEM on that SOP, and an admin/safety_manager can see who + which SOP (D-08)"
  artifacts:
    - path: "scripts/apply-phase37-migration.mjs"
      provides: "non-interactive db push + PGRST reload + cache-bypassing assertions for all five columns, both constraints and the policy"
      contains: "00056_assessor_governance"
    - path: "src/actions/observations.ts"
      provides: "gated recordObservation + assessor status read + request/list actions"
      exports: ["recordObservation", "getAssessorStatusForSop", "requestAssessorReview", "listAssessmentRequests"]
  key_links:
    - from: "src/actions/observations.ts"
      to: "src/lib/competency/assessor.ts"
      via: "isSignedOffAssessor called inside the verdict === 'performed_to_sop' branch only"
      pattern: "isSignedOffAssessor\\("
    - from: "src/actions/observations.ts"
      to: "worker_notifications"
      via: "admin-client insert with type 'assessment_requested' and subject_user_id = the blocked recorder"
      pattern: "assessment_requested"
---

<objective>
Apply migration 00056 to the live database, then put the assessor gate in front of the advancing observation verdict and add the three read/request actions the UI needs.

Purpose: `tsc` and `next build` both pass without the push (the types are hand-extended, not generated from the live DB), so without the [BLOCKING] task the phase reaches verification in a false-positive state and the first real override insert fails on a missing column. And the gate itself is the phase: success criterion 1 is exactly "only a signed-off assessor can record a competence-advancing observation".
Output: live schema, gated `recordObservation`, `getAssessorStatusForSop`, `requestAssessorReview`, `listAssessmentRequests`, and the observation half of `assessor-gate.spec.ts` flipped live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-assessor-governance/37-CONTEXT.md
@.planning/phases/37-assessor-governance/37-RESEARCH.md
@.planning/phases/37-assessor-governance/37-PATTERNS.md
@.planning/phases/37-assessor-governance/37-01-SUMMARY.md
@.planning/phases/37-assessor-governance/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration 00056 to the live database and prove every object exists</name>
  <files>scripts/apply-phase37-migration.mjs</files>
  <read_first>
    - scripts/apply-phase36-migration.mjs (the exact harness to copy-adapt: its `.env.local` loader, its `npx supabase db push` invocation using `SUPABASE_ACCESS_TOKEN` for non-interactive auth, its Management-API `POST https://api.supabase.com/v1/projects/{ref}/database/query` assertion path, and its Management-API SQL fallback when `db push` demands an interactive prompt)
    - supabase/migrations/00056_assessor_governance.sql (created in 37-01 — the exact column, constraint and policy names to assert)
    - .planning/phases/37-assessor-governance/37-RESEARCH.md § Assumptions Log A3 (`worker_notifications.type` has no CHECK constraint restricting values — this must be verified against the LIVE schema here, not just the migration file, because a later migration could have added one)
  </read_first>
  <action>
    Copy-adapt `scripts/apply-phase36-migration.mjs` to `scripts/apply-phase37-migration.mjs`, repointing `MIGRATION_FILE` at `supabase/migrations/00056_assessor_governance.sql`. Keep the `.env.local` loader, the `npx supabase db push` call with `SUPABASE_ACCESS_TOKEN`, the Management-API SQL path for assertions, and the fallback of executing the migration body directly through `database/query` if `db push` demands an interactive prompt (the phase 25/29/34/36 precedent). Record which path was used in the SUMMARY.

    Replace the phase-36 assertions with these five, all issued as raw SQL through the Management API so they bypass the PostgREST schema cache entirely:

    1. Columns exist: query `information_schema.columns` and assert exactly one row for each of the five — `public.sop_observations.is_assessor_override` (`data_type='boolean'`, `is_nullable='NO'`, `column_default` containing `false`), `public.sop_observations.override_reason` (`text`, nullable), `public.completion_sign_offs.is_assessor_override` (same shape as the first), `public.completion_sign_offs.override_reason` (`text`, nullable), `public.worker_notifications.subject_user_id` (`uuid`, nullable).
    2. Both CHECK constraints are LIVE, not merely present in the file: probe `pg_constraint` for `sop_observations_override_reason_required` and `completion_sign_offs_override_reason_required` by name on their respective relations, AND prove the behaviour by attempting, inside a transaction that ROLLS BACK, an insert with `is_assessor_override = true` and `override_reason = null` — it must be REJECTED. A constraint in the file but not in the database is exactly the false positive this task exists to prevent.
    3. The re-created insert policy is live and carries the override clause: query `pg_policies` for `policyname = 'sop_observations_insert_recorder'` on `public.sop_observations` and assert the `with_check` expression text contains both `current_user_role` and `is_assessor_override`.
    4. Assumption A3 check: query `pg_constraint` for any CHECK constraint on `public.worker_notifications` whose definition references the `type` column. Assert ZERO rows. If any exists, FAIL loudly with the constraint definition printed — `'assessment_requested'` would be rejected at runtime and Task 3 needs a migration instead of a bare new string value.
    5. Then issue `NOTIFY pgrst, 'reload schema';` through the same Management API call so PostgREST sees the columns immediately rather than within its ~1-minute auto-reload window.

    Distinguish error codes in the output: `PGRST205` (the phrase "in the schema cache") means a STALE CACHE, not a missing object; `42P01`/`42703` mean genuinely absent (CLAUDE.md 2026-06-15). Never report a pass on a non-zero exit or on a PGRST205 without a successful re-probe after the reload.
  </action>
  <verify>
    <automated>node scripts/apply-phase37-migration.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/apply-phase37-migration.mjs` exits 0 and prints an explicit PASS line for each of the five assertion groups
    - The `information_schema` probe returns all five columns with the stated types and nullability
    - The rolled-back `is_assessor_override = true, override_reason = null` insert is REJECTED by the database, not accepted
    - The `pg_policies` probe shows `sop_observations_insert_recorder`'s `with_check` text containing `is_assessor_override`
    - The `worker_notifications.type` CHECK-constraint probe returns zero rows (A3 confirmed against the live schema)
    - The script prints the `NOTIFY pgrst` reload confirmation
  </acceptance_criteria>
  <done>All five columns, both constraints and the amended insert policy are live on the remote database and visible to PostgREST.</done>
</task>

<task type="auto">
  <name>Task 2: Gate recordObservation on the advancing verdict only (D-03/D-04/D-05/D-06)</name>
  <files>src/actions/observations.ts</files>
  <read_first>
    - src/actions/observations.ts (the WHOLE file — `recordObservation` lines 31-68 is the function being extended, the file-header comment at lines 3-14 explicitly claims "no admin client is reached for on the observation insert path" and MUST be updated, and `RECORDER_ROLES` at line 132 plus `resolveDisplayNames` at line 148 are reused by Task 3)
    - src/lib/competency/assessor.ts (created in 37-02 — the exact `isSignedOffAssessor(personId, sopId, client, orgId)` signature and its self-enforced org scoping)
    - src/lib/validators/observations.ts (extended in 37-01 — `overrideReason` is optional with a 10-char floor; there is deliberately NO client-supplied override flag)
    - src/actions/completions.ts lines 127-132 (the `if (decision === 'rejected')` early-branch idiom this gate's `if (verdict === 'performed_to_sop')` branch mirrors)
  </read_first>
  <action>
    Extend `recordObservation` in `src/actions/observations.ts`. Import `isSignedOffAssessor` from `@/lib/competency/assessor`.

    Insert the gate AFTER the existing `sops.version` lookup and BEFORE the insert, as an early branch on verdict:

    - If `verdict !== 'performed_to_sop'`, the predicate is NEVER called. `needs_support` is the coaching-not-discipline default (Phase 34 D-01, this phase's D-04) and is the higher-frequency write — calling the predicate unconditionally would both cost queries on every coaching note and put the gate one edit away from blocking coaching (37-RESEARCH Pitfall 3).
    - If `verdict === 'performed_to_sop'`: call `await isSignedOffAssessor(userId, sopId, createAdminClient(), organisationId)`. When it returns true, proceed with `isOverride = false`. When it returns false:
      - `role === 'admin' || role === 'safety_manager'` (D-06 — plain supervisors never get the override): if `parsed.data.overrideReason` is absent, return `{ success: false, error: 'ASSESSOR_OVERRIDE_REQUIRED' }` so the client can reveal its reason field; otherwise set `isOverride = true` and stamp the reason.
      - any other role (i.e. `supervisor`): return `{ success: false, error: 'NOT_SIGNED_OFF_ASSESSOR' }` so the client can show the blocked state and the request CTA.
    - Return these two values as BARE error codes, not prose. They are protocol between server and client (37-04/37-05 branch on them); user-facing copy lives in the components.

    Use `createAdminClient()` for the PREDICATE READ ONLY. Reason, and state it in a code comment: the predicate reads `sop_completions`, `completion_sign_offs` and `sop_observations` on the CALLER's own behalf, and RLS on those tables does not reliably return a supervisor's own sign-off rows — a session-client read would return empty and report a legitimate assessor as blocked (the 2026-07-20 "RLS silently EMPTIES a same-org read the action needs" class, inverted into a false deny). The predicate self-enforces org scope on every query, and `organisationId` here is session-derived and never accepted from client input. The INSERT itself stays on the SESSION client exactly as today — RLS remains the write gate (Phase 34 D-12).

    Then update the insert payload with `is_assessor_override: isOverride` and `override_reason: isOverride ? parsed.data.overrideReason : null`. Never write `override_reason` when `isOverride` is false — the DB CHECK allows it, but a reason without an override is a misleading audit row.

    Finally, correct the file-header comment: it currently asserts no admin client is reached on the observation insert path. Amend it to say the admin client is now used for the Phase 37 assessor PREDICATE READ (with the reason above) while the insert remains session-client + RLS.

    Change nothing else in `recordObservation` — the Zod parse, the role allow-list, the `organisationId` check, the server-resolved `sop_version` (D-10) and the error handling all stay byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isSignedOffAssessor(" src/actions/observations.ts` == 1 — one call site, inside the advancing branch
    - The `isSignedOffAssessor` call appears textually AFTER `verdict === 'performed_to_sop'` in the file (branch-before-gate; assert by index comparison, not by eye)
    - `grep -c "NOT_SIGNED_OFF_ASSESSOR\|ASSESSOR_OVERRIDE_REQUIRED" src/actions/observations.ts` >= 2 — both bare error codes present
    - The insert payload contains `is_assessor_override:` and `override_reason:`
    - The file header no longer claims the observation path reaches no admin client
    - `npx tsc --noEmit` exits 0 AND `npm run build` completes — `observations.ts` is a `'use server'` module, and a sync export or non-async helper added here breaks `next build` while passing `tsc` (CLAUDE.md 2026-06-27)
  </acceptance_criteria>
  <done>The advancing verdict is gated on derived assessorship; the coaching verdict is untouched and never pays the predicate cost.</done>
</task>

<task type="auto">
  <name>Task 3: Assessor status read, request-assessment write, request list (D-08) + flip the observation gate spec live</name>
  <files>src/actions/observations.ts, tests/phase37/assessor-gate.spec.ts</files>
  <read_first>
    - src/actions/observations.ts (as amended in Task 2 — reuse `RECORDER_ROLES` at line 132 rather than declaring a third copy of the role array, which 37-RESEARCH Pitfall 5 warns about; reuse `resolveDisplayNames` at line 148 for the request list's subject names)
    - src/actions/completions.ts lines 206-222 (the `worker_notifications` admin-client insert to copy for the request write — same shape, `type: 'assessment_requested'` instead of `'completion_rejected'`, plus the new `subject_user_id`)
    - supabase/migrations/00009_worker_notifications.sql (the `admins_can_insert_notifications` policy is admin/safety_manager-ONLY — this is precisely why the request write, made BY a blocked supervisor, must use the admin client with self-enforced org scope; a session-client insert would fail with `42501 new row violates row-level security policy`, the exact Phase 25 bug)
    - tests/phase37/assessor-gate.spec.ts (the stubs created in 37-01 to flip live here)
  </read_first>
  <action>
    Add three exported async functions to `src/actions/observations.ts`. All three start with `getSessionContext()`, check `userId`, check `role` against the existing `RECORDER_ROLES` constant (do not declare a new array), then check `organisationId`.

    1. `getAssessorStatusForSop(sopId: string): Promise<{ isAssessor: boolean; canOverride: boolean }>` — returns `{ isAssessor: await isSignedOffAssessor(userId, sopId, createAdminClient(), organisationId), canOverride: role === 'admin' || role === 'safety_manager' }`. On any auth/role/org failure return `{ isAssessor: false, canOverride: false }` (fail-closed). Export the return type as `AssessorStatus`. This exists so the UI can DISABLE the advancing control up-front per D-08 rather than only after a failed save; it is UX only — Task 2's server-side recomputation remains the authority and is never skipped.

    2. `requestAssessorReview(sopId: string): Promise<{ success: true } | { success: false; error: string }>` — the D-08 request path. Resolve the org's recipients by querying `organisation_members` for `organisation_id = organisationId` and `role in ('admin','safety_manager')` (Assumption A1: recipients are the override-capable roles, not a fan-out to every currently-signed-off peer, which would need a per-member predicate evaluation). Before inserting, dedupe: query `worker_notifications` for an existing row with `organisation_id = organisationId`, `sop_id = sopId`, `subject_user_id = userId`, `type = 'assessment_requested'`, `read = false` — if any exists, return `{ success: true }` without inserting (repeat taps must not spam the list). Otherwise insert one row per recipient with `organisation_id: organisationId`, `user_id: <recipient>`, `sop_id: sopId`, `subject_user_id: userId`, `type: 'assessment_requested'`, `read: false`. Use `createAdminClient()` for BOTH the recipient lookup and the insert, and add a comment naming the reason: the caller is typically a plain SUPERVISOR, and `admins_can_insert_notifications` only permits admin/safety_manager inserts, so a session-client write is denied — while service-role bypasses RLS, so this function self-enforces org scope by deriving `organisationId` from the session and filtering the recipient query on it (CLAUDE.md 2026-06-15). Also verify the SOP belongs to `organisationId` before inserting, so a foreign `sopId` cannot create rows referencing another org's SOP. Return a friendly error if there are zero recipients.

    3. `listAssessmentRequests(): Promise<AssessmentRequest[]>` — role-gated to `admin`/`safety_manager` only (a plain supervisor has no assess-others queue). Read the CALLER's own unread requests with the SESSION client (`worker_notifications` RLS `users_see_own_notifications` already scopes to `user_id = auth.uid()` + org — the correct gate, no admin client needed here): `select('id, sop_id, subject_user_id, created_at, sops(title)')`, `.eq('type', 'assessment_requested')`, `.eq('read', false)`, newest first, limit 20. Resolve subject display names via the existing `resolveDisplayNames` helper. Export the row type as `AssessmentRequest` with fields `id`, `sopId`, `sopTitle`, `subjectUserId`, `subjectName`, `createdAt`.

    Every one of these three MUST be `async` — a sync or non-async export in a `'use server'` module fails `next build` while passing `tsc` (CLAUDE.md 2026-06-27, the Phase 23-03 incident).

    Then flip `tests/phase37/assessor-gate.spec.ts` from `test.fixme` to live source-contract assertions over `src/actions/observations.ts`:
    - `isSignedOffAssessor` is imported and called exactly once, and its call-site index is greater than the index of `verdict === 'performed_to_sop'` (branch-before-gate, D-03/D-04 — assert positionally, not by mere presence, because presence alone is the 2026-06-05 dead-feature blind spot)
    - both bare error codes appear, and `'ASSESSOR_OVERRIDE_REQUIRED'` appears inside a branch that also references `'admin'`/`'safety_manager'` (D-06)
    - the insert payload lines contain `is_assessor_override` and `override_reason`
    - `requestAssessorReview` uses `createAdminClient(` and references `'assessment_requested'` and `subject_user_id`
    - `listAssessmentRequests` does NOT use `createAdminClient(` for its notification read (assert the substring is absent from that function's body slice) — RLS self-read is the intended gate
    - all three new exports are declared `export async function`
    Use `[\s\S]` rather than the `/s` flag and strip `\r` before multi-line matching.
  </action>
  <verify>
    <automated>npx playwright test --project=phase37 --project=phase35-unit</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase37` green with `assessor-gate.spec.ts` LIVE (zero fixme in that file)
    - `grep -c "export async function" src/actions/observations.ts` increased by exactly 3
    - `grep -c "RECORDER_ROLES" src/actions/observations.ts` shows the existing constant reused — no second role-array literal is introduced in the three new functions
    - `requestAssessorReview`'s body contains `createAdminClient(`, `'assessment_requested'`, `subject_user_id`, and a dedupe query on `read` + `subject_user_id` before its insert
    - `listAssessmentRequests`'s body contains NO `createAdminClient(` for the `worker_notifications` read
    - `npx tsc --noEmit` exits 0 AND `npm run build` completes
  </acceptance_criteria>
  <done>The UI has a status read, a request write and a request list; the observation gate is proven live by source-contract assertions that check wiring position, not token presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `recordObservation` | Untrusted `sopId`, `workerId`, `verdict`, `overrideReason` cross here |
| client → `requestAssessorReview` | Untrusted `sopId` crosses here and reaches a service-role insert |
| dev machine → live Supabase | Task 1 mutates production schema and re-creates a live RLS policy |
| service-role client → `worker_notifications` / evidence tables | RLS bypassed; org scope is the action's own responsibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Elevation of Privilege | non-assessor supervisor recording `performed_to_sop` | mitigate | Server-side `isSignedOffAssessor` recomputation inside the advancing branch; the client's disabled state is UX only and is never trusted. Enforced by a positional source-contract assertion, not a presence grep |
| T-37-03-02 | Elevation of Privilege | supervisor self-granting the override | mitigate | Override branch is reachable only for `role === 'admin' || role === 'safety_manager'` (D-06), with the role read from `getSessionContext()`, never from the payload; the 37-01 RLS conjunct is the second line |
| T-37-03-03 | Repudiation | override written without a reason | mitigate | Action returns `ASSESSOR_OVERRIDE_REQUIRED` before any insert when `overrideReason` is absent; Zod floors its length; the live DB CHECK (proved by Task 1's rolled-back probe) is the final backstop |
| T-37-03-04 | Tampering | `requestAssessorReview` creating rows against another org's SOP or members | mitigate | `organisationId` is session-derived only; the SOP is org-verified before insert; the recipient query is filtered on `organisation_id`; service-role use is accompanied by explicit self-enforced scoping (CLAUDE.md 2026-06-15) |
| T-37-03-05 | Denial of Service | repeat "request assessment" taps flooding the admin list | mitigate | Unread-duplicate check on (org, sop, subject, type) before insert; a repeat tap returns success without a new row |
| T-37-03-06 | Repudiation | false-positive verification from an unpushed migration | mitigate | [BLOCKING] Task 1 asserts all five columns, BOTH constraints (by behaviour, in a rolled-back transaction), the amended policy text, and Assumption A3 — all through cache-bypassing raw SQL before any code inserts the new columns |
| T-37-03-07 | Elevation of Privilege | legitimate assessor falsely DENIED because RLS emptied the evidence read | mitigate | The predicate read uses the service-role client with self-enforced org scope, precisely because `sop_assignments`/`completion_sign_offs`-style RLS returns zero rows for supervisor callers (2026-07-20 learning); the unit tests in 37-02 cover the positive path |
| T-37-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages in this phase (37-RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `node scripts/apply-phase37-migration.mjs` → all five assertion groups PASS
- `npx playwright test --project=phase37 --project=phase35-unit` green, `assessor-gate.spec.ts` fully live
- `npx tsc --noEmit` clean; `npm run build` clean
</verification>

<success_criteria>
- Migration 00056 is live and PostgREST-visible; both CHECK constraints reject a reasonless override in a real probe
- `recordObservation` blocks a non-assessor supervisor's advancing verdict and never calls the predicate for `needs_support`
- Admin/safety_manager override requires a reason and stamps both audit columns
- `getAssessorStatusForSop`, `requestAssessorReview`, `listAssessmentRequests` exist, are async, and use the correct client for each direction (service-role for the RLS-blocked write, session for the RLS-scoped read)
</success_criteria>

## Artifacts this phase produces

See `37-01-PLAN.md` § "Artifacts this phase produces". This plan introduces `scripts/apply-phase37-migration.mjs`, `getAssessorStatusForSop`, `requestAssessorReview`, `listAssessmentRequests`, types `AssessorStatus` / `AssessmentRequest`, error codes `NOT_SIGNED_OFF_ASSESSOR` / `ASSESSOR_OVERRIDE_REQUIRED`, and the `'assessment_requested'` notification type value.

<output>
Create `.planning/phases/37-assessor-governance/37-03-SUMMARY.md` when done
</output>
