---
phase: 37-assessor-governance
plan: 08
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/apply-phase37-migration.mjs
  - src/lib/competency/assessor.ts
  - src/lib/competency/__tests__/assessor.test.ts
  - src/components/observations/RecordObservationModal.tsx
  - src/app/(protected)/activity/[completionId]/CompletionDetailClient.tsx
  - tests/phase37/gap-migration-and-state.spec.ts
autonomous: true
requirements: [ASR-01]

must_haves:
  truths:
    - "Re-running scripts/apply-phase37-migration.mjs can never re-drop the 00057 cross-org write guard — the fallback applies 00056 AND 00057, in order (CR-02)"
    - "The script's own assertions FAIL if sop_observation_refs_in_org is missing from the live policy's with_check — it can no longer print ALL PASS over a reopened hole (CR-02)"
    - "A completion carrying both an approved and a rejected sign-off row still makes its owner a signed-off assessor — the approved row can no longer be shadowed (WR-01)"
    - "Changing the selected SOP in the observation modal clears the previous SOP's blocked/override/request state before the new status arrives (WR-03)"
    - "When the server returns ASSESSOR_OVERRIDE_REQUIRED to an admin whose client-side isAssessor was stale-true, the override sheet opens — the user can act on the error instead of reloading (WR-04)"
  artifacts:
    - path: "scripts/apply-phase37-migration.mjs"
      provides: "both-migration fallback apply + restored-conjunct assertion"
      contains: "sop_observation_refs_in_org"
    - path: "src/lib/competency/assessor.ts"
      provides: "order-independent sign-off evaluation"
      exports: ["isSignedOffAssessor"]
    - path: "src/lib/competency/__tests__/assessor.test.ts"
      provides: "behavioural proof that a rejected row cannot shadow an approved one"
    - path: "tests/phase37/gap-migration-and-state.spec.ts"
      provides: "source-contract regression guards for CR-02, WR-01, WR-03, WR-04"
  key_links:
    - from: "scripts/apply-phase37-migration.mjs"
      to: "supabase/migrations/00057_restore_sop_observations_cross_org_guard.sql"
      via: "MIGRATION_FILES array iterated in the db-push fallback"
      pattern: "00057_restore_sop_observations_cross_org_guard"
    - from: "src/app/(protected)/activity/[completionId]/CompletionDetailClient.tsx"
      to: "setOverrideSheetOpen"
      via: "server ASSESSOR_OVERRIDE_REQUIRED handler"
      pattern: "ASSESSOR_OVERRIDE_REQUIRED"
---

<objective>
Make the phase's own migration tool incapable of silently reopening the cross-tenant write hole it already caused once (CR-02), and close the three correctness/dead-end defects on the predicate and the two gate UIs (WR-01, WR-03, WR-04).

Purpose: CR-02 is the sharpest of the two blockers in kind — 00056 dropped the `sop_observation_refs_in_org` conjunct precisely BECAUSE the post-apply assertion did not check for it, and 00057's own header documents that. The applier still has both defects baked in, so the tool built to prevent this class currently triggers it and prints a green banner. WR-01 is a false-DENY in the safety predicate; WR-03/WR-04 are UI states that contradict their own in-code comments.
Output: a fixed applier, an order-independent predicate with a behavioural unit test, two corrected UI state machines, and `tests/phase37/gap-migration-and-state.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-assessor-governance/37-REVIEW.md
@.planning/phases/37-assessor-governance/37-VERIFICATION.md
@.planning/phases/37-assessor-governance/37-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration applier must apply 00056+00057 and assert the restored conjunct (CR-02)</name>
  <files>scripts/apply-phase37-migration.mjs</files>
  <read_first>
    - scripts/apply-phase37-migration.mjs (the WHOLE file — 400 lines: the header comment at 1-20, `MIGRATION_FILE` at line 29, the `managementSql` helper at 64-84, the banner at 90-96, the db-push try/catch at 98-136 including the fallback `readFileSync`+`managementSql` at 114-119 and the manual-instructions block at 122-135, assertion group 3 at 337-348, and the success banner at 385-391)
    - supabase/migrations/00057_restore_sop_observations_cross_org_guard.sql (the WHOLE file — its header documents exactly why the assertion gap is the root cause, and its policy body is the ground truth the assertion must pin: the four conjuncts `current_organisation_id`, `current_user_role`, `sop_observation_refs_in_org`, `is_assessor_override`)
    - supabase/migrations/00056_assessor_governance.sql (the policy body that LACKS the org-ref conjunct — this is what the fallback currently re-applies on its own)
  </read_first>
  <action>
    Two defects, both in this one file. Fix both; do not restructure anything else.

    **1. The fallback must apply both migrations, in order.** Replace the single `MIGRATION_FILE` const at line 29 with:

        `const MIGRATION_FILES = [ 'supabase/migrations/00056_assessor_governance.sql', 'supabase/migrations/00057_restore_sop_observations_cross_org_guard.sql' ]` (each `path.join(ROOT, ...)`)

    In the catch block at 114-119, replace the single read+apply with a sequential `for (const file of MIGRATION_FILES)` loop that reads and applies each via `managementSql`, logging the filename before each. Order matters and is not incidental: 00057 re-creates the same policy 00056 creates, so applying them out of order or applying 00056 alone leaves the live policy WITHOUT the `sop_observation_refs_in_org` conjunct — i.e. re-opens T-34-03-01 on production. Add a comment saying exactly that, so a future editor cannot "simplify" it back to one file.

    Update the manual-fallback instructions at 122-135 to name BOTH files in both Option A and Option B, in order, and add a line stating that applying 00056 without 00057 reopens the cross-org write guard.

    **2. Assertion group 3 must pin the restored conjunct.** In the check at 337-348, extend the `ok` expression to also require `withCheck.includes('sop_observation_refs_in_org')`, and update the assertion label to name all three required substrings. This is the assertion whose absence caused the original regression — a comment should say so and reference 00057's header.

    Also update the cosmetic strings so the tool does not misreport what it did: the header comment at 1-20, the banner at line 90/94, and the success message at 388-390 all say "00056" only — make them say "00056 + 00057". A tool that names one migration while applying two is how the next person re-introduces the single-file fallback.

    Do NOT run the script as part of this task — it mutates the live production Supabase project. It is verified by the source-contract spec in Task 3 and left for a deliberate operator run.
  </action>
  <verify>
    <automated>node --check scripts/apply-phase37-migration.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MIGRATION_FILE\b" scripts/apply-phase37-migration.mjs` == 0 — the single-file const is gone, not shadowed
    - The file contains `MIGRATION_FILES` and both literals `00056_assessor_governance.sql` and `00057_restore_sop_observations_cross_org_guard.sql`, with the 00056 index LESS than the 00057 index (order is asserted, not assumed)
    - The catch-block slice (from `supabase db push failed` to `applied via`) contains a `for (` loop over `MIGRATION_FILES`
    - Assertion group 3's `ok:` expression contains all three of `current_user_role`, `is_assessor_override`, `sop_observation_refs_in_org`
    - `node --check scripts/apply-phase37-migration.mjs` exits 0
    - The script is NOT executed during this task (it writes to the live DB)
  </acceptance_criteria>
  <done>The applier can no longer re-drop the org-ref guard, and would fail loudly rather than print ALL PASS if the live policy ever lost it.</done>
</task>

<task type="auto">
  <name>Task 2: Order-independent sign-off evaluation (WR-01) and two UI state fixes (WR-03, WR-04)</name>
  <files>src/lib/competency/assessor.ts, src/lib/competency/__tests__/assessor.test.ts, src/components/observations/RecordObservationModal.tsx, src/app/(protected)/activity/[completionId]/CompletionDetailClient.tsx</files>
  <read_first>
    - src/lib/competency/assessor.ts (the WHOLE file — 92 lines: the `latestOf` helper at 24-27, the completions query at 43-51, the parallel sign-off/observation queries at 53-64, `signOffByCompletion` at line 69, and the five inputs handed to `classifyCompetency` at 83-89)
    - src/lib/competency/__tests__/assessor.test.ts lines 1-60 (the `makeStubClient` chainable stub and the `targetSop` fixture — every new case must reuse this stub, not a new fake; the composed `resolveLineage`/`classifyCompetency` run for real against it)
    - src/lib/competency/classify.ts (`classifyCompetency` — confirm that a `rejected` sign-off is NOT a reset input; only `latestNeedsSupportAt` resets, which is why the fix below is safe)
    - src/components/observations/RecordObservationModal.tsx lines 60-140 (the open-transition reset at 70-83, the labels/picker effect at 88-102, the assessor-status effect at 107-117 WITH its now-incorrect comment at 104-106, and the derived `blocked`/`canOverride`/`blockedVerdict` at 133-138)
    - src/app/(protected)/activity/[completionId]/CompletionDetailClient.tsx lines 100-215 (the state block at 103-113, `blockedFromApproving` at 119, `handleApprove` at 121-166 including the `else` branch at 155-160 whose comment names this exact race, and `handleApproveClick` at 168-174)
  </read_first>
  <action>
    **WR-01 — `src/lib/competency/assessor.ts`.** `signOffByCompletion = new Map(signOffs.map(s => [s.completion_id, s]))` keeps only the LAST row per completion in unordered query results. `completion_sign_offs` is append-only and `signOffCompletion` never checks for an existing row (the `alreadySigned` guard is client-side only), so two rows per completion is reachable — and if the retained row is `rejected`, a legitimately signed-off assessor is DENIED, and the approved timestamp is dropped from `latestPositiveEvidenceAt`, skewing the D-02 reset comparison.

    - Delete the `signOffByCompletion` Map entirely (line 69).
    - `hasSignOff` becomes `signOffs.some(s => s.decision === 'approved')`.
    - The sign-off half of `latestPositiveEvidenceAt` becomes `...signOffs.filter(s => s.decision === 'approved').map(s => s.created_at)`.
    - The Map was never needed for scoping: `signOffs` is already restricted by `.in('completion_id', completionIds)` where `completionIds` come from the org-scoped, person-scoped completions query at 43-51. Note that in a comment so the Map is not reintroduced as a "missing scope" fix.
    - `hasCompletion`, `latestNeedsSupportAt`, the `classifyCompetency` call and the final `result.state === 'competent_signed_off'` comparison stay byte-identical. Do not fork or re-implement any ladder logic here (the file's own header rule).

    **WR-01 test — `src/lib/competency/__tests__/assessor.test.ts`.** Add one behavioural case using the existing `makeStubClient` + `targetSop`: a single completion `c1` with TWO `completion_sign_offs` rows — `{completion_id:'c1', decision:'approved', created_at:'2026-01-02T00:00:00.000Z'}` followed by `{completion_id:'c1', decision:'rejected', created_at:'2026-01-03T00:00:00.000Z'}` — and no observations. Expect `true`. Name it for WR-01 and state in the test name that the rejected row must not shadow the approved one. This case is RED against the current Map implementation and GREEN after the fix — say so in a comment.

    **WR-03 — `src/components/observations/RecordObservationModal.tsx`.** Per-SOP state is reset only on the `open` transition (lines 79-82), so tapping "Change" and picking a different SOP leaves the PREVIOUS SOP's `assessorStatus` driving `blocked`/`canOverride` until the new fetch resolves — a blocked panel (or an enabled advancing verdict) flashes for the wrong SOP, and "Request sent" can display for a SOP no request was made for.

    - Move the per-SOP resets into the `[sopId]` effect (line 107) so they run on EVERY `sopId` change: set `assessorStatus` to null, `requestSent` to false, `overrideOpen` to false and `overrideReason` to `''`, then `if (!sopId) return` and proceed with the existing fetch + `cancelled` guard unchanged.
    - Rewrite the comment at 104-106 to describe what the code now actually does (it currently asserts the opposite). Leave the open-transition reset block at 70-83 in place — it also resets non-per-SOP fields.

    **WR-04 — `CompletionDetailClient.tsx`.** `isAssessor` is computed once at page render. If a `needs_support` reset lands between render and click, an admin clicks Approve, `handleApproveClick` skips the sheet (`blockedFromApproving` is false), the server returns `ASSESSOR_OVERRIDE_REQUIRED`, and the client renders copy telling the user to supply a reason — with no way to supply one, because the sheet only opens when `blockedFromApproving && canOverride` and `blockedFromApproving` never updates. Dead end in exactly the race the code's own comment at line 156 names.

    - In `handleApprove`'s `else` branch (155-160), before `setActionError(...)`, add: if `result.error === 'ASSESSOR_OVERRIDE_REQUIRED' && canOverride`, call `setOverrideSheetOpen(true)`.
    - Keep `setActionError(mapSignOffError(result.error))` on every path — the sheet opening does not replace the explanation.
    - This is safe when the sheet is already open (the override-confirm path re-entering with a too-short reason): setting an already-true state is a no-op. Do not gate it on the sheet being closed.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35-unit -g "assessor" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "signOffByCompletion" src/lib/competency/assessor.ts` == 0
    - `src/lib/competency/assessor.ts` contains `signOffs.some(s => s.decision === 'approved')` and `signOffs.filter(s => s.decision === 'approved')`
    - `npx playwright test --project=phase35-unit -g "assessor"` passes with 9+ tests (8 existing plus the new WR-01 case) — record the exact count in the SUMMARY
    - The new unit case is proven RED against the old implementation: state in the SUMMARY that it was run against the pre-fix Map version (e.g. via `git stash` or a temporary local revert) and observed to FAIL, per CLAUDE.md 2026-06-05 — a test that passes both before and after is testing nothing
    - In `RecordObservationModal.tsx`, the effect slice from `useEffect(() => {` preceding `getAssessorStatusForSop` through `}, [sopId])` contains `setAssessorStatus(null)`, `setRequestSent(false)` and `setOverrideOpen(false)`
    - In `CompletionDetailClient.tsx`, the 400-character window following `ASSESSOR_OVERRIDE_REQUIRED` contains `setOverrideSheetOpen(true)`
    - `npx tsc --noEmit` exits 0 AND `npm run build` completes
  </acceptance_criteria>
  <done>A rejected sign-off can no longer shadow an approved one, the modal's per-SOP state matches its own documentation, and the server's override demand is actionable without a page reload.</done>
</task>

<task type="auto">
  <name>Task 3: Regression spec for the applier and the two UI state machines</name>
  <files>tests/phase37/gap-migration-and-state.spec.ts</files>
  <read_first>
    - tests/phase37/assessor-ui-signoff.spec.ts (the phase's slice-scoped source-contract idiom — how a window is taken around a control's markup so an assertion proves WIRING, not token presence; reuse its helper shape rather than inventing another)
    - tests/phase37/override-audit-schema.spec.ts (how this phase asserts over migration SQL files)
    - playwright.config.ts lines 483-505 (the `phase37` project regex is deliberately broad — a new `tests/phase37/*.spec.ts` file needs no config edit, but discovery must still be PROVEN, CLAUDE.md 2026-05-25)
    - All four files modified in Tasks 1-2
  </read_first>
  <action>
    Create `tests/phase37/gap-migration-and-state.spec.ts`, header comment naming the closure source (37-VERIFICATION.md CR-02 plus 37-REVIEW.md WR-01/WR-03/WR-04) and the project regex it registers under. Read every target with `readFileSync` + `.replace(/\r\n/g, '\n')` (CLAUDE.md 2026-07-18 — a worktree checkout re-smudges to CRLF and breaks `\n`-literal matching).

    **CR-02 block:**
    - `scripts/apply-phase37-migration.mjs` contains `MIGRATION_FILES` and does NOT contain the standalone identifier `MIGRATION_FILE` followed by a non-`S` character (assert the old const is gone, using a word-boundary regex rather than a substring, since `MIGRATION_FILES` contains `MIGRATION_FILE`).
    - Both migration filenames appear, and `indexOf('00056_assessor_governance.sql') < indexOf('00057_restore_sop_observations_cross_org_guard.sql')` — apply order asserted, not assumed.
    - The `ok:` expression of assertion group 3 (slice from `sop_observations_insert_recorder policy with_check` forward ~600 chars) contains all three of `current_user_role`, `is_assessor_override`, `sop_observation_refs_in_org`.
    - Cross-check against ground truth: read `supabase/migrations/00057_restore_sop_observations_cross_org_guard.sql` and assert its policy body contains `sop_observation_refs_in_org` — so the assertion is pinned to what the migration actually creates, not to a string someone typed into the script.

    **WR-01 block:** `src/lib/competency/assessor.ts` contains neither `signOffByCompletion` nor `new Map(`, and contains `signOffs.some(s => s.decision === 'approved')`.

    **WR-03 block:** slice `RecordObservationModal.tsx` from the `getAssessorStatusForSop` occurrence backwards to the nearest preceding `useEffect(` and forwards to the next `}, [sopId])`, and assert that window contains `setAssessorStatus(null)`, `setRequestSent(false)` and `setOverrideOpen(false)`. Scoping to the effect window is the point — asserting those setters exist anywhere in the file would pass on the pre-fix code, where they live in the open-transition block.

    **WR-04 block:** slice `CompletionDetailClient.tsx` from `ASSESSOR_OVERRIDE_REQUIRED` forward 400 characters and assert it contains `setOverrideSheetOpen(true)`. Note in a comment that a file-global search would pass on the broken version (the setter exists in `handleApproveClick`), which is why the window is scoped — this is the 2026-06-05 dead-feature trap in miniature.

    Use `[\s\S]` rather than the `/s` regex flag.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase37 && npx playwright test --project=phase37</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase37` lists `tests/phase37/gap-migration-and-state.spec.ts`
    - `npx playwright test --project=phase37` green, zero `test.fixme` in the new file
    - The WR-03 and WR-04 assertions are slice-scoped, and the SUMMARY states the window boundaries chosen and why each would pass on the pre-fix code if taken file-globally
    - The CR-02 block asserts apply ORDER by index comparison, and cross-checks the asserted conjunct against 00057's own SQL
    - `npm run test` shows no new failures in other projects
  </acceptance_criteria>
  <done>All four fixes in this plan are pinned by assertions that fail if the fix is reverted, including the apply order that makes CR-02 a security bug rather than a cosmetic one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator running the applier → live production DB | The script executes raw SQL via the Management API with a service-role/access token; its output is the only signal the operator gets |
| client-rendered `isAssessor` prop → server verdict | The prop is a render-time snapshot; the server can and does disagree |
| unordered PostgREST result set → safety predicate | Row order is not guaranteed; any order-dependent collapse is a latent verdict flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08-01 | Tampering | applier fallback re-dropping `sop_observation_refs_in_org` on production | mitigate | `MIGRATION_FILES` loop applies 00056 then 00057; apply order asserted by index comparison in the spec |
| T-37-08-02 | Repudiation | applier printing ALL PASS over a reopened write hole | mitigate | Assertion group 3 additionally requires `sop_observation_refs_in_org`, cross-checked against 00057's SQL body |
| T-37-08-03 | Denial of Service (feature-level) | order-dependent Map collapse falsely denying a legitimate assessor | mitigate | `.some()`/`.filter()` over all rows; behavioural unit test proven RED against the old implementation |
| T-37-08-04 | Spoofing (UI) | blocked/override state displayed for the wrong SOP | mitigate | Per-SOP state cleared inside the `[sopId]` effect; slice-scoped assertion so the pre-fix arrangement cannot satisfy it. Server-side re-gating means this was never a write-path hole |
| T-37-08-05 | Denial of Service (feature-level) | override demanded but unreachable after a stale-isAssessor race | mitigate | Server `ASSESSOR_OVERRIDE_REQUIRED` opens the sheet when `canOverride`; error copy still shown |
| T-37-08-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages in this closure pass (37-RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx playwright test --list --project=phase37` discovers `gap-migration-and-state.spec.ts`; `npx playwright test --project=phase37` green
- `npx playwright test --project=phase35-unit -g "assessor"` green with the new WR-01 case, and that case confirmed RED against the pre-fix predicate
- `node --check scripts/apply-phase37-migration.mjs` exits 0 (the script itself is NOT run — it mutates production)
- `npx tsc --noEmit` clean; `npm run build` clean; `npm run test` shows no new failures elsewhere
</verification>

<success_criteria>
- The applier applies both migrations in order on every path, and its assertions cannot pass while the org-ref conjunct is missing
- A completion with an approved and a rejected sign-off row yields assessor = true regardless of row order
- Changing the SOP in the observation modal never shows the previous SOP's blocked/override/request state
- A server-side override demand opens the override sheet for a role that holds the override
- Every fix has a scoped assertion that fails on revert
</success_criteria>

## Artifacts this phase produces

Extends `37-01-PLAN.md` § "Artifacts this phase produces". This plan changes no exported symbol signatures. It adds:

| Artifact | Kind | Change |
|----------|------|--------|
| `scripts/apply-phase37-migration.mjs` | script (modified) | `MIGRATION_FILE` → `MIGRATION_FILES` (00056 + 00057); fallback applies both in order; assertion group 3 pins `sop_observation_refs_in_org` |
| `isSignedOffAssessor` (`src/lib/competency/assessor.ts`) | predicate (modified) | `signOffByCompletion` Map removed; `hasSignOff` / `latestPositiveEvidenceAt` computed order-independently |
| `src/lib/competency/__tests__/assessor.test.ts` | unit spec (extended) | WR-01 case: approved + rejected on one completion → true |
| `RecordObservationModal` (`src/components/observations/RecordObservationModal.tsx`) | component (modified) | per-SOP state reset moved into the `[sopId]` effect |
| `CompletionDetailClient` (`src/app/(protected)/activity/[completionId]/CompletionDetailClient.tsx`) | component (modified) | server `ASSESSOR_OVERRIDE_REQUIRED` opens the override sheet when `canOverride` |
| `tests/phase37/gap-migration-and-state.spec.ts` | spec (new) | slice-scoped regression guards for CR-02, WR-01, WR-03, WR-04 |

No route added, removed or renamed and no user-facing flow changed — `src/lib/journeys/journeys.ts` and `src/lib/uat/tests.ts` need no edit in this plan (WR-03/WR-04 restore states those maps already describe).

**Deliberately NOT closed in this pass** (both Info-level, neither trivially co-located — a decision, not an oversight):
- **IN-01** (multi-recipient request rows stale after one admin acts) needs a new org-scoped server action marking all recipient rows read, plus an `AssessmentRequestsPanel` change — its own small plan, not a co-located one-liner.
- **IN-02** (dead re-validation of `overrideReason` in `completions.ts`) touches no file in either closure plan; the proper fix (loosen the Zod floor so the mapped error code is the single failure surface) ripples into `src/lib/validators/completions.ts` and the sign-off UI copy. Harmless as-is.

<output>
Create `.planning/phases/37-assessor-governance/37-08-SUMMARY.md` when done
</output>
