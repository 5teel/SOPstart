---
phase: 37-assessor-governance
plan: 06
type: execute
wave: 4
depends_on: ["37-04", "37-05"]
files_modified:
  - tests/phase37/bootstrap-override-runtime.spec.ts
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: true
requirements: [ASR-01]

must_haves:
  truths:
    - "A brand-new organisation with zero signed-off assessors is NOT deadlocked: an admin override records the first advancing evidence and the row carries `is_assessor_override = true` plus a reason (success criterion 2, proved at runtime against the live database)"
    - "A plain supervisor in that same zero-assessor org is rejected — the gate is real, not decorative (success criterion 1)"
    - "Once the first person is signed off, they can assess others with no override — the bootstrap resolves and never re-deadlocks (D-05)"
    - "`/pathways` shows 0 not-mapped for every route this phase touches, and the assessor gate + request path appear as pathway steps"
    - "The phase gate is green on the FULL suite, `npx tsc --noEmit` AND `npm run build` — not just the phase project"
  artifacts:
    - path: "tests/phase37/bootstrap-override-runtime.spec.ts"
      provides: "live zero-assessor bootstrap probe: admin override succeeds, supervisor rejected, post-sign-off assessor needs no override"
    - path: "src/lib/journeys/journeys.ts"
      provides: "record-observation journey extended with the gate branch + a request-assessment pathway"
    - path: "src/lib/uat/tests.ts"
      provides: "layman-language UAT items for the blocked state, the override, and the request list"
  key_links:
    - from: "tests/phase37/bootstrap-override-runtime.spec.ts"
      to: "sop_observations.is_assessor_override"
      via: "post-insert read asserting the stamped override columns on a real row"
      pattern: "is_assessor_override"
---

<objective>
Prove success criterion 2 against the live database with a real zero-assessor bootstrap, then close the phase's living-map obligations and run the full gate.

Purpose: every other plan in this phase verifies with source-contract assertions, and CLAUDE.md's 2026-06-08 learning is explicit that a phase verified entirely by greps ships mount-time and runtime failures invisibly. The bootstrap-deadlock scenario in particular cannot be proven by reading source: it is a claim about what the database accepts for an org with no evidence rows at all.
Output: one live runtime probe, updated `journeys.ts` and `uat/tests.ts`, and a full-suite phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-assessor-governance/37-CONTEXT.md
@.planning/phases/37-assessor-governance/37-RESEARCH.md
@.planning/phases/37-assessor-governance/37-03-SUMMARY.md
@.planning/phases/37-assessor-governance/37-04-SUMMARY.md
@.planning/phases/37-assessor-governance/37-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live zero-assessor bootstrap probe (success criteria 1 + 2)</name>
  <files>tests/phase37/bootstrap-override-runtime.spec.ts</files>
  <read_first>
    - tests/phase37/bootstrap-override-runtime.spec.ts (the fixme stub created in 37-01)
    - tests/phase34/observation-cross-org-isolation.spec.ts (the ephemeral-throwaway-org runtime pattern this probe copies: create org + members with the service-role client, exercise, assert, tear down — the precedent established because no staging Supabase project exists, per the Phase 32-05 note in STATE.md)
    - src/lib/competency/assessor.ts (the predicate under test — the probe calls it directly with a service-role client and an explicit orgId, which is exactly its intended call shape)
    - scripts/apply-phase37-migration.mjs (37-03 Task 1 — confirms the columns are live; this probe INSERTS into them, so it fails hard if the push was skipped)
  </read_first>
  <action>
    Convert `tests/phase37/bootstrap-override-runtime.spec.ts` from `test.fixme` to a live runtime probe against the real Supabase project, using the service-role client and an EPHEMERAL throwaway organisation (the Phase 34 cross-org-isolation precedent — this project has no staging instance, and `recordObservation`/`signOffCompletion` cannot be invoked outside a Next.js request scope, so the probe exercises the predicate plus direct table writes rather than the server actions themselves).

    Setup: create one throwaway `organisations` row and three `organisation_members` (an `admin`, a `supervisor`, a `worker`), plus one published `sops` row in that org. Insert NO completions, NO sign-offs and NO observations — this is the day-one state that success criterion 2 is about. Register teardown in `afterAll` (or a `try/finally`) that deletes the org, relying on the existing `on delete cascade` chains, and make teardown run even when an assertion fails.

    Assertions, in this order:
    1. **Zero assessors exist.** `isSignedOffAssessor(adminId, sopId, admin, orgId)` is `false` AND `isSignedOffAssessor(supervisorId, sopId, admin, orgId)` is `false`. This establishes the deadlock precondition rather than assuming it.
    2. **The override bootstraps (SC-2).** Insert a `sop_observations` row as the admin with `verdict: 'performed_to_sop'`, `observed_worker_id: workerId`, `observed_by: adminId`, `is_assessor_override: true`, `override_reason: <a >=10-char reason>`. Assert the insert SUCCEEDS, then read the row back and assert `is_assessor_override === true` and `override_reason` equals what was written — the audit trail must be reconstructible (who / when / which worker / which SOP / which record / reason), so assert `observed_by`, `created_at`, `observed_worker_id`, `sop_id`, `id` and the reason are all present on the single row (D-07).
    3. **The reason is mandatory at the DB level.** Attempt the same insert with `is_assessor_override: true` and `override_reason: null`. Assert it is REJECTED with a CHECK-constraint violation (`23514`), naming `sop_observations_override_reason_required` in the failure message if available.
    4. **A supervisor cannot self-stamp an override (37-01 T-37-01-02).** Assert that the amended `sop_observations_insert_recorder` policy blocks an override insert made under a plain-supervisor session. If constructing a supervisor session in this harness is not feasible, assert the policy's `with_check` text via the Management API instead and record in the SUMMARY which form was used — do NOT silently drop the assertion.
    5. **The bootstrap resolves and never re-deadlocks (D-05).** Insert a `sop_completions` row for the supervisor on that SOP plus an approved `completion_sign_offs` row, then assert `isSignedOffAssessor(supervisorId, sopId, admin, orgId)` is now `true` — the newly signed-off supervisor can assess others without any override.
    6. **The reset suspends capability again.** Insert a `needs_support` observation about the supervisor dated AFTER their sign-off, then assert `isSignedOffAssessor(supervisorId, ...)` is `false` again (D-02 semantics carried into assess capability, live rather than only unit-tested).

    Load credentials from `.env.local` the same way the existing runtime specs do. If `SUPABASE_SERVICE_ROLE_KEY` is absent, `test.skip` with an explicit message naming the missing variable — never pass silently.
  </action>
  <verify>
    <automated>npx playwright test --project=phase37 -g "bootstrap"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase37 -g "bootstrap"` green with zero `fixme` in the file
    - The probe asserts BOTH directions of the gate on a real database: the reasonless override is rejected with `23514`, and the reasoned override succeeds and reads back with both audit fields intact
    - Assertion 5 proves `false → true` for the same person on the same SOP after a sign-off (the deadlock provably resolves)
    - Assertion 6 proves `true → false` after a later `needs_support` (the reset provably suspends assess capability)
    - Teardown deletes the throwaway org even on assertion failure — re-running the spec twice in a row is green both times
    - The spec skips loudly (with the variable name) rather than passing when service-role credentials are absent
  </acceptance_criteria>
  <done>Success criteria 1 and 2 are proven against the live database, including the day-one deadlock, its resolution, and the reset.</done>
</task>

<task type="auto">
  <name>Task 2: journeys.ts gate branch + request pathway, and layman UAT items</name>
  <files>src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts lines 224-256 (the `record-observation` and `worker-sees-observations` journeys — the exact `Journey` step shape: `type: 'start' | 'screen' | 'decision' | 'action' | 'end'`, `route` values that must be real for coverage + deep-links to work, and `branches` on a decision node)
    - src/lib/uat/tests.ts lines 676-725 (the `p34-record-observation` and `p34-worker-sees-observations` entries — the exact field set: `id`, `dateAdded`, `category`, `title`, `status`, `summary`, `tryIt`, `links`, `questions`, `background`)
    - CLAUDE.md § Pathways Map Maintenance (the rule: a changed flow is not done until `journeys.ts` reflects it, and `/pathways` "All screens" must show 0 not-mapped for this phase's routes)
    - the components modified in 37-04 and 37-05 (so the pathway steps describe what actually shipped, not what was planned)
  </read_first>
  <action>
    **journeys.ts** — extend the existing `record-observation` journey rather than adding a competing one: after the `modal` screen step, insert a `decision` node asking whether the recorder is signed off on that SOP themselves, with three branches — signed off (falls through to the existing `save` action), admin/safety_manager override (a new `action` step describing the reason field and the audit stamp), and blocked supervisor (a new `action` step for "Request assessment"). Then add ONE new short journey `request-assessment` in the Supervisor/Admin group covering the recipient side: `/admin/team` → the `Assessment requests` panel → "Assess now" opens the modal preset to that person + SOP → the observation is saved. Every `route` value must be a real route (`/admin/team`, `/activity`) so `/pathways` deep-links and coverage both work. Reference the decisions by id in the `detail` strings (D-03/D-05/D-06/D-08) as the existing entries do.

    No new routes are created by this phase, so no previously-unmapped screen appears — but verify that claim rather than assuming it: after editing, confirm every route touched by 37-04/37-05 (`/activity/[completionId]`, `/admin/team`) is covered by at least one journey.

    **uat/tests.ts** — add three `active` entries under a new `category: 'Phase 37 — Assessor governance'`, written in plain click-path language with yes/no questions and NO jargon, no internal ids, no decision codes in the user-visible fields (keep D-refs in `background` only):
    - `p37-blocked-supervisor` — as a supervisor who has not been signed off on a procedure, try to record that someone else did it correctly; confirm the option is unavailable, that the on-screen explanation makes sense, that the coaching option ("needs support") still works, and that you can ask someone to sign you off.
    - `p37-admin-override` — as an admin on a brand-new organisation with nobody signed off yet, record that a worker performed a procedure correctly; confirm you are asked for a reason, that the screen tells you it goes on the permanent record, and that it saves.
    - `p37-assessment-requests` — as an admin or safety manager, open Team and check the assessment-requests list shows who asked and which procedure, and that "Assess now" opens the form with the right person and procedure already filled in.
    Each entry gets `links` to the real routes and a `background` naming ASR-01 plus the plans that shipped it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase37</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "request-assessment" src/lib/journeys/journeys.ts` >= 1 and the `record-observation` journey contains a decision node whose branches name the override and the blocked/request paths
    - Every `route` value added to `journeys.ts` matches an existing route directory under `src/app/(protected)/` (verify each by path existence, not by eye)
    - `grep -c "p37-" src/lib/uat/tests.ts` == 3 and all three carry `status: 'active'`
    - No UAT `title`, `summary`, `tryIt` or `questions` string contains an internal identifier (`ASR-01`, `D-0`, `is_assessor_override`, `performed_to_sop`, a UUID, or a function name) — layman language only; D-refs live in `background`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The pathways map and the UAT hub describe the shipped gate, override and request flows in the language each audience needs.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate</name>
  <files>tests/phase37/bootstrap-override-runtime.spec.ts</files>
  <read_first>
    - .planning/phases/37-assessor-governance/37-VALIDATION.md (the 9-row Per-Task Verification Map — every row must now be ✅ green with a named spec, and `nyquist_compliant` flipped true)
    - .planning/phases/37-assessor-governance/37-CONTEXT.md (the nine decisions D-01..D-09 — audit each against what shipped and record the evidence)
    - the SUMMARY files from 37-01 through 37-05
  </read_first>
  <action>
    Run the full phase gate and record the result. This is a verification task, not an implementation task — if anything is red, fix forward in the smallest possible diff and note it in the SUMMARY rather than reshaping the phase.

    1. `npm run test` — the FULL suite. Compare failures against the pre-phase baseline; any NEW failure is a regression and must be fixed before the phase closes. Pre-existing failures in unrelated older phase stubs are not regressions (the Phase 23 precedent) but must be listed explicitly in the SUMMARY so the distinction is on the record.
    2. `npx tsc --noEmit` — must exit 0.
    3. `npm run build` — must complete. This gate is NOT optional and NOT implied by tsc: this phase edits three `'use server'` modules (`observations.ts`, `completions.ts`, and the pages importing them), and a non-async export in one of them fails `next build` while passing `tsc` (CLAUDE.md 2026-06-27, the Phase 23-03 Railway incident).
    4. `npx playwright test --list --project=phase37` — all six specs still discovered (a phase whose specs stopped being discovered is a phase with no gates, CLAUDE.md 2026-05-25).
    5. Worker-surface bundle sanity: confirm `/sops/[sopId]` First Load JS is unchanged against the recorded baseline. Everything this phase added is admin/supervisor surface; a delta means an assessor module leaked into the worker graph, which is also what `no-competency-gate-worker.spec.ts` guards from the source side.
    6. Update `.planning/phases/37-assessor-governance/37-VALIDATION.md`: fill the Task ID / Plan / Wave / Threat Ref columns for all nine rows, set every Status to ✅ with the spec that proves it, and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter.
    7. Write a short D-01..D-09 audit into the SUMMARY: one line per decision, the artifact that implements it, and the test that proves it. Explicitly record the two accepted dispositions (the raw-REST residual gap T-37-01-04 and the worker-visible override reason T-37-01-05) so neither is closed silently.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit && npm run build && npx playwright test --list --project=phase37</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` shows ZERO new failures versus the pre-phase baseline; any pre-existing failures are enumerated in the SUMMARY
    - `npx tsc --noEmit` exits 0 and `npm run build` completes
    - `npx playwright test --list --project=phase37` lists all six phase37 specs
    - `/sops/[sopId]` First Load JS is within the recorded tolerance of its baseline
    - `37-VALIDATION.md` has all nine rows ✅ with a named proving spec, and `nyquist_compliant: true`
    - The SUMMARY contains a D-01..D-09 audit table and names both accepted-disposition threats
  </acceptance_criteria>
  <done>The phase is green on the full suite, both build gates, and every decision has a named artifact plus a named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Supabase | The bootstrap probe creates and deletes real rows in the production project |
| `/uat` + `/pathways` public-ish surfaces | Copy must not leak internal identifiers or schema names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06-01 | Tampering | runtime probe leaving orphan rows in the production project | mitigate | Ephemeral throwaway org with `afterAll`/`finally` teardown relying on existing `on delete cascade` chains; re-runnability is an acceptance criterion, so a leak surfaces on the second run |
| T-37-06-02 | Repudiation | phase declared done on a false-positive green | mitigate | The gate is the FULL suite plus `tsc` plus `next build` plus spec-discovery, not the phase project alone — the exact combination the 2026-06-02, 2026-06-27 and 2026-05-25 learnings each caught separately |
| T-37-06-03 | Information Disclosure | internal identifiers in `/uat` copy | mitigate | Acceptance criterion forbids `ASR-01`, `D-0`, column names, verdict literals, UUIDs and function names in user-visible UAT fields; D-refs are confined to `background` |
| T-37-06-04 | Elevation of Privilege | supervisor self-stamped override passing unnoticed | mitigate | Probe assertion 4 exercises the RLS conjunct directly, with a Management-API policy-text fallback that must be recorded rather than dropped |
| T-37-06-05 | Tampering | assessor module leaking into the worker bundle | mitigate | Bundle-baseline check plus the source-side `no-competency-gate-worker.spec.ts` guard — two independent detections of the same regression |
| T-37-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages in this phase (37-RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx playwright test --project=phase37` green, all six specs live, zero fixme
- `npm run test` → no new failures; `npx tsc --noEmit` clean; `npm run build` clean
- `/pathways` covers `/admin/team` and `/activity/[completionId]`; `/uat` shows three new active Phase 37 items
- `37-VALIDATION.md` fully green with `nyquist_compliant: true`
</verification>

<success_criteria>
- The zero-assessor bootstrap is proven live: reasonless override rejected, reasoned override recorded with a reconstructible audit row, deadlock resolves after the first sign-off, reset suspends capability again
- A plain supervisor is provably blocked from both advancing write paths
- `journeys.ts` and `uat/tests.ts` describe the shipped gate, override and request flows
- Full suite, `tsc` and `next build` all green; both accepted security dispositions recorded, not silently closed
</success_criteria>

## Artifacts this phase produces

See `37-01-PLAN.md` § "Artifacts this phase produces". This plan introduces the live `tests/phase37/bootstrap-override-runtime.spec.ts` probe, the `request-assessment` journey, and the three `p37-*` UAT entries.

<output>
Create `.planning/phases/37-assessor-governance/37-06-SUMMARY.md` when done
</output>
