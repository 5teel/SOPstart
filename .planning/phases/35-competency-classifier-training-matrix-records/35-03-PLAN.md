---
phase: 35-competency-classifier-training-matrix-records
plan: 03
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - src/components/admin/competency/TrainingMatrixView.tsx
  - src/components/admin/competency/TrainingRecordSection.tsx
  - src/components/admin/org-model/PersonPanel.tsx
  - src/components/admin/org-model/TeamViewShell.tsx
  - src/lib/competency/download-csv.ts
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
  - tests/phase35/training-matrix-view.spec.ts
  - tests/phase35/matrix-filters.spec.ts
  - tests/phase35/training-record.spec.ts
autonomous: true
requirements: [MTX-01, MTX-03, TRN-01, TRN-02, CMP-01]

must_haves:
  truths:
    - "Admin/supervisor opens a training matrix as a third view mode on /admin/team, defaulting to one department (D-06)"
    - "Matrix shows labelled state pills with per-person and per-SOP rollups, and auto-compacts past the fit threshold (D-07/D-08)"
    - "Filters narrow the matrix by department, worker and SOP (MTX-03)"
    - "Clicking a cell opens the PersonPanel focused on that person, scrolled to that SOP's evidence trail (D-09)"
    - "PersonPanel shows a per-worker training record grouped by SOP, plus an other-completed-SOPs section (TRN-01/D-12/D-13)"
    - "Both the matrix header (current filtered cut, dept/worker/SOP + date range) and the PersonPanel record (one worker) export a CSV: the button's onClick calls exportTrainingCsv() and streams { csv, filename } to a Blob download (D-16/TRN-02)"
    - "/pathways maps the matrix flow — 0 not-mapped for the new view"
  artifacts:
    - path: "src/components/admin/competency/TrainingMatrixView.tsx"
      provides: "matrix table + rollups + MTX-03 filters + cell-click deep-link + D-16 Export CSV (filtered cut)"
      contains: "onSelectCell"
    - path: "src/components/admin/competency/TrainingRecordSection.tsx"
      provides: "per-worker training record grouped by SOP + other-completed bucket + D-16 Export CSV (this worker)"
      contains: "focusSopId"
    - path: "src/lib/competency/download-csv.ts"
      provides: "client Blob-download helper turning { csv, filename } into a file save (forks the FlowGraphCanvas idiom)"
      contains: "createObjectURL"
    - path: "src/components/admin/org-model/TeamViewShell.tsx"
      provides: "third 'matrix' view value + focusSopId wiring to PersonPanel"
      contains: "matrix"
    - path: "src/lib/journeys/journeys.ts"
      provides: "training-matrix journey (route-accurate)"
      contains: "/admin/team"
  key_links:
    - from: "src/components/admin/org-model/TeamViewShell.tsx"
      to: "TrainingMatrixView + PersonPanel"
      via: "matrix view branch; onSelectCell sets selectedPerson + focusSopId passed to PersonPanel"
      pattern: "focusSopId"
    - from: "src/components/admin/competency/TrainingMatrixView.tsx"
      to: "getTrainingMatrix"
      via: "fetch on department load (client)"
      pattern: "getTrainingMatrix"
    - from: "src/components/admin/org-model/PersonPanel.tsx"
      to: "getTrainingRecordForPerson via TrainingRecordSection"
      via: "renders TrainingRecordSection as a third section"
      pattern: "TrainingRecordSection"
    - from: "src/components/admin/competency/TrainingMatrixView.tsx + TrainingRecordSection.tsx"
      to: "exportTrainingCsv + downloadCsv"
      via: "Export CSV button onClick → exportTrainingCsv(filters) → downloadCsv(csv, filename) (D-16 two entry points, one generator)"
      pattern: "exportTrainingCsv"
---

<objective>
Build the primary admin deliverable: the training matrix as a third /admin/team view mode, and the per-worker training record inside the existing PersonPanel — wired so a matrix cell click opens the panel focused on that person and SOP, and so both surfaces can export the SuccessFactors-shaped CSV. This is where the derived competency state becomes the pre-audit scan view auditors/ACC ask for, and the point at which TRN-02's CSV export actually becomes reachable from the UI.

Purpose: Render the classifier's output through the approved sketch-05 design (labelled pills, both-axis rollups, department-first cut, compact fallback) with MTX-03 filters, reusing the PersonPanel as the single evidence renderer (D-09 — no second evidence UI), and expose the D-16 two-entry-point CSV export (filtered cut + single worker) over the one shared generator. Keep /pathways truthful.
Output: TrainingMatrixView, TrainingRecordSection, modified TeamViewShell + PersonPanel, the two D-16 CSV export buttons + shared download-csv helper, journeys/uat updates, and the matrix/filter/record/export source-contract specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-PATTERNS.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrainingMatrixView — department-first table, rollups, filters, cell deep-link</name>
  <files>src/components/admin/competency/TrainingMatrixView.tsx, tests/phase35/training-matrix-view.spec.ts, tests/phase35/matrix-filters.spec.ts</files>
  <read_first>
    - src/components/admin/org-model/OrgColumnsBoard.tsx (the sibling view rendered by TeamViewShell — match its client-fetch + loading/empty-state shape and how it receives tree/departments/onSelectPerson)
    - src/components/admin/competency/StatePill.tsx (created in 35-02 — the pill this table renders per cell)
    - src/actions/competency.ts (getTrainingMatrix signature + returned cells/rowRollups/colRollups shape)
    - src/lib/validators/competency.ts (MatrixFiltersSchema — the dept/worker/SOP filter contract)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-06 department-first default + switcher; D-07 labelled pills auto-compact past threshold + horizontal scroll backup; D-08 both-axis rollups with needs-support surfaced; D-09 cell click; D-10 required SOPs only)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md (Pitfall 5 — progressive compact threshold, don't hardcode a magic column count)
    - src/components/observations/ObservationRow.tsx (blueprint frame/mono idiom for the table chrome)
    - sketches/supervisor-observations/index.html (§05 the approved matrix preview — the design anchor)
  </read_first>
  <action>
    Create src/components/admin/competency/TrainingMatrixView.tsx as a client component rendered by TeamViewShell's matrix branch. Props: departments, an initial/selected department (default the first — D-06 department-first, whole-org is NOT the default), and onSelectCell(personId, sopId). On department change, call getTrainingMatrix({ departmentId, ...filters }) (client fetch, loading skeleton + empty state — Claude's discretion). Render rows = people with at least one required SOP (D-10), columns = that department's required SOPs; each cell = a StatePill for the person-by-SOP CompetencyResult. Add a department switcher (reuse the /admin/team department-filter idiom) and MTX-03 filter widgets for worker and SOP that narrow the visible matrix. Render both-axis rollups (D-08): a per-person column ("4/6 competent") and a per-SOP row ("3/5 signed off"), each surfacing needs-support flags. Implement D-07 progressive compaction: labelled pills at normal department scale; when columns exceed what fits (would need horizontal scroll anyway), switch to compact colored cells plus a legend, horizontal scroll as backup — base the switch on fit, not a hardcoded integer (Pitfall 5). Every cell is clickable and calls onSelectCell(personId, sopId) (D-09). No cell interaction gates anything (CMP-04 — pills are passive). Hold the current filter state (departmentId + optional workerId/sopId) in a way Task 4 can read for the Export CSV button (the export reuses this same cut).
    Create tests/phase35/training-matrix-view.spec.ts (source-contract): TrainingMatrixView imports getTrainingMatrix and StatePill; renders a department switcher; renders per-person and per-SOP rollups; wires onSelectCell; contains no disabled/lock affordance keyed on state (CMP-04). Create tests/phase35/matrix-filters.spec.ts (source-contract): the view passes departmentId/workerId/sopId into the getTrainingMatrix call (MTX-03) and re-fetches/re-filters on change.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35 tests/phase35/training-matrix-view.spec.ts tests/phase35/matrix-filters.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - training-matrix-view.spec.ts green: imports getTrainingMatrix + StatePill, renders a department switcher, renders both-axis rollups, wires onSelectCell
    - matrix-filters.spec.ts green: department/worker/SOP filters feed the getTrainingMatrix call (MTX-03)
    - No `disabled` or lock affordance is keyed on competency state anywhere in the view (source-negative, CMP-04)
    - Compaction is fit-driven, not a hardcoded column integer (source assertion: no literal magic threshold like `> 8`)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The matrix renders live, department-first, with labelled pills + both-axis rollups + MTX-03 filters + fit-driven compaction, and every cell deep-links via onSelectCell.</done>
</task>

<task type="auto">
  <name>Task 2: TrainingRecordSection + PersonPanel growth-point (TRN-01 / D-09 focus)</name>
  <files>src/components/admin/competency/TrainingRecordSection.tsx, src/components/admin/org-model/PersonPanel.tsx, tests/phase35/training-record.spec.ts</files>
  <read_first>
    - src/components/admin/org-model/PersonPanel.tsx (the growth-point comment at lines 127-129 — replace it; the useState reset-on-person-change idiom lines 40-45; existing section structure lines 87-125; PersonPanelProps lines 21-24)
    - src/actions/competency.ts (getTrainingRecordForPerson signature + per-SOP evidence + other-completed bucket shape)
    - src/components/admin/competency/StatePill.tsx (state pill per required-SOP block header)
    - src/components/observations/ObservationRow.tsx (evidence-row rendering idiom; observations render chronologically within a block)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-11 PersonPanel-only no dedicated page; D-12 grouped by SOP with state pill header + evidence trail; D-13 other-completed-SOPs separate section excluded from matrix; D-09 scroll to focusSopId)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md (Code Examples — PersonPanelProps focusSopId extension lines 274-286)
  </read_first>
  <action>
    Create src/components/admin/competency/TrainingRecordSection.tsx (client): props { personId, focusSopId }. Fetch getTrainingRecordForPerson(personId) (useEffect + reset-on-personId-change idiom like PersonPanel). Render one block per REQUIRED SOP (D-12), each headed by a StatePill for that SOP's state, with the evidence trail beneath: completions (version + date + sign-off chain) and observations chronologically — NOT a flat timeline. Below the required set, render a separate "Other completed SOPs" section (D-13) for completions outside the person's grants — still training evidence, visually distinct, and clearly not part of the matrix/rollups. When focusSopId is set, scroll/highlight that SOP's block on open (ref + scrollIntoView). Purely informational — no edit/gate control (CMP-04). (The per-worker Export CSV button is added in Task 4 — leave a stable header region for it.)
    Modify src/components/admin/org-model/PersonPanel.tsx: extend PersonPanelProps with `focusSopId?: string | null` (RESEARCH Code Examples). Replace the Phase-35 growth-point comment (lines 127-129) with a `<TrainingRecordSection personId={person.id} focusSopId={focusSopId} />` element as a THIRD section sibling of the Record CTA + Observation history sections. If focusSopId needs reset-on-person-change, reuse the existing render-time prevPersonId useState idiom (do not add a setState-in-effect — react-hooks/set-state-in-effect, CLAUDE.md 2026-07-14).
    Create tests/phase35/training-record.spec.ts (source-contract): TrainingRecordSection imports getTrainingRecordForPerson + StatePill; groups by SOP (renders per-SOP blocks) and renders an "Other completed" section (D-13); PersonPanel renders TrainingRecordSection and accepts focusSopId; the section has no edit/gate control (CMP-04 negative).
  </action>
  <verify>
    <automated>npx playwright test --project=phase35 tests/phase35/training-record.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - training-record.spec.ts green: TrainingRecordSection imports getTrainingRecordForPerson + StatePill, renders grouped-by-SOP blocks + an "Other completed" section, is informational-only
    - PersonPanel.tsx renders TrainingRecordSection and its props include focusSopId (source assertion)
    - focusSopId reset (if any) uses the render-time prevPersonId idiom, not setState-in-effect (source-negative for a setState inside a useEffect body keyed on the prop)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>PersonPanel grows a per-worker training record grouped by SOP with an other-completed section, focusable by SOP — the single evidence renderer for both the panel and the matrix cell-click.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the matrix into TeamViewShell + update /pathways + /uat</name>
  <files>src/components/admin/org-model/TeamViewShell.tsx, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/components/admin/org-model/TeamViewShell.tsx (the whole 67-line file — extend the view union, VIEW_OPTIONS, and add the third render branch; it already owns selectedPerson state + renders PersonPanel)
    - src/components/admin/org-model/ViewToggle.tsx (pure controlled toggle — NO change needed, keyed by string value)
    - src/components/admin/competency/TrainingMatrixView.tsx (the component this branch renders + its onSelectCell signature)
    - src/lib/journeys/journeys.ts (the Journey/JourneyStep interfaces near line 17-37 + an existing admin Journey to copy the shape; keep route values real so /pathways coverage + deep-links work — CLAUDE.md Pathways rule)
    - src/lib/uat/tests.ts (existing UAT test entry shape; write items as plain click-paths + yes/no, no jargon — MEMORY feedback_uat_layman_language)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-PATTERNS.md (TeamViewShell modification lines 141-160 — the exact union/options/branch edits)
  </read_first>
  <action>
    Modify src/components/admin/org-model/TeamViewShell.tsx: extend the view union to `'chart' | 'columns' | 'matrix'`; add `{ value: 'matrix', label: '▦ Matrix' }` to VIEW_OPTIONS; add a `focusSopId` state; add a third render branch for `view === 'matrix'` rendering TrainingMatrixView with departments and an onSelectCell handler that sets selectedPerson (resolved from personId) and setFocusSopId(sopId); pass `focusSopId={focusSopId}` to the existing PersonPanel and reset focusSopId when the panel closes. ViewToggle itself needs zero changes. Keep the onSelectCell person lookup honest — resolve the person label from the tree/matrix rows so PersonPanel gets a real { id, name, roleLabel }.
    Update src/lib/journeys/journeys.ts: add a Journey for the training matrix + records flow (persona admin/supervisor) — steps: /admin/team screen, toggle to Matrix (▦), cell click, PersonPanel training record (route /admin/team), export CSV action. Also add or extend a step noting the worker /profile "My competency" section (route /profile) so the worker-facing informational surface built in 35-04 is mapped (this journeys edit is descriptive config only — it references routes, not the 35-04 component, so there is no compile dependency). Confirm /pathways "All screens" shows 0 not-mapped for the new view (grep the new route strings are present).
    Update src/lib/uat/tests.ts: add layman UAT items — (1) "Open Team, switch to the Matrix view — do you see each person's training state per SOP?"; (2) "Click a coloured cell — does the person's record open at that SOP?"; (3) "Press Export CSV and open the file — are the columns and the completion-date range right?" (plain language, yes/no, no internal IDs).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase35</automated>
  </verify>
  <acceptance_criteria>
    - TeamViewShell view union includes 'matrix', VIEW_OPTIONS has the ▦ Matrix entry, and the matrix branch renders TrainingMatrixView with an onSelectCell that sets both selectedPerson and focusSopId (source assertion)
    - PersonPanel receives focusSopId from TeamViewShell (source assertion)
    - journeys.ts contains the /admin/team matrix journey and references /profile for the worker competency section (grep the routes) — /pathways shows 0 not-mapped for the new view
    - uat/tests.ts contains the three layman matrix/record/CSV items
    - `npx tsc --noEmit` clean; full phase35 project green
  </acceptance_criteria>
  <done>The matrix is the third /admin/team view mode with cell-click deep-linking into the PersonPanel training record, and /pathways + /uat reflect the new flow (MTX-01).</done>
</task>

<task type="auto">
  <name>Task 4: Wire the two CSV export entry points (D-16 / TRN-02)</name>
  <files>src/lib/competency/download-csv.ts, src/components/admin/competency/TrainingMatrixView.tsx, src/components/admin/competency/TrainingRecordSection.tsx, tests/phase35/training-matrix-view.spec.ts, tests/phase35/training-record.spec.ts</files>
  <read_first>
    - src/components/sop/flow/FlowGraphCanvas.tsx (lines 198-209 — the EXISTING browser download idiom: build a Blob → document.createElement('a') → a.href = URL.createObjectURL(blob) → a.download = filename → a.click() → URL.revokeObjectURL(a.href); reuse this verbatim, do NOT invent a new download mechanism or add a library)
    - src/actions/competency.ts (exportTrainingCsv signature — takes CsvExportFilters, returns `{ csv, filename }` on success or `{ error }`; the client turns the string into the Blob — the action stays a role-gated server action, never a cookie-less route)
    - src/lib/validators/competency.ts (CsvExportFiltersSchema — departmentId/workerId/sopId/dateFrom/dateTo; the matrix export forwards the CURRENT cut, the record export passes `{ workerId: personId }`)
    - src/components/admin/competency/TrainingMatrixView.tsx (Task 1 — the current departmentId/workerId/sopId filter state to forward into the export call)
    - src/components/admin/competency/TrainingRecordSection.tsx (Task 2 — personId is the workerId for the per-worker export; the header region left for the button)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-16 two entry points / one generator; D-14 one row per completion; D-15 email+name; Claude's-discretion date-range = filter on completion date)
    - CLAUDE.md 2026-06-05 + 2026-06-08 (the empty-onClick dead-feature class — the spec MUST assert the handler INVOKES exportTrainingCsv, not merely that the string appears somewhere in the file)
  </read_first>
  <action>
    Create src/lib/competency/download-csv.ts — a tiny CLIENT util (plain module, NO 'use server') exporting `downloadCsv(csv: string, filename: string)`: fork the FlowGraphCanvas idiom exactly — `new Blob([csv], { type: 'text/csv;charset=utf-8;' })`, `document.createElement('a')`, `a.href = URL.createObjectURL(blob)`, `a.download = filename`, `a.click()`, then `URL.revokeObjectURL(a.href)`. One shared helper for both callers so the Blob dance is not inlined twice.
    Modify src/components/admin/competency/TrainingMatrixView.tsx (D-16 entry point 1 — the filtered cut): add an "Export CSV" button in the matrix header. Its onClick handler `await exportTrainingCsv({ departmentId, workerId, sopId, dateFrom, dateTo })` passing the view's CURRENT filter state; on the `{ csv, filename }` success branch call `downloadCsv(csv, filename)`; on an `{ error }` branch surface the error (toast/inline) and do NOT download. Add two native `<input type="date">` controls (dateFrom / dateTo — completion-date range, Claude's discretion) feeding the export filters; native inputs only, no picker library (ladder rung 4). The MTX-03 dept/worker/SOP filters from Task 1 are reused as-is. The button is a passive action — never disabled/gated on competency state (CMP-04).
    Modify src/components/admin/competency/TrainingRecordSection.tsx (D-16 entry point 2 — one worker): add an "Export CSV" button in the record header. Its onClick handler `await exportTrainingCsv({ workerId: personId })`, then `downloadCsv(csv, filename)` on success (same error-branch guard). Same passive, ungated action.
    Extend tests/phase35/training-matrix-view.spec.ts: assert TrainingMatrixView imports exportTrainingCsv (from '@/actions/competency') and downloadCsv (from '@/lib/competency/download-csv'), AND that the Export button's onClick/handler body INVOKES exportTrainingCsv (wiring assertion — grep the handler calls `exportTrainingCsv(` and then `downloadCsv(`, mirroring the 2026-06-05 empty-onClick guard; a bare token mention is NOT sufficient), AND the export control carries no `disabled=` keyed on competency state (CMP-04). Extend tests/phase35/training-record.spec.ts: same wiring assertions for TrainingRecordSection — imports exportTrainingCsv + downloadCsv, its export handler INVOKES `exportTrainingCsv(` with a workerId and calls `downloadCsv(`, and no gating affordance.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35 tests/phase35/training-matrix-view.spec.ts tests/phase35/training-record.spec.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/competency/download-csv.ts exists (client util, no 'use server') and builds a `text/csv` Blob → anchor `download` click → `revokeObjectURL` (source assertion)
    - TrainingMatrixView has an Export CSV button whose handler INVOKES exportTrainingCsv (with the current dept/worker/SOP + date-range filters) then downloadCsv — spec asserts the invocation, not just the token (CLAUDE.md 2026-06-05)
    - TrainingRecordSection has an Export CSV button whose handler INVOKES exportTrainingCsv({ workerId: personId }) then downloadCsv — spec asserts the invocation
    - Neither export control is disabled/gated on competency state (CMP-04 source-negative)
    - `npx tsc --noEmit` clean; training-matrix-view + training-record specs green
  </acceptance_criteria>
  <done>Both D-16 export entry points (matrix filtered-cut header button + PersonPanel/TrainingRecordSection per-worker button) call the shared exportTrainingCsv action and stream { csv, filename } to a Blob download — TRN-02 is reachable from the UI, not a dead action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix cell click → server action | onSelectCell forwards personId/sopId; the opened PersonPanel/record re-fetches via getTrainingRecordForPerson which re-applies role + org scope server-side — the client never becomes the trust anchor. |
| Export CSV button → server action | The button forwards only filter ids/dates to exportTrainingCsv; the 35-02 action re-applies the RECORDER_ROLES gate + org-self-enforce; the client turns the returned string into a Blob (no new endpoint). |
| rendered competency state → worker control flow | Competency pills are rendered admin-side; they must never become a gate on any worker surface (CMP-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | Information Disclosure | TrainingMatrixView / TrainingRecordSection client fetch | mitigate | All data comes from the 35-02 role-gated, org-self-enforced actions; the client passes only ids, never widens scope; a spoofed personId/deptId is re-validated server-side (department-org + membership checks in getTrainingMatrix/getTrainingRecordForPerson). |
| T-35-03-02 | Elevation of Privilege | CMP-04 — a state pill becomes a worker gate | mitigate | Matrix/record are admin-route-only; no disabled/lock keyed on state (source-negative in this plan's specs, incl. the Task 4 export buttons); the 35-01 no-competency-gate guard covers worker surfaces. |
| T-35-03-03 | Repudiation | /pathways drift (new view unmapped) | mitigate | journeys.ts updated in the same change; acceptance asserts the routes are present and 0 not-mapped (CLAUDE.md Pathways rule). |
| T-35-03-04 | Information Disclosure | CSV export button forwards filters | mitigate | The Export buttons call the 35-02 role-gated, org-self-enforced exportTrainingCsv action; the client passes only filter ids/dates, never widens scope; the download is a passive client-side Blob save over the returned string — no new cookie-less /api route (RESEARCH Security threat 3). |
| T-35-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH: no new dependencies; the CSV download reuses the native Blob/anchor idiom already in FlowGraphCanvas). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test --project=phase35` — training-matrix-view, matrix-filters, training-record source-contracts green (plus all prior phase35 specs still green)
- Both Export CSV buttons (matrix filtered-cut + per-worker) invoke exportTrainingCsv → downloadCsv — asserted by the extended training-matrix-view / training-record specs (wiring, not string presence, CLAUDE.md 2026-06-05)
- `npx tsc --noEmit` clean
- Manual (sopstart.com UAT): Matrix renders correctly, cell-click opens the record at the right SOP, rollups + compaction behave (CSS-token/visual, invisible to automated gates per CLAUDE.md 2026-07-14); Export CSV downloads a file that opens in Excel with the expected columns + honours the completion-date range
- `/pathways` → All screens: 0 not-mapped for the new matrix view
</verification>

<success_criteria>
- Training matrix is a third /admin/team view mode, department-first, with labelled pills + both-axis rollups + MTX-03 filters + fit-driven compaction (MTX-01/MTX-03/D-06/D-07/D-08)
- Cell click opens the PersonPanel training record focused on that person + SOP (D-09), grouped by SOP with an other-completed section (TRN-01/D-12/D-13)
- Both D-16 export entry points (matrix filtered cut + per-worker record) stream exportTrainingCsv output to a Blob download — TRN-02 is reachable, not a dead action (CLAUDE.md 2026-06-05 no-dead-feature)
- Competency state renders as passive pills only — no worker gate (CMP-04)
- /pathways + /uat reflect the new flow
</success_criteria>

<artifacts_this_phase_produces>
Symbols/paths CREATED or MODIFIED by this plan:
- `src/components/admin/competency/TrainingMatrixView.tsx` — exports: `TrainingMatrixView` component (props include `onSelectCell`; header gains a D-16 Export CSV button)
- `src/components/admin/competency/TrainingRecordSection.tsx` — exports: `TrainingRecordSection` component (props `personId`, `focusSopId`; header gains a D-16 per-worker Export CSV button)
- `src/lib/competency/download-csv.ts` — exports: `downloadCsv(csv, filename)` client Blob-download helper
- `src/components/admin/org-model/PersonPanel.tsx` — MODIFIED: `PersonPanelProps` gains `focusSopId?`; renders `TrainingRecordSection`
- `src/components/admin/org-model/TeamViewShell.tsx` — MODIFIED: view union gains `'matrix'`; new `focusSopId` state
- `src/lib/journeys/journeys.ts` — MODIFIED: new training-matrix Journey
- `src/lib/uat/tests.ts` — MODIFIED: three new UAT items
- `tests/phase35/training-matrix-view.spec.ts`, `matrix-filters.spec.ts`, `training-record.spec.ts` (matrix + record specs extended in Task 4 with the export-wiring assertions)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-competency-classifier-training-matrix-records/35-03-SUMMARY.md` when done
</output>
