---
phase: 35-competency-classifier-training-matrix-records
plan: 04
type: execute
wave: 3
depends_on: [35-02]
files_modified:
  - src/components/profile/CompetencySection.tsx
  - src/app/(protected)/profile/page.tsx
  - tests/phase35/profile-competency.spec.ts
autonomous: true
requirements: [CMP-01, CMP-04]

must_haves:
  truths:
    - "A worker sees their own competency state per required SOP on /profile — read-only, informational, never gating (D-04)"
    - "The section uses the same trust framing as the neighbouring observations section (yours to see, nothing hidden)"
    - "The CMP-04 guard now actively asserts CompetencySection has no competency-state gate"
  artifacts:
    - path: "src/components/profile/CompetencySection.tsx"
      provides: "worker's own competency states, self-scoped, informational"
      contains: "getMyCompetencyStates"
    - path: "src/app/(protected)/profile/page.tsx"
      provides: "CompetencySection mounted next to ObservationsSection"
      contains: "CompetencySection"
  key_links:
    - from: "src/components/profile/CompetencySection.tsx"
      to: "getMyCompetencyStates"
      via: "self-scoped server action (auth.uid()), read-only render"
      pattern: "getMyCompetencyStates"
    - from: "src/app/(protected)/profile/page.tsx"
      to: "CompetencySection"
      via: "rendered below ObservationsSection"
      pattern: "CompetencySection"
---

<objective>
Add the worker-facing "My competency" section to /profile — the worker's own derived states per required SOP, mirroring the Phase 34 observations transparency principle. Read-only, informational, and covered by the locked CMP-04 guard (no lock icons, no blocked CTAs, no "you can't do this yet" copy anywhere worker-facing).

Purpose: Extend the transparency principle (D-04) — a worker can see where they stand without it ever gating access. This activates the CMP-04 guard's CompetencySection branch created in 35-01.
Output: CompetencySection RSC, its mount on the profile page, and the informational-only source-contract spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompetencySection RSC + mount on /profile</name>
  <files>src/components/profile/CompetencySection.tsx, src/app/(protected)/profile/page.tsx</files>
  <read_first>
    - src/components/profile/ObservationsSection.tsx (the full analog — async Server Component, blueprint-frame card, trust-framing caption, read-only map over rows; copy this shape wholesale and swap the action + copy)
    - src/app/(protected)/profile/page.tsx (mount point — ObservationsSection currently renders below OrgSwitcher; add CompetencySection as a sibling)
    - src/actions/competency.ts (getMyCompetencyStates — self-scoped, returns the caller's own per-required-SOP states)
    - src/components/admin/competency/StatePill.tsx (the same pill the matrix uses — reuse it here per required SOP)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-04 workers see their own states, read-only informational never gating; keep it lean, observations section is the neighbour; Specifics — needs-support flag reads as coaching not discipline)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-PATTERNS.md (CompetencySection analog lines 184-212)
  </read_first>
  <action>
    Create src/components/profile/CompetencySection.tsx as an async Server Component modelled on ObservationsSection: await getMyCompetencyStates(), render a blueprint-frame card headed "Your training" (or similar), with the same trust-framing caption style as ObservationsSection ("These show where you're up to on the SOPs you're trained on — they're yours to see, and they never lock anything"). For each required SOP, render its title + a StatePill for the state (reuse the shared component). Purely informational: NO lock icon, NO disabled control, NO "you can't do this yet" / "blocked until signed off" copy — nothing that reads as a gate (CMP-04 + D-04). Handle the empty state ("No required SOPs yet"). Do NOT introduce any if/ternary on competency state that changes what the worker can DO (only what is displayed).
    Modify src/app/(protected)/profile/page.tsx: import and render CompetencySection as a sibling below the existing ObservationsSection (keep the same section spacing/idiom).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase35 tests/phase35/no-competency-gate.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - CompetencySection.tsx awaits getMyCompetencyStates() and renders a StatePill per required SOP (source assertion)
    - profile/page.tsx renders CompetencySection below ObservationsSection (source assertion)
    - CompetencySection contains no lock/disabled/gating token and no competency-state conditional that alters worker actions (the 35-01 no-competency-gate guard now asserts this file — its fs.existsSync branch activates)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>A worker sees their own competency states on /profile, informational and read-only, and the CMP-04 guard now actively covers CompetencySection.</done>
</task>

<task type="auto">
  <name>Task 2: profile-competency source-contract (informational + self-scoped)</name>
  <files>tests/phase35/profile-competency.spec.ts</files>
  <read_first>
    - src/components/profile/CompetencySection.tsx (the file under contract)
    - tests/phase35/no-competency-gate.spec.ts (created in 35-01 — this is the gate guard; profile-competency.spec.ts is the positive-behaviour complement, not a duplicate)
    - tests/phase35/state-pill.spec.ts (the source-contract assertion style to mirror)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-04 read-only informational; trust framing)
  </read_first>
  <action>
    Create tests/phase35/profile-competency.spec.ts (source-contract, fs.readFileSync). Assert: CompetencySection.tsx imports getMyCompetencyStates from '@/actions/competency' (the SELF-scoped action, never getTrainingMatrix/getTrainingRecordForPerson — a worker must not call the admin reads); imports and renders StatePill; contains a trust-framing caption (grep for the "yours to see" style phrase); contains no lock/disabled/gating affordance and no worker-action conditional on competency state (CMP-04 negative, complementary to the gate guard); and that profile/page.tsx renders CompetencySection. Registered under the phase35 project.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35 tests/phase35/profile-competency.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - profile-competency.spec.ts green: CompetencySection imports getMyCompetencyStates (self-scoped) and NOT getTrainingMatrix/getTrainingRecordForPerson
    - The spec asserts StatePill is rendered and a trust-framing caption is present
    - The spec asserts no lock/disabled/gating token in CompetencySection (CMP-04)
    - profile/page.tsx renders CompetencySection (source assertion)
  </acceptance_criteria>
  <done>The worker competency surface is pinned as self-scoped, informational, and gate-free by an automated source-contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → getMyCompetencyStates | A worker reads only their OWN states — the action self-scopes to auth.uid() (35-02); this component must never call an admin read. |
| competency state → worker control flow | Displayed state must never gate any worker action (CMP-04 locked north star). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Information Disclosure | CompetencySection reads peers' data | mitigate | Uses getMyCompetencyStates (self-scoped, session client, auth.uid()) only; source-contract asserts it never imports getTrainingMatrix/getTrainingRecordForPerson. |
| T-35-04-02 | Elevation of Privilege | competency state gates a worker action | mitigate | No lock/disabled/gating affordance; covered by BOTH the 35-01 no-competency-gate guard (now active on this file) and this plan's positive source-contract. |
| T-35-04-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH: no new dependencies). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test --project=phase35` — no-competency-gate (now asserting CompetencySection) + profile-competency green
- `npx tsc --noEmit` clean
- Manual (sopstart.com UAT): a worker sees "Your training" states on /profile with no lock icons or blocked CTAs
</verification>

<success_criteria>
- Worker sees their own competency states per required SOP on /profile, read-only and informational (CMP-01/D-04)
- The section never gates worker access — enforced by the now-active CMP-04 guard + a positive source-contract (CMP-04)
- The worker read is self-scoped (getMyCompetencyStates), never an admin read
</success_criteria>

<artifacts_this_phase_produces>
Symbols/paths CREATED or MODIFIED by this plan:
- `src/components/profile/CompetencySection.tsx` — exports: `CompetencySection` async Server Component
- `src/app/(protected)/profile/page.tsx` — MODIFIED: renders `CompetencySection`
- `tests/phase35/profile-competency.spec.ts` — informational/self-scoped source-contract
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-competency-classifier-training-matrix-records/35-04-SUMMARY.md` when done
</output>
