---
phase: 35-competency-classifier-training-matrix-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/competency/classify.ts
  - src/lib/competency/matrix.ts
  - src/lib/competency/csv.ts
  - src/lib/competency/__tests__/classify.test.ts
  - src/lib/competency/__tests__/matrix.test.ts
  - src/lib/competency/__tests__/csv.test.ts
  - tests/phase35/matrix-derivation.spec.ts
  - tests/phase35/no-competency-gate.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [CMP-01, CMP-02, MTX-02, TRN-02, CMP-04]

must_haves:
  truths:
    - "A pure classifyCompetency() returns the correct state for every evidence combination (D-01 ladder, D-02 needs_support reset)"
    - "buildMatrix() assembles person×SOP cells + both-axis rollups from pre-fetched arrays with zero DB access"
    - "generateTrainingCsv() emits one row per completion with RFC-4180-safe quoting"
    - "The CMP-04 guard fails the phase if any worker-facing file gains a competency-state conditional"
  artifacts:
    - path: "src/lib/competency/classify.ts"
      provides: "CompetencyState union + classifyCompetency() pure function"
      contains: "export function classifyCompetency"
    - path: "src/lib/competency/matrix.ts"
      provides: "buildMatrix() pure assembler + cell/rollup types"
      contains: "export function buildMatrix"
    - path: "src/lib/competency/csv.ts"
      provides: "generateTrainingCsv() pure generator + RFC-4180 csvField helper"
      contains: "export function generateTrainingCsv"
    - path: "tests/phase35/no-competency-gate.spec.ts"
      provides: "CMP-04 regression guard (forked D28-07 GATE_PATTERN)"
      contains: "GATE_PATTERN"
    - path: "playwright.config.ts"
      provides: "phase35 + phase35-unit project registration"
      contains: "phase35-unit"
  key_links:
    - from: "src/lib/competency/matrix.ts"
      to: "src/lib/competency/classify.ts"
      via: "import classifyCompetency, mapped over every (person, sop) pair"
      pattern: "classifyCompetency"
    - from: "src/lib/competency/matrix.ts"
      to: "(nothing in src/actions/grants.ts or access_grants)"
      via: "MUST NOT import — reads materialized junctions only (MTX-02)"
      pattern: "access_grants"
---

<objective>
Build the three pure competency derivation modules (classifier, matrix assembler, CSV generator) and the Phase 35 test harness — including the locked CMP-04 north-star guard. This is the Wave-0 foundation: everything downstream (server actions, matrix UI, worker profile, CSV export) imports these DB-free functions and calls the one classifier, never recomputing the ladder inline.

Purpose: Establish the single source of truth for competency state (mirrors the resolveEffectiveAccess / classifyGovernanceRow precedent — one resolver, every view calls it) and register the automated verification net before any surface is built.
Output: src/lib/competency/{classify,matrix,csv}.ts, their behavioral unit tests, the MTX-02 no-double-derivation source-contract, the CMP-04 guard, and two new Playwright projects (phase35, phase35-unit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md
@.planning/phases/35-competency-classifier-training-matrix-records/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure classifier classifyCompetency() + behavioral unit tests + register phase35 projects</name>
  <files>src/lib/competency/classify.ts, src/lib/competency/__tests__/classify.test.ts, playwright.config.ts</files>
  <read_first>
    - src/lib/governance/classify.ts (the exact analog — sync export, no 'use server', no supabase import, injectable now?: Date; copy this discipline verbatim)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md (Pattern 1, lines 119-165 — the target classifyCompetency() drafted against this repo)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-01 ladder, D-02 needs_support reset, D-05 keep version stamps in output shape)
    - playwright.config.ts (lines 405-433 — the phase33/phase34 project block format to copy; and any *-unit project e.g. phase32-unit for the static-@/-import testDir pattern)
    - src/lib/voice/__tests__/intent-classifier.test.ts (precedent: static @/ imports resolve inside a testDir-scoped *-unit project — CLAUDE.md 2026-06-24)
  </read_first>
  <action>
    Create src/lib/competency/classify.ts as a pure, sync, DB-free module (NO 'use server', NO supabase import — a sync export in a 'use server' file breaks next build, CLAUDE.md 2026-06-27). Export the CompetencyState union with EXACTLY four members per CMP-01 wording: 'not_started' | 'read' | 'supervised' | 'competent_signed_off'. Export interface CompetencyEvidence { hasCompletion, hasPerformedToSopObservation, hasSignOff, latestNeedsSupportAt: string | null, latestPositiveEvidenceAt: string | null }. Export interface CompetencyResult { state: CompetencyState; needsSupportFlag: boolean; awaitingSignOff: boolean }. Implement classifyCompetency(ev): apply the D-01 highest-evidence-wins ladder with NO prerequisite ordering — hasCompletion→'read', hasPerformedToSopObservation→'supervised', hasSignOff→'competent_signed_off' (a sign-off alone still yields competent_signed_off; existing signed-off workers are never demoted on day one). Then apply D-02: if latestNeedsSupportAt is set AND state !== 'not_started' AND (latestPositiveEvidenceAt is null OR latestNeedsSupportAt > latestPositiveEvidenceAt), reset state to 'read' and set needsSupportFlag = true (never demote below 'read' — the completion happened). Compute awaitingSignOff = final state === 'read' && ev.hasCompletion && !needsSupportFlag (Open Question 1 — a presentation-only derived boolean surfacing the sketch-05 "Awaiting sign-off" pill without adding a 5th canonical state). Keep the enum at four members.
    Create src/lib/competency/__tests__/classify.test.ts as REAL behavioral Playwright tests using a STATIC import from '@/lib/competency/classify'. Cover: no evidence → not_started; completion only → read + awaitingSignOff true; performed_to_sop only → supervised; sign-off only → competent_signed_off (no observation prerequisite, D-01); sign-off + newer needs_support → read + needsSupportFlag true (D-02); needs_support OLDER than latest positive → stays competent_signed_off, no flag; not_started + needs_support → stays not_started, no flag (never demotes below read).
    In playwright.config.ts add TWO project blocks (copy the phase34 block format, comments included): (1) name 'phase35', testDir '.', testMatch /tests\/phase35\/.*\.(spec|test)\.ts$/ for source-contract specs; (2) name 'phase35-unit', testDir 'src/lib/competency/__tests__', testMatch /.*\.test\.ts$/ for static-@/-import behavioral tests. Include the CLAUDE.md 2026-05-25 registration-discipline comment on both.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase35-unit | grep classify.test && npx playwright test --project=phase35-unit tests=classify</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase35-unit` lists src/lib/competency/__tests__/classify.test.ts (empty list = registration FAIL)
    - `npx playwright test --list --project=phase35` runs without "no tests found" registration error
    - classify.test.ts passes green for all seven ladder/reset cases
    - src/lib/competency/classify.ts contains no `'use server'` directive and no `from '@/lib/supabase` import (grep -c returns 0 after filtering comment lines)
    - CompetencyState has exactly four string members (source assertion: no 5th union member added)
  </acceptance_criteria>
  <done>classifyCompetency() is a registered, green, DB-free pure function implementing the D-01 ladder + D-02 reset with a four-member state enum, and both phase35 projects are discoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure matrix assembler + CSV generator + unit tests + MTX-02 no-double-derivation source-contract</name>
  <files>src/lib/competency/matrix.ts, src/lib/competency/csv.ts, src/lib/competency/__tests__/matrix.test.ts, src/lib/competency/__tests__/csv.test.ts, tests/phase35/matrix-derivation.spec.ts</files>
  <read_first>
    - src/lib/org-model/resolve-sop-access.ts (the pure-assembler analog — pre-fetched arrays + maps in, computed sets out, zero I/O)
    - src/lib/competency/classify.ts (just created — matrix.ts imports classifyCompetency and its types)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md (Pattern 2 lines 167-195 for the fetched-array shapes; Don't Hand-Roll table lines 207-213 for the RFC-4180 csvField helper; D-14/D-15/A1/A2 for CSV columns)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (D-08 both-axis rollups, D-10 required-SOPs-only, D-13 other-completed-SOPs, D-14 one-row-per-completion, D-15 email+name)
  </read_first>
  <action>
    Create src/lib/competency/matrix.ts — pure, sync, no 'use server', no supabase import, and CRITICALLY no import from '@/actions/grants' or any access_grants reference (MTX-02: requirements come from the already-materialized sop_departments/sop_access_people output only, never a second derivation of the inheritance chain). Export buildMatrix(input) where input carries pre-fetched arrays: people (id + displayName), requiredSopsByPerson (Map/record personId→sopId[]), sops (id + title + sop_number), completions (worker_id, sop_id, sop_version, submitted_at, id), signOffs (completion_id, decision, created_at), observations (observed_worker_id, sop_id, verdict, created_at). For each (person, requiredSopId) pair assemble a CompetencyEvidence object from the three evidence arrays (hasCompletion from completions; hasSignOff from signOffs joined via completion ids where decision='approved'; hasPerformedToSopObservation / latestNeedsSupportAt / latestPositiveEvidenceAt from observations by verdict) and call classifyCompetency(). Return { cells, rowRollups, colRollups } where each cell also carries latestCompletionAt + latestCompletionVersion (Phase 36 consumer note — expose per-pair latest completion so version-currency math builds on this without refactoring, D-05). rowRollups = per-person counts (e.g. competentCount/total + needsSupportCount, D-08); colRollups = per-SOP counts (signedOffCount/total + needsSupportCount, D-08). Required set is required SOPs only (D-10); do not add extra-curricular completions to cells/rollups.
    Create src/lib/competency/csv.ts — pure, sync. Export an internal csvField(val) RFC-4180 quote helper (wrap in double-quotes and double any embedded quote when the value contains comma, quote or newline — SOP titles like "Lockout, Tagout" require this). Export generateTrainingCsv(rows) → string, one row per completion event (D-14, SuccessFactors Learning-History-shaped LEARNING EVENTS not state snapshots). Columns (defensible GENERIC shape — mark in a header comment that exact SF column names/order are unverified per RESEARCH A2, treat as generic training-events CSV): worker_email, worker_name, sop_identifier, sop_title, sop_version, completion_date, signoff_status, signoff_by, signoff_date. worker_name falls back to worker_email because no full-name field exists anywhere in this codebase (RESEARCH A1 — do NOT invent a full_name source). Emit a header row then one quoted row per completion.
    Create __tests__/matrix.test.ts (static @/ import): hand-build small arrays and assert cell states, both-axis rollup counts, that an extra-curricular completion (SOP not in requiredSopsByPerson) never appears in cells/rollups (D-10), and that cells expose latestCompletionAt/version. Create __tests__/csv.test.ts (static @/ import): assert header + one-row-per-completion, and that a title containing a comma is correctly double-quoted (RFC-4180), and worker_name === worker_email when no name supplied (A1).
    Create tests/phase35/matrix-derivation.spec.ts (source-contract, fs.readFileSync): assert src/lib/competency/matrix.ts does NOT contain 'access_grants' and does NOT import from '@/actions/grants' (MTX-02 zero-double-derivation, RESEARCH anti-pattern), and that it imports classifyCompetency from '@/lib/competency/classify'.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35-unit && npx playwright test --project=phase35 tests/phase35/matrix-derivation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - matrix.test.ts + csv.test.ts pass green under phase35-unit
    - matrix-derivation.spec.ts passes: matrix.ts contains no 'access_grants' substring and no `@/actions/grants` import (MTX-02 mechanically enforced)
    - matrix.ts and csv.ts contain no `'use server'` and no `@/lib/supabase` import (source assertion)
    - csv.test.ts proves a comma-bearing SOP title is emitted wrapped in double quotes (RFC-4180)
    - Each matrix cell object exposes latestCompletionAt and latestCompletionVersion fields (Phase 36 forward-compat)
  </acceptance_criteria>
  <done>buildMatrix() and generateTrainingCsv() are green DB-free pure functions; MTX-02's no-second-derivation rule is guarded by an automated source-contract; CSV quoting is RFC-4180-safe.</done>
</task>

<task type="auto">
  <name>Task 3: CMP-04 north-star guard (fork of the D28-07 GATE_PATTERN test)</name>
  <files>tests/phase35/no-competency-gate.spec.ts</files>
  <read_first>
    - tests/phase28/library-and-worker.spec.ts (the exact analog — fork the GATE_PATTERN + fs.readFileSync structure verbatim; lines 30-47 and the ReadTab/worker-route describe blocks)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-RESEARCH.md (Code Examples lines 251-272 — the exact competency GATE_PATTERN regex and the three target files)
    - .planning/phases/35-competency-classifier-training-matrix-records/35-CONTEXT.md (CMP-04 locked north star, D-04 worker-facing surface is informational-only, no lock icons / blocked CTAs / "you can't do this yet" copy)
    - src/components/sop/tabs/ReadTab.tsx (a target file — confirm current path)
    - src/app/(protected)/sops/[sopId]/page.tsx (a target file — the worker SOP detail/walkthrough route)
  </read_first>
  <action>
    Create tests/phase35/no-competency-gate.spec.ts by forking tests/phase28/library-and-worker.spec.ts. Set GATE_PATTERN = /competency_state\s*[<>=!]|competencyState\s*[<>=!]|if\s*\([^)]*(competency_state|competencyState)/ (matches any comparison/if-branch inspecting competency state in a worker-facing file — a bare mention in a documentation comment is fine, matching the analog's tolerance). Target THREE files: ReadTab.tsx, the worker SOP detail/walkthrough route page.tsx, and the (not-yet-created) worker profile competency component at src/components/profile/CompetencySection.tsx. Because CompetencySection.tsx lands in plan 35-04, guard each read with fs.existsSync — for a file that exists, assert `.not.toMatch(GATE_PATTERN)`; for a not-yet-present file, skip via test.skip so Wave 1 stays green and the assertion self-activates the moment 35-04 creates the file (mirrors the phase22 fs.existsSync green-when-absent precedent, CLAUDE.md 2026-06-24). This spec is registered under the phase35 project created in Task 1.
    Do NOT weaken the guard to only-existing-files permanently — the CompetencySection branch MUST become a real assertion once that file exists (verify in 35-04). Add a header comment stating CMP-04 is a locked north star and this guard is the regression mechanism.
  </action>
  <verify>
    <automated>npx playwright test --project=phase35 tests/phase35/no-competency-gate.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase35` lists no-competency-gate.spec.ts
    - The spec passes green in Wave 1 (ReadTab + worker route asserted clean; CompetencySection branch skipped via fs.existsSync until 35-04)
    - GATE_PATTERN matches a competencyState comparison/if-branch (unit-of-the-regex: a self-check that `/competencyState\s*[<>=!]/` matches the string "if (competencyState === 'read')" — include one inline assertion proving the regex is live, not inert, mirroring the analog)
    - The spec reads exactly three target paths: ReadTab.tsx, worker SOP detail route, CompetencySection.tsx
  </acceptance_criteria>
  <done>The CMP-04 locked guard is registered and green, asserting no competency-state conditional exists in worker read/walkthrough surfaces, and self-activates on the worker profile section when 35-04 ships it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure functions) | classify/matrix/csv perform zero I/O; callers own all data-fetching and scoping. No untrusted input crosses a boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Information Disclosure | matrix.ts / classify.ts | accept | Pure functions cannot leak on their own — they classify whatever the caller passes. The org/role scoping that PREVENTS cross-worker/cross-org leakage lives in 35-02's server actions (T-35-02-*). Rationale: no data source is reachable from these modules. |
| T-35-01-02 | Tampering | MTX-02 double-derivation | mitigate | matrix-derivation.spec.ts fails the phase if matrix.ts ever imports access_grants / @/actions/grants (a second derivation layer would drift from the materialized truth). |
| T-35-01-03 | Elevation of Privilege | CMP-04 worker gate | mitigate | no-competency-gate.spec.ts fails the phase if competency state ever becomes a worker-facing control-flow branch (locked north star). |
| T-35-01-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (RESEARCH: no new dependencies). Nothing to audit; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test --project=phase35-unit` — all classify/matrix/csv behavioral tests green
- `npx playwright test --project=phase35` — matrix-derivation + no-competency-gate source-contracts green
- `npx playwright test --list --project=phase35-unit` and `--project=phase35` both list their specs (registration proof, CLAUDE.md 2026-05-25)
- `npx tsc --noEmit` clean for the three new lib modules
</verification>

<success_criteria>
- classifyCompetency() implements the D-01 ladder + D-02 reset with a four-member state enum (CMP-01/02)
- buildMatrix() and generateTrainingCsv() are DB-free pure functions with green behavioral tests (MTX-02 assembly, TRN-02 quoting)
- MTX-02 no-double-derivation and CMP-04 no-worker-gate are both mechanically guarded and green
- Both phase35 and phase35-unit Playwright projects are registered and discoverable
</success_criteria>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift verification — they are new):
- `src/lib/competency/classify.ts` — exports: `CompetencyState` (type), `CompetencyEvidence` (interface), `CompetencyResult` (interface), `classifyCompetency()` (function)
- `src/lib/competency/matrix.ts` — exports: `buildMatrix()` (function) + matrix cell/rollup input+output types (e.g. `MatrixCell`, `BuildMatrixInput`, `TrainingMatrix`)
- `src/lib/competency/csv.ts` — exports: `generateTrainingCsv()` (function), internal `csvField()` helper, `TrainingCsvRow` type
- `src/lib/competency/__tests__/classify.test.ts`, `matrix.test.ts`, `csv.test.ts` — behavioral unit specs (phase35-unit)
- `tests/phase35/matrix-derivation.spec.ts` — MTX-02 source-contract
- `tests/phase35/no-competency-gate.spec.ts` — CMP-04 guard (`GATE_PATTERN`)
- `playwright.config.ts` — new projects: `phase35`, `phase35-unit`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/35-competency-classifier-training-matrix-records/35-01-SUMMARY.md` when done
</output>
