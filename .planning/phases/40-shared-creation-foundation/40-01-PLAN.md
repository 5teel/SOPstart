---
phase: 40-shared-creation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase40/spine-freeze.spec.ts
  - tests/phase40/dup01-file-intake.spec.ts
  - tests/phase40/dup02-metadata-picker.spec.ts
  - tests/phase40/dup03-job-progress.spec.ts
  - tests/phase40/dup04-page-shell.spec.ts
  - tests/phase40/dat01-category-column.spec.ts
  - tests/phase40/dat01-migration.spec.ts
autonomous: true
requirements: [DUP-01, DUP-02, DUP-03, DUP-04, DAT-01]

must_haves:
  truths:
    - "Every spec dropped into tests/phase40/ is discovered and run by `npx playwright test --project=phase40` with no further config edit"
    - "The frozen publish spine (assertPublishGates) is guarded by a live spec from wave 1 onward, so later plans editing publish-core.ts cannot silently break it"
    - "Each of the five phase requirements has a named spec file waiting for it"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase40 project registration with a deliberately broad testMatch"
      contains: "phase40"
    - path: "tests/phase40/spine-freeze.spec.ts"
      provides: "live guard on the frozen parse->AI-review->verify->publish spine"
      min_lines: 25
    - path: "tests/phase40/dat01-migration.spec.ts"
      provides: "clause-pinning migration assertions (fixme until 40-04/40-06)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase40/*.spec.ts"
      via: "project testMatch regex"
      pattern: "tests\\\\\\\\phase40"
---

<objective>
Register the `phase40` Playwright project and create the Nyquist spec harness for the whole phase, so that no later plan needs a config edit and no guard silently never runs.

Purpose: CLAUDE.md [2026-05-25] — a spec file matched by no project regex NEVER runs, and its assertions certify nothing. Every prior phase (26/28/29/30/32/33/34/35/36/37) uses one broad `tests/phaseNN/**` registration as the single registration point. This plan does the same for 40, and lands one genuinely-live guard (the frozen publish spine) before any plan starts editing `publish-core.ts`.

Output: one `phase40` project entry, seven spec files (one live, six `test.fixme` stubs that later plans activate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-shared-creation-foundation/40-CONTEXT.md
@.planning/phases/40-shared-creation-foundation/40-RESEARCH.md
@.planning/phases/40-shared-creation-foundation/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase40 Playwright project</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (read the `phase36` and `phase37` project entries near the end of the `projects` array — copy their exact shape, comment style, and `use` block)
  </read_first>
  <action>
Append a new project entry to the `projects` array in `playwright.config.ts`, immediately after the existing `phase37` entry and before the array closes.

Exact fields, matching the `phase37` precedent:
- `name: 'phase40'`
- `testDir: '.'`
- `testMatch: /tests\/phase40\/.*\.(spec|test)\.ts$/`
- `use: { browserName: 'chromium' }`

Precede the entry with a comment block in the same house style as `phase36`/`phase37` that states: the phase name ("Phase 40 -- Shared Creation Foundation"), the CLAUDE.md 2026-05-25 rule (a spec in no project regex never runs), that the `testMatch` is deliberately broad so later plans drop specs into `tests/phase40/` with no config edit, the verification command `npx playwright test --list --project=phase40`, and the wave-0 file inventory listed in Task 2 (which are live, which are fixme).

Do not modify any other project entry.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase40 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains the literal string `name: 'phase40'`
    - `playwright.config.ts` contains the literal regex source `tests\/phase40\/.*\.(spec|test)\.ts$`
    - `npx playwright test --list --project=phase40` exits 0 and lists exactly the seven spec files created in Task 2 (zero discovered = FAIL)
    - No existing project entry (`phase26`..`phase37`, `phase15-stubs`, `phase15-unit`, integration, e2e) is added, removed, or reordered — `git diff playwright.config.ts` shows only an inserted block
  </acceptance_criteria>
  <done>`npx playwright test --list --project=phase40` discovers all seven wave-0 spec files.</done>
</task>

<task type="auto">
  <name>Task 2: Create the seven wave-0 spec files</name>
  <files>tests/phase40/spine-freeze.spec.ts, tests/phase40/dup01-file-intake.spec.ts, tests/phase40/dup02-metadata-picker.spec.ts, tests/phase40/dup03-job-progress.spec.ts, tests/phase40/dup04-page-shell.spec.ts, tests/phase40/dat01-category-column.spec.ts, tests/phase40/dat01-migration.spec.ts</files>
  <read_first>
    - tests/lint/no-bulk-verify-ui.spec.ts (the source-contract sweep pattern: read files with `fs.readFileSync`, assert a forbidden pattern has zero matches)
    - tests/lint/no-undefined-css-tokens.spec.ts (exhaustive negative-assertion sweep across a directory)
    - tests/phase37/*.spec.ts (pick any one — copy the header-comment convention naming the requirement ID and the plan that activates the spec)
    - src/lib/governance/publish-core.ts (for the spine-freeze assertions — read the `assertPublishGates` signature and the exported names)
    - src/app/api/sops/[sopId]/publish/route.ts (confirm it calls `assertPublishGates(`)
  </read_first>
  <action>
Create `tests/phase40/` with seven files. Every file starts with a header comment naming its requirement ID, the CONTEXT.md decisions it encodes, and which plan activates it.

**Shared helper convention (repeat in each file, do not create a shared module):** read source with `fs.readFileSync(path, 'utf8').replace(/\r\n/g, '\n')` — CLAUDE.md [2026-07-18], worktree checkouts CRLF-normalize files and break `\n`-literal assertions. When counting occurrences, strip comment lines first (`src.split('\n').filter(l => !l.trim().startsWith('//') && !l.trim().startsWith('--') && !l.trim().startsWith('*'))`) so header prose is never self-invalidating.

1. `spine-freeze.spec.ts` — **LIVE from this plan onward.** Three assertions:
   - `src/lib/governance/publish-core.ts` exports `assertPublishGates`
   - the exported `assertPublishGates` body still contains the literals `unverified_blocks` and `status: 400`
   - `src/app/api/sops/[sopId]/publish/route.ts` contains `assertPublishGates(`
   Header comment states why: CLAUDE.md [2026-07-13] — a relocated/edited guard whose spec still greps the old file is a guard that stopped guarding; plan 40-05 edits `publish-core.ts` (cadence read) and must not touch the gate.

2. `dup01-file-intake.spec.ts` — `test.fixme`, activated by 40-02 / 40-07. Assertions to write now (D-04/D-05):
   - exactly one file under `src/` declares `ACCEPTED_MIME_TYPES` (the shared intake module)
   - `src/components/admin/UploadDropzone.tsx`, `src/components/admin/VideoFormatSelectionModal.tsx`, and `src/app/(protected)/admin/sops/[sopId]/versions/page.tsx` each import from `@/lib/upload/file-intake` and declare no local accept-list / `MAX_FILE_SIZE` / `BLOCKED_EXTENSIONS` / `heic2any` literal
   - zero occurrences of the substring `.doc,` anywhere in `src/` (the dropped extension, D-04)
   - the shared module's accept list contains `image/webp`, `video/mp4`, `video/quicktime`

3. `dup02-metadata-picker.spec.ts` — `test.fixme`, activated by 40-08 (D-09..D-12):
   - `PromptClient.tsx`, `WizardClient.tsx`, `VoiceDraftClient.tsx` each import `SopMetadataFields` and contain zero `<DepartmentPicker` JSX of their own
   - `src/components/admin/SopMetadataFields.tsx` renders `DepartmentPicker` with `localOnly` and sources its category options from `@/lib/sop-categories` (not a `.from('sops').select('category')` query)
   - `src/app/(protected)/admin/sops/upload/page.tsx` does NOT import `SopMetadataFields` (D-12 — upload is Phase 42 scope; adding it here is a scope regression)
   - a behavioural assertion (not just import presence, CLAUDE.md [2026-06-05]) that the picker's `onChange` result reaches `assignSopDepartments` — assert `src/app/api/sops/ai-prompt/route.ts` contains `assignSopDepartments(` and `src/actions/sops.ts` contains `assignSopDepartments(`, and that neither file contains a direct `.from('sop_departments').insert` (D-11)

4. `dup03-job-progress.spec.ts` — `test.fixme`, activated by 40-03 (D-07/D-08):
   - `src/components/admin/PipelineStepper.tsx` does not exist
   - `PipelineProgressClient.tsx` contains zero occurrences of `postgres_changes`, `setInterval`, `REALTIME_GRACE_MS`, `REALTIME_STALE_MS`, `POLL_INTERVAL_MS`
   - exactly one file under `src/` contains both `REALTIME_GRACE_MS` and `REALTIME_STALE_MS` and it is `src/components/admin/ParseJobStatus.tsx`
   - `src/lib/admin/job-stages.ts` exports `STAGE_SETS` with a `video_generation` key and exports a plain-language map whose values include the strings `Uploading`, `Reading your document`, `Building the draft`, `Checking`, `Making the video`, `Ready`
   - no file under `src/` or `supabase/migrations/` renames the DB stage values — assert the internal keys `extracting_audio`, `transcribing`, `structuring`, `verifying`, `prompting`, `drafting` all still appear in `src/lib/admin/job-stages.ts` (D-07: map at render, never rename)

5. `dup04-page-shell.spec.ts` — `test.fixme`, activated by 40-09:
   - each of `admin/sops/upload/page.tsx`, `admin/sops/new/blank/page.tsx`, `admin/sops/new/ai/page.tsx`, `admin/sops/[sopId]/versions/page.tsx`, `admin/sops/pipeline/[pipelineId]/PipelineProgressClient.tsx` imports `AdminPageShell`
   - zero occurrences of the literal `Back to library` in any file under `src/app/(protected)/admin/sops/` other than `src/components/admin/AdminPageShell.tsx`
   - `AdminPageShell.tsx` renders `<AdminNav` and accepts an optional contextual back-link prop (RESEARCH Pitfall 5 — the per-SOP back-link must survive)

6. `dat01-category-column.spec.ts` — `test.fixme`, activated by 40-04 / 40-05 (D-01):
   - zero occurrences of `category_tag` as a `sops` column read or write anywhere under `src/` (the `blocks.category_tags` array column is a different column — scope the sweep to exclude `category_tags` with the trailing `s`)
   - zero write-side occurrences of `category:` in the five SOP-creating API routes (`parse`, `restructure`, `transcribe`, `youtube`, `ai-prompt`) and `src/actions/sops.ts`
   - `src/lib/sop-categories.ts` exports `SOP_CATEGORIES`, `categoryLabel`, `isValidCategorySlug`
   - `src/lib/org-model/sop-collections.ts` and `src/actions/governance.ts` select `category_slug`, not `category`

7. `dat01-migration.spec.ts` — `test.fixme`, activated by 40-04 / 40-06. Encodes CLAUDE.md [2026-07-28] (an applier that omits a later corrective migration, or an assertion that pins only some clauses, certifies nothing):
   - `scripts/apply-phase40-migration.mjs` declares its migration file list as an ordered array and that array contains every `supabase/migrations/*.sql` file whose name matches the phase-40 migrations on disk, in ascending filename order (assert by comparing the array to a `fs.readdirSync` glob, index by index)
   - `supabase/migrations/00058_sop_category_slug.sql` contains `add column if not exists category_slug`, an index on `category_slug`, and `comment on column public.sops.category is` / `comment on column public.sops.category_tag is` marking both retired
   - the slug list inlined in the migration's deterministic backfill exactly equals the slug set exported from `src/lib/sop-categories.ts` (parse both, compare as sorted arrays — this is the drift guard that replaces a lookup table)
   - `scripts/verify-category-backfill.mjs` issues a live `count` query pinning BOTH retired columns (`category is not null` AND `category_tag is not null`), not just one

Every `test.fixme` block must contain the real assertion body (commented-in code that will run once un-fixme'd), not a `TODO` placeholder — the later plan's job is to delete the `.fixme`, not to write the test.
  </action>
  <verify>
    <automated>npx playwright test --project=phase40 --grep "spine" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase40` lists all seven files
    - `npx playwright test --project=phase40` exits 0 with the spine-freeze tests PASSED and all other tests reported as skipped/fixme (zero failures)
    - Every file reads source via `.replace(/\r\n/g, '\n')`
    - Every occurrence-count assertion filters comment lines before counting (no bare `grep -c`-equivalent on unfiltered text)
    - `tests/phase40/spine-freeze.spec.ts` contains the literals `assertPublishGates`, `unverified_blocks`, and `status: 400`
    - Every `test.fixme` body contains executable assertion code, not a `TODO`/`expect(true)` placeholder
  </acceptance_criteria>
  <done>Seven spec files exist, the phase40 project discovers all seven, spine-freeze passes live, the other six are fixme with real bodies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> production safety | A spec that never runs is a safety control that silently does not exist; the publish gate is the last thing standing between an unverified SOP and a worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Tampering | `playwright.config.ts` project regex | mitigate | Task 1 verify command is `--list --project=phase40`; zero discovered files fails the task (CLAUDE.md 2026-05-25) |
| T-40-01-02 | Repudiation | `publish-core.ts` publish gate | mitigate | `spine-freeze.spec.ts` is LIVE from wave 1, pinning `assertPublishGates` + `unverified_blocks` + `status: 400` and the route's call site, before 40-05 edits that file |
| T-40-01-03 | Tampering | fixme stubs with placeholder bodies | mitigate | Acceptance criterion forbids `TODO`/`expect(true)` bodies — a stub that asserts nothing is the 2026-06-05 dead-guard class |
| T-40-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages; RESEARCH.md § Package Legitimacy Audit records the phase as install-free |
</threat_model>

<verification>
- `npx playwright test --list --project=phase40` lists 7 files
- `npx playwright test --project=phase40` — 0 failures, spine-freeze green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- One broad `phase40` project registration exists; no later plan in this phase needs a `playwright.config.ts` edit
- The frozen publish spine has a live guard before any plan touches `publish-core.ts`
- All five requirement IDs have a named, body-complete spec waiting for them
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by THIS plan (exclude from drift verification):
- `playwright.config.ts` project entry `phase40`
- `tests/phase40/spine-freeze.spec.ts`
- `tests/phase40/dup01-file-intake.spec.ts`
- `tests/phase40/dup02-metadata-picker.spec.ts`
- `tests/phase40/dup03-job-progress.spec.ts`
- `tests/phase40/dup04-page-shell.spec.ts`
- `tests/phase40/dat01-category-column.spec.ts`
- `tests/phase40/dat01-migration.spec.ts`

Symbols these specs REFERENCE that later plans create (do not exist yet — this is intentional, hence `test.fixme`):
`src/lib/upload/file-intake.ts`, `src/lib/upload/start-video-sop-upload.ts`, `src/lib/admin/job-stages.ts`, `src/lib/sop-categories.ts`, `src/components/admin/SopMetadataFields.tsx`, `src/components/admin/AdminPageShell.tsx`, `supabase/migrations/00058_sop_category_slug.sql`, `scripts/survey-sop-categories.mjs`, `scripts/apply-phase40-migration.mjs`, `scripts/backfill-sop-category.mjs`, `scripts/verify-category-backfill.mjs`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/40-shared-creation-foundation/40-01-SUMMARY.md` when done
</output>
