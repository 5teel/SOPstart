---
phase: 40-shared-creation-foundation
plan: 14
type: execute
wave: 2
depends_on: [40-11, 40-13]
gap_closure: true
files_modified:
  - src/actions/versioning.ts
  - src/actions/sops.ts
  - src/lib/upload/start-video-sop-upload.ts
  - src/components/admin/VideoPreviewPanel.tsx
  - src/components/admin/VideoFormatSelectionModal.tsx
  - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx
  - supabase/migrations/00059_sop_videos_storage_scope.sql
  - scripts/apply-phase40-migration.mjs
  - tests/phase40/no-service-role-to-browser.spec.ts
  - tests/phase40/dat01-migration.spec.ts
autonomous: false
requirements: [DUP-01]

must_haves:
  truths:
    - "No server action returns SUPABASE_SERVICE_ROLE_KEY to a browser — the only file under src/ that references the variable is src/lib/supabase/admin.ts"
    - "Video TUS uploads authenticate with the caller's own session access token, the same way the document TUS branch already does"
    - "A worker-role member cannot write into the sop-videos bucket, and an admin cannot write outside their own organisation's path prefix"
    - "The phase-40 migration applier applies 00058 and 00059 in order, and its ordering guard covers both"
  artifacts:
    - path: "supabase/migrations/00059_sop_videos_storage_scope.sql"
      provides: "Org-prefix + admin-role scoped INSERT and UPDATE policies on storage.objects for the sop-videos bucket, replacing the bucket-wide authenticated INSERT policy"
      contains: "current_organisation_id"
    - path: "src/lib/upload/start-video-sop-upload.ts"
      provides: "The single client video-upload routine resolving its own session access token"
      contains: "auth.getSession()"
    - path: "tests/phase40/no-service-role-to-browser.spec.ts"
      provides: "Sweep proving the service-role key is referenced by exactly one file under src/"
  key_links:
    - from: "src/lib/upload/start-video-sop-upload.ts"
      to: "src/lib/upload/tus-upload.ts"
      via: "accessToken sourced from supabase.auth.getSession()"
      pattern: "access_token"
    - from: "supabase/migrations/00059_sop_videos_storage_scope.sql"
      to: "storage.objects (bucket sop-videos)"
      via: "org-prefix + role scoped INSERT/UPDATE policies"
      pattern: "storage.foldername"
---

<objective>
Close verification GAP 2 (DUP-01 / security): `uploadNewVersion`'s video branch returns `process.env.SUPABASE_SERVICE_ROLE_KEY` to the browser as a TUS access token. Two sibling sites in `src/actions/sops.ts` (`createVideoUploadSession` line ~184, `createVideoSopPipelineSession` line ~477) predate this phase; plan 40-07 copied the pattern into a new surface instead of closing it.

Purpose: the service-role key bypasses RLS entirely. Any admin or safety_manager of any tenant currently receives the full key in their browser, visible in DevTools — a captured key grants cross-tenant read/write of the whole database via PostgREST and every storage bucket. Per CLAUDE.md rule #5 ("fix the scope"), all three sites close in one pass. The correct token is the one the document TUS branch in `UploadDropzone.tsx` line 302 already uses: the caller's own session `access_token`.

That swap makes the `sop-videos` storage RLS policy load-bearing for the first time — it becomes the thing authorising the write. Migration 00012 left that bucket with `WITH CHECK (bucket_id = 'sop-videos')` and no org or role scope, so migration 00059 tightens it to the `sop-documents` shape from 00005 (org path prefix + admin role).

Output: zero service-role key references outside `src/lib/supabase/admin.ts`, video uploads authenticated as the user, and a bucket policy that actually scopes them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-shared-creation-foundation/40-CONTEXT.md
@.planning/phases/40-shared-creation-foundation/40-VERIFICATION.md
@.planning/phases/40-shared-creation-foundation/40-REVIEW.md
@src/lib/upload/tus-upload.ts
@src/lib/upload/start-video-sop-upload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop returning the service-role key; authenticate video TUS uploads as the caller</name>
  <files>src/actions/versioning.ts, src/actions/sops.ts, src/lib/upload/start-video-sop-upload.ts, src/components/admin/VideoPreviewPanel.tsx, src/components/admin/VideoFormatSelectionModal.tsx, src/app/(protected)/admin/sops/[sopId]/versions/page.tsx</files>
  <read_first>
    - src/actions/versioning.ts lines 102-140 — `uploadNewVersion`'s video branch, the `token: process.env.SUPABASE_SERVICE_ROLE_KEY ?? ''` at line 136 and the comment above it claiming this is "the same token source createVideoUploadSession uses"; and lines 20-26, the return type union that declares `token: string`
    - src/actions/sops.ts lines 114-202 — `createVideoUploadSession`: `const token = process.env.SUPABASE_SERVICE_ROLE_KEY ?? ''` at 184, the `signedUploadUrl` branch for files under 10MB at 186-194 (a legitimate signed upload URL — keep it), and the return shape at 196-201
    - src/actions/sops.ts lines 446-514 — `createVideoSopPipelineSession`'s video branch returning the key at 477, and the non-video branch at 507-514 returning a REAL `createSignedUploadUrl` token (that one stays)
    - src/lib/upload/tus-upload.ts (whole file, 57 lines) — `accessToken` becomes the `authorization: Bearer` header; `x-upsert: 'true'` is set, which is why 00059 needs an UPDATE policy as well as INSERT
    - src/components/admin/UploadDropzone.tsx lines 288-330 — THE CORRECT PRECEDENT: the document TUS branch calls `supabase.auth.getSession()` and passes `authSession.access_token`, with a "Not authenticated" error path. Copy this shape.
    - src/components/admin/UploadDropzone.tsx lines 240-262 — passes `sessionResult` as a VARIABLE to `startVideoSopUpload`, so removing `token` from the session interface requires no edit here (excess-property checks apply only to object literals). Confirm this while editing.
    - The three literal call sites that DO need editing: src/components/admin/VideoFormatSelectionModal.tsx line ~89 (`session: { sopId, path, token: session.token }`), src/app/(protected)/admin/sops/[sopId]/versions/page.tsx line ~263 (same literal), src/components/admin/VideoPreviewPanel.tsx lines 148 + 198 (destructures `token` and passes it straight to `tusUpload`)
  </read_first>
  <action>
**Server side — remove the key from three return values.**
1. `src/actions/versioning.ts` `uploadNewVersion`: delete the `token:` line from the VIDEO branch return and delete the comment above it claiming the service-role key is the correct TUS token. Make `token` optional (`token?: string`) on the success arm of the return union — the non-video branch keeps returning `signedData.token`, which is a genuine `createSignedUploadUrl` token and must not change.
2. `src/actions/sops.ts` `createVideoUploadSession`: delete `const token = process.env.SUPABASE_SERVICE_ROLE_KEY ?? ''` and the `token` field from the return, along with the "Use service role key for TUS upload auth" comment. Keep `signedUploadUrl` and its under-10MB branch exactly as they are. Update the declared return type accordingly.
3. `src/actions/sops.ts` `createVideoSopPipelineSession`: delete the `token:` line from the video branch return; make `token` optional on the success type; leave the non-video branch's `signedData.token` untouched.

**Client side — each upload routine resolves its own token.**
4. `src/lib/upload/start-video-sop-upload.ts` is the single shared client routine for three of the four call sites, so the token is resolved THERE, once: drop `token` from `StartVideoSopUploadSession`; inside `startVideoSopUpload`, `const supabase = createClient()` (from `@/lib/supabase/client`), `const { data: { session } } = await supabase.auth.getSession()`, and return `{ ok: false, error: 'Not authenticated' }` (also calling `onError`) when there is no session. Pass `session.access_token` as `tusUpload`'s `accessToken`. Everything else — the audio extraction, the 0-50/50-100 progress split, the `/api/sops/transcribe` trigger on success — is unchanged.
5. Update the two object-literal call sites (`VideoFormatSelectionModal.tsx`, `versions/page.tsx`) to stop passing `token`.
6. `src/components/admin/VideoPreviewPanel.tsx` calls `tusUpload` directly with already-extracted audio, so it cannot go through `startVideoSopUpload`: give it the same `supabase.auth.getSession()` + `access_token` treatment as `UploadDropzone`'s document branch, with a `showToast` + `setPanelState('upload-error')` path when there is no session. Stop destructuring `token` from the session result.

Both `src/actions/*.ts` files are `'use server'` modules: every export must remain an async function (CLAUDE.md [2026-06-27]).

Do not change `src/lib/upload/tus-upload.ts` — its `accessToken` parameter is correct; only its callers were feeding it the wrong value.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SUPABASE_SERVICE_ROLE_KEY" src/` returns exactly one file: `src/lib/supabase/admin.ts`
    - `src/lib/upload/start-video-sop-upload.ts` contains `auth.getSession()` and `access_token`, and `StartVideoSopUploadSession` no longer declares `token`
    - `src/components/admin/VideoPreviewPanel.tsx` contains `access_token` and no longer passes a server-supplied `token` to `tusUpload`
    - `VideoFormatSelectionModal.tsx` and `versions/page.tsx` no longer pass `token:` into `startVideoSopUpload`
    - The non-video branches of `uploadNewVersion` and `createVideoSopPipelineSession` still return `signedData.token`, and `createVideoUploadSession` still returns `signedUploadUrl`
    - `npx tsc --noEmit` clean; `npm run build` clean
  </acceptance_criteria>
  <done>The service-role key never leaves the server; video uploads carry the user's own JWT, so storage RLS applies to them.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the sop-videos bucket policies and apply the migration, with the sweep and ordering guards</name>
  <files>supabase/migrations/00059_sop_videos_storage_scope.sql, scripts/apply-phase40-migration.mjs, tests/phase40/no-service-role-to-browser.spec.ts, tests/phase40/dat01-migration.spec.ts</files>
  <read_first>
    - supabase/migrations/00012_video_transcription.sql lines 77-107 — the bucket creation and the two policies to replace: `"Authenticated users can upload to sop-videos"` (`WITH CHECK (bucket_id = 'sop-videos')`, no org, no role) and `"Authenticated users can read from sop-videos"`
    - supabase/migrations/00005_sop_storage_rls.sql lines 29-36 — THE TARGET SHAPE: `bucket_id = 'sop-documents' and (storage.foldername(name))[1] = public.current_organisation_id()::text and public.current_user_role() in ('admin','safety_manager')`
    - scripts/apply-phase40-migration.mjs lines 1-60 — the header's [2026-07-28] rule, the `MIGRATIONS` array (currently `['00058_sop_category_slug.sql']`) and `MIGRATION_FILES`; then read the rest for the Management API query helper and the post-apply assertion block, which is where the new assertions go
    - tests/phase40/dat01-migration.spec.ts lines 59-84 — the ordering guard. Note line 69: it filters on-disk migrations with `f.startsWith('00058')`, so adding 00059 on disk would NOT be seen and the guard would go stale-green. This is the [2026-07-13] stale-guard class and MUST be fixed in this task.
    - src/actions/sops.ts + src/actions/versioning.ts as left by Task 1 — the storage paths written are `${organisationId}/${sopId}/audio/audio.${ext}`, so `(storage.foldername(name))[1]` is the org id
    - src/app/api/sops/transcribe/route.ts line ~119 and src/app/api/sops/[sopId]/source-url/route.ts line ~87 — the two READ paths on this bucket (admin client, and a session-client `createSignedUrl` for admins in the SOP's own org), so you can state in the SUMMARY why SELECT is left alone
  </read_first>
  <action>
**`supabase/migrations/00059_sop_videos_storage_scope.sql`** (new). Purely a policy change on `storage.objects` for `bucket_id = 'sop-videos'`:
- Drop the bucket-wide `"Authenticated users can upload to sop-videos"` INSERT policy from 00012 (guarded `DROP POLICY IF EXISTS`, matching this repo's `DO $$ ... IF NOT EXISTS` idiom in 00012).
- Create an INSERT policy scoped exactly like 00005's `admins_can_upload_sop_docs`: `bucket_id = 'sop-videos' AND (storage.foldername(name))[1] = public.current_organisation_id()::text AND public.current_user_role() IN ('admin','safety_manager')`.
- Create an UPDATE policy with the same predicate in both `USING` and `WITH CHECK`. This is required, not optional: `tus-upload.ts` sends `x-upsert: 'true'`, so a resumed or repeated upload to an existing object needs UPDATE, and without it the swap in Task 1 would break resumable uploads.
- Leave the SELECT policy from 00012 untouched and say so in a header comment with the reason: reads on this bucket go through the admin client (`transcribe`) or a session-client signed URL already gated by an admin role check and RLS on `sops` (`source-url`); narrowing SELECT is a separate pre-existing concern and is out of this gap-closure pass's scope.
- Add no table, no DROP COLUMN, no SECURITY DEFINER function.

**`scripts/apply-phase40-migration.mjs`**:
- Extend `MIGRATIONS` to `['00058_sop_category_slug.sql', '00059_sop_videos_storage_scope.sql']` — order is load-bearing and the header comment already forbids dropping entries.
- Add post-apply assertions in the existing Management-API assertion block, pinning EVERY clause of the new policies rather than their existence (CLAUDE.md [2026-07-28]): query `pg_policies` for `schemaname='storage' AND tablename='objects'` and assert (a) no policy named `Authenticated users can upload to sop-videos` remains, (b) the new INSERT policy's `with_check` contains all three of `sop-videos`, `current_organisation_id`, `current_user_role`, and (c) the new UPDATE policy exists with the same three tokens in BOTH `qual` and `with_check`. Print each assertion's pass/fail with the clause it checked, in the style of the script's existing output.
- Add one pre-apply audit query, printed not enforced: count objects in `storage.objects` where `bucket_id = 'sop-videos'` and the first path segment is not a valid UUID — a legacy object outside the org-prefix convention would lose write access under the new policy. Report the count in the SUMMARY.
- Then RUN it: `node scripts/apply-phase40-migration.mjs`. This applies to the live remote database, exactly as plans 40-04 and 40-06 did. Do not hand-edit the live policy; the script is the record.

**`tests/phase40/dat01-migration.spec.ts`**:
- Fix the stale filter at line 69: replace `f.startsWith('00058')` with a check covering every phase-40 migration (a `PHASE40_MIGRATION_PREFIXES = ['00058', '00059']` array, or a regex `/^000(58|59)_/`), with a comment explaining that a filter narrower than the phase's migration set silently exempts a later corrective migration from the ordering guard — the exact regression class the file's own header cites.
- Add a clause-pinning test for 00059 mirroring the existing 00058 one: strip `--` comment lines first, then assert the SQL contains `drop policy` for the old permissive name, contains `storage.foldername`, `current_organisation_id`, `current_user_role`, has both an `for insert` and a `for update` policy, and contains none of `drop column`, `create table`, `security definer`.

**`tests/phase40/no-service-role-to-browser.spec.ts`** (new; auto-discovered by the broad `phase40` `testMatch`, but confirm with `npx playwright test --list --project=phase40 | grep no-service-role-to-browser`):
- Walk `src/`, and assert the set of files containing `SUPABASE_SERVICE_ROLE_KEY` is exactly `['src/lib/supabase/admin.ts']`. Keyed on the secret itself, not on a list of known-bad functions — a fourth site anywhere under `src/` fails immediately.
- Assert `src/lib/upload/tus-upload.ts` still takes `accessToken` from its caller (no hardcoded token) and that `start-video-sop-upload.ts` supplies it from `auth.getSession()`.
- Assert no file under `src/actions/` returns an object literal whose `token` value is a `process.env.` expression (regex `token:\s*process\.env\.`), so a future action cannot re-introduce the pattern under a different env-var name.
  </action>
  <verify>
    <automated>node scripts/apply-phase40-migration.mjs && npx playwright test --project=phase40 --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/apply-phase40-migration.mjs` exits 0 with every post-apply assertion printed PASS, including the three-token clause checks on both new policies and the absence of `Authenticated users can upload to sop-videos`
    - `scripts/apply-phase40-migration.mjs`'s `MIGRATIONS` array lists 00058 then 00059, in that order
    - `tests/phase40/dat01-migration.spec.ts` no longer contains `startsWith('00058')`
    - The ordering guard FAILS if 00059 is removed from the `MIGRATIONS` array while present on disk — run it once with the entry removed, confirm RED, restore, confirm GREEN; record both in the SUMMARY
    - The service-role sweep FAILS if `process.env.SUPABASE_SERVICE_ROLE_KEY` is temporarily re-added to `src/actions/sops.ts` — record the observation
    - `npx playwright test --project=phase40 --reporter=line` — 0 failures
    - Pre-apply audit count of non-UUID-prefixed `sop-videos` objects recorded in the SUMMARY
  </acceptance_criteria>
  <done>The bucket that now authorises video uploads by RLS is scoped to the caller's organisation and admin role, applied live, with the applier's ordering guard covering both phase-40 migrations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm video upload still works on sopstart.com after the auth change</name>
  <what-built>
Video uploads no longer carry the service-role key. They now authenticate with the signed-in admin's own session token, and migration 00059 restricts the `sop-videos` bucket to writes inside the caller's own organisation folder by an admin or safety manager.

Every check available in code has been run — typecheck, build, the phase40 suite, and the applier's live policy assertions. None of them can prove that a real browser upload still completes, because that requires a real session, a real file and the deployed app (CLAUDE.md [2026-07-14]: a green suite proves nothing about a live path).
  </what-built>
  <action>Pause and hand the verification steps below to Simon. Do not mark the plan complete, and do not write a SUMMARY claiming the upload path is verified, until he replies with the resume signal. If he reports a 403 or an RLS violation on upload, capture the exact message and treat it as a follow-up migration adjusting 00059's predicate — never restore the service-role key.</action>
  <files>none — verification only, on the deployed app</files>
  <how-to-verify>
1. Push and let Railway deploy, then open https://sopstart.com signed in as an admin.
2. Go to **Admin → SOPs → Upload** and drop an **MP4 larger than 10MB** (this forces the resumable TUS path — smaller files take a different, unchanged route). The progress bar should reach 100% and the SOP should appear with a transcription job running.
3. Open an existing SOP → **Versions** → upload an MP4 as a new version. It should upload and land on the builder for the new version.
4. Open a video SOP's review/source view and confirm the source video still plays (this checks that reads were not affected).
5. In DevTools → Network, open the response of the `createVideoUploadSession` / `uploadNewVersion` action call and confirm there is **no long `eyJ...` service key** in it.

If any upload fails with a 403 or "new row violates row-level security policy", report it — that means the 00059 policy predicate needs adjusting, and the fix is a follow-up migration, never a rollback to the service-role key.
  </how-to-verify>
  <resume-signal>Type "approved", or paste the error text and the step it failed on</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server action -> browser | Anything in a server action's return value is readable in DevTools; this is where the service-role key was crossing |
| Browser -> Supabase Storage `/storage/v1/upload/resumable` | TUS request authorised by whatever bearer token the client holds; RLS on `storage.objects` is the only gate |
| Admin/safety_manager session -> `sop-videos` bucket | Must be confined to the caller's own organisation path prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-14-01 | Information Disclosure | `SUPABASE_SERVICE_ROLE_KEY` returned to the browser by three server actions | mitigate | All three return sites removed; the sweep spec pins the key to `src/lib/supabase/admin.ts` alone and also blocks any `token: process.env.*` return under `src/actions/` |
| T-40-14-02 | Elevation of Privilege | a captured service-role key grants cross-tenant read/write of every table and bucket via PostgREST | mitigate | Root cause of T-40-14-01; the key is now server-only. Key rotation after deploy is an operator decision — flag it in the SUMMARY as recommended, since the key has been reaching browsers since Phase 6 |
| T-40-14-03 | Tampering | any authenticated user writes arbitrary objects into `sop-videos` (00012's bucket-wide INSERT policy) | mitigate | 00059 replaces it with org-prefix + admin-role INSERT and UPDATE policies mirroring 00005; the applier pins all three clauses of each |
| T-40-14-04 | Denial of Service | resumable upload breaks because `x-upsert` needs UPDATE and no UPDATE policy exists | mitigate | 00059 creates the UPDATE policy with the same predicate; the human checkpoint exercises a >10MB TUS upload end to end |
| T-40-14-05 | Information Disclosure | any authenticated user can read any org's SOP audio (00012's bucket-wide SELECT policy) | accept | Pre-existing and outside this gap's scope; documented in the migration header and the SUMMARY as a follow-up. Not made worse by this plan — no read path changes |
| T-40-14-06 | Tampering | a legacy `sop-videos` object stored outside the `{org_id}/...` convention loses write access under the new policy | mitigate | Pre-apply audit query counts non-UUID-prefixed objects and reports before the policy change; a non-zero count is reported in the SUMMARY rather than silently ignored |
| T-40-14-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; RESEARCH.md records the phase as install-free |
</threat_model>

<verification>
- `grep -rn "SUPABASE_SERVICE_ROLE_KEY" src/` → one file only
- `node scripts/apply-phase40-migration.mjs` exits 0, all clause assertions PASS
- `npx playwright test --project=phase40 --reporter=line` — 0 failures
- `npx playwright test --list --project=phase40 | grep no-service-role-to-browser` — discovered
- `npx tsc --noEmit` clean
- `npm run build` clean (this plan edits two `'use server'` files)
- Human checkpoint (Task 3) approved on sopstart.com
</verification>

<success_criteria>
- 40-VERIFICATION.md GAP 2 closed at all three sites, not just the one plan 40-07 introduced
- 40-REVIEW.md CR-01 closed
- Video TUS uploads authenticate exactly like the document TUS branch already did
- The `sop-videos` bucket enforces org and role on writes, applied live and clause-pinned by the applier
</success_criteria>

<artifacts_this_phase_produces>
Created/changed by THIS plan (exclude from drift verification):
- `token` removed from the video-branch return of `uploadNewVersion`, `createVideoUploadSession`, `createVideoSopPipelineSession`
- `StartVideoSopUploadSession` loses its `token` field; `startVideoSopUpload` resolves the session token itself
- `supabase/migrations/00059_sop_videos_storage_scope.sql` (new)
- `scripts/apply-phase40-migration.mjs` `MIGRATIONS` array extended + new policy-clause assertions
- `tests/phase40/no-service-role-to-browser.spec.ts` (new); `dat01-migration.spec.ts` ordering filter widened

Referenced from earlier work (created there, not new here): `src/lib/upload/tus-upload.ts`, `startVideoSopUpload`, `createSignedUploadUrl` document branches, `public.current_organisation_id()` / `public.current_user_role()` (migration 00005)

Deliberately NOT changed (documented, out of scope): the `sop-videos` SELECT policy; service-role key rotation (operator action, recommended in the SUMMARY)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/40-shared-creation-foundation/40-14-SUMMARY.md` when done
</output>
