---
phase: 40-shared-creation-foundation
plan: 05
type: execute
wave: 3
depends_on: [40-04]
files_modified:
  - src/actions/governance.ts
  - src/lib/governance/cadences.ts
  - src/lib/governance/publish-core.ts
  - src/app/api/sops/[sopId]/publish/route.ts
  - src/lib/org-model/sop-collections.ts
  - src/app/(protected)/admin/settings/page.tsx
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/sops/page.tsx
  - src/components/sop/SopLibraryCard.tsx
  - src/components/sop/tabs/ReadTab.tsx
  - src/components/admin/governance/GovernanceQueueRow.tsx
  - src/hooks/useAssignedSops.ts
  - src/app/api/voice/query/route.ts
  - tests/phase40/dat01-category-column.spec.ts
autonomous: true
requirements: [DAT-01]

must_haves:
  truths:
    - "A SOP created via the AI route and a SOP created via the wizard appear together under the same category filter on both the worker library and the admin list"
    - "A per-category review cadence an org configured still resolves to that org's SOP after the migration"
    - "A per-category approval chain still diverts a publish to approval after the migration"
    - "Publishing a SOP still creates or joins the org-model Collection named after its category"
    - "assertPublishGates is byte-unchanged — the parse -> AI-review -> verify -> publish spine is frozen"
  artifacts:
    - path: "src/actions/governance.ts"
      provides: "cadence resolution and the governance queue reading category_slug"
      contains: "category_slug"
    - path: "src/lib/org-model/sop-collections.ts"
      provides: "org-model Collection creation keyed off category_slug, named with the vocabulary label"
      contains: "categoryLabel"
  key_links:
    - from: "src/lib/org-model/sop-collections.ts"
      to: "src/lib/sop-categories.ts"
      via: "categoryLabel import for the collection name"
      pattern: "categoryLabel"
    - from: "src/hooks/useAssignedSops.ts"
      to: "sops.category_slug"
      via: "client-side category filter"
      pattern: "category_slug"
---

<objective>
Repoint every reader of the retiring category columns onto `sops.category_slug`, including the three settings tables keyed by the old free-text value that a source-only sweep never finds.

Purpose: DAT-01 / RESEARCH Pitfall 1 — a migration that adds and backfills the new column looks complete while `sop_review_cadences`, `approval_chains` and `collections` keep reading the old one. If the old column is later nulled (plan 40-06), governance review dates silently reset, per-category approval chains silently stop diverting a publish, and the org-model Collection tree silently stops growing. This plan closes all three before the data moves.

Output: thirteen read sites on the new column, `assertPublishGates` untouched, and the reader half of `dat01-category-column.spec.ts` live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-shared-creation-foundation/40-CONTEXT.md
@.planning/phases/40-shared-creation-foundation/40-RESEARCH.md
@.planning/phases/40-shared-creation-foundation/40-PATTERNS.md
@src/lib/sop-categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the three category-keyed settings consumers</name>
  <files>src/actions/governance.ts, src/lib/governance/cadences.ts, src/lib/governance/publish-core.ts, src/app/api/sops/[sopId]/publish/route.ts, src/lib/org-model/sop-collections.ts, src/app/(protected)/admin/settings/page.tsx</files>
  <read_first>
    - src/lib/sop-categories.ts (`SOP_CATEGORIES`, `categoryLabel` — written in plan 40-04)
    - src/actions/governance.ts (lines 55-105 for the cadence map builder and its row type; 205-230 for `sopRow.category` feeding `resolveCadenceMonths`; 260-295 for the cadence-write action keyed by `(organisation_id, category)`; 315-330 and 425-440 for the governance-queue select and row mapping)
    - src/lib/governance/cadences.ts (the `resolveCadenceMonths` signature — its first parameter is the SOP's category value)
    - src/lib/governance/publish-core.ts (lines 180-195: the cadence read that mirrors `governance.ts`. **`assertPublishGates` in this same file is FROZEN — do not touch it.** `tests/phase40/spine-freeze.spec.ts` pins it)
    - src/app/api/sops/[sopId]/publish/route.ts (lines 47-61: `select('category, approval_state')` then `approval_chains ... .eq('category', sopRow?.category ?? '')` — the SECOND category-keyed settings table, not recorded in RESEARCH.md)
    - src/lib/org-model/sop-collections.ts (whole file — `ensureSopCollectionsForOrg` selects `category` at line 33 and names the Collection after it at lines 62/70/78)
    - src/app/(protected)/admin/settings/page.tsx (lines 60-85: the live `DISTINCT sops.category` query that populates BOTH the cadence editor and the approval-chain editor)
    - supabase/migrations/00043_ownership_review_governance.sql and supabase/migrations/00045_approval_chains.sql (both tables' primary keys are `(organisation_id, category text)` — the COLUMN NAME on those tables does not change; only the VALUES stored in it are remapped, by plan 40-06's backfill)
  </read_first>
  <action>
The design decision that makes this cheap, and which must be stated in a comment at each site: **`sop_review_cadences.category` and `approval_chains.category` keep their column name and their text type. Only the values change** — from the old free-text `sops.category` string to a `SOP_CATEGORIES` slug. Plan 40-06's backfill remaps the existing rows with the same mapping it applies to `sops`. No schema change to either table.

**RESEARCH open question 1 — RESOLVED: remap, do not reset.** Existing per-category cadence overrides survive. Losing them is a silent governance regression for any org that customised a cadence, and the remap is the same mapping the SOP backfill already computes.

**RESEARCH open question 2 — RESOLVED: leave existing `collections` rows in place; do not rename or merge them.** Collections carry admin-customised `colour`/`sort` and are wired into the Phase 33 grant surface; renaming them risks that wiring for no user-visible gain. `ensureSopCollectionsForOrg` is repointed to read `category_slug` and to name new collections with the vocabulary **label** (via `categoryLabel`), so where an existing collection is already named e.g. `Safety` and the vocabulary label is `Safety`, the existing select-then-insert matches by name and the SOP joins the collection it would have joined anyway. Collections named after a free-text value with no vocabulary equivalent simply stop gaining members. Record this as a stated choice in the file header comment.

**`src/actions/governance.ts`:**
- the cadence-map builder (lines ~88-101): keep selecting `category, months` from `sop_review_cadences` (that column name is unchanged); the map key is now a slug
- the SOP read at line ~212: `.select('category')` becomes `.select('category_slug')`, and `resolveCadenceMonths(sopRow.category, ...)` becomes `resolveCadenceMonths(sopRow.category_slug, ...)`
- the cadence-write action at line ~266: keep the `category: string` parameter name and the `onConflict: 'organisation_id,category'` upsert, but gate the incoming value with `isValidCategorySlug` and return `{ error: 'unknown category' }` otherwise — this stops a new free-text cadence key being minted after the migration
- the governance-queue select at line ~323 and its row mapping at line ~434: `category` becomes `category_slug`, and the value handed to the UI becomes `categoryLabel(sop.category_slug)` so the queue keeps rendering a human label
- update the `category: string | null` field on the row type at line ~62 to `category_slug: string | null` and adjust every consumer of that type

**`src/lib/governance/cadences.ts`:** rename the `resolveCadenceMonths` first parameter to `categorySlug` and update its doc comment. The lookup logic is unchanged (a text key against a text-keyed map).

**`src/lib/governance/publish-core.ts`:** in the cadence block at lines ~180-195 only, change `sopRow?.category` to `sopRow?.category_slug` and add `category_slug` to that block's `sops` select. **Do not modify `assertPublishGates` or anything it calls.** After editing, run `git diff` on this file and confirm the diff touches only the cadence lines.

**`src/app/api/sops/[sopId]/publish/route.ts`:** change `.select('category, approval_state')` to `.select('category_slug, approval_state')` and `.eq('category', sopRow?.category ?? '')` to `.eq('category', sopRow?.category_slug ?? '')`. The `approval_chains` column stays named `category`; the value is now a slug. Add a comment naming this as the second category-keyed settings table and pointing at plan 40-06's remap. Do not change the surrounding `assertPublishGates` call or the pending-divert logic.

**`src/lib/org-model/sop-collections.ts`:** change the SOP select at line ~33 from `'id, organisation_id, category'` to `'id, organisation_id, category_slug'`; derive `const category = categoryLabel(sopRow.category_slug as string | null)` and use that label for the `collections.name` match and insert. Keep the select-then-insert / 23505-race-fallback shape and the `organisation_id !== orgId` guard verbatim — that org guard is the CLAUDE.md [2026-06-15]/[2026-07-28] service-role self-enforcement and must not be weakened.

**`src/app/(protected)/admin/settings/page.tsx`:** delete the live `DISTINCT sops.category` query at lines ~60-85 and source the cadence-editor and approval-chain-editor category lists from `SOP_CATEGORIES` instead. This is the fixed-seed read D-03 requires; RESEARCH names the live-distinct-query pattern as an anti-pattern that must not survive DAT-01. Existing cadence/chain rows whose key is not in the vocabulary must still render (so an admin can see and clear them) — merge the seed list with the distinct keys actually present in `sop_review_cadences` / `approval_chains` for display, marking non-vocabulary keys as legacy.
  </action>
  <verify>
    <automated>npx playwright test --project=phase40 --reporter=line && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `git diff src/lib/governance/publish-core.ts` touches only the cadence block; `assertPublishGates` is byte-unchanged and `tests/phase40/spine-freeze.spec.ts` still passes
    - `src/actions/governance.ts`, `src/lib/governance/publish-core.ts`, `src/app/api/sops/[sopId]/publish/route.ts`, `src/lib/org-model/sop-collections.ts` each contain `category_slug` and none of them selects the bare `category` column from `sops`
    - `src/lib/org-model/sop-collections.ts` contains `categoryLabel(` and still contains `sopRow.organisation_id !== orgId`
    - `src/app/(protected)/admin/settings/page.tsx` contains no `.from('sops')` query and does contain `SOP_CATEGORIES`
    - `src/actions/governance.ts` cadence-write action rejects a value failing `isValidCategorySlug`
    - `npx tsc --noEmit` clean; `npm run build` completes without error
  </acceptance_criteria>
  <done>Cadence resolution, approval-chain lookup, org-model Collections and the settings editors all read the new column and the fixed vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the display and filter surfaces</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/app/(protected)/sops/page.tsx, src/components/sop/SopLibraryCard.tsx, src/components/sop/tabs/ReadTab.tsx, src/components/admin/governance/GovernanceQueueRow.tsx, src/hooks/useAssignedSops.ts, src/app/api/voice/query/route.ts, tests/phase40/dat01-category-column.spec.ts</files>
  <read_first>
    - src/lib/sop-categories.ts (`categoryLabel`, `SOP_CATEGORIES`)
    - src/app/(protected)/admin/sops/page.tsx (line ~111, the `SOP_SELECT` constant; and lines ~140-150 where the collections column is built from category)
    - src/app/(protected)/sops/page.tsx (line ~447 the row type, line ~516 the `[sop.category, sop.department]` meta line)
    - src/components/sop/SopLibraryCard.tsx (line ~38, same meta pattern)
    - src/components/sop/tabs/ReadTab.tsx (line ~119, `<MetaRow label="Category" value={sop.category} />`)
    - src/components/admin/governance/GovernanceQueueRow.tsx (line ~68)
    - src/hooks/useAssignedSops.ts (lines 8, 17, 23-35 — the search term match and the `category` equality filter)
    - src/app/api/voice/query/route.ts (line ~117, the select list containing `category_tag`)
    - src/lib/offline/db.ts (lines 92-160 — the Dexie schema; note that `sops` is synced with `select('*')` so `category_slug` arrives automatically, and the existing `category` index is never queried. Do NOT bump the Dexie version: adding a v6 purely to rename an unused index is churn with an upgrade-path risk for installed PWA clients)
  </read_first>
  <action>
Every display site renders `categoryLabel(sop.category_slug)` instead of the raw `sop.category` string, and every filter site compares against `category_slug`.

- `src/app/(protected)/admin/sops/page.tsx`: in `SOP_SELECT`, replace `category` with `category_slug`. Any downstream use of that field renders through `categoryLabel`.
- `src/app/(protected)/sops/page.tsx`: change the row type field to `category_slug: string | null` and the meta line to `[categoryLabel(sop.category_slug), sop.department].filter(Boolean).join(' · ')`.
- `src/components/sop/SopLibraryCard.tsx`: same meta-line change.
- `src/components/sop/tabs/ReadTab.tsx`: `<MetaRow label="Category" value={categoryLabel(sop.category_slug)} />`.
- `src/components/admin/governance/GovernanceQueueRow.tsx`: render `categoryLabel(row.category_slug)`.
- `src/hooks/useAssignedSops.ts`: change the optional `category` field on its options type to `category_slug`, filter with `sop.category_slug === category_slug`, and make the free-text search term match the *label* (`categoryLabel(sop.category_slug)?.toLowerCase().includes(term)`) so a worker typing "safety" still finds Safety SOPs.
- `src/app/api/voice/query/route.ts`: replace `category_tag` in the select list at line ~117 with `category_slug`, and check whether the voice SOP-pack builder downstream reads that field — if it renders it to the model, render the label.

Do not modify `src/lib/offline/db.ts`. The Dexie `sops` index string still lists `category`; that index is not used by any query (`useAssignedSops` filters in JS over the fetched array) and `sync-engine.ts` uses `select('*')`, so `category_slug` reaches IndexedDB with no schema change. A v6 bump would force a schema upgrade on every installed PWA client to rename an index nothing reads.

Then un-fixme the remaining assertions in `tests/phase40/dat01-category-column.spec.ts`:
- `src/lib/org-model/sop-collections.ts` and `src/actions/governance.ts` select `category_slug`, not `category`
- zero occurrences of `category_tag` as a `sops` column anywhere under `src/` outside the deprecated type declaration (exclude the `blocks.category_tags` array column by requiring the match not be followed by `s`)
- add one assertion pinning the three category-keyed settings tables: `src/app/api/sops/[sopId]/publish/route.ts` still queries `approval_chains` and `src/actions/governance.ts` still queries `sop_review_cadences`, both with an `organisation_id` filter present — proving the repoint did not drop the org scope (CLAUDE.md [2026-07-28]: an assertion must pin every security-relevant clause of the object it verifies)
  </action>
  <verify>
    <automated>npx playwright test --project=phase40 --reporter=line && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "sop\.category\b\|row\.category\b\|\.select('category'\|, category," src/ --include=*.ts --include=*.tsx` returns zero matches referring to the `sops` table (matches on `sop_review_cadences.category`, `approval_chains.category`, `blocks.category` and the UAT `tests.ts` `category` field are unrelated and permitted)
    - All seven files listed in `<files>` contain `category_slug`
    - `src/lib/offline/db.ts` is unmodified (`git diff --stat` shows no entry for it)
    - `src/hooks/useAssignedSops.ts` search matches on `categoryLabel(...)`, not on a raw slug
    - `npx playwright test --project=phase40` — 0 failures, `dat01-category-column.spec.ts` fully live (no remaining `test.fixme` in that file)
    - `npx tsc --noEmit` clean; `npm run build` completes without error
  </acceptance_criteria>
  <done>Every reader and every filter is on the new column; SOPs from all on-ramps are filterable together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-role admin client -> `sops` / `collections` | `ensureSopCollectionsForOrg` runs with RLS bypassed; its own `organisation_id` guard is the only tenant boundary |
| Admin browser -> cadence/approval-chain settings write | Org-scoped settings writes keyed by a category value |
| Worker session -> SOP list read | RLS-scoped; the category filter is presentation, not authorisation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05-01 | Elevation of Privilege | `ensureSopCollectionsForOrg` org guard | mitigate | The `sopRow.organisation_id !== orgId` check is preserved verbatim and pinned as an acceptance criterion. CLAUDE.md [2026-07-28]: an org predicate must derive from the session org, never from the fetched row alone — this function already compares the fetched row against the caller-supplied `orgId` and that comparison must survive the column repoint untouched |
| T-40-05-02 | Repudiation | per-category approval chain silently ceasing to divert publishes | mitigate | `publish/route.ts` is repointed in the same phase as the data remap (plan 40-06), and the spec pins that the `approval_chains` query still carries `.eq('organisation_id', organisationId)` |
| T-40-05-03 | Tampering | frozen publish gate collateral damage | mitigate | `tests/phase40/spine-freeze.spec.ts` is live from wave 1; acceptance criterion requires `git diff` on `publish-core.ts` to touch only the cadence block |
| T-40-05-04 | Tampering | free-text cadence key minted after the migration | mitigate | The cadence-write action gates its `category` parameter with `isValidCategorySlug` and rejects anything else, so the settings table cannot re-diverge from the vocabulary |
| T-40-05-05 | Information Disclosure | governance queue / settings page leaking cross-org category values | accept | Both surfaces already read through org-scoped queries unchanged by this plan; no new cross-org read path is introduced |
| T-40-05-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; RESEARCH.md records the phase as install-free |
</threat_model>

<verification>
- `npx playwright test --project=phase40` — 0 failures, `spine-freeze` still green
- `npx tsc --noEmit` clean
- `npm run build` clean
- `git diff src/lib/governance/publish-core.ts` — cadence lines only
- `git diff --stat src/lib/offline/db.ts` — empty
</verification>

<success_criteria>
- All thirteen readers are on `category_slug` (DAT-01, SC-5 "no code path writes the retired column" and its read-side twin)
- Both hidden settings consumers (`sop_review_cadences`, `approval_chains`) and the org-model Collections path are explicitly resolved, not silently orphaned (RESEARCH Pitfall 1)
- RESEARCH open questions 1 and 2 are answered in code with the rationale recorded in comments
- The frozen publish spine is untouched
</success_criteria>

<artifacts_this_phase_produces>
Modified-only plan — no new files. New/changed symbols:
- `resolveCadenceMonths` first parameter renamed to `categorySlug` (signature shape unchanged)
- governance row type field `category` renamed to `category_slug`
- `useAssignedSops` options field `category` renamed to `category_slug`
- `src/app/(protected)/admin/sops/page.tsx` `SOP_SELECT` now lists `category_slug`

Referenced from plan 40-04 (already created there, not new here): `src/lib/sop-categories.ts`, `categoryLabel`, `isValidCategorySlug`, `SOP_CATEGORIES`, `sops.category_slug`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/40-shared-creation-foundation/40-05-SUMMARY.md` when done
</output>
