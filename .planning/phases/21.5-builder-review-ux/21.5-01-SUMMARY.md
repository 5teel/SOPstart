---
phase: 21.5-builder-review-ux
plan: "01"
subsystem: admin-builder
tags: [humanized-labels, verify-checklist, block-type-labels, tdd]
dependency_graph:
  requires: []
  provides: [BLOCK_TYPE_LABELS, humanizeBlockType, humanized-verify-row]
  affects: [BlockChecklistRow, ReviewStation-NavRow-plan-04]
tech_stack:
  added: []
  patterns: [pure-ts-label-map, tdd-red-green]
key_files:
  created:
    - src/lib/builder/block-type-labels.ts
    - src/lib/builder/__tests__/block-type-labels.test.ts
  modified:
    - src/components/admin/verify-checklist/BlockChecklistRow.tsx
    - playwright.config.ts
decisions:
  - "Added phase21.5-unit playwright project pointing at src/lib/builder/__tests__/ — required for test discovery (Rule 3 auto-fix)"
  - "SomeRandomBlock test assertion: not.toMatch(/Block$/) contradicts .toBe('Block') — corrected to not.toBe('SomeRandomBlock') which correctly captures the R4 invariant (no echo of raw input)"
  - "Secondary a/d keyboard hints preserved as <kbd> superscript inside word-labelled buttons per SPEC R5 (hints may remain as secondary only)"
metrics:
  duration: "~25 minutes"
  completed: "2026-06-02"
  tasks_completed: 2
  files_created: 2
  files_modified: 2
requirements: [R4, R5]
---

# Phase 21.5 Plan 01: Humanized Labels + Word-Labelled Verify Actions Summary

**One-liner:** `BLOCK_TYPE_LABELS` const map + `humanizeBlockType()` lookup (14 entries, PhotoGrid alias, case-insensitive slug resolution) consumed by restyled `BlockChecklistRow` showing "Verify step" / "Send back to edit" word-labelled actions in place of single-letter `a`/`d`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create BLOCK_TYPE_LABELS + humanizeBlockType | 9859c52 | src/lib/builder/block-type-labels.ts, src/lib/builder/__tests__/block-type-labels.test.ts |
| 1 RED | TDD failing test + playwright project registration | 66f9c25 | src/lib/builder/__tests__/block-type-labels.test.ts, playwright.config.ts |
| 2 | Restyle BlockChecklistRow | c2cc615 | src/components/admin/verify-checklist/BlockChecklistRow.tsx |

## Verification Results

- `npx playwright test --project=phase21.5-unit` — 27/27 passed
- `npx playwright test --project=phase15-stubs no-bulk-verify-ui` — 1/1 passed (D-21-07 lock green)
- `npx eslint src/lib/builder/block-type-labels.ts src/lib/builder/__tests__/block-type-labels.test.ts src/components/admin/verify-checklist/BlockChecklistRow.tsx` — 0 errors, 0 warnings

## Success Criteria

- [x] BLOCK_TYPE_LABELS map + humanizeBlockType exist and are unit-tested (27 tests)
- [x] BlockChecklistRow shows humanizeBlockType(block.type) — no raw symbol renders
- [x] row-approve button visible text: "Verify step"; row-decline: "Send back to edit"
- [x] All four data-testid values unchanged (block-checklist-row, row-approve, row-decline, flag-badge)
- [x] no-bulk-verify-ui lint guard stays green
- [x] Plan 04 (ReviewStation NavRow) can import BLOCK_TYPE_LABELS from @/lib/builder/block-type-labels

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Playwright project missing for src/lib/builder/__tests__/**
- **Found during:** Task 1 (TDD RED phase — `npx playwright test src/lib/builder/__tests__/block-type-labels.test.ts` returned "No tests found")
- **Issue:** The global `playwright.config.ts` had `testDir: './tests'`; no project pointed at `src/lib/builder/__tests__/`. Tests existed but Playwright never discovered them.
- **Fix:** Added `phase21.5-unit` project with `testDir: './src/lib/builder/__tests__'` and `testMatch: /.*\.test\.ts$/` to `playwright.config.ts`.
- **Files modified:** playwright.config.ts
- **Commit:** 66f9c25 (included in RED commit)

**2. [Rule 1 - Bug] Contradictory test assertion on SomeRandomBlock fallback**
- **Found during:** Task 1 GREEN phase (26/27 passed on first run)
- **Issue:** Test asserted both `not.toMatch(/Block$/)` AND `.toBe('Block')` — these are contradictory because the safe fallback `'Block'` itself ends in `'Block'`. The spec's R4 invariant is "don't echo the raw input", not "the fallback can't be the word Block".
- **Fix:** Changed assertion to `not.toBe('SomeRandomBlock')` (no echo of raw input) + `.toBe('Block')`.
- **Files modified:** src/lib/builder/__tests__/block-type-labels.test.ts
- **Commit:** 9859c52

## TDD Gate Compliance

- RED gate: commit `66f9c25` — `test(21.5-01): add failing tests for BLOCK_TYPE_LABELS + humanizeBlockType`
- GREEN gate: commit `9859c52` — `feat(21.5-01): implement BLOCK_TYPE_LABELS + humanizeBlockType lookup`

Both gates present. REFACTOR not needed — implementation was clean on first pass.

## Known Stubs

None — both files are complete implementations with no hardcoded placeholders.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Pure label-map utility (admin-only by convention) + presentation restyle of existing component.

## Self-Check: PASSED
