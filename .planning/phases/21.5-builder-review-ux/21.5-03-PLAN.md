---
phase: 21.5-builder-review-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
autonomous: true
requirements: [R6, R7]
must_haves:
  truths:
    - "A dedicated Publish stage surface exists with the heading 'Publish this SOP' and subtext, and is the only place the publish callback is triggered"
    - "When publish is blocked, an always-visible amber strip states 'N of M steps left to verify before you can publish' (never hover-only)"
    - "The publish button is disabled (cursor-not-allowed) until the verify gate is satisfied, then becomes active and calls the supplied publish callback"
    - "Source-less SOPs see the Publish stage with no verify-count progress summary (heading + subtext + button only)"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx"
      provides: "Dedicated Publish stage with inline blocked-reason strip, gated publish button, error banner, and Back-to-Review link"
      exports: ["PublishStage"]
      contains: "PublishStage"
  key_links:
    - from: "PublishStage"
      to: "onPublish callback prop"
      via: "button onClick → onPublish (the existing handlePublish chain)"
      pattern: "onPublish"
---

<objective>
Build the dedicated Publish stage (SPEC R7) as a controlled presentational
component: heading + subtext + optional verify progress summary + an
always-visible inline publish-gate reason (SPEC R6) + a gated "Publish SOP"
button that calls the existing publish callback + the existing publish-error
banner + a "Back to Review" link. The publish data path (`handlePublish` →
`POST /api/sops/[sopId]/publish`) is supplied as a prop by Plan 05 and is NOT
reimplemented here (Req 10 callback-chain preservation).

Purpose: Moves publish out of the buried far-right `VerifyProgressIndicator`
into a named, reachable stage with the gate reason on screen — closing the
"hidden publish gate" and "hover-only reason" UAT failures.

Output: `PublishStage.tsx` (new, controlled/presentational).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.5-builder-review-ux/21.5-SPEC.md
@.planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md
@src/components/admin/verify-checklist/VerifyProgressIndicator.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PublishStage — gated publish surface + inline blocked reason + error banner</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Layout: Publish Stage" full anatomy 1-6 + § "Publish Gate Inline Reason" Publish-stage amber strip + § "Copywriting Contract" → "Publish Gate Blocked Reason" / "Primary CTA" / "Error States" / "Adaptive Stepper" source-less note + § "Safety Gate Invariants" publish-button contract)
    - src/components/admin/verify-checklist/VerifyProgressIndicator.tsx (current publish button: `disabled={!isReady}`, `data-testid="publish-button"`, label "Publish SOP" — preserve this contract on the new stage button)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx (lines 129-161 handlePublish + lines 196-212 publish-error-banner — the new stage reuses the SAME callback + the SAME `data-testid="publish-error-banner"` markup)
  </read_first>
  <action>
    Create `PublishStage.tsx` ('use client', presentational). Props:
    `verifiedCount: number`, `totalCount: number`, `isReady: boolean`,
    `hasSourceDoc: boolean` (controls whether the verify progress summary
    shows), `publishing: boolean`, `publishError: string | null`,
    `onPublish: () => void`, `onDismissError: () => void`,
    `onBackToReview: () => void`. Layout per UI-SPEC § "Layout: Publish Stage":
    centered column, max-width 640px, margin auto, padding 48px 24px. Render
    in order: (1) heading "Publish this SOP" (Inter 17px weight 700,
    `--ink-900`); (2) subtext "Once published, workers assigned to this SOP
    will see it in their library." (Inter 14px `--ink-500`); (3) progress
    summary — ONLY when `hasSourceDoc` is true: a green check + `{verifiedCount}
    steps verified` in `--accent-ok` mono 12px uppercase (omit entirely for
    source-less SOPs per the Adaptive Stepper note); (4) the inline publish
    gate status, ALWAYS rendered when `!isReady`: an amber strip (`border 1px
    solid #fdba74; background #fff7ed; radius 4px; padding 8px 16px`) with mono
    text `{totalCount - verifiedCount} of {totalCount} steps left to verify
    before you can publish` in `#c2410c`, carrying
    `data-testid="publish-blocked-reason"`; when `isReady` the strip is removed
    from the DOM; (5) the publish button — keep `data-testid="publish-button"`,
    `disabled={!isReady || publishing}`, label "Publish SOP", onClick calls
    `onPublish`; blocked style `background --steel-700; color --ink-500;
    cursor-not-allowed`; ready style `background --brand-yellow; color
    --ink-900; weight 700`; (6) a "← Back to Review" text link
    (`--ink-500` 12px mono) calling `onBackToReview` — render this link only
    when `hasSourceDoc` (source-less SOPs have no Review stage to return to).
    Reuse the EXISTING publish-error-banner markup from
    BuilderWithSourceViewer (same `data-testid="publish-error-banner"`,
    role="alert", click-to-dismiss → `onDismissError`) below the button when
    `publishError` is non-null. Root carries `data-testid="publish-stage"`.
    Do NOT call fetch / the publish API directly — only invoke `onPublish`.
    Do NOT introduce any bulk-verify / approve-all wording.
  </action>
  <verify>
    <automated>npx playwright test --project=phase15-stubs no-bulk-verify-ui --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `PublishStage` calls only the `onPublish` prop — no `fetch` / Supabase / publish-route import in the file
    - The publish button keeps `data-testid="publish-button"`, `disabled` when `!isReady` (or publishing), label "Publish SOP"
    - `data-testid="publish-blocked-reason"` strip is present with the exact N-of-M blocked copy when `!isReady`, ABSENT when `isReady`
    - For `hasSourceDoc=false` the verify progress summary and Back-to-Review link are NOT rendered
    - The publish-error-banner markup matches the existing `data-testid="publish-error-banner"` contract
    - `tests/lint/no-bulk-verify-ui.spec.ts` stays green
  </acceptance_criteria>
  <done>Publish stage renders gated button + always-visible blocked reason + error banner; presentational; no API call inside.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publish stage UI → /api/sops/[sopId]/publish | The stage button only invokes the prop callback (the existing handlePublish). The server 400 `unverified_blocks` gate stays authoritative; UI `disabled` is advisory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-03-01 | Tampering / EoP | Publish gate bypass during UI move | mitigate | Button keeps `disabled={!isReady}` AND publish goes through the unchanged `onPublish`/`handlePublish` → unchanged `/publish` route which still returns 400 `unverified_blocks` (Req 10). No server logic edited in this plan. |
| T-21.5-03-02 | Tampering | Bulk-verify affordance introduction | mitigate | No verify controls on the Publish stage at all; `no-bulk-verify-ui.spec.ts` runs in verify step (D-21-07) |
| T-21.5-03-SC | Tampering | npm installs | mitigate | No new packages; nothing to audit |
</threat_model>

<verification>
- `npx playwright test --project=phase15-stubs no-bulk-verify-ui` stays green
- `npm run lint` clean on PublishStage.tsx
- Grep PublishStage.tsx: no `fetch(`, no `@/lib/supabase`, no `/publish` literal — only `onPublish` callback usage
- `publish-blocked-reason` present iff `!isReady`; `publish-button` disabled iff `!isReady`
</verification>

<success_criteria>
- A dedicated, reachable Publish stage is the sole publish trigger surface
- Publish gate reason is always on screen when blocked (no hover dependency)
- Publish button stays gated on `isReady`; callback chain preserved for Req 10
- Source-less SOPs get a clean heading+subtext+button publish surface
</success_criteria>

<artifacts_produced>
- `src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx` — NEW. Exports `PublishStage` (gated publish surface + inline blocked reason + error banner + back-to-review link).
</artifacts_produced>

<output>
Create `.planning/phases/21.5-builder-review-ux/21.5-03-SUMMARY.md` when done.
</output>
