---
phase: 21.5-builder-review-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx
autonomous: true
requirements: [R1, R6, R8, R9]
must_haves:
  truths:
    - "A Build → Review → Publish stepper renders the three stages and visibly marks which one is active (active / done / todo states)"
    - "For a source-less SOP the stepper renders only Build → Publish (2 chips, Review omitted)"
    - "When publish is blocked, a top-bar inline reason 'N of M steps left to verify' is visible without hovering; when ready it is removed from the DOM"
    - "Below the top bar during Review, an orientation strip states the stage purpose and a verified/total progress counter"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx"
      provides: "Stage stepper chip row (adaptive 2/3 stage) + inline publish-gate reason span, rendered in the dark top bar"
      exports: ["BuilderStageStepper"]
      contains: "BuilderStageStepper"
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx"
      provides: "Review-stage purpose strip with stage badge, prose, and verified/total progress bar"
      exports: ["OrientationStrip"]
      contains: "OrientationStrip"
  key_links:
    - from: "BuilderStageStepper"
      to: "activeStage / hasSourceDoc / isReady props"
      via: "controlled presentational props (no internal data fetching)"
      pattern: "activeStage"
---

<objective>
Build the two presentational chrome components of the new shell: the
`BuilderStageStepper` (SPEC R1 stage sequence; R8 adaptive 2-vs-3 stage; R6
top-bar inline publish reason; R9 number-only chips at tablet width) and the
`OrientationStrip` (SPEC R2 pane-purpose microcopy + progress counter). Both
are pure controlled components driven entirely by props supplied later by the
shell (Plan 05) — they fetch no data themselves.

Purpose: Gives the builder an explicit, always-visible sense of order and the
publish reason on screen (not hover-only), the two top orientation failures
from Phase 21 UAT.

Output: `BuilderStageStepper.tsx` + `OrientationStrip.tsx` (both new, both
controlled/presentational).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.5-builder-review-ux/21.5-SPEC.md
@.planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuilderStageStepper — adaptive chips + inline publish reason</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Component: BuilderStageStepper" chip anatomy + § "Stepper State Colors" table + § "Adaptive Stepper for Source-less SOPs" + § "Publish Gate Inline Reason" top-bar span + § "Responsive Behaviour" tablet number-only chips + § "Color" tokens)
    - .claude/skills/sketch-findings-SOPstart/SKILL.md (paper/ink tokens — load references/design-tokens.md for the exact CSS variable values; chips live on the `--steel-900` dark top bar)
  </read_first>
  <action>
    Create `BuilderStageStepper.tsx` ('use client'). Props:
    `activeStage: 'build' | 'review' | 'publish'`,
    `hasSourceDoc: boolean`,
    `isReady: boolean`, `verifiedCount: number`, `totalCount: number`,
    `onStageSelect: (stage) => void`.
    Render the chip row inside the dark top bar (caller positions it). When
    `hasSourceDoc` build the 3-chip sequence Build → Review & verify → Publish;
    when false build the 2-chip sequence Build → Publish (omit Review chip) per
    the UI-SPEC adaptive table. Chip anatomy from the UI-SPEC: 16×16px number
    dot, chip padding 4px 12px, gap 8px, border-radius 2px, `→` arrow glyph
    (#3f3f46, 10px) between chips. Apply the three chip states from the Stepper
    State Colors table: done (transparent bg, `--accent-ok` text, filled green
    dot with `✓`), active (white bg, `--ink-900` text, filled `--accent-step`
    dot with white number), todo (transparent, `--ink-500` text, empty dot
    `border 1px solid #52525b`). Derive per-chip state from `activeStage` +
    ordinal position (chips before active = done, chip at active = active,
    after = todo). Make chips clickable via `onStageSelect`, but disable
    (no onClick + aria-disabled) the Publish chip when `!isReady`, disable the
    Review chip when `!hasSourceDoc`; Build chip is always clickable (per
    UI-SPEC keyboard/click rules). Render the inline publish-gate reason span
    BELOW the publish chip per § "Publish Gate Inline Reason": when
    `!isReady`, render `🔒 {totalCount - verifiedCount} of {totalCount} steps
    left to verify` as a JetBrains-Mono 10px `#a1a1aa` span with a stable
    `data-testid="stepper-publish-reason"`; when `isReady`, render NOTHING for
    that span (DOM removed, not hidden). Add responsive behaviour: at
    768–1023px (use a CSS media query or container query class, not JS
    breakpoint detection) abbreviate chips to number-circle only with the full
    stage name as a `title` tooltip; full text labels restore at ≥1024px.
    Add `data-testid="builder-stage-stepper"` on the root and
    `data-active-stage={activeStage}` for downstream tests. NO data fetching —
    fully controlled by props.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 src/app/\(protected\)/admin/sops/builder/\[sopId\]/BuilderStageStepper.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `BuilderStageStepper` is a controlled component (no useQuery / fetch / Supabase import)
    - With `hasSourceDoc=true` it renders 3 chips; with `hasSourceDoc=false` it renders 2 chips and no Review chip
    - Active stage chip carries `data-active-stage` and active styling; chips before it read done, after it read todo
    - `data-testid="stepper-publish-reason"` span is present with the N-of-M text when `!isReady` and ABSENT from the DOM when `isReady`
    - Publish chip is non-interactive when `!isReady`; Review chip non-interactive when `!hasSourceDoc`
  </acceptance_criteria>
  <done>Adaptive stepper renders correct chips + states + inline publish reason; presentational only.</done>
</task>

<task type="auto">
  <name>Task 2: OrientationStrip — Review-stage purpose + progress counter</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Component: OrientationStrip" layout + § "Copywriting Contract" → "Orientation Strip Prose (by stage)" exact Review prose + "Stage Labels" stage badge text + § "Color"/"Spacing"/"Typography" tokens)
    - .claude/skills/sketch-findings-SOPstart/SKILL.md (token values for badge / progress bar)
  </read_first>
  <action>
    Create `OrientationStrip.tsx` ('use client', presentational). Props:
    `stageBadge: string` (e.g. "Step 2 of 3"), `verifiedCount: number`,
    `totalCount: number`. Render a full-width white strip with `border-bottom:
    1px solid --ink-300`, flex row align-center gap 8px, padding 8px 16px per
    UI-SPEC § OrientationStrip. Left: a blue filled badge (background
    `--accent-step`, white text, 10px/weight 600/radius 2px) showing
    `stageBadge`. Centre: the EXACT Review prose string from the Copywriting
    Contract: "Check each step against the original document, then mark it
    verified. The AI safety check flags anything that looks off." (Inter 12px
    `--ink-700`). Right: progress counter `{verifiedCount} / {totalCount}
    verified` (JetBrains Mono 12px `--ink-500`) followed by a horizontal
    progress bar 120px×4px (background `--ink-200`, fill `--accent-step`, width
    `= verifiedCount/totalCount`, border-radius 3px). Root carries
    `data-testid="orientation-strip"`. The component renders ONLY the Review
    stage's content; the shell (Plan 05) controls whether it mounts at all
    (hidden for Build + Publish), so this component does not need stage-gating
    logic itself.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 src/app/\(protected\)/admin/sops/builder/\[sopId\]/OrientationStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `OrientationStrip` is presentational (no data fetching)
    - Renders the exact Review prose string from the Copywriting Contract verbatim
    - Renders the stage badge text from props and a `{verified} / {total} verified` counter + a proportional progress bar
    - Root has `data-testid="orientation-strip"`
  </acceptance_criteria>
  <done>Orientation strip renders stage badge + exact prose + progress counter; presentational only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stepper UI → publish action | Stepper only signals stage intent via onStageSelect; it never calls the publish API. Disabling the Publish chip is advisory; the server 400 gate (Req 10) remains authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-02-01 | Spoofing | Publish-chip clickability | mitigate | Disabling the Publish chip on `!isReady` is UI advisory only; Plan 05 wires the actual publish trigger to PublishStage, and the server `/publish` 400 gate is untouched (Req 10 defence-in-depth) |
| T-21.5-02-02 | Information disclosure | Inline reason text | accept | Reason text exposes only verified/total counts already visible to the admin; no sensitive data |
| T-21.5-02-SC | Tampering | npm installs | mitigate | No new packages; nothing to audit |
</threat_model>

<verification>
- `npm run lint` clean on both new files
- BuilderStageStepper: rendered chip count is 3 when hasSourceDoc, 2 otherwise; `stepper-publish-reason` present iff `!isReady`
- OrientationStrip: Review prose string matches the Copywriting Contract verbatim
- Neither component imports `@tanstack/react-query`, `@/lib/supabase/*`, or any data hook
</verification>

<success_criteria>
- Adaptive Build→Review→Publish (or Build→Publish) stepper renders with correct active/done/todo marking
- Inline publish reason visible without hover and removed from DOM when ready
- Orientation strip surfaces stage purpose + progress
- Both components are controlled/presentational, ready for Plan 05 wiring
</success_criteria>

<artifacts_produced>
- `src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx` — NEW. Exports `BuilderStageStepper` (adaptive 2/3-chip stepper + inline publish reason).
- `src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx` — NEW. Exports `OrientationStrip` (Review-stage purpose + progress).
</artifacts_produced>

<output>
Create `.planning/phases/21.5-builder-review-ux/21.5-02-SUMMARY.md` when done.
</output>
