---
phase: 21.5-builder-review-ux
plan: 05
type: execute
wave: 3
depends_on: ["21.5-01", "21.5-02", "21.5-03", "21.5-04"]
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/page.tsx
  - playwright.config.ts
  - tests/builder/builder-review-flow.spec.ts
autonomous: true
requirements: [R1, R8, R10]
must_haves:
  truths:
    - "Opening the builder renders the new stage shell: top bar + stepper, with Build active by default and the Build (Puck) editor inside the Build stage"
    - "Selecting Review shows the ReviewStation + OrientationStrip; selecting Publish shows the PublishStage; the stepper marks the active stage in all states"
    - "An AI-prompt / source-less SOP renders a Build → Publish stepper with no Review stage and no source pane and still publishes"
    - "The publish trigger fires only from the PublishStage and still routes through the unchanged handlePublish → POST /publish (which returns HTTP 400 unverified_blocks on direct unverified POST)"
    - "tests/lint/no-bulk-verify-ui.spec.ts stays green and the new builder-review-flow spec is registered in a playwright project"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx"
      provides: "Outermost builder shell: top bar + stepper + adaptive stage switch (Build/Review/Publish), owns activeStage + publish handler + selection-sync provider"
      exports: ["BuilderStageShell"]
      min_lines: 80
      contains: "BuilderStageShell"
    - path: "tests/builder/builder-review-flow.spec.ts"
      provides: "Source-contract assertions for stepper presence, single-step review, publish-only-on-publish-stage, adaptive 2-stage, safety-gate preservation"
      contains: "builder-review-flow"
  key_links:
    - from: "src/app/(protected)/admin/sops/builder/[sopId]/page.tsx"
      to: "BuilderStageShell"
      via: "render BuilderStageShell instead of BuilderWithSourceViewer"
      pattern: "BuilderStageShell"
    - from: "BuilderStageShell"
      to: "BuilderStageStepper / OrientationStrip / ReviewStation / PublishStage / BuilderClient"
      via: "stage-switched composition + shared selection-sync provider"
      pattern: "ReviewStation|PublishStage|BuilderStageStepper"
    - from: "BuilderStageShell handlePublish"
      to: "/api/sops/[sopId]/publish"
      via: "preserved Phase-21 publish callback chain (unchanged route)"
      pattern: "/publish"
---

<objective>
Compose the new builder shell (SPEC R1) that wraps and stage-switches the
Wave-1/2 pieces: the dark top bar hosting `BuilderStageStepper`, the
`OrientationStrip` (Review only), and a stage content area that renders the
existing `BuilderClient` (Build), `ReviewStation` (Review), or `PublishStage`
(Publish). Carry the adaptive 2-vs-3-stage logic (SPEC R8) by reusing the
EXACT `showPane`/`showVerifyGate`/`hasSourceDoc` derivation already in
`BuilderWithSourceViewer.tsx`. Own `activeStage` state and the PRESERVED
`handlePublish` → `POST /api/sops/[sopId]/publish` callback chain (SPEC R10).
Point `page.tsx` at the new shell, register the verification spec, and prove
the safety invariants still hold.

Purpose: The integration step that turns the redesign into the live builder
surface while guaranteeing the publish gate (UI + server 400) and the
no-bulk-verify lock are intact.

Output: `BuilderStageShell.tsx` (new), updated `page.tsx`, registered
`builder-review-flow.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.5-builder-review-ux/21.5-SPEC.md
@.planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/page.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuilderStageShell — top bar + stepper + adaptive stage switch + preserved publish chain</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Shell: BuilderStageShell" props + adaptive modes + activeStage state + § "Component File Map" reuse list + § "Safety Gate Invariants" publish-button contract + onPublish/handlePublish preservation)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx (THE SOURCE OF TRUTH to migrate: deriveSourcePaneKind, showPane (line 121), showVerifyGate (line 127), isAiPrompt (line 116), hasSourceDoc = showPane, the handlePublish callback (lines 132-161), publishing/publishError state, transcriptSegments derivation (lines 164-185), the SourceViewerSelectionProvider wrapper (line 188). The shell REPLACES this file's role but MUST preserve handlePublish verbatim and the provider wrapper.)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx (Plan 02 — props: activeStage, hasSourceDoc, isReady, verifiedCount, totalCount, onStageSelect)
    - src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx (Plan 02 — props: stageBadge, verifiedCount, totalCount)
    - src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx (Plan 04 — props: sopId, sourceType, transcriptSegments)
    - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx (Plan 03 — props: verifiedCount, totalCount, isReady, hasSourceDoc, publishing, publishError, onPublish, onDismissError, onBackToReview)
    - src/components/admin/verify-checklist/useVerifyChecklist.ts (the shell needs isReady/verifiedCount/totalCount for the stepper + publish gate — call useVerifyChecklist(sopId) at the shell level so the stepper, ReviewStation, and PublishStage share one source of gate truth; gate is bypassed/forced-ready when !showVerifyGate per the source-less carve-out)
  </read_first>
  <action>
    Create `BuilderStageShell.tsx` ('use client') with the same props as
    `BuilderWithSourceViewerProps` (`sopId`, `initialSop`, `parseJob`). Migrate
    VERBATIM from BuilderWithSourceViewer: `deriveSourcePaneKind`, the
    `rawType`/`sourceType`/`sourceFilePath`/`isAiPrompt` derivation, `showPane`,
    `showVerifyGate`, the `transcriptSegments` extraction, and the entire
    `handlePublish` callback (POST `/api/sops/${sopId}/publish`, the
    `unverified_blocks` error mapping, `router.refresh()` on success,
    publishing/publishError state). Set `hasSourceDoc = showPane`. Add
    `const [activeStage, setActiveStage] = useState<'build'|'review'|'publish'>('build')`.
    Call `useVerifyChecklist(sopId)` at this level to get `isReady`,
    `verifiedCount`, `totalCount`; when `!showVerifyGate` (source-less /
    AI-prompt) treat the gate as satisfied (`isReady = true`, counts 0) so the
    adaptive flow publishes without a Review stage. Wrap everything in
    `<SourceViewerSelectionProvider>` (same as today). Layout: a dark
    `--steel-900` top bar (height 48px) containing the SOP title + version
    subtitle (from initialSop) on the left and `<BuilderStageStepper
    activeStage hasSourceDoc isReady verifiedCount totalCount
    onStageSelect={setActiveStage} />` right-of-center. Guard `onStageSelect`:
    ignore `'review'` when `!hasSourceDoc`, ignore `'publish'` when `!isReady`.
    Below the top bar, render the stage content by `activeStage`:
      - `'build'` → `<BuilderClient sopId initialSop />` (REUSED AS-IS, full
        width; no OrientationStrip in Build).
      - `'review'` (only reachable when hasSourceDoc) → `<OrientationStrip
        stageBadge="Step 2 of 3" verifiedCount totalCount />` then
        `<ReviewStation sopId sourceType={sourceType} transcriptSegments=
        {transcriptSegments} />`.
      - `'publish'` → `<PublishStage verifiedCount totalCount isReady
        hasSourceDoc={hasSourceDoc} publishing={publishing}
        publishError={publishError} onPublish={handlePublish}
        onDismissError={() => setPublishError(null)} onBackToReview={() =>
        setActiveStage('review')} />`.
    The PublishStage is the SOLE publish trigger: do NOT mount
    VerifyChecklistGate's publish button or VerifyProgressIndicator anywhere in
    the shell (Req 7). Keep `data-testid="builder-with-source-viewer"` on the
    root OR add `data-testid="builder-stage-shell"` AND keep a
    `data-publishing` attribute (downstream UAT/tests may key on it). Drive
    `activeStage` from useState only — never `router.push` for stage changes
    (CLAUDE.md 2026-05-13). Note in a top-of-file comment that this file
    supersedes BuilderWithSourceViewer.tsx as the shell.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 src/app/\(protected\)/admin/sops/builder/\[sopId\]/BuilderStageShell.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `handlePublish` in the shell is byte-equivalent in behaviour to the BuilderWithSourceViewer original (POST `/api/sops/[sopId]/publish`, maps `unverified_blocks`, `router.refresh()` on ok) — no publish API logic rewritten
    - `hasSourceDoc`/`showPane`/`showVerifyGate`/`isAiPrompt` derivation is preserved from the original
    - Build renders BuilderClient; Review renders OrientationStrip + ReviewStation (only when hasSourceDoc); Publish renders PublishStage; publish triggers ONLY from PublishStage
    - For `!hasSourceDoc`, the shell never renders ReviewStation or the source pane and treats the gate as ready
    - Shell is wrapped in `SourceViewerSelectionProvider`; stage state is useState (no `router.push` for stage)
  </acceptance_criteria>
  <done>Shell composes stepper + adaptive stages + preserved publish chain; publish only on PublishStage.</done>
</task>

<task type="auto">
  <name>Task 2: Point page.tsx at the shell + register the verification spec + builder-review-flow source-contract tests</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/page.tsx, playwright.config.ts, tests/builder/builder-review-flow.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/page.tsx (lines 7 + 73-81: swap the import + the rendered component from BuilderWithSourceViewer to BuilderStageShell; props unchanged)
    - playwright.config.ts (the projects array — add a `phase21.5-stubs` project whose testMatch regex includes `block-type-labels` (Plan 01 unit test) AND `builder-review-flow`; CLAUDE.md 2026-05-25: a lint/spec file not in any project regex NEVER runs)
    - src/app/api/sops/[sopId]/publish/route.ts (the server 400 `unverified_blocks` gate — the spec asserts this route file still contains the unverified_blocks 400 branch, a source-contract proxy for the live 400 since chromium UAT is deferred)
    - tests/lint/no-bulk-verify-ui.spec.ts (the lock — the spec confirms it is still registered + green)
    - .planning/phases/21.5-builder-review-ux/21.5-SPEC.md (§ "Acceptance Criteria" — the 10 pass/fail criteria the spec encodes as source-contract assertions)
  </read_first>
  <action>
    Edit `page.tsx`: replace the `BuilderWithSourceViewer` import (line 7) and
    its render (lines 75-79) with `BuilderStageShell`, passing the SAME
    `sopId`/`initialSop`/`parseJob` props inside the existing `<Suspense>`.
    Leave the server data-fetch (SOP + sections + parse_job + signLayoutData)
    untouched. Do NOT delete BuilderWithSourceViewer.tsx in this plan (kept as
    a reference shim; its removal is a follow-up) — but ensure nothing else
    imports it by grepping; if only page.tsx imported it, note that in the
    SUMMARY.

    Edit `playwright.config.ts`: add a project
    `{ name: 'phase21.5-stubs', testMatch:
    /(block-type-labels|builder-review-flow)\.(test|spec)\.ts$/ }`. After
    editing, the executor MUST verify discovery: `npx playwright test --list
    --project=phase21.5-stubs` lists both files (CLAUDE.md 2026-05-25 — if a
    file doesn't appear it isn't gated).

    Create `tests/builder/builder-review-flow.spec.ts` as SOURCE-CONTRACT
    assertions (no chromium runtime needed — Rule-3 trade-off consistent with
    Phase 15/21; live UAT is deferred per CLAUDE.md Railway-only-testing). The
    spec reads the relevant source files from disk and asserts:
      1. R1/R7: `BuilderStageShell.tsx` imports `BuilderStageStepper`,
         `ReviewStation`, `PublishStage`, and `BuilderClient`; `page.tsx`
         renders `BuilderStageShell` (and no longer renders
         BuilderWithSourceViewer).
      2. R7/R10: `BuilderStageShell.tsx` contains exactly the publish trigger
         via `handlePublish`/`onPublish` and does NOT mount
         `VerifyProgressIndicator` or a second `publish-button`; the publish
         POST target `/api/sops/${'$'}{sopId}/publish` string is present.
      3. R10 server gate: `src/app/api/sops/[sopId]/publish/route.ts` still
         contains the `'unverified_blocks'` literal and a `{ status: 400 }`
         response (the gate is intact).
      4. R4: neither `ReviewStation.tsx` nor `NavRow.tsx` nor
         `BlockChecklistRow.tsx` renders a bare `{block.type}`; all import
         `humanizeBlockType`.
      5. R10 lock: `tests/lint/no-bulk-verify-ui.spec.ts` exists and is
         referenced by a playwright project regex (string-match the config).
      6. R8: `BuilderStageShell.tsx` derives `hasSourceDoc`/`showPane` and
         gates ReviewStation/source rendering on it.
    Each assertion fails loudly with the file + missing token. Keep the spec
    Windows-path-safe (use path.join + normalize `\\` like the existing lint
    guard does).
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.5-stubs builder-review-flow --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` renders `<BuilderStageShell ... />` inside the existing Suspense; server fetch logic unchanged
    - `playwright.config.ts` has a `phase21.5-stubs` project; `npx playwright test --list --project=phase21.5-stubs` lists both `block-type-labels` and `builder-review-flow`
    - `builder-review-flow.spec.ts` passes all six source-contract assertions
    - `tests/lint/no-bulk-verify-ui.spec.ts` stays green
    - No remaining importer of BuilderWithSourceViewer other than (optionally) the kept reference file itself
  </acceptance_criteria>
  <done>page.tsx uses the new shell; verification spec registered + passing; safety lock + server gate proven intact via source contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin builder shell → /api/sops/[sopId]/publish | Shell is the only mount of the publish callback; the route's server-side 400 `unverified_blocks` gate is authoritative and unchanged |
| admin route → worker bundle (D-21-09) | Shell mounts source-viewer/reviewer/verify code only on the admin builder route; SourceViewerPane stays dynamic-imported; no worker route imports the shell |
| client publish UI → server gate | UI `disabled` is advisory; a direct POST with unverified blocks must still 400 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-05-01 | Tampering / EoP | Accidental publish-gate bypass during shell migration | mitigate | `handlePublish` migrated verbatim; server `/publish` route untouched; `builder-review-flow.spec.ts` asserts the route still returns 400 `unverified_blocks`; publish disabled until `isReady` |
| T-21.5-05-02 | Tampering | Bulk-verify affordance slipping into the shell | mitigate | Shell mounts no verify-all control; `no-bulk-verify-ui.spec.ts` runs in verify step AND is asserted-registered by the new spec (D-21-07) |
| T-21.5-05-03 | EoP | pdfjs/mammoth/reviewer leaking into worker bundle | mitigate | Shell + ReviewStation are admin-route-only; SourceViewerPane stays `next/dynamic` (ssr:false); BuilderClient unchanged; no static worker import path created (D-21-09) |
| T-21.5-05-04 | Repudiation | Unregistered verification spec gives false green | mitigate | Executor must run `--list --project=phase21.5-stubs` and confirm both files appear before declaring done (CLAUDE.md 2026-05-25) |
| T-21.5-05-SC | Tampering | npm installs | mitigate | No new packages; nothing to audit |
</threat_model>

<verification>
- `npx playwright test --project=phase21.5-stubs` runs and passes (block-type-labels + builder-review-flow)
- `npx playwright test --project=phase15-stubs no-bulk-verify-ui` stays green
- `npx playwright test --list --project=phase21.5-stubs` lists both spec/test files (registration proof)
- `npm run lint` clean on BuilderStageShell.tsx + page.tsx
- `npm run build` succeeds (next build --webpack) — shell compiles into the admin route
- Grep page.tsx: renders `BuilderStageShell`, no `BuilderWithSourceViewer` render
- Grep BuilderStageShell.tsx: `/publish` POST present; no `VerifyProgressIndicator` mount; `SourceViewerSelectionProvider` wraps content
</verification>

<success_criteria>
- The live builder route renders the Build→Review→Publish (or Build→Publish) shell with correct active-stage marking
- Publish fires only from PublishStage through the preserved callback chain
- Source-less SOPs get the 2-stage flow with no Review/source and still publish
- Server 400 publish gate + no-bulk-verify lock proven intact (source-contract + registered lint guard)
</success_criteria>

<artifacts_produced>
- `src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx` — NEW. Exports `BuilderStageShell` (outermost shell: top bar + stepper + adaptive Build/Review/Publish switch + preserved publish chain + selection-sync provider).
- `src/app/(protected)/admin/sops/builder/[sopId]/page.tsx` — MODIFIED. Renders BuilderStageShell.
- `playwright.config.ts` — MODIFIED. New `phase21.5-stubs` project registering block-type-labels + builder-review-flow.
- `tests/builder/builder-review-flow.spec.ts` — NEW. Six source-contract assertions covering R1/R4/R7/R8/R10.
</artifacts_produced>

<output>
Create `.planning/phases/21.5-builder-review-ux/21.5-05-SUMMARY.md` when done.
</output>
