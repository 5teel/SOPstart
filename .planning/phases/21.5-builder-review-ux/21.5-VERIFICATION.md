---
phase: 21.5-builder-review-ux
verified: 2026-06-02T14:30:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open the builder route on sopstart.com for a parsed SOP that has a source document"
    expected: "A dark 48px top bar appears with a Build → Review & verify → Publish 3-chip stepper; Build chip is active; SOP title + version show on the left"
    why_human: "Presentational layout, CSS styling, and correct chip active/done/todo visual states cannot be verified from source contracts alone"
  - test: "While on Build stage, click the 'Review & verify' chip in the stepper"
    expected: "The white OrientationStrip appears below the top bar with correct prose ('Check each step against the original document…'), the 3-zone ReviewStation renders with a step navigator on the left, one step card in the centre, and the source document on the right"
    why_human: "Zone grid layout, responsive reflow, and source pane sync all require runtime rendering to observe"
  - test: "In the Review stage, click different NavRows in the left zone"
    expected: "The centre zone updates to show the selected step's card; the source pane auto-scrolls to that step's region; the NavRow active border + highlight change correctly"
    why_human: "Multi-zone synchronisation via useSelectionSync and SourceViewerPane scroll depend on runtime browser state"
  - test: "In the Review stage, click an AI safety flag within the step card centre zone"
    expected: "The source pane scrolls to the flagged region in the original document; no console error about null provenance"
    why_human: "CR-01 fix (ReviewerFlagsPanel blockProvenance type) is verifiable in source but the actual scroll behaviour requires a live session with real provenance data"
  - test: "At 768px viewport width (DevTools resize or tablet), open the Review stage"
    expected: "The grid shows 2 columns (240px + flex-1); Zone 3 (source pane) is hidden; a 'Show source' button appears in the Zone 2 header; clicking it reveals the source as an overlay drawer"
    why_human: "CSS media query reflow at a specific viewport width requires a browser render; the CSS rules are present in source but their effect cannot be asserted without rendering"
  - test: "Open an AI-prompt SOP (no source document) in the builder on sopstart.com"
    expected: "The stepper shows only 2 chips: Build → Publish (no Review chip); clicking Publish chip shows the PublishStage with heading 'Publish this SOP'; no source pane and no 'Back to Review' link appear; Publish button works"
    why_human: "Adaptive 2-stage flow behaviour requires a live SOP with no source_file_path/ai_prompt source_type to exercise the conditional rendering path"
  - test: "Verify all steps in a draft SOP, then navigate to the Publish stage"
    expected: "The stepper marks Review as done (green ✓); the Publish chip becomes clickable; on the Publish stage the amber blocked-reason strip disappears and the 'Publish SOP' button activates in brand-yellow; clicking it publishes successfully"
    why_human: "End-to-end verify-gate satisfaction and publish flow require a real Supabase session, real block verification mutations, and a live POST /publish call"
  - test: "On the Publish stage with 1+ unverified steps, confirm the blocked-reason strip is visible without hovering"
    expected: "An amber strip reading 'N of M steps left to verify before you can publish' is always visible on screen (not in a tooltip); count updates as steps are verified"
    why_human: "The inline-visible-without-hover R6 requirement specifically excludes tooltip-only patterns; only a human can confirm the element is never hidden behind a hover state"
---

# Phase 21.5: Builder Review UX — Verification Report

**Phase Goal:** A first-time admin can, without guidance, understand each builder pane's purpose and complete review and publish — via a sequenced Build → Review → Publish flow that replaces the 5–6 always-on jargon-labelled panels.
**Verified:** 2026-06-02T14:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All 10 automated must-haves are VERIFIED in the codebase. The safety invariants (R10) are also VERIFIED with live test execution. Human verification is required for visual layout, CSS responsive behaviour, and live-session publish flow — consistent with the CLAUDE.md Railway-only-testing convention for this project.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (R1) | A Build → Review → Publish stepper renders with correct done/active/todo chip states and marks the active stage | VERIFIED | `BuilderStageStepper.tsx` — full chip row with `ChipState` derivation from ordinal; `data-active-stage` + `data-testid`; wired in `BuilderStageShell` top bar; shell renders `BuilderStageStepper` with live props |
| 2 (R2) | In Review, exactly one step shows at a time (navigator / step-card / source); selecting another row switches centre + source | VERIFIED | `ReviewStation.tsx` — single `activeBlock = blocks[activeIdx]`; `handleSelectRow` calls `setActiveIdx` + `setActiveProvenance`; NavRow `onSelect` triggers both; `SourceViewerPane` auto-subscribes to provenance sync |
| 3 (R3) | AI reviewer flags render inside the step card in Review; no standalone global flags panel remains | VERIFIED | `ReviewStation.tsx` lines 578-583 — `<ReviewerFlagsPanel sopId blockId={activeBlock.id} blockProvenance={activeBlock.provenance} />` mounted inline below the step card; no global `ReviewerFlagsPanel` render elsewhere in `ReviewStation` |
| 4 (R4) | No internal symbol names (`StepBlock`, `PhotoGrid`, `*Block`) are rendered anywhere in the builder review surface | VERIFIED | `block-type-labels.ts` — 14-entry BLOCK_TYPE_LABELS + humanizeBlockType; `BlockChecklistRow.tsx` imports + uses `humanizeBlockType(block.type)`, no raw `{block.type}`; `NavRow.tsx` same; `ReviewStation.tsx` same; 27 unit tests + `builder-review-flow.spec.ts` Test 4 assert the invariant; all 33 tests green |
| 5 (R5) | Verify / send-back row controls show word labels ("Verify step" / "Send back to edit"), not single letters a/d | VERIFIED | `BlockChecklistRow.tsx` line 105: `Verify step`; line 118: `Send back to edit`; `<kbd>A</kbd>`/`<kbd>D</kbd>` as secondary muted hints only; `ReviewStation.tsx` action bar: `✓ Looks right — verify step` / `✎ Send back to edit` |
| 6 (R6) | When publish is blocked, the remaining-count reason is always visible without hovering | VERIFIED (source) | `BuilderStageStepper.tsx` — `data-testid="stepper-publish-reason"` span rendered inline when `!isReady`, removed from DOM when `isReady`; `PublishStage.tsx` — `data-testid="publish-blocked-reason"` amber strip conditionally rendered when `!isReady`; no `title` tooltip pattern used anywhere |
| 7 (R7) | A dedicated Publish stage is the sole publish trigger; gated on verify checklist | VERIFIED | `BuilderStageShell.tsx` — only mounts `<PublishStage onPublish={handlePublish} .../>` in the `activeStage === 'publish'` branch; no `VerifyProgressIndicator` in shell; `builder-review-flow.spec.ts` Test 2 asserts no second publish-button; shell guards `'publish'` entry on `!effectiveIsReady` |
| 8 (R8) | An AI-prompt / source-less SOP renders a Build → Publish stepper (no Review stage, no source pane) | VERIFIED | `BuilderStageShell.tsx` — `hasSourceDoc = showPane` derived verbatim from CONV-12 logic; `activeStage === 'review' && hasSourceDoc` gates ReviewStation; `effectiveIsReady = true` when `!showVerifyGate`; `BuilderStageStepper` renders 2 chips when `!hasSourceDoc`; `builder-review-flow.spec.ts` Test 6 asserts derivation |
| 9 (R9) | At ≥768px the Review stage shows all three zones without horizontal page scroll | VERIFIED (source) | `ReviewStation.tsx` — CSS `@media (max-width: 1023px)` collapses to `240px 1fr`, hides Zone 3 (`display: none`), shows `rs-drawer-toggle` button; `BuilderStageStepper.tsx` — `@media (min-width: 768px) and (max-width: 1023px)` hides `.stepper-chip-label`; CSS queries, no JS breakpoint detection |
| 10 (R10) | Safety invariants preserved: server 400 gate intact + no-bulk-verify lock green | VERIFIED | `publish/route.ts` lines 97-102: `{ error: 'unverified_blocks', count }` with `{ status: 400 }`; `no-bulk-verify-ui.spec.ts` ran LIVE: 1 passed (scans all of `src/`); WR-01 fix: `useEffect` in shell demotes off Publish when `isReady` flips false; `builder-review-flow.spec.ts` Tests 3+5 assert both gates; all 33 tests green |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/builder/block-type-labels.ts` | BLOCK_TYPE_LABELS map + humanizeBlockType() | VERIFIED | 120 lines; 14+1 entries; 3-level lookup; FALLBACK_LABEL='Block'; R4 invariant enforced |
| `src/lib/builder/__tests__/block-type-labels.test.ts` | 27 unit tests for label resolution + symbol-leak invariant | VERIFIED | 176 lines; 27 tests; all passing in phase21.5-unit + phase21.5-stubs projects |
| `src/components/admin/verify-checklist/BlockChecklistRow.tsx` | Humanized label + word-labelled verify/send-back actions | VERIFIED | Imports `humanizeBlockType`; no raw `{block.type}`; "Verify step" / "Send back to edit" as primary labels; a/d as `<kbd>` secondary hints; all 4 testids preserved |
| `src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx` | Adaptive 2/3-chip stepper + inline publish reason | VERIFIED | 316 lines; controlled presentational; 3-chip (hasSourceDoc) or 2-chip; `data-testid="stepper-publish-reason"` absent when isReady; CSS media query for tablet; no data fetching |
| `src/app/(protected)/admin/sops/builder/[sopId]/OrientationStrip.tsx` | Review-stage purpose strip + progress counter | VERIFIED | 139 lines; exact Review prose from Copywriting Contract; `data-testid="orientation-strip"`; `role=progressbar`; no data fetching |
| `src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx` | Dedicated Publish stage: blocked-reason strip + gated button + error banner | VERIFIED | 218 lines; `data-testid="publish-stage"` / `"publish-button"` / `"publish-blocked-reason"` / `"publish-error-banner"`; no fetch/Supabase calls; `onPublish` callback only |
| `src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx` | Navigator row: check dot + humanized kind pill + label + flag badge | VERIFIED | 216 lines; `humanizeBlockType` imported and used; no raw `block.type`; `data-testid="nav-row"` + `data-block-id` + `data-active` + `data-verified` |
| `src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx` | 3-zone review grid: navigator / step-in-app+inline-AI / source, responsive ≥768px | VERIFIED | 785 lines; imports `useVerifyChecklist`, `useReviewerFlags`, `useChecklistKeybinds`, `useSelectionSync`, `ReviewerFlagsPanel`, `SourceViewerPane`, `NavRow`; CSS grid responsive; no `router.push` for step state; no global flags panel |
| `src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx` | Outermost shell: top bar + stepper + adaptive stage switch + preserved publish chain | VERIFIED | 320 lines; handlePublish verbatim from BuilderWithSourceViewer; `SourceViewerSelectionProvider` wraps content; `useEffect` WR-01 guard; `data-testid="builder-stage-shell"` |
| `src/app/(protected)/admin/sops/builder/[sopId]/page.tsx` | Renders BuilderStageShell instead of BuilderWithSourceViewer | VERIFIED | Line 7: `import { BuilderStageShell }...`; lines 75-79: `<BuilderStageShell .../>` inside Suspense; no BuilderWithSourceViewer import or render |
| `playwright.config.ts` | phase21.5-stubs project + phase21.5-unit project registered | VERIFIED | Lines 98-110: `phase21.5-unit` (testDir `./src/lib/builder/__tests__`); lines 104-110: `phase21.5-stubs` (testDir `.`, testMatch `/(block-type-labels|builder-review-flow)/`) |
| `tests/builder/builder-review-flow.spec.ts` | 6 source-contract assertions for R1/R4/R7/R8/R10 | VERIFIED | 212 lines; 6 tests; all 6 pass; asserts shell imports, handlePublish, server gate, humanized labels, lock registration, adaptive derivation |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `page.tsx` | `BuilderStageShell` | import + JSX render | WIRED | Line 7 import; line 75 `<BuilderStageShell sopId initialSop parseJob />` |
| `BuilderStageShell` | `BuilderStageStepper` | import + render in top bar | WIRED | Line 272: `<BuilderStageStepper activeStage hasSourceDoc isReady ... />` |
| `BuilderStageShell` | `ReviewStation` | import + render in `'review'` branch | WIRED | Line 295: `<ReviewStation sopId sourceType transcriptSegments />` gated on `hasSourceDoc` |
| `BuilderStageShell` | `PublishStage` | import + render in `'publish'` branch | WIRED | Line 304: `<PublishStage ... onPublish={handlePublish} .../>` — sole publish mount |
| `BuilderStageShell` | `/api/sops/[sopId]/publish` | POST in handlePublish | WIRED | Line 113: `fetch(\`/api/sops/${sopId}/publish\`, { method: 'POST' })` |
| `ReviewStation` | `useVerifyChecklist` | hook call at component top | WIRED | Line 56: `const checklist = useVerifyChecklist(sopId)` |
| `ReviewStation` | `useReviewerFlags` | hook call + byBlockId.get | WIRED | Line 57: `const reviewer = useReviewerFlags(sopId)`; line 99: `reviewer.byBlockId.get(activeBlock.id)` |
| `ReviewStation` | `ReviewerFlagsPanel` | inline render below step card | WIRED | Line 578-582: `<ReviewerFlagsPanel sopId blockId={activeBlock.id} blockProvenance={activeBlock.provenance} />` |
| `ReviewStation` | `useSelectionSync.setActiveProvenance` | handleSelectRow + handlePrev + focusSourcePane | WIRED | Lines 107-110: row select; lines 143-146: handlePrev (WR-03 fix); line 70: focusSourcePane |
| `NavRow` | `block-type-labels.ts` | import humanizeBlockType + BLOCK_TYPE_LABELS | WIRED | Lines 22-25 in NavRow.tsx |
| `BlockChecklistRow` | `block-type-labels.ts` | import humanizeBlockType | WIRED | Line 21 in BlockChecklistRow.tsx |
| `ReviewerFlagsPanel` | `setActiveProvenance` (CR-01 fix) | blockProvenance bare region, no `.region` dereference | WIRED | Lines 58-59: `const region = blockProvenance ?? null; setActiveProvenance(region, blockId)` — typed `SourceProvenanceRegion | null` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `BuilderStageShell` | `isReady`/`verifiedCount`/`totalCount` | `useVerifyChecklist(sopId)` → TanStack Query → Supabase `sop_section_blocks` | Yes — real DB query | FLOWING |
| `ReviewStation` checklist | `blocks`, `activeIdx` | `useVerifyChecklist(sopId)` — same cache key as shell | Yes — real DB query | FLOWING |
| `ReviewStation` AI flags | `reviewer.byBlockId` | `useReviewerFlags(sopId)` → TanStack Query → `ai_review_flags` table | Yes — real DB query | FLOWING |
| `PublishStage` | `verifiedCount`, `totalCount`, `isReady` | passed from shell's `useVerifyChecklist` call | Yes — same real query result | FLOWING |
| `BuilderStageStepper` | `verifiedCount`, `totalCount`, `isReady` | passed from shell | Yes — same real query result | FLOWING |
| `NavRow` step card body prose | `block.preview` | `ChecklistBlock.preview` from `useVerifyChecklist` | Yes — populated from DB snapshot | FLOWING (60-char truncated preview by design) |

Note on step card body prose: `ReviewStation.tsx` lines 510-512 render a static description sentence ("This step has been parsed from the source document. Review it against the original to the right.") as the body prose regardless of the block's actual full content. The `block.preview` (60-char) is used as the step title (h2). This is intentional and documented in the Plan 04 Summary "Known Stubs" section — the full Puck canvas block rendering is out of scope for this phase. It does not prevent the admin from reviewing the block against the source document (Zone 3), which is the primary review mechanism. Not a gap.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 33 phase21.5-stubs tests pass (R1/R4/R7/R8/R10 source contracts + label units) | `npx playwright test --project=phase21.5-stubs` | 33 passed (2.0s) | PASS |
| D-21-07 no-bulk-verify lock still green across all of src/ | `npx playwright test --project=phase15-stubs no-bulk-verify-ui` | 1 passed (1.5s) | PASS |
| Server publish route still contains 400 unverified_blocks gate | `builder-review-flow.spec.ts` Test 3 (file read assertion) | Passes — `unverified_blocks` literal + `status: 400` at lines 97-102 of route.ts | PASS |

### Probe Execution

No probe scripts defined for this phase. Source-contract specs serve as the CI gate per project convention (Railway-only-testing + `tests/builder/builder-review-flow.spec.ts` pattern established in Phase 21).

### Requirements Coverage

Phase 21.5 uses SPEC.md-local IDs (R1-R10), not the global REQUIREMENTS.md SCP-* codes. The SPEC.md is the authoritative requirement source. No `Phase 21.5` entries exist in `.planning/REQUIREMENTS.md` — this is expected (the phase was defined after the global requirements freeze). All 10 SPEC requirements are covered:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| R1 stepper | Plans 02, 05 | 3-stage Build→Review→Publish stepper, always visible, marks active stage | SATISFIED | `BuilderStageStepper` + `BuilderStageShell`; 3 chips with done/active/todo states |
| R2 review-station | Plan 04 | One step at a time: navigator / step-in-app / source, all synced | SATISFIED | `ReviewStation.tsx` 3-zone grid; `handleSelectRow` syncs all three zones |
| R3 inline-AI | Plan 04 | AI flags inside the step card they concern; no standalone panel in Review | SATISFIED | `ReviewerFlagsPanel` mounted inline below active block card; no global panel in `ReviewStation` |
| R4 humanized-labels | Plan 01 | No `*Block`/`PhotoGrid` internal symbols rendered to user | SATISFIED | `humanizeBlockType` in `BlockChecklistRow`, `NavRow`, `ReviewStation`; 27 unit tests + spec Test 4 |
| R5 plain-actions | Plan 01 | "Verify step" / "Send back to edit" as primary affordance; a/d secondary only | SATISFIED | `BlockChecklistRow` row-approve/decline; `ReviewStation` action bar words |
| R6 inline-publish-reason | Plans 02, 03 | Blocked-publish reason visible without hovering | SATISFIED | `stepper-publish-reason` span + `publish-blocked-reason` amber strip; both conditionally in DOM |
| R7 publish-stage | Plan 03, 05 | Dedicated Publish stage; sole publish trigger | SATISFIED | `PublishStage` component; shell gates publish to that branch only; spec Test 2 |
| R8 adaptive-stepper | Plans 02, 05 | Source-less SOPs: 2-stage Build→Publish, no Review | SATISFIED | `hasSourceDoc = showPane` derivation; 2-chip stepper + ReviewStation gated; spec Test 6 |
| R9 tablet-responsive | Plans 02, 04 | ≥768px: no horizontal scroll, Review zones reflow | SATISFIED (source) | CSS `@media` queries in `ReviewStation` (drawer) + `BuilderStageStepper` (number-only chips); no JS breakpoint detection |
| R10 safety-invariants | Plans 03, 05 | Server 400 gate + no-bulk-verify lock preserved | SATISFIED | Route.ts intact; `no-bulk-verify-ui.spec.ts` green; WR-01/CR-01 fixes applied |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `BuilderStageShell.tsx` | 55, 61 | `return null` | INFO | Inside `deriveSourcePaneKind()` helper — correct null returns for unrecognised source types; not a stub |
| `ReviewStation.tsx` | 510-512 | Static body prose string | INFO | Intentional — `block.preview` is 60-char truncated; full block content rendering is out of scope (documented in Plan 04 Summary). Admin reviews against Zone 3 source pane. Not a stub. |
| `BuilderStageShell.tsx` | 291 | `stageBadge="Step 2 of 3"` hardcoded | WARNING (WR-04, robustness) | Duplicates ordinal logic from BuilderStageStepper; drifts silently if R8 flow changes. Not a goal blocker. Tracked in REVIEW.md WR-04. |
| `ReviewStation.tsx` | 98-101 | Three independent `useReviewerFlags` instances in subtree | WARNING (WR-05, eventual-consistency) | Brief count disagreement between AI panel header and NavRow badges during rerun mutation. Not a correctness bug for the gate. Tracked in REVIEW.md WR-05. |
| `BuilderStageShell.tsx` | 143-164 | `transcriptSegments` array rebuilt every render | WARNING (WR-06, perf) | No `useMemo`; causes unnecessary referential churn. Only affects video SOPs. Tracked in REVIEW.md WR-06. |
| `NavRow.tsx` | 123 | `hover:bg-[var(--paper-2)]` Tailwind class layered over inline `background` style | INFO (IN-01) | Hover background never applies (inline style wins). Minor visual polish issue. Tracked in REVIEW.md IN-01. |
| `ReviewStation.tsx` + `BuilderStageStepper.tsx` | multiple | `<style>` injected in render tree | INFO (IN-04) | Static responsive rules duplicated per mount; global class names could collide if mounted twice (not an issue in practice). Tracked in REVIEW.md IN-04. |

No TBD / FIXME / XXX debt markers found in any Phase 21.5 files. No blockers from anti-pattern scan.

### Human Verification Required

**IMPORTANT:** All automated checks pass (10/10 truths verified, 33 tests green, safety locks confirmed). Human verification is required only for visual appearance, responsive layout, and live session flows per project convention (CLAUDE.md Railway-only-testing — UAT on sopstart.com, not localhost).

#### 1. Build→Review→Publish Stepper Visual State

**Test:** Open the admin builder on sopstart.com for any parsed SOP with a source document.
**Expected:** Dark 48px top bar with a 3-chip stepper showing Build (active), Review & verify (todo), Publish (todo). Active chip = white background, blue number dot. Todo chips = transparent, grey text, empty ring dot. SOP title + version visible on the left.
**Why human:** Chip visual states (colours, borders, dot fills) require browser rendering to confirm.

#### 2. Review Station 3-Zone Sync

**Test:** Click the "Review & verify" chip. Then click different rows in the left navigator.
**Expected:** White OrientationStrip appears below the top bar with correct prose. The centre zone updates to show the selected step's card (title + type pill). The source document on the right auto-scrolls to the corresponding passage. The NavRow active highlight follows the selection.
**Why human:** Multi-zone synchronisation, scroll behaviour, and zone layout cannot be assessed without a running browser connected to Supabase.

#### 3. AI Flag Inline + Source Scroll (CR-01 fix validation)

**Test:** Open a SOP with AI reviewer flags. In the Review stage, find a step card showing "AI safety check · N thing(s) to look at". Click one of the flag rows.
**Expected:** The source pane in Zone 3 scrolls to the flagged region of the original document. No console error about null provenance.
**Why human:** CR-01 fix is verified in source (bare region, not `.region`), but the actual scroll requires real provenance data from the DB.

#### 4. Tablet Responsive at 768px

**Test:** Open the builder in DevTools device emulation at 768px viewport width, navigate to Review stage.
**Expected:** Navigator (Zone 1) and step centre (Zone 2) visible. Zone 3 hidden with a "Show source" toggle button visible in Zone 2 header. Clicking the button reveals source as an overlay. No horizontal page scroll.
**Why human:** CSS `@media (max-width: 1023px)` rules require a browser render to confirm correct reflow.

#### 5. Adaptive 2-Stage Flow (Source-less SOP)

**Test:** Open an AI-prompt SOP (created without a source file upload) in the builder on sopstart.com.
**Expected:** Stepper shows only "Build → Publish" (2 chips). Clicking Publish shows the stage with heading "Publish this SOP", no verify-count progress summary, no "Back to Review" link. Publish button works (if no verify gate applies).
**Why human:** Requires a real SOP with `source_type = 'ai_prompt'` and no `source_file_path` to exercise the CONV-12 conditional.

#### 6. Publish Gate — Inline Reason Visible Without Hovering

**Test:** From the Publish stage with ≥1 unverified step, confirm the blocked reason is always on screen.
**Expected:** Amber strip "N of M steps left to verify before you can publish" is permanently visible on the page — not in a tooltip, not requiring hover. As steps are verified (go to Review, verify them, return to Publish), the count updates.
**Why human:** The R6 acceptance criterion specifically excludes hover-tooltip patterns. Only a human can confirm the element is not hidden behind a hover or visibility state.

#### 7. Publish Flow End-to-End

**Test:** Verify all steps for a draft SOP, navigate to Publish stage, click "Publish SOP".
**Expected:** Review chip in stepper shows green ✓ (done state). Publish button activates (brand-yellow). Post-publish: builder reflects the new `published` status (router.refresh() fires). Error banner shows if server rejects.
**Why human:** Requires a real Supabase session with write permission, real block verify mutations, and a live POST /publish call that returns 200.

---

## Gaps Summary

No gaps. All 10 requirement truths are VERIFIED in the codebase. The 3 code-review blockers/warnings (CR-01, WR-01, WR-03) were fixed in commit `eb03145` before this verification ran. The remaining WR-02/04/05/06 and 4 INFO items are robustness/perf/eventual-consistency notes that do not block the phase goal.

Human verification is needed only to confirm visual appearance, CSS responsive behaviour, and live-session publish flow on sopstart.com — consistent with the project's Railway-only-testing convention.

---

_Verified: 2026-06-02T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
