---
phase: 21.5-builder-review-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/builder/block-type-labels.ts
  - src/components/admin/verify-checklist/BlockChecklistRow.tsx
autonomous: true
requirements: [R4, R5]
must_haves:
  truths:
    - "No internal symbol name (StepBlock, PhotoGrid, *Block) is rendered to the admin anywhere in the verify checklist row"
    - "Every block type displays a human label (Step, Hazard, Measure, etc.)"
    - "Verify / send-back row controls show word labels (Verify step / Send back to edit), not single letters a/d, as the primary affordance"
  artifacts:
    - path: "src/lib/builder/block-type-labels.ts"
      provides: "BLOCK_TYPE_LABELS const map + humanizeBlockType() lookup"
      exports: ["BLOCK_TYPE_LABELS", "humanizeBlockType"]
      contains: "BLOCK_TYPE_LABELS"
    - path: "src/components/admin/verify-checklist/BlockChecklistRow.tsx"
      provides: "Checklist row consuming humanized labels + word-labelled verify/send-back actions"
      contains: "humanizeBlockType"
  key_links:
    - from: "src/components/admin/verify-checklist/BlockChecklistRow.tsx"
      to: "src/lib/builder/block-type-labels.ts"
      via: "import humanizeBlockType / BLOCK_TYPE_LABELS"
      pattern: "block-type-labels"
---

<objective>
Create the humanized block-type label map (SPEC R4) and convert the verify
checklist row from raw `{block.type}` symbols + single-letter `a`/`d` actions
to human labels + word-labelled "Verify step" / "Send back to edit" controls
(SPEC R5). This is the foundation Wave-2's ReviewStation NavRow also consumes.

Purpose: Removes internal-symbol jargon and cryptic single-letter actions —
the two most-cited confusion drivers from Phase 21 UAT — without touching the
verify data layer (`useVerifyChecklist`, server actions) at all.

Output: `src/lib/builder/block-type-labels.ts` (new) and a restyled
`BlockChecklistRow.tsx` that imports it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.5-builder-review-ux/21.5-SPEC.md
@.planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md
@src/components/admin/verify-checklist/BlockChecklistRow.tsx
@src/components/admin/verify-checklist/useVerifyChecklist.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BLOCK_TYPE_LABELS map + humanizeBlockType lookup</name>
  <files>src/lib/builder/block-type-labels.ts, src/lib/builder/__tests__/block-type-labels.test.ts</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Humanized Type Label Mapping" — the full 14-row internal-type → human-label table is the authoritative source)
    - src/components/admin/verify-checklist/useVerifyChecklist.ts (lines 113-128: `type` is the snapshot `kind` field, lowercase-ish; the label map must tolerate both the PascalCase block-component names from the UI-SPEC table AND the snapshot `kind` slug values)
  </read_first>
  <behavior>
    - humanizeBlockType('StepBlock') === 'Step'
    - humanizeBlockType('HazardCardBlock') === 'Hazard'
    - humanizeBlockType('PhotoBlock') === 'Photo'
    - humanizeBlockType('PhotoGrid') === 'Photo grid' (legacy alias from SPEC R4 acceptance text)
    - humanizeBlockType('MeasurementBlock') === 'Measure'
    - humanizeBlockType('unknown') and humanizeBlockType('') return a safe non-symbol fallback ('Block') — NEVER echoes a raw value ending in 'Block' or containing 'Grid'
    - Lookup is case-insensitive on the leading token so snapshot `kind` slugs (e.g. 'step', 'hazard', 'measurement') resolve to the same human label
    - BLOCK_TYPE_LABELS additionally exposes a `kind` pill variant key per the UI-SPEC table (kind-step / kind-haz / kind-meas / kind-ins / kind-dec / kind-esc / kind-sign) for NavRow consumption in Plan 04
  </behavior>
  <action>
    Create `src/lib/builder/block-type-labels.ts`. Export `BLOCK_TYPE_LABELS`
    as a Record keyed by the 14 internal type identifiers from the UI-SPEC
    "Humanized Type Label Mapping" table (StepBlock, HazardCardBlock,
    MeasurementBlock, InspectBlock, DecisionBlock, EscalateBlock, SignOffBlock,
    PhotoBlock, TextBlock, HeadingBlock, CalloutBlock, PPECardBlock,
    VoiceNoteBlock, ZoneBlock), each value being `{ label, pillVariant }`
    where label is the exact human string and pillVariant is the kind-* class
    suffix from the table. Add the legacy alias 'PhotoGrid' → { label: 'Photo
    grid', pillVariant: 'kind-step' } because SPEC R4 acceptance text names it
    explicitly. Export `humanizeBlockType(type: string): string` that
    normalizes input (trim; try exact key; then try a leading-token
    case-insensitive match against the snapshot `kind` slugs so 'measurement'
    → 'Measure', 'step' → 'Step', 'hazard' → 'Hazard') and returns the matched
    `label`, falling back to the literal string `'Block'` for unknown/empty
    input — the fallback MUST NOT echo any input ending in 'Block' or
    containing 'Grid' (R4 invariant: no internal symbol leaks). Pure module,
    no React, no server imports — safe for both admin and (per D-21-09) it is
    NOT imported by worker routes; add a top-of-file comment stating
    admin-only consumption.
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.5-stubs block-type-labels --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/builder/block-type-labels.ts` exports `BLOCK_TYPE_LABELS` and `humanizeBlockType`
    - Unit test asserts the behavior cases above pass (StepBlock→Step, PhotoGrid→Photo grid, unknown→Block, case-insensitive slug match)
    - `humanizeBlockType` never returns a string ending in 'Block' or containing 'Grid' for any input except the explicit 'Photo grid' label
  </acceptance_criteria>
  <done>BLOCK_TYPE_LABELS + humanizeBlockType exist with passing unit tests; no raw-symbol echo possible.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle BlockChecklistRow — humanized label + word-labelled verify/send-back actions</name>
  <files>src/components/admin/verify-checklist/BlockChecklistRow.tsx</files>
  <read_first>
    - src/components/admin/verify-checklist/BlockChecklistRow.tsx (current: line 62-64 renders raw `{block.type}` mono-uppercase; lines 85-110 render the `a` / `d` buttons)
    - src/components/admin/verify-checklist/__tests__/VerifyChecklistGate.test.tsx (the no-bulk-verify static-analysis test — confirm no banned phrase is introduced)
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Verify Actions" + § "Copywriting Contract" — "Verify step" replaces `a`, "Send back to edit" replaces `d`; keyboard hints may remain as SECONDARY hints only)
    - src/lib/builder/block-type-labels.ts (created in Task 1 — the import source)
  </read_first>
  <action>
    Edit `BlockChecklistRow.tsx`. Replace the raw `{block.type}` span body with
    `{humanizeBlockType(block.type)}` imported from `@/lib/builder/block-type-labels`.
    Keep the `data-testid="block-checklist-row"` / `data-block-id` /
    `data-verified` attributes UNCHANGED (downstream tests + ReviewStation rely
    on them). Replace the two single-letter buttons: the `row-approve` button
    label becomes the text "Verify step" (keep `data-testid="row-approve"` and
    `aria-label`), and the `row-decline` button label becomes "Send back to
    edit" (keep `data-testid="row-decline"`). Apply the UI-SPEC action styling:
    "Verify step" uses `--accent-ok` semantics (green), "Send back to edit"
    uses `--accent-measure` text/border (`#c2410c` / orange). The single-letter
    `a`/`d` may persist ONLY as a secondary `kbd`-style hint inside or beside
    the button (e.g. a muted superscript), never as the primary visible
    affordance. Do NOT introduce any "verify all" / "approve all" / "select
    all" / bulk wording — the no-bulk-verify lint guard
    (tests/lint/no-bulk-verify-ui.spec.ts) must stay green. Do not change the
    component props, the onApprove/onDecline call sites, or the flag-badge
    block.
  </action>
  <verify>
    <automated>npx playwright test --project=phase15-stubs no-bulk-verify-ui --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - BlockChecklistRow renders `humanizeBlockType(block.type)` not raw `{block.type}` (grep: file imports from `@/lib/builder/block-type-labels` and no longer contains the bare `{block.type}` render at the type-label span)
    - The row-approve button visible text is "Verify step"; row-decline visible text is "Send back to edit"
    - `tests/lint/no-bulk-verify-ui.spec.ts` stays green (no banned bulk phrase introduced)
    - `data-testid` values (block-checklist-row, row-approve, row-decline, flag-badge) are unchanged
  </acceptance_criteria>
  <done>Checklist row shows human labels + word actions; safety lint guard green; testids preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin builder UI → verify server actions | Label/action restyle is presentation-only; mutation path (verifyBlock/unverifyBlock) untouched |
| src/ source tree → no-bulk-verify CI grep | Any new copy string is scanned for bulk-verify language |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-01-01 | Tampering | BlockChecklistRow copy strings | mitigate | New action labels are exactly "Verify step" / "Send back to edit"; no "approve all"/"verify all"/"select all" introduced — `no-bulk-verify-ui.spec.ts` runs in verify step (D-21-07 lock) |
| T-21.5-01-02 | Elevation of Privilege | block-type-labels.ts bundle leak | accept | Pure label module, no pdfjs/mammoth/reviewer code; admin-only by convention (comment) — no worker-bundle attack surface (D-21-09) |
| T-21.5-01-SC | Tampering | npm installs | mitigate | No new packages installed in this plan; nothing to audit |
</threat_model>

<verification>
- `npx playwright test --project=phase21.5-stubs block-type-labels` passes (Task 1 unit test)
- `npx playwright test --project=phase15-stubs no-bulk-verify-ui` stays green
- `npm run lint` clean on both modified/created files
- Grep `src/components/admin/verify-checklist/BlockChecklistRow.tsx` for `{block.type}` at the type-label span returns nothing; grep for `block-type-labels` returns the import
</verification>

<success_criteria>
- BLOCK_TYPE_LABELS map + humanizeBlockType exist and are unit-tested
- BlockChecklistRow shows human labels + "Verify step" / "Send back to edit"
- No internal symbol name renders from the row; no bulk-verify phrase introduced
- Plan 04 (ReviewStation NavRow) can import the same label map
</success_criteria>

<artifacts_produced>
- `src/lib/builder/block-type-labels.ts` — NEW. Exports `BLOCK_TYPE_LABELS` (Record of `{ label, pillVariant }`) and `humanizeBlockType(type)`.
- `src/lib/builder/__tests__/block-type-labels.test.ts` — NEW. Unit tests for label resolution + symbol-leak invariant.
- `src/components/admin/verify-checklist/BlockChecklistRow.tsx` — MODIFIED. Humanized label + word-labelled verify/send-back actions.
</artifacts_produced>

<output>
Create `.planning/phases/21.5-builder-review-ux/21.5-01-SUMMARY.md` when done.
</output>
