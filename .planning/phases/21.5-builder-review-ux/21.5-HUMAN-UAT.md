---
status: partial
phase: 21.5-builder-review-ux
source: [21.5-VERIFICATION.md]
started: 2026-06-02T14:30:00Z
updated: 2026-06-02T14:30:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Top bar + 3-chip stepper on a source-backed SOP
expected: Open the builder route on sopstart.com for a parsed SOP that has a source document. A dark 48px top bar appears with a Build → Review & verify → Publish 3-chip stepper; Build chip is active; SOP title + version show on the left.
result: [pending]

### 2. Enter Review stage — orientation strip + 3-zone grid
expected: While on Build stage, click the "Review & verify" chip. The white OrientationStrip appears below the top bar with correct prose ("Check each step against the original document…"), and the 3-zone ReviewStation renders with a step navigator (left), one step card (centre), and the source document (right).
result: [pending]

### 3. Navigator selection syncs centre + source
expected: In the Review stage, click different NavRows in the left zone. The centre zone updates to the selected step's card; the source pane auto-scrolls to that step's region; the NavRow active border + highlight change correctly.
result: [pending]

### 4. AI flag → source scroll (CR-01 live validation)
expected: In the Review stage, click an AI safety flag within the step card centre zone. The source pane scrolls to the flagged region in the original document; no console error about null provenance.
result: [pending]

### 5. 768px tablet responsive reflow
expected: At 768px viewport width (DevTools resize or tablet), open the Review stage. The grid shows 2 columns (240px + flex-1); Zone 3 (source pane) is hidden; a "Show source" button appears in the Zone 2 header; clicking it reveals the source as an overlay drawer.
result: [pending]

### 6. Adaptive 2-stage flow on a source-less (AI-prompt) SOP
expected: Open an AI-prompt SOP (no source document) in the builder. The stepper shows only 2 chips: Build → Publish (no Review chip); clicking Publish shows the PublishStage with heading "Publish this SOP"; no source pane and no "Back to Review" link appear; Publish works.
result: [pending]

### 7. End-to-end verify → publish flow
expected: Verify all steps in a draft SOP, then navigate to the Publish stage. The stepper marks Review as done (green ✓); the Publish chip becomes clickable; on the Publish stage the amber blocked-reason strip disappears and the "Publish SOP" button activates in brand-yellow; clicking it publishes successfully.
result: [pending]

### 8. Inline blocked-reason visible without hover (R6)
expected: On the Publish stage with 1+ unverified steps, confirm the blocked-reason strip is visible without hovering — an amber strip reading "N of M steps left to verify before you can publish" is always on screen (not in a tooltip); count updates as steps are verified.
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps
