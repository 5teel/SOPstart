---
phase: 21.5-builder-review-ux
plan: 04
type: execute
wave: 2
depends_on: ["21.5-01"]
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx
autonomous: true
requirements: [R2, R3, R9]
must_haves:
  truths:
    - "In Review, exactly one step renders in the centre zone at a time; clicking another navigator row switches both the centre step card and the source pane to that step"
    - "Each step's AI reviewer flags render inside that step's own card (centre zone); a clean step shows no AI chrome; no standalone reviewer-flags panel remains in Review"
    - "The navigator left zone lists every step with a humanized kind pill, a verified check dot, and an unresolved-flag badge"
    - "At 768px the Review stage shows all three zones' content (zone 3 as a drawer) with no horizontal page scroll or overlapping panes"
    - "The action bar shows word-labelled Verify step / Send back to edit / Previous step controls with secondary keyboard hints"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx"
      provides: "3-zone Review grid (navigator / step-in-app+inline-AI / source) rendering one step at a time, responsive ≥768px"
      exports: ["ReviewStation"]
      min_lines: 80
      contains: "ReviewStation"
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx"
      provides: "Single navigator row (check dot + humanized kind pill + label + flag badge) with active/verified states"
      exports: ["NavRow"]
      contains: "NavRow"
  key_links:
    - from: "ReviewStation"
      to: "useVerifyChecklist"
      via: "reuse existing hook for blocks/verifiedCount/approve/decline/setActiveIdx"
      pattern: "useVerifyChecklist"
    - from: "ReviewStation centre zone"
      to: "useReviewerFlags / ReviewerFlagsPanel"
      via: "render the active block's flags inline inside the step card"
      pattern: "useReviewerFlags|ReviewerFlagsPanel"
    - from: "ReviewStation navigator selection"
      to: "useSelectionSync.setActiveProvenance"
      via: "selecting a row scrolls the source pane to that step's provenance region"
      pattern: "setActiveProvenance"
    - from: "NavRow"
      to: "src/lib/builder/block-type-labels.ts"
      via: "humanizeBlockType for the kind pill"
      pattern: "block-type-labels"
---

<objective>
Build the Review Station (SPEC R2): a 3-zone grid — step navigator (left),
the selected step rendered as the worker sees it with its AI flags INLINE
inside the card (centre, SPEC R3), and the source document (right, the
existing `SourceViewerPane`) — showing exactly one step at a time and keeping
all three zones in sync. Make it responsive to 768px (SPEC R9: zone 3 becomes
a drawer, no horizontal page scroll). Reuse the shipped data layer
(`useVerifyChecklist`, `useReviewerFlags`, `useSelectionSync`, the existing
keyboard bindings) — restructure presentation only, no new data/query code.

Purpose: Replaces the 5-6 always-on panels with a single-step focused review
surface, the core of the redesign. Eliminates the disconnected
reviewer-flags panel by attaching flags to the step they concern.

Output: `ReviewStation.tsx` + `NavRow.tsx` (both new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.5-builder-review-ux/21.5-SPEC.md
@.planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md
@src/components/admin/verify-checklist/useVerifyChecklist.ts
@src/components/admin/verify-checklist/VerifyChecklistGate.tsx
@src/components/admin/verify-checklist/keyboard-bindings.ts
@src/components/admin/ai-reviewer/ReviewerFlagsPanel.tsx
@src/components/admin/ai-reviewer/useReviewerFlags.ts
@src/components/admin/ai-reviewer/RerunReviewerButton.tsx
@src/components/admin/source-viewer/useSelectionSync.tsx
@src/lib/builder/block-type-labels.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavRow — single navigator row with humanized kind pill + states</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Zone 1 — Step Navigator" full NavRow anatomy: check dot 16px, kind pill 74px, label flex truncated, flag badge; row states default/active/verified; kind pill colors)
    - src/components/admin/verify-checklist/BlockChecklistRow.tsx (the existing row's data shape + testids — NavRow is the new Review-zone styling of the same `ChecklistBlock`; reuse the verified/hasFlags derivation)
    - src/components/admin/verify-checklist/useVerifyChecklist.ts (the `ChecklistBlock` type: id, type, preview, verified_by_admin_id, flags_count, provenance)
    - src/lib/builder/block-type-labels.ts (humanizeBlockType + pillVariant for the kind pill)
  </read_first>
  <action>
    Create `NavRow.tsx` ('use client'). Props: `block: ChecklistBlock`
    (import the type from `@/components/admin/verify-checklist/useVerifyChecklist`),
    `active: boolean`, `onSelect: () => void`. Render per UI-SPEC § "Zone 1":
    a flex row (align-center, gap 8px, padding 8px, border-radius 3px,
    cursor-pointer) containing — a 16×16px check dot (border-radius 50%; empty
    = border 1.5px `--ink-300`; verified = filled `--accent-ok` with white
    `✓`), a 74px-fixed kind pill (10px/weight 600/uppercase/radius 2px/center)
    whose label is `humanizeBlockType(block.type)` and whose colour comes from
    the `pillVariant` in BLOCK_TYPE_LABELS, a flex-1 truncated label showing
    `block.preview` (title attr = full preview), and — only when
    `block.flags_count > 0 && !verified` — a circular red flag badge
    (`--accent-hazard`, white, 16px, font 10px bold) showing the count. Row
    states: default border 1px transparent + hover background `--paper-2`;
    active `border 1px solid --accent-step; background #eff4ff`; verified text
    `--ink-500`. Derive `verified = block.verified_by_admin_id !== null`. Root
    carries `data-testid="nav-row"`, `data-block-id={block.id}`,
    `data-active`, `data-verified`. onClick → `onSelect`. Presentational;
    no data fetching. Do NOT render the raw `block.type`.
  </action>
  <verify>
    <automated>npm run lint -- --max-warnings=0 src/app/\(protected\)/admin/sops/builder/\[sopId\]/NavRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - NavRow renders `humanizeBlockType(block.type)` (imports from `@/lib/builder/block-type-labels`); raw `block.type` never rendered
    - Verified rows show a filled green check dot + muted text; active rows show the blue border + `#eff4ff` tint
    - Flag badge appears only when `flags_count > 0 && !verified`
    - Root carries `data-testid="nav-row"` + `data-block-id` + `data-active` + `data-verified`
  </acceptance_criteria>
  <done>NavRow renders one navigator row with humanized pill + correct states; presentational.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewStation — 3-zone grid, one step at a time, inline AI, responsive ≥768px</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx</files>
  <read_first>
    - .planning/phases/21.5-builder-review-ux/21.5-UI-SPEC.md (§ "Layout: Review Station" all three zones + § "Component: Inline AI Check Panel" + § "Action Bar" + § "Responsive Behaviour" desktop/tablet grids + drawer + § "Copywriting Contract" zone headers / action-bar labels / empty states + § "Color"/"Spacing"/"Typography" tokens)
    - src/components/admin/verify-checklist/VerifyChecklistGate.tsx (the canonical wiring: useVerifyChecklist + useChecklistKeybinds + focusSourcePane via useSelectionSync.setActiveProvenance — ReviewStation reuses ALL of this, only the rendering changes; do NOT rebuild the hook or keybinds)
    - src/components/admin/verify-checklist/useVerifyChecklist.ts (blocks, activeIdx, setActiveIdx, approve, decline, verifiedCount, totalCount, activeBlockId)
    - src/components/admin/ai-reviewer/ReviewerFlagsPanel.tsx (mount this per-active-block INSIDE the step card; it self-hides when zero flags) + src/components/admin/ai-reviewer/useReviewerFlags.ts (byBlockId for the inline AI header count) + RerunReviewerButton.tsx (restyle via className for the AI panel header per UI-SPEC)
    - src/components/admin/source-viewer/SourceViewerPane.tsx (zone 3 content — rendered as-is; props sopId/sourceType/transcriptSegments) + useSelectionSync.tsx (setActiveProvenance to sync source on row select)
    - CLAUDE.md Learnings 2026-05-13 (drive frequently-changing active-step state from useState synced to URL via history.replaceState, NOT router.push — avoids RSC-fetch lag under Serwist) and 2026-05-25 (any new tests/lint spec must be registered in playwright.config.ts — Plan 05 owns registration)
  </read_first>
  <action>
    Create `ReviewStation.tsx` ('use client'). Props: `sopId: string`,
    `sourceType` + `transcriptSegments` (pass-through to SourceViewerPane,
    same shapes BuilderWithSourceViewer derives today). Internally call the
    SHIPPED `useVerifyChecklist(sopId)` for blocks/activeIdx/setActiveIdx/
    approve/decline/verifiedCount/totalCount/activeBlockId and
    `useReviewerFlags(sopId)` for the active block's flag count; reuse
    `useChecklistKeybinds(...)` exactly as VerifyChecklistGate does (j/k nav,
    a verify, d send-back, Enter focus-source) with `focusSourcePane` calling
    `useSelectionSync().setActiveProvenance(block.provenance ?? null,
    block.id)`. Render a CSS grid per UI-SPEC § Responsive: at ≥1024px
    `grid-template-columns: 280px 1fr 1fr`; at 768–1023px `240px 1fr` with
    zone 3 hidden behind a "Show source" drawer toggle (icon button in zone 2
    header, `aria-label="Show original document"`); action bar spans full
    width at tablet, `grid-column: 2 / 4` at desktop. Use CSS media/container
    queries, NOT JS breakpoint detection (D-04 SSR-safety pattern).

    Zone 1 (navigator): zone header "Steps in this SOP" / sub "Click any step
    to review it"; map `checklist.blocks` to `<NavRow>` (Task 1), `active =
    idx === activeIdx`, `onSelect` → `setActiveIdx(idx)` AND
    `setActiveProvenance(block.provenance ?? null, block.id)` so the source
    pane follows. Empty state: when `totalCount === 0` show "Nothing to verify
    yet" / "Add content in the Build stage, then come back to review it." per
    the Copywriting Contract; loading state "Loading checklist…".

    Zone 2 (step in app): zone header "The step, in the app" / sub "This is
    what the worker will see on their phone". Render ONE step card for the
    active block (`checklist.blocks[activeIdx]`) per the UI-SPEC card anatomy
    (1.5px `--accent-step` border, strip with type pill + context, h2 step
    title, body prose, meta pills). Derive the human title/body from the
    block's snapshot/preview that `useVerifyChecklist` already exposes (use
    `block.preview` for the title text; humanized kind pill via
    humanizeBlockType for the strip). IMMEDIATELY BELOW the card, mount the
    inline AI Safety Check panel: render `<ReviewerFlagsPanel sopId={sopId}
    blockId={activeBlock.id} />` (it self-renders null when the block is clean
    → no AI chrome for clean steps, satisfying R3). Wrap it with the UI-SPEC
    AI panel header (status dot `--accent-ok` when no flags / `--accent-measure`
    when flags; label "AI safety check · N thing(s) to look at" or "AI safety
    check — all clear" using `useReviewerFlags.byBlockId.get(activeBlock.id)`
    length) and the restyled `<RerunReviewerButton sopId={sopId} />` ("↻
    Re-run") on the right. There MUST be exactly one step's content in the
    centre at a time and NO standalone global reviewer-flags panel anywhere in
    ReviewStation.

    Zone 3 (source): zone header "Original document" / sub "Auto-scrolled to
    the part this step came from"; render the existing `<SourceViewerPane
    sopId sourceType transcriptSegments />` as-is filling the column (it
    already subscribes to selection-sync and scrolls to the active provenance).

    Action bar (spans zones 2+3 desktop / full width tablet): "◀ Previous
    step" ghost button → `setActiveIdx(Math.max(0, activeIdx-1))`; "✓ Looks
    right — verify step" primary button (`--accent-ok` bg) →
    `checklist.approve(activeBlock.id)` then advance to next unverified or
    next idx; "✎ Send back to edit" button (`--accent-measure` text/border) →
    `checklist.decline(activeBlock.id)`; right-aligned keyboard-hint block
    `or press A verify · E edit · J/K next/prev` styled with kbd elements per
    UI-SPEC (secondary hints only). Use the exact label strings from the
    Copywriting Contract. Keep `data-testid="publish-button"` OUT of this
    component (publish lives only on the PublishStage — Req 7). Root carries
    `data-testid="review-station"`. Drive active-step from the hook's
    `activeIdx`; if you mirror it to the URL for deep-linking, use
    `window.history.replaceState`, never `router.push` (CLAUDE.md 2026-05-13).
    Do NOT introduce any "verify all"/"approve all"/"select all" affordance.
  </action>
  <verify>
    <automated>npx playwright test --project=phase15-stubs no-bulk-verify-ui --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - ReviewStation reuses `useVerifyChecklist`, `useReviewerFlags`, `useChecklistKeybinds`, `useSelectionSync` (grep confirms imports; no re-implementation of fetch/mutations)
    - Exactly one step card renders in zone 2 (keyed on `activeIdx`); selecting a NavRow updates `activeIdx` AND calls `setActiveProvenance` for that block
    - The active block's AI flags render inline below its card via `ReviewerFlagsPanel`; a clean block shows no flag rows; there is NO standalone global flags panel in the component
    - Grid is `280px 1fr 1fr` ≥1024px and `240px 1fr` 768–1023px with zone 3 as a drawer; no JS breakpoint detection (uses CSS queries)
    - Action bar shows "✓ Looks right — verify step" / "✎ Send back to edit" / "◀ Previous step" word labels + secondary kbd hints
    - `tests/lint/no-bulk-verify-ui.spec.ts` stays green
  </acceptance_criteria>
  <done>Review Station renders 3 synced zones, one step at a time, inline AI per step, responsive to 768px; data layer reused untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Review UI → verify server actions | ReviewStation drives verify/unverify via the existing `useVerifyChecklist` mutations only; no new write path |
| Admin bundle isolation (D-21-09) | ReviewStation imports the source-viewer + reviewer + verify hooks — these stay admin-only; ReviewStation is mounted only by the admin BuilderStageShell (Plan 05), never by a worker route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.5-04-01 | Tampering | Bulk-verify affordance via one-step→advance flow | mitigate | The verify action operates on the single active block only; no select-all/approve-all control added; `no-bulk-verify-ui.spec.ts` runs in verify step (D-21-07) |
| T-21.5-04-02 | EoP | pdfjs/mammoth/reviewer leaking to worker bundle | mitigate | ReviewStation lives under the admin builder route and is mounted only by the admin shell; SourceViewerPane stays dynamic-imported (Plan 05 keeps the dynamic boundary); no static worker-route import path introduced (D-21-09) |
| T-21.5-04-03 | DoS | Re-run button abuse | accept | RerunReviewerButton uses the existing `useReviewerFlags` rerun with the server-side per-day/per-org rate caps (Phase 21); no change here |
| T-21.5-04-SC | Tampering | npm installs | mitigate | No new packages; nothing to audit |
</threat_model>

<verification>
- `npx playwright test --project=phase15-stubs no-bulk-verify-ui` stays green
- `npm run lint` clean on both new files
- Grep ReviewStation.tsx: contains `useVerifyChecklist`, `useReviewerFlags`, `useChecklistKeybinds`, `setActiveProvenance`, `ReviewerFlagsPanel`, `SourceViewerPane`; does NOT contain `verifyBlock(`/`unverifyBlock(` direct calls or a second fetch implementation
- Grep ReviewStation.tsx: no `router.push` for step state; uses `setActiveIdx` (+ optional `history.replaceState`)
- Grep ReviewStation.tsx + NavRow.tsx: no raw `{block.type}` render; both use `humanizeBlockType`
</verification>

<success_criteria>
- One-step-at-a-time Review with navigator/centre/source all synced on selection
- AI flags attached inline per step; no global flags panel; clean steps show no AI chrome
- Humanized kind pills + word-labelled action bar
- Responsive to 768px with no horizontal page scroll
- Zero changes to the verify/reviewer/source-viewer data layer
</success_criteria>

<artifacts_produced>
- `src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx` — NEW. Exports `NavRow` (navigator row: check dot + humanized kind pill + label + flag badge).
- `src/app/(protected)/admin/sops/builder/[sopId]/ReviewStation.tsx` — NEW. Exports `ReviewStation` (3-zone synced review grid with inline per-step AI, responsive ≥768px).
</artifacts_produced>

<output>
Create `.planning/phases/21.5-builder-review-ux/21.5-04-SUMMARY.md` when done.
</output>
