---
phase: 15-manufacturing-line-mode
plan: 01
type: execute
wave: 1
depends_on: [15-00]
files_modified:
  - supabase/migrations/00030_sub_trades.sql
  - src/types/database.types.ts
  - src/types/sop.ts
  - src/lib/validators/voice-query.ts
  - src/lib/validators/sub-trades.ts
  - src/hooks/useViewport.ts
  - src/stores/walkthrough.ts
autonomous: false
requirements: [SB-LINE-01, SB-LINE-02, SB-LINE-05]
must_haves:
  truths:
    - "Postgres has three new tables (`sub_trades`, `users_sub_trades`, `sops_sub_trades`) with RLS enabled"
    - "Postgres seed vocabulary is exactly 5 rows: operator/fitter/sparky/maintainer/other"
    - "`current_user_sub_trades()` SECURITY DEFINER helper returns the calling user's sub-trade UUIDs and is reachable from RLS predicates"
    - "Empty `sops_sub_trades` for a SOP means workers see it regardless of their sub-trade (backward compat for Phase 1-14 data)"
    - "`completions.step_ack_trace` JSONB column exists with default `'[]'::jsonb` so Wave 2 can persist ack traces without another migration"
    - "TypeScript can compile imports of `SubTrade`, `UsersSubTrade`, `SopsSubTrade` types"
    - "Zod schemas `voiceQuerySchema` (sopId uuid + question 5..500 chars) and `subTradeAssignmentSchema` exist and are exported"
    - "`useViewport()` hook returns `'mobile'` on SSR and switches to `'desktop'` on a ≥1024px client after mount"
    - "`useWalkthroughStore` exposes `markStepAcknowledged`, `getHighestAckIndex`, `getAckTrace`, and the existing mobile completion behaviour is unchanged"
  artifacts:
    - path: "supabase/migrations/00030_sub_trades.sql"
      provides: "Junction-table schema + SECURITY DEFINER helper + seed vocab + completions.step_ack_trace column"
      contains: "create table if not exists public.sub_trades"
    - path: "src/types/database.types.ts"
      provides: "Manual type extension for sub_trades, users_sub_trades, sops_sub_trades, completions.step_ack_trace, plus Functions entries"
      contains: "sub_trades"
    - path: "src/types/sop.ts"
      provides: "`SubTrade` interface + `VoiceQueryResponse` type + extended completion shape"
      contains: "SubTrade"
    - path: "src/lib/validators/voice-query.ts"
      provides: "`voiceQuerySchema` + `VoiceQueryInput` + `VoiceQueryResponse` types"
      contains: "voiceQuerySchema"
    - path: "src/lib/validators/sub-trades.ts"
      provides: "`subTradeAssignmentSchema` (subTradeIds: uuid[])"
      contains: "subTradeAssignmentSchema"
    - path: "src/hooks/useViewport.ts"
      provides: "SSR-safe `useViewport(): 'mobile' | 'desktop'` based on `window.matchMedia('(min-width: 1024px)')`"
      contains: "matchMedia"
    - path: "src/stores/walkthrough.ts"
      provides: "Extended Zustand store with `ackTrace`, `markStepAcknowledged`, `getHighestAckIndex`, `getAckTrace`"
      contains: "markStepAcknowledged"
  key_links:
    - from: "RLS policy `sops_visible_by_sub_trade` on `public.sops`"
      to: "function `public.sub_trade_id_intersects(p_sop_id uuid)`"
      via: "USING clause"
      pattern: "sub_trade_id_intersects\\(sops\\.id\\)"
    - from: "`sub_trade_id_intersects(uuid)`"
      to: "`current_user_sub_trades()`"
      via: "IN (select * from public.current_user_sub_trades())"
      pattern: "current_user_sub_trades\\(\\)"
    - from: "`src/hooks/useViewport.ts`"
      to: "`window.matchMedia('(min-width: 1024px)')`"
      via: "useEffect listener"
      pattern: "min-width:\\s*1024px"
---

<objective>
Build the **foundation layer** for Phase 15a: database schema + manual type extensions + Zod validators + SSR-safe viewport hook + extended walkthrough store. Every later wave consumes something built here. Wave 2 needs the hook + the store extension. Wave 3 needs the validators + the types. Wave 4 needs the migration + RLS.

Purpose: Honour D-10 (junction-table schema, NOT text[]), D-13 (seed vocab), D-04 (SSR-safe viewport switch), D-21 (ack-trace state).

Output: One Supabase migration (idempotent), manually-extended `database.types.ts`, two new validator files, one new hook file, one extended Zustand store. All independent of Wave 2-4 work — runs in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-manufacturing-line-mode/15-SPEC.md
@.planning/phases/15-manufacturing-line-mode/15-CONTEXT.md
@.planning/phases/15-manufacturing-line-mode/15-RESEARCH.md
@.planning/phases/15-manufacturing-line-mode/15-PATTERNS.md
@CLAUDE.md
@supabase/migrations/00022_block_library_phase13.sql
@supabase/migrations/00029_ai_prompt_input_type.sql
@supabase/migrations/00017_multi_org_membership.sql
@supabase/migrations/00001_init.sql
@src/stores/walkthrough.ts
@src/lib/validators/sop.ts
@src/types/sop.ts
@src/types/database.types.ts

<interfaces>
<!-- Key types/exports Wave 2-4 will consume from this plan. -->

From `src/types/database.types.ts` (Phase 14 manual-extension pattern; do NOT auto-regen):
```typescript
// Existing shape — extend the Database['public']['Tables'] block by adding three new tables
// AND extend Database['public']['Functions'] with current_user_sub_trades + sub_trade_id_intersects.
```

New from `src/types/sop.ts`:
```typescript
export interface SubTrade {
  id: string
  slug: 'operator' | 'fitter' | 'sparky' | 'maintainer' | 'other' | string
  label: string
  sort_order: number
}

export interface VoiceQueryResponse {
  answer: string
  citations: string[]
  verifier_flags: VerificationFlag[]
}

export type AckTraceEntry = { stepId: string; timestamp: number }
```

New from `src/lib/validators/voice-query.ts`:
```typescript
export const voiceQuerySchema = z.object({
  sopId: z.string().uuid(),
  question: z.string().min(5).max(500),
})
export type VoiceQueryInput = z.infer<typeof voiceQuerySchema>
```

New from `src/lib/validators/sub-trades.ts`:
```typescript
export const subTradeAssignmentSchema = z.object({
  subTradeIds: z.array(z.string().uuid()).max(10),
})
export type SubTradeAssignmentInput = z.infer<typeof subTradeAssignmentSchema>
```

New from `src/hooks/useViewport.ts`:
```typescript
export function useViewport(): 'mobile' | 'desktop'
```

Extended from `src/stores/walkthrough.ts`:
```typescript
ackTrace: Record<string, AckTraceEntry[]>
markStepAcknowledged: (sopId: string, stepId: string) => void
getHighestAckIndex: (sopId: string, allStepIds: string[]) => number
getAckTrace: (sopId: string) => AckTraceEntry[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 00030 — sub-trade junctions + RLS helper + seed + completions.step_ack_trace</name>
  <files>supabase/migrations/00030_sub_trades.sql</files>
  <read_first>
    - `15-RESEARCH.md` lines 763-891 (full migration body — paste verbatim)
    - `15-PATTERNS.md` lines 549-610 (analog: 00022 controlled-vocab seed + SECURITY DEFINER helper)
    - `supabase/migrations/00022_block_library_phase13.sql` (entire file — the analog migration)
    - `supabase/migrations/00029_ai_prompt_input_type.sql` (entire file — idempotent ALTER pattern)
    - `supabase/migrations/00017_multi_org_membership.sql` lines 1-58 (organisation_members shape; needed for users_sub_trades_self_read policy)
    - `supabase/migrations/00001_init.sql` — grep for `policy "sops_select` to confirm existing SELECT policies on `sops` are permissive (D-10 RLS additivity assumption A4)
    - `CLAUDE.md` Learnings 2026-05-08 (ALTER TABLE RENAME doesn't rewrite SQL function bodies — must comment the helper)
  </read_first>
  <behavior>
    - Migration applies idempotently (re-run = no-op via `create table if not exists` + `on conflict (slug) do nothing`)
    - After apply: `select count(*) from public.sub_trades` returns 5
    - After apply: an authenticated user can `select * from public.current_user_sub_trades()` without error (returns 0 rows when no assignments)
    - After apply: `select column_name, data_type from information_schema.columns where table_name='completions' and column_name='step_ack_trace'` returns one row, type jsonb
    - After apply: `select policyname from pg_policies where tablename='sops'` includes `sops_visible_by_sub_trade`
    - Backward compat: a worker with zero `users_sub_trades` rows can still SELECT a SOP that has zero `sops_sub_trades` rows (because the policy `not exists` branch is true)
  </behavior>
  <action>
    Create `supabase/migrations/00030_sub_trades.sql` by **pasting the full migration body from `15-RESEARCH.md` lines 763-891 verbatim** (per D-10, D-11, D-13, D-21). The migration creates, in order, all inside one `begin; ... commit;`:

    1. `public.sub_trades` table — id uuid pk, slug text unique not null, label text not null, sort_order int not null default 100, created_at timestamptz not null default now()
       - RLS enabled; policy `sub_trades_read_all` for select to authenticated using (true)
       - Comment: `'Phase 15: controlled vocabulary of worker sub-trades (operator/fitter/sparky/maintainer/other). Locked in 15a; admin-editable in 15b.'`

    2. `public.users_sub_trades` junction — (user_id uuid fk auth.users on delete cascade, sub_trade_id uuid fk sub_trades on delete cascade, assigned_at timestamptz default now(), assigned_by uuid fk auth.users, primary key (user_id, sub_trade_id))
       - RLS enabled; policy `users_sub_trades_self_read` — self OR same-org admin/safety_manager via `organisation_members` join
       - Indexes on user_id and sub_trade_id

    3. `public.sops_sub_trades` junction — (sop_id uuid fk sops on delete cascade, sub_trade_id uuid fk sub_trades on delete cascade, primary key (sop_id, sub_trade_id))
       - RLS enabled; policy `sops_sub_trades_read_for_org` — read if the user can SELECT the parent SOP (delegates to existing sops RLS)
       - Index on sop_id

    4. `public.current_user_sub_trades()` SECURITY DEFINER helper — `language sql stable security definer set search_path = public` returning setof uuid; body: `select sub_trade_id from public.users_sub_trades where user_id = auth.uid();`
       - Comment **must include** the [2026-05-08] CLAUDE.md learning text: `'Phase 15: returns sub_trade_ids assigned to the calling user. Used by sops_visible_by_sub_trade policy. CRITICAL: SQL function body references users_sub_trades by NAME — any later rename MUST recompile this function (CLAUDE.md learning 2026-05-08).'`

    5. `public.sub_trade_id_intersects(p_sop_id uuid) returns boolean` SECURITY DEFINER helper — wraps the join into `sops_sub_trades` + `current_user_sub_trades()` set. Kept separate from the policy for testability.

    6. Seed insert into `public.sub_trades` — exactly 5 rows (slug + label + sort_order):
       - operator / Operator / 10
       - fitter / Fitter / 20
       - sparky / Sparky / Electrician / 30
       - maintainer / Maintainer / 40
       - other / Other / 90
       - `on conflict (slug) do nothing`

    7. RLS extension on `public.sops` SELECT:
       ```sql
       create policy "sops_visible_by_sub_trade" on public.sops for select to authenticated
         using (
           not exists (select 1 from public.sops_sub_trades sst where sst.sop_id = sops.id)
           or sub_trade_id_intersects(sops.id)
         );
       ```
       This is ADDITIVE — multiple permissive policies on the same command OR together (assumption A4 from RESEARCH).

    8. `alter table public.completions add column if not exists step_ack_trace jsonb not null default '[]'::jsonb;` with comment per RESEARCH lines 884-888.

    All wrapped `begin; ... commit;`. All idempotent (`create table if not exists`, `create or replace function`, `on conflict do nothing`, `add column if not exists`).
  </action>
  <acceptance_criteria>
    - `Test-Path supabase/migrations/00030_sub_trades.sql` returns True
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'create table if not exists public\.sub_trades'` returns 1 match
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'create table if not exists public\.users_sub_trades'` returns 1 match
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'create table if not exists public\.sops_sub_trades'` returns 1 match
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'create or replace function public\.current_user_sub_trades'` returns 1 match
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'security definer'` returns ≥ 2 matches (both helpers)
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern "CLAUDE\.md learning 2026-05-08"` returns ≥ 1 match (the tripwire comment)
    - `(Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern "'operator'","'fitter'","'sparky'","'maintainer'","'other'" | Measure-Object).Count` ≥ 5 (5 seed rows)
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'add column if not exists step_ack_trace jsonb'` returns 1 match
    - `Select-String -Path supabase/migrations/00030_sub_trades.sql -Pattern 'sops_visible_by_sub_trade'` returns ≥ 1 match
    - File starts with `begin;` and ends with `commit;`
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$f='supabase/migrations/00030_sub_trades.sql'; if (-not (Test-Path $f)) { exit 1 }; $c = (Select-String -Path $f -Pattern 'create table if not exists','security definer','sub_trade_id_intersects','sops_visible_by_sub_trade','step_ack_trace','on conflict \(slug\) do nothing','CLAUDE\.md learning 2026-05-08' | Measure-Object).Count; if ($c -lt 7) { Write-Error \"Migration missing required clauses: $c/7\"; exit 1 }"</automated>
  </verify>
  <done>Migration file exists, contains all 3 tables, 2 SECURITY DEFINER helpers, the additive `sops_visible_by_sub_trade` policy, the 5-row seed, the `completions.step_ack_trace` column add, and the CLAUDE.md tripwire comment on `current_user_sub_trades()`. File is idempotent.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [BLOCKING]: Apply migration 00030 to Supabase dev and verify end-to-end</name>
  <what-built>Migration file `supabase/migrations/00030_sub_trades.sql` from Task 1.</what-built>
  <how-to-verify>
    Simon — please run these in PowerShell in `C:\Development\SOPstart`:

    1. Push the migration to the linked Supabase project:
       ```
       npx supabase db push --include-all
       ```
    2. Confirm the 5 seed rows exist:
       ```
       npx supabase db remote query "select slug, label, sort_order from public.sub_trades order by sort_order;"
       ```
       Expect exactly 5 rows in order: operator, fitter, sparky, maintainer, other.
    3. Confirm the SECURITY DEFINER helper compiles and executes (without auth context returns 0 rows, no error):
       ```
       npx supabase db remote query "select count(*) from public.current_user_sub_trades();"
       ```
       Expect: `0` rows, no `42P01` / `42883` error.
    4. Confirm the `completions.step_ack_trace` column:
       ```
       npx supabase db remote query "select column_name, data_type, column_default from information_schema.columns where table_name='completions' and column_name='step_ack_trace';"
       ```
       Expect: one row, `jsonb`, default `'[]'::jsonb`.
    5. Confirm the new RLS policy is registered:
       ```
       npx supabase db remote query "select policyname from pg_policies where tablename='sops' and policyname='sops_visible_by_sub_trade';"
       ```
       Expect: 1 row.

    **Mirrors Phase 14-01 Task 6 pattern** — schema MUST be live in Postgres before Wave 3 builds the voice route (which reads sub-trade gates via RLS) and before Wave 4 builds the admin UI. Do not start Wave 2/3/4 until this checkpoint resumes.
  </how-to-verify>
  <resume-signal>Type "applied" with the seed-row count and helper-call output, OR describe failures so we can fix the migration before Wave 2/3/4 spin up.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Manually extend src/types/database.types.ts + add SubTrade / AckTraceEntry / VoiceQueryResponse types to src/types/sop.ts</name>
  <files>src/types/database.types.ts, src/types/sop.ts</files>
  <read_first>
    - `15-PATTERNS.md` lines 713-723 (manual extension rule; never auto-regen)
    - `CLAUDE.md` Learnings 2026-05-08 (database.types.ts manual)
    - `src/types/database.types.ts` — locate `Database['public']['Tables']` and `Database['public']['Functions']` to extend
    - `src/types/sop.ts` (entire file) — additive imports + types pattern
  </read_first>
  <action>
    **`src/types/database.types.ts`** — manual extension only. Add to `Database['public']['Tables']`:
    1. `sub_trades` with Row/Insert/Update shapes: `id: string; slug: string; label: string; sort_order: number; created_at: string`
    2. `users_sub_trades` with Row/Insert/Update: `user_id: string; sub_trade_id: string; assigned_at: string; assigned_by: string | null` (primary key (user_id, sub_trade_id))
    3. `sops_sub_trades` with Row/Insert/Update: `sop_id: string; sub_trade_id: string`

    Extend the existing `completions` Row/Insert/Update: add `step_ack_trace: Json` (Insert/Update: `step_ack_trace?: Json | null`).

    Add to `Database['public']['Functions']`:
    - `current_user_sub_trades: { Args: Record<string, never>; Returns: string[] }`
    - `sub_trade_id_intersects: { Args: { p_sop_id: string }; Returns: boolean }`

    **`src/types/sop.ts`** — add additive types (do NOT modify existing shapes):
    ```ts
    export interface SubTrade {
      id: string
      slug: 'operator' | 'fitter' | 'sparky' | 'maintainer' | 'other' | string
      label: string
      sort_order: number
    }

    export type AckTraceEntry = { stepId: string; timestamp: number }

    // Used by the Wave 3 voice route response
    import type { VerificationFlag } from '@/lib/parsers/verify-sop'
    export interface VoiceQueryResponse {
      answer: string
      citations: string[]
      verifier_flags: VerificationFlag[]
    }
    ```

    Run `npx tsc --noEmit` to confirm no type errors are introduced.
  </action>
  <acceptance_criteria>
    - `Select-String -Path src/types/database.types.ts -Pattern "sub_trades:" | Measure-Object | %{$_.Count -ge 1}`
    - `Select-String -Path src/types/database.types.ts -Pattern "users_sub_trades:" | Measure-Object | %{$_.Count -ge 1}`
    - `Select-String -Path src/types/database.types.ts -Pattern "sops_sub_trades:" | Measure-Object | %{$_.Count -ge 1}`
    - `Select-String -Path src/types/database.types.ts -Pattern "step_ack_trace" | Measure-Object | %{$_.Count -ge 1}`
    - `Select-String -Path src/types/database.types.ts -Pattern "current_user_sub_trades" | Measure-Object | %{$_.Count -ge 1}`
    - `Select-String -Path src/types/sop.ts -Pattern "export interface SubTrade" | Measure-Object | %{$_.Count -eq 1}`
    - `Select-String -Path src/types/sop.ts -Pattern "AckTraceEntry","VoiceQueryResponse" | Measure-Object | %{$_.Count -ge 2}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Database types include three new tables + step_ack_trace column + two function signatures. `SubTrade`, `AckTraceEntry`, `VoiceQueryResponse` exported from `src/types/sop.ts`. Project compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Zod validators (voice-query.ts + sub-trades.ts) + write useViewport hook + extend walkthrough store</name>
  <files>src/lib/validators/voice-query.ts, src/lib/validators/sub-trades.ts, src/hooks/useViewport.ts, src/hooks/__tests__/useViewport.test.ts, src/stores/walkthrough.ts</files>
  <read_first>
    - `15-RESEARCH.md` lines 155-178 (Pattern 1 — useViewport, copy verbatim)
    - `15-RESEARCH.md` lines 425-462 (Pattern 7 — walkthrough store extension)
    - `15-PATTERNS.md` lines 462-493 (validator analog — `aiPromptSchema` shape)
    - `15-PATTERNS.md` lines 642-682 (existing `useWalkthroughStore` shape — mirror `markStepComplete`)
    - `src/stores/walkthrough.ts` (entire file)
    - `src/lib/validators/sop.ts` lines 159-168 (`aiPromptSchema` analog)
  </read_first>
  <behavior>
    Tests for `useViewport` (`src/hooks/__tests__/useViewport.test.ts` — vitest or Playwright component test, choose whichever the repo already has wired):
    - Test 1: initial render returns `'mobile'` (matches SSR per D-04)
    - Test 2: after mount, on a window where `matchMedia('(min-width: 1024px)')` reports `matches: true`, hook returns `'desktop'`
    - Test 3: changing `matches` (simulated `change` event) re-renders with the new value
    - Test 4: hook does NOT touch `window` during initial render (assert via mocking `window` undefined during the first React render call)

    Tests for `useWalkthroughStore` extension:
    - Test 5: `markStepAcknowledged('sop-a', 'step-1')` appends `{ stepId: 'step-1', timestamp: <now> }` to `ackTrace['sop-a']`
    - Test 6: calling `markStepAcknowledged('sop-a', 'step-1')` twice does NOT duplicate (dedupe by stepId — mirror `markStepComplete`)
    - Test 7: `getHighestAckIndex('sop-a', ['step-1','step-2','step-3'])` returns `0` after acking step-1, `1` after acking step-2, `-1` when nothing acked
    - Test 8: `getAckTrace('sop-a')` returns the ordered array; `getAckTrace('sop-b')` returns `[]`

    If the project has no existing vitest config (it likely doesn't — package.json shows Playwright only), wire these as Playwright component tests under `tests/integration/use-viewport.spec.ts` and `tests/integration/walkthrough-store-ack.spec.ts` using Playwright's `expect(page.evaluate(...))` pattern to instantiate the hook/store in a sandbox page. Cheaper alternative: use `npx playwright test` with `@playwright/experimental-ct-react` if installed, else a small Node-side test using `react-test-renderer` invoked via tsx. Pick whichever exists in package.json.
  </behavior>
  <action>
    **`src/lib/validators/voice-query.ts`** (new):
    ```ts
    import { z } from 'zod'
    import type { VerificationFlag } from '@/lib/parsers/verify-sop'

    export const voiceQuerySchema = z.object({
      sopId: z.string().uuid(),
      question: z.string().min(5, 'Question must be at least 5 characters').max(500, 'Question must be under 500 characters'),
    })
    export type VoiceQueryInput = z.infer<typeof voiceQuerySchema>

    export type VoiceQueryResponse = {
      answer: string
      citations: string[]
      verifier_flags: VerificationFlag[]
    }
    ```

    **`src/lib/validators/sub-trades.ts`** (new):
    ```ts
    import { z } from 'zod'

    export const subTradeAssignmentSchema = z.object({
      subTradeIds: z.array(z.string().uuid()).max(10, 'Maximum 10 sub-trades per assignment'),
    })
    export type SubTradeAssignmentInput = z.infer<typeof subTradeAssignmentSchema>
    ```

    **`src/hooks/useViewport.ts`** (new) — paste verbatim from `15-RESEARCH.md` lines 155-178 (Pattern 1). D-04 demands initial state `'mobile'` so SSR matches the mobile variant.

    **`src/stores/walkthrough.ts`** (modify) — extend the existing `WalkthroughState` interface per `15-RESEARCH.md` lines 425-434. Implement:
    - `ackTrace: Record<string, AckTraceEntry[]>` (initial `{}`)
    - `markStepAcknowledged(sopId, stepId)` — append `{ stepId, timestamp: Date.now() }`; dedupe by stepId (mirror `markStepComplete` pattern at lines 663-668 in research notes)
    - `getHighestAckIndex(sopId, allStepIds)` — return the highest index `i` such that `allStepIds[i] ∈ ackTrace[sopId]`; return `-1` if none
    - `getAckTrace(sopId)` — return `ackTrace[sopId] ?? []`
    - Reset on `resetWalkthrough(sopId)` if that method exists — clear `ackTrace[sopId]` too
    - **DO NOT** change any existing methods or default state. Mobile walkthrough must remain byte-identical (SPEC constraint #1).

    Import `AckTraceEntry` from `@/types/sop` (added in Task 3).

    Write the tests described in `<behavior>` — Playwright integration tests under `tests/integration/use-viewport.spec.ts` (delete the Wave-0 fixme test, replace with real coverage) + new file `tests/integration/walkthrough-store-ack.spec.ts`. Both should be live (not fixme) and must PASS.
  </action>
  <acceptance_criteria>
    - `Select-String -Path src/lib/validators/voice-query.ts -Pattern "z\.string\(\)\.uuid\(\)","min\(5\)","max\(500\)"` returns ≥ 3 matches
    - `Select-String -Path src/lib/validators/sub-trades.ts -Pattern "subTradeAssignmentSchema"` returns ≥ 1 match
    - `Select-String -Path src/hooks/useViewport.ts -Pattern "matchMedia","\\(min-width: 1024px\\)","'mobile'","'desktop'"` returns ≥ 4 matches
    - `Select-String -Path src/hooks/useViewport.ts -Pattern "useState<'mobile' \| 'desktop'>\\('mobile'\\)"` returns ≥ 1 match (initial = mobile per D-04)
    - `Select-String -Path src/stores/walkthrough.ts -Pattern "markStepAcknowledged","getHighestAckIndex","getAckTrace","ackTrace"` returns ≥ 4 matches
    - `Select-String -Path src/stores/walkthrough.ts -Pattern "markStepComplete"` STILL returns ≥ 1 match (preserved, not removed)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npx playwright test tests/integration/walkthrough-store-ack.spec.ts -x` passes
    - `npx playwright test tests/integration/use-viewport.spec.ts -x` passes
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; if ($?) { npx tsc --noEmit; if ($?) { npx playwright test tests/integration/use-viewport.spec.ts tests/integration/walkthrough-store-ack.spec.ts -x } }</automated>
  </verify>
  <done>Two validators, the useViewport hook, and the walkthrough store extension all exist with passing tests. Mobile completion behaviour preserved (existing tests still pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker session → `public.sops` RLS | Client claim of sub-trade tags is never trusted; helper `current_user_sub_trades()` resolves from `auth.uid()` |
| Worker session → `public.users_sub_trades` | Self-reads allowed; admin-reads allowed via `organisation_members` join; writes denied to authenticated (server actions use admin client OR `requireAdmin()` ctx) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | `users.role` ⊕ sub-trade tags | mitigate | Sub-trade tags are RLS-gated via SECURITY DEFINER `current_user_sub_trades()`; client cannot forge tags. Server actions in Wave 4 enforce `requireAdmin()` for writes. |
| T-15-01-02 | Information Disclosure | RLS bypass via direct REST API | mitigate | `users_sub_trades_self_read` policy restricts SELECT to self + same-org admin/safety_manager. No anonymous policy. `sub_trades` is read-all (vocabulary is not sensitive). |
| T-15-01-03 | Tampering | SQL function body references `users_sub_trades` by name | mitigate | Per CLAUDE.md 2026-05-08, function comment includes the tripwire warning. Any future rename migration MUST `create or replace function current_user_sub_trades()`. |
| T-15-01-04 | Elevation of Privilege | SECURITY DEFINER helper leaks privileges | mitigate | `current_user_sub_trades()` body uses `auth.uid()` (caller's identity), not service_role; `set search_path = public` prevents search-path attacks. Pattern matches `is_summit_admin()` Phase 13 precedent. |
| T-15-01-05 | DoS | Unbounded sub-trade assignment | mitigate | Zod `subTradeAssignmentSchema` caps `subTradeIds.length` at 10. Junction PK prevents duplicate rows. |
| T-15-01-06 | Tampering | Backward-compat regression — empty `sops_sub_trades` should mean "all workers" | mitigate | Policy `using (not exists (...) or sub_trade_id_intersects(...))` short-circuits to visible when no rows exist. Tested explicitly via `tests/integration/sub-trade-rls-backward-compat.spec.ts` in Wave 0 scaffolds. |
</threat_model>

<verification>
- Migration 00030 applied to Supabase dev; `select count(*) from public.sub_trades` returns 5; `select * from public.current_user_sub_trades()` runs without error
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- `npx playwright test tests/integration/use-viewport.spec.ts tests/integration/walkthrough-store-ack.spec.ts -x` passes
- `npx playwright test tests/integration/sub-trade-rls-backward-compat.spec.ts -x` passes (backward-compat test for SB-LINE-05 — workers without sub-trade tags still see SOPs with no `sops_sub_trades` rows)
- Mobile worker walkthrough behaviour unchanged — sanity check by manually loading any existing SOP at 390×844 viewport and confirming the immersive step card still renders (no Phase-12.5 regression)
</verification>

<success_criteria>
- All 6 truths in `must_haves` verified
- Wave 2 can `import { useViewport } from '@/hooks/useViewport'` and `import { useWalkthroughStore } from '@/stores/walkthrough'` with the new methods
- Wave 3 can `import { voiceQuerySchema } from '@/lib/validators/voice-query'` and `import type { SubTrade, AckTraceEntry, VoiceQueryResponse } from '@/types/sop'`
- Wave 4 can write server actions referencing `users_sub_trades` and `sops_sub_trades` (tables exist)
- RLS backward compat verified — a Phase-1-14 worker with no sub-trade tags can still see Phase-1-14 SOPs with no `sops_sub_trades` rows
</success_criteria>

<output>
After completion, create `.planning/phases/15-manufacturing-line-mode/15-01-SUMMARY.md` recording:
- Confirmation that migration 00030 was applied (date + Supabase env)
- Output of `select slug, label from public.sub_trades order by sort_order` (5 rows)
- Any deviations from RESEARCH.md migration body (should be none)
- The 4 new files + the 1 modified file in this plan
</output>
