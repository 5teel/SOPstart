---
phase: 29-approval-chains
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00045_approval_chains.sql
  - scripts/apply-phase29-migration.mjs
  - database.types.ts
  - src/lib/governance/approvals.ts
  - src/lib/governance/__tests__/approvals.test.ts
  - src/lib/validators/approvals.ts
  - src/lib/governance/publish-core.ts
  - src/app/api/sops/[sopId]/publish/route.ts
  - playwright.config.ts
  - tests/phase29/approval-schema.spec.ts
  - tests/phase29/publish-core-extraction.spec.ts
autonomous: true
requirements: [APR-01, APR-02, APR-04]
user_setup: []

must_haves:
  truths:
    - "approval_chains + sop_approvals tables and sops.approval_state/approval_snapshot columns exist live in the remote DB (verified via to_regclass, not PostgREST)"
    - "A no-chain SOP publish produces the byte-identical response + gate order it did before Phase 29 (performPublish is the same code, just relocated)"
    - "resolveNextStepIndex treats ONLY action='approved' rows as done; stepMatchesCaller matches on userId OR role"
  artifacts:
    - path: "supabase/migrations/00045_approval_chains.sql"
      provides: "additive sops columns + approval_chains (current_organisation_id RLS) + sop_approvals (partial unique index where action='approved')"
      contains: "current_organisation_id"
    - path: "src/lib/governance/approvals.ts"
      provides: "pure chain-progression logic"
      exports: ["resolveNextStepIndex", "stepMatchesCaller", "isChainComplete", "ChainStep"]
    - path: "src/lib/governance/publish-core.ts"
      provides: "performPublish() — single source of publish truth"
      exports: ["performPublish"]
    - path: "src/lib/validators/approvals.ts"
      provides: "Zod schemas for chain step + chain (1-4 steps, exactly one of role/userId)"
      exports: ["chainStepSchema", "approvalChainSchema"]
  key_links:
    - from: "src/app/api/sops/[sopId]/publish/route.ts"
      to: "src/lib/governance/publish-core.ts"
      via: "route delegates steps 2-5 to performPublish()"
      pattern: "performPublish\\("
---

<objective>
Lay the Phase 29 foundation: the 00045 migration (pushed live, unattended), the pure chain-progression logic module, its Zod validators, and the `performPublish()` extraction that makes APR-01's "byte-identical no-chain publish" and APR-04's "same publish logic completes automatically" both literally true.

Purpose: Every later plan hangs off these three artifacts — the schema, the pure resolver, and the shared publish function. This is the riskiest refactor (relocating a live publish route), so it lands first with byte-identical regression cover.
Output: Migration + apply script + extended `database.types.ts`; `src/lib/governance/approvals.ts` (+ unit test); `src/lib/validators/approvals.ts`; `src/lib/governance/publish-core.ts`; a route rewired to delegate; `phase29`/`phase29-unit` Playwright projects registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/phases/29-approval-chains/29-PATTERNS.md
@.planning/phases/29-approval-chains/29-VALIDATION.md

# Exact analog files to copy shapes from
@supabase/migrations/00043_ownership_review_governance.sql
@supabase/migrations/00044_fix_cadence_rls_org_claim.sql
@scripts/apply-phase23-migration.mjs
@src/lib/governance/classify.ts
@src/app/api/sops/[sopId]/publish/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 00045 + unattended live push + database.types.ts</name>
  <read_first>
    - supabase/migrations/00043_ownership_review_governance.sql (settings-table + append-only shapes)
    - supabase/migrations/00044_fix_cadence_rls_org_claim.sql (the CORRECT current_organisation_id() predicate)
    - scripts/apply-phase23-migration.mjs (db push + Management API to_regclass verification pattern)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 1 (full migration skeleton)
  </read_first>
  <files>supabase/migrations/00045_approval_chains.sql, scripts/apply-phase29-migration.mjs, database.types.ts</files>
  <action>
    Write `supabase/migrations/00045_approval_chains.sql` per D29-01/D29-02/D29-03 exactly as the 29-PATTERNS.md §1 skeleton:
    (1) `alter table public.sops add column if not exists approval_state text check (approval_state in ('pending','approved')), add column if not exists approval_snapshot jsonb;` — NO new RLS policy (rides admins_can_update_sops / org_members_can_view_sops from 00003; do NOT touch the SopStatus enum).
    (2) `approval_chains(organisation_id, category, steps jsonb, created_by, created_at, updated_at)` PK `(organisation_id, category)`, RLS enabled, SELECT policy `using (organisation_id = public.current_organisation_id())` — use the 00044 predicate directly; any `app_metadata` text is the HR-01 bug signature (RESEARCH Pitfall 2). NO authenticated INSERT/UPDATE/DELETE policy (service-role writes only).
    (3) `sop_approvals(id, sop_id, organisation_id, version, step_index, approver_user_id, action check in ('approved','changes_requested'), comment, created_at)` with `sop_approvals_sop_id_idx`, RLS enabled, SELECT policy org-scoped via current_organisation_id(), INSERT policy `with check (organisation_id = public.current_organisation_id() and approver_user_id = auth.uid())`, NO UPDATE/DELETE. Idempotency guard MUST be the PARTIAL unique index `create unique index if not exists sop_approvals_one_approval_per_step on public.sop_approvals(sop_id, version, step_index) where action = 'approved';` — a blanket unique(sop_id,version,step_index) breaks multi-cycle reject/resubmit (RESEARCH Pitfall 4).
    Write `scripts/apply-phase29-migration.mjs` by copying `apply-phase23-migration.mjs` and repointing it at 00045: run `npx supabase db push`, then verify via the Management API `to_regclass('public.approval_chains')` / `to_regclass('public.sop_approvals')` and `information_schema.columns` for `sops.approval_state`/`approval_snapshot` (NOT PostgREST `.from().select()` — PGRST205 ≠ 42P01, per the schema-cache learning), then `NOTIFY pgrst, 'reload schema'`. Run it UNATTENDED (Simon's autonomous mandate; SUPABASE_ACCESS_TOKEN is already in .env.local from 13-01/28). If `db push` lacks a password, fall back to the Management API `POST /v1/projects/{ref}/database/query` raw-SQL path the script already exposes to apply the migration text directly. Supabase records applied migrations in `supabase_migrations.schema_migrations` — do not hand-edit that table.
    Extend `database.types.ts` manually (type regen unavailable in this env, same as 28): add `approval_state: string | null` and `approval_snapshot: Json | null` to the `sops` Row/Insert/Update. Leave `approval_chains`/`sop_approvals` accessed via `(supabase as any)` (documented convention).
  </action>
  <verify>
    <automated>node scripts/apply-phase29-migration.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/apply-phase29-migration.mjs` reports approval_chains + sop_approvals present (to_regclass non-null) and sops.approval_state + approval_snapshot columns present.
    - Migration text contains `current_organisation_id` and contains NO `app_metadata`.
    - Migration contains the partial index line with `where action = 'approved'`.
    - `npx tsc --noEmit` clean after database.types.ts edit.
  </acceptance_criteria>
  <done>00045 is live in the remote DB (tables + columns verified via to_regclass), the apply script is committed, and database.types.ts carries the two new sops columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure approvals.ts + Zod validators + phase29 registration + unit tests</name>
  <read_first>
    - src/lib/governance/classify.ts (pure-module extraction discipline — no 'use server', sync exports)
    - src/lib/governance/__tests__/classify.test.ts (phase28-unit test shape to mirror)
    - playwright.config.ts (phase28 / phase28-unit registration block, ~line 253-270)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 2 and § 10
    - src/types/auth.ts (AppRole)
  </read_first>
  <files>src/lib/governance/approvals.ts, src/lib/validators/approvals.ts, src/lib/governance/__tests__/approvals.test.ts, playwright.config.ts, tests/phase29/approval-schema.spec.ts</files>
  <behavior>
    - resolveNextStepIndex(3, {0,1}) → 2; resolveNextStepIndex(3, {0,1,2}) → -1 (complete).
    - resolveNextStepIndex only counts approved indexes — a set built from action='approved' rows; a changes_requested row must NEVER mark a step done (asserted at the action layer in 29-02, but the resolver's contract is "given the approved set").
    - stepMatchesCaller matches when step.userId === caller.userId OR step.role === caller.role; rejects when neither matches.
    - isChainComplete(n, set) === (resolveNextStepIndex === -1).
    - approvalChainSchema: steps array length 1-4; each step has EXACTLY one of role/userId set (never both, never neither) + non-empty label; role restricted to admin|safety_manager (Pitfall 3 scoping).
  </behavior>
  <action>
    Create `src/lib/governance/approvals.ts` exporting the `ChainStep` interface and pure `resolveNextStepIndex(stepCount, approvedStepIndexes: Set<number>)`, `stepMatchesCaller(step, caller)`, `isChainComplete(stepCount, approvedStepIndexes)` verbatim from 29-PATTERNS.md §2 — NO 'use server', no I/O, no supabase import (2026-06-27 sync-export learning).
    Create `src/lib/validators/approvals.ts` with `chainStepSchema` (refine: exactly one of role/userId; role ∈ {'admin','safety_manager'}; label min 1) and `approvalChainSchema` (steps array min 1 max 4).
    Register both Playwright projects in `playwright.config.ts` alongside the phase28 entries: `phase29-unit` (`testDir: './src/lib/governance/__tests__'`, `testMatch: /.*\.test\.ts$/`) and `phase29` (`testDir: '.'`, `testMatch: /tests\/phase29\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`). NOTE: `phase29-unit` shares `./src/lib/governance/__tests__` with phase28-unit's classify.test.ts — scope phase29-unit's testMatch to `/approvals\.test\.ts$/` so the two projects don't double-run each other's files.
    Create `src/lib/governance/__tests__/approvals.test.ts` (phase29-unit) with static `@/` imports covering the behaviors above.
    Create `tests/phase29/approval-schema.spec.ts` (phase29) — source-contract: `fs.readFileSync` the migration, assert it contains `current_organisation_id`, does NOT contain `app_metadata`, and contains `where action = 'approved'` (the partial-index proof for APR-01/APR-02).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase29 --project=phase29-unit && npx playwright test --project=phase29-unit --project=phase29 -g "approval_chains RLS|resolveNextStepIndex|stepMatchesCaller|snapshot"</automated>
  </verify>
  <acceptance_criteria>
    - `--list` shows both phase29 and phase29-unit projects with the new files.
    - Unit tests for resolveNextStepIndex / stepMatchesCaller / isChainComplete pass.
    - approval-schema.spec.ts passes (RLS predicate + partial-index assertions).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Pure chain logic + validators exist and are unit-tested; both phase29 Playwright projects are registered and discoverable.</done>
</task>

<task type="auto">
  <name>Task 3: Extract performPublish() + rewire publish route + byte-identical proof</name>
  <read_first>
    - src/app/api/sops/[sopId]/publish/route.ts (the FULL current flow — steps 1-5; the exact extraction target)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 4 (before/after side-by-side)
    - .planning/phases/29-approval-chains/29-RESEARCH.md § Pattern 4 + Pitfall 6
  </read_first>
  <files>src/lib/governance/publish-core.ts, src/app/api/sops/[sopId]/publish/route.ts, tests/phase29/publish-core-extraction.spec.ts</files>
  <action>
    Create `src/lib/governance/publish-core.ts` exporting `async function performPublish(supabase, { sopId, organisationId, userId, approvalState? }: { sopId: string; organisationId: string; userId: string; approvalState?: 'approved' })` returning `{ success: true; pipelineAutoQueued: boolean } | { success: false; error: string; status: number }`. Move the route's steps 2, 2b, 3, 3b, 4, 5 into it VERBATIM (unapproved-sections gate → verify-checklist gate → status→published UPDATE → review-clock reset + superseded event → video auto-queue → agent synthesis). ALSO export `async function assertPublishGates(supabase, sopId): Promise<{ ok: true } | { ok: false; error: string; status: number; count?: number }>` — factor the unapproved-sections + unverified_blocks checks into it and make performPublish call it as its first step (behavior identical, just factored). This export exists so 29-02's chain-gate can run the SAME gates BEFORE diverting an SOP into pending_approval (locked D29-03 ordering — plan-checker Blocker 1 fix). The ONLY functional change: the status→published UPDATE also sets `approval_state: params.approvalState` when provided (so the final-approval caller stamps 'approved' in the same statement); when `approvalState` is undefined the UPDATE is byte-equivalent to today. This is a plain module (not 'use server') — sync/async exports both fine here, but keep it importable by both the route and 29-02's approveStep.
    Rewire `route.ts` to keep step 1 (auth + org resolution) unchanged, then call `const result = await performPublish(supabase, { sopId, organisationId, userId: user.id })` and map `result` to the existing `NextResponse` shapes (`{ success: true, pipelineAutoQueued }` on success; `{ error }` with `result.status` on failure). Do NOT add the chain-gate here yet — that lands in 29-02 (this task proves the no-chain path is untouched first). Preserve the 409 "SOP is not a draft" short-circuit behavior inside performPublish.
    Create `tests/phase29/publish-core-extraction.spec.ts` (phase29) — source-contract proving: route.ts imports + calls `performPublish(`; publish-core.ts contains the unapproved-sections gate (`.eq('approved', false)`), the `unverified_blocks` gate string, the `status: 'published'` UPDATE, `triggerAgentSynthesis(`, and `enqueueVideoGenerationForPipeline(` — i.e. all five relocated behaviors survived. Assert the no-chain route body does NOT branch on `approval_chains` (that comes in 29-02).
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "performPublish shared|no-chain publish unchanged" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Route imports and calls performPublish; publish-core.ts contains all five relocated gate/side-effect behaviors.
    - Source-contract spec confirms no `approval_chains` branch in the route yet.
    - `npx tsc --noEmit` clean AND `npm run build` clean (route touches src/actions-adjacent + build-graph — 2026-06-27 async-only-export gate applies; full next build, not just tsc).
    - Bundle gate unchanged (server route, no worker bundle impact).
  </acceptance_criteria>
  <done>performPublish is the single relocated source of publish truth; the no-chain publish path is byte-identical and proven so; the route is a thin caller.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → Postgres (RLS) | new tables must not leak or accept cross-tenant rows |
| migration push tooling → remote DB | service-role/access-token credentials cross into a privileged apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Information Disclosure | approval_chains / sop_approvals SELECT | mitigate | RLS SELECT `using (organisation_id = public.current_organisation_id())` — the 00044-correct predicate, never app_metadata (Pitfall 2) |
| T-29-01-02 | Tampering | sop_approvals INSERT | mitigate | INSERT `with check (organisation_id = current_organisation_id() and approver_user_id = auth.uid())`; no UPDATE/DELETE (append-only) |
| T-29-01-03 | Tampering | approval_chains writes | mitigate | NO authenticated write policy — service-role only (write path lands in 29-02, org self-enforced from JWT) |
| T-29-01-SC | Tampering | npm installs | accept | Zero new dependencies this phase (dnd-kit already installed Phase 26); no package legitimacy gate needed |
</threat_model>

<verification>
- `node scripts/apply-phase29-migration.mjs` — tables/columns live (to_regclass, not PostgREST).
- `npx playwright test --project=phase29-unit --project=phase29` green.
- `npx tsc --noEmit` + `npm run build` clean.
- phase28 regression untouched (no phase28 file modified here).
</verification>

<success_criteria>
Schema is live and org-scoped; pure chain logic + validators exist and are unit-tested; `performPublish()` is extracted with the no-chain path proven byte-identical; both phase29 Playwright projects registered.
</success_criteria>

<artifacts_produced>
- `supabase/migrations/00045_approval_chains.sql` (live) + `scripts/apply-phase29-migration.mjs`
- `database.types.ts` extended (sops.approval_state / approval_snapshot)
- `src/lib/governance/approvals.ts` (resolveNextStepIndex, stepMatchesCaller, isChainComplete, ChainStep)
- `src/lib/validators/approvals.ts` (chainStepSchema, approvalChainSchema)
- `src/lib/governance/publish-core.ts` (performPublish)
- `src/app/api/sops/[sopId]/publish/route.ts` (thin caller — no chain gate yet)
- `playwright.config.ts` (phase29 + phase29-unit registered)
- `tests/phase29/approval-schema.spec.ts`, `tests/phase29/publish-core-extraction.spec.ts`, `src/lib/governance/__tests__/approvals.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-01-SUMMARY.md` when done.
</output>
