---
phase: 29-approval-chains
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - src/components/admin/governance/ApprovalChainEditor.tsx
  - src/app/(protected)/admin/governance/page.tsx
  - tests/phase29/approval-chain-editor.spec.ts
autonomous: true
requirements: [APR-01]
user_setup: []

must_haves:
  truths:
    - "Admin can pick a category and define 1-4 ordered approver steps (each a role OR a named admin/safety_manager member) on the /admin/governance page — no new route"
    - "Saving calls setApprovalChain(category, steps); existing chains render for edit"
    - "Step rows reorder by drag (dnd-kit, already installed); role/member pickers offer only admin + safety_manager (Pitfall 3)"
  artifacts:
    - path: "src/components/admin/governance/ApprovalChainEditor.tsx"
      provides: "category picker + 1-4 step drag-order editor, wired to setApprovalChain"
      min_lines: 60
    - path: "src/app/(protected)/admin/governance/page.tsx"
      provides: "Approval chains panel mounted on the existing governance page"
      contains: "ApprovalChainEditor"
  key_links:
    - from: "src/components/admin/governance/ApprovalChainEditor.tsx"
      to: "src/actions/approvals.ts"
      via: "save button calls setApprovalChain"
      pattern: "setApprovalChain\\("
    - from: "src/app/(protected)/admin/governance/page.tsx"
      to: "src/actions/approvals.ts"
      via: "getApprovalChains fetch for existing chains"
      pattern: "getApprovalChains\\("
---

<objective>
Add the "Approval chains" configuration panel to the existing `/admin/governance` page: a category picker plus a 1-4 step drag-order editor where each step targets a role OR a named member, wired to `setApprovalChain`.

Purpose: APR-01's admin-facing half — the opt-in-per-category chain definition. No new route, no wizard (D29-05): one panel on the page admins already visit.
Output: `ApprovalChainEditor.tsx` (dnd-kit reorder, role/member pickers scoped to admin/safety_manager) mounted on `governance/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/phases/29-approval-chains/29-PATTERNS.md

# Analog files / design language
@src/app/(protected)/admin/governance/page.tsx
@src/components/admin/governance/OwnerPicker.tsx
@src/components/admin/governance/GovernanceFilterChips.tsx
@src/actions/approvals.ts
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApprovalChainEditor.tsx — category picker + 1-4 step dnd-kit editor</name>
  <read_first>
    - src/components/admin/governance/OwnerPicker.tsx (open/fetch-on-first-open/commit popover shape + getOrgMembers reuse + inline error surfacing + router.refresh)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 5
    - src/components/sop/... any existing DndContext/SortableContext/useSortable usage from Phase 26 block canvas (grep `useSortable` to find the installed dnd-kit idiom)
    - .claude/skills/sketch-findings-SOPstart/SKILL.md (paper/ink tokens: `pill`, `blueprint-frame`, `mono`, `evidence-btn`, `--ink-*`, `--accent-signoff`)
  </read_first>
  <files>src/components/admin/governance/ApprovalChainEditor.tsx</files>
  <action>
    Create a client component `ApprovalChainEditor` taking props `{ categories: string[]; members: {user_id; role; label}[]; chains: Record<category, ChainStep[]> }` (all fetched server-side by the page — presentational + local edit state, no internal data fetch beyond the save action call). Render:
    - A category `<select>` from `categories` (distinct sops.category values for the org). Selecting a category loads its existing steps from `chains` into local state (empty = new chain).
    - 1-4 step rows, each a role-OR-member picker: a small `<select>` toggling role vs member, then either a role `<select>` of `['admin','safety_manager']` ONLY (Pitfall 3) or a member `<select>` filtered to members whose role ∈ {admin, safety_manager}, plus a free-text `label`. Enforce exactly-one-of role/userId per row (mirror the Zod refine).
    - Drag-reorder via `@dnd-kit/core` + `@dnd-kit/sortable` (`DndContext`/`SortableContext`/`useSortable`) — already installed Phase 26, do NOT hand-roll drag logic. Add/remove-step buttons bounded to 1..4.
    - A "Save chain" button calling `setApprovalChain(category, steps)` inside a `useTransition`, surfacing `{ error }` inline (never swallowed), `router.refresh()` on success. All onClick handlers WIRED to the action (2026-06-05 — no empty handlers).
    Use paper/ink tokens (`blueprint-frame`, `mono`, `evidence-btn`, `--ink-*`). Keep it ONE panel, no wizard.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Component imports `@dnd-kit/*` and `useSortable`; role/member pickers only offer admin/safety_manager.
    - Save button calls `setApprovalChain(` inside a transition and surfaces errors inline.
    - Step count bounded 1..4; exactly-one-of role/userId enforced per row.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A self-contained, drag-orderable 1-4 step chain editor exists, wired to setApprovalChain, scoped to admin/safety_manager approvers.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the editor on /admin/governance + source-contract spec</name>
  <read_first>
    - src/app/(protected)/admin/governance/page.tsx (RSC page: auth guard, listGovernanceQueue, filter chips, row list — mount point is below the queue)
    - src/actions/approvals.ts (getApprovalChains)
    - src/actions/assignments.ts (getOrgMembers — reuse for member labels)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 5
  </read_first>
  <files>src/app/(protected)/admin/governance/page.tsx, tests/phase29/approval-chain-editor.spec.ts</files>
  <action>
    In `governance/page.tsx` (RSC), after the queue list, fetch server-side: distinct `sops.category` values for the org (simple `.select('category')` distinct/dedupe in JS — no new table), `getApprovalChains()`, and `getOrgMembers()` (filter to admin/safety_manager for the member options; reuse — third call site). Render an "Approval chains" section (`pill` + `mono` heading matching the page's existing header idiom) containing `<ApprovalChainEditor categories={...} members={...} chains={...} />`. Do NOT add a new route (D29-05) and do NOT change the existing queue/filter behavior. Since no route is added, journeys.ts needs no edit (confirm /pathways still shows 0 not-mapped for /admin/governance).
    Create `tests/phase29/approval-chain-editor.spec.ts` (phase29) — source-contract: page imports + renders `<ApprovalChainEditor`, calls `getApprovalChains(`; editor file calls `setApprovalChain(` and imports `@dnd-kit`; role options limited to admin/safety_manager (grep the role `<select>` options).
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "approval chain editor|chain config" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Governance page mounts ApprovalChainEditor with server-fetched categories/members/chains; queue behavior unchanged.
    - Source-contract spec green; no new route added.
    - `npx tsc --noEmit` + `npm run build` clean.
  </acceptance_criteria>
  <done>Admins configure per-category chains from the existing governance page; the panel reads existing chains and saves new ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → setApprovalChain | client-chosen category/steps must be Zod-validated + org-scoped server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Elevation of Privilege | non-admin reaching the editor | mitigate | /admin/governance already hard-redirects non-admin/safety_manager (existing guard, unchanged) |
| T-29-03-02 | Input Validation | malformed/oversized chain | mitigate | server-side approvalChainSchema (1-4 steps, exactly one of role/userId, role ∈ admin/safety_manager) enforced in setApprovalChain (29-02) |
| T-29-03-SC | Tampering | npm installs | accept | zero new dependencies (dnd-kit already installed) |
</threat_model>

<verification>
- `npx playwright test --project=phase29` green.
- `npx tsc --noEmit` + `npm run build` clean; bundle gate unaffected (admin-only surface).
- `/pathways` shows 0 not-mapped for /admin/governance (no new route).
</verification>

<success_criteria>
Admin can define, reorder, and save a 1-4 step per-category approval chain from the existing governance page, with approvers scoped to admin/safety_manager.
</success_criteria>

<artifacts_produced>
- `src/components/admin/governance/ApprovalChainEditor.tsx`
- `src/app/(protected)/admin/governance/page.tsx` (Approval chains panel)
- `tests/phase29/approval-chain-editor.spec.ts`
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-03-SUMMARY.md` when done.
</output>
