---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - src/actions/governance.ts
  - src/app/api/sops/[sopId]/publish/route.ts
  - tests/phase28/governance-actions.spec.ts
autonomous: true
requirements: [OWN-02, OWN-03, REV-01, REV-04, GQ-01, GQ-02, GQ-03]
must_haves:
  truths:
    - "setSopOwner verifies the target user is a member of the caller's org before writing, via plain session client on RLS-gated sops (OWN-02)"
    - "confirmSopCurrent sets last_reviewed_at=now, review_due_at=now+cadence, last_reviewed_by=caller AND inserts a sop_review_events 'confirmed_current' row (REV-04)"
    - "setReviewCadence writes sop_review_cadences via service-role client with org id sourced ONLY from JWT (REV-01)"
    - "listGovernanceQueue composes sops + organisation_members + sop_departments→departments in one read and maps each row through classifyGovernanceRow (GQ-01/GQ-03)"
    - "Publishing a superseding version (parent_sop_id set) resets review_due_at + last_reviewed_at and appends a 'superseded' review event (REV-04/D28-04)"
  artifacts:
    - path: "src/actions/governance.ts"
      provides: "setSopOwner, confirmSopCurrent, setReviewCadence, listGovernanceQueue server actions"
      exports: ["setSopOwner", "confirmSopCurrent", "setReviewCadence", "listGovernanceQueue"]
    - path: "src/app/api/sops/[sopId]/publish/route.ts"
      provides: "Review-clock reset + superseded event on publish"
      contains: "review_due_at"
    - path: "tests/phase28/governance-actions.spec.ts"
      provides: "Source-contract coverage of org-scope + wiring + append-only event"
      contains: "setSopOwner"
  key_links:
    - from: "src/actions/governance.ts::setSopOwner"
      to: "organisation_members"
      via: "membership verify before UPDATE sops (setDepartmentOwner mirror)"
      pattern: "organisation_members"
    - from: "src/actions/governance.ts::listGovernanceQueue"
      to: "src/lib/governance/classify.ts"
      via: "classifyGovernanceRow per row"
      pattern: "classifyGovernanceRow"
    - from: "src/app/api/sops/[sopId]/publish/route.ts"
      to: "sop_review_events"
      via: "insert action='superseded' when parent_sop_id set"
      pattern: "sop_review_events"
---

<objective>
Build the governance server-action layer over the 28-01 schema and 28-02 pure logic: owner
reassignment, one-click confirm-current (with audit event), per-category cadence settings, and the
single composed governance-queue read. Hook the existing publish route so a supersede resets the
review clock.

Purpose: OWN-02 (≤2-click reassign, org-scoped), REV-04 (confirm-current + append-only event +
supersede reset), REV-01 (cadence settings write), GQ-01/GQ-02/GQ-03 (queue data + stale-role
detection). Critical correctness rule (RESEARCH Pitfall 1): `sops` writes use the PLAIN session
client (existing `admins_can_update_sops` RLS gates them) — only `sop_review_cadences` uses the
service-role client. Pure helpers stay imported from `src/lib/governance/*` (never inlined into
this 'use server' file — 2026-06-27).
Output: `src/actions/governance.ts` + publish-route reset + source-contract specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-RESEARCH.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-PATTERNS.md
@src/actions/ai-settings.ts
@src/actions/departments.ts
@src/actions/assignments.ts
@src/app/api/sops/[sopId]/publish/route.ts
@src/lib/supabase/jwt.ts
@tests/phase27/ai-settings-org-scope.spec.ts
</context>

## Artifacts this phase produces (this plan)

- **Actions (`src/actions/governance.ts`, all async):**
  - `setSopOwner(sopId: string, userId: string | null)` → `{ success: true } | { error }`
  - `confirmSopCurrent(sopId: string)` → `{ success: true } | { error }`
  - `setReviewCadence(category: string, months: number)` → `{ success: true } | { error }`
  - `listGovernanceQueue()` → `{ success: true; rows: GovernanceRow[] } | { error }` (each row = SOP meta + `GovernanceFlag[]` + owner display + review_due_at)
  - private `requireAdmin()` (ai-settings shape: `{ userId, organisationId } | { error }`)
- **Type:** `GovernanceRow` (exported for the page + widget in 28-04/28-05)
- **Publish hook:** review-clock reset + `sop_review_events` superseded insert in `publish/route.ts`
- **Spec:** `tests/phase28/governance-actions.spec.ts`

<tasks>

<task type="auto">
  <name>Task 1: governance.ts — owner, confirm-current, cadence actions</name>
  <files>src/actions/governance.ts</files>
  <read_first>
    - src/actions/ai-settings.ts (requireAdmin via parseJwtPayload; service-role write with `(supabase as any)` + org-from-JWT-only for the settings table)
    - src/actions/departments.ts::setDepartmentOwner (membership-verify-before-write block — copy, but use plain session client for the sops UPDATE)
    - src/lib/supabase/jwt.ts (parseJwtPayload — NEVER raw atob; 2026-06-26)
    - src/lib/governance/cadences.ts (resolveCadenceMonths / computeReviewDueDate from 28-02)
    - CLAUDE.md Learnings 2026-06-15 (service-role must self-enforce org), 2026-06-27 ('use server' exports must be async)
  </read_first>
  <action>
    Create `src/actions/governance.ts` starting with `'use server'`. Add private `requireAdmin()` copied verbatim from ai-settings.ts (createClient → getUser → parseJwtPayload(session.access_token) → role in admin/safety_manager → returns `{ userId, organisationId }`).
    `setSopOwner(sopId, userId)` (D28-01/OWN-02): guard sopId; `requireAdmin`; if userId !== null, verify via plain `createClient()` that an `organisation_members` row exists for `(user_id=userId, organisation_id=ctx.organisationId)` — else return `{ error: 'Owner must be a member of this organisation' }` (mirrors setDepartmentOwner; blocks cross-tenant owner assignment T-28-03-01). Then UPDATE `sops` set `owner_user_id = userId` where `id = sopId` using the PLAIN session client (NOT createAdminClient — `admins_can_update_sops` RLS already scopes org+role; Pitfall 1). Return `{ success: true } | { error }`.
    `confirmSopCurrent(sopId)` (D28-04/REV-04): `requireAdmin`; load the sop's `category` + `owner_user_id` (plain client, RLS-scoped); resolve cadence months = `resolveCadenceMonths(category, await fetchOrgCadences(ctx.organisationId))` where `fetchOrgCadences` reads `sop_review_cadences` via `(supabase as any)` into a `Record<category, months>`; `const now = new Date().toISOString()`, `reviewDue = computeReviewDueDate(now, months)`; UPDATE sops set `last_reviewed_at = now, review_due_at = reviewDue, last_reviewed_by = ctx.userId` where id=sopId (plain client); then INSERT `sop_review_events` `{ sop_id, organisation_id: ctx.organisationId, reviewed_by: ctx.userId, action: 'confirmed_current' }` (plain client — the authenticated INSERT policy from 28-01 gates it). Return `{ success: true } | { error }`.
    `setReviewCadence(category, months)` (D28-03/REV-01): `requireAdmin`; validate `months` is an int 1..120 and category is a non-empty string (Zod or inline); write `sop_review_cadences` via `createAdminClient() as any` `.upsert({ organisation_id: ctx.organisationId, category, months, updated_by: ctx.userId, updated_at: now }, { onConflict: 'organisation_id,category' })` — org id sourced ONLY from `ctx.organisationId` (JWT), never a parameter (mirrors setAiModelSetting; T-28-03-02). Return `{ success: true } | { error }`.
    Every export is `async`. All pure math is imported from `src/lib/governance/cadences.ts` — nothing sync is defined-and-exported here (Pitfall 5). Error shape `{ success: true } | { error: string }`, log via `console.error('[fnName] ...', error)`, never throw.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/governance.ts','utf8'); if(!s.startsWith(\"'use server'\")&&!s.trimStart().startsWith(\"'use server'\")){console.error('missing use server');process.exit(1)} const need=['setSopOwner','confirmSopCurrent','setReviewCadence','parseJwtPayload','organisation_members','resolveCadenceMonths','sop_review_events',\"action: 'confirmed_current'\"]; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(s.includes('atob(')){console.error('raw atob forbidden');process.exit(1)} if(/export\s+function\s/.test(s)){console.error('sync export in use-server file');process.exit(1)} console.log('actions OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `setSopOwner` verifies org membership before UPDATE and uses the plain session client (no `createAdminClient` for the sops write).
    - `confirmSopCurrent` writes all three sops fields AND inserts a `confirmed_current` review event.
    - `setReviewCadence` uses `createAdminClient` with org id from JWT only, `onConflict: 'organisation_id,category'`.
    - No raw `atob`; no `export function` (sync) in the file; `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Three write actions implemented with correct client selection + org-scope, tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: listGovernanceQueue composed read + GovernanceRow type</name>
  <files>src/actions/governance.ts</files>
  <read_first>
    - src/lib/governance/classify.ts (GovernanceInput/GovernanceFlag/classifyGovernanceRow)
    - src/app/(protected)/admin/sops/page.tsx (existing sop_departments fetch pattern via `(supabase as any)` — reuse the dept-map approach)
    - src/actions/assignments.ts::getOrgMembers (member list — reuse for owner display, don't hand-roll a second member query)
    - 28-RESEARCH.md § System Architecture Diagram (the LEFT JOIN composition) + Pitfall 3 (departments only, NOT sub_trades)
  </read_first>
  <action>
    Add `export interface GovernanceRow { id, title, category, status, ownerUserId, ownerLabel, reviewDueAt, flags: GovernanceFlag[] }` and `export async function listGovernanceQueue()`.
    Compose in ONE read pass (server component will call this): `requireAdmin`; fetch org `sops` (`id, title, category, status, owner_user_id, review_due_at, last_reviewed_at`) — RLS already scopes to the org; fetch org `organisation_members` (`user_id`) into a Set for active-member lookup (OWN-03/D28-02 — absence ⇒ unowned; a removed member has no row per removeMember hard-delete); fetch `sop_departments` (`sop_id, department_id`) via `(supabase as any)` for the listed sop ids and `departments` (`id, updated_at`) for the org into a Map. For each sop compute the `GovernanceInput`: `ownerIsActiveMember = memberSet.has(owner_user_id)`; `danglingDepartmentRefs = any sop_departments.department_id for this sop that is NOT in the departments Map` (GQ-03 dangling — department deleted/archived); `departmentRenamedSinceReview = sop.last_reviewed_at ? (any still-present tagged department whose updated_at > sop.last_reviewed_at) : false` — NULL-GUARD REQUIRED: `last_reviewed_at` is null for every backfilled/never-confirmed SOP (28-01 only backfills `review_due_at`), and an unguarded timestamp-vs-null comparison would spuriously flag nearly every department-tagged SOP as stale_role on every read (plan-checker WARNING-1; add this null case to the 28-02 unit spec for the classifier input builder). Map through `classifyGovernanceRow`. Build `ownerLabel` from `getOrgMembers()` result (user_id → role/label; owner not-a-member ⇒ label `'No owner'`). Return `{ success: true, rows }`. Scope stale-role to DEPARTMENTS only — do NOT join sub_trades (Pitfall 3: sub_trades have no admin rename path, nothing to detect).
    Keep this a pure composition of the RLS-scoped reads + the 28-02 classifier — no new SECURITY DEFINER RPC (Security Domain: if one were ever added it must derive identity from auth.uid(), never a param — 00041).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/actions/governance.ts','utf8'); const need=['listGovernanceQueue','GovernanceRow','classifyGovernanceRow','organisation_members','sop_departments','departments','danglingDepartmentRefs','departmentRenamedSinceReview']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(/sops_sub_trades|sub_trades/.test(s)){console.error('sub_trades must NOT be joined for stale-role (Pitfall 3)');process.exit(1)} console.log('queue OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `listGovernanceQueue` fetches sops + organisation_members + sop_departments + departments and maps every row through `classifyGovernanceRow`.
    - Unowned computed from membership Set absence (OWN-03); stale_role computed from departments only (no sub_trades join — verify command fails if sub_trades referenced).
    - Returns typed `GovernanceRow[]`; `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Single-query governance read returns classified rows with owner labels; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Publish-route review-clock reset + source-contract specs</name>
  <files>src/app/api/sops/[sopId]/publish/route.ts, tests/phase28/governance-actions.spec.ts</files>
  <read_first>
    - src/app/api/sops/[sopId]/publish/route.ts (step 3 is the single draft→published UPDATE — the correct hook point per RESEARCH Pattern 4; already selects source_type/source_file_path at step 2b — extend that select)
    - src/lib/governance/cadences.ts (resolveCadenceMonths / computeReviewDueDate)
    - tests/phase27/ai-settings-org-scope.spec.ts (source-contract + test.fixme runtime carry convention — mirror for cross-org isolation)
    - CLAUDE.md Learnings 2026-06-05 (source-contract must assert the wired call, not just a token) and 2026-05-25 (spec must be registered — phase28 project already exists from 28-02)
  </read_first>
  <action>
    In `publish/route.ts`: extend the step-2b sop select to also read `category, parent_sop_id`. After the successful step-3 publish UPDATE, add the review-clock reset (D28-04/Pattern 4): resolve cadence months from the org's `sop_review_cadences` for `sopRow.category` (fetch via the same `supabase` client cast to any → Record; `resolveCadenceMonths`), compute `reviewDue = computeReviewDueDate(publishedAt, months)`, and UPDATE sops set `review_due_at = reviewDue, last_reviewed_at = publishedAt` where id=sopId. When `sopRow.parent_sop_id` is not null (this publish supersedes a prior version), INSERT a `sop_review_events` row `{ sop_id, organisation_id, reviewed_by: user.id, action: 'superseded' }`. Do this AFTER the publish succeeds and make it non-fatal (log on error, never roll back the publish — mirrors the step-4 auto-queue + step-5 synthesis fire-and-forget shape). Do NOT reset the clock inside cloneSopAsDraft (Anti-pattern: a cloned-but-unpublished draft must not look "just reviewed").
    Create `tests/phase28/governance-actions.spec.ts` (source-contract, reads action + route source): assert (a) `setSopOwner` source contains the `organisation_members` membership check AND uses `createClient()` (plain) for the sops UPDATE and does NOT `createAdminClient` for it (OWN-02); (b) `confirmSopCurrent` source writes `last_reviewed_at`, `review_due_at`, `last_reviewed_by` AND inserts `sop_review_events` with `action: 'confirmed_current'` (REV-04); (c) `setReviewCadence` sources organisation_id from ctx/JWT (`ctx.organisationId`) and NOT from a function parameter (REV-01 cross-org guard); (d) `listGovernanceQueue` calls `classifyGovernanceRow`, does not join sub_trades (GQ-03), AND contains the `last_reviewed_at` null-guard on the renamed-since-review comparison (assert source matches `last_reviewed_at\s*\?` or an equivalent explicit null check before the `updated_at >` comparison — plan-checker WARNING-1); (e) publish route inserts a `superseded` review event guarded by `parent_sop_id`. Add `test.fixme` runtime cases for live cross-org write isolation of setSopOwner/confirmSopCurrent with full inline Steps docs (Railway-only-testing convention; phase27 precedent).
  </action>
  <verify>
    <automated>npx playwright test --project=phase28 -g "governance-actions|setSopOwner|confirmSopCurrent|setReviewCadence" && node -e "const s=require('fs').readFileSync('src/app/api/sops/[sopId]/publish/route.ts','utf8'); const need=['review_due_at','last_reviewed_at','computeReviewDueDate','sop_review_events','parent_sop_id']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('publish hook OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Publish route resets `review_due_at`+`last_reviewed_at` and inserts a `superseded` event only when `parent_sop_id` is set; reset failures are non-fatal (do not fail the publish).
    - Spec asserts each action's WIRED behavior (real call, not just prop/token presence — 2026-06-05), runs under `--project=phase28`, and is green.
    - Cross-org runtime isolation carried as `test.fixme` with Steps docs.
  </acceptance_criteria>
  <done>Publish resets review clock on supersede; source-contract specs green under phase28 project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → setSopOwner(userId) | Untrusted userId — must be re-verified as a member of the caller's org before write |
| client → setReviewCadence | org id must come from JWT, never the client, or org A could set org B's cadence |
| client → publish route | Existing auth + org guard; review-event write must carry the RLS-verified org id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Tampering / Elevation of Privilege | setSopOwner | mitigate | Verify target userId ∈ organisation_members of ctx.organisationId before UPDATE (setDepartmentOwner mirror); sops write rides `admins_can_update_sops` RLS via plain client |
| T-28-03-02 | Tampering | setReviewCadence | mitigate | organisation_id sourced only from parseJwtPayload (never a parameter); service-role write self-enforces org (setAiModelSetting mirror) |
| T-28-03-03 | Tampering | confirmSopCurrent event insert | mitigate | Insert rides the append-only `sop_review_events_insert_admin` policy (reviewed_by=auth.uid()); no update/delete path |
| T-28-03-04 | Tampering (malformed token) | governance.ts JWT decode | mitigate | parseJwtPayload only — raw atob forbidden (verify command asserts absence) |
| T-28-03-SC | Tampering | package installs | accept | Zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase28 --project=phase28-unit` green.
- `npx tsc --noEmit` green.
- No `createAdminClient` touching `sops`; no `atob(`; no sub_trades join in the queue.
</verification>

<success_criteria>
- Owner reassign, confirm-current (+audit event), cadence write, and composed queue read all implemented with correct client/org-scope.
- Publish resets the review clock on supersede.
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-03-SUMMARY.md` when done.
</output>
