---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/governance/classify.ts
  - src/lib/governance/cadences.ts
  - src/lib/governance/__tests__/classify.test.ts
  - playwright.config.ts
autonomous: true
requirements: [OWN-03, REV-01, GQ-01, GQ-03]
must_haves:
  truths:
    - "classifyGovernanceRow flags unowned when owner is null OR owner is not an active member (D28-02/OWN-03)"
    - "classifyGovernanceRow flags overdue (due<now) and due_soon (due within 30 days) from review_due_at (D28-05)"
    - "classifyGovernanceRow flags stale_role on dangling OR renamed-since-review department refs (D28-06/GQ-03)"
    - "resolveCadenceMonths order: per-SOP override > org category setting > 12-month default (REV-01)"
    - "phase28 + phase28-unit Playwright projects appear in --list and run the unit spec (2026-05-25)"
  artifacts:
    - path: "src/lib/governance/classify.ts"
      provides: "Pure classifyGovernanceRow() + GovernanceFlag/GovernanceInput types + DUE_SOON_WINDOW_DAYS"
      exports: ["classifyGovernanceRow", "GovernanceFlag", "GovernanceInput", "DUE_SOON_WINDOW_DAYS"]
    - path: "src/lib/governance/cadences.ts"
      provides: "Pure resolveCadenceMonths() + computeReviewDueDate()"
      exports: ["resolveCadenceMonths", "computeReviewDueDate"]
    - path: "src/lib/governance/__tests__/classify.test.ts"
      provides: "Unit coverage for all 4 classification branches + cadence resolution order"
      contains: "unowned"
  key_links:
    - from: "playwright.config.ts"
      to: "src/lib/governance/__tests__"
      via: "phase28-unit project testDir"
      pattern: "phase28-unit"
    - from: "src/lib/governance/classify.ts"
      to: "GovernanceFlag union"
      via: "returns GovernanceFlag[]"
      pattern: "classifyGovernanceRow"
---

<objective>
Create the pure, DB-free governance logic that every later plan composes over: the queue
classifier (`classifyGovernanceRow`) and cadence resolution (`resolveCadenceMonths` /
`computeReviewDueDate`). Register the `phase28` + `phase28-unit` Playwright projects and land the
unit spec covering all four classification branches and cadence resolution order.

Purpose: OWN-03 (unowned detection), GQ-01/GQ-03 (queue buckets incl. stale-role), REV-01
(cadence resolution). These MUST live outside any `'use server'` file so they are sync + directly
unit-testable (2026-06-27 learning — a sync export inside a 'use server' module breaks `next build`).
Output: pure modules + registered test projects + green unit spec — the Nyquist harness for the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-RESEARCH.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-PATTERNS.md
@src/lib/builder/version-lineage.ts
@playwright.config.ts
</context>

## Artifacts this phase produces (this plan)

- **Module:** `src/lib/governance/classify.ts` — `classifyGovernanceRow(input): GovernanceFlag[]`; types `GovernanceFlag` (`'overdue'|'due_soon'|'unowned'|'stale_role'`), `GovernanceInput`; const `DUE_SOON_WINDOW_DAYS = 30`
- **Module:** `src/lib/governance/cadences.ts` — `resolveCadenceMonths(category, orgCadences, perSopOverrideMonths?): number`; `computeReviewDueDate(baseIso, months, now?): string`
- **Spec:** `src/lib/governance/__tests__/classify.test.ts`
- **Playwright projects:** `phase28-unit` (testDir `./src/lib/governance/__tests__`), `phase28` (testDir `.`, testMatch `tests/phase28/**`) in `playwright.config.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure classifier + cadence modules</name>
  <files>src/lib/governance/classify.ts, src/lib/governance/cadences.ts</files>
  <read_first>
    - src/lib/builder/version-lineage.ts (the mandatory "pure sync helper lives OUTSIDE the 'use server' file" pattern; 2026-06-27)
    - 28-RESEARCH.md § "Code Examples → Governance queue classification" (exact target signature/types to copy verbatim)
    - 28-CONTEXT.md D28-02/D28-05/D28-06 (unowned / due windows / stale-role definitions), D28-03 (default cadence 12mo, resolution order)
  </read_first>
  <behavior>
    - classifyGovernanceRow: ownerUserId null OR ownerIsActiveMember false → includes 'unowned'.
    - review_due_at in the past → 'overdue'; within DUE_SOON_WINDOW_DAYS (30) and not past → 'due_soon'; null review_due_at → no due flag.
    - danglingDepartmentRefs OR departmentRenamedSinceReview true → 'stale_role'.
    - Multiple flags can co-occur (e.g. unowned + overdue).
    - resolveCadenceMonths: perSopOverrideMonths (if provided) wins; else org cadence for the exact category; else org 'default' row; else 12.
    - computeReviewDueDate: advances baseIso by N months, returns ISO string; deterministic given `now`.
  </behavior>
  <action>
    Create `classify.ts` with NO `'use server'` directive. Export `type GovernanceFlag = 'overdue' | 'due_soon' | 'unowned' | 'stale_role'`, `interface GovernanceInput { reviewDueAt: string | null; ownerUserId: string | null; ownerIsActiveMember: boolean; danglingDepartmentRefs: boolean; departmentRenamedSinceReview: boolean; now?: Date }`, `const DUE_SOON_WINDOW_DAYS = 30`, and `function classifyGovernanceRow(input: GovernanceInput): GovernanceFlag[]` implementing D28-02/D28-05/D28-06 exactly per the RESEARCH code example (copy verbatim). Use `[\s\S]`-safe plain JS date math (`due.getTime() - now.getTime() <= DUE_SOON_WINDOW_DAYS * 86_400_000`).
    Create `cadences.ts` (also no `'use server'`). Export `resolveCadenceMonths(category: string | null, orgCadences: Record<string, number>, perSopOverrideMonths?: number | null): number` — return `perSopOverrideMonths` when a positive number, else `orgCadences[category ?? '']` when set, else `orgCadences['default']` when set, else 12. Export `computeReviewDueDate(baseIso: string, months: number, now?: Date): string` — start from `new Date(baseIso)`, `setMonth(getMonth()+months)`, return `.toISOString()`.
    Do NOT import supabase or anything async here — these stay pure so 28-01's backfill and 28-03's actions can both reuse the contract, and so `next build` never sees a sync export inside a server module (Pitfall 5).
  </action>
  <verify>
    <automated>npx playwright test --project=phase28-unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -L "use server" src/lib/governance/classify.ts src/lib/governance/cadences.ts` lists both files (neither is a server module).
    - `classify.ts` exports `classifyGovernanceRow`, `GovernanceFlag`, `GovernanceInput`, `DUE_SOON_WINDOW_DAYS`.
    - `cadences.ts` exports `resolveCadenceMonths`, `computeReviewDueDate`.
    - `npx tsc --noEmit` clean for both files.
  </acceptance_criteria>
  <done>Both pure modules exist, no 'use server', exports present, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register phase28 Playwright projects + unit spec</name>
  <files>playwright.config.ts, src/lib/governance/__tests__/classify.test.ts</files>
  <read_first>
    - playwright.config.ts lines ~220-250 (phase27-unit + phase27-stubs + phase26 broad-testMatch blocks — copy their exact shape)
    - 28-PATTERNS.md § playwright.config.ts (phase28-unit / phase28-stubs target blocks)
    - CLAUDE.md Learnings 2026-05-25 (unregistered spec never runs) and 2026-06-02 (use `[\s\S]`, not the `/s` flag, in specs)
  </read_first>
  <action>
    In `playwright.config.ts` add two projects mirroring phase27: (1) `{ name: 'phase28-unit', testDir: './src/lib/governance/__tests__', testMatch: /.*\.test\.ts$/ }` (static `@/` imports resolve here — 2026-04-24), and (2) `{ name: 'phase28', testDir: '.', testMatch: /tests\/phase28\/.*\.(spec|test)\.ts$/, use: { browserName: 'chromium' } }` — deliberately broad like phase26 so 28-03/04/05 drop specs into `tests/phase28/` with no further config edit. Add a comment on each citing the 2026-05-25 registration gate.
    Create `src/lib/governance/__tests__/classify.test.ts` with STATIC imports from `@/lib/governance/classify` and `@/lib/governance/cadences`. Cover: (a) unowned when ownerUserId null, (b) unowned when ownerIsActiveMember false, (c) overdue when reviewDueAt past, (d) due_soon within 30 days, (e) no due flag when reviewDueAt null, (f) stale_role on dangling ref, (g) stale_role on renamed-since-review, (h) co-occurring flags, (i) resolveCadenceMonths order: override > category > default > 12, (j) computeReviewDueDate advances N months deterministically. Use Playwright `test`/`expect`.
    Verify registration with `npx playwright test --list --project=phase28-unit` (must list the spec) and `--project=phase28` (must not error).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase28-unit && npx playwright test --list --project=phase28 >/dev/null && npx playwright test --project=phase28-unit</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase28-unit` shows `classify.test.ts` cases (spec is discovered — 2026-05-25 gate).
    - `npx playwright test --list --project=phase28` exits 0 (project registered even before stub specs exist).
    - `npx playwright test --project=phase28-unit` passes all 10 behavior cases.
    - Spec uses static `@/` imports (not dynamic `import()`), and no `/s` regex flag.
  </acceptance_criteria>
  <done>Both projects registered + discoverable; unit spec green covering all classifier + cadence branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure logic) | These modules take already-fetched, RLS-scoped rows as plain inputs; no I/O, no trust boundary crossed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering | classifyGovernanceRow | accept | Pure function over caller-supplied rows; the org-scope trust boundary is enforced by the RLS-scoped query that feeds it (28-03), not here |
| T-28-02-SC | Tampering | package installs | accept | Zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase28-unit` green.
- `npx playwright test --list --project=phase28` non-error (broad project live for later waves).
- Neither `classify.ts` nor `cadences.ts` contains `'use server'`.
</verification>

<success_criteria>
- Pure classifier + cadence modules exist outside 'use server', fully unit-tested.
- phase28 + phase28-unit projects registered and discoverable.
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-02-SUMMARY.md` when done.
</output>
