---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00043_ownership_review_governance.sql
  - src/types/database.types.ts
  - scripts/backfill-owner-review.mjs
autonomous: true
requirements: [OWN-01, REV-01, REV-04]
must_haves:
  truths:
    - "Every sops row has an owner_user_id (creator, else org admin) after backfill (D28-01)"
    - "Every published sops row has a review_due_at after backfill; many backfill as already-overdue and that is correct (D28-03)"
    - "New SOPs default owner_user_id = created_by automatically on insert across every create path (D28-01)"
    - "sop_review_cadences stores per-org per-category cadence; sop_review_events is append-only audit (D28-03/D28-04)"
  artifacts:
    - path: "supabase/migrations/00043_ownership_review_governance.sql"
      provides: "4 additive sops columns + owner-default trigger + sop_review_cadences + sop_review_events + RLS + indexes"
      contains: "owner_user_id"
    - path: "src/types/database.types.ts"
      provides: "sops Row/Insert/Update extended with owner_user_id/review_due_at/last_reviewed_at/last_reviewed_by"
      contains: "owner_user_id"
    - path: "scripts/backfill-owner-review.mjs"
      provides: "Idempotent owner + review_due_at backfill for existing SOPs"
      contains: "owner_user_id"
  key_links:
    - from: "public.sops (insert)"
      to: "public.default_sop_owner()"
      via: "BEFORE INSERT trigger set_sop_owner_default"
      pattern: "before insert on public.sops"
    - from: "scripts/backfill-owner-review.mjs"
      to: "public.sops"
      via: "conditional patch update (no null-clobber)"
      pattern: "\\.from\\('sops'\\)\\.update"
---

<objective>
Land the entire Phase 28 data foundation in one migration: four additive `sops` columns
(`owner_user_id`, `review_due_at`, `last_reviewed_at`, `last_reviewed_by`), a BEFORE INSERT
trigger that defaults every new SOP's owner to its creator, the org-scoped `sop_review_cadences`
settings table, and the append-only `sop_review_events` audit table — then push it live, extend
`database.types.ts`, and backfill existing rows.

Purpose: OWN-01 (every SOP owned, auto-backfilled), REV-01 (review-due date auto-backfilled),
REV-04 (append-only review event log). All three new state pieces have exact live precedents —
this is copy-the-shape work, not new design.
Output: migration 00043 applied to prod, typed `sops`, populated owner/review columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-RESEARCH.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-PATTERNS.md
@supabase/migrations/00042_ai_model_settings.sql
@supabase/migrations/00010_completion_schema.sql
@supabase/migrations/00003_sop_schema.sql
@scripts/backfill-section-layouts.ts
</context>

## Artifacts this phase produces (this plan)

- **Migration:** `supabase/migrations/00043_ownership_review_governance.sql`
- **Columns (public.sops):** `owner_user_id uuid`, `review_due_at timestamptz`, `last_reviewed_at timestamptz`, `last_reviewed_by uuid`
- **Indexes:** `sops_owner_user_id_idx`, `sops_review_due_at_idx`, `sop_review_events_sop_id_idx`
- **Function + trigger:** `public.default_sop_owner()` + `set_sop_owner_default` (BEFORE INSERT on sops)
- **Tables:** `public.sop_review_cadences(organisation_id, category, months, updated_by, updated_at)` PK `(organisation_id, category)`; `public.sop_review_events(id, sop_id, organisation_id, reviewed_by, action, created_at)`
- **RLS policies:** `sop_review_cadences_read_org` (SELECT only); `sop_review_events_read_org` (SELECT), `sop_review_events_insert_admin` (INSERT, reviewed_by=auth.uid())
- **Script:** `scripts/backfill-owner-review.mjs`
- **Types:** `database.types.ts` sops Row/Insert/Update extension

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00043 (columns + owner trigger + 2 tables + RLS)</name>
  <files>supabase/migrations/00043_ownership_review_governance.sql</files>
  <read_first>
    - supabase/migrations/00003_sop_schema.sql (existing `admins_can_update_sops` / `org_members_can_view_sops` policies on sops — DO NOT touch them; the 4 new columns ride them)
    - supabase/migrations/00042_ai_model_settings.sql (exact settings-table + org-scoped read RLS shape to copy for sop_review_cadences)
    - supabase/migrations/00010_completion_schema.sql (append-only audit RLS shape: SELECT org, INSERT with auth.uid() check, NO update/delete — copy for sop_review_events)
    - 28-PATTERNS.md § migration analogs (Analog 1/2/3 excerpts)
  </read_first>
  <action>
    Create the migration. Section 1: `alter table public.sops add column if not exists owner_user_id uuid references auth.users(id) on delete set null, add column if not exists review_due_at timestamptz, add column if not exists last_reviewed_at timestamptz, add column if not exists last_reviewed_by uuid references auth.users(id) on delete set null;` then `create index if not exists sops_owner_user_id_idx on public.sops(owner_user_id);` and `create index if not exists sops_review_due_at_idx on public.sops(review_due_at);`. Add NO new policy on sops — comment that `admins_can_update_sops` (org + role admin/safety_manager) already gates writes and `org_members_can_view_sops` gates reads (per D28-01, RESEARCH Pitfall 1).
    Section 2 (D28-01 default owner across ALL insert paths — wizard/upload/ai-prompt/voice/blank/clone — with zero route edits): `create or replace function public.default_sop_owner() returns trigger language plpgsql as $$ begin if new.owner_user_id is null then new.owner_user_id := new.created_by; end if; return new; end $$;` then `drop trigger if exists set_sop_owner_default on public.sops; create trigger set_sop_owner_default before insert on public.sops for each row execute function public.default_sop_owner();`. This is the lazy correct choice: one trigger instead of patching every create route.
    Section 3 (D28-03): create `public.sop_review_cadences(organisation_id uuid not null references public.organisations(id) on delete cascade, category text not null, months int not null default 12, updated_by uuid references auth.users(id) on delete set null, updated_at timestamptz not null default now(), primary key (organisation_id, category))`; `enable row level security`; add ONLY `create policy sop_review_cadences_read_org ... for select using (organisation_id = (auth.jwt()->'app_metadata'->>'organisation_id')::uuid)` — NO authenticated insert/update/delete policy (writes go through the service-role action in 28-03, mirroring ai_model_settings exactly).
    Section 4 (D28-04): create `public.sop_review_events(id uuid primary key default gen_random_uuid(), sop_id uuid not null references public.sops(id) on delete cascade, organisation_id uuid not null references public.organisations(id) on delete cascade, reviewed_by uuid references auth.users(id) on delete set null, action text not null check (action in ('confirmed_current','superseded')), created_at timestamptz not null default now())`; `create index if not exists sop_review_events_sop_id_idx on public.sop_review_events(sop_id);`; `enable row level security`; `sop_review_events_read_org` for select to authenticated using `organisation_id = public.current_organisation_id()`; `sop_review_events_insert_admin` for insert to authenticated with check `organisation_id = public.current_organisation_id() and public.current_user_role() in ('admin','safety_manager') and reviewed_by = auth.uid()`. NO update/delete policy (append-only, COMP-07 precedent).
    Use `create ... if not exists` throughout so the migration is idempotent for re-run.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('supabase/migrations/00043_ownership_review_governance.sql','utf8'); const need=['owner_user_id','review_due_at','last_reviewed_at','last_reviewed_by','default_sop_owner','before insert on public.sops','sop_review_cadences','sop_review_events',\"action in ('confirmed_current','superseded')\",'reviewed_by = auth.uid()']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(/create policy sop_review_cadences_\w+ .*for (insert|update|delete)/i.test(s)){console.error('cadences must have NO authenticated write policy');process.exit(1)} console.log('migration OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File contains all 4 columns, the `default_sop_owner` function, a `before insert on public.sops` trigger, both new tables, and the append-only + org-scoped policies (verify command exits 0).
    - `sop_review_cadences` has a SELECT policy and NO authenticated INSERT/UPDATE/DELETE policy.
    - `sop_review_events` has SELECT + INSERT (with `reviewed_by = auth.uid()`) and NO UPDATE/DELETE policy.
    - No ALTER/DROP against existing `admins_can_update_sops` / `org_members_can_view_sops`.
  </acceptance_criteria>
  <done>Migration 00043 written, idempotent, mirrors 00042/00010/00003 analogs, verify command green.</done>
</task>

<!-- gate changed blocking-human → auto per Simon's autonomous mandate (2026-07-12): idempotent IF NOT EXISTS DDL + established unattended db-push precedent (Phases 13/25/27); Simon reviews on completion -->
<task type="auto">
  <name>Task 2 [BLOCKING]: Push migration + extend database.types.ts</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - src/types/database.types.ts (find the `sops` Row/Insert/Update blocks and the prior manual `parent_sop_id`/`superseded_by` extensions — mirror that edit shape; Pitfall 4)
    - CLAUDE.md Learnings 2026-06-15 (PGRST205 schema-cache staleness ≠ 42P01)
  </read_first>
  <action>
    Run `npx supabase db push` non-interactively (SUPABASE_ACCESS_TOKEN is already in `.env`/`.env.local` from Phases 27/13 — the 13-04 precedent confirms auto-push is correct when creds are present). If post-push assertions hit a "Could not find the table ... in the schema cache" (PGRST205) error, treat it as reload-pending (NOT 42P01 table-absent) — either wait ~60s or issue `NOTIFY pgrst, 'reload schema'` via the Supabase Management API `POST /v1/projects/{ref}/database/query`; verify table existence bypassing PostgREST with `select to_regclass('public.sop_review_events')`.
    Then extend `database.types.ts`: add `owner_user_id: string | null`, `review_due_at: string | null`, `last_reviewed_at: string | null`, `last_reviewed_by: string | null` to the `sops` Row, Insert (all optional), and Update (all optional) type blocks. Do NOT attempt a full type regen and do NOT add types for `sop_review_cadences`/`sop_review_events` — those are accessed via `(supabase as any)` casts in 28-03 (matches ai_model_settings/departments precedent).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/types/database.types.ts','utf8'); const need=['owner_user_id','review_due_at','last_reviewed_at','last_reviewed_by']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('types OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` reports success (or "up to date"); `select to_regclass('public.sop_review_cadences')` and `('public.sop_review_events')` both return non-null.
    - `database.types.ts` sops Row/Insert/Update carry the 4 new fields; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Schema live on prod, tables verified via to_regclass, types extended, tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill owner + review_due_at for existing SOPs</name>
  <files>scripts/backfill-owner-review.mjs</files>
  <read_first>
    - scripts/backfill-section-layouts.ts (idempotent-only-missing-rows + admin-client setup + per-row logging shape — copy exactly; 2026-07-05 no-null-clobber learning)
    - 28-RESEARCH.md § "Backfill script skeleton" (the exact conditional-patch logic)
    - CLAUDE.md Learnings 2026-05-08 (anon key is NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY) and 2026-07-05 (per-step failure must NOT null-clobber)
  </read_first>
  <action>
    Write `scripts/backfill-owner-review.mjs` mirroring `scripts/backfill-section-layouts.ts` client setup (service-role admin client from env). Per D28-01/D28-03: select all sops rows; for each row missing `owner_user_id` OR `review_due_at`, build a conditional patch object. owner = `created_by` if that user has an `organisation_members` row for the SOP's `organisation_id`, else the org's earliest (`created_at asc`) member with role in ('admin','safety_manager'); only set `patch.owner_user_id` if currently null AND a resolved owner exists. review_due_at (only if currently null): resolve cadence months = org's `sop_review_cadences` row for `sops.category` (fall back to org 'default' row, else 12 — inline this rule; a one-off script does not import the shared helper) then `dueAt = max(published_at ?? updated_at, updated_at)` advanced by cadence months, `.toISOString()`. Skip the update entirely when the patch is empty. Log per-row outcome (`[backfill] <sop_id> set {keys}` / `skip`). Run the script with prod creds; it is idempotent and only touches rows missing a value.
    Do NOT overwrite an already-populated owner or due date. Many rows will compute an already-past review_due_at — that is correct and desirable (D28-03).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/backfill-owner-review.mjs','utf8'); const need=['owner_user_id','review_due_at','organisation_members','safety_manager']; const miss=need.filter(t=>!s.includes(t)); if(miss.length){console.error('MISSING',miss);process.exit(1)} if(!/if\s*\(.*patch|Object\.keys\(patch\)/.test(s)){console.error('must guard empty-patch / conditional set');process.exit(1)} console.log('backfill script OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Script computes owner (creator-if-active else earliest admin/safety_manager) and review_due_at conditionally, skips empty patches, logs per row.
    - Re-running the script is a no-op on already-populated rows (idempotent; no null-clobber).
    - After run: no published SOP has NULL `owner_user_id` unless the org has zero admins; every published SOP has a `review_due_at`.
  </acceptance_criteria>
  <done>Backfill script written + executed; existing SOPs carry owner + review_due_at.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → sops columns | Owner/review writes cross here — gated by existing `admins_can_update_sops` RLS (org + admin/safety_manager) |
| authenticated client → sop_review_cadences | No authenticated write policy; only the service-role action (28-03) writes |
| service-role backfill script → all orgs | Bypasses RLS by design; must scope every computed owner to the SOP's own org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Elevation of Privilege | sop_review_cadences | mitigate | No authenticated INSERT/UPDATE/DELETE policy (verify command asserts absence); writes only via service-role action next plan |
| T-28-01-02 | Tampering | sop_review_events | mitigate | Append-only: INSERT requires `reviewed_by = auth.uid()` + role, NO update/delete policy (COMP-07 precedent) |
| T-28-01-03 | Tampering | backfill script | mitigate | Owner resolved only from `organisation_members` of the SOP's own `organisation_id`; conditional patch never null-clobbers (2026-07-05) |
| T-28-01-SC | Tampering | package installs | accept | Zero new dependencies this phase (RESEARCH confirms) — supply-chain surface unchanged |
</threat_model>

<verification>
- `npx tsc --noEmit` green.
- `select to_regclass('public.sop_review_cadences')`, `('public.sop_review_events')` non-null on prod.
- Trigger present: `select tgname from pg_trigger where tgname='set_sop_owner_default'` returns a row.
</verification>

<success_criteria>
- Migration 00043 applied; 4 columns + trigger + 2 tables + RLS live.
- database.types.ts typed for the 4 sops columns; tsc green.
- Existing SOPs backfilled with owner + review_due_at (idempotent script).
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-01-SUMMARY.md` when done.
</output>
