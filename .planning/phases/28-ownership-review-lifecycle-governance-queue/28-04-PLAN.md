---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 04
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - src/app/(protected)/admin/governance/page.tsx
  - src/components/admin/governance/GovernanceFilterChips.tsx
  - src/components/admin/governance/GovernanceQueueRow.tsx
  - src/components/admin/governance/OwnerPicker.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase28/governance-queue.spec.ts
autonomous: true
requirements: [GQ-01, GQ-02, GQ-03, OWN-02]
must_haves:
  truths:
    - "/admin/governance renders one list of classified rows with filter chips (Overdue / Due soon / Unowned / Stale-role / All), filtered by ?filter= (D28-05/GQ-01)"
    - "Every queue row has exactly ONE wired primary inline action — Confirm current / Assign owner / Fix assignment — that invokes the real server action, not an empty handler (GQ-02)"
    - "OwnerPicker reassigns an SOP owner in ≤2 clicks from the queue row via setSopOwner (OWN-02)"
    - "stale-role rows deep-link to the assign/builder surface (GQ-03)"
    - "/admin/governance is mapped in journeys.ts in the same commit (project convention)"
  artifacts:
    - path: "src/app/(protected)/admin/governance/page.tsx"
      provides: "Governance queue RSC page with auth guard + filter chips + rows"
      contains: "listGovernanceQueue"
    - path: "src/components/admin/governance/GovernanceQueueRow.tsx"
      provides: "One row with one wired primary action + flag badges"
      contains: "setSopOwner"
    - path: "src/components/admin/governance/OwnerPicker.tsx"
      provides: "≤2-click inline owner reassignment popover"
      contains: "getOrgMembers"
    - path: "src/lib/journeys/journeys.ts"
      provides: "Governance-queue journey step with route /admin/governance"
      contains: "/admin/governance"
  key_links:
    - from: "src/app/(protected)/admin/governance/page.tsx"
      to: "src/actions/governance.ts::listGovernanceQueue"
      via: "server-side call in RSC"
      pattern: "listGovernanceQueue"
    - from: "src/components/admin/governance/GovernanceQueueRow.tsx"
      to: "confirmSopCurrent / setSopOwner"
      via: "wired onClick / form action"
      pattern: "confirmSopCurrent|setSopOwner"
    - from: "src/components/admin/governance/OwnerPicker.tsx"
      to: "setSopOwner"
      via: "member select → action call"
      pattern: "setSopOwner"
---

<objective>
Build the single governance surface: `/admin/governance` — one RSC page rendering the classified
queue with filter chips and a one-click primary action per row, plus the inline OwnerPicker for
≤2-click reassignment. Map the new route in `journeys.ts` in the same commit.

Purpose: GQ-01 (one unified queue), GQ-02 (one wired action per row — no empty handlers, the
2026-06-05 dead-feature trap), GQ-03 (stale-role rows deep-link to fix), OWN-02 (reassign from the
SOP context). No new backend — reads come entirely from 28-03's `listGovernanceQueue`.
Output: governance page + 3 components + journeys entry + source-contract specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-RESEARCH.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-PATTERNS.md
@src/app/(protected)/admin/sops/page.tsx
@src/components/admin/sop/SopDepartmentEditor.tsx
@src/actions/assignments.ts
@src/lib/journeys/journeys.ts
</context>

## Artifacts this phase produces (this plan)

- **Route:** `src/app/(protected)/admin/governance/page.tsx` (RSC, admin/safety_manager guard, `?filter=` searchParam)
- **Components:** `GovernanceFilterChips.tsx` (5 chips → `?filter=`), `GovernanceQueueRow.tsx` (flag badges + one wired primary action), `OwnerPicker.tsx` (inline member popover → `setSopOwner`)
- **Journey:** new `JourneyStep`/`Journey` in `journeys.ts` with `route: '/admin/governance'`
- **Spec:** `tests/phase28/governance-queue.spec.ts`

<tasks>

<task type="auto">
  <name>Task 1: Governance queue page + filter chips + row</name>
  <files>src/app/(protected)/admin/governance/page.tsx, src/components/admin/governance/GovernanceFilterChips.tsx, src/components/admin/governance/GovernanceQueueRow.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/page.tsx (auth+role guard verbatim, STATUS_TABS chip render loop, en-NZ formatDate, blueprint-frame row markup, icon-button + StatusBadge convention)
    - src/actions/governance.ts (listGovernanceQueue + GovernanceRow shape from 28-03)
    - 28-PATTERNS.md § governance page/row/chips analogs + § "Wired-handler requirement (GQ-02)"
    - CLAUDE.md Learnings 2026-06-05 (a row action must reference the real callback, not just a prop name)
  </read_first>
  <action>
    Create `src/app/(protected)/admin/governance/page.tsx` (RSC). Copy the admin/sops auth+role guard verbatim (getUser → organisation_members role → redirect if not admin/safety_manager). Read `?filter=` (default `all`) from searchParams. Call `listGovernanceQueue()`; if error, render an empty/error blueprint-frame. Filter rows client-agnostically in the server component: `all` shows every row with ≥1 flag; `overdue`/`due_soon`/`unowned`/`stale_role` show rows whose `flags` include that flag. Render `<GovernanceFilterChips active={filter} counts={...}/>` then the row list. Reuse `en-NZ` `formatDate` for review-due dates. Add the admin sub-nav tab pointing to `/admin/governance` (mirror the existing SOPs/Library/Team/Departments nav).
    Create `GovernanceFilterChips.tsx` mirroring the STATUS_TABS render loop: 5 chips (`All`, `Overdue`, `Due soon`, `Unowned`, `Stale-role`) as `<Link>`s to `/admin/governance?filter=<value>` (All → `/admin/governance`), `data-active` on the active chip; show the count per chip.
    Create `GovernanceQueueRow.tsx` — one row: SOP title + category + owner label + flag badges (paper/ink pill style per flag: overdue red-ish, due_soon amber, unowned + stale_role neutral/warning). Render exactly ONE primary action chosen by flag priority: `unowned` → Assign owner (renders `<OwnerPicker sopId owner/>`); else `stale_role` → "Fix assignment" `<Link href={/admin/sops/${sopId}/assign}>`; else (overdue/due_soon) → "Confirm current" button wired to `confirmSopCurrent(sopId)` (client component, `onClick` invokes the imported action then refreshes). The Confirm-current button MUST call the real action (GQ-02 — not an empty onClick; the source-contract test greps for the `confirmSopCurrent(` call).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('src/app/(protected)/admin/governance/page.tsx','utf8'); const r=fs.readFileSync('src/components/admin/governance/GovernanceQueueRow.tsx','utf8'); const c=fs.readFileSync('src/components/admin/governance/GovernanceFilterChips.tsx','utf8'); if(!p.includes('listGovernanceQueue')){console.error('page must call listGovernanceQueue');process.exit(1)} if(!/confirmSopCurrent\(/.test(r)){console.error('row must WIRE confirmSopCurrent (GQ-02)');process.exit(1)} if(!/filter=/.test(c)){console.error('chips must link ?filter=');process.exit(1)} console.log('page/row/chips OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Page calls `listGovernanceQueue`, guards admin/safety_manager, filters by `?filter=`.
    - Each row renders exactly one primary action; Confirm-current button contains a real `confirmSopCurrent(` call (not an empty handler).
    - Filter chips link to `?filter=` values incl. All; `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Governance queue page renders classified rows with chips + one wired action each; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: OwnerPicker (≤2-click reassign) + journeys.ts entry</name>
  <files>src/components/admin/governance/OwnerPicker.tsx, src/lib/journeys/journeys.ts</files>
  <read_first>
    - src/components/admin/sop/SopDepartmentEditor.tsx (inline popover + draft/commit state shape to mirror)
    - src/actions/assignments.ts::getOrgMembers (member list source — reuse, do not hand-roll a second member query)
    - src/actions/governance.ts::setSopOwner (the action the picker calls)
    - src/lib/journeys/journeys.ts (Journey/JourneyStep shape + JOURNEY_GROUPS; add under 'Library & team')
    - CLAUDE.md § Pathways Map Maintenance (journeys.ts updated in the SAME commit as the route)
  </read_first>
  <action>
    Create `OwnerPicker.tsx` (client component): a compact button showing the current owner label; click 1 opens a popover listing org members from `getOrgMembers()` (plus a "No owner" clear option); click 2 selects a member → calls `setSopOwner(sopId, userId)` then closes + refreshes. That is ≤2 clicks total (OWN-02). Mirror `SopDepartmentEditor`'s popover open/close + pending-state structure. Surface `setSopOwner`'s `{ error }` inline (e.g. toast/inline text) — do not swallow it.
    In `journeys.ts` add a governance journey (group `'Library & team'`, persona `'SOP Admin'`) with a screen step `{ id, type: 'screen', label: 'Governance queue', route: '/admin/governance', detail: 'Overdue / due-soon / unowned / stale-role SOPs with one-click actions' }` — a real route so `/pathways` coverage ticks it off (RESEARCH Pitfall 7). Keep it to the real current flow.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const o=fs.readFileSync('src/components/admin/governance/OwnerPicker.tsx','utf8'); const j=fs.readFileSync('src/lib/journeys/journeys.ts','utf8'); if(!/setSopOwner\(/.test(o)){console.error('OwnerPicker must call setSopOwner');process.exit(1)} if(!o.includes('getOrgMembers')){console.error('OwnerPicker must reuse getOrgMembers');process.exit(1)} if(!j.includes('/admin/governance')){console.error('journeys.ts must map /admin/governance');process.exit(1)} console.log('picker/journeys OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - OwnerPicker lists members via `getOrgMembers` and reassigns via `setSopOwner` in ≤2 clicks; errors surfaced not swallowed.
    - `journeys.ts` contains a step with `route: '/admin/governance'` (same commit as the route — `/pathways` will show 0 not-mapped for it).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Inline ≤2-click OwnerPicker wired to setSopOwner; new route mapped in journeys.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Source-contract specs for queue + wiring</name>
  <files>tests/phase28/governance-queue.spec.ts</files>
  <read_first>
    - tests/phase28/governance-actions.spec.ts (28-03 spec style + phase28 project already registered)
    - CLAUDE.md Learnings 2026-06-05 (assert the wired handler/call, not just a token) + 2026-05-25 (spec must match the phase28 regex — it does: tests/phase28/*)
  </read_first>
  <action>
    Create `tests/phase28/governance-queue.spec.ts` (source-contract): assert (a) `governance/page.tsx` calls `listGovernanceQueue` and guards role (GQ-01); (b) `GovernanceQueueRow.tsx` WIRES `confirmSopCurrent(` (real call, GQ-02) and renders a single primary action; (c) `GovernanceFilterChips.tsx` links all five `?filter=` values including stale_role (GQ-01/GQ-03); (d) `OwnerPicker.tsx` calls `setSopOwner(` and uses `getOrgMembers` (OWN-02); (e) `journeys.ts` contains `route: '/admin/governance'` (pathways coverage). Read files with `fs.readFileSync` + `toContain`/regex; use `[\s\S]` not `/s`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase28 -g "governance-queue|row action wired|governance queue renders|owner"</automated>
  </verify>
  <acceptance_criteria>
    - Spec asserts wired calls (`confirmSopCurrent(`, `setSopOwner(`) and route mapping, not just prop-name presence.
    - Runs under `--project=phase28` and is green.
  </acceptance_criteria>
  <done>Queue + wiring source-contract specs green under phase28 project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/governance | RSC page must enforce admin/safety_manager role before rendering org SOP governance data |
| OwnerPicker → setSopOwner | Owner reassignment crosses to a server action that re-verifies org membership (28-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information Disclosure | governance page | mitigate | Copy the admin/sops auth+role guard verbatim (redirect non-admins); queue data is already RLS-scoped by listGovernanceQueue |
| T-28-04-02 | Elevation of Privilege | OwnerPicker | mitigate | Reassignment authority lives in setSopOwner's membership check (28-03); the picker is a thin UI over it |
| T-28-04-SC | Tampering | package installs | accept | Zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase28 --project=phase28-unit` green.
- `npx tsc --noEmit` green.
- Manual: `/pathways` shows `/admin/governance` mapped (0 not-mapped for it).
</verification>

<success_criteria>
- One governance queue at `/admin/governance` with chips + one wired action per row.
- ≤2-click owner reassignment inline; route mapped in journeys.ts.
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-04-SUMMARY.md` when done.
</output>
