---
phase: 41-one-sop-surface
plan: 06
type: execute
wave: 4
depends_on: ["41-05"]
files_modified:
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/admin/governance/page.tsx
  - src/components/layout/TopHeader.tsx
  - src/lib/auth/role-home.ts
  - src/lib/journeys/journeys.ts
  - tests/lint/no-static-admin-lens-import.spec.ts
  - tests/phase30/admin-nav.spec.ts
  - tests/phase30/governance-fold.spec.ts
  - tests/phase41/nav-and-shim.spec.ts
autonomous: true
requirements: [SUR-01, SUR-03, SUR-04, SUR-06]

must_haves:
  truths:
    - "/admin/sops renders no UI — it only redirects to /sops, preserving every query param"
    - "An unauthenticated or non-admin hit on /admin/sops never learns the destination shape"
    - "The header carries exactly one SOPs entry and no Manage SOPs entry"
    - "An admin logging in lands on /sops"
    - "/pathways shows 0 not-mapped screens and journeys.ts describes the merged surface and the shim"
  artifacts:
    - path: "src/app/(protected)/admin/sops/page.tsx"
      provides: "Redirect shim to /sops preserving the query string"
      contains: "URLSearchParams"
      max_lines: 60
    - path: "src/lib/journeys/journeys.ts"
      provides: "Pathways map reflecting one SOP surface + the shim"
      contains: "/sops?view=attention"
  key_links:
    - from: "src/app/(protected)/admin/sops/page.tsx"
      to: "/sops"
      via: "redirect() after the admin guard"
      pattern: "redirect\\(`?/sops"
    - from: "src/components/layout/TopHeader.tsx"
      to: "/sops?view=attention"
      via: "ADMIN_LINKS governance entry"
      pattern: "/sops\\?view=attention"
    - from: "src/lib/auth/role-home.ts"
      to: "/sops"
      via: "admin case"
      pattern: "case 'admin'"
---

<objective>
Close the second SOP door. `/admin/sops` becomes a redirect shim to `/sops` preserving every query param, `TopHeader`'s `ADMIN_LINKS` drops "Manage SOPs" and gains a governance entry deep-linking `/sops?view=attention`, `roleHome('admin')` lands on `/sops`, and `journeys.ts` is updated in this same commit.

Purpose: SUR-03 (exactly one top-level "SOPs" entry, no second destination) and SUR-01 (the second list route survives at most as a shim). Re-adding a "Manage SOPs" nav entry after this plan is a regression, not a restoration of Phase 30's UX-02.
Output: the shim, the nav change, the role-home change, the pathways map, and repointed Phase 30 guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-RESEARCH.md
@.planning/phases/41-one-sop-surface/41-PATTERNS.md
@.planning/phases/41-one-sop-surface/41-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace /admin/sops with a redirect shim and update journeys.ts</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/app/(protected)/admin/governance/page.tsx, src/lib/journeys/journeys.ts, tests/lint/no-static-admin-lens-import.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/governance/page.tsx (FULL file — the Phase 30 shim: `getSessionContext()`, `if (!userId) redirect('/login')`, the `['admin','safety_manager']` gate, `await searchParams`, then `redirect(...)`. This ordering is the template and must be preserved exactly)
    - src/app/(protected)/admin/sops/page.tsx (FULL file — everything being deleted. Before deleting, confirm every capability it renders now has a home: status/drafts/published → `listAdminSopRows` + `AdminStatusLens`; attention grouping → `AdminAttentionLens`; access assembly → `listAdminAccessData` + `AdminAccessLens`; owner filter, rail counts, scope departments, no-audience count, "Open in library (N)" banner → all in 41-02/41-04; helpers + FLAG maps → `src/lib/sop-list/admin-rows.ts` + `src/lib/governance/flag-display.ts`. If ANY of those is missing, stop and report rather than deleting)
    - src/app/(protected)/sops/page.tsx (the URL each admin scope writes, per 41-05-SUMMARY — the shim must produce those exact shapes)
    - src/lib/journeys/journeys.ts (:60-75, :98-115, :290-300, :425-455, :560-575 — every step whose `route` is `/admin/sops` or `/admin/sops?view=…`, plus the existing `/admin/governance` "Redirect shim →" step at :568 which is the wording precedent)
    - src/lib/journeys/routes.ts (:18-50 — `listAppRoutes()` walks the file tree, so `/admin/sops` REMAINS a route as long as `page.tsx` exists; the shim must therefore stay mapped in `journeys.ts`, not be removed from it, or `/pathways` will flag it as not-mapped)
  </read_first>
  <action>
Replace the entire body of `src/app/(protected)/admin/sops/page.tsx` with a redirect shim modelled byte-for-byte on `src/app/(protected)/admin/governance/page.tsx`:
- `getSessionContext()`, `if (!userId) redirect('/login')`, then `if (!role || !['admin','safety_manager'].includes(role)) redirect('/dashboard')`. The guard stays IN FRONT of the destination redirect so an unauthenticated or unauthorised hit never learns the destination shape. Keep the literal `['admin', 'safety_manager']` string — `tests/phase30/admin-nav.spec.ts` asserts it on this file and will keep doing so.
- `const params = await searchParams` typed for all seven pass-through params: `view`, `status`, `owner`, `filter`, `departments`, `collection`, `sop`.
- Build the destination with `URLSearchParams` over the FIXED path `/sops` — append each param only when present, then `redirect(qs ? \`/sops?${qs}\` : '/sops')`. Never interpolate a user-supplied value into the PATH; the path is a constant and the query is the only variable part (open-redirect hygiene).
- Delete `export const metadata` (a shim renders nothing) and every import the old body used.
- Add a header comment matching the governance shim's: this route survives only so bookmarks and legacy deep-links keep working, the surviving destination is `/sops`, and the seven params map onto the merged surface's scopes.

`status`, `owner` and `filter` are included deliberately — RESEARCH/CONTEXT name only four params, but `UploadDropzone` pushes `?status=draft` and `/admin/governance` forwards `?filter=`. Dropping them would break live CTAs.

Retarget `src/app/(protected)/admin/governance/page.tsx`'s redirect from `/admin/sops?view=attention` to `/sops?view=attention` (with the `&filter=` pass-through preserved) so a legacy governance bookmark resolves in ONE hop instead of two.

Update `src/lib/journeys/journeys.ts` in this same commit (CLAUDE.md § Pathways Map Maintenance — a changed flow is not done until `journeys.ts` reflects it):
- Repoint every step whose `route` is `/admin/sops` or `/admin/sops?view=…` to `/sops` / `/sops?view=attention` / `/sops?view=access`, and rewrite the `label`/`detail` text so it describes lenses on one surface rather than a separate admin library (drop "SOP management" and "Admin SOP library" as destination names — D-05/SUR-06: "Library" and its synonyms must not name a destination).
- ADD one step keeping `/admin/sops` mapped as a redirect shim, worded like the existing `/admin/governance` step at :568 ("Redirect shim → /sops, preserving ?view=/?status=/?owner=/?filter=/?departments=/?collection=/?sop= so old bookmarks keep working"). `listAppRoutes()` reads the file tree, so the shim route still exists and must stay mapped or `/pathways` reports a not-mapped screen.
- Update the `admin-home` / `home` login-landing steps to `/sops`.
- Update the `filter` action step's detail: the "Open in library →" link now targets `/sops?departments=<id>` / `/sops?collection=<id>`.

Finally, remove the TEMPORARY allowlist entry for `src/app/(protected)/admin/sops/page.tsx` from `tests/lint/no-static-admin-lens-import.spec.ts` (the comment written in 41-01 Task 3 marks it) — the shim imports none of the three lens components, so the guard tightens to the three lens files only, in the same commit that makes that true.
  </action>
  <acceptance_criteria>
    - `src/app/(protected)/admin/sops/page.tsx` is under 60 lines, contains `redirect(`, `URLSearchParams`, `['admin', 'safety_manager']`, and all seven param names
    - that file contains none of: `SopMillerBrowser`, `WiringPatchBayShell`, `GovernanceQueueRow`, `listGovernanceQueue`, `listOrgTree`, `listGrants`, `getTeamMembersWithEmails`, `export const metadata`
    - the admin guard's line index is lower than the `/sops` redirect's line index (positional — the guard must precede the destination)
    - the `/sops` destination path is a string constant, never a template hole filled from `params`
    - `src/app/(protected)/admin/governance/page.tsx` contains `/sops?view=attention` and no `/admin/sops`
    - `src/lib/journeys/journeys.ts` contains `/sops?view=attention`, `/sops?view=access`, and exactly one step whose `route` is `/admin/sops` (the shim), whose `detail` contains `Redirect shim`
    - `grep -c "SOP management" src/lib/journeys/journeys.ts` is 0
    - `tests/lint/no-static-admin-lens-import.spec.ts` no longer allowlists `admin/sops/page.tsx`
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0
    - `npx playwright test --project=phase15-stubs` exits 0 (the tightened import guard passes)
    - `npx playwright test --project=phase30 -g "every App Router page route is mapped by a journeys.ts step"` passes — this is the existing 0-not-mapped gate
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx playwright test --project=phase15-stubs --project=phase30 -g "journeys"</automated>
  </verify>
  <done>`/admin/sops` is a guard-first redirect shim producing the merged surface's own URL shapes, the legacy governance bookmark resolves in one hop, and the pathways map describes one SOP surface plus one shim with zero not-mapped screens.</done>
</task>

<task type="auto">
  <name>Task 2: One SOPs entry in TopHeader; admin role-home lands on /sops</name>
  <files>src/components/layout/TopHeader.tsx, src/lib/auth/role-home.ts</files>
  <read_first>
    - src/components/layout/TopHeader.tsx (:121-147 — `NavLink`, `BASE_LINKS` (`SOPs` → `/sops`, `Sign-off` → `/activity`) and `ADMIN_LINKS` (`Manage SOPs`, `Create New SOP`, `Content`, `Team`, `Settings`) with their comment blocks; :158-170 `activeHref`'s longest-prefix matching, which is why nested admin hrefs do not double-highlight)
    - src/lib/auth/role-home.ts (full file — the `roleHome` switch and its "ONE place the role → home mapping is decided (UX-01)" contract; note the CLAUDE.md 2026-06-27 comment: it is deliberately a plain module, not a server-action file)
    - .planning/phases/41-one-sop-surface/41-CONTEXT.md (D-04 and the RESEARCH correction: `AdminNav.tsx` does NOT exist — it was deleted 2026-07-30 and its links live in `ADMIN_LINKS`. Do not create or edit an `AdminNav`)
  </read_first>
  <action>
In `src/components/layout/TopHeader.tsx`:
- Delete the `{ label: 'Manage SOPs', href: '/admin/sops' }` entry from `ADMIN_LINKS`. `Create New SOP` (`/admin/sops/new`), `Content` (`/admin/blocks`), `Team` (`/admin/team`) and `Settings` (`/admin/settings`) all survive unchanged — "Create New SOP" stays where it is; Phase 42 moves it.
- Add `{ label: 'Governance', href: '/sops?view=attention' }` to `ADMIN_LINKS`, positioned first (where Manage SOPs was). This is the governance deep-link D-04 requires.
- `BASE_LINKS` keeps its single `{ label: 'SOPs', href: '/sops' }` — that is now the one and only SOPs door for every role.
- Check `activeHref`: it does longest-prefix matching on `pathname` only and ignores the query string, so the new `Governance` entry's `/sops?view=attention` href will never win the active match (its `href` includes `?`, so `pathname === href` and `pathname.startsWith(href + '/')` are both false) and `SOPs` correctly highlights on `/sops`. Confirm this by reading the function; if the entry does light up incorrectly, strip the query before matching rather than restructuring the array.
- Update the `ADMIN_LINKS` comment block to record that the SOPs list is one shared route as of Phase 41 and that re-adding a second SOPs entry is a regression (SUR-03).

In `src/lib/auth/role-home.ts`: change the `case 'admin'` return from `/admin/sops` to `/sops`, and update the mapping comment in the file header. Keep the module a plain module (no `'use server'`) — a sync export in a server-action file breaks `next build` (CLAUDE.md 2026-06-27).
  </action>
  <acceptance_criteria>
    - `src/components/layout/TopHeader.tsx` contains `Manage SOPs` zero times and `'/admin/sops'` zero times
    - it contains `'/sops?view=attention'` exactly once and `{ label: 'SOPs', href: '/sops' }` exactly once
    - it still contains `'/admin/sops/new'`, `'/admin/blocks'`, `'/admin/team'`, `'/admin/settings'`
    - `src/lib/auth/role-home.ts` contains `/admin/sops` zero times and returns `/sops` for `case 'admin'`
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Exactly one SOPs entry exists in the header for every role, the governance link deep-links a lens on it, and an admin's home is `/sops`.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the two Phase 30 nav/fold guards and flip the nav-and-shim spec live</name>
  <files>tests/phase30/admin-nav.spec.ts, tests/phase30/governance-fold.spec.ts, tests/phase41/nav-and-shim.spec.ts</files>
  <read_first>
    - tests/phase30/admin-nav.spec.ts (FULL file — :49-67 asserts the header contains `Manage SOPs` and `'/admin/sops'` and that `ADMIN_PAGES[0]` (the admin sops page) contains `/admin/sops?view=attention`; :69-82 asserts every admin page keeps `['admin', 'safety_manager']`; :100-107 the account-menu assertions)
    - tests/phase30/governance-fold.spec.ts (:27-70 — asserts `/admin/sops` renders the attention view behind `?view=attention` and that `/admin/governance` is a shim mapping `?filter=`, guard first; :119 the "every App Router page route is mapped by a journeys.ts step" 0-not-mapped gate)
    - tests/phase41/nav-and-shim.spec.ts (the fixme stubs from 41-01 Task 2)
    - ./CLAUDE.md (2026-07-13 learning — a source-contract guard that goes stale-red has stopped guarding; repoint in the SAME commit as the move)
  </read_first>
  <action>
Repoint `tests/phase30/admin-nav.spec.ts` in this commit (not later): the UX-02 contract it encodes is still real, only its targets moved.
- The `AdminNav component is deleted; header carries the admin links` test: keep `expect(fs.existsSync(ADMIN_NAV)).toBe(false)`; change the expected header items from `['Manage SOPs', 'Create New SOP', 'Content', 'Team', 'Settings']` to `['Governance', 'Create New SOP', 'Content', 'Team', 'Settings']`; change the expected hrefs from `'/admin/sops'` to `'/sops?view=attention'`; change the final "attention view stays reachable" assertion from reading the admin sops page to reading `src/app/(protected)/sops/page.tsx` and asserting it resolves `view=attention`. Add a new assertion that the header contains `Manage SOPs` zero times (SUR-03 — the entry must not come back).
- The `no admin page mounts AdminNav … guards survive` test: `ADMIN_PAGES[0]` is now the shim. Keep it in the list — the `['admin', 'safety_manager']` guard assertion is exactly what must survive the rewrite — and add an assertion that the shim renders no UI (`expect(shimSrc).not.toContain('<div')`).
- Leave the `/admin/settings` and account-menu tests untouched.

Repoint `tests/phase30/governance-fold.spec.ts`: the `renders the needs-attention view behind ?view=attention` assertion moves from the admin page to `src/components/sop/lenses/AdminAttentionLens.tsx` + `src/app/(protected)/sops/page.tsx`; the `/admin/governance is a redirect shim` assertion's expected destination becomes `/sops?view=attention`; the `in-flight SOPs and the attention view are both reachable from the scope column` assertion moves to `sops/page.tsx`'s `ADMIN_SCOPES`. Keep the `approveStep stays wired` and `every App Router page route is mapped` tests unchanged — the latter is the 0-not-mapped gate and must go green against the updated `journeys.ts`.

Then flip `tests/phase41/nav-and-shim.spec.ts` live (remove every `test.fixme`) and make its assertions real: the SUR-03 header contract; the shim's guard-first ordering, `URLSearchParams` construction, seven pass-through params and fixed `/sops` path; the shim importing none of the admin components; and the SUR-04 single-list→builder-chain assertion (exactly one component reachable from the list links directly to `/admin/sops/builder/` — `SopMillerBrowser` — while `SopWorkerBrowser.tsx` has zero such hrefs, AND `src/app/(protected)/sops/[sopId]/page.tsx:73` still carries its `Edit in builder` link, which is the documented second DESTINATION rather than a second list→builder chain).

Do NOT touch the seven other legacy specs that read `admin/sops/page.tsx` (`tests/phase28/*`, `tests/phase29/*`, `tests/phase30/create-entry.spec.ts`, `tests/phase30/list-rows.spec.ts`, `tests/phase32/*`, `tests/phase33/*`, `tests/sb-auth-builder.test.ts`) — plan 41-08 owns those, and splitting the two keeps each diff reviewable. They will be RED between this plan and 41-08; record that explicitly in the summary so the wave-4 verifier does not read it as a new break.
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase41` exits 0 with no `test.fixme` remaining in `tests/phase41/nav-and-shim.spec.ts`
    - `npx playwright test --project=phase30 -g "AdminNav component is deleted"` passes
    - `npx playwright test --project=phase30 -g "every App Router page route is mapped"` passes (0 not-mapped)
    - `npx playwright test --project=phase30 -g "redirect shim"` passes with the `/sops?view=attention` destination
    - the SUR-04 assertions pass and `src/app/(protected)/sops/[sopId]/page.tsx` still contains `/admin/sops/builder/`
    - Mutation proof (run, then revert): re-add `{ label: 'Manage SOPs', href: '/admin/sops' }` to `ADMIN_LINKS` → both the phase30 and phase41 nav assertions fail. Revert.
    - Mutation proof (run, then revert): move the shim's admin guard AFTER the redirect → the guard-first positional assertion fails. Revert.
    - The summary lists, by filename, every spec left red for 41-08
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase41 --project=phase30</automated>
  </verify>
  <done>The two Phase 30 guards that pinned the moved code are repointed in the same commit, the nav/shim contract is live and mutation-proven, and the remaining stale specs are explicitly handed to 41-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated / non-admin request → `/admin/sops` | a redirect target leaks information about internal structure if it is emitted before the guard runs |
| user-supplied query string → the shim's redirect target | seven params are copied from the request into a `Location` header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03 | Information Disclosure / Spoofing (open redirect) | `/admin/sops` shim | mitigate | The redirect PATH is the string constant `/sops`; only the query string is built from request values, via `URLSearchParams` (which percent-encodes), so no supplied value can introduce a scheme, host or path traversal. Asserted: the destination is never a template hole filled from `params` |
| T-41-03b | Information Disclosure | `/admin/sops` shim | mitigate | `getSessionContext()` → `redirect('/login')` → role gate → only then the destination redirect, copied from the Phase 30 governance shim. Guard-first ordering asserted POSITIONALLY and mutation-proven |
| T-41-09 | Tampering (a guard that stopped guarding) | `tests/phase30/admin-nav.spec.ts`, `tests/phase30/governance-fold.spec.ts` | mitigate | Both repointed in the same commit as the move (CLAUDE.md 2026-07-13); the shim retains the `['admin', 'safety_manager']` literal specifically so the surviving guard assertion still means something |
| T-41-10 | Repudiation (a dead href presenting as a crash) | every internal `/admin/sops` link | mitigate | The shim keeps the route alive, so no link 404s even before 41-07's sweep repoints them; the sweep then removes the extra hop. This ordering is deliberate — deleting the route first would reproduce the CLAUDE.md 2026-06-08 dead-href class |
| T-41-02 | Information Disclosure | worker bundle | mitigate | The shim imports none of the lens components; the temporary allowlist entry is removed from `tests/lint/no-static-admin-lens-import.spec.ts` in this same commit, tightening the guard to the three lens files |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Changed by this plan:
- `src/app/(protected)/admin/sops/page.tsx` → redirect shim (seven pass-through params)
- `src/app/(protected)/admin/governance/page.tsx` → retargeted to `/sops?view=attention`
- `ADMIN_LINKS` in `src/components/layout/TopHeader.tsx` → `Governance` replaces `Manage SOPs`
- `roleHome('admin')` → `/sops`
- `src/lib/journeys/journeys.ts` → merged-surface steps + one shim step
- Repointed: `tests/phase30/admin-nav.spec.ts`, `tests/phase30/governance-fold.spec.ts`
- `tests/phase41/nav-and-shim.spec.ts` live

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0; both gated routes green
- `npx playwright test --project=phase41 --project=phase30 --project=phase15-stubs` exits 0
- `npx playwright test` (full suite) will be RED on the seven specs handed to 41-08; the summary must name each one. No other spec may be red.
</verification>

<success_criteria>
- `/admin/sops` renders nothing and redirects to `/sops` with every param preserved, guard first
- Exactly one SOPs nav entry; `Manage SOPs` gone; Governance deep-links `/sops?view=attention`
- `roleHome('admin')` is `/sops`
- `journeys.ts` maps the merged surface and the shim; `/pathways` reports 0 not-mapped
- The two Phase 30 guards are repointed in this commit, not deferred
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-06-SUMMARY.md` when done. List by filename every spec left red for 41-08, and record the shim's exact param→scope mapping.
</output>
</content>
