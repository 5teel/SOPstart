---
phase: 41-one-sop-surface
plan: 08
type: execute
wave: 5
depends_on: ["41-06"]
files_modified:
  - tests/phase28/governance-queue.spec.ts
  - tests/phase28/library-and-worker.spec.ts
  - tests/phase29/queue-approve-action.spec.ts
  - tests/phase30/create-entry.spec.ts
  - tests/phase30/list-rows.spec.ts
  - tests/phase32/library-filter-deeplink.spec.ts
  - tests/phase32/wire-up-mode.spec.ts
  - tests/phase33/sop-drilldown.spec.ts
  - tests/sb-auth-builder.test.ts
  - tests/phase41/spec-repoint-inventory.spec.ts
autonomous: true
requirements: [SUR-01, SUR-02, SUR-04]

must_haves:
  truths:
    - "Every requirement contract previously pinned to admin/sops/page.tsx is asserted at the code's new home"
    - "The full Playwright suite is green"
    - "No spec file reads src/app/(protected)/admin/sops/page.tsx except the two that assert the shim itself"
    - "The approval-chain gating, owner filter, no-audience-department and single-join-read contracts all still have a live guard"
  artifacts:
    - path: "tests/phase41/spec-repoint-inventory.spec.ts"
      provides: "Live guard that no legacy spec is left pointing at the emptied page"
      min_lines: 40
  key_links:
    - from: "tests/phase28/governance-queue.spec.ts"
      to: "src/components/sop/lenses/AdminAttentionLens.tsx"
      via: "repointed source-contract path constant"
      pattern: "AdminAttentionLens"
    - from: "tests/phase32/wire-up-mode.spec.ts"
      to: "src/actions/admin-access-view.ts"
      via: "repointed source-contract path constant"
      pattern: "admin-access-view"
---

<objective>
Repoint the nine legacy source-contract specs that read `src/app/(protected)/admin/sops/page.tsx` onto the code's new homes, and flip the repoint-inventory guard live.

These are not stale-test housekeeping. Between them they are the only live guards on the governance-queue wiring (GQ-01), the owner filter (OWN-04/D28-08), the approval-step gating (APR-03/04), the Phase 32 deep-link filters, the Phase 33 access-view single-join read, the UX-06 one-line-row contract and the no-audience department semantics. A guard pointing at an emptied file is a guard that stopped guarding (CLAUDE.md 2026-07-13), and several of these sit on safety-critical paths.

Purpose: restore a fully green suite with every pre-existing contract still enforced, at the new location, in one reviewable diff.
Output: nine repointed spec files, one live inventory guard.

This plan runs in parallel with 41-07 — different files, no shared assertions. It touches NO file under `src/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-02-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-03-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-04-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the governance, owner-filter and approval-gating specs (phase28, phase29)</name>
  <files>tests/phase28/governance-queue.spec.ts, tests/phase28/library-and-worker.spec.ts, tests/phase29/queue-approve-action.spec.ts</files>
  <read_first>
    - tests/phase28/governance-queue.spec.ts (FULL file — the `FOLDED_PAGE` constant at :32 and every test that reads it: `calls listGovernanceQueue`, `guards admin/safety_manager role and redirects otherwise`, `serves the attention view behind ?view=attention`, `renders the queue rows grouped by worst flag`)
    - tests/phase28/library-and-worker.spec.ts (FULL file — the `LIBRARY_PAGE` constant at :35 and the owner-filter / column-select / owner-label / flag-chip / attention-grouping tests)
    - tests/phase29/queue-approve-action.spec.ts (FULL file — the `LIBRARY_PAGE` constant at :28 and the `GovernanceFilterChips is gone` + `awaiting_approval is a grouped section` tests; the APR-03/04 `approveStep` assertions read `GovernanceQueueRow.tsx`, which did NOT move)
    - src/components/sop/lenses/AdminAttentionLens.tsx, src/actions/admin-sop-list.ts (the new homes, per 41-02-SUMMARY and 41-04-SUMMARY)
    - src/app/(protected)/admin/sops/page.tsx (the shim — confirm what genuinely still belongs there: the `['admin', 'safety_manager']` guard, and nothing else)
  </read_first>
  <action>
For each file, replace the path constant and move each assertion to the file that now owns the behaviour. Do not weaken or delete an assertion to make it pass — if a contract has no new home, stop and report it (that means the merge dropped a behaviour).

`tests/phase28/governance-queue.spec.ts`:
- `calls listGovernanceQueue` → assert against `src/components/sop/lenses/AdminAttentionLens.tsx`
- `guards admin/safety_manager role and redirects otherwise` → split: the SHIM still redirects non-admins (`admin/sops/page.tsx`, keep this assertion there), and the DATA is now guarded by `listGovernanceQueue`'s own `requireAdminContext()` — add an assertion against `src/actions/governance.ts` so the real gate is pinned, not just the shim's
- `serves the attention view behind ?view=attention` → assert `src/app/(protected)/sops/page.tsx` resolves `view=attention` to the attention scope
- `renders the queue rows grouped by worst flag` → assert against `AdminAttentionLens.tsx` (`FLAG_PRIORITY.find(`, `GovernanceQueueRow`, no chip row)
- leave the tests that read `GovernanceQueueRow.tsx` / `GovernanceOwnerCell` / other unmoved files untouched

`tests/phase28/library-and-worker.spec.ts`:
- owner filter (`.eq('owner_user_id'`), the `owner_user_id, review_due_at` column select, the flag-chip-per-row mapping → `src/actions/admin-sop-list.ts`
- the `Owned by me` chip → `src/app/(protected)/sops/page.tsx` (it is now a scope row in the Miller column)
- the owner LABEL on each one-line row → `src/actions/admin-sop-list.ts` (`ownerLabel` on `MillerSop`) and/or `src/components/admin/SopMillerBrowser.tsx`, whichever actually renders it
- the attention-view grouping assertion → `AdminAttentionLens.tsx`
- leave the worker-side tests in this file untouched

`tests/phase29/queue-approve-action.spec.ts`:
- the `approveStep` / `handleApprove` / `awaiting_approval && isCallerNextApprover` assertions read `GovernanceQueueRow.tsx` and must NOT be changed — that file did not move, and 41-04 duplicated these assertions into `tests/phase41/status-attention-lenses.spec.ts` precisely so this contract was never unguarded during the move. Keep both copies; a safety gate with two guards is not a duplication problem.
- `GovernanceFilterChips is gone from the page and from disk` → keep the on-disk absence assertion; repoint the "from the page" half to `AdminAttentionLens.tsx`
- `awaiting_approval is a grouped section with a plain-language blurb` → `AdminAttentionLens.tsx` (`FLAG_DESC`)

Update each file's header comment to record where the assertions moved and why (the phase and the commit intent), so the next reader does not have to reconstruct it.
  </action>
  <acceptance_criteria>
    - none of the three files contains the string `'admin', 'sops', 'page.tsx'` except where it asserts the SHIM's surviving guard
    - `tests/phase28/governance-queue.spec.ts` references `AdminAttentionLens` and `src/actions/governance.ts`
    - `tests/phase28/library-and-worker.spec.ts` references `admin-sop-list`
    - `tests/phase29/queue-approve-action.spec.ts` still asserts `approveStep`, `isCallerNextApprover` and `handleApprove` against `GovernanceQueueRow.tsx` (unchanged)
    - `npx playwright test --project=phase28 --project=phase29` exits 0
    - no test was deleted or converted to `test.fixme` — `git diff` shows zero net reduction in `test(` count across the three files
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase28 --project=phase29</automated>
  </verify>
  <done>The governance-queue, owner-filter and approval-gating contracts are all live again at the code's new homes, with the approval gate double-guarded across the move.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the row-contract and create-entry specs (phase30)</name>
  <files>tests/phase30/create-entry.spec.ts, tests/phase30/list-rows.spec.ts</files>
  <read_first>
    - tests/phase30/list-rows.spec.ts (FULL file — the `ADMIN_SOPS_PAGE` constant at :23 and every test: `admin rows contain no SopDepartmentEditor / LibraryReviewCell / icon-only actions`, `builder shell owns a labelled action menu`, `row is one line: title + status chip + one flag chip, and reaches the builder`, `selecting a SOP is client state, not a URL push`, `the detail pane fixes what it surfaces`, `"No department" is a reachable scope`, `"No department" means no AUDIENCE — all_departments does not count as unassigned`)
    - tests/phase30/create-entry.spec.ts (:19-30 the four path constants; :78-92 the `/admin/sops has no duplicate create entry` test which reads the admin page for absent intake hrefs and asserts `TopHeader` carries `{ label: 'Create New SOP', href: '/admin/sops/new' }`; :94-104 the src-wide stray-intake-href sweep with its `allowed` set; :105-114 the journeys + worker-tab tests)
    - src/actions/admin-sop-list.ts, src/components/sop/lenses/AdminStatusLens.tsx, src/app/(protected)/sops/page.tsx (the new homes)
    - src/components/admin/SopMillerBrowser.tsx (unmoved — the row/detail-pane assertions that already read this file stay as they are)
  </read_first>
  <action>
`tests/phase30/list-rows.spec.ts` — repoint `ADMIN_SOPS_PAGE` usage per assertion:
- `row is one line … and reaches the builder` and `selecting a SOP is client state, not a URL push` and `the detail pane fixes what it surfaces`: the halves that read `SopMillerBrowser.tsx` are unchanged; the halves that read the admin page (e.g. `expect(page).toContain('<SopMillerBrowser')`) repoint to `src/components/sop/lenses/AdminStatusLens.tsx`
- `"No department" is a reachable scope, not a dead label`: the scope row now lives in `src/app/(protected)/sops/page.tsx` (rendered from `scopeDepartments` / `noAudienceCount`) — assert the page renders the scope and that `src/actions/admin-sop-list.ts` supplies `noAudienceCount`
- `"No department" means no AUDIENCE — all_departments does not count as unassigned`: repoint to `src/actions/admin-sop-list.ts` and keep BOTH clauses pinned (`!tagged.has(...)` and `!r.all_departments` in the same expression). This assertion is the only guard on a bug whose symptom is "the fix looks like it did nothing" — do not soften it
- `admin rows contain no SopDepartmentEditor / LibraryReviewCell / icon-only actions`: repoint the page half to `AdminStatusLens.tsx`; keep the on-disk absence assertions
- `builder shell owns a labelled action menu`: reads builder files only — leave untouched

`tests/phase30/create-entry.spec.ts`:
- `/admin/sops has no duplicate create entry`: the shim renders nothing, so the "no intake href on the page" half is now trivially true — repoint it to `src/app/(protected)/sops/page.tsx` (the merged surface must still NOT carry a create entry; "Create New SOP" stays in the header until Phase 42 moves it). Keep the `TopHeader` assertion for `{ label: 'Create New SOP', href: '/admin/sops/new' }` exactly as is
- `no stray intake hrefs anywhere in src outside the picker`: the `allowed` set already contains `WORKER_SOPS_PAGE`; leave the sweep intact and drop `ADMIN_SOPS_PAGE` from any allowance it no longer needs
- `journeys.ts maps the /admin/sops/new method picker` and `worker /sops "Create SOP" tab removed`: unchanged
- rename `ADMIN_SOPS_PAGE` to `ADMIN_SOPS_SHIM` where it still points at the shim, so the constant's name stops lying
  </action>
  <acceptance_criteria>
    - `tests/phase30/list-rows.spec.ts` references `AdminStatusLens` and `admin-sop-list`, and still asserts both clauses of the no-audience rule in one expression
    - `tests/phase30/create-entry.spec.ts` asserts the merged surface carries no intake href, and still asserts `{ label: 'Create New SOP', href: '/admin/sops/new' }` in `TopHeader`
    - neither file contains a constant named `ADMIN_SOPS_PAGE` pointing at a file that renders nothing
    - `npx playwright test --project=phase30` exits 0
    - no test deleted or converted to `test.fixme`
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase30</automated>
  </verify>
  <done>The UX-06 row contract, the no-audience department semantics and the one-create-entry rule are live again at the new homes.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the access-view specs and flip the inventory guard live (phase32, phase33, sb-auth)</name>
  <files>tests/phase32/library-filter-deeplink.spec.ts, tests/phase32/wire-up-mode.spec.ts, tests/phase33/sop-drilldown.spec.ts, tests/sb-auth-builder.test.ts, tests/phase41/spec-repoint-inventory.spec.ts</files>
  <read_first>
    - tests/phase32/library-filter-deeplink.spec.ts (FULL file — `PAGE_PATH` at :22; the `page.tsx server-filters the SOP list by ?departments=/?collection= and renders an Open in library (N) header` test, and the `WiringPatchBay exposes a focus-based Open in library deep-link` test whose expected hrefs 41-07 just changed to `/sops?...`)
    - tests/phase32/wire-up-mode.spec.ts (FULL file — `PAGE` at :31; especially `the page resolves the pinned SOP's collections server-side via ensureSopCollections (CR-02 runtime path)` at :113 and the grant/blast-radius tests that read `WiringPatchBay.tsx`)
    - tests/phase33/sop-drilldown.spec.ts (FULL file — `PAGE` at :36; the single `.in(collection_id, ids)` join-read test at :43 and the `?sop= survives only as a deep-link pre-expand` test at :80)
    - tests/sb-auth-builder.test.ts (:101-125 — the SB-AUTH-05 test reads `src/app/(protected)/admin/sops/page.tsx` for `source_type` and `/admin/sops/new`)
    - src/actions/admin-access-view.ts, src/actions/admin-sop-list.ts, src/components/sop/lenses/AdminAccessLens.tsx, src/components/sop/lenses/AdminStatusLens.tsx, src/app/(protected)/sops/page.tsx
    - tests/phase41/spec-repoint-inventory.spec.ts (the fixme stub from 41-01 Task 2)
  </read_first>
  <action>
`tests/phase32/library-filter-deeplink.spec.ts`: the server-side `?departments=`/`?collection=` filtering assertions repoint to `src/actions/admin-sop-list.ts`; the `Open in library (N)` header assertion repoints to `src/components/sop/lenses/AdminStatusLens.tsx`; the `WiringPatchBay exposes a focus-based deep-link` expected hrefs become `/sops?departments=` and `/sops?collection=` (41-07 changed them). Add an assertion that `src/app/(protected)/sops/page.tsx` drops `departments`/`collection` when the resolved scope is the access lens — the `!isAccessView` precedence this spec's original page carried.

`tests/phase32/wire-up-mode.spec.ts`: the `ensureSopCollections` CR-02 runtime-path assertion repoints to `src/actions/admin-access-view.ts` and must keep its ORDERING character (ensure-before-collections-read), not degrade to mere presence. Every test that reads `WiringPatchBay.tsx` / `SelectionStrip` is unchanged.

`tests/phase33/sop-drilldown.spec.ts`: the single `.in('collection_id', ids)` join-read assertion and the `?sop=` pre-expand assertion repoint to `src/actions/admin-access-view.ts`; the `?sop=` deep-link entry point additionally repoints to `src/app/(protected)/sops/page.tsx` (which resolves `sop` and passes it as `pinnedSopId`). Every `WiringPatchBay.tsx` test is unchanged.

`tests/sb-auth-builder.test.ts` SB-AUTH-05: `source_type` is now selected by `src/actions/admin-sop-list.ts` (its `SOP_SELECT` column list) and the one create entry is `TopHeader`'s `/admin/sops/new` — repoint both halves accordingly and update the inline "Repointed 2026-07-13" comment with a new dated line explaining the Phase 41 move. The publish-route assertion is unchanged.

Then flip `tests/phase41/spec-repoint-inventory.spec.ts` live: remove the `test.fixme`, walk `tests/` for any reference to `src/app/(protected)/admin/sops/page.tsx` in any form (a `path.join(... 'admin', 'sops', 'page.tsx')` call or a literal path string), and assert the only files that reference it are the allowlist — `tests/phase41/nav-and-shim.spec.ts`, `tests/phase30/admin-nav.spec.ts`, and any file from this task that legitimately still asserts the shim's guard. Print offenders as `file:line`. Build the allowlist from the actual final state, and state in a comment WHY each entry is allowed.
  </action>
  <acceptance_criteria>
    - `npx playwright test` (FULL suite, every project) exits 0
    - `npx playwright test --project=phase41 -g "repoint"` passes with no fixme remaining in `tests/phase41/spec-repoint-inventory.spec.ts`
    - `tests/phase32/wire-up-mode.spec.ts` still asserts the ensure-before-collections-read ORDER, not just the presence of `ensureSopCollections`
    - `tests/phase33/sop-drilldown.spec.ts` still asserts exactly one `.in('collection_id'` read
    - Mutation proof (run, then revert): repoint one spec back to `src/app/(protected)/admin/sops/page.tsx` → `tests/phase41/spec-repoint-inventory.spec.ts` fails naming that file. Revert.
    - `git diff --stat` shows this plan touched zero files under `src/`
    - no test deleted or converted to `test.fixme` across the four repointed files
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>The full suite is green with every Phase 32/33 access-view and Phase 2 authoring contract enforced at the new homes, and a mutation-proven inventory guard prevents any spec being left pointed at the emptied page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-contract spec → the code it claims to guard | a spec reading an emptied file asserts nothing while reporting green or red for the wrong reason |
| repoint edit → assertion strength | the easiest way to make a repointed assertion pass is to weaken it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09 | Tampering (a guard that stopped guarding) | nine legacy source-contract specs | mitigate | Every assertion is moved to the code's new home rather than deleted; `git diff` asserted to show zero net reduction in `test(` count; `tests/phase41/spec-repoint-inventory.spec.ts` goes live and is mutation-proven so no spec can be left pointed at the shim |
| T-41-06 | Tampering (regression to a safety gate) | APR-03/04 approval-step gating | mitigate | The `approveStep` / `isCallerNextApprover` / `handleApprove` assertions read `GovernanceQueueRow.tsx`, which did not move, and are deliberately duplicated in `tests/phase41/status-attention-lenses.spec.ts` (41-04) so the gate had two guards throughout the move |
| T-41-13 | Tampering | the no-audience department rule and the CR-02 ensure-ordering | mitigate | Both explicitly forbidden from degrading to presence-only assertions; the ordering character of the CR-02 assertion and both clauses of the no-audience expression are named in the acceptance criteria |
| T-41-14 | Repudiation | the merge dropped a behaviour with no new home | mitigate | The task instruction is to STOP AND REPORT rather than delete an assertion that has nowhere to go — a contract with no new home means the merge lost a capability, which is a finding, not a test problem |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Changed by this plan (test files only, no `src/`):
- `tests/phase28/governance-queue.spec.ts`, `tests/phase28/library-and-worker.spec.ts`, `tests/phase29/queue-approve-action.spec.ts`
- `tests/phase30/create-entry.spec.ts`, `tests/phase30/list-rows.spec.ts`
- `tests/phase32/library-filter-deeplink.spec.ts`, `tests/phase32/wire-up-mode.spec.ts`, `tests/phase33/sop-drilldown.spec.ts`
- `tests/sb-auth-builder.test.ts`
- `tests/phase41/spec-repoint-inventory.spec.ts` live

<verification>
- `npx playwright test` (full suite, all projects) exits 0 — this is the plan's whole point
- `npx tsc --noEmit` exits 0 (specs are type-checked; prefer `[\s\S]` over the `/s` regex flag, CLAUDE.md 2026-06-02)
- `git diff --stat src/` is empty
</verification>

<success_criteria>
- Full suite green with zero deleted or fixme'd tests
- Every repointed assertion pins the same behaviour it pinned before, at the new file
- The no-audience rule, the CR-02 ensure-ordering and the single-join read keep their original strength
- The inventory guard is live and mutation-proven
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-08-SUMMARY.md` when done. Record a from→to table of every repointed assertion, and flag any contract that had no new home.
</output>
</content>
