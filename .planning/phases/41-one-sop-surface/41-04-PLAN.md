---
phase: 41-one-sop-surface
plan: 04
type: execute
wave: 2
depends_on: ["41-02"]
files_modified:
  - src/components/sop/lenses/AdminStatusLens.tsx
  - src/components/sop/lenses/AdminAttentionLens.tsx
  - tests/phase41/status-attention-lenses.spec.ts
autonomous: true
requirements: [SUR-02, SUR-04, SUR-05]

must_haves:
  truths:
    - "The admin status/drafts/published list renders inside the Miller frame from one action call"
    - "The governance queue renders grouped worst-flag-first, every flagged SOP appearing exactly once"
    - "The 'Open in library (N)' filtered banner still appears for a ?collection= deep link"
    - "There is exactly one direct list→builder link, inside SopMillerBrowser"
  artifacts:
    - path: "src/components/sop/lenses/AdminStatusLens.tsx"
      provides: "Client lens wrapping SopMillerBrowser unchanged, fed by listAdminSopRows"
      min_lines: 50
    - path: "src/components/sop/lenses/AdminAttentionLens.tsx"
      provides: "Client lens rendering the grouped governance queue from listGovernanceQueue"
      min_lines: 50
  key_links:
    - from: "src/components/sop/lenses/AdminStatusLens.tsx"
      to: "src/actions/admin-sop-list.ts"
      via: "useQuery queryFn"
      pattern: "listAdminSopRows"
    - from: "src/components/sop/lenses/AdminAttentionLens.tsx"
      to: "src/actions/governance.ts"
      via: "useQuery queryFn"
      pattern: "listGovernanceQueue"
    - from: "src/components/sop/lenses/AdminAttentionLens.tsx"
      to: "src/lib/governance/flag-display.ts"
      via: "FLAG_PRIORITY grouping"
      pattern: "FLAG_PRIORITY"
---

<objective>
Build the two remaining admin lenses as client components: `AdminStatusLens` (wraps `SopMillerBrowser` unchanged, fed by `listAdminSopRows`) and `AdminAttentionLens` (renders the grouped governance queue from `listGovernanceQueue` + `GovernanceQueueRow`, both unchanged).

Purpose: SUR-02 — draft/published status and the governance queue become lenses. Neither wrapped component is modified; only their data plumbing moves from a server page's `Promise.all` into a client `useQuery`.
Output: `AdminStatusLens`, `AdminAttentionLens`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-RESEARCH.md
@.planning/phases/41-one-sop-surface/41-PATTERNS.md
@.planning/phases/41-one-sop-surface/41-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AdminStatusLens</name>
  <files>src/components/sop/lenses/AdminStatusLens.tsx</files>
  <read_first>
    - src/components/admin/SopMillerBrowser.tsx (:52-62 the exact prop contract `{ sops, scopeLabel, departments }`; :64-72 the empty state; :105-115 the below-lg row→builder `<Link>`; :155-175 the detail pane's `Open` / `Versions` links — this is the ONE direct list→builder chain SUR-04 permits)
    - src/app/(protected)/admin/sops/page.tsx (:527-538 the `filterIds !== null && !departmentFilter` "Open in library (N)" banner with its `Clear filter` link; :694 the `<SopMillerBrowser scopeLabel sops departments />` call site)
    - src/actions/admin-sop-list.ts and src/lib/sop-list/admin-rows.ts (the `AdminSopListResult` field list from 41-02)
    - src/app/(protected)/sops/page.tsx (:560-565 the pulse-skeleton idiom; :510 the Miller grid classes — this lens renders INTO that grid's `lg:col-span-2` region)
  </read_first>
  <action>
Create `src/components/sop/lenses/AdminStatusLens.tsx`, `'use client'`.

Props: `{ status: 'all' | 'draft' | 'published' | 'failed'; ownerOnly: boolean; departments?: string; collection?: string; onResult?: (r: AdminSopListResult) => void; onClearFilter: () => void }`.

`onResult` exists so the merged page can render the admin scope-column counts (`railCounts`, `scopeDepartments`, `noAudienceCount`, `flaggedCount`) from the same single fetch instead of issuing a second one. Call it from a `useEffect` keyed on the query data, never during render.

Body: one `useQuery` with `queryKey: ['admin-sop-rows', status, ownerOnly, departments ?? null, collection ?? null]`, `queryFn: () => listAdminSopRows({ status, owner: ownerOnly ? 'me' : undefined, departments, collection })`, `staleTime: 1000 * 60` (matches the worker surface's 1-2 minute staleness convention).

Render:
- loading → the pulse-skeleton idiom from `sops/page.tsx:560-565`, spanning `lg:col-span-2`
- `{ error }` → the `blueprint-frame` ERROR panel shape used elsewhere, rendering the returned error string
- success → when `data.filtered && !departments` is true, the "Open in library (N)" banner above the browser with a `Clear filter` control that is a `<button onClick={onClearFilter}>`, NOT a `<Link href="/admin/sops">` (scope/filter changes are client state on the merged page — RESEARCH Pitfall 3); then `<SopMillerBrowser sops={data.sops} scopeLabel={data.scopeLabel} departments={data.departments} />`

Do not modify `SopMillerBrowser`. Its below-lg row link and its detail-pane `Open` button are the single permitted direct list→builder chain (SUR-04); leave both exactly as they are and add no builder affordance of your own.

Copy the banner's class strings verbatim from the source render rather than re-deriving token names (`var(--accent-step)` etc. — an undefined custom property renders nothing and fails no gate; CLAUDE.md 2026-07-14).

`SopMillerBrowser`, `DepartmentPicker` and `setSopCategory` reach the client only through this file and the `dynamic()` boundary that 41-05 puts on `sops/page.tsx`; `tests/lint/no-static-admin-lens-import.spec.ts` allowlists this file for that import.
  </action>
  <acceptance_criteria>
    - line 1 is `'use client'`; the file contains `useQuery`, `listAdminSopRows`, `SopMillerBrowser`, `onClearFilter`, `Open in library`
    - the file contains zero occurrences of `router.push` and zero `href=` attributes
    - `onResult` is invoked inside a `useEffect`, not in the render body (assert `useEffect` appears within 6 lines before the `onResult?.(` call)
    - the loading, error and success branches all exist
    - `grep -c "admin/sops/builder" src/components/sop/lenses/AdminStatusLens.tsx` is 0 — the builder link stays inside `SopMillerBrowser`
    - `npx tsc --noEmit` exits 0; `npx playwright test --project=phase15-stubs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase15-stubs</automated>
  </verify>
  <done>The admin status list renders from one action call inside the Miller frame, with the filtered banner preserved and no new route chain to the builder.</done>
</task>

<task type="auto">
  <name>Task 2: Build AdminAttentionLens</name>
  <files>src/components/sop/lenses/AdminAttentionLens.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/page.tsx (:384-394 the `rowFlag` worst-flag-per-row derivation and `attentionGroups`; :489-523 the whole attention-view render — the error panel, the "Nothing needs attention" CLEAR panel, and the per-flag `<section>` with its flag chip · count · hairline · blurb header and `<ul>` of `GovernanceQueueRow`)
    - src/components/admin/governance/GovernanceQueueRow.tsx (:1-30 — it is already `'use client'`, takes `{ row }`, and owns its own `approveStep` + `isCallerNextApprover` gating; DO NOT modify it and do not refactor it to import the new flag-display module — `tests/phase30/list-rows.spec.ts` is the sync contract for its private FLAG maps)
    - src/actions/governance.ts (:321-330 — `listGovernanceQueue()` returns `{ success: true, rows }` or `{ error }` and already self-guards)
    - src/lib/governance/flag-display.ts (from 41-02 — `FLAG_PRIORITY`, `FLAG_STYLE`, `FLAG_LABEL`, `FLAG_DESC`)
  </read_first>
  <action>
Create `src/components/sop/lenses/AdminAttentionLens.tsx`, `'use client'`.

Props: `{ onBack: () => void }` — same callback-exit contract as `AdminAccessLens`.

Body: one `useQuery` with `queryKey: ['governance-queue']`, `queryFn: listGovernanceQueue`, `staleTime: 1000 * 60`. Derive, from the returned rows: `flaggedRows` (rows with at least one flag), `rowFlag` (each row's WORST flag via `FLAG_PRIORITY.find`), and `attentionGroups` (`FLAG_PRIORITY` order, each group holding only the rows whose worst flag is that flag, groups with zero rows dropped) — copied from `page.tsx:384-394`. Every flagged SOP appears exactly once, under its worst flag; there is no chip row (sketch 004).

Render, reproducing `page.tsx:489-523` verbatim in structure and copy:
- loading → pulse skeleton
- `{ error }` → the `blueprint-frame` ERROR panel with the returned string
- zero groups → the CLEAR panel: `Nothing needs attention` / `Every SOP is owned, current, and correctly assigned.`
- otherwise → one `<section>` per group with the `FLAG_LABEL · count` chip styled by `FLAG_STYLE[flag]`, the hairline spacer, the `FLAG_DESC[flag]` blurb, and a `<ul>` of `<GovernanceQueueRow key={row.id} row={row} />`

Above all states, the `← Back to your SOPs` `<button onClick={onBack}>` using the same `mono text-[11px] uppercase tracking-wider` classes as the current back link. Like the access lens, this lens renders FULL WIDTH replacing the Miller frame — that is exactly what the current page does for `?view=attention`, so the wide grouped queue keeps its width.

Copy class strings verbatim; declare no new CSS custom properties.
  </action>
  <acceptance_criteria>
    - line 1 is `'use client'`; the file contains `useQuery`, `listGovernanceQueue`, `GovernanceQueueRow`, `FLAG_PRIORITY`, `FLAG_DESC`, `onBack`
    - the file contains `Nothing needs attention` and `Every SOP is owned, current, and correctly assigned.`
    - the file contains zero occurrences of `router.push`, `<Link`, `href=`
    - the worst-flag derivation uses `FLAG_PRIORITY.find(` (one flag per row), and groups are built in `FLAG_PRIORITY` order with empty groups filtered out
    - `src/components/admin/governance/GovernanceQueueRow.tsx` is byte-identical to its pre-plan state (`git diff --stat` shows it untouched)
    - `npx tsc --noEmit` exits 0; `npx playwright test --project=phase15-stubs --project=phase28 --project=phase29` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase15-stubs --project=phase28 --project=phase29</automated>
  </verify>
  <done>The grouped worst-flag-first governance queue renders as a client lens from the existing guarded action, with `GovernanceQueueRow` and its approval gating untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Source-contract spec for both lenses</name>
  <files>tests/phase41/status-attention-lenses.spec.ts</files>
  <read_first>
    - tests/phase28/governance-queue.spec.ts (:43-75 — the attention-view assertions currently pinned to `admin/sops/page.tsx`; this spec asserts them at the new home and 41-08 repoints the old ones)
    - tests/phase30/list-rows.spec.ts (:83-140 — the row/detail-pane and "selecting a SOP is client state, not a URL push" assertions)
  </read_first>
  <action>
Create `tests/phase41/status-attention-lenses.spec.ts` (LIVE). Assert on comment-stripped source.

`AdminStatusLens.tsx`: calls `listAdminSopRows` in a `useQuery`; renders `SopMillerBrowser`; contains `Open in library`; `onClearFilter` is a button handler not a link (no `href=`, no `<Link`, no `router.push`); contains zero `admin/sops/builder` references; `onResult` is called inside a `useEffect`.

`AdminAttentionLens.tsx`: calls `listGovernanceQueue` in a `useQuery`; imports `FLAG_PRIORITY`/`FLAG_STYLE`/`FLAG_LABEL`/`FLAG_DESC` from `@/lib/governance/flag-display`; renders `GovernanceQueueRow`; derives one worst flag per row via `FLAG_PRIORITY.find(`; contains the CLEAR-panel copy; no `href=`/`<Link`/`router.push`.

Preservation assertions (the APR-03/04 hard constraint — approval gating must survive the relocation): `src/components/admin/governance/GovernanceQueueRow.tsx` still imports `approveStep` from `@/actions/approvals`, still gates the Approve branch on `awaiting_approval && isCallerNextApprover`, and its Approve `onClick` is still wired to `handleApprove`. These assertions duplicate `tests/phase29/queue-approve-action.spec.ts` deliberately — that file is being repointed in 41-08, and the safety-critical assertion must not be in flight unguarded during that move.

SUR-04 assertion: across `src/components/sop/lenses/`, `src/components/sop/SopWorkerBrowser.tsx` and `src/app/(protected)/sops/page.tsx`, exactly zero files contain an `/admin/sops/builder` href — the single direct list→builder link lives in `src/components/admin/SopMillerBrowser.tsx` (assert it is still there).
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase41 -g "lens"` passes and selects at least 12 tests
    - Mutation proof (run, then revert): add `<Link href={'/admin/sops/builder/x'}>` to `AdminStatusLens.tsx` → the SUR-04 assertion fails. Revert.
    - Mutation proof (run, then revert): change `FLAG_PRIORITY.find(` to `FLAG_PRIORITY.filter(` in the attention lens → the worst-flag assertion fails. Revert.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase41</automated>
  </verify>
  <done>Both lenses' data wiring, callback-exit contract, worst-flag grouping and the single-builder-chain rule are pinned by a mutation-proven spec, and the approval-gating assertions are double-covered across the 41-08 repoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker session → admin lens chunk | these two files are client code; if they enter the worker's chunk set the data-fetch stubs and the admin UI ship to every role |
| lens → `listGovernanceQueue` / `listAdminSopRows` | client components now call admin-only actions directly, with no page-level gate in front |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02 | Information Disclosure | `/sops/page` client chunks | mitigate | Both lenses live under `src/components/sop/lenses/` and are reached only through `dynamic({ ssr: false })` (added in 41-05); `Pick another scope on the left.`, `Pick a SOP to see its detail here.` and `Owner role gone` are forbidden markers on the `/sops/page` bundle gate; `tests/lint/no-static-admin-lens-import.spec.ts` blocks any other import site |
| T-41-01 | Elevation of Privilege | `listGovernanceQueue` called from a client lens | transfer | `listGovernanceQueue` self-guards via `requireAdmin()` → `requireAdminContext()` (verified at plan time, `src/actions/governance.ts:324`); `listAdminSopRows` self-guards per 41-02 |
| T-41-04 | Elevation of Privilege | `useIsAdmin()`-gated rendering mistaken for access control | mitigate | Neither lens performs its own role check and neither is trusted to; the gate is the action. Documented in each file's header comment so a later reader does not assume UI gating is the control |
| T-41-06 | Tampering (regression to a safety gate) | `GovernanceQueueRow` approval gating | mitigate | `GovernanceQueueRow.tsx` asserted byte-identical; the `approveStep` / `isCallerNextApprover` / `handleApprove` assertions are duplicated into `tests/phase41/status-attention-lenses.spec.ts` so the APR-03/04 contract is never unguarded while 41-08 repoints `tests/phase29/queue-approve-action.spec.ts` |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Created by this plan:
- `src/components/sop/lenses/AdminStatusLens.tsx` — props `{ status, ownerOnly, departments?, collection?, onResult?, onClearFilter }`
- `src/components/sop/lenses/AdminAttentionLens.tsx` — props `{ onBack }`
- `tests/phase41/status-attention-lenses.spec.ts`

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0; `/sops/page` bundle delta is 0 (lenses not yet referenced by the page)
- `npx playwright test --project=phase41 --project=phase15-stubs --project=phase28 --project=phase29 --project=phase30` exits 0
- `git diff --stat src/components/admin/` shows only `SopMillerBrowser.tsx` from 41-02's type re-export; `GovernanceQueueRow.tsx` and `wiring/` untouched
</verification>

<success_criteria>
- Both lenses render from one action call each, with loading / error / empty / success states
- No wrapped component (`SopMillerBrowser`, `GovernanceQueueRow`, `WiringPatchBayShell`) is modified by this plan
- No lens introduces a navigation for a scope, filter or exit — all are callbacks
- Exactly one direct list→builder link exists, in `SopMillerBrowser`
- `/sops/page` bundle delta is 0
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-04-SUMMARY.md` when done. Record both lens prop signatures — 41-05 wires them.
</output>
</content>
