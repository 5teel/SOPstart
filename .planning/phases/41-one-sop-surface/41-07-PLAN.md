---
phase: 41-one-sop-surface
plan: 07
type: execute
wave: 5
depends_on: ["41-06"]
files_modified:
  - src/actions/ai-fields.ts
  - src/components/admin/ParseJobStatus.tsx
  - src/components/admin/UploadDropzone.tsx
  - src/components/admin/wiring/WiringPatchBay.tsx
  - src/lib/journeys/roles.ts
  - src/lib/uat/tests.ts
  - tests/phase41/reference-sweep.spec.ts
autonomous: true
requirements: [SUR-01, SUR-03, SUR-04, SUR-06]

must_haves:
  truths:
    - "No code path inside src/ sends a user to /admin/sops any more — every internal link goes straight to /sops"
    - "The wiring patch bay's Open in library link lands on the merged surface, filtered"
    - "A finished upload's Go to drafts CTA lands on the merged surface's Drafts scope"
    - "The roles map and the UAT feedback hub name /sops, not a second admin library"
    - "The word Library never names a destination or a nav label"
  artifacts:
    - path: "tests/phase41/reference-sweep.spec.ts"
      provides: "Live guard that no src/ file outside the shim references the /admin/sops list route"
      min_lines: 50
  key_links:
    - from: "src/components/admin/wiring/WiringPatchBay.tsx"
      to: "/sops?departments= / /sops?collection="
      via: "focus-based Open in library deep link"
      pattern: "/sops\\?departments="
    - from: "src/components/admin/UploadDropzone.tsx"
      to: "/sops?status=draft"
      via: "post-upload CTA"
      pattern: "/sops\\?status=draft"
---

<objective>
Sweep every remaining internal reference to the `/admin/sops` list route onto `/sops`, so the shim exists only for external bookmarks and no in-app click pays a redirect hop. Update the roles/access map and the UAT feedback hub in the same pass, then flip the reference-sweep guard live.

Purpose: SUR-03/SUR-04 — a second door that nothing in the app links to is a shim; one that the app still routes through is a second destination wearing a shim's clothes. Also SUR-06: `roles.ts` and `uat/tests.ts` are where the word "Library" still names a destination.
Output: five repointed source files, the roles map, the UAT hub, and a live reference sweep.

This plan runs in parallel with 41-08 — different files, no shared assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-RESEARCH.md
@.planning/phases/41-one-sop-surface/41-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint every code reference to the /admin/sops list route</name>
  <files>src/actions/ai-fields.ts, src/components/admin/ParseJobStatus.tsx, src/components/admin/UploadDropzone.tsx, src/components/admin/wiring/WiringPatchBay.tsx</files>
  <read_first>
    - src/actions/ai-fields.ts (:200-260 — two `revalidatePath('/admin/sops')` calls alongside a `revalidatePath('/admin/sops/builder/${sopId}')`)
    - src/components/admin/ParseJobStatus.tsx (:310-320 — `router.push('/admin/sops')` after a parse job resolves)
    - src/components/admin/UploadDropzone.tsx (:670-690 — the post-upload `router.push('/admin/sops?status=draft')` CTA; also note :211, :677, :732 which target `/admin/sops/builder/...` sub-routes and must NOT be touched)
    - src/components/admin/wiring/WiringPatchBay.tsx (:625-635 — the focus-based `Open in library` hrefs `/admin/sops?departments=${focus}` and `/admin/sops?collection=${focus}`)
    - src/app/(protected)/admin/sops/page.tsx (the shim from 41-06 — the pass-through param names these links must match)
    - ./CLAUDE.md (2026-06-08 learning — internal hrefs are not type-checked, so the build stays green with dead or stale URLs; grep is the only net)
  </read_first>
  <action>
Repoint each site to `/sops`, preserving the query shape the merged surface resolves (per 41-06-SUMMARY's param→scope mapping):
- `src/actions/ai-fields.ts`: both `revalidatePath('/admin/sops')` → `revalidatePath('/sops')`. Leave the `revalidatePath('/admin/sops/builder/${sopId}')` call alone — it is a live sub-route.
- `src/components/admin/ParseJobStatus.tsx`: `router.push('/admin/sops')` → `router.push('/sops')`.
- `src/components/admin/UploadDropzone.tsx`: `router.push('/admin/sops?status=draft')` → `router.push('/sops?status=draft')`. Touch nothing else in this file — its other three `/admin/sops/builder/...` targets are correct.
- `src/components/admin/wiring/WiringPatchBay.tsx`: both `Open in library` hrefs → `/sops?departments=${focus}` and `/sops?collection=${focus}`. Also review the surrounding link COPY: "Open in library" still reads as a destination named Library. Change the visible label to `Open in the SOP list →` (D-05/SUR-06 — "Library" survives only as a filter/scope label, never a destination). If that copy lives in `SelectionStrip`, change it there and note the file in the summary.

After editing, run a full sweep to confirm nothing was missed: `grep -rn "/admin/sops" src/ --include=*.ts --include=*.tsx | grep -v "/admin/sops/"` must return only the shim file itself and `src/lib/journeys/journeys.ts`'s shim-documenting step. Anchor any regex you write so `/admin/sops/new`, `/admin/sops/builder/`, `/admin/sops/pipeline/` and `/admin/sops/[sopId]/...` are never matched — those sub-routes all survive this phase.
  </action>
  <acceptance_criteria>
    - `grep -rn "'/admin/sops'" src/ --include=*.ts --include=*.tsx` returns zero lines
    - `grep -rn "/admin/sops?" src/ --include=*.ts --include=*.tsx` returns zero lines
    - `src/components/admin/UploadDropzone.tsx` contains `/sops?status=draft` and still contains three `/admin/sops/builder/` references
    - `src/components/admin/wiring/WiringPatchBay.tsx` contains `/sops?departments=` and `/sops?collection=` and zero `/admin/sops`
    - `Open in library` no longer appears as user-visible link copy in `src/components/admin/wiring/`
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0
    - `npx playwright test --project=phase32 -g "Open in library"` — this assertion belongs to 41-08's repoint set; if it is red here, confirm it is on 41-06-SUMMARY's handover list and leave it
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>No in-app click routes through the shim; every internal link lands on `/sops` directly, with sub-routes untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update the roles/access map and the UAT feedback hub</name>
  <files>src/lib/journeys/roles.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/journeys/roles.ts (:92-110 the `admin` role's `landsOn: { label: 'SOP library (admin)', route: '/admin/sops' }` and its `can`/`cannot` lists; :172-186 the `ACCESS_MATRIX` rows — `My SOPs (read / walk)` → `/sops` and `Manage SOPs` → `/admin/sops` with per-role booleans)
    - src/lib/uat/tests.ts (the 18 entries whose `links` point at `/admin/sops` or `/admin/sops?view=access` — lines ~234, 259, 283, 307, 331, 354, 377, 426, 454, 480, 507, 534, 561, 613, 638, 664, 688, 854 — plus the surrounding entry shape so labels stay in the file's plain-language register)
    - ./CLAUDE.md (§ UAT feedback hub / § Pathways Map Maintenance — `uat/tests.ts` is updated when a change is worth team review; a reroute of the admin SOP surface is)
    - src/app/(protected)/sops/page.tsx (the admin scope labels, so UAT link labels match what Simon will actually see)
  </read_first>
  <action>
`src/lib/journeys/roles.ts`:
- `admin.landsOn` → `{ label: 'SOP list', route: '/sops' }`. Drop the parenthetical "(admin)" and the word "library" — one surface, one label (D-05/SUR-06).
- `ACCESS_MATRIX`: delete the `Manage SOPs` → `/admin/sops` row and REPLACE it with a row describing the lenses: `surface: 'SOP list — admin lenses'`, `route: '/sops?view=attention'`, `access: { worker: false, supervisor: false, safety_manager: true, admin: true }`. Keep the existing `My SOPs (read / walk)` → `/sops` row for all four roles: the same route now legitimately appears twice in this table with different access, which is exactly the point of the phase — annotate it with a short comment so a future reader does not "fix" the apparent duplicate.

`src/lib/uat/tests.ts`: repoint every `/admin/sops` href to `/sops`, and every `/admin/sops?view=access` to `/sops?view=access`. Update the labels so they name what Simon will see on the merged surface — replace `SOP management` / `SOP library` destination labels with `SOPs` or `SOPs — Drafts` / `SOPs — Needs attention` / `SOPs — Access` as appropriate to each entry. Keep every entry's plain-language click-path register (no jargon, no internal ids) and change no test's actual question.

Add one new UAT entry for this phase, written as a plain click-path plus a yes/no question, covering the merge itself: open SOPs as an admin, do you see the extra scopes (Drafts, Published, Needs attention, Access) in the left column; click Needs attention, does the queue appear; click back, are you where you started. Link it to `/sops`.
  </action>
  <acceptance_criteria>
    - `grep -c "/admin/sops" src/lib/journeys/roles.ts` is 0; `grep -c "/admin/sops" src/lib/uat/tests.ts` is 0
    - `src/lib/journeys/roles.ts` contains `'/sops?view=attention'` and the comment explaining the intentional duplicate route in `ACCESS_MATRIX`
    - `grep -c "SOP management" src/lib/uat/tests.ts` is 0
    - the new UAT entry exists, links to `/sops`, and contains no internal identifier (no `SUR-`, no `D-0`, no component name)
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The roles map and the UAT hub describe one SOP surface with permission-gated lenses, and the phase has a plain-language UAT entry Simon can run on sopstart.com.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the reference-sweep guard live</name>
  <files>tests/phase41/reference-sweep.spec.ts</files>
  <read_first>
    - tests/phase41/reference-sweep.spec.ts (the fixme stubs from 41-01 Task 2)
    - tests/phase30/dead-weight.spec.ts (:1-60 — the reference-sweep idiom; and the CLAUDE.md 2026-08-04 lesson it encodes: a deletion/reroute guard must assert the absence of REFERENCES, not just the absence of a file)
  </read_first>
  <action>
Remove every `test.fixme` from `tests/phase41/reference-sweep.spec.ts` and make it real:

1. Walk `src/` for `.ts`/`.tsx` files and collect every line matching an EXACT `/admin/sops` list-route reference — the path followed by a quote, a backtick, a `?`, or end-of-string — with `/admin/sops/` (any sub-route) explicitly excluded. Assert the only permitted files are `src/app/(protected)/admin/sops/page.tsx` (the shim) and `src/lib/journeys/journeys.ts` (which documents the shim). Print `file:line  text` for every violation.

2. Assert a fixed EXPECTED COUNT of permitted references (the shim's own and the journeys step's) so the sweep fails when the number grows silently — a new `/admin/sops` link added by a later phase must be a deliberate decision, not a drift.

3. Assert positively that the sub-routes survive: `src/components/admin/UploadDropzone.tsx`, `src/app/(protected)/sops/[sopId]/page.tsx` and `src/components/admin/SopMillerBrowser.tsx` all still contain `/admin/sops/builder/`, and `src/components/layout/TopHeader.tsx` still contains `/admin/sops/new`. A sweep that over-matches and strips a live sub-route is the failure mode this assertion catches.

4. SUR-06: assert the word `Library` appears as a nav label or route label in none of `src/components/layout/TopHeader.tsx`, `src/lib/journeys/roles.ts`, `src/lib/uat/tests.ts` — while allowing it in `src/app/(protected)/sops/page.tsx` where it is the existing worker scope-GROUP header (a filter label, which D-05 permits). Scope the assertion to label/href positions, not free prose in comments, and strip comment lines before matching.

Strip comment lines before every count-based assertion so a file's own explanatory prose cannot satisfy or break it (`grep -v '^\s*[*/]'` equivalent — the CLAUDE.md self-invalidating-header class).
  </action>
  <acceptance_criteria>
    - `npx playwright test --project=phase41 -g "reference sweep"` passes with no fixme remaining in the file
    - Mutation proof (run, then revert): add `router.push('/admin/sops')` to `src/components/admin/ParseJobStatus.tsx` → the sweep fails naming that file:line. Revert.
    - Mutation proof (run, then revert): delete `/admin/sops/builder/` from `src/components/admin/SopMillerBrowser.tsx` → the sub-route-survival assertion fails. Revert.
    - Mutation proof (run, then revert): add a `Library` nav label to `TopHeader.tsx` → the SUR-06 assertion fails. Revert.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase41</automated>
  </verify>
  <done>A mutation-proven sweep pins the exact set of permitted `/admin/sops` references, proves the sub-routes survived, and enforces that "Library" names no destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal link graph → route reality | internal hrefs are not type-checked; a stale link compiles, builds and ships |
| `revalidatePath` targets → cache correctness | revalidating a path nothing renders any more leaves the real surface stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | Repudiation (a dead/stale link presenting as a crash) | every internal `/admin/sops` reference | mitigate | Sweep every reference and pin the permitted set by exact count in `tests/phase41/reference-sweep.spec.ts`; assert positively that the four live sub-routes survived, so an over-broad sweep is caught (CLAUDE.md 2026-08-04, 2026-06-08) |
| T-41-11 | Repudiation (a stale cache) | `revalidatePath` in `src/actions/ai-fields.ts` | mitigate | Both list-route revalidations repointed to `/sops`; the builder sub-route revalidation left intact |
| T-41-03 | Information Disclosure | `/admin/sops` shim | mitigate | Unchanged from 41-06; this plan only removes in-app traffic through it, so the shim's remaining callers are external bookmarks only |
| T-41-12 | Information Disclosure (doc drift misleading a reviewer) | `src/lib/journeys/roles.ts` `ACCESS_MATRIX` | mitigate | The `Manage SOPs` row is replaced rather than deleted, so the access map still states that the admin lenses are admin/safety_manager-only; the deliberate duplicate `/sops` route entry is annotated |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Changed by this plan:
- `src/actions/ai-fields.ts`, `src/components/admin/ParseJobStatus.tsx`, `src/components/admin/UploadDropzone.tsx`, `src/components/admin/wiring/WiringPatchBay.tsx` — repointed to `/sops`
- `src/lib/journeys/roles.ts` — `admin.landsOn` → `/sops`; `ACCESS_MATRIX` `SOP list — admin lenses` row
- `src/lib/uat/tests.ts` — 18 links repointed + one new phase-41 UAT entry
- `tests/phase41/reference-sweep.spec.ts` live

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0; both gated routes green
- `npx playwright test --project=phase41 --project=phase30 --project=phase15-stubs` exits 0
- `grep -rn "/admin/sops" src/ --include=*.ts --include=*.tsx | grep -v "/admin/sops/"` returns only the shim and the journeys shim step
</verification>

<success_criteria>
- Zero in-app links to the `/admin/sops` list route; all four live sub-routes intact
- Roles map and UAT hub name one SOP surface; a new plain-language UAT entry exists for the merge
- The reference sweep is live, count-pinned and mutation-proven in three directions
- "Library" names no destination or nav label anywhere
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-07-SUMMARY.md` when done. Record the pinned permitted-reference count and the file that owns the "Open in library" copy change.
</output>
</content>
