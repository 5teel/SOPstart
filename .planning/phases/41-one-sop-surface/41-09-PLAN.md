---
phase: 41-one-sop-surface
plan: 09
type: execute
wave: 6
depends_on: ["41-07", "41-08"]
files_modified:
  - .planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md
autonomous: false
requirements: [SUR-01, SUR-02, SUR-03, SUR-04, SUR-05, SUR-06]

must_haves:
  truths:
    - "`npm run build` is green with /sops/page within 2 KB of its Wave-0 baseline and no admin-lens marker in the worker chunks"
    - "The full Playwright suite is green"
    - "Simon has confirmed on sopstart.com that an admin sees the extra scopes and a worker does not"
    - "Simon has confirmed the legacy bookmarks still land where they used to"
    - "/pathways reports 0 not-mapped screens"
  artifacts:
    - path: ".planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md"
      provides: "Recorded SUR-05 evidence — measured KB, delta, marker results for both gated routes"
  key_links:
    - from: "npm run build"
      to: ".bundle-baseline.json"
      via: "postbuild check-bundle-size"
      pattern: "check-bundle-size"
---

<objective>
Prove SUR-05 with recorded numbers rather than a passing exit code, run the full suite, deploy, and put the merged surface in front of Simon as plain click-paths with yes/no answers.

Purpose: SUR-05 is a phase-failing condition and the ROADMAP requires the bundle result to be a measured fact, not an assumption. SUR-01/02/03/06 are visual and role-dependent — CSS-token and sizing bugs are invisible to every automated gate in this repo (CLAUDE.md 2026-07-14: an undefined custom property renders the element wrong and fails nothing), so a human must look at the deployed page.
Output: `41-BUNDLE-PROOF.md` and a human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-VALIDATION.md
@.planning/phases/41-one-sop-surface/41-01-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-05-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the SUR-05 bundle proof and run the full gate</name>
  <files>.planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md</files>
  <read_first>
    - .planning/phases/41-one-sop-surface/41-01-SUMMARY.md (the Wave-0 `/sops/page` baseline KB and the exact forbidden-marker literals chosen)
    - .planning/phases/41-one-sop-surface/41-05-SUMMARY.md (the measured KB and delta at the point the surface merged)
    - .bundle-baseline.json (the committed floor — this plan must NOT re-capture it; a re-capture would absorb a regression and silently move the floor)
    - scripts/check-bundle-size.ts (the summary lines whose output is the evidence)
  </read_first>
  <action>
Run `npm run build` on the final tree and `npx playwright test` (full suite, every project). Capture the real console output.

Write `.planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md` containing:
- For BOTH gated routes: the baseline KB, the measured KB, the delta, and the tolerance — copied from the actual `check-bundle-size:` output lines, not restated from memory
- The result of each forbidden-marker assertion for `/sops/page`, listing each marker literal and stating that it was found somewhere in the build (the self-validation, proving the assertion is not vacuous) and absent from the route's chunk set
- The `DesktopWalkthrough` / `WalkthroughVoiceModal` chunk-existence results for `/sops/[sopId]/page`, confirming the pre-existing gate is untouched
- The full-suite result: total tests, passed, failed, skipped, and the per-project list
- A one-line verdict per success criterion SC-1..SC-5 from the ROADMAP with the evidence for each

Do NOT re-run `scripts/capture-bundle-baseline.ts`. The floor is the pre-merge number and must stay that way for the next phase to inherit; re-capturing is how a regression becomes the new normal.

Then commit and `git push origin master` so Railway deploys — the human verification below happens on sopstart.com, never locally (D-10). Wait for the deploy to finish before the checkpoint.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0; its output contains a `/sops/page` line whose delta is ≤ +2 KB against the Wave-0 baseline
    - `npx playwright test` exits 0 with zero failures across every project
    - `.bundle-baseline.json` is unchanged by this plan (`git diff` empty for that file)
    - `41-BUNDLE-PROOF.md` quotes the real measured numbers for both routes and names every forbidden marker with its found-somewhere / absent-here result
    - `npx tsc --noEmit` exits 0
    - the commit is pushed to `master` and the Railway deploy has completed
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test && npx tsc --noEmit</automated>
  </verify>
  <done>SUR-05 is proven with recorded numbers for both routes, the full suite is green, and the change is live on sopstart.com.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the merged surface on sopstart.com</name>
  <files>none — verification only, no file is modified by this task</files>
  <read_first>
    - .planning/phases/41-one-sop-surface/41-05-SUMMARY.md (the admin scope labels and the URL each scope writes — the checkpoint questions must name what Simon will actually see)
    - .planning/phases/41-one-sop-surface/41-06-SUMMARY.md (the shim's param mapping — questions D15/D16 exercise it)
    - .planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md (Task 1's recorded evidence — do not open the checkpoint until it exists and the deploy is live)
  </read_first>
  <action>
Confirm the Railway deploy from Task 1 has completed, then present the click-path script below to Simon verbatim and wait. Do not start work on any reported failure inside this plan: record each failing question number and what he saw in the summary, and hand it to `/gsd-plan-phase 41 --gaps`. Never give local dev, localhost or Playwright instructions here (D-10).
  </action>
  <acceptance_criteria>
    - The Railway deploy for the Task 1 commit shows as complete before the script is presented
    - Every question A2-A4, B5-B10, C11-C13, D14-D17, E18 is answered; F19 is answered or explicitly "skipped"
    - Simon's response is recorded verbatim in `41-09-SUMMARY.md`
    - Any "no" answer is recorded as a numbered gap, not patched inside this plan
  </acceptance_criteria>
  <what-built>
    One SOP list for everyone at sopstart.com/sops. An admin now sees extra rows in the left-hand column — Drafts, Published, Needs attention, Access, Owned by me — on the same page where they see their own SOPs. A worker sees only their own. The old second "Manage SOPs" menu item is gone, and old bookmarks to it now forward to the new page.
  </what-built>
  <how-to-verify>
Do these in order on sopstart.com (signed in as yourself, an admin). Answer yes or no to each.

**A — the admin view**
1. Open the menu and click **SOPs**.
2. In the left-hand column, do you see a group headed **Admin** with rows for **All SOPs**, **Drafts**, **Published**, **Needs attention**, **Access** and **Owned by me**? (yes / no)
3. Do those rows show numbers next to them? (yes / no)
4. Are all three columns the same height, with no gap, no stray border and no invisible button? (yes / no — if no, describe what looks wrong)

**B — the lenses**
5. Click **Drafts**. Does a list of draft SOPs appear, with a detail panel on the right when you click a row? (yes / no)
6. Click a row, then click **Open**. Does the builder open for that SOP? (yes / no)
7. Go back to SOPs and click **Needs attention**. Does the grouped list of SOPs needing attention appear? (yes / no)
8. Click **← Back to your SOPs**. Are you back on the list you started on, without the page reloading? (yes / no)
9. Click **Access**. Does the access/wiring map appear, full width? (yes / no)
10. Click **← Back to your SOPs** again. Back where you started? (yes / no)

**C — your own SOPs still work**
11. Click **All yours**. Is your own SOP list exactly as it was before — same SOPs, same refresher dates, same badges? (yes / no — if no, say what changed)
12. Use the search icon at the top. Does search still work? (yes / no)
13. Pick a department in the left column. Does the list filter? (yes / no)

**D — the old doors**
14. Is there now only ONE "SOPs" item in the menu? (yes / no)
15. Paste `sopstart.com/admin/sops?view=access` into the address bar. Do you land on the access map on the new page? (yes / no)
16. Paste `sopstart.com/admin/sops?status=draft`. Do you land on Drafts? (yes / no)
17. Click **Governance** in the menu. Does it open Needs attention? (yes / no)

**E — the map**
18. Open **Pathways** from the account menu, then the **All screens** panel. Is the not-mapped count 0? (yes / no)

**F — the worker view** (only if you have a worker login handy; skip and say "skipped" if not)
19. Sign in as a worker and open SOPs. Is the **Admin** group absent from the left column? (yes / no)
  </how-to-verify>
  <resume-signal>Type "approved" if every answer was yes (or skipped), otherwise list the question numbers that failed and what you saw.</resume-signal>
  <verify>
    <human-check>Simon answers every question; "approved" means the merged surface is confirmed live on sopstart.com for both an admin and (if available) a worker session.</human-check>
    <automated>npm run build && npx playwright test</automated>
  </verify>
  <done>Simon has answered every question and his response is recorded verbatim; any "no" is filed as a numbered gap for `/gsd-plan-phase 41 --gaps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated gates → visual reality | no gate in this repo renders a page; a CSS-token or sizing bug ships green |
| baseline file → regression detection | re-capturing the baseline converts a regression into the new floor |
| worker session vs admin session | the only way to confirm the role gate visually is to look with both |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-15 | Repudiation (a green gate that proves nothing) | `.bundle-baseline.json` | mitigate | Explicitly forbidden from re-capturing; `git diff` on that file asserted empty; the measured numbers for both routes are transcribed into `41-BUNDLE-PROOF.md` from real output |
| T-41-16 | Repudiation (an invisible UI bug) | the merged Miller frame | mitigate | Blocking human checkpoint with explicit look-at-it questions (column heights, stray borders, invisible controls) — the class CLAUDE.md 2026-07-14 proved no automated gate in this repo catches |
| T-41-04 | Elevation of Privilege | `useIsAdmin()` lens gating | mitigate | Checkpoint question 19 confirms a real worker session sees no admin group; the server-side gate is already asserted by the action specs, so this question checks the UX layer only |
| T-41-03 | Information Disclosure | `/admin/sops` shim | mitigate | Checkpoint questions 15-17 confirm the legacy deep links resolve to the right lenses in one hop |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Created by this plan:
- `.planning/phases/41-one-sop-surface/41-BUNDLE-PROOF.md` — recorded SUR-05 evidence for both gated routes plus the SC-1..SC-5 verdict table

<verification>
- `npm run build` exits 0 with both route lines inside tolerance
- `npx playwright test` exits 0, zero failures, zero unexpected skips
- `npx tsc --noEmit` exits 0
- `.bundle-baseline.json` unchanged
- Human sign-off recorded for every checkpoint question
</verification>

<success_criteria>
- SC-1: a worker, a supervisor and an admin all reach the list at `/sops`, each seeing only what their permissions allow, with every worker behaviour intact (checkpoint A, C, F + the phase41 preservation specs)
- SC-2: draft/published status, the governance queue and the access map render as lenses, and `?departments=` / `?collection=` / `?sop=` / `?view=attention` / `?view=access` all resolve as before (checkpoint B, D + the deep-link specs)
- SC-3: exactly one top-level SOPs entry; "Library" names no destination (checkpoint D14 + the SUR-03/SUR-06 specs)
- SC-4: one route chain from the list to the builder (checkpoint B6 + the SUR-04 sweep)
- SC-5: `SB-LINE-06` green for both routes, no governance-queue / org-tree / wiring-patch-bay code in the worker chunks (`41-BUNDLE-PROOF.md`)
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-09-SUMMARY.md` when done, including the human sign-off verbatim and any question that failed. If a checkpoint question fails, record it as a gap for `/gsd-plan-phase 41 --gaps` rather than patching ad hoc.
</output>
</content>
