---
phase: 41-one-sop-surface
plan: 05
type: execute
wave: 3
depends_on: ["41-03", "41-04"]
files_modified:
  - src/app/(protected)/sops/page.tsx
  - .planning/codebase/CAPABILITY-MATRIX.md
  - tests/phase41/merged-surface.spec.ts
autonomous: true
requirements: [SUR-01, SUR-02, SUR-05, SUR-06]

must_haves:
  truths:
    - "A worker on /sops sees exactly the six worker scopes and no admin scopes"
    - "An admin on /sops sees an Admin scope group with All SOPs, Drafts, Published, Needs attention and Access"
    - "Every worker behaviour is unchanged: assigned-first ordering, offline Dexie read, self-add/remove, department filter, search, refresher due dates, Updated badge"
    - "?view=attention, ?view=access, ?status=draft, ?owner=me, ?departments=, ?collection= and ?sop= all resolve on /sops to the same views they resolve to today"
    - "?departments= and ?collection= are inert when the active scope is the access lens"
    - "Switching scope never issues a navigation — the URL is rewritten with history.replaceState"
    - "The /sops/page bundle stays within 2 KB of its Wave-0 baseline and contains no admin-lens marker"
  artifacts:
    - path: "src/app/(protected)/sops/page.tsx"
      provides: "The one SOP list surface for every role"
      contains: "useIsAdmin"
    - path: ".planning/codebase/CAPABILITY-MATRIX.md"
      provides: "Access-channel record for the merged surface's admin lenses"
      contains: "listAdminSopRows"
  key_links:
    - from: "src/app/(protected)/sops/page.tsx"
      to: "src/components/sop/lenses/AdminStatusLens.tsx"
      via: "next/dynamic ssr:false"
      pattern: "dynamic\\([^)]*AdminStatusLens"
    - from: "src/app/(protected)/sops/page.tsx"
      to: "src/components/providers/RoleProvider.tsx"
      via: "useIsAdmin()"
      pattern: "useIsAdmin\\(\\)"
    - from: "src/app/(protected)/sops/page.tsx"
      to: "browser history"
      via: "history.replaceState on scope change"
      pattern: "history\\.replaceState"
---

<objective>
Turn `/sops` into the one SOP list surface: add an admin scope group to the existing sketch-005-variant-C Miller column, resolve the legacy admin deep links client-side, and render the three lenses through `next/dynamic({ ssr: false })` gated on `useIsAdmin()`. The worker code path is extended, never rewritten.

Rendering model (D-03, decided in 41-02 and implemented here): client shell, code-split admin lenses. `/sops` keeps `'use client'`, `useAssignedSops`, `useSopSync`, Dexie, self-add/remove, search and the department filter exactly as they are; the admin views arrive as additional scopes whose components live in separate chunks.

Purpose: SUR-01 and SUR-02. This is the plan where the second SOP door stops being needed; 41-06 then closes it.
Output: the merged surface, plus the capability-matrix record of who may see each lens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/41-one-sop-surface/41-CONTEXT.md
@.planning/phases/41-one-sop-surface/41-RESEARCH.md
@.planning/phases/41-one-sop-surface/41-PATTERNS.md
@.planning/phases/41-one-sop-surface/41-03-SUMMARY.md
@.planning/phases/41-one-sop-surface/41-04-SUMMARY.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope model, deep-link resolution and replaceState URL plumbing in SopsPage</name>
  <files>src/app/(protected)/sops/page.tsx</files>
  <read_first>
    - src/app/(protected)/sops/page.tsx (FULL file, 645 lines — every line is either preserved verbatim or is one of the three edit sites; in particular :34-37 the existing `dynamic()` idiom and its SB-LINE-06 comment, :62-80 `WorkerScope` / `WORKER_SCOPES` / `SCOPE_LABEL`, :82-218 `SopsPage`, :466-473 the summary line, :507-585 the Miller grid and its loading/empty/browser branches, :592-644 `MillerColumnHeader` / `MillerItem`)
    - src/app/(protected)/admin/sops/page.tsx (:117-155 the param reads and the `!isAccessView` filter precedence; :31-36 `STATUS_TABS` and the "counts must add up to All" comment; :449-458 `scopeItems` incl. the `failed`-only-when-non-zero filter)
    - src/components/providers/RoleProvider.tsx (full file — `useIsAdmin()`)
    - src/app/(protected)/layout.tsx (:13-35 — role is resolved server-side once and handed to `RoleProvider`, so `useIsAdmin()` costs no fetch and returns the same value during SSR and hydration)
    - src/app/(protected)/admin/sops/[sopId]/versions/diff/page.tsx (the repo's established bare-`useSearchParams()`-in-a-client-page pattern — no Suspense wrapper anywhere in this codebase; follow it and let `npm run build` be the gate)
    - 41-03-SUMMARY.md and 41-04-SUMMARY.md (the three lens prop signatures)
  </read_first>
  <action>
Edit `src/app/(protected)/sops/page.tsx`. Add, above `SopsPage`:

`type AdminScope = 'admin-all' | 'admin-draft' | 'admin-published' | 'admin-failed' | 'admin-attention' | 'admin-access'`, `export type SopScope = WorkerScope | AdminScope`, and `const ADMIN_SCOPES: { key: AdminScope; label: string }[]` with labels `All SOPs`, `Drafts`, `Published`, `Still working`, `Needs attention`, `Access`. Keep `WorkerScope`, `WORKER_SCOPES` and `SCOPE_LABEL` exactly as they are (`SCOPE_LABEL` stays keyed on `WorkerScope` and is indexed only on the worker branch).

Add three sibling `dynamic()` declarations next to the existing `SopWorkerBrowser` one, same `{ ssr: false }` shape, using the named-export form `.then((m) => ({ default: m.X }))` for `AdminStatusLens`, `AdminAttentionLens`, `AdminAccessLens` from `@/components/sop/lenses/…`. Extend the existing SB-LINE-06 comment block to say these three are the reason the gate now covers `/sops/page`, and that any static import of them (or of `SopMillerBrowser` / `GovernanceQueueRow` / `WiringPatchBayShell` / `DepartmentPicker` / `setSopCategory` / `@/actions/governance` / `@/actions/org-model` / `@/actions/grants`) in this file is a bundle regression blocked by `tests/lint/no-static-admin-lens-import.spec.ts`.

In `SopsPage`:
- `const isAdmin = useIsAdmin()` and `const searchParams = useSearchParams()`.
- Write a module-scope pure function `resolveInitialScope(params: URLSearchParams, isAdmin: boolean): { scope: SopScope; ownerOnly: boolean; departments?: string; collection?: string; sop?: string }`. Precedence, mirroring `admin/sops/page.tsx:137-154`: non-admin → always `{ scope: 'all', ownerOnly: false }` and every admin param dropped. Admin → `view=attention` → `admin-attention`; `view=access` → `admin-access` and `departments`/`collection` DROPPED (they are deliberately inert under the access view — RESEARCH Pitfall 5 / `page.tsx:153-154`) while `sop` is kept; `status` in `draft|published|failed` → `admin-{status}`; `owner=me` → `admin-all` + `ownerOnly`; `departments` or `collection` present → `admin-all` + that filter; nothing → `admin-all`.
- Seed state from it once: `const [nav, setNav] = useState(() => resolveInitialScope(new URLSearchParams(searchParams.toString()), isAdmin))`. This is hydration-safe because the client component's SSR pass sees the same `searchParams` and the same server-supplied `RoleProvider` role, so the first client render computes an identical value. Do NOT read `window.location` — that WOULD diverge (CLAUDE.md 2026-06-08 hydration class).
- An admin's default scope is `admin-all`, preserving today's admin landing experience (they land on the library, not on their own assigned list); a non-admin's default stays `all`.
- `function applyScope(next: SopScope, patch?: { ownerOnly?: boolean; departments?: string; collection?: string })`: update state, then rewrite the URL with `history.replaceState(null, '', url)` where `url` is built from `URLSearchParams` over a fixed `/sops` path — `/sops` bare for worker scopes, `?view=attention` / `?view=access` / `?status=draft|published|failed` / `?owner=me` / `?departments=` / `?collection=` / `?sop=` for admin scopes. Never `router.push`, never `<Link>`, for a scope change: the admin page's `<Link>`-per-scope pattern is correct only for a server component and on this client page would fire an RSC request through the service worker on every click (RESEARCH Pitfall 3 / CLAUDE.md 2026-05-13). When leaving the access scope, drop `sop` from the URL.
- Add a `useEffect` on `searchParams.toString()` that re-resolves `nav` when the query string changes from OUTSIDE the page (back/forward, a deep link arriving while mounted), mirroring the pattern the 2026-05-13 learning prescribes. Guard it so the `replaceState` writes above do not re-trigger it into a loop (compare the resolved value to current state before setting).
- Keep an `adminCounts` state slot (`AdminSopListResult | null`) fed by `AdminStatusLens`'s `onResult`, so the admin scope column keeps showing counts after the user switches to a worker scope and the lens unmounts.

Render branch in `SopsPage`, replacing the single `<SopsSection … />` call:
- `nav.scope === 'admin-attention'` → `<AdminAttentionLens onBack={() => applyScope('all')} />`
- `nav.scope === 'admin-access'` → `<AdminAccessLens pinnedSopId={nav.sop} onBack={() => applyScope('all')} />`
- otherwise → `<SopsSection … />` with the new props from Task 2
These two lenses replace the whole Miller frame rather than sitting inside it — exactly what `/admin/sops` does today for those two views (`page.tsx:464-524`), which is why each lens owns a back affordance. Keep the sticky `SOPs` header, the search overlay and the department bottom sheet mounted in all branches.

Change nothing about `useSsopSync`/`useAssignedSops`/`db.syncMeta`/`departments`/`sopDeptMap`/`handleDeptSelect`/`deptMatches` or the search overlay. Add no new CSS custom property; reuse the existing class strings.
  </action>
  <acceptance_criteria>
    - the file contains `useIsAdmin()`, `useSearchParams`, `resolveInitialScope`, `history.replaceState`, `ADMIN_SCOPES`
    - the file contains `router.push` zero times and `next/link` zero times
    - `grep -c "dynamic(" src/app/(protected)/sops/page.tsx` is 4 (the worker browser plus three lenses), each with `ssr: false`
    - the file contains none of: `from '@/components/admin/SopMillerBrowser'`, `WiringPatchBayShell`, `GovernanceQueueRow`, `DepartmentPicker`, `setSopCategory`, `@/actions/governance`, `@/actions/org-model`, `@/actions/grants`
    - the file still contains all of `useAssignedSops`, `useSopSync`, `db.syncMeta`, `selfAddSop`, `selfRemoveSop`, `requestRemoveAssignment`, `getUserSopAssignments`, `refresherDueDate`, `SopSearchInput`, `DepartmentBottomSheet`, `categoryLabel`
    - `resolveInitialScope` returns `scope: 'all'` and drops every admin param when `isAdmin` is false (assert both the non-admin early return and that `departments`/`collection` are dropped when the resolved scope is `admin-access`)
    - the file contains no `window.location` read
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0 with the `/sops/page` line inside ±2 KB
    - `npx playwright test --project=phase15-stubs` exits 0 (admin-lens import guard + undefined-CSS-token guard)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx playwright test --project=phase15-stubs</automated>
  </verify>
  <done>Every legacy admin deep link resolves to a scope on `/sops`, scope changes rewrite the URL without a navigation, admin params are inert for non-admins and under the access lens, and the three lenses are reachable only through `dynamic({ ssr: false })`.</done>
</task>

<task type="auto">
  <name>Task 2: Admin scope group in the Miller column and the status-lens render region</name>
  <files>src/app/(protected)/sops/page.tsx</files>
  <read_first>
    - src/app/(protected)/sops/page.tsx (:222-235 `SopsSectionProps`; :475-505 the summary line and the `lg:hidden` mobile scope strip; :507-585 the Miller grid — the scope `<nav>`'s two `MillerColumnHeader` groups, the `By department` group, and the `loading ? … : workerSops.length === 0 ? … : <SopWorkerBrowser>` chain; :592-644 the two Miller primitives)
    - src/app/(protected)/admin/sops/page.tsx (:449-458 — `scopeItems` and its `failed`-only-when-non-zero filter; :663-690 the current `Needs attention` / `Access map` / `Owned by me` rows at the bottom of the admin scope column)
    - .claude/skills/sketch-findings-SOPstart/references/layout-primitives.md (the Miller frame rules — the frame is one bordered surface with hairline-separated flush rows and ink-fill selection; a new group is a header plus rows, never a nested card or a second frame)
    - 41-04-SUMMARY.md (`AdminStatusLens` prop signature)
  </read_first>
  <action>
Extend `SopsSectionProps` with `isAdmin: boolean`, `adminCounts: AdminSopListResult | null`, `onAdminResult: (r: AdminSopListResult) => void`, `nav` (the resolved nav object) and `onApplyScope: (next: SopScope, patch?) => void`; widen the existing `scope`/`onScopeChange` props to `SopScope`.

Three edits inside `SopsSection`, nothing else:

1. **Scope column (desktop).** After the existing `By department` group, render — only when `isAdmin` — a `<MillerColumnHeader>Admin</MillerColumnHeader>` followed by one `<MillerItem>` per `ADMIN_SCOPES` entry, using the SAME primitives (do not build a second styled list). Counts come from `adminCounts`: `admin-all` → `railCounts.all`, `admin-draft` → `railCounts.draft`, `admin-published` → `railCounts.published`, `admin-failed` → `railCounts.failed`, `admin-attention` → `flaggedCount`, `admin-access` → no count. Pass `count={undefined}` when `adminCounts` is null — `MillerItem` already renders no number for undefined. Filter out `admin-failed` when `adminCounts?.railCounts.failed === 0` (the "Still working" tab is rendered only when non-zero, `admin/sops/page.tsx:450`); render it when counts are unknown. `selected` compares against `nav.scope`; `onClick` calls `onApplyScope(sc.key)`. The header word is `Admin` — not `Library`, which survives only as the existing worker filter-group label (D-05 / SUR-06).
   Also add a `Owned by me` toggle row under the admin group that calls `onApplyScope('admin-all', { ownerOnly: !nav.ownerOnly })` and shows `selected={nav.ownerOnly}`, preserving `admin/sops/page.tsx:679-689`.

2. **Mobile scope strip.** In the existing `lg:hidden` horizontal strip, append the same admin scopes (same filter rule) when `isAdmin`, reusing the existing button classes. Three columns do not fit on a phone and a hidden scope is worse than a scrolling one — that is why this strip exists.

3. **Render region.** Restructure the branch chain so the admin status scopes are checked BEFORE the worker loading/empty branches:
   `isAdminStatusScope(nav.scope) ? <AdminStatusLens status={…} ownerOnly={nav.ownerOnly} departments={nav.departments} collection={nav.collection} onResult={onAdminResult} onClearFilter={() => onApplyScope('admin-all')} /> : loading ? <skeleton> : workerSops.length === 0 ? <No SOPs yet> : <SopWorkerBrowser … />`.
   This ordering is load-bearing: an admin whose own assigned list is empty must still see Drafts. With the current ordering the `workerSops.length === 0` branch would render "No SOPs yet" over a full library.
   Map the scope to the lens's `status` prop: `admin-all`→`all`, `admin-draft`→`draft`, `admin-published`→`published`, `admin-failed`→`failed`.
   Give the lens wrapper the same `lg:col-span-2` placement the worker branches use so the frame's geometry is unchanged.

4. **Summary line.** Render the existing worker summary paragraph only for worker scopes. For admin scopes render nothing there — the worker counts and the Dexie sync label describe the wrong list, and the admin counts already live in the scope column.

Leave `workerSops`, `inScope`, `counts`, `refresherState`, `hasNewerVersion`, `handleAdd`, `handleRemove`, `getAssignmentInfo`, the four `useQuery` blocks and both Miller primitives byte-identical.
  </action>
  <acceptance_criteria>
    - the file contains `MillerColumnHeader>Admin<` (the admin group uses the existing primitive) and `ADMIN_SCOPES.` is referenced in both the desktop column and the `lg:hidden` strip
    - the admin status branch is positionally BEFORE the `workerSops.length === 0` branch (assert the index of the `AdminStatusLens` reference is lower than the index of `No SOPs yet`)
    - `Owned by me` appears in the file
    - `grep -c "SopWorkerBrowser" src/app/(protected)/sops/page.tsx` is unchanged from pre-plan (the dynamic decl + one render site)
    - `git diff src/app/(protected)/sops/page.tsx` shows zero changed lines inside `workerSops`, `inScope`, `refresherState`, `hasNewerVersion`, `handleAdd`, `handleRemove`, `MillerColumnHeader`, `MillerItem`
    - `npx tsc --noEmit` exits 0; `npm run build` exits 0 with `/sops/page` within ±2 KB and no admin-lens marker reported
    - `npx playwright test --project=phase15-stubs --project=phase30` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx playwright test --project=phase15-stubs --project=phase30</automated>
  </verify>
  <done>The admin scopes are rows in the existing first Miller column for entitled roles only, the status lens renders inside the frame ahead of the worker empty-state branch, and no worker-side logic changed.</done>
</task>

<task type="auto">
  <name>Task 3: Update the capability matrix and flip the merged-surface spec live</name>
  <files>.planning/codebase/CAPABILITY-MATRIX.md, tests/phase41/merged-surface.spec.ts</files>
  <read_first>
    - .planning/codebase/CAPABILITY-MATRIX.md (the `## Matrix (access channel)` table — especially the `Governance queue` and `Manage departments` rows and the `Enforced at` column convention; the two-channels preamble; the `Last updated:` line)
    - ./CLAUDE.md (§ Capability Matrix — the matrix changes in the SAME commit as any change to a role gate; this is that commit)
    - tests/phase41/merged-surface.spec.ts (the fixme stubs written in 41-01 Task 2)
  </read_first>
  <action>
Edit `.planning/codebase/CAPABILITY-MATRIX.md`:
- Add one row: `SOP list — admin lenses (drafts / published / needs attention / access)` with `—` for worker and supervisor, ✅ for admin and safety_manager, `—` for chain approver, and an `Enforced at` naming `listAdminSopRows` (`src/actions/admin-sop-list.ts`) and `listAdminAccessData` (`src/actions/admin-access-view.ts`), both `requireAdminContext()`, plus an explicit note that `useIsAdmin()` lens hiding on `/sops` is a UX convenience and NOT the gate.
- Amend the `Governance queue` row's `Enforced at`: the queue is now a code-split lens on the shared `/sops` route, so the former `/admin/sops` page-level `redirect('/dashboard')` is gone and `listGovernanceQueue`'s own `requireAdminContext()` is the sole access gate.
- Amend the `Manage departments` row's `Enforced at` the same way for `listOrgTree` / `listGrants` / `ensureSopCollections` reached through the access lens.
- Bump `Last updated:` to the execution date.
- Add a one-paragraph note under the matrix recording that as of this phase the ROUTE is no longer an access boundary for SOP-list surfaces — every admin capability on `/sops` is gated at the server action, and a reviewer must not read "the route is reachable" as "the capability is reachable".

Then flip `tests/phase41/merged-surface.spec.ts` live: remove every `test.fixme` in that file and make the assertions real against the edited page. Assert at minimum: the `useIsAdmin()` gate; four `dynamic(` calls all with `ssr: false`; each lens referenced ONLY inside a `dynamic(` argument; the six worker imports still present (worker-behaviour preservation); `useSearchParams` + all seven params resolved; `departments`/`collection` dropped under `admin-access`; non-admin resolution drops every admin param; `history.replaceState` present and `router.push` absent; the admin-status branch positioned before the `No SOPs yet` branch; no `window.location` read; SUR-06 — the string `Library` appears in the file only as the existing worker scope-group header.
  </action>
  <acceptance_criteria>
    - `.planning/codebase/CAPABILITY-MATRIX.md` contains `listAdminSopRows`, `listAdminAccessData`, and the phrase noting route-is-not-a-boundary
    - the `Governance queue` and `Manage departments` rows no longer imply a `/admin/sops` page-level gate
    - `tests/phase41/merged-surface.spec.ts` contains zero `test.fixme`
    - `npx playwright test --project=phase41 -g "SUR-01"` and `-g "SUR-02"` and `-g "SUR-06"` and `-g "deep-link"` all pass
    - Mutation proof (run, then revert): change one lens's `dynamic(` to a static import → the merged-surface spec fails AND `tests/lint/no-static-admin-lens-import.spec.ts` fails AND `npm run build` fails the `/sops/page` marker assertion. All three should trip; record which did. Revert.
    - Mutation proof (run, then revert): swap the admin-status branch below the `workerSops.length === 0` branch → the positional assertion fails. Revert.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase41 && npm run build</automated>
  </verify>
  <done>The capability matrix records that access control moved from the route to the action, and the SUR-01/02/06 + deep-link contracts are live and mutation-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every authenticated role → `/sops` | this route stops being worker-only; a worker, supervisor, admin and safety_manager all render the same page component |
| URL query string → scope resolution | `view`, `status`, `owner`, `departments`, `collection`, `sop` arrive from bookmarks, external links and the shim |
| client render gate (`useIsAdmin()`) → admin data | the UI decides what to MOUNT; it does not and must not decide what a caller may READ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Elevation of Privilege | `useIsAdmin()`-gated lens rendering | mitigate | Treated as UX only. A non-admin session that forces an admin scope (devtools state edit) mounts a lens whose every action returns `{ error }` from `requireAdminContext()` (41-02, 41-03) and therefore renders the error panel with no data. Recorded in CAPABILITY-MATRIX as "route is not the boundary" |
| T-41-02 | Information Disclosure | `/sops/page` client chunk | mitigate | Three `dynamic({ ssr: false })` boundaries; `tests/lint/no-static-admin-lens-import.spec.ts` blocks a static import; the `/sops/page` bundle gate asserts ≤ +2 KB and the absence of five admin-lens string markers; Task 3 mutation-proves all three trip together |
| T-41-05 | Tampering | admin deep-link params | mitigate | `resolveInitialScope` drops every admin param for a non-admin and drops `departments`/`collection` under `admin-access`; params are forwarded only as opaque filter values to already-guarded actions |
| T-41-07 | Information Disclosure (wrong-tenant data in a shared render) | worker data hooks vs admin lens data | mitigate | The two data sets never merge: worker rows come from `useAssignedSops`/Dexie, admin rows from the guarded actions, and the branch renders one or the other. `workerSops` is asserted byte-identical so no admin row can enter the worker list |
| T-41-08 | Denial of Service (self-inflicted) | scope-change hot path | mitigate | `history.replaceState`, never `router.push` — an RSC fetch per scope click through the Serwist service worker is the exact regression CLAUDE.md 2026-05-13 fixed; asserted by the absence of `router.push` |
| T-41-SC | Tampering | npm installs | accept | No packages installed by this plan |
</threat_model>

## Artifacts this phase produces

Created/changed by this plan:
- `SopScope`, `AdminScope`, `ADMIN_SCOPES`, `resolveInitialScope`, `applyScope`, `isAdminStatusScope` in `src/app/(protected)/sops/page.tsx`
- Three `dynamic({ ssr: false })` lens bindings on that page
- New `Admin` group in the Miller scope column + the mobile scope strip
- `SOP list — admin lenses` row in `.planning/codebase/CAPABILITY-MATRIX.md`

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0; `/sops/page` within ±2 KB of the Wave-0 baseline; no forbidden marker reported for that route; `/sops/[sopId]/page` still green
- `npx playwright test` (full suite) exits 0 — note that several phase28/29/30/32/33 specs still read `admin/sops/page.tsx`, which this plan does not touch, so they must still be green here; they are repointed in 41-08 after 41-06 turns that file into a shim
- `npx playwright test --project=phase41` exits 0 with the merged-surface tests live
</verification>

<success_criteria>
- An admin's scope column carries All SOPs / Drafts / Published / (Still working) / Needs attention / Access / Owned by me; a worker's does not
- All seven legacy query params resolve on `/sops`; `departments`/`collection` inert under `admin-access`; all admin params inert for non-admins
- Scope changes rewrite the URL with `replaceState` and issue no navigation
- Worker logic byte-identical (`git diff` proves it for the named regions)
- `/sops/page` bundle within 2 KB and free of all five admin-lens markers
- CAPABILITY-MATRIX updated in this same commit
</success_criteria>

<output>
Create `.planning/phases/41-one-sop-surface/41-05-SUMMARY.md` when done. Record the measured `/sops/page` KB and delta, and the exact URL each admin scope writes — 41-06's shim must produce those same URLs.
</output>
</content>
