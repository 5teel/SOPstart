---
phase: 21.6-builder-edit-stage-redesign
plan: 05
type: execute
wave: 2
depends_on: ["21.6-02", "21.6-03", "21.6-04"]
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
  - src/lib/builder/puck-config.tsx
autonomous: true
requirements: [E1, E3, E6, E7]
must_haves:
  truths:
    - "BuilderClient mounts BuilderTreeRail in place of SectionListSidebar and passes ui={{leftSideBarVisible:false,rightSideBarVisible:false}} so exactly one persistent list shows"
    - "The Add control and From-library picker are wired (AddMenu + the shipped Phase 13 picker)"
    - "Orphan-photo HeadingBlock shows a 'Reference images' chip on the canvas via componentOverlay (display-only)"
    - "Autosave, junctionId stamping, AI overlays, the server 400 publish gate, and the no-bulk-verify lint guard are all intact (E7)"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx"
      provides: "BuilderTreeRail mount; ui sidebar suppression; AddMenu + library picker wiring; StructuredFieldPopover mount on canvas selection"
      contains: "BuilderTreeRail"
    - path: "src/lib/builder/puck-config.tsx"
      provides: "componentOverlay extended with the E6 Reference-images canvas chip (display-only, no prop mutation)"
      contains: "Reference images"
  key_links:
    - from: "BuilderClient.tsx"
      to: "BuilderTreeRail (replaces SectionListSidebar)"
      via: "import + JSX mount"
      pattern: "BuilderTreeRail"
    - from: "BuilderClient.tsx <Puck>"
      to: "suppressed native sidebars"
      via: "ui={{ leftSideBarVisible:false, rightSideBarVisible:false }}"
      pattern: "leftSideBarVisible:\\s*false"
    - from: "BuilderClient.tsx handleChange"
      to: "useBuilderAutosave → Dexie → useDraftLayoutSync → Supabase"
      via: "unchanged onChange wiring (E7)"
      pattern: "useBuilderAutosave"
---

<objective>
Wire the new surfaces into the live Build stage and close the loop on E1/E3/E6/E7: mount `BuilderTreeRail` in `BuilderClient` (replacing `SectionListSidebar`'s neighbour role), pass the `ui` prop to suppress Puck's native sidebars (proving E3's single-list invariant at runtime), connect `AddMenu` + the shipped Phase 13 library picker to the rail's Add control, mount `StructuredFieldPopover` off canvas selection, and add the E6 "Reference images" canvas chip via `componentOverlay` — all without touching autosave, junctionId, the AI overlays, or the publish gate (E7).

Purpose: This is the integration plan — the previous plans built the parts in isolation; this makes the reconfigured Build stage real. Per RESEARCH Q2/Q5 + PATTERNS, the autosave hook (`handleChange = useBuilderAutosave(...)`), the `componentIdToJunction`/`junctionMap` useMemo, and `componentItem`/`componentOverlay` must remain byte-for-byte intact — only ADD the rail mount, the `ui` prop, the Add/popover wiring, and the E6 overlay chip. Pitfall 5: pass `ui` as a prop (not setUi) so it survives the `key={activeSection.id}` remount.
Output: modified `BuilderClient.tsx` + a `componentOverlay` extension in `puck-config.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-SPEC.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-RESEARCH.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-PATTERNS.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan modifies:
- `BuilderClient.tsx`: swap `SectionListSidebar` → `BuilderTreeRail`; add `ui={{ leftSideBarVisible:false, rightSideBarVisible:false }}` to `<Puck>`; wire `AddMenu` open/close + the Phase 13 library picker (`onOpenLibrary`); mount `StructuredFieldPopover` on canvas block selection.
- `puck-config.tsx` `componentOverlay`: add the E6 "Reference images" chip for orphan-photo HeadingBlocks (display-only overlay, no prop mutation).
This plan flips the final Wave-0 assertions (E3 one-list `leftSideBarVisible: false`) GREEN and keeps the E7 tripwire GREEN.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the E6 Reference-images canvas chip via componentOverlay</name>
  <files>src/lib/builder/puck-config.tsx</files>
  <read_first>
    - src/lib/builder/puck-config.tsx (`createPuckOverrides` componentOverlay — read the existing UpdateAvailableBadge + ReviewerFlagsPanel + SelectionSyncTap render so the new chip ADDS to, not replaces, the existing overlay; the componentId → layout entry mapping)
    - 21.6-RESEARCH.md § Q6 (E6 via componentOverlay — render a replacement label chip OVER the block when a HeadingBlock's text startsWith 'Unanchored figures'; no layout_data change) + Pitfall 6
    - 21.6-UI-SPEC.md § Reference Images Gallery (neutral chip, kind-step tint, label "Reference images", no attach UI, no gate badge)
  </read_first>
  <action>
    In `createPuckOverrides()` `componentOverlay` in `src/lib/builder/puck-config.tsx`, ADD logic that — when the overlaid item is a `HeadingBlock` whose `props.text` (resolved via the existing componentId→layout lookup) `String(...).startsWith('Unanchored figures')` — renders a small neutral "Reference images" label chip (kind-step tint pill geometry per UI-SPEC) above/over the native block, alongside the existing overlay children. Do NOT remove or alter the existing UpdateAvailableBadge / ReviewerFlagsPanel / SelectionSyncTap rendering — wrap-and-add only. Do NOT mutate `props.text` or any layout_data; this is a render-time overlay only (Pitfall 6). Add no publish-gate logic and no attach button.
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs -g "E7"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `componentOverlay` still renders the existing UpdateAvailableBadge/ReviewerFlagsPanel/SelectionSyncTap AND adds a "Reference images" chip branch gated on `startsWith('Unanchored figures')`; no `props.text =` assignment and no `layout_data` write.
    - BEHAVIOR: E7 tripwire (`unverified_blocks`) stays GREEN; componentOverlay key still present; no publish-gate change introduced.
    - TEST: `npx playwright test --project=phase21.6-stubs -g "E7"` passes; `npx playwright test tests/lint/no-bulk-verify-ui.spec.ts` passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Orphan-photo headings carry a display-only "Reference images" canvas chip; existing overlays + publish gate untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mount BuilderTreeRail, suppress sidebars, wire Add + popover</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (FULL file — the SectionListSidebar mount ~lines 377-382; the `<Puck>` element ~lines 386-394; `handleChange = useBuilderAutosave(...)` line 61; the componentIdToJunction/junctionMap useMemo lines 187-205; the existing Phase 13 library picker modal mount + addBlockToSection call site)
    - src/components/admin/builder/BuilderTreeRail.tsx (Plan 03 — props contract: sections, activeSection, activeSectionId, onSelect, sopId, activeStepIndex/onStepSelect)
    - src/components/admin/builder/AddMenu.tsx + StructuredFieldPopover.tsx (Plan 04 — AddMenu props insertAfterIndex/onClose/onOpenLibrary; popover props blockId/blockType/anchorRef/onClose/children)
    - 21.6-RESEARCH.md § Q2/Q5 + Pitfall 5 (pass `ui` as a prop on every remount) + 21.6-PATTERNS.md § BuilderClient.tsx (swap snippet + ui prop snippet + "preserve junctionId/autosave exactly")
  </read_first>
  <action>
    In `BuilderClient.tsx`: (1) Replace the `SectionListSidebar` import + JSX mount with `BuilderTreeRail` from `@/components/admin/builder/BuilderTreeRail`, passing `sections`, `activeSection`, `activeSectionId`, `onSelect={setActiveSectionId}`, `sopId`. Keep `SectionListSidebar.tsx` on disk (source for fold; do not delete this phase). (2) Add `ui={{ leftSideBarVisible: false, rightSideBarVisible: false }}` to the `<Puck>` element — as a prop so it survives the `key={activeSection.id}` remount (Pitfall 5). Do NOT remove `key`, `config`, `overrides`, `data`, `onChange`, or `viewports`. (3) Wire the Add flow: hold AddMenu open/anchor state; render `<AddMenu insertAfterIndex={...} onClose={...} onOpenLibrary={openLibraryPicker} />` where `openLibraryPicker` opens the EXISTING Phase 13 block-picker modal/`addBlockToSection` flow already wired in this file (reuse it — do not reimplement); the insert index comes from the rail's active step (Open Question 2). After a library insert, preserve the existing junctionId-stamp follow-up. (4) Mount `<StructuredFieldPopover>` when a structured (non-text) block is selected on canvas, passing the selected block's anchor ref + the existing Puck field inputs as `children`. (5) DO NOT touch `handleChange = useBuilderAutosave(...)` (line 61) or the `componentIdToJunction`/`junctionMap` useMemo (lines 187-205) — these must remain identical (E7). Verify the wrapping flex layout gives the canvas `flex-1` width with the 280px rail (UI-SPEC layout).
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `BuilderClient.tsx` imports + mounts `BuilderTreeRail` (no `<SectionListSidebar` JSX mount remains), contains `leftSideBarVisible: false` and `rightSideBarVisible: false` on `<Puck>`, still contains `useBuilderAutosave` and the unchanged junctionMap useMemo, and reuses the existing library picker via `onOpenLibrary` (no second picker implementation).
    - BEHAVIOR: the Wave-0 E3 one-list assertion (`leftSideBarVisible: false`/`rightSideBarVisible: false`) flips RED→GREEN; all five phase21.6-stubs E-assertions are GREEN; the E7 tripwire + no-bulk-verify lint stay GREEN.
    - TEST: `npx playwright test --project=phase21.6-stubs` fully GREEN; `npx playwright test --project=phase21.5-unit --project=phase21.5-stubs --project=phase15-stubs` GREEN (no cross-phase regression); `npx tsc --noEmit` clean; `npm run build` succeeds (Windows/Next 16 webpack).
  </acceptance_criteria>
  <done>The reconfigured Build stage mounts the tree rail, suppresses native sidebars (one list), wires the humanised Add + shipped library picker + structured popover, and preserves autosave/junction/overlays/gate; full phase21.6-stubs suite GREEN; no cross-phase regression; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → BuilderClient (client) | All edit/insert paths route through the existing Puck `onChange` → `useBuilderAutosave` → Dexie → `useDraftLayoutSync` → Supabase; no NEW write path is introduced |
| admin → publish route (server 400 gate) | Unchanged — this plan adds no publish-gate logic; the server `400 unverified_blocks` gate remains the sole authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.6-05-01 | Tampering | Mount refactor accidentally drops the junctionMap useMemo or autosave hook (publish gate + 13-04 badges break) | mitigate | Action forbids touching line 61 + the 187-205 useMemo; acceptance greps both still present; E7 `unverified_blocks` tripwire + componentOverlay retention guard it |
| T-21.6-05-02 | Tampering | `ui` set via setUi instead of prop → sidebar flashes back on section remount (E3 regression) | mitigate | Pass `ui` as a `<Puck>` prop (Pitfall 5); Wave-0 E3 one-list assertion gates the literal |
| T-21.6-05-03 | Elevation of Privilege | Library picker reimplemented, bypassing Phase 13 RLS/junction contract | mitigate | Reuse the EXISTING picker via `onOpenLibrary` (D-03); acceptance forbids a second picker implementation |
| T-21.6-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npx playwright test --project=phase21.6-stubs` — all E1/E2/E3/E4/E5/E6/E7 assertions GREEN.
- `npx playwright test --project=phase21.5-unit --project=phase21.5-stubs --project=phase15-stubs` — no regression.
- `npx playwright test tests/lint/no-bulk-verify-ui.spec.ts` — GREEN.
- `npx tsc --noEmit` clean; `npm run build` succeeds.
- Runtime click-to-edit / popover feel + 768px reflow → Railway UAT on sopstart.com post-deploy (per project convention; see VALIDATION § Manual-Only).
</verification>

<success_criteria>
- The reconfigured Build stage is live: humanised step-centric rail, one list, inline editing, Add affordance with library, structured popover, Reference-images chip — with autosave/junction/overlays/publish gate fully preserved.
- All 7 requirements (E1–E7) are covered across Plans 01–05 and gated by the phase21.6-stubs suite.
</success_criteria>

<output>
Create `.planning/phases/21.6-builder-edit-stage-redesign/21.6-05-SUMMARY.md` when done.
</output>
