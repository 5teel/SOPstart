---
phase: 21.6-builder-edit-stage-redesign
plan: "01"
subsystem: builder-edit-stage
tags: [playwright, source-contract, lint-guard, tdd-scaffold]
dependency_graph:
  requires: []
  provides:
    - phase21.6-stubs playwright project registered
    - tests/builder/builder-edit-stage.spec.ts (E3/E4/E5/E6/E7 source-contract)
    - tests/lint/no-raw-block-types-in-build.spec.ts (E2 lint guard)
  affects:
    - playwright.config.ts (new project regex)
    - Plans 02-05 (gate: E3/E4/E5/E6/E3-one-list will flip GREEN as each plan lands)
tech_stack:
  added: []
  patterns:
    - source-contract spec pattern (no chromium, fs.readFileSync assertions)
    - lint-guard spec pattern (directory walk + banned-phrase detection)
key_files:
  created:
    - tests/builder/builder-edit-stage.spec.ts
    - tests/lint/no-raw-block-types-in-build.spec.ts
  modified:
    - playwright.config.ts
decisions:
  - "E7 acts as a tripwire regression guard (GREEN now, must stay GREEN through Plans 02-05)"
  - "E2 lint guard passes vacuously until Plan 03 creates src/components/admin/builder/"
  - "phase21.6-stubs testMatch covers both no-raw-block-types-in-build and builder-edit-stage"
metrics:
  duration_minutes: 8
  completed_date: "2026-06-05"
  tasks_completed: 2
  files_changed: 3
---

# Phase 21.6 Plan 01: Wave-0 Validation Scaffold Summary

**One-liner:** Playwright `phase21.6-stubs` project + E2–E7 source-contract and lint-guard specs giving RED gates for all five downstream plans.

## What Was Built

Two test files and one playwright project registration form the Wave-0 validation scaffold for Phase 21.6:

- `tests/builder/builder-edit-stage.spec.ts` — 6 source-contract assertions (E3, E4, E5, E6, E7, E3-one-list), reading source files from disk, no chromium. E7 is a regression tripwire (GREEN now); E3/E4/E5/E6/E3-one-list fail RED until Plans 02–05 write the production code.
- `tests/lint/no-raw-block-types-in-build.spec.ts` — E2 lint guard walks `src/components/admin/builder/` and asserts no raw PascalCase `*Block` names render as user-visible strings. Passes vacuously until Plan 03 creates the directory; traps leaks automatically thereafter.
- `playwright.config.ts` — new `phase21.6-stubs` project with `testMatch: /(no-raw-block-types-in-build|builder-edit-stage)\.(test|spec)\.ts$/`.

## Verification Results

```
npx playwright test --list --project=phase21.6-stubs
Total: 7 tests in 2 files

npx playwright test --project=phase21.6-stubs
  GREEN: E7 (tripwire — unverified_blocks gate still present)
  GREEN: E2 (vacuous — builder dir not yet created)
  RED:   E3, E4, E5, E6, E3-one-list (production code not yet written)

npx tsc --noEmit
(clean — no type errors)
```

RED/GREEN state is correct for a TDD Wave-0 scaffold.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create source-contract spec and lint guard | e9ec826 | tests/builder/builder-edit-stage.spec.ts, tests/lint/no-raw-block-types-in-build.spec.ts |
| 2 | Register phase21.6-stubs playwright project | eb8cbb1 | playwright.config.ts |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. This plan creates only test infrastructure; no production components are stubbed.

## Threat Flags

None. No new runtime surface introduced (test files only).

## Self-Check: PASSED

- `tests/builder/builder-edit-stage.spec.ts`: FOUND
- `tests/lint/no-raw-block-types-in-build.spec.ts`: FOUND
- `playwright.config.ts` contains `phase21.6-stubs`: FOUND
- Commit e9ec826: FOUND
- Commit eb8cbb1: FOUND
- `npx playwright test --list --project=phase21.6-stubs` shows 7 tests in 2 files: VERIFIED
