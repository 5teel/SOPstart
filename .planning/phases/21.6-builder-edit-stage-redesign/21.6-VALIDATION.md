---
phase: 21.6
slug: builder-edit-stage-redesign
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-05
---

# Phase 21.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright (source-contract + lint guards; no chromium runtime locally — Railway-only UAT) |
| **Config file** | `playwright.config.ts` (add a new `phase21.6-stubs` project per RESEARCH Wave 0) |
| **Quick run command** | `npx playwright test --project=phase21.6-stubs` |
| **Full suite command** | `npx tsc --noEmit` then `npx playwright test --project=phase21.6-stubs` |
| **Estimated runtime** | ~5–10 s (source-contract specs, no browser) |

---

## Sampling Rate

- **After every task commit:** `npx playwright test --project=phase21.6-stubs`
- **After every plan wave:** `npx tsc --noEmit` + the phase21.6-stubs project
- **Before `/gsd-verify-work`:** Full suite green + `npx tsc --noEmit` clean
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _planner fills_ | | | E1–E7 | — | — | source-contract / lint | `npx playwright test --project=phase21.6-stubs` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/lint/no-raw-block-types-in-build.spec.ts` — E2 guard: no raw `*Block`/`Grid`/`PhotoGrid` identifiers rendered in the Build-stage UI
- [ ] `tests/builder/builder-edit-stage.spec.ts` — source-contract for E3 (one list), E4 (inline edit), E5 (section rail), E6 (Reference images relabel), E7 (autosave/junction/overlay/gate preserved)
- [ ] Register both in a new `phase21.6-stubs` project regex in `playwright.config.ts` (CLAUDE.md learning 2026-05-25 — validate with `npx playwright test --list --project=phase21.6-stubs`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Inline click-to-edit caret + popover feel | E4 | Requires live browser render + Puck runtime | On sopstart.com: click a step's text → caret appears → edit → autosaves; structured field popover anchors to block |
| Nested tree rail visual + reorder | E1/E5 | Requires live render | Section ▸ Steps renders; drag-reorder sections works; humanised step rows, no jargon |
| 768px responsive behaviour | (UI-SPEC) | CSS reflow needs browser | Resize to 768px — rail + canvas usable |

*Runtime UAT runs on sopstart.com post-deploy per project convention (Railway-only testing).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the two specs above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
