---
phase: 21.6-builder-edit-stage-redesign
plan: 04
type: execute
wave: 2
depends_on: ["21.6-02", "21.6-03"]
files_modified:
  - src/components/admin/builder/AddMenu.tsx
  - src/components/admin/builder/StructuredFieldPopover.tsx
autonomous: true
requirements: [E2, E3, E4]
must_haves:
  truths:
    - "A single '＋ Add' menu inserts a humanised-typed block after the active step (or at section end) via Puck dispatch, and preserves the Phase 13 'From library…' picker"
    - "Structured (non-text) fields remain editable via a compact popover anchored to the selected block, routed through Puck's onChange (no parallel write path)"
    - "Every Add-menu label resolves through humanizeBlockType — no raw block-type identifier"
  artifacts:
    - path: "src/components/admin/builder/AddMenu.tsx"
      provides: "Grouped humanised add menu + From-library entry; useGetPuck insert dispatch at root:default-zone"
      contains: "root:default-zone"
    - path: "src/components/admin/builder/StructuredFieldPopover.tsx"
      provides: "Anchored popover for structured field editing; Escape/click-out close"
  key_links:
    - from: "AddMenu.tsx"
      to: "Puck InsertAction (root:default-zone)"
      via: "useGetPuck().dispatch insert at insertAfterIndex+1"
      pattern: "destinationZone:\\s*'root:default-zone'"
    - from: "AddMenu.tsx 'From library…'"
      to: "Phase 13 addBlockToSection / block picker"
      via: "onOpenLibrary callback (D-03 — preserve shipped picker)"
      pattern: "onOpenLibrary"
    - from: "AddMenu / StructuredFieldPopover labels"
      to: "humanizeBlockType"
      via: "single label source (E2)"
      pattern: "humanizeBlockType"
---

<objective>
Build the two interaction surfaces that complete E3's "one list + deliberate add affordance" and E4's structured-field editing: `AddMenu` (a single grouped, humanised "＋ Add" menu that dispatches Puck inserts and preserves the Phase 13 library picker per D-03) and `StructuredFieldPopover` (a compact anchored editor for the non-text fields that can't be inline-edited per D-04). Both route through Puck's existing dispatch/onChange — never a parallel write path.

Purpose: E3 collapses the two old lists into one persistent list (the Plan 03 rail) PLUS one explicit Add control — this plan is that control. D-03 mandates the library picker survives, so AddMenu exposes a `From library…` entry that triggers the shipped Phase 13 flow. D-04 keeps structured fields (photo layout enum, decision branches, measurement units) reachable via popover. Per RESEARCH Q3, inserts use `useGetPuck().dispatch` with `destinationZone: 'root:default-zone'`; the insert index comes from the rail's `activeStepIndex` (Open Question 2 recommendation).
Output: 2 new files under `src/components/admin/builder/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-SPEC.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-RESEARCH.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-PATTERNS.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `src/components/admin/builder/AddMenu.tsx` — grouped humanised add menu (STEPS / ANNOTATIONS / SAFETY / STRUCTURED groups + `From library…` entry); `useGetPuck()` insert dispatch at `root:default-zone`.
- `src/components/admin/builder/StructuredFieldPopover.tsx` — anchored popover hosting the existing Puck structured field inputs; Escape + click-outside close.
(Plan 05 wires AddMenu into BuilderTreeRail's Add control and the library picker callback, and mounts StructuredFieldPopover off canvas selection.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build AddMenu (humanised groups + library entry + insert dispatch)</name>
  <files>src/components/admin/builder/AddMenu.tsx</files>
  <read_first>
    - src/components/admin/blocks/BlockPicker.tsx (grouped list + selection analog)
    - 21.6-RESEARCH.md § Pattern 3 + Q3 (useGetPuck dispatch InsertAction; root:default-zone; junctionId stamp contract for the library path) + Open Question 2 (insert index from rail activeStepIndex)
    - 21.6-PATTERNS.md § AddMenu (useGetPuck dispatch; menu group header + item row + container; From-library entry using Library icon; "All labels resolve through humanizeBlockType")
    - 21.6-UI-SPEC.md § Add Menu (group structure STEPS/ANNOTATIONS/SAFETY/STRUCTURED; item geometry 60px pill + 32px row; container border/shadow/maxHeight 320px; From library… row) + § Copywriting (group labels, `From library…`)
    - src/lib/builder/block-type-labels.ts (humanizeBlockType, BLOCK_TYPE_LABELS pillVariant) and src/actions/sop-section-blocks.ts (addBlockToSection contract — caller stamps junctionId)
  </read_first>
  <action>
    Create `src/components/admin/builder/AddMenu.tsx` (`'use client'` + D-21-09 isolation comment). Props: `{ insertAfterIndex: number, onClose: () => void, onOpenLibrary: () => void }`. Capture `const getPuck = useGetPuck()` from `@puckeditor/core`. Implement `insertBlock(componentType: string)` that calls `getPuck().dispatch({ type: 'insert', componentType, destinationIndex: insertAfterIndex + 1, destinationZone: 'root:default-zone' })` then `onClose()` — hard-code the `'root:default-zone'` string per RESEARCH Pitfall 2. Render `role="menu"` container (`--paper` bg, `1px solid var(--ink-300)`, radius 4px, box-shadow, maxHeight 320px, overflowY auto, z-50, `data-testid="add-menu"`). Render four groups exactly per UI-SPEC: STEPS (StepBlock, StepWithPhotosBlock), ANNOTATIONS (HeadingBlock, TextBlock, CalloutBlock, VoiceNoteBlock), SAFETY (HazardCardBlock, PPECardBlock, EscalateBlock), STRUCTURED (MeasurementBlock, DecisionBlock, InspectBlock, SignOffBlock). Each group header: JetBrains Mono 10px/600 uppercase `--ink-400`, `padding: 4px 8px`. Each item: `role="menuitem"`, 32px row, 60px kind pill (`getPillStyle(BLOCK_TYPE_LABELS[type].pillVariant)`) + label = `humanizeBlockType(type)` (JetBrains Mono 12px/400) — NEVER inline a PascalCase string as the visible label. Final row: `From library…` (no pill, Lucide `Library` 14px `--ink-500`, italic) whose onClick calls `onOpenLibrary()` — this preserves the Phase 13 picker (D-03); do NOT reimplement the picker here. Close on Escape (window keydown listener, cleanup on unmount).
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs -g "E2"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `AddMenu.tsx` contains `useGetPuck`, `destinationZone: 'root:default-zone'`, `onOpenLibrary`, and resolves every visible item label via `humanizeBlockType`; it does NOT contain a hand-rolled library picker (delegates via `onOpenLibrary`).
    - BEHAVIOR: lint guard finds zero raw `*Block` user-visible string literals in this file; the four groups + `From library…` entry are present.
    - TEST: `npx playwright test --project=phase21.6-stubs -g "E2"` passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A single grouped humanised Add menu dispatches inserts at root:default-zone and routes the library path to the shipped Phase 13 picker; no jargon; types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build StructuredFieldPopover (anchored structured-field editor)</name>
  <files>src/components/admin/builder/StructuredFieldPopover.tsx</files>
  <read_first>
    - src/components/admin/blocks/BlockUpdateReviewModal.tsx (anchored panel + field display analog; Escape close pattern)
    - 21.6-RESEARCH.md § Q1 "Blocks where fields stay panel/popover only" (which fields belong here: array/select/number/radio) + § D-04 + Open Question 1 (anchor positioning; slim-rail fallback allowed if cramped)
    - 21.6-PATTERNS.md § StructuredFieldPopover (container; header; Escape close; anchor via getBoundingClientRect)
    - 21.6-UI-SPEC.md § "Structured field popover" (280px width, 360px maxHeight, anchor bottom-left of selected block, header = humanised type + ×, close on Escape/click-out/reselect; Decision/Measurement slim-rail fallback allowance)
  </read_first>
  <action>
    Create `src/components/admin/builder/StructuredFieldPopover.tsx` (`'use client'` + D-21-09 isolation comment). Props per UI-SPEC Component Inventory: `{ blockId, blockType, anchorRef, onClose, children }` (the `children` slot receives the existing Puck field inputs for the structured fields — this popover is a CHROME wrapper; it does NOT reimplement field editing, so edits continue to flow through Puck's own onChange → autosave, satisfying E4/E7 "no parallel write path"). `role="dialog"`, `aria-label={"Edit " + humanizeBlockType(blockType) + " fields"}`, `data-testid="structured-field-popover"`. Container: `position: fixed`, width 280px, maxHeight 360px, overflowY auto, `--paper` bg, `1.5px solid var(--ink-300)`, radius 4px, box-shadow, z-50. Position from `anchorRef.current?.getBoundingClientRect()` → `top: rect.bottom + 4, left: rect.left`. Header row: humanised type name (JetBrains Mono 11px/600 uppercase `--ink-500`) + `×` close button (`aria-label="Close"`). Close on: Escape (window keydown), click outside (mousedown listener checking the popover ref), and expose an effect that calls `onClose()` when `blockId` changes (reselect). For the Decision/Measurement complexity case, leave a code comment marking where the slim 280px right-rail fallback (D-04 allowance) would mount — implement the popover form; the fallback is allowed-not-required, so do not build a second component unless the popover form for Decision arrays is unworkable (note the decision in the SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `StructuredFieldPopover.tsx` exists, is `role="dialog"` with `aria-label` via `humanizeBlockType`, accepts a `children` slot for Puck field inputs, and contains an Escape + click-outside close.
    - BEHAVIOR: the popover renders chrome only and does NOT write to Supabase/Dexie directly (no import of autosave or a server action that mutates layout_data) — edits stay on Puck's onChange path; `data-testid="structured-field-popover"` present.
    - TEST: `npx tsc --noEmit` clean; `npx playwright test --project=phase21.6-stubs -g "E2"` passes (no jargon leak in this file).
  </acceptance_criteria>
  <done>Anchored structured-field popover exists as a chrome wrapper over existing Puck fields with correct a11y + close behaviour; no parallel write path; types clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → Puck dispatch (insert) | AddMenu inserts via the engine's own InsertAction; no direct layout_data write — same trust level as existing Phase 13 picker |
| admin → addBlockToSection (library path) | Existing Phase 13 server action invoked via onOpenLibrary callback; no new boundary, reuses shipped junctionId stamp contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.6-04-01 | Tampering | StructuredFieldPopover writes layout_data directly, bypassing autosave/junction | mitigate | Popover is a chrome wrapper only; edits flow through Puck onChange (acceptance asserts no autosave/server-action import); E7 tripwire guards regression |
| T-21.6-04-02 | Tampering | Insert dispatched to wrong/nonexistent zone (silent data loss) | mitigate | Hard-coded `'root:default-zone'` per RESEARCH Pitfall 2 (verified from Puck source); key_link grep gate asserts the literal |
| T-21.6-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npx playwright test --project=phase21.6-stubs` — E2 (lint) GREEN, E7 tripwire stays GREEN.
- `npx tsc --noEmit` clean.
- AddMenu contains `'root:default-zone'` and `onOpenLibrary`; StructuredFieldPopover has no autosave/server-action import (grep).
</verification>

<success_criteria>
- One explicit Add affordance (humanised + library-preserving) and a structured-field popover exist, both on Puck's native dispatch/onChange path.
</success_criteria>

<output>
Create `.planning/phases/21.6-builder-edit-stage-redesign/21.6-04-SUMMARY.md` when done.
</output>
