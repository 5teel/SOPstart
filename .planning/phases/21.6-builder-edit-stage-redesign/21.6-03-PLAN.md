---
phase: 21.6-builder-edit-stage-redesign
plan: 03
type: execute
wave: 1
depends_on: ["21.6-01"]
files_modified:
  - src/components/admin/builder/BuilderTreeRail.tsx
  - src/components/admin/builder/TreeSectionRow.tsx
  - src/components/admin/builder/TreeStepRow.tsx
  - src/components/admin/builder/TreeBlockRow.tsx
autonomous: true
requirements: [E1, E2, E5, E6]
must_haves:
  truths:
    - "For the active section, an ordered humanised Step list is the spine; non-step blocks nest under the nearest preceding step (presentation-only, layout_data untouched)"
    - "Every block-type label in the rail resolves through humanizeBlockType — no raw PascalCase leaks"
    - "The section rail is labelled SECTIONS, renders in sort_order, and keeps drag-reorder via reorderSections"
    - "Orphan-photo HeadingBlock displays as 'Reference images', not 'Unanchored figures'"
  artifacts:
    - path: "src/components/admin/builder/BuilderTreeRail.tsx"
      provides: "Nested Section ▸ Steps rail; deriveStepTree transform; reorderSections wiring; Reference-images display override"
      contains: "reorderSections"
    - path: "src/components/admin/builder/TreeSectionRow.tsx"
      provides: "Draggable section parent row (grip + collapse chevron)"
    - path: "src/components/admin/builder/TreeStepRow.tsx"
      provides: "Numbered step child row (kind pill + label)"
    - path: "src/components/admin/builder/TreeBlockRow.tsx"
      provides: "Indented block grandchild row (kind pill + muted label)"
  key_links:
    - from: "BuilderTreeRail.tsx"
      to: "humanizeBlockType / BLOCK_TYPE_LABELS"
      via: "single label + pillVariant source (E2)"
      pattern: "humanizeBlockType"
    - from: "BuilderTreeRail.tsx"
      to: "reorderSections server action"
      via: "optimistic drag-reorder (folded from SectionListSidebar)"
      pattern: "reorderSections"
    - from: "BuilderTreeRail.tsx (HeadingBlock display)"
      to: "'Reference images' label"
      via: "startsWith('Unanchored figures') display substitution (E6, no mutation)"
      pattern: "Reference images"
---

<objective>
Build the custom left-rail tree (E1/E2/E5) plus its E6 display relabel: a single collapsible `Section ▸ Steps` rail that replaces both `SectionListSidebar`'s neighbour role and Puck's native outline. It derives a presentation-only step tree from the flat `layout_data.content[]`, humanises every label via `humanizeBlockType`, folds in the existing `reorderSections` drag logic, and renders orphan-photo headings as "Reference images" — all read-only over `layout_data` (no mutation). Pure React, no API changes.

Purpose: This is the E1 spine and E5 section clarity. Per D-01/D-02 + RESEARCH Pattern 4, grouping is presentation-only (`deriveStepTree`); per E6 + Pitfall 6 the "Reference images" label is a display substitution only — the parser string `Unanchored figures (review)` is NEVER changed (mutating it would autosave back and corrupt round-trips). The rail renders OUTSIDE `<Puck>`, so it captures `useGetPuck()` for later dispatch (consumed by Plan 04's AddMenu).
Output: 4 new files under `src/components/admin/builder/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-SPEC.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-RESEARCH.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-PATTERNS.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-UI-SPEC.md
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- `src/components/admin/builder/BuilderTreeRail.tsx` — root rail: `deriveStepTree` transform (STEP_TYPES set), SECTIONS heading, section rows, AddMenu mount point (placeholder until Plan 04), `useGetPuck()` capture, `reorderSections` optimistic drag, E6 display substitution.
- `src/components/admin/builder/TreeSectionRow.tsx` — section parent row.
- `src/components/admin/builder/TreeStepRow.tsx` — numbered step child row.
- `src/components/admin/builder/TreeBlockRow.tsx` — block grandchild row.
(Add menu + structured popover wiring lands in Plan 04; BuilderClient mount lands in Plan 05.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build TreeSectionRow, TreeStepRow, TreeBlockRow</name>
  <files>src/components/admin/builder/TreeSectionRow.tsx, src/components/admin/builder/TreeStepRow.tsx, src/components/admin/builder/TreeBlockRow.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/NavRow.tsx (EXACT analog for TreeStepRow: getPillStyle lines 37-83 copy verbatim; active state + pill geometry lines 109-189)
    - src/app/(protected)/admin/sops/builder/[sopId]/SectionListSidebar.tsx (li-row analog for TreeSectionRow: grip + select button lines 70-101)
    - src/lib/builder/block-type-labels.ts (humanizeBlockType, BLOCK_TYPE_LABELS, pillVariant — single label source)
    - 21.6-UI-SPEC.md § "Tree rail row anatomy" (section row 40px / step row 36px + 24px indent / block row 28px + 40px indent) + § Typography table + § Semantic Pill Colors
    - 21.6-PATTERNS.md § TreeSectionRow / TreeStepRow / TreeBlockRow (geometry + data-testid + getPillStyle copy)
  </read_first>
  <action>
    Create three `'use client'` row components, each with the D-21-09 isolation header comment (`// D-21-09 isolation: admin-only; never imported by worker routes.`) as the first line after `'use client'`.
    `TreeSectionRow.tsx`: props `{ section, isActive, isExpanded, onToggle, onSelect, draggable drag handlers }`. Render `data-testid="tree-section-row"`, `role="button"`, `aria-expanded={isExpanded}`, `aria-label={section.title}`. Left GripVertical (14px, `var(--ink-500)`, cursor-grab, `data-drag-handle`, `aria-hidden`), section title Inter 13px/500 (`--ink-900` active / `--ink-500` inactive), right ChevronDown/ChevronRight (14px `--ink-400`) toggling expansion. Active state `border-left: 2px solid var(--accent-step)` + `background: var(--paper-2)`. Min height 40px.
    `TreeStepRow.tsx`: copy NavRow's `getPillStyle` switch VERBATIM. Props `{ item (PuckItem), stepNumber, isActive, onSelect }`. Render `data-testid="tree-step-row"`, `role="button"`, `tabIndex={0}`, `aria-label={"Step " + stepNumber + ": " + preview}`. Left 16×16 circle dot (empty = `--ink-300` border). 74px kind pill (JetBrains Mono 10px/600 uppercase, `getPillStyle(BLOCK_TYPE_LABELS[item.type].pillVariant)`), then `Step {stepNumber}` prefix + truncated preview (JetBrains Mono 12px/500, `--ink-900`). Preview = `item.props.text ?? item.props.title ?? item.props.label ?? ''` truncated 60 chars. Active `border: 1px solid var(--accent-step); background: #eff4ff`; hover `var(--paper-2)`; `paddingLeft: 24px`.
    `TreeBlockRow.tsx`: reuse the SAME getPillStyle (import a shared copy or duplicate per PATTERNS). Props `{ item, onSelect }`. `data-testid="tree-block-row"`, `role="button"`, `aria-label={humanizeBlockType(item.type) + ": " + preview}`. 60px kind pill (narrower than step row), label JetBrains Mono 12px/400 `--ink-500` truncated 40 chars, no circle dot, `paddingLeft: 40px`, min height 28px. Every block-type label MUST come from `humanizeBlockType(item.type)` — never inline a PascalCase string.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: all three files exist under `src/components/admin/builder/`, start with `'use client'` + the D-21-09 comment, and import `humanizeBlockType`/`BLOCK_TYPE_LABELS` from `@/lib/builder/block-type-labels`.
    - BEHAVIOR: no raw PascalCase block-type string literal appears as user-visible text (labels resolve via humanizeBlockType); each row carries its `data-testid` and `aria-label`.
    - TEST: `npx tsc --noEmit` clean; `npx playwright test --project=phase21.6-stubs -g "E2"` (lint guard) passes once the dir exists.
  </acceptance_criteria>
  <done>Three pill-coded, humanised, accessible rows exist matching the UI-SPEC geometry; no jargon leaks; types clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build BuilderTreeRail (deriveStepTree + reorder + E6 relabel)</name>
  <files>src/components/admin/builder/BuilderTreeRail.tsx</files>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/SectionListSidebar.tsx (drag-reorder + optimistic commit/revert lines 30-101; reorderSections call shape; error strip)
    - 21.6-RESEARCH.md § Pattern 4 (deriveStepTree algorithm — STEP_TYPES, preFlight before-first-step grouping) + § Q4 (preview field names per block type) + Pitfall 6 (E6 no mutation)
    - 21.6-PATTERNS.md § BuilderTreeRail (imports incl. useGetPuck; deriveStepTree verbatim; "Reference images" display override snippet; nav wrapper aria-label; data-testid)
    - 21.6-UI-SPEC.md § Left Rail layout (280px ≥1024px / 240px 768-1023px) + § Copywriting (SECTIONS heading, "Before first step" header, empty-section state, reorder error copy)
    - src/actions/sections.ts (reorderSections signature — `{ sopId, orderedSectionIds }` returns `{ error }` on failure)
  </read_first>
  <action>
    Create `src/components/admin/builder/BuilderTreeRail.tsx` (`'use client'` + D-21-09 isolation comment). Props per UI-SPEC Component Inventory: `{ sections, activeSection, activeSectionId, onSelect, sopId }` (plus an `activeStepIndex`/`onStepSelect` local-state pair for the future AddMenu insert anchor — see Open Question 2 recommendation: tree-rail `activeStepIndex` is the insert source of truth). `<nav aria-label="SOP builder — sections and steps">`, width `w-[280px]` (240px at 768-1023px via responsive class), `data-testid="builder-tree-rail"`.
    Implement the `STEP_TYPES = new Set(['StepBlock','StepWithPhotosBlock'])` and the `deriveStepTree(content)` transform VERBATIM from RESEARCH Pattern 4 / PATTERNS (pure read of `activeSection`'s `layout_data.content[]`; no mutation; access props defensively with optional chaining since content items are `unknown` in LayoutDataSchema). When the active section HAS steps, render TreeStepRow per step with nested TreeBlockRow children + a non-clickable "Before first step" header (JetBrains Mono 10px/500 uppercase `--ink-500`) above any pre-first-step blocks. When it has NO steps (D-02), render each top-level block as a flat TreeBlockRow.
    Fold the SectionListSidebar reorder: `useState` order sorted by `sort_order`, `commitReorder` calling `reorderSections({ sopId, orderedSectionIds })` with optimistic set + revert-on-error + the red error strip (copy: `Couldn't save order — try again`). Render a "SECTIONS" heading (JetBrains Mono 10px/600 uppercase `--ink-500`) above the TreeSectionRow list. Each section row collapses/expands its children (local `expandedSectionId` state, active section expanded by default).
    E6 display override (NO mutation): when rendering a `HeadingBlock` row whose `String(item.props?.text ?? '').startsWith('Unanchored figures')`, show `Reference images` as the row label instead of the raw text; otherwise use `item.props.text ?? item.props.title ?? item.props.label ?? humanizeBlockType(item.type)`. Never write this substitution back to props/layout_data.
    Capture `const getPuck = useGetPuck()` (from `@puckeditor/core`) at the top and expose an `insertBlock(componentType, afterIndex)` helper (dispatch placeholder wired in Plan 04 — for now define it to call `getPuck().dispatch({ type:'insert', componentType, destinationIndex: afterIndex+1, destinationZone: 'root:default-zone' })`). Render a placeholder Add control button (`＋ Add step or block`, dashed border, 32px, `data-testid="add-control"`) at each section end whose `onClick` is a no-op stub commented `// AddMenu wired in Plan 04` (do NOT import AddMenu yet — that file lands in Plan 04). Empty-section state copy: `This section has no content yet. Use ＋ Add to add your first step.`
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs -g "E5|E6"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `BuilderTreeRail.tsx` contains `reorderSections`, `useGetPuck`, `root:default-zone`, `deriveStepTree`, `Reference images`, and `STEP_TYPES`; it does NOT mutate `item.props` or call any layout_data write.
    - BEHAVIOR: Wave-0 E5 (`reorderSections` wired) and E6 (`Reference images` present; no non-comment `Unanchored figures` line lacking `startsWith`) assertions flip RED→GREEN; a section with K steps yields K numbered rows in source order with non-step blocks nested.
    - TEST: `npx playwright test --project=phase21.6-stubs -g "E5|E6"` passes; `npx playwright test --project=phase21.6-stubs -g "E2"` (lint) passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>BuilderTreeRail renders the Section ▸ Steps spine over flat layout_data, folds reorder, relabels orphan photos display-only, and captures useGetPuck for Plan 04; E5+E6 contracts GREEN; no jargon leak; no mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → reorderSections server action | Existing boundary folded from SectionListSidebar; no new write path, no new validation surface — reuses the shipped Phase 12 RPC + RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.6-03-01 | Tampering | E6 relabel mutates layout_data → autosaves "Reference images" over parser string | mitigate | Display-only substitution in render path; never written to props (RESEARCH Pitfall 6); Wave-0 E6 guard asserts no non-comment raw-string write |
| T-21.6-03-02 | Information Disclosure | Tree rail reads layout_data for a non-owned org | accept | Admin-only route + RLS gate the data BuilderClient passes in; rail is pure presentation over already-authorised data; D-21-09 isolation comment present |
| T-21.6-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npx playwright test --project=phase21.6-stubs` — E2 (lint), E5, E6 GREEN; E7 tripwire stays GREEN.
- `npx tsc --noEmit` clean.
- No `item.props.* =` assignment or layout_data write anywhere in the 4 files (grep).
</verification>

<success_criteria>
- The humanised Section ▸ Steps spine exists with reorder + E6 relabel, all read-only over flat layout_data; no jargon; useGetPuck captured for Plan 04.
</success_criteria>

<output>
Create `.planning/phases/21.6-builder-edit-stage-redesign/21.6-03-SUMMARY.md` when done.
</output>
