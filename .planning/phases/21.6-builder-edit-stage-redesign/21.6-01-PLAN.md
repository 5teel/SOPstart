---
phase: 21.6-builder-edit-stage-redesign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/lint/no-raw-block-types-in-build.spec.ts
  - tests/builder/builder-edit-stage.spec.ts
  - playwright.config.ts
autonomous: true
requirements: [E2, E3, E4, E5, E6, E7]
must_haves:
  truths:
    - "A registered phase21.6-stubs playwright project discovers both new specs"
    - "Source-contract spec asserts the E3/E4/E5/E6/E7 structural invariants and fails RED at phase head"
    - "A lint guard asserts no raw block-type identifier leaks to Build-stage UI (E2)"
  artifacts:
    - path: "tests/builder/builder-edit-stage.spec.ts"
      provides: "Source-contract assertions for E3/E4/E5/E6/E7"
      contains: "unverified_blocks"
    - path: "tests/lint/no-raw-block-types-in-build.spec.ts"
      provides: "E2 raw-block-type leak guard"
    - path: "playwright.config.ts"
      provides: "phase21.6-stubs project registration"
      contains: "phase21.6-stubs"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/builder/builder-edit-stage.spec.ts + tests/lint/no-raw-block-types-in-build.spec.ts"
      via: "phase21.6-stubs testMatch regex"
      pattern: "no-raw-block-types-in-build|builder-edit-stage"
---

<objective>
Lay down the Wave-0 validation scaffold for Phase 21.6: two source-contract/lint specs plus a registered `phase21.6-stubs` playwright project, so every downstream task (Plans 02–05) has an automated RED→GREEN feedback gate. No production code is touched in this plan.

Purpose: Establish the Nyquist gate FIRST. Per CLAUDE.md 2026-05-25, a spec file not in a playwright project regex NEVER runs — so registration is part of this plan, validated with `--list`. Runtime UAT is Railway-only; these are static source-contract + lint guards (Phase 15/21/21.5 precedent), no chromium runtime.
Output: `tests/builder/builder-edit-stage.spec.ts`, `tests/lint/no-raw-block-types-in-build.spec.ts`, `phase21.6-stubs` project in `playwright.config.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-SPEC.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-RESEARCH.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-PATTERNS.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates:
- New test file: `tests/builder/builder-edit-stage.spec.ts` (source-contract: E3/E4/E5/E6/E7)
- New test file: `tests/lint/no-raw-block-types-in-build.spec.ts` (E2 raw-block-type leak guard)
- New playwright project: `phase21.6-stubs` in `playwright.config.ts`
(Downstream plans add: BuilderTreeRail/TreeSectionRow/TreeStepRow/TreeBlockRow/AddMenu/StructuredFieldPopover components, `components`/`outline` override keys, `contentEditable: true` fields, E6 componentOverlay relabel.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the source-contract spec and lint guard</name>
  <files>tests/builder/builder-edit-stage.spec.ts, tests/lint/no-raw-block-types-in-build.spec.ts</files>
  <read_first>
    - tests/lint/no-bulk-verify-ui.spec.ts (EXACT structure to copy for the lint guard — walk + phrase detection lines ~65-109)
    - tests/builder/builder-review-flow.spec.ts (21.5 source-contract pattern — fs.readFileSync + assert structural properties, no chromium)
    - 21.6-PATTERNS.md § `tests/builder/builder-edit-stage.spec.ts` (the five copy-ready assertions) and § `tests/lint/no-raw-block-types-in-build.spec.ts`
    - 21.6-RESEARCH.md § Validation Architecture (Phase Requirements → Test Map; Wave 0 Gaps)
  </read_first>
  <action>
    Create `tests/builder/builder-edit-stage.spec.ts` as a source-contract suite (import `fs`/`path`, REPO_ROOT = resolve __dirname '..' '..', NO chromium). Add these assertions, each reading source from disk:
    (E3) `src/lib/builder/puck-config.tsx` matches both `components:` and `outline:` returning an empty fragment `<></>` inside the createPuckOverrides return — write the test to expect these to be PRESENT (will fail RED now, pass after Plan 02).
    (E4) `src/lib/builder/puck-config.tsx` contains `contentEditable: true` (fails RED now, passes after Plan 02).
    (E5) `src/components/admin/builder/BuilderTreeRail.tsx` exists and contains `reorderSections` (fails RED now, passes after Plan 03).
    (E6) `src/components/admin/builder/BuilderTreeRail.tsx` contains `Reference images`, AND no non-comment line contains the literal `Unanchored figures` unless that same line also contains `startsWith` (the display-substitution guard) — fails RED now, passes after Plan 03.
    (E7) `src/app/api/sops/[sopId]/publish/route.ts` still contains `unverified_blocks` (passes GREEN now — regression tripwire that must STAY green through Plans 02-05).
    (E3-one-list) `src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx` contains `leftSideBarVisible: false` and `rightSideBarVisible: false` (fails RED now, passes after Plan 05).
    Then create `tests/lint/no-raw-block-types-in-build.spec.ts`: copy no-bulk-verify-ui.spec.ts structure verbatim; set the walk root to `src/components/admin/builder/`; BANNED = user-visible string literals/JSX-text containing raw PascalCase `*Block` names (e.g. `StepBlock`, `PhotoGridBlock`) and the bare tokens `'PhotoGrid'`/`'Grid'`; ALLOWLIST `src/lib/builder/block-type-labels.ts` and treat object KEYS / type identifiers (imports, `item.type ===` comparisons, `STEP_TYPES` set members) as non-violations (only flag strings that would render to the user). Description: `'E2: no raw block-type identifiers rendered in Build stage'`. Use `grep -v '^#'`-style comment filtering in JS (filter out lines starting with `//` or `*`) before counting. The directory may not exist yet at phase head — guard with an existence check so the test passes vacuously until Plan 03 creates the dir, then traps leaks.
    Add the D-21-09 isolation header comment line to neither test (tests are not under src/).
  </action>
  <verify>
    <automated>npx playwright test --list tests/builder/builder-edit-stage.spec.ts tests/lint/no-raw-block-types-in-build.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `tests/builder/builder-edit-stage.spec.ts` and `tests/lint/no-raw-block-types-in-build.spec.ts` both exist and import `fs`/`path` (no `page` fixture, no chromium).
    - BEHAVIOR: the E7 `unverified_blocks` assertion passes GREEN immediately (tripwire); the E3/E4/E5/E6/E3-one-list assertions are present and fail RED at phase head (production code not yet written).
    - TEST: `npx playwright test --list tests/builder/builder-edit-stage.spec.ts tests/lint/no-raw-block-types-in-build.spec.ts` lists both files without a config/collection error.
  </acceptance_criteria>
  <done>Both spec files exist, are syntactically valid (listable), and encode the E2–E7 contract; the E7 tripwire is GREEN and the not-yet-built assertions are RED.</done>
</task>

<task type="auto">
  <name>Task 2: Register the phase21.6-stubs playwright project</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts lines 96-111 (the phase21.5-unit + phase21.5-stubs project blocks — copy the phase21.5-stubs shape exactly: testDir '.', testMatch regex)
    - CLAUDE.md learning 2026-05-25 (lint-guard specs must be in a project regex or they NEVER run; validate with --list --project)
  </read_first>
  <action>
    Add a new project object to the `projects` array in `playwright.config.ts`, immediately after the `phase21.5-stubs` block (before the closing `]`). Name it `phase21.6-stubs`; set `testDir: '.'`; set `testMatch: /(no-raw-block-types-in-build|builder-edit-stage)\.(test|spec)\.ts$/`. Add a one-line comment citing CLAUDE.md 2026-05-25 (a spec not in a project regex never runs). Do NOT modify any existing project regex.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase21.6-stubs</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `playwright.config.ts` contains a project named `phase21.6-stubs` with `testMatch` matching both `no-raw-block-types-in-build` and `builder-edit-stage`.
    - BEHAVIOR: `npx playwright test --list --project=phase21.6-stubs` lists BOTH new spec files (≥2 specs discovered).
    - TEST: the existing `phase21.5-stubs` and `phase21.5-unit` projects still list their original specs (no regression to other regexes).
  </acceptance_criteria>
  <done>`phase21.6-stubs` is registered; `--list --project=phase21.6-stubs` shows both new specs; existing projects unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan creates only test specs + a playwright project; no runtime data path, route, or auth change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.6-01-01 | Tampering | New `tests/lint/*` spec never runs (silent gate loss) | mitigate | Task 2 registers `phase21.6-stubs`; Task 2 `<verify>` runs `--list --project` to prove discovery (CLAUDE.md 2026-05-25) |
| T-21.6-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH: zero new packages); no legitimacy gate required |
</threat_model>

<verification>
- `npx playwright test --list --project=phase21.6-stubs` discovers both specs.
- E7 `unverified_blocks` tripwire is GREEN.
- `npx tsc --noEmit` clean (test files type-check).
</verification>

<success_criteria>
- phase21.6-stubs project registered and discovering both specs.
- Source-contract + lint guards encode the E2–E7 invariants; downstream plans flip them GREEN.
</success_criteria>

<output>
Create `.planning/phases/21.6-builder-edit-stage-redesign/21.6-01-SUMMARY.md` when done.
</output>
