# Phase 21.6: Builder Edit Stage Redesign — Discussion Log

**Date:** 2026-06-05
**Mode:** discuss (default), SPEC.md loaded (7 requirements locked)

> Human-reference record of the discussion. Not consumed by downstream agents — they read CONTEXT.md + SPEC.md.

## Areas selected for discussion

All four offered gray areas were selected: Sections + Steps layout, Non-step sections, Add flow, Inline editing surface.

## Decisions

| Area | Options presented | Selected | Notes |
|------|-------------------|----------|-------|
| Sections + Steps layout | Nested tree (one rail) / Two columns / Steps-only + switcher | **Nested tree, one rail** | Section ▸ Steps; sections keep drag-reorder; canvas gets remaining width; satisfies E5 in one rail (D-01) |
| Non-step sections | Each block = own row / Ungrouped 'Section items' / Synthesise pseudo-steps | **Each block = its own humanised row** | No fake "Step N"; matches flat data + SPEC section-start rule (D-02) |
| Add step / block flow | One '＋ Add' menu + library / Add menu only / Library wizard primary | **One '＋ Add' menu, keep library option** | Humanised type menu + "From library…" (Phase 13 picker); inserts after selected step / section end (D-03) |
| Inline editing surface | Click-to-edit + popover / Click-to-edit + slim rail / Always-editable + popover | **Click-to-edit + block-anchored popover** | No always-on contentEditable (safety); slim-rail fallback allowed for complex blocks; existing autosave path (D-04) |

## Carried forward (not re-asked)

- WHAT/WHY locked by `21.6-SPEC.md` (E1–E7) — discussion was HOW only.
- Reconfigure-Puck-in-place, flat `layout_data`, step-presentation grouping, AI photo work → 21.7 — all set during spec phase.
- Visual system from `sketch-findings-SOPstart` (paper/ink, JetBrains Mono, 6 semantic pills) — carried, not re-litigated.

## Deferred ideas

- **Phase 21.7 (proposed):** AI photo-anchoring (zero unanchored photos / AI omission reasons). Split out of 21.6 during spec.
- Real `layout_data` nesting of blocks under steps — rejected to preserve flat schema + publish gate.

## Scope-creep redirects

None — discussion stayed within the SPEC boundary.

---

*Phase: 21.6-builder-edit-stage-redesign*
