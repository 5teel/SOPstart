---
phase: 21.6-builder-edit-stage-redesign
plan: 02
type: execute
wave: 1
depends_on: ["21.6-01"]
files_modified:
  - src/lib/builder/puck-config.tsx
autonomous: true
requirements: [E3, E4, E7]
must_haves:
  truths:
    - "Puck's native component palette and outline are suppressed via overrides"
    - "Text-bearing block fields render as inline-editable on the canvas"
    - "AI flag overlays + update badges + library three-dot menu still fire (no E7 regression)"
  artifacts:
    - path: "src/lib/builder/puck-config.tsx"
      provides: "createPuckOverrides with components+outline null-renderers AND retained componentItem+componentOverlay; contentEditable:true on text/textarea fields"
      contains: "contentEditable: true"
  key_links:
    - from: "src/lib/builder/puck-config.tsx (createPuckOverrides)"
      to: "Puck left palette + outline panels"
      via: "components: () => <></> and outline: () => <></>"
      pattern: "(components|outline):\\s*\\(\\)\\s*=>\\s*<>"
    - from: "text/textarea field definitions"
      to: "Puck InlineTextField on canvas → replace dispatch → onChange → useBuilderAutosave"
      via: "contentEditable: true field flag"
      pattern: "contentEditable:\\s*true"
---

<objective>
Reconfigure `puck-config.tsx` in place to (a) suppress Puck's native palette + outline so only one persistent list survives (E3), and (b) turn on native inline canvas text editing for text-bearing fields (E4) — while preserving the existing `componentItem` (library save) and `componentOverlay` (AI flags + update badges) overrides intact (E7). This is the core Puck-engine reconfigure; no new files.

Purpose: Per RESEARCH, Puck 0.21.2 has both capabilities natively — `overrides.components`/`overrides.outline` returning `<></>` removes the panels, and `contentEditable: true` on `text`/`textarea` fields renders the native `InlineTextField` (click/hover-to-edit) that dispatches a `replace` through the existing `onChange` → autosave path. Implements D-04 inline behaviour and E3's "one list" without re-architecting the engine.
Output: modified `src/lib/builder/puck-config.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-SPEC.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-RESEARCH.md
@.planning/phases/21.6-builder-edit-stage-redesign/21.6-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates/modifies:
- Modified `createPuckOverrides` in `src/lib/builder/puck-config.tsx`: NEW override keys `components` (→ `<></>`) and `outline` (→ `<></>`); EXISTING `componentItem` + `componentOverlay` retained unchanged.
- `contentEditable: true` added to the flat string `text`/`textarea` fields of: StepBlock, StepWithPhotosBlock, HeadingBlock, TextBlock, CalloutBlock, HazardCardBlock, ZoneBlock, EscalateBlock, VoiceNoteBlock, SignOffBlock (per RESEARCH inline-edit table).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Suppress palette + outline; preserve componentItem + componentOverlay</name>
  <files>src/lib/builder/puck-config.tsx</files>
  <read_first>
    - src/lib/builder/puck-config.tsx (the `createPuckOverrides` factory, ~lines 874-1015 per CONTEXT — read the full return object: `componentItem` = BlockOverflowMenu library save; `componentOverlay` = UpdateAvailableBadge + ReviewerFlagsPanel + SelectionSyncTap)
    - 21.6-RESEARCH.md § Pattern 1 + Q2 + § "Full createPuckOverrides Extension" (the exact two keys to add and the two to keep)
    - 21.6-PATTERNS.md § `src/lib/builder/puck-config.tsx (modify ...)`
  </read_first>
  <action>
    In the object returned by `createPuckOverrides()` in `src/lib/builder/puck-config.tsx`, ADD two keys: `components` returning an empty React fragment, and `outline` returning an empty React fragment (both as `(): ReactElement => <></>`). Leave `componentItem` and `componentOverlay` exactly as they are — do NOT delete, rename, or reorder them (componentItem drives the Phase 13 BlockOverflowMenu three-dot library save; componentOverlay drives the 13-04 UpdateAvailableBadge + Phase 21 ReviewerFlagsPanel + SelectionSyncTap — both are E7-critical). Ensure `ReactElement` is imported (it is used elsewhere in the file; reuse the existing react type import). Add a one-line comment on each new key noting it suppresses the palette/outline for E3. Do NOT touch `parsed-sop-to-layout-data.ts` or any block render function in this task.
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs -g "E3"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `createPuckOverrides` return object contains `components: () => <></>` and `outline: () => <></>`, AND still contains `componentItem` and `componentOverlay` keys.
    - BEHAVIOR: the Wave-0 E3 source-contract assertion (`components`/`outline` null-renderers present) flips from RED to GREEN; the E7 `componentOverlay` retention is structurally intact (key still present).
    - TEST: `npx playwright test --project=phase21.6-stubs -g "E3"` passes; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Palette + outline suppressed via two new override keys; componentItem + componentOverlay untouched; E3 contract assertion GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable inline canvas editing on text-bearing fields</name>
  <files>src/lib/builder/puck-config.tsx</files>
  <behavior>
    - Each listed block's flat string `text`/`textarea`/title-style field carries `contentEditable: true`.
    - No `select`, `radio`, `number`, or `array` field gains `contentEditable` (structured fields stay panel/popover only).
    - `junctionId` and all sibling props survive an inline edit (Puck `InlineTextField` uses `setDeep` on the single flat prop path — only flat top-level string props are targeted, never nested arrays).
  </behavior>
  <read_first>
    - src/lib/builder/puck-config.tsx (the `fields` definition for each of the 16 block components — confirm field NAMES and field TYPES before editing)
    - 21.6-RESEARCH.md § Q1 table "Blocks where contentEditable: true should be added" + § "Blocks where fields stay panel/popover only" + Pitfall 3 (only flat string props)
    - 21.6-RESEARCH.md § Pattern 2 (InlineTextField behaviour: hover→plaintext-only, dispatch replace → onChange)
  </read_first>
  <action>
    Add `contentEditable: true` to ONLY these flat string `text`/`textarea` fields in their existing `fields` definitions (do not change field `type`, `label`, or render): StepBlock.text, StepWithPhotosBlock.text, HeadingBlock.text, TextBlock.content, CalloutBlock.title + CalloutBlock.body, HazardCardBlock.title + HazardCardBlock.body, ZoneBlock.label + ZoneBlock.notes, EscalateBlock.title + EscalateBlock.reason, VoiceNoteBlock.prompt, SignOffBlock.title + SignOffBlock.acknowledgementText. For MeasurementBlock, DecisionBlock, PPECardBlock, InspectBlock you MAY add `contentEditable: true` ONLY to their single flat title/label/question field (MeasurementBlock.label, DecisionBlock.question, PPECardBlock.title, InspectBlock.title) and MUST leave their `array`/`select` fields (options, items, unit, layout, photos) untouched. Do NOT add `contentEditable` to any `number` field (e.g. StepBlock.number) or any `select`/`radio`/`array` field — per RESEARCH anti-pattern, InlineTextField only applies to text/textarea and would otherwise risk the setDeep nested-prop bug (Pitfall 3). Do NOT add `contentEditable` to nested array item fields. The block render functions stay unchanged — Puck swaps in InlineTextField automatically from the field flag.
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.6-stubs -g "E4"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `contentEditable: true` appears on the StepBlock text field and every other listed flat string field; it appears on NO `number`/`select`/`radio`/`array` field (grep the file: every `contentEditable: true` is on a line whose nearest field type is `text` or `textarea`).
    - BEHAVIOR: the Wave-0 E4 assertion (`contentEditable: true` present in puck-config) flips RED→GREEN; junctionId-bearing props are not on any contentEditable field path (only flat string props targeted).
    - TEST: `npx playwright test --project=phase21.6-stubs -g "E4"` passes; `npx tsc --noEmit` clean; existing `phase21.5-unit` (block-type-labels) still GREEN.
  </acceptance_criteria>
  <done>Inline editing enabled on exactly the flat string text fields; structured fields untouched; E4 contract GREEN; no type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → Puck canvas (client) | Inline text edits enter via Puck `InlineTextField`; no NEW boundary — edits flow through the existing `onChange` → autosave path already trusted in Phase 12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.6-02-01 | Tampering | Inline edit drops `junctionId` (breaks publish gate + 13-04 badges) | mitigate | `contentEditable: true` restricted to flat top-level string props only; Puck `InlineTextField` uses `setDeep` preserving siblings (RESEARCH Pitfall 3); Wave-0 E7 tripwire + componentOverlay retention guard it |
| T-21.6-02-02 | Information Disclosure | Suppressing palette/outline exposes hidden block-type internals | accept | Admin-only route (D-21-09 isolation); no PII; overrides only hide UI panels, no data path change |
| T-21.6-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npx playwright test --project=phase21.6-stubs` — E3 + E4 GREEN, E7 tripwire stays GREEN.
- `npx tsc --noEmit` clean.
- componentItem + componentOverlay keys still present in createPuckOverrides (grep).
</verification>

<success_criteria>
- One persistent list path enabled (native palette/outline suppressed) and inline canvas editing live, with AI overlays + library menu preserved.
</success_criteria>

<output>
Create `.planning/phases/21.6-builder-edit-stage-redesign/21.6-02-SUMMARY.md` when done.
</output>
