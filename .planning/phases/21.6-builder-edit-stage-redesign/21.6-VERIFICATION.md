---
phase: 21.6-builder-edit-stage-redesign
verified: 2026-06-05T00:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open an SOP in the Build stage on sopstart.com. Confirm the left rail shows a SECTIONS heading, section rows, and for the active section an ordered Step 1 / Step 2 list with nested block rows underneath."
    expected: "No PascalCase block names visible anywhere in the rail or canvas headers. Numbered steps visible. Non-step blocks indented under parent step."
    why_human: "Visual rendering and real layout_data with steps required; source-contract tests cover structure but not runtime rendering."
  - test: "Click '＋ Add step or block' in the rail. Confirm the AddMenu appears at centre-left of canvas with grouped humanised labels (STEPS / ANNOTATIONS / SAFETY / STRUCTURED). Click a type and confirm a block is inserted on the canvas."
    expected: "Menu opens on click, block inserted, menu closes."
    why_human: "Puck dispatch at runtime; insertAfterStepIndex anchor behaviour only verifiable live."
  - test: "Click a StepBlock or TextBlock on the canvas. Confirm the field text is directly editable inline (contentEditable experience). Type a change, navigate away, return — confirm the change persisted."
    expected: "Inline edit saves via autosave; change visible after reload."
    why_human: "Autosave Dexie→Supabase round-trip requires live session."
  - test: "Click a MeasurementBlock or DecisionBlock on the canvas. Confirm the StructuredFieldPopover opens anchored beneath the block with humanised title, and editing a field value persists."
    expected: "Popover visible with correct label; Escape dismisses; field change saved."
    why_human: "Popover anchor positioning (getBoundingClientRect) and Puck field threading require runtime."
  - test: "Confirm a section with orphan images (PhotoGridBlock titled 'Unanchored figures …') renders the rail row as 'Reference images', NOT 'Unanchored figures'. Canvas chip on the same block should also show 'Reference images'."
    expected: "Both rail label and canvas chip show the humanised label."
    why_human: "Requires an SOP with a parsed orphan-photo block in layout_data."
  - test: "Reorder sections by drag in the rail. Confirm sort_order updates (page reload shows new order). Navigate away and back — confirm order is stable."
    expected: "Optimistic reorder commits to server; stable after reload."
    why_human: "Drag-and-drop and reorderSections server action require live session."
  - test: "Attempt to publish a draft SOP with unverified blocks. Confirm the server returns 400 with unverified_blocks and the UI surfaces an error."
    expected: "Publish gate unchanged; 400 returned; no regression."
    why_human: "Full publish flow including unverified blocks requires live data and session."
---

# Phase 21.6: Builder Edit Stage Redesign — Verification Report

**Phase Goal:** A first-time admin can edit SOP content in the Build stage without internal jargon or redundant panels — one block list (not separate palette + outline), humanized block labels, content editing in the central canvas rather than a cramped side rail.
**Verified:** 2026-06-05
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                               | Status     | Evidence                                                                                                      |
|----|-----------------------------------------------------------------------------------------------------|------------|---------------------------------------------------------------------------------------------------------------|
| E1 | Build stage presents ordered humanised Steps as spine; non-step blocks nest under nearest step      | ✓ VERIFIED | `deriveStepTree()` in BuilderTreeRail implements D-01/D-02 logic; stepNumber counter; preFlight nesting       |
| E2 | No raw PascalCase block names rendered anywhere in Build stage                                      | ✓ VERIFIED | `no-raw-block-types-in-build.spec.ts` PASSES (7/7); all labels route through `humanizeBlockType`             |
| E3 | One persistent content list; `+` Add affordance exists and is wired                                 | ✓ VERIFIED | `components:()=><></>` + `outline:()=><></>` in `createPuckOverrides`; `leftSideBarVisible: false`; `rightSideBarVisible: false`; `onOpenAddMenu?.()` in button onClick; `onOpenAddMenu={() => setAddMenuOpen(true)}` passed from BuilderClient |
| E4 | Text-bearing blocks have `contentEditable: true` on text/textarea fields                           | ✓ VERIFIED | 17 occurrences of `contentEditable: true` in puck-config.tsx across TextBlock, HeadingBlock, StepBlock, CalloutBlock, HazardCardBlock, etc. |
| E5 | Section rail clearly labelled, imports `reorderSections`, drag-reorder implemented                  | ✓ VERIFIED | `BuilderTreeRail.tsx` exists; imports `reorderSections`; `SECTIONS` heading in JSX; `commitReorder` + drag handlers present |
| E6 | Orphan photos render as "Reference images"; `displayLabel` prop wired to `TreeBlockRow`             | ✓ VERIFIED | `getDisplayLabel()` returns `'Reference images'` for Unanchored-figures HeadingBlocks; `displayLabel` passed to both flat and step-child `<TreeBlockRow>`; `componentOverlay` in puck-config.tsx also guards E6 for canvas chip |
| E7 | Publish gate (`unverified_blocks` 400), autosave, junctionId, AI overlay unchanged                 | ✓ VERIFIED | `builder-edit-stage.spec.ts` E7 test PASSES; `createPuckOverrides` componentItem + componentOverlay intact; `closeStructuredPopover` uses `useCallback`; no autosave import in StructuredFieldPopover |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact                                                                              | Expected                                        | Status     | Details                                                          |
|---------------------------------------------------------------------------------------|-------------------------------------------------|------------|------------------------------------------------------------------|
| `src/components/admin/builder/BuilderTreeRail.tsx`                                    | Step-centric tree rail with section reorder     | ✓ VERIFIED | 437 lines; all props wired including `onOpenAddMenu`             |
| `src/components/admin/builder/TreeBlockRow.tsx`                                       | Nested block row with `displayLabel` prop       | ✓ VERIFIED | `displayLabel?: string` accepted; used as `preview` override     |
| `src/components/admin/builder/TreeStepRow.tsx`                                        | Step row with stepNumber                        | ✓ VERIFIED | Imported and rendered in BuilderTreeRail                         |
| `src/components/admin/builder/TreeSectionRow.tsx`                                     | Section row with drag handles                   | ✓ VERIFIED | Imported and rendered in BuilderTreeRail                         |
| `src/components/admin/builder/AddMenu.tsx`                                            | Humanised grouped add menu with Puck dispatch   | ✓ VERIFIED | `useGetPuck` dispatch; all labels via `humanizeBlockType`        |
| `src/components/admin/builder/StructuredFieldPopover.tsx`                             | Anchored popover for structured fields          | ✓ VERIFIED | `aria-modal="true"` present; `useCallback`-stabilised `onClose` passed from BuilderClient |
| `src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx`                   | Mounts BuilderTreeRail + AddMenu + suppresses Puck sidebars | ✓ VERIFIED | All wiring confirmed; `addMenuOpen` state; `setAddMenuOpen(true)` passed as `onOpenAddMenu` |
| `src/lib/builder/puck-config.tsx`                                                     | `contentEditable: true` fields; null-renderers; E6 chip | ✓ VERIFIED | 17 contentEditable fields; `components:()=><></>`; `outline:()=><></>`; componentOverlay E6 guard |
| `tests/builder/builder-edit-stage.spec.ts`                                            | Source-contract assertions for E3–E7            | ✓ VERIFIED | 6 tests, all PASS                                                |
| `tests/lint/no-raw-block-types-in-build.spec.ts`                                     | E2 lint guard                                   | ✓ VERIFIED | 1 test, PASSES; registered in `phase21.6-stubs` project          |

---

### Key Link Verification

| From                           | To                                    | Via                                        | Status     | Details                                                                       |
|--------------------------------|---------------------------------------|--------------------------------------------|------------|-------------------------------------------------------------------------------|
| BuilderClient                  | BuilderTreeRail                       | `onOpenAddMenu={() => setAddMenuOpen(true)}`| ✓ WIRED   | Line 484 of BuilderClient.tsx                                                 |
| BuilderTreeRail add-control    | AddMenu open state                    | `onClick: onOpenAddMenu?.()`               | ✓ WIRED   | Line 393 of BuilderTreeRail.tsx                                               |
| BuilderClient                  | AddMenu                               | `addMenuOpen && <AddMenu ...>`             | ✓ WIRED   | Lines 524–556 of BuilderClient.tsx                                            |
| AddMenu                        | Puck dispatch                         | `useGetPuck().dispatch({type:'insert',...})`| ✓ WIRED  | AddMenu.tsx lines 96–105                                                      |
| BuilderTreeRail flat nodes     | TreeBlockRow displayLabel             | `displayLabel={getDisplayLabel(node.item)}`| ✓ WIRED  | Both flat branch (line 348) and step-child branch (line 373)                  |
| BuilderClient                  | StructuredFieldPopover                | `selectedBlockId && selectedBlockType`     | ✓ WIRED   | Lines 510–517; `onClose={closeStructuredPopover}` (useCallback-stable)       |
| puck-config createPuckOverrides | Puck sidebar suppression              | `components:()=><></>` + `outline:()=><>`  | ✓ WIRED  | Lines 924–926 of puck-config.tsx                                              |
| BuilderClient Puck             | Sidebar suppression                   | `ui={{ leftSideBarVisible: false, rightSideBarVisible: false }}` | ✓ WIRED | Line 499 of BuilderClient.tsx |

---

### Behavioral Spot-Checks (source-contract, no server needed)

| Behavior                                    | Command                                                                   | Result      | Status  |
|---------------------------------------------|---------------------------------------------------------------------------|-------------|---------|
| E2 lint: no raw block names in builder dir  | `npx playwright test --project=phase21.6-stubs`                           | 7/7 passed  | ✓ PASS  |
| E3 null-renderers present                   | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |
| E4 contentEditable fields                   | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |
| E5 BuilderTreeRail imports reorderSections  | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |
| E6 Reference images label / no leak        | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |
| E7 publish gate tripwire                    | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |
| E3-one-list sidebar suppression             | spec assertion in builder-edit-stage.spec.ts                              | PASS        | ✓ PASS  |

---

### Anti-Patterns Found

| File                                | Pattern               | Severity  | Impact                                                                                          |
|-------------------------------------|-----------------------|-----------|-------------------------------------------------------------------------------------------------|
| `BuilderTreeRail.tsx` (line 189–198)| Dead `insertBlock` fn | INFO      | `insertBlock` in rail is dead code (AddMenu has its own dispatch). No runtime impact; cleanup deferred. |
| Multiple builder files              | `getPillStyle` × 4    | INFO      | Verbatim in `TreeStepRow`, `TreeBlockRow`, `BuilderTreeRail`, `AddMenu`. Extraction to `pill-style.ts` deferred; no correctness issue. |
| `StructuredFieldPopover.tsx`        | Children slot empty   | INFO      | `children` prop accepted but BuilderClient never injects Puck field inputs. **Intentional documented stub** — field-input threading is post-phase scope per SPEC boundaries. |

No `TBD`, `FIXME`, or `XXX` markers found in phase-modified files.

---

### Notes on REVIEW.md Critical Issue (CR-01)

The REVIEW.md flagged CR-01 ("AddMenu permanently unreachable") as a critical issue. **This was resolved before or during the final commit.** The current source shows:

- `BuilderTreeRail` Props interface includes `onOpenAddMenu?: () => void` (line 59)
- The add-control `onClick` calls `onOpenAddMenu?.()` (line 393) — NOT a no-op
- `BuilderClient` passes `onOpenAddMenu={() => setAddMenuOpen(true)}` (line 484)

CR-01 is closed. REVIEW.md warnings WR-02 (order stale-prop) and WR-03 (onClose identity) were addressed: `useCallback` wrapping is present in BuilderClient (`closeStructuredPopover`). WR-04 (`aria-modal`) is also resolved — `aria-modal="true"` is present in StructuredFieldPopover (line 99).

---

### Human Verification Required

#### 1. Step-centric rail renders correctly with real SOP data

**Test:** Open a parsed SOP in the Build stage on sopstart.com. Inspect the left rail for a numbered Step 1, Step 2… list with nested block rows indented beneath each step.
**Expected:** Numbered steps visible in source order; non-step blocks indented under parent step; no PascalCase block names anywhere visible.
**Why human:** Runtime rendering with real `layout_data` required; deriveStepTree logic verified structurally but not with live data.

#### 2. Add affordance opens AddMenu and inserts a block

**Test:** Click "＋ Add step or block" in the rail. Confirm the AddMenu appears with grouped humanised labels. Click a block type.
**Expected:** Menu opens; block inserted on canvas; menu closes; no raw PascalCase visible.
**Why human:** Puck dispatch at runtime; `insertAfterStepIndex` anchor behaviour.

#### 3. Inline text editing and autosave

**Test:** Click a StepBlock or TextBlock on the canvas. Edit the text inline. Navigate away and back.
**Expected:** `contentEditable` fields allow inline edit; change persists via autosave.
**Why human:** Autosave Dexie→Supabase round-trip requires live session.

#### 4. StructuredFieldPopover opens and edits persist

**Test:** Click a MeasurementBlock or DecisionBlock on the canvas.
**Expected:** Popover anchors beneath block with humanised title; Escape dismisses; field edit saves.
**Why human:** Anchor positioning (`getBoundingClientRect`) and Puck field child threading require runtime. Note: `children` slot is currently empty (intentional stub) — structured field inputs will not appear yet, only the popover chrome.

#### 5. E6 "Reference images" label visible for orphan-photo blocks

**Test:** Open an SOP containing parsed orphan images. Check the rail row and canvas chip for the block.
**Expected:** Rail shows "Reference images" (not "Unanchored figures"); canvas chip matches.
**Why human:** Requires an SOP with an orphan-photo `HeadingBlock` in `layout_data`.

#### 6. Section drag-reorder persists

**Test:** Drag a section row to a new position. Reload the page.
**Expected:** New order persists; optimistic update commits to server.
**Why human:** Drag-and-drop and `reorderSections` server action require live session.

#### 7. Publish gate regression check

**Test:** Attempt to publish a draft SOP with unverified blocks.
**Expected:** Server returns 400 with `unverified_blocks`; UI surfaces error; no regression.
**Why human:** Full publish flow with unverified blocks requires live data.

---

### Gaps Summary

No blockers. All 7/7 source-contract truths verified. The REVIEW.md critical issue (CR-01) was resolved in final code. Human verification items are standard live-UAT checks for runtime behaviour (autosave round-trip, drag-drop, Puck dispatch) that cannot be verified statically.

One intentional documented stub: `StructuredFieldPopover` children slot is empty (field-input threading is post-phase scope per SPEC § Boundaries). This is expected and not a gap.

---

_Verified: 2026-06-05_
_Verifier: Claude (gsd-verifier)_
