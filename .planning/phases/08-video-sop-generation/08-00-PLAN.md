---
phase: 08-video-sop-generation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/video-gen-slideshow.test.ts
  - tests/video-gen-scroll.test.ts
  - tests/video-chapters.test.ts
  - tests/video-admin-preview.test.ts
  - tests/video-player.test.ts
  - tests/video-completion.test.ts
  - tests/sw-video-exclusion.test.ts
  - playwright.config.ts
autonomous: true
requirements:
  - VGEN-01
  - VGEN-02
  - VGEN-03
  - VGEN-04
  - VGEN-05
  - VGEN-06
  - VGEN-07
  - VGEN-08
  - VGEN-09
  - INFRA-03

must_haves:
  truths:
    - "All Phase 8 requirement IDs have at least one test.fixme stub"
    - "phase8-stubs Playwright project runs and discovers all stub files"
    - "VGEN-03 stub is marked DEFERRED per D-01"
  artifacts:
    - path: "tests/video-gen-slideshow.test.ts"
      provides: "VGEN-01 + VGEN-06 stubs"
      contains: "VGEN-01"
    - path: "tests/video-gen-scroll.test.ts"
      provides: "VGEN-02 stub"
      contains: "VGEN-02"
    - path: "tests/video-chapters.test.ts"
      provides: "VGEN-04 stub"
      contains: "VGEN-04"
    - path: "tests/video-admin-preview.test.ts"
      provides: "VGEN-05 stub"
      contains: "VGEN-05"
    - path: "tests/video-player.test.ts"
      provides: "VGEN-07 + VGEN-08 stubs"
      contains: "VGEN-07"
    - path: "tests/video-completion.test.ts"
      provides: "VGEN-09 stub"
      contains: "VGEN-09"
    - path: "tests/sw-video-exclusion.test.ts"
      provides: "INFRA-03 stub"
      contains: "INFRA-03"
    - path: "playwright.config.ts"
      provides: "phase8-stubs project"
      contains: "phase8-stubs"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/video-*.test.ts"
      via: "testMatch regex"
      pattern: "video-gen|video-player|video-chapters|video-admin|video-completion|sw-video"
---

<objective>
Wave 0: Create Playwright test stubs for all Phase 8 requirements and add the phase8-stubs project to playwright.config.ts.

Purpose: Establish the test inventory before any implementation begins, following the established Wave 0 pattern from Phases 2, 3, and 6.
Output: 7 test files with test.fixme stubs covering VGEN-01 through VGEN-09 and INFRA-03. VGEN-03 is marked DEFERRED.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-video-sop-generation/08-CONTEXT.md
@.planning/phases/08-video-sop-generation/08-RESEARCH.md

<interfaces>
<!-- Existing test stub patterns from prior phases -->

From playwright.config.ts:
```typescript
// Existing project entries pattern:
{
  name: 'phase6-stubs',
  testMatch: /video-upload|youtube-url|youtube-no-captions|stage-progress|transcript-review|publish-gate|safety-warning/,
},
```

<!-- Phase 8 requirement IDs to cover -->
VGEN-01: Admin can generate a narrated slideshow video
VGEN-02: Admin can generate a screen-recording-style video
VGEN-03: DEFERRED — full AI video with avatar (D-01)
VGEN-04: Generated videos include chapter markers
VGEN-05: Admin can preview generated video and re-generate
VGEN-06: Video generation shows async progress with named stages
VGEN-07: Workers can access video from SOP view
VGEN-08: Video player supports chapter navigation, timestamp jumps, speed control
VGEN-09: Worker video viewing tracked as completion event
INFRA-03: Generated videos excluded from SW caching
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase8-stubs project to playwright.config.ts and create all 7 test stub files</name>
  <files>
    playwright.config.ts,
    tests/video-gen-slideshow.test.ts,
    tests/video-gen-scroll.test.ts,
    tests/video-chapters.test.ts,
    tests/video-admin-preview.test.ts,
    tests/video-player.test.ts,
    tests/video-completion.test.ts,
    tests/sw-video-exclusion.test.ts
  </files>
  <read_first>
    playwright.config.ts,
    tests/video-upload.test.ts,
    tests/stage-progress.test.ts
  </read_first>
  <action>
1. Add a `phase8-stubs` project entry to `playwright.config.ts` after the existing `phase6-stubs` entry:
```typescript
{
  name: 'phase8-stubs',
  testMatch: /video-gen-slideshow|video-gen-scroll|video-chapters|video-admin-preview|video-player|video-completion|sw-video-exclusion/,
},
```

2. Create 7 test stub files following the established pattern from Phase 6 stubs (test.fixme with requirement ID prefix in name). Each file uses the Playwright test import and marks tests as `test.fixme`:

**tests/video-gen-slideshow.test.ts** — 2 stubs:
- `VGEN-01: Admin can trigger narrated slideshow generation from a published SOP`
- `VGEN-06: Video generation progress shows named stages (analyzing, generating slides, adding narration, rendering, ready)`

**tests/video-gen-scroll.test.ts** — 1 stub:
- `VGEN-02: Admin can trigger screen-recording-style video generation from a published SOP`

**tests/video-chapters.test.ts** — 1 stub:
- `VGEN-04: Generated video includes chapter markers per SOP section with timestamps for direct navigation`

**tests/video-admin-preview.test.ts** — 2 stubs:
- `VGEN-05: Admin can preview generated video and re-generate before publishing`
- `VGEN-03: [DEFERRED per D-01] Admin can generate a full AI video with avatar — validate demand with standard formats first`

**tests/video-player.test.ts** — 2 stubs:
- `VGEN-07: Workers see Video tab in SOP detail when a published generated video exists`
- `VGEN-08: Video player supports chapter navigation, timestamp jumps, and playback speed control`

**tests/video-completion.test.ts** — 1 stub:
- `VGEN-09: Worker video viewing at 80% duration creates a video_view completion record`

**tests/sw-video-exclusion.test.ts** — 1 stub:
- `INFRA-03: Generated video URLs are excluded from service worker CacheFirst handler`

Each stub file follows this pattern:
```typescript
import { test } from '@playwright/test'

test.fixme('VGEN-XX: description', async ({ page }) => {
  // Phase 8: Video SOP Generation
})
```

The VGEN-03 stub must include a comment: `// DEFERRED per D-01 — full AI video deferred to future phase. Not implemented in Phase 8.`
  </action>
  <verify>
    <automated>npx playwright test --project phase8-stubs --list 2>&1 | grep -c "test.fixme"</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts contains `name: 'phase8-stubs'`
    - playwright.config.ts testMatch regex matches all 7 test file names
    - tests/video-gen-slideshow.test.ts contains "VGEN-01" and "VGEN-06"
    - tests/video-gen-scroll.test.ts contains "VGEN-02"
    - tests/video-chapters.test.ts contains "VGEN-04"
    - tests/video-admin-preview.test.ts contains "VGEN-05" and "VGEN-03"
    - tests/video-player.test.ts contains "VGEN-07" and "VGEN-08"
    - tests/video-completion.test.ts contains "VGEN-09"
    - tests/sw-video-exclusion.test.ts contains "INFRA-03"
    - VGEN-03 stub contains "DEFERRED per D-01"
    - `npx playwright test --project phase8-stubs --list` discovers 10 test stubs
  </acceptance_criteria>
  <done>All 10 test.fixme stubs across 7 files discovered by phase8-stubs Playwright project. VGEN-03 marked DEFERRED.</done>
</task>

</tasks>

<verification>
- `npx playwright test --project phase8-stubs --list` shows 10 test entries
- Each requirement ID (VGEN-01 through VGEN-09, INFRA-03) appears in at least one test name
- VGEN-03 stub has DEFERRED comment
</verification>

<success_criteria>
- phase8-stubs project added to playwright.config.ts
- 7 test files created with 10 total test.fixme stubs
- All Phase 8 requirement IDs covered
- VGEN-03 correctly flagged as deferred
</success_criteria>

<output>
After completion, create `.planning/phases/08-video-sop-generation/08-00-SUMMARY.md`
</output>
