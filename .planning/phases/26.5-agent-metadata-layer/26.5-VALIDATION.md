---
phase: 26.5
slug: agent-metadata-layer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-04
---

# Phase 26.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright 1.x (project-scoped test suites) |
| **Config file** | `playwright.config.ts` (Wave 0 adds a `phase26.5` project regex) |
| **Quick run command** | `npx playwright test --project=phase26.5` |
| **Full suite command** | `npx playwright test --project=phase26.5 && npx tsc --noEmit` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx playwright test --project=phase26.5`
- **After every plan wave:** Run `npx playwright test --project=phase26.5 && npx tsc --noEmit`
- **Before `/gsd-verify-work`:** Full suite green AND real `npm run build` green (CLAUDE.md 2026-06-27 — tsc is necessary, not sufficient)
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

*To be filled by the planner from the Validation Architecture section of 26.5-RESEARCH.md.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| — | — | — | D-XX | — | — | — | — | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `playwright.config.ts` — register `phase26.5` project regex (CLAUDE.md 2026-05-25: unregistered specs never run; validate with `npx playwright test --list --project=phase26.5`)
- [ ] `tests/phase26.5/` — stub specs for migration assertions, synthesis-job unit surface, proposal-evidence contract
- [ ] Migration assertion script must distinguish PGRST205 (schema-cache stale) from 42P01 (table absent) — CLAUDE.md 2026-06-15

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `⚇ Agent layer` toggle look/feel vs sketch | D-09 | Visual design parity on sopstart.com | Open admin builder post-deploy, toggle agent view, compare to `sketches/sop-builder-redesign/index.html` agentview |
| VOYAGE_API_KEY live on Railway | D-03 | Env provisioning outside repo | Railway dashboard → service variables → confirm `VOYAGE_API_KEY` before deploy (Deepgram precedent) |
| Backfill run against live DB | D-15 | One-off production job | Run backfill script, spot-check embeddings/tags rows for a known SOP |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
