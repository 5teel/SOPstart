---
phase: 26.5-agent-metadata-layer
plan: 7
type: execute
wave: 5
depends_on: ["26.5-04"]
files_modified:
  - src/actions/agent-layer.ts
  - src/components/admin/builder-v2/agent/AgentPanel.tsx
  - src/components/admin/builder-v2/agent/AgentBlockMeta.tsx
  - src/components/admin/builder-v2/agent/AgentBanner.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx
  - src/styles/blueprint-theme.css
  - tests/phase26.5/agent-panel-readonly.spec.ts
autonomous: true
requirements: [D-02, D-09, D-10, D-12]

must_haves:
  truths:
    - "Admin/safety-manager-only server actions read SOP + per-block agent metadata and approve/decline proposals, all org-scoped (D-09/D-10)"
    - "The builder has an agentview toggle that reveals a purple SOP-level agent panel + per-block metadata rows over the same canvas (D-09)"
    - "Per-block metadata rows are keyed by the sop_section_blocks junction id (D-02)"
    - "The agent panel is read-only — no onChange/editable input on any tag/entity/embedding display (D-10)"
    - "Server actions read organisation_id via parseJwtPayload, never atob"
  artifacts:
    - path: "src/actions/agent-layer.ts"
      provides: "requireAdmin-guarded read + approve/decline server actions (D-09/D-10)"
      contains: "parseJwtPayload"
    - path: "src/components/admin/builder-v2/agent/AgentPanel.tsx"
      provides: "SOP-level purple agent panel (read-only, D-09/D-10)"
      contains: "agentpanel"
    - path: "src/components/admin/builder-v2/agent/AgentBlockMeta.tsx"
      provides: "Per-block metadata rows keyed by junction id (D-02)"
      contains: "junctionId"
  key_links:
    - from: "BuilderClient agentview toggle"
      to: "AgentPanel + AgentBlockMeta"
      via: "agentview class shows/hides the purple layer"
      pattern: "agentview"
    - from: "src/actions/agent-layer.ts approveProposal"
      to: "src/lib/ai-fields/agent-proposals.ts"
      via: "delegates to the org-scoped lifecycle fn"
      pattern: "approveProposal"
---

<objective>
Surface the machine layer in the builder (D-09 surface 1): admin-only server actions to read SOP + per-block metadata and act on proposals, plus the `⚇ agentview` toggle that reveals the purple JetBrains-Mono agent panel + per-block metadata rows over the same canvas — strictly read-only (D-10).

Purpose: This is one of the two D-09 surfaces. The sketch already validated the CSS (`.agentpanel`, `.ameta`, `.agentbanner`, `body.agentview`, `--ai` purple) — it is a direct lift. D-10 forbids hand-editing machine metadata (it regenerates on publish), so the panel renders tags/entities/embeddings as static chips/spans with no edit handler — and the readonly spec asserts the ABSENCE of a handler, not just a class name (CLAUDE.md 2026-06-05). D-02 requires per-block rows keyed by junction id.

Output: `src/actions/agent-layer.ts`, 3 agent components, agentview toggle in BuilderClient, `--ai` token + agent CSS, live readonly spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/actions/departments.ts
@src/lib/supabase/jwt.ts
@sketches/sop-builder-redesign/index.html
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- File `src/actions/agent-layer.ts` — exports `getSopAgentMetadata`, `getBlockAgentMetadata`, `getAgentDashboardData`, `approveProposalAction`, `declineProposalAction` (all `'use server'` async)
- Files `AgentPanel.tsx`, `AgentBlockMeta.tsx`, `AgentBanner.tsx` under `src/components/admin/builder-v2/agent/`
- `agentview` boolean toggle + `⚇` control in `BuilderClient.tsx`
- CSS token `--ai` + classes `.agentpanel`, `.ameta`, `.agentbanner`, `body.agentview` (or a wrapper-scoped equivalent) in `src/styles/blueprint-theme.css`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Admin-only agent-layer server actions (D-09/D-10)</name>
  <files>src/actions/agent-layer.ts</files>
  <read_first>
    - src/actions/departments.ts (the `requireAdmin()` helper shape lines 33-57 + `createAdminClient()` self-enforced org-scope in the assign* actions — copy the shape; BUT see the deviation below)
    - src/lib/supabase/jwt.ts (parseJwtPayload — USE THIS instead of the `atob` in departments.ts requireAdmin; CLAUDE.md 2026-06-26)
    - src/lib/ai-fields/agent-proposals.ts (approveProposal, declineProposal from Plan 04 — delegate to these)
    - supabase/migrations/00040_agent_metadata_schema.sql (the tables to read)
  </read_first>
  <action>
    Create `src/actions/agent-layer.ts` (`'use server'` — EVERY export MUST be an async function; no sync/pure exports, CLAUDE.md 2026-06-27). Add a local `requireAdmin()` modelled on departments.ts BUT decode the JWT with `parseJwtPayload(session.access_token)` (NOT `atob`), reading `user_role` + `organisation_id`; reject non-`admin`/`safety_manager`. Export:
    - `getSopAgentMetadata(sopId)` — org-scoped read of `sop_agent_metadata` for the caller's org (filter `.eq('organisation_id', ctx.organisationId)` explicitly even though RLS also gates it).
    - `getBlockAgentMetadata(sopId)` — org-scoped read of `block_agent_metadata` rows keyed by junction id for the SOP (D-02).
    - `getAgentDashboardData()` — pending proposals + recent memory/refresh activity for the org (consumed by Plan 08's dashboard).
    - `approveProposalAction(proposalId)` / `declineProposalAction(proposalId)` — verify admin ctx, then delegate to `approveProposal(ctx.organisationId, proposalId)` / `declineProposal(...)` from agent-proposals.ts (org self-enforced there and here). Return the discriminated `{ data } | { error }` union, never throw (departments.ts convention).
    All functions read `organisation_id` from JWT claims only, never from client input.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "actions/agent-layer" || echo "agent-layer actions typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `agent-layer.ts` uses `parseJwtPayload` and contains no `atob(` call
    - Every export is an async function (no sync/pure value export in the `'use server'` file)
    - `approveProposalAction`/`declineProposalAction` delegate to agent-proposals lifecycle fns and check admin ctx first
    - Reads filter `organisation_id` explicitly; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Admin-only read + proposal-action server actions exist, org-scoped, JWT-safe.</done>
</task>

<task type="auto">
  <name>Task 2: agentview toggle + purple agent panel/block-meta/banner (read-only) (D-02/D-09/D-10/D-12)</name>
  <files>src/components/admin/builder-v2/agent/AgentPanel.tsx, src/components/admin/builder-v2/agent/AgentBlockMeta.tsx, src/components/admin/builder-v2/agent/AgentBanner.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx, src/styles/blueprint-theme.css, tests/phase26.5/agent-panel-readonly.spec.ts</files>
  <read_first>
    - sketches/sop-builder-redesign/index.html (lines 188-211 the `.agentpanel`/`.ameta`/`.agentbanner`/`body.agentview` CSS — verbatim lift; lines 617-651 the `AMETA` field taxonomy: `id · type`, tags chips, entities, vector embedded+confidence, links cross-SOP+block-library, agent read/write+memory/review attach point — use as the literal per-row field list)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx (the header + `<main>` mount; add the `⚇ agentview` toggle button in the header cluster next to the SAVED pill / RerunReviewerButton, and render `<AgentPanel>` at the top of `<main>` gated by the toggle)
    - src/styles/blueprint-theme.css (the `:root` token block ~lines 30-49 where `--paper`/`--ink-*`/`--accent-*` live — add `--ai: #8b5cf6` here + the agent CSS classes)
    - src/actions/agent-layer.ts (getSopAgentMetadata, getBlockAgentMetadata from Task 1)
  </read_first>
  <action>
    Add `--ai: #8b5cf6` to the `:root` block in `blueprint-theme.css` and the sketch's `.agentpanel` / `.ameta` / `.agentbanner` classes plus an `agentview`-scoped show/hide rule (lift the CSS verbatim; scope the toggle via a class on the builder root wrapper rather than global `body` if simpler, but keep the sketch selectors). Ensure JetBrains Mono is applied to these classes (add the font import/family if not already present).

    Create three components under `src/components/admin/builder-v2/agent/`:
    - `AgentPanel.tsx` — SOP-level panel: summary, tags (chips), entities, embedding presence + assessment badge (fresh/drifting/needs-review, D-12), cross-SOP links. Fetches via `getSopAgentMetadata(sopId)`.
    - `AgentBlockMeta.tsx` — per-block `.ameta` rows keyed by `junctionId` (the sop_section_blocks id, D-02), rendering the sketch's AMETA field taxonomy. Fetches via `getBlockAgentMetadata(sopId)`.
    - `AgentBanner.tsx` — the sticky `.agentbanner` shown when agentview is on.
    ALL THREE ARE READ-ONLY (D-10): render tags/entities/embeddings as `<span>` chips / static text — NEVER an `<input>`, `<textarea>`, `contentEditable`, or an `onChange` handler on any metadata field. Admin actions on the panel are limited to proposal approve/decline (calling `approveProposalAction`/`declineProposalAction`) — those are the only interactive affordances.

    In `BuilderClient.tsx`: add an `agentview` boolean `useState` + a `⚇ Agent layer` toggle control in the header; when on, apply the `agentview` class to the builder root wrapper and render `<AgentBanner>` + `<AgentPanel sopId=...>` (and per-block `<AgentBlockMeta>` either injected into the block list keyed by junctionId, or grouped in the panel — executor's discretion based on how invasive the EditableDocument loop is; keep it read-only either way). Workers never reach this route (admin builder) so no worker-gating needed here.

    Make `tests/phase26.5/agent-panel-readonly.spec.ts` LIVE and BEHAVIOURAL (not just source-contract): render `AgentPanel`/`AgentBlockMeta` (shell out via a tsx react-dom/server harness if Playwright's JSX transform can't render the real components, per the phase26 render-harness precedent) and assert the output contains NO editable control (`<input`, `<textarea`, `contenteditable`, `onChange` on a metadata field) — assert the ABSENCE of an edit handler, per CLAUDE.md 2026-06-05. Also assert the `agentview` toggle in BuilderClient references the state setter (the handler is WIRED, not an empty onClick — the 21.6 dead-affordance trap).
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/agent-panel-readonly.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `blueprint-theme.css` defines `--ai: #8b5cf6` and the `.agentpanel`/`.ameta`/`.agentbanner` classes with a JetBrains Mono family
    - `AgentPanel`/`AgentBlockMeta`/`AgentBanner` render with ZERO `<input>`/`<textarea>`/`contentEditable`/`onChange` on any metadata field (readonly spec asserts absence)
    - `AgentBlockMeta` keys rows by `junctionId` (D-02)
    - BuilderClient's `⚇` toggle calls the `agentview` state setter (wired, not empty)
    - `agent-panel-readonly.spec.ts` green; `npx tsc --noEmit` clean; `npm run build` unaffected for the worker route (admin-only surface)
  </acceptance_criteria>
  <done>agentview toggle reveals the read-only purple agent layer over the canvas; per-block rows keyed by junction id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → agent-layer server actions | Only admin/safety_manager may read metadata / act on proposals |
| admin panel → machine metadata | Read-only — no edit path that could be clobbered or abused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-07-01 | Elevation of Privilege | agent-layer server actions | mitigate | `requireAdmin()` gates every action to admin/safety_manager; org read from JWT via parseJwtPayload, never client input |
| T-26.5-07-02 | Tampering | JWT org claim decode | mitigate | parseJwtPayload, never atob (CLAUDE.md 2026-06-26) |
| T-26.5-07-03 | Tampering | hand-edit of machine metadata | mitigate | Panel is read-only (D-10) — no edit handler exists; readonly spec asserts absence (would be clobbered on next publish anyway) |
| T-26.5-07-04 | Information Disclosure | cross-org proposal approve/decline | mitigate | Delegates to agent-proposals lifecycle which `.eq('organisation_id')` on the write |
</threat_model>

<verification>
- agent-panel-readonly.spec.ts green (behavioural: no edit handler)
- `npx tsc --noEmit` clean; worker route bundle unaffected
- agentview toggle wired to state setter
</verification>

<success_criteria>
- Admin-only read + proposal-action server actions, org-scoped, JWT-safe
- agentview toggle reveals a read-only purple agent panel + per-block (junction-keyed) rows over the canvas
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-07-SUMMARY.md` when done.
</output>
