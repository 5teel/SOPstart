---
phase: 26.5-agent-metadata-layer
verified: 2026-07-05T00:56:09Z
status: human_needed
score: 15/16 D-IDs verified (1 verified-with-noted-deviation)
overrides_applied: 0
re_verification: null
human_verification:
  - test: "Confirm VOYAGE_API_KEY + CRON_SECRET are set on Railway before/at next deploy"
    expected: "Both env vars present in Railway service variables; synthesis-sweep route returns 200 (not 503) after a real cron hit"
    why_human: "Env provisioning outside the repo — deliberate deferred gate (Simon: 'Not yet — continue anyway', 2026-07-04). Route fails closed (503) in the interim, so no silent breakage, but the layer cannot actually run until this is done."
  - test: "Run scripts/backfill-agent-metadata.mjs against the live production DB"
    expected: "Every published SOP gets a sop_agent_metadata row (last_synthesis_status='ok'), spot-checked embeddings/tags/entities on a known SOP"
    why_human: "One-off production job requiring live VOYAGE_API_KEY/ANTHROPIC_API_KEY — cannot run in this verification pass. D-15 code is correct and tested (source-contract + WR-05 fix verified below) but has zero live-DB executions yet — the layer is empty until this runs."
  - test: "Visual/design parity of the ⚇ Agent layer builder panel + /admin/agent dashboard vs sketches/sop-builder-redesign/index.html"
    expected: "Purple --ai bordered panel, monospace key/value rows, agentview toggle look-and-feel matches the validated sketch"
    why_human: "Visual design parity on sopstart.com — cannot be verified by grep/tsc; needs a real authenticated browser session per project precedent (2026-06-08 learning)."
  - test: "Decide whether D-07's 'reuse gateWrite, no parallel approval machinery' directive is satisfied by the sibling agent_learning_proposals table + its own createLearningProposal/approveProposal/declineProposal lifecycle (does NOT call gateWrite or write to ai_field_proposals, and approving a proposal never calls descriptor.write() — it only flips a status column, per D-13)"
    expected: "Human confirms this is an acceptable, intentional divergence from the literal D-07 wording (the outcome — evidence-backed, pending, admin-reviewed, no-auto-apply — is achieved; the mechanism is a new sibling state machine, not the reused gateWrite function) OR requests a follow-up to route proposals through the X-03 approval spine"
    why_human: "This is a locked-decision (D-07) wording conflict, not a functionality gap — the dashboard/panel approve/decline genuinely work end-to-end (tested, live). Flagging because 'no parallel approval machinery' is explicit in CONTEXT.md and the phase's own code-review pass (26.5-REVIEW.md) did not surface this architectural point."
---

# Phase 26.5: Agent Metadata Layer Verification Report

**Phase Goal:** Every published SOP carries a machine-readable agent-metadata layer — semantic tags, entities, Voyage-3/pgvector embeddings, cross-SOP links, append-only memory, evidence-backed learning proposals, and a per-SOP agent assessment — populated by a real synthesis job (Haiku 4.5) reading the four org signal sources, and surfaced to admins via the `⚇ Agent layer` builder toggle + an org dashboard (proposals queue + activity feed). Infrastructure + surfacing only; no flagship agent capability this phase, but no dead UI either — backfill embeds/tags/links all currently-published SOPs.

**Verified:** 2026-07-05T00:56:09Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

This phase's requirement source is `26.5-CONTEXT.md` D-01..D-16 (no REQUIREMENTS.md mapping). Each D-ID checked directly against the codebase (not SUMMARY claims).

### Observable Truths (D-01..D-16)

| # | Truth (D-ID) | Status | Evidence |
|---|------|--------|----------|
| 1 | D-01: Dedicated org-scoped tables are source of truth; `layout_data` stays frozen | ✓ VERIFIED | 5 tables in `supabase/migrations/00040_agent_metadata_schema.sql`, all RLS SELECT via `current_organisation_id()` with zero cross-table reference (confirmed by grep + live `schema-contract.spec.ts`); `synthesis.ts` `loadPublishedSop` select omits `layout_data`/`layout_version` (grep-verified, comment reworded to avoid the literal string per plan's own test) |
| 2 | D-02: SOP + block granularity, block keyed by junction id | ✓ VERIFIED | `block_agent_metadata.block_id` FK → `sop_section_blocks.id` (unique-indexed); `AgentBlockMeta.tsx` keys rows by `row.junctionId` (behavioural spec `agent-panel-readonly.spec.ts` asserts `key={row.junctionId}`, not `block_id`/`sort_order`) |
| 3 | D-03: Voyage-3 + pgvector embeddings | ✓ VERIFIED | `voyageai@0.4.0` exact-pinned in `package.json`; `EMBED_MODEL` constant (`voyage-3.5` default, env-overridable to `voyage-3`); `create extension vector`, HNSW cosine indexes on both embedding columns (live-confirmed via `apply-phase26.5-migration.mjs`); `match_sop_agent_metadata` RPC wraps `<=>` |
| 4 | D-04: Generate/refresh on publish, not autosave | ✓ VERIFIED | `publish/route.ts` calls `triggerAgentSynthesis(sopId, organisationId)` as an additive, non-blocking step after the frozen gate (git diff scoped to import + 5 lines, confirmed); `phase26` regression suite (102/102) proves the frozen gate is untouched; no autosave code path imports `synthesis.ts` (grep-confirmed, zero hits) |
| 5 | D-05: Memory scope per-SOP + org-wide | ✓ VERIFIED | `agent_memory.scope` check constraint (`'sop'`/`'org'`), nullable `sop_id` for org-wide rows; `appendMemory()` free-append, no status/approval field |
| 6 | D-06: All four signal sources feed the layer | ✓ VERIFIED | `signals.ts` exports `readCompletionSignals`/`readReviewerSignals`/`readVerifySignals`/`readVoiceSignals`, all four independently try/catch-isolated; `/api/voice/query` now writes `sop_voice_qa_log` (previously the missing signal — closes RESEARCH Pitfall 1), verified live in `voice-qa-persistence.spec.ts` |
| 7 | D-07: Proposals reuse X-03 approval path, extended with evidence (no parallel approval machinery) | ⚠️ VERIFIED WITH DEVIATION | Outcome achieved (evidence-backed, always-pending, admin-reviewed, no auto-apply — `agent_learning_proposals.evidence jsonb`, `status check (pending/applied/rejected)`) but the **mechanism is a new sibling state machine**, not a reuse of `gateWrite`/`ai_field_proposals`/`acceptProposal`/`rejectProposal` (`src/lib/ai-fields/approval.ts`, `src/actions/ai-fields.ts`). `createLearningProposal`/`approveProposal`/`declineProposal` (`src/lib/ai-fields/agent-proposals.ts`) never call `gateWrite`, never touch `ai_field_proposals`, and `approveProposal` only flips a status column — it never calls `descriptor.write()`. This is disclosed in Plan 04's SUMMARY decisions ("sibling table... not a shared row shape") but the CONTEXT.md wording is explicit: "ride gateWrite → pending_approval → admin review (**no parallel approval machinery**)". Routed to human decision — see frontmatter. |
| 8 | D-08: Memory writes are free append-only, no gate | ✓ VERIFIED | `appendMemory` has no status/approval field; no authenticated write policy but service-role write self-enforces `organisation_id` |
| 9 | D-09: Two surfaces — builder toggle + org dashboard, admin/safety-manager only | ✓ VERIFIED | `BuilderClient.tsx` `⚇ Agent layer` toggle wired to `setAgentview` (behavioural test confirms not an empty handler — 21.6 dead-affordance trap explicitly guarded against); `/admin/agent` SSR route redirects non-admin/safety_manager (same guard shape as `/admin/departments`) |
| 10 | D-10: Read-only + proposal actions only | ✓ VERIFIED | `agent-panel-readonly.spec.ts` real `react-dom/server` render asserts zero `<input>`/`<textarea>`/`contentEditable`/`onChange` in `AgentPanel`/`AgentBlockMeta`/`AgentBanner`; only sanctioned buttons are Approve/Decline |
| 11 | D-11: Dashboard = proposals queue (primary) + activity feed (secondary), no graph viz | ✓ VERIFIED | `AgentDashboardClient.tsx` renders `pendingProposals` queue + `recentMemory` feed; `agent-dashboard.spec.ts` explicitly asserts no `react-konva`/`Konva`/`FlowGraphCanvas`/`<canvas` anywhere |
| 12 | D-12: Per-SOP agent assessment (fresh/drifting/needs-review) | ✓ VERIFIED | `deriveAssessment(bundle)` in `synthesis.ts` — real logic: critical flags or `allRunsErrored` → `needs-review`; >30% unverified or >3 warnings → `drifting`; else `fresh`. Upserted into `sop_agent_metadata.assessment` |
| 13 | D-13: Infra + surfacing only, no flagship capability | ✓ VERIFIED | No cross-SOP summarise/Q&A UI shipped; `matchSopAgentMetadata` has zero callers this phase (documented as deliberate scaffolding, IN-01); approving a proposal never edits SOP content |
| 14 | D-14: Scheduled synthesis job (not dead UI) | ✓ VERIFIED (code) / ⬜ NOT YET RUNNING (env) | `POST /api/agent-layer/synthesis-sweep` — constant-time fail-closed `CRON_SECRET` auth, 503 fail-fast on missing `VOYAGE_API_KEY`, deterministic `updated_at`-ordered candidate selection (WR-03 fix), batch-capped at 20/invocation. Code correct and tested (7/7 live specs) but **`VOYAGE_API_KEY`/`CRON_SECRET` are not yet set on Railway** — known, disclosed residual (see human_verification) |
| 15 | D-15: Backfill all currently-published SOPs | ✓ VERIFIED (code) / ⬜ NOT YET RUN (prod) | `scripts/backfill-agent-metadata.mjs` reuses the real `synthesizeSop` pipeline (no duplicate embed logic), per-org cap, and — post review-fix WR-05 — now reads back `last_synthesis_status` and reports genuine `FAIL` lines + exits non-zero on any failure (previously the FAIL branch was dead code). **Has not been run against production** — known, disclosed residual |
| 16 | D-16: Synthesis model = Claude Haiku 4.5, shared constant + env override | ✓ VERIFIED | `SYNTHESIS_MODEL = process.env.SYNTHESIS_MODEL ?? 'claude-haiku-4-5-20251001'` in `model-constants.ts`, single literal occurrence (grep-verified); `extractTagsAndEntities` in `synthesis.ts` calls `getAnthropic().messages.create({ model: SYNTHESIS_MODEL, ... })` with a real structured-JSON extraction prompt |

**Score:** 15/16 fully verified, 1 verified-with-disclosed-architectural-deviation (D-07) routed to human decision.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/00040_agent_metadata_schema.sql` | 5 tables + pgvector + HNSW + RPC | ✓ VERIFIED | Live on DB, confirmed via Management API `to_regclass`/`pg_extension`/`pg_proc` (re-ran `apply-phase26.5-migration.mjs` myself — all 8 assertions PASS) |
| `supabase/migrations/00041_match_rpc_lockdown.sql` | CR-01 fix: revoke EXECUTE from client roles | ✓ VERIFIED | Live on DB, confirmed via a fresh `has_function_privilege` query I ran directly: `anon=false, authenticated=false, service_role=true` |
| `src/lib/agent-layer/synthesis.ts` | Full synthesis pipeline | ✓ VERIFIED | Real Voyage embed + real Haiku tag/entity extraction + 4 signal reads + memory/proposal/assessment writes, all org-scoped, all independently error-isolated |
| `src/lib/agent-layer/signals.ts` | 4 signal readers | ✓ VERIFIED | All 4 present, org-scoped, try/catch isolated; WR-04 fix confirmed (`allRunsErrored` requires non-empty `job_status`) |
| `src/lib/ai-fields/agent-proposals.ts` | Proposal lifecycle + memory append | ✓ VERIFIED (see D-07 note) | WR-02 dedupe fix confirmed live in code |
| `src/app/api/sops/[sopId]/publish/route.ts` | Additive synthesis hook, frozen gate untouched | ✓ VERIFIED | CR-02 fix confirmed (`parseJwtPayload`, not `atob`); phase26 regression suite 102/102 green |
| `src/app/api/agent-layer/synthesis-sweep/route.ts` | Authed cron sweep | ✓ VERIFIED | WR-03 fix confirmed (`.order('updated_at')`, error-path `regenerated_at` stamp) |
| `scripts/backfill-agent-metadata.mjs` | D-15 backfill | ✓ VERIFIED (code), ⬜ not run in prod | WR-05 fix confirmed (`sopsFailed` tracked, `process.exit(1)` on failures) |
| `src/components/admin/builder-v2/agent/{AgentPanel,AgentBlockMeta,AgentBanner}.tsx` | Read-only purple panel | ✓ VERIFIED | Real `react-dom/server` render proves no edit affordances |
| `src/app/(protected)/admin/sops/builder/[sopId]/BuilderClient.tsx` | agentview toggle wired | ✓ VERIFIED | WR-01 + WR-06 fixes confirmed (await+check result before removing row; `.catch` + `agentError` state added) |
| `src/app/(protected)/admin/agent/{page.tsx,AgentDashboardClient.tsx}` | Org dashboard | ✓ VERIFIED | SSR admin/safety_manager guard; approve/decline genuinely remove rows on success |
| `src/lib/journeys/journeys.ts` + `src/lib/uat/tests.ts` | Pathways map updated | ✓ VERIFIED | `agent-layer` Journey with route `/admin/agent`; `agent-layer-dashboard` UAT entry present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| Publish route | `triggerAgentSynthesis` | direct call, non-blocking | ✓ WIRED | Confirmed in code + `synthesis-pipeline.spec.ts` D-04 test |
| `synthesizeSop` | Voyage API | `getVoyageClient().embed(...)` | ✓ WIRED | Real API call with `EMBED_MODEL`; best-effort try/catch |
| `synthesizeSop` | Anthropic API | `getAnthropic().messages.create(...)` | ✓ WIRED | Real API call with `SYNTHESIS_MODEL`, JSON-only prompt |
| `synthesizeSop` | `sop_agent_metadata`/`block_agent_metadata`/`agent_memory`/`agent_learning_proposals` | `createAdminClient()` upserts/inserts | ✓ WIRED | All org-scoped; live migration confirms table shapes |
| `BuilderClient` toggle | agent data fetch | `useEffect` gated on `agentview`, calls 3 server actions | ✓ WIRED | Confirmed via behavioural test + manual read |
| `AgentDashboardClient` Approve/Decline | `approveProposalAction`/`declineProposalAction` | `useTransition` + local filter on success | ✓ WIRED | Confirmed row genuinely removed only on `{success:true}` |
| Sweep route | `synthesizeSop` | serial awaited loop, batch-capped | ✓ WIRED | Confirmed with deterministic ordering fix |

### Behavioral Spot-Checks / Test Suite Execution (run live by verifier, not taken from SUMMARY)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase 26.5 full spec suite | `npx playwright test --project=phase26.5` | 44 passed, 6 skipped (deliberate `test.fixme` seeded-DB/live-key deferrals) | ✓ PASS |
| Phase 26 frozen-spine regression | `npx playwright test --project=phase26` | 102 passed, 0 failed | ✓ PASS |
| Full typecheck | `npx tsc --noEmit` | zero errors | ✓ PASS |
| Production build | `npm run build` | clean; `/admin/agent` + `/api/agent-layer/synthesis-sweep` present in output; bundle-size/isolation postbuild checks all pass (`/sops/[sopId]/page` Δ0 KB) | ✓ PASS |
| Migration + RPC live-DB state | `node scripts/apply-phase26.5-migration.mjs` (re-run by verifier) | ALL POST-APPLY ASSERTIONS PASSED (5 tables, extension, RPC, no-42P17) | ✓ PASS |
| CR-01 RPC lockdown live-DB state | Fresh `has_function_privilege` query via Management API (run by verifier, not reused from REVIEW-FIX) | `anon=false, authenticated=false, service_role=true` | ✓ PASS |

### Requirements Coverage (D-IDs, see CONTEXT.md — no REQUIREMENTS.md mapping)

See Observable Truths table above — all 16 D-IDs individually checked against code. 15/16 fully verified; D-07 flagged for human decision (architectural wording conflict, not a functional gap).

### Anti-Patterns Found

None outstanding. 8/8 Critical+Warning findings from `26.5-REVIEW.md` are confirmed FIXED in the actual codebase (not just claimed in `26.5-REVIEW-FIX.md`) — CR-01 (RPC lockdown, live-DB verified independently), CR-02 (`parseJwtPayload` swap), WR-01 (await+check before row removal), WR-02 (pending-proposal dedupe), WR-03 (deterministic sweep ordering + error-path timestamp), WR-04 (`allRunsErrored` empty-array guard), WR-05 (backfill FAIL branch no longer dead code), WR-06 (fetch-chain `.catch` + error surfacing) — all independently re-read in source by the verifier. 6 Info-level findings (IN-01..IN-06) deliberately left unfixed per the fix pass's own scope decision (out of Critical/Warning scope); none of them block the phase goal. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any phase-touched file.

### Human Verification Required

See YAML frontmatter `human_verification` — 4 items:
1. Confirm `VOYAGE_API_KEY` + `CRON_SECRET` on Railway (known deferred gate, carries to pre-deploy checklist).
2. Run the backfill script against production (needs the same key).
3. Visual/design parity check of the agentview panel + dashboard on sopstart.com.
4. Decide whether the D-07 "sibling proposal table, not gateWrite reuse" architecture is an acceptable deviation from the locked CONTEXT.md decision, or should be routed to a follow-up plan.

### Gaps Summary

No functional gaps. All artifacts exist, are substantive (not stubs), are wired end-to-end, and the tests (44/44 phase26.5, 102/102 phase26 regression, tsc clean, build clean) were re-run directly by the verifier rather than taken on SUMMARY/REVIEW-FIX claim — all reproduced independently, including two live-DB checks (migration assertions, RPC EXECUTE-privilege query) run fresh by the verifier rather than reused from the fix report. The phase does not ship dead UI: the panel and dashboard render real (though currently backfill-empty, pending the deferred key) data paths, and the synthesis pipeline makes real Voyage/Anthropic calls with real DB writes.

The only items keeping status at `human_needed` rather than `passed`:
- Two disclosed, deliberately-deferred production-readiness residuals (Railway keys, backfill run) that this phase's own plans flagged as carried-forward — not new findings.
- One newly-surfaced architectural note (D-07 mechanism vs. wording) that does not break functionality but deviates from the literal locked-decision text and deserves an explicit accept/reject from the developer rather than a silent pass.
- Visual parity of the sketch-matched UI is unverifiable by this pass (established project precedent for admin surfaces).

---

_Verified: 2026-07-05T00:56:09Z_
_Verifier: Claude (gsd-verifier)_
