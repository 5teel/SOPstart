---
phase: 26.5-agent-metadata-layer
plan: 6
type: execute
wave: 5
depends_on: ["26.5-04"]
files_modified:
  - src/app/api/agent-layer/synthesis-sweep/route.ts
  - tests/phase26.5/synthesis-sweep-auth.spec.ts
autonomous: false
requirements: [D-14]

must_haves:
  truths:
    - "A scheduled sweep route runs synthesis for orgs/SOPs with new signals since the last run (D-14)"
    - "The sweep route rejects unauthenticated requests (no valid shared secret -> 401) — it is NOT session-cookie gated (D-14)"
    - "Every write the sweep triggers self-enforces org-scope"
    - "VOYAGE_API_KEY is confirmed present on Railway before the first deploy of this phase (Deepgram precedent)"
  artifacts:
    - path: "src/app/api/agent-layer/synthesis-sweep/route.ts"
      provides: "Cron-invoked authed catch-up sweep (D-14)"
      contains: "synthesizeSop"
  key_links:
    - from: "Railway Cron"
      to: "src/app/api/agent-layer/synthesis-sweep/route.ts"
      via: "authenticated request with shared bearer secret"
      pattern: "CRON_SECRET|authorization"
---

<objective>
Deliver the scheduled synthesis catch-up (D-14) as an authed internal route a Railway Cron job hits, sweeping orgs/SOPs whose signals changed since publish (completions, voice Q&A, reviewer flags accumulate between publishes). Gate the first deploy on VOYAGE_API_KEY being live on Railway.

Purpose: The publish hook (Plan 05) only covers publish events; completions/voice/reviewer signals accrue continuously, so without a periodic sweep the assessment + memory would go stale. Railway Cron (separate one-shot process) is the right mechanism — an in-process setInterval would violate the single-process/no-PM2-cluster constraint this codebase already documents. The route must NOT use the session-cookie pattern (no user) — it authenticates a shared secret. VOYAGE_API_KEY has no fallback and has bitten this project twice (Deepgram precedent), so a human confirms it before deploy.

Output: `src/app/api/agent-layer/synthesis-sweep/route.ts`, live sweep-auth spec, Railway key confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/lib/agent-layer/synthesis.ts
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- File `src/app/api/agent-layer/synthesis-sweep/route.ts`
- Env var `CRON_SECRET` (shared secret for the sweep route)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [GATE] Confirm VOYAGE_API_KEY on Railway before deploy (D-03/D-14)</name>
  <read_first>
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Environment Availability (VOYAGE_API_KEY blocking, no fallback) + Open Question 1 (Deepgram-key precedent — twice-shipped-broken)
    - .planning/phases/26.5-agent-metadata-layer/26.5-VALIDATION.md § Manual-Only Verifications
  </read_first>
  <action>
    Do not deploy this phase until the human confirms `VOYAGE_API_KEY` is set on the Railway SOPstart service. Pause here and ask the human to check the Railway service Variables (steps below). This is the one blocking env var with no fallback — the exact Deepgram-key failure mode that shipped broken twice. Resume only on "approved".
  </action>
  <what-built>
    Nothing to build here — this gate confirms the one blocking env var this phase needs at runtime. The synthesis pipeline (Plan 04) and this sweep both call Voyage; without `VOYAGE_API_KEY` on Railway, publish "succeeds" and the whole layer silently ships broken (the exact Deepgram-key failure mode, twice before).
  </what-built>
  <how-to-verify>
    1. Railway dashboard → the SOPstart service → Variables.
    2. Confirm `VOYAGE_API_KEY` is present and non-empty (add it from the Voyage AI dashboard if missing).
    3. Optionally confirm `CRON_SECRET` is set (needed by this route's auth) — or note it will be added alongside the Railway Cron job config.
  </how-to-verify>
  <acceptance_criteria>
    - `VOYAGE_API_KEY` visible in Railway service Variables (non-empty)
    - Simon confirms with "approved" (or names the blocker)
  </acceptance_criteria>
  <resume-signal>Type "approved" once VOYAGE_API_KEY is set on Railway, or describe what's missing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Authed cron-invoked synthesis-sweep route (D-14)</name>
  <files>src/app/api/agent-layer/synthesis-sweep/route.ts, tests/phase26.5/synthesis-sweep-auth.spec.ts</files>
  <read_first>
    - .planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md § No Analog Found (synthesis-sweep — base auth on a shared secret/bearer header, NOT the session-cookie pattern; RESEARCH's "unauthenticated -> 401" test spec)
    - src/lib/agent-layer/synthesis.ts (synthesizeSop / triggerAgentSynthesis)
    - src/lib/agent-layer/signals.ts (to find orgs/SOPs with new signals since last synthesis)
    - src/app/api/sops/[sopId]/publish/route.ts (auth + org-resolution shape, for reference only — do NOT copy the cookie auth for a machine route)
  </read_first>
  <action>
    Create `src/app/api/agent-layer/synthesis-sweep/route.ts` (POST). Auth: read the `authorization` header (or a custom header) and compare against `process.env.CRON_SECRET` using a length-safe constant-time comparison; missing/mismatched → 401 immediately (no session/cookie auth — this is a machine endpoint). If `CRON_SECRET` is unset, fail closed (401), not open.

    Body of the sweep: find SOPs whose signals changed since their `sop_agent_metadata.regenerated_at` (e.g. newer completions / voice Q&A rows / reviewer flags), bounded to a max batch per invocation (Railway Cron min interval is 5 min — keep each run short, one-shot). For each affected SOP, run `synthesizeSop(sopId, organisationId)` (awaited within the batch cap). Every write self-enforces org-scope (inherited from synthesizeSop). Return `{ processed, skipped }`. Add a comment: deploy as a Railway Cron job (one-shot process hitting this route with the CRON_SECRET) — respects the single-process deploy; NOT an in-process setInterval (RESEARCH Anti-Patterns / voice-query concurrency-cap comment).

    Make `tests/phase26.5/synthesis-sweep-auth.spec.ts` LIVE via source-contract: assert the route returns 401 when the secret is absent/wrong (grep for the `CRON_SECRET` compare + a 401 path), fails closed when `CRON_SECRET` is unset, and that it does NOT authenticate via `supabase.auth.getUser()` cookie session. Assert the sweep calls `synthesizeSop`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/synthesis-sweep-auth.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The route returns 401 on missing/invalid secret and fails closed when `CRON_SECRET` is unset
    - Auth uses a constant-time secret compare, NOT `supabase.auth.getUser()` cookie session
    - The sweep calls `synthesizeSop` with a per-invocation batch cap
    - `synthesis-sweep-auth.spec.ts` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Authed cron sweep route processes stale-signal SOPs, fails closed, org-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → sweep route | A machine endpoint with no user session — must authenticate a shared secret |
| sweep → agent tables | Service-role writes must stay org-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-06-01 | Elevation of Privilege / Spoofing | synthesis-sweep route auth | mitigate | Constant-time `CRON_SECRET` bearer compare; fail closed if unset; no cookie-session path (unauthenticated → 401) |
| T-26.5-06-02 | Information Disclosure | sweep service-role writes | mitigate | synthesizeSop self-enforces org-scope per SOP |
| T-26.5-06-03 | Denial of Service | unbounded sweep work / double-fire | mitigate | Per-invocation batch cap; one-shot Railway Cron process (no in-process interval that double-fires across deploys) |
| T-26.5-06-04 | Availability (missing key) | VOYAGE_API_KEY absent on Railway | mitigate | Blocking human checkpoint before deploy (Deepgram precedent) |
</threat_model>

<verification>
- synthesis-sweep-auth.spec.ts green
- `npx tsc --noEmit` clean
- VOYAGE_API_KEY confirmed on Railway (checkpoint)
</verification>

<success_criteria>
- Authed cron sweep route processes stale-signal SOPs, fails closed on bad/absent secret
- Every sweep write is org-scoped
- VOYAGE_API_KEY confirmed live before deploy
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-06-SUMMARY.md` when done.
</output>
