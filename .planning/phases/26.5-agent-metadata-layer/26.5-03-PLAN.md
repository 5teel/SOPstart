---
phase: 26.5-agent-metadata-layer
plan: 3
type: execute
wave: 3
depends_on: ["26.5-02"]
files_modified:
  - src/app/api/voice/query/route.ts
  - src/lib/agent-layer/signals.ts
  - src/lib/agent-layer/similarity.ts
  - tests/phase26.5/voice-qa-persistence.spec.ts
  - tests/phase26.5/signal-readers.spec.ts
autonomous: true
requirements: [D-03, D-05, D-06]

must_haves:
  truths:
    - "POST /api/voice/query writes an append-only transcript row (question, answer, citations, sopId, orgId) after answering (D-06 signal #3, Pitfall 1)"
    - "signals.ts reads all four sources: completions+step_ack_trace, ai_review_results, block verify send-back state, voice Q&A log (D-06)"
    - "similarity.ts wraps match_sop_agent_metadata via .rpc() — never a raw .select() with a vector operator (D-03, Pitfall 3)"
    - "The voice write uses createAdminClient with org-scope set explicitly on insert (append-only table has no auth write policy)"
  artifacts:
    - path: "src/lib/agent-layer/signals.ts"
      provides: "Four signal-source readers returning structured signal rows (D-06)"
      contains: "sop_completions"
    - path: "src/lib/agent-layer/similarity.ts"
      provides: "RPC wrapper for cross-SOP vector search (D-03)"
      contains: "match_sop_agent_metadata"
    - path: "src/app/api/voice/query/route.ts"
      provides: "Voice Q&A transcript persistence (D-06 signal #3)"
      contains: "sop_voice_qa_log"
  key_links:
    - from: "src/app/api/voice/query/route.ts"
      to: "sop_voice_qa_log"
      via: "createAdminClient insert after answerSopQuestion"
      pattern: "sop_voice_qa_log"
    - from: "src/lib/agent-layer/similarity.ts"
      to: "match_sop_agent_metadata RPC"
      via: "supabase.rpc"
      pattern: "\\.rpc\\('match_sop_agent_metadata'"
---

<objective>
Make the four D-06 signal sources readable — and fix the one that does not exist yet. The voice Q&A route is stateless today (Pitfall 1): add an append-only transcript write so signal source #3 is real. Then build `signals.ts` (four readers) and `similarity.ts` (the vector-search RPC wrapper) so Plan 04's synthesis pipeline has clean inputs.

Purpose: D-06 lists four signals; three are pure reads of existing tables, but voice Q&A transcripts are never persisted. Without the write path the synthesis job would forever see zero voice rows — the same "zero-rows = never-collected, not no-questions" failure the research flags. This plan closes that gap and centralises signal reads.

Output: modified `/api/voice/query/route.ts`, `src/lib/agent-layer/signals.ts`, `src/lib/agent-layer/similarity.ts`, live persistence + signal-reader specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/app/api/voice/query/route.ts
@src/lib/supabase/admin.ts
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- File `src/lib/agent-layer/signals.ts` — exports the four signal-reader functions (completions, reviewer flags, verify state, voice Q&A)
- File `src/lib/agent-layer/similarity.ts` — exports `matchSopAgentMetadata` (RPC wrapper)
- New write step in `src/app/api/voice/query/route.ts` (insert into `sop_voice_qa_log`)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Voice Q&A transcript write path (D-06 signal #3, Pitfall 1)</name>
  <files>src/app/api/voice/query/route.ts, tests/phase26.5/voice-qa-persistence.spec.ts</files>
  <read_first>
    - src/app/api/voice/query/route.ts (the existing stateless route — the answer is computed at line ~100 `answerSopQuestion(...)` and returned directly; add the write AFTER the result is computed, before/parallel to the response)
    - supabase/migrations/00040_agent_metadata_schema.sql (the `sop_voice_qa_log` columns from Plan 02)
    - src/lib/supabase/admin.ts (createAdminClient)
    - src/lib/supabase/jwt.ts (parseJwtPayload — use this to read organisation_id from the session token, NEVER atob)
  </read_first>
  <action>
    In `/api/voice/query/route.ts`, after `answerSopQuestion(...)` returns `result`, insert one append-only row into `sop_voice_qa_log` using `createAdminClient()`: `{ organisation_id, sop_id: sopId, user_id: user.id, question, answer: result.answer, citations: result.citations }`. Resolve `organisation_id` from the session access token via `parseJwtPayload(session.access_token)['organisation_id']` (the route already has the session for auth) — do NOT use `atob` (CLAUDE.md 2026-06-26). The table has no authenticated write policy, so the admin client is required; set `organisation_id` explicitly on insert (self-enforced org-scope, CLAUDE.md 2026-06-15). Wrap the insert in its own try/catch and `console.error` on failure with a greppable prefix — a failed log write must NEVER change the 200 response the worker receives (do not await-block the answer, or await it but swallow errors). The org read from the sop row is also acceptable (`sop.organisation_id` is already selected) — prefer the sop row's `organisation_id` since it is already fetched and RLS-verified, and it avoids a JWT decode; use parseJwtPayload only if you read from the token.

    Make `tests/phase26.5/voice-qa-persistence.spec.ts` LIVE: source-contract assert the route file contains an insert into `sop_voice_qa_log`, references `createAdminClient`, sets `organisation_id` on the inserted row, and does NOT use `atob(` for the org claim. (Runtime insert test is deferred to phase UAT — but assert the write is WIRED, not merely that the table name string appears, per CLAUDE.md 2026-06-05.)
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/voice-qa-persistence.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `/api/voice/query/route.ts` inserts into `sop_voice_qa_log` via `createAdminClient()` with `organisation_id` set explicitly
    - The route does NOT introduce an `atob(` call for the org claim
    - The insert cannot alter the worker's 200 answer response (errors caught + logged, not thrown)
    - `voice-qa-persistence.spec.ts` passes and asserts the wired insert (not just a string presence)
  </acceptance_criteria>
  <done>Voice Q&A transcripts persist append-only; signal source #3 now produces real rows.</done>
</task>

<task type="auto">
  <name>Task 2: Four signal readers + vector-search RPC wrapper (D-03/D-06)</name>
  <files>src/lib/agent-layer/signals.ts, src/lib/agent-layer/similarity.ts, tests/phase26.5/signal-readers.spec.ts</files>
  <read_first>
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Signal Source Read Paths + Code Examples § pgvector similarity RPC wrapper (the .rpc call shape)
    - .planning/phases/26.5-agent-metadata-layer/26.5-CONTEXT.md § Signal sources (the exact tables: sop_completions + step_ack_trace; ai_review_results; sop_section_blocks verify send-back; sop_voice_qa_log)
    - src/lib/supabase/admin.ts (createAdminClient — signal reads use service role, org-scoped explicitly)
  </read_first>
  <action>
    Create `src/lib/agent-layer/signals.ts` exporting four async readers, each taking `(organisationId, sopId)` and returning a small structured summary (counts + notable rows, not raw dumps). Use `createAdminClient()` and filter every query by `organisation_id` (or by sop scoped to org) explicitly:
    - `readCompletionSignals` — from `sop_completions` + the `step_ack_trace` jsonb (Phase 15): step durations, skipped/slow steps.
    - `readReviewerSignals` — from `ai_review_results` (via parse_jobs): reviewer flag counts + verify send-back history.
    - `readVerifySignals` — from `sop_section_blocks`: unverified / recently-sent-back junction rows for the SOP's sections.
    - `readVoiceSignals` — from `sop_voice_qa_log` (created in Task 1): recent questions asked against the SOP.
    Each reader is independently try/caught so a failing source returns an empty/neutral result and never blanks the whole synthesis run (mirrors the ai-reviewer orchestrator per-job isolation). Export a `SignalBundle` type aggregating the four.

    Create `src/lib/agent-layer/similarity.ts` exporting `matchSopAgentMetadata(organisationId, queryEmbedding, matchCount = 10)` that calls `supabase.rpc('match_sop_agent_metadata', { p_organisation_id, query_embedding, match_count })` — NEVER a `.select()` with a vector operator (PostgREST cannot express `<=>`, Pitfall 3). Return the `{ sop_id, similarity }[]`.

    Make `tests/phase26.5/signal-readers.spec.ts` LIVE: source-contract assert signals.ts exports the four readers and each references its correct table name; assert similarity.ts calls `.rpc('match_sop_agent_metadata'` and contains no raw `<=>` in a `.select(`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/signal-readers.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `signals.ts` exports `readCompletionSignals`, `readReviewerSignals`, `readVerifySignals`, `readVoiceSignals`, and `SignalBundle`
    - Each reader references its table (`sop_completions`, `ai_review_results`, `sop_section_blocks`, `sop_voice_qa_log`) and filters by org explicitly
    - `similarity.ts` uses `.rpc('match_sop_agent_metadata'` and has no vector operator inside a `.select(`
    - `signal-readers.spec.ts` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Four signal readers + similarity RPC wrapper exist and are org-scoped; Plan 04 can consume them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker request → voice log write | Untrusted question text persisted; must be org-scoped |
| service role → signal tables | Signal reads bypass RLS; must self-enforce org filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-03-01 | Information Disclosure / Elevation | voice-query insert via admin client | mitigate | `organisation_id` set explicitly from the RLS-verified sop row (or parseJwtPayload); append-only table has no auth write policy |
| T-26.5-03-02 | Tampering | JWT org claim decode | mitigate | Use `parseJwtPayload`, never `atob` (CLAUDE.md 2026-06-26) |
| T-26.5-03-03 | Information Disclosure | signal readers over service role | mitigate | Every query filters `organisation_id` explicitly (CLAUDE.md 2026-06-15) |
| T-26.5-03-04 | Denial of Service | voice log write blocking the answer | accept | Write is fire-and-forget/error-swallowed; a log failure never affects the worker's answer |
</threat_model>

<verification>
- voice-qa-persistence.spec.ts + signal-readers.spec.ts green
- `npx tsc --noEmit` clean
- Voice route still returns the same 200 answer shape (no response contract change)
</verification>

<success_criteria>
- All four D-06 signal sources are readable (voice source now real)
- Voice transcripts persist append-only, org-scoped
- Vector search wrapped via RPC (no raw operator)
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-03-SUMMARY.md` when done.
</output>
