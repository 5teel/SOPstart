---
phase: 26.5-agent-metadata-layer
plan: 5
type: execute
wave: 5
depends_on: ["26.5-04"]
files_modified:
  - src/app/api/sops/[sopId]/publish/route.ts
  - scripts/backfill-agent-metadata.mjs
  - tests/phase26.5/backfill-coverage.spec.ts
autonomous: true
requirements: [D-04, D-15]

must_haves:
  truths:
    - "Publishing an SOP fires triggerAgentSynthesis AFTER the frozen publish gate succeeds, never awaited, never affecting the response (D-04)"
    - "The publish gate logic (section-approval + verify-checklist + status flip) is byte-unchanged (26 D-01 frozen spine)"
    - "The backfill script embeds+tags+links every currently-published SOP across all orgs, one-off, with a per-org cost guardrail (D-15)"
  artifacts:
    - path: "src/app/api/sops/[sopId]/publish/route.ts"
      provides: "Additive post-publish synthesis hook (D-04)"
      contains: "triggerAgentSynthesis"
    - path: "scripts/backfill-agent-metadata.mjs"
      provides: "One-off backfill of all published SOPs (D-15)"
      contains: "synthesizeSop"
  key_links:
    - from: "src/app/api/sops/[sopId]/publish/route.ts"
      to: "triggerAgentSynthesis"
      via: "fire-and-forget after publish succeeds"
      pattern: "triggerAgentSynthesis\\(.*\\)\\.catch"
---

<objective>
Wire regeneration-on-publish (D-04) as a single additive, fire-and-forget line off the FROZEN publish gate, and write the one-off backfill script (D-15) so the layer is populated for every already-published SOP day one — no dead UI.

Purpose: D-04 requires metadata to regenerate when a version publishes; the publish route's gate logic is frozen (26 D-01 / the 2026-07-03 spine-preservation learning), so the hook must be strictly additive and non-blocking — identical shape to the existing video auto-queue step. D-15's backfill makes the dashboard/panel useful immediately instead of only after the next publish.

Output: modified publish route, `scripts/backfill-agent-metadata.mjs`, live backfill-coverage spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/app/api/sops/[sopId]/publish/route.ts
@src/lib/agent-layer/synthesis.ts
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- New fire-and-forget hook line in `src/app/api/sops/[sopId]/publish/route.ts`
- File `scripts/backfill-agent-metadata.mjs`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Additive post-publish synthesis hook (D-04)</name>
  <files>src/app/api/sops/[sopId]/publish/route.ts</files>
  <read_first>
    - src/app/api/sops/[sopId]/publish/route.ts (the FROZEN gate: steps 1-3 section-approval + verify-checklist + status flip at lines 15-119; step 4 video auto-queue at lines 121-132 is the EXACT fire-and-forget shape to replicate)
    - .planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md § publish/route.ts (the exact new line + critical constraints)
    - src/lib/agent-layer/synthesis.ts (triggerAgentSynthesis from Plan 04)
  </read_first>
  <action>
    Add ONE new step AFTER the existing publish succeeds (after the `publishError` check clears, near the video auto-queue step) in `/api/sops/[sopId]/publish/route.ts`: call `triggerAgentSynthesis(sopId, organisationId)` and attach `.catch((err) => console.error('[agent-layer] synthesis trigger failed:', err instanceof Error ? err.message : err))`. Do NOT `await` it, do NOT change the response shape or status code, do NOT touch steps 1-3 (the frozen gate) or the `enqueueVideoGenerationForPipeline` call. `organisationId` is already resolved earlier in the route. The response body stays exactly `{ success: true, pipelineAutoQueued }` — synthesis wiring must be invisible to the caller.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "publish/route" || echo "publish route typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - The route calls `triggerAgentSynthesis(sopId, organisationId)` with a `.catch` and no `await`
    - The `git diff` of the route touches ONLY the additive block (steps 1-3 + the video auto-queue call + the response object unchanged)
    - Response body remains `{ success: true, pipelineAutoQueued: ... }`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Publish additively fires synthesis, fire-and-forget, frozen gate untouched.</done>
</task>

<task type="auto">
  <name>Task 2: One-off backfill of all published SOPs (D-15)</name>
  <files>scripts/backfill-agent-metadata.mjs, tests/phase26.5/backfill-coverage.spec.ts</files>
  <read_first>
    - src/lib/agent-layer/synthesis.ts (synthesizeSop — the backfill loops this per published SOP)
    - scripts/ (an existing one-off script using SUPABASE_ACCESS_TOKEN / service-role client + .env.local, to match the invocation + env convention)
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Environment Availability (VOYAGE_API_KEY blocking; cost note at 50-500 SOPs/org)
  </read_first>
  <action>
    Create `scripts/backfill-agent-metadata.mjs`: iterate every org, select every SOP with `status='published'`, and run `synthesizeSop(sopId, organisationId)` for each — awaited sequentially (batch, not fire-and-forget), with a per-org cap/guardrail on total AI calls (log a warning + skip remainder if a per-org ceiling is hit — cost guardrail, discretion). Print a per-org progress line and a final total (`{orgs, sopsProcessed, sopsSkipped}`). Read keys from `.env.local` (VOYAGE_API_KEY, ANTHROPIC_API_KEY, SUPABASE service role). The actual production run is manual against live DB (Manual-Only Verifications) — the script only needs to be runnable and correct.

    Make `tests/phase26.5/backfill-coverage.spec.ts` LIVE via source-contract: assert the script selects `status = 'published'` (or equivalent), calls `synthesizeSop` in a loop, and enforces a per-org cap. (Row-count coverage against a seeded DB is a manual verify.)
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/backfill-coverage.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/backfill-agent-metadata.mjs` selects published SOPs, loops `synthesizeSop`, and has a per-org guardrail
    - `node --check scripts/backfill-agent-metadata.mjs` passes (syntax valid)
    - `backfill-coverage.spec.ts` green
  </acceptance_criteria>
  <done>Backfill script covers all published SOPs with a cost guardrail; coverage spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish route → synthesis | Additive hook must not weaken the frozen safety gate |
| batch script → agent tables | Service-role backfill must stay org-scoped per SOP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-05-01 | Tampering | frozen publish gate | mitigate | Hook is additive + fire-and-forget; steps 1-3 + response shape byte-unchanged (git diff scoped to the additive block) |
| T-26.5-05-02 | Elevation of Privilege / Information Disclosure | backfill service-role writes | mitigate | synthesizeSop self-enforces org-scope per SOP (inherited from Plan 04) |
| T-26.5-05-03 | Denial of Service (availability) | synthesis failure on publish | mitigate | `.catch` + greppable log; a failed synthesis never fails the publish |
| T-26.5-05-04 | Availability / cost | unbounded backfill spend | mitigate | Per-org AI-call ceiling in the script |
</threat_model>

<verification>
- backfill-coverage.spec.ts green
- publish route typechecks; git diff scoped to the additive hook
- `node --check` on the backfill script
</verification>

<success_criteria>
- Publish regenerates metadata (fire-and-forget) without touching the frozen gate
- Backfill script populates all published SOPs with a cost guardrail
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-05-SUMMARY.md` when done.
</output>
