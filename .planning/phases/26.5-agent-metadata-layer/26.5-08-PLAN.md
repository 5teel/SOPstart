---
phase: 26.5-agent-metadata-layer
plan: 8
type: execute
wave: 6
depends_on: ["26.5-07"]
files_modified:
  - src/app/(protected)/admin/agent/page.tsx
  - src/app/(protected)/admin/agent/AgentDashboardClient.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
  - tests/phase26.5/agent-dashboard.spec.ts
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-13]

must_haves:
  truths:
    - "An org-level agent dashboard route exists under /admin, admin/safety-manager-only (D-09/D-11)"
    - "The dashboard shows the evidence-backed proposals queue (primary) + a recent-activity feed of memory writes/metadata refreshes (secondary) (D-11)"
    - "Admins can approve/decline proposals from the dashboard; the action re-fetches and the row leaves the queue (D-10)"
    - "No cross-SOP graph visualisation ships this phase (D-11/D-13)"
    - "journeys.ts + uat/tests.ts reflect the new /admin/agent route and agentview toggle (pathways-map rule)"
  artifacts:
    - path: "src/app/(protected)/admin/agent/page.tsx"
      provides: "SSR org agent dashboard route, admin-guarded (D-09/D-11)"
      contains: "getAgentDashboardData"
    - path: "src/app/(protected)/admin/agent/AgentDashboardClient.tsx"
      provides: "Proposals queue + activity feed + approve/decline UI (D-10/D-11)"
      contains: "approveProposalAction"
    - path: "src/lib/journeys/journeys.ts"
      provides: "Pathways-map coverage for the agent dashboard + agentview flow"
      contains: "/admin/agent"
  key_links:
    - from: "src/app/(protected)/admin/agent/page.tsx"
      to: "getAgentDashboardData server action"
      via: "SSR fetch passed to the client grid"
      pattern: "getAgentDashboardData"
    - from: "AgentDashboardClient approve/decline"
      to: "approveProposalAction / declineProposalAction"
      via: "server action call + re-fetch"
      pattern: "approveProposalAction"
---

<objective>
Ship D-09 surface 2: the org-wide agent dashboard under `/admin/agent` — the evidence-backed proposals queue (primary) plus a recent-activity feed (secondary) — with working approve/decline, and update the living pathways map (journeys.ts + uat/tests.ts) so the new route + agentview toggle are covered (CLAUDE.md pathways rule). Infrastructure + surfacing only; no cross-SOP graph viz (D-11/D-13).

Purpose: The dashboard is where admins act on what the synthesis job found. Its primary surface is the pending proposals queue (each carrying its structured evidence, D-07/D-11); the secondary is a feed of memory writes / metadata refreshes so the layer is visibly alive (D-14 payoff). The approve/decline must actually mutate + re-fetch (behavioural, not a dead affordance — CLAUDE.md 2026-06-05). Per this project's convention, a new route is not done until journeys.ts reflects it.

Output: `admin/agent/page.tsx` + `AgentDashboardClient.tsx`, journeys.ts + uat/tests.ts updates, live dashboard spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/app/(protected)/admin/departments/page.tsx
@src/lib/journeys/journeys.ts
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- Files `src/app/(protected)/admin/agent/page.tsx`, `src/app/(protected)/admin/agent/AgentDashboardClient.tsx`
- New `Journey` entry (id like `agent-layer`) in `src/lib/journeys/journeys.ts` covering `/admin/agent` + the builder agentview toggle
- New UAT test entry/entries in `src/lib/uat/tests.ts` for the agent dashboard + agentview surface
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Org agent dashboard route — proposals queue + activity feed + approve/decline (D-09/D-10/D-11/D-12)</name>
  <files>src/app/(protected)/admin/agent/page.tsx, src/app/(protected)/admin/agent/AgentDashboardClient.tsx, tests/phase26.5/agent-dashboard.spec.ts</files>
  <read_first>
    - src/app/(protected)/admin/departments/page.tsx (the auth-guard shape: createClient → getUser → redirect('/login') if none → org member role check → redirect('/dashboard') if not admin/safety_manager → fetch via server action → pass to a client component; copy this shape exactly)
    - src/actions/agent-layer.ts (getAgentDashboardData, approveProposalAction, declineProposalAction from Plan 07)
    - .planning/phases/26.5-agent-metadata-layer/26.5-CONTEXT.md D-11 (dashboard = proposals queue primary + activity feed secondary; NO cross-SOP graph viz) + D-12 (assessment surfaces here too)
    - src/components/admin/departments/DepartmentGrid.tsx (a client component consuming server-fetched data + calling server actions — structural analog for the interactive queue)
  </read_first>
  <action>
    Create `src/app/(protected)/admin/agent/page.tsx` as an SSR Server Component: guard with the departments.ts page pattern (getUser → redirect('/login'); org member role fetch → redirect('/dashboard') if role not in admin|safety_manager, D-09 admin-only). Fetch via `getAgentDashboardData()` and pass the data to `<AgentDashboardClient>`.

    Create `src/app/(protected)/admin/agent/AgentDashboardClient.tsx` (client component): render (1) the PRIMARY evidence-backed proposals queue — each pending proposal shows kind, description, its structured evidence (source memory rows + signal counts), and the target SOP + assessment badge (D-12); Approve / Decline buttons call `approveProposalAction(id)` / `declineProposalAction(id)` then re-fetch (or optimistically remove the row) so it leaves the queue. (2) The SECONDARY recent-activity feed — recent memory writes / metadata refreshes. NO cross-SOP graph visualisation (D-11/D-13). Style with the `--ai` purple / JetBrains-Mono agent aesthetic (reuse the classes from Plan 07). Use the purple `--ai` accent for the machine-layer identity.

    Make `tests/phase26.5/agent-dashboard.spec.ts` LIVE via source-contract + behavioural: assert `page.tsx` guards admin/safety_manager (redirect on non-admin) and calls `getAgentDashboardData`; assert `AgentDashboardClient` wires Approve/Decline to `approveProposalAction`/`declineProposalAction` (the handler references the action, not an empty onClick — CLAUDE.md 2026-06-05); assert there is NO graph/canvas viz component imported (D-11 scope).
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/agent-dashboard.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/agent/page.tsx` redirects non-admin/safety_manager and calls `getAgentDashboardData`
    - `AgentDashboardClient` Approve/Decline buttons invoke `approveProposalAction`/`declineProposalAction` and re-fetch/remove the row (wired handler)
    - Proposals render their structured evidence; activity feed present; no graph-viz import
    - `agent-dashboard.spec.ts` green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Org agent dashboard live: proposals queue + activity feed + working approve/decline, admin-only.</done>
</task>

<task type="auto">
  <name>Task 2: Update the living pathways map (journeys.ts + uat/tests.ts)</name>
  <files>src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts (the `Journey`/`JourneyStep` interfaces + `JOURNEY_GROUPS` list + an existing admin journey to model on; keep `route` values real so /pathways coverage + deep-links work)
    - CLAUDE.md § Pathways Map Maintenance (a new route/flow is not "done" until journeys.ts reflects it; /pathways "All screens" must show 0 not-mapped for the new route)
    - src/lib/uat/tests.ts (the shape of a UAT/design-feedback entry to add the agent-dashboard + agentview items)
  </read_first>
  <action>
    Add a `Journey` (e.g. id `agent-layer`, a fitting `JOURNEY_GROUPS` group such as `Library & team` or `Refine & publish`) that covers: admin opens the builder → toggles `⚇ Agent layer` (agentview) → inspects the read-only per-SOP + per-block machine metadata → opens the org agent dashboard at `route: '/admin/agent'` → reviews the proposals queue + activity feed → approves/declines a proposal. Use real `route` values (`/admin/sops/builder/[sopId]` for the toggle step, `/admin/agent` for the dashboard) so the /pathways "All screens" panel shows 0 not-mapped for the new route.

    Add matching UAT entr(y/ies) to `src/lib/uat/tests.ts` for the agent dashboard + agentview surface (team-review-worthy per CLAUDE.md — visual parity vs the sketch + approve/decline flow).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "journeys|uat/tests" || echo "journeys + uat typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - `journeys.ts` contains a Journey with a step `route: '/admin/agent'` and a step referencing the builder agentview toggle
    - `uat/tests.ts` has a new entry for the agent dashboard / agentview surface
    - `npx tsc --noEmit` clean (both files compile)
  </acceptance_criteria>
  <done>Pathways map + UAT config reflect the new /admin/agent route and agentview toggle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /admin/agent | Only admin/safety_manager may view the dashboard or act on proposals |
| dashboard action → proposal state | Approve/decline must stay org-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-08-01 | Elevation of Privilege | /admin/agent route | mitigate | SSR guard redirects non-admin/safety_manager (departments.ts page pattern) |
| T-26.5-08-02 | Information Disclosure | dashboard data fetch | mitigate | getAgentDashboardData is org-scoped (Plan 07); RLS SELECT also gates rows |
| T-26.5-08-03 | Tampering | cross-org proposal approve/decline from dashboard | mitigate | Actions delegate to lifecycle fns that `.eq('organisation_id')` on the write |
| T-26.5-08-04 | Repudiation | proposal decision attribution | mitigate | approve/decline records reviewed_by + reviewed_at (from Plan 04 schema) |
</threat_model>

<verification>
- agent-dashboard.spec.ts green
- `npx tsc --noEmit` clean
- /pathways "All screens" shows 0 not-mapped for /admin/agent (journeys.ts updated)
- Final phase gate: real `npm run build` green (CLAUDE.md 2026-06-27 — this phase adds `'use server'` actions)
</verification>

<success_criteria>
- Org agent dashboard live: proposals queue + activity feed + working approve/decline, admin-only
- No cross-SOP graph viz (D-11/D-13 scope held)
- Pathways map + UAT config updated for the new route + toggle
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-08-SUMMARY.md` when done.
</output>
