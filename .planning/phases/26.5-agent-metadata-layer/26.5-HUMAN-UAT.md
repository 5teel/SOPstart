---
status: partial
phase: 26.5-agent-metadata-layer
source: [26.5-VERIFICATION.md]
started: 2026-07-05T00:00:00Z
updated: 2026-07-05T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. D-07 mechanism deviation — accept or reject
expected: Decision: proposals are implemented as a dedicated `agent_learning_proposals` table + lifecycle (createLearningProposal/approve/decline) instead of literally riding `gateWrite`/`ai_field_proposals`. Functionally equivalent (evidence-backed, pending_approval, admin-only, no auto-apply), fully tested — but D-07's wording said "no parallel approval machinery". Accept the implementation as satisfying D-07's intent, or require a refactor onto gateWrite.
result: ACCEPTED 2026-07-05 — Simon accepted the implementation as satisfying D-07's intent (proposals are structurally different from field-writes; dedicated table is the right shape). Recorded as accepted deviation.

### 2. VOYAGE_API_KEY + CRON_SECRET on Railway
expected: Both env vars set on the Railway SOPstart service BEFORE relying on synthesis. Until set: synthesis-sweep returns 503 `voyage_api_key_missing`; sweep auth fails closed 401. (Deferred at the plan 06 gate — "continue anyway".)
result: [pending]

### 3. Production backfill run
expected: After the key lands: `npx tsx scripts/backfill-agent-metadata.mjs` completes with 0 failures; spot-check a known published SOP has rows in `sop_agent_metadata` (embedding, tags, assessment) and the agentview panel shows real data.
result: [pending]

### 4. Agentview panel + dashboard visual parity (sopstart.com)
expected: `⚇ Agent layer` toggle in the admin builder reveals the purple `--ai` bordered panel with JetBrains Mono per-block metadata rows + sticky agent banner matching `sketches/sop-builder-redesign/index.html` agentview; `/admin/agent` dashboard shows proposals queue + activity feed; workers never see any of it.
result: [pending]

## Summary

total: 4
passed: 1
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
