---
phase: 26.5-agent-metadata-layer
plan: 2
type: execute
wave: 2
depends_on: ["26.5-01"]
files_modified:
  - supabase/migrations/00040_agent_metadata_schema.sql
  - src/types/database.types.ts
  - tests/phase26.5/schema-contract.spec.ts
  - scripts/apply-phase26.5-migration.mjs
autonomous: true
requirements: [D-01, D-02, D-03, D-05, D-07, D-08]

must_haves:
  truths:
    - "Five append-only org-scoped tables exist: sop_agent_metadata, block_agent_metadata, agent_memory, agent_learning_proposals, sop_voice_qa_log (D-01/D-05/D-07/D-08)"
    - "pgvector extension is enabled and vector(1024) columns carry HNSW cosine indexes (D-03)"
    - "block_agent_metadata is keyed by the sop_section_blocks junction id (D-02)"
    - "Every table's RLS SELECT policy uses current_organisation_id() directly with ZERO cross-table subquery (42P17-safe)"
    - "No authenticated INSERT/UPDATE/DELETE policy on any new table (writes via service role only)"
    - "match_sop_agent_metadata RPC wraps the <=> operator and self-enforces org-scope inside the function body (D-03)"
  artifacts:
    - path: "supabase/migrations/00040_agent_metadata_schema.sql"
      provides: "All 5 agent-layer tables + pgvector + HNSW indexes + similarity RPC (D-01/D-02/D-03/D-05/D-07/D-08)"
      contains: "create extension if not exists vector"
    - path: "src/types/database.types.ts"
      provides: "Hand-extended TS types for the 5 new tables"
      contains: "sop_agent_metadata"
    - path: "tests/phase26.5/schema-contract.spec.ts"
      provides: "Source-contract assertions on RLS + no-cross-table-policy shape (D-01/D-02)"
  key_links:
    - from: "block_agent_metadata.block_id"
      to: "sop_section_blocks.id"
      via: "foreign key (junction identity — D-02)"
      pattern: "references public.sop_section_blocks"
    - from: "match_sop_agent_metadata"
      to: "sop_agent_metadata.embedding"
      via: "vector cosine operator inside SECURITY DEFINER body"
      pattern: "embedding <=> query_embedding"
---

<objective>
Ship the entire agent-layer data model as one migration (00040), mirroring the 00038/00039 append-only + org-scoped-SELECT-only precedent exactly, plus pgvector enablement, HNSW indexes, and the SECURITY DEFINER similarity RPC. Then run the mandatory `supabase db push` and verify the tables landed. Hand-extend `database.types.ts` (no type regen in this env).

Purpose: This is the foundation every later plan writes to. D-01 (dedicated tables, layout_data untouched), D-02 (block granularity via junction id), D-03 (pgvector), D-05 (memory table), D-07/D-08 (proposal + free-append memory tables) all land here. Getting the RLS shape byte-identical to 00039 avoids the 42P17 recursion and cross-org classes that have bitten this codebase twice.

Output: `supabase/migrations/00040_agent_metadata_schema.sql`, extended `database.types.ts`, live schema-contract spec, applied migration verified via `to_regclass`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@supabase/migrations/00039_sop_image_annotations.sql
@supabase/migrations/00038_phase23_schema.sql
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- Migration `supabase/migrations/00040_agent_metadata_schema.sql`
- Tables `sop_agent_metadata`, `block_agent_metadata`, `agent_memory`, `agent_learning_proposals`, `sop_voice_qa_log`
- Postgres extension `vector` (pgvector)
- RPC function `match_sop_agent_metadata(p_organisation_id, query_embedding, match_count)`
- HNSW indexes `idx_sop_agent_metadata_embedding`, `idx_block_agent_metadata_embedding`
- database.types.ts table type additions for the 5 tables
- Script `scripts/apply-phase26.5-migration.mjs`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migration 00040 — 5 tables + pgvector + HNSW + similarity RPC + database.types (D-01/D-02/D-03/D-05/D-07/D-08)</name>
  <files>supabase/migrations/00040_agent_metadata_schema.sql, src/types/database.types.ts, tests/phase26.5/schema-contract.spec.ts</files>
  <read_first>
    - supabase/migrations/00039_sop_image_annotations.sql (the EXACT append-only + org-scoped-SELECT-only shape to copy — table + enable RLS + single SELECT policy + no-write-policy comment + indexes + comment on table)
    - supabase/migrations/00038_phase23_schema.sql (the ai_field_proposals shape — status column + evidence-adjacent columns)
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Architecture Patterns Pattern 1 (the literal sop_agent_metadata SQL to start from) + Code Examples § pgvector similarity RPC wrapper
    - src/types/database.types.ts (find how a recent table like sop_image_annotations was hand-added — match that structure)
  </read_first>
  <action>
    Create `supabase/migrations/00040_agent_metadata_schema.sql`. Start with `create extension if not exists vector;` at the top. Then create FIVE tables, each following the 00039 shape verbatim (id uuid pk default gen_random_uuid(); organisation_id uuid not null references public.organisations(id) on delete cascade; created_at/updated_at timestamptz):

    1. `sop_agent_metadata` — sop_id uuid not null references public.sops(id) on delete cascade; summary text; tags text[] not null default '{}'; entities jsonb not null default '[]'::jsonb; embedding vector(1024); assessment text not null default 'fresh' check (assessment in ('fresh','drifting','needs-review')); links jsonb not null default '[]'::jsonb (cross-SOP links, D-03 discretion); last_synthesis_status text; last_synthesis_error text; regenerated_at timestamptz. Unique index on sop_id.
    2. `block_agent_metadata` — block_id uuid not null references public.sop_section_blocks(id) on delete cascade (D-02 junction identity); sop_id uuid references public.sops(id) on delete cascade; tags text[] default '{}'; entities jsonb default '[]'::jsonb; embedding vector(1024); regenerated_at timestamptz. Unique index on block_id.
    3. `agent_memory` — sop_id uuid references public.sops(id) on delete set null (nullable = org-wide observations, D-05); scope text not null check (scope in ('sop','org')); observation text not null; signal_source text; metadata jsonb not null default '{}'::jsonb. Append-only (D-08 — no update/delete policy).
    4. `agent_learning_proposals` — sop_id uuid references public.sops(id) on delete cascade; kind text not null; description text not null; evidence jsonb not null default '[]'::jsonb (D-07 structured evidence — source memory rows + signal counts); status text not null default 'pending' check (status in ('pending','applied','rejected')); reviewed_by uuid references auth.users(id); reviewed_at timestamptz.
    5. `sop_voice_qa_log` — sop_id uuid not null references public.sops(id) on delete cascade; user_id uuid references auth.users(id); question text not null; answer text; citations jsonb not null default '[]'::jsonb (Pitfall 1 — the missing voice signal source, written in Plan 03).

    For EACH table: `alter table ... enable row level security;` then ONE SELECT policy `for select to authenticated using (organisation_id = public.current_organisation_id())` — direct column comparison, ZERO `exists (select ... from public.sops)` (42P17 risk, CLAUDE.md 2026-05-13). Add the comment block explaining "NO authenticated INSERT/UPDATE/DELETE policy — writes via createAdminClient() with self-enforced org-scope (CLAUDE.md 2026-06-15)". Add `idx_<table>_org on (organisation_id)` + FK indexes. Add HNSW cosine indexes `using hnsw (embedding vector_cosine_ops)` on the two embedding columns. End each table with a `comment on table` summarizing the security model, matching 00039's style.

    Add the `match_sop_agent_metadata(p_organisation_id uuid, query_embedding vector(1024), match_count int default 10)` function — `language sql stable security definer`, body selects `sop_id, 1 - (embedding <=> query_embedding) as similarity from public.sop_agent_metadata where organisation_id = p_organisation_id order by embedding <=> query_embedding limit match_count`. The org filter INSIDE the body is the self-enforcement (SECURITY DEFINER bypasses RLS — Pitfall 3).

    Hand-extend `src/types/database.types.ts` with Row/Insert/Update types for all 5 tables, matching how sop_image_annotations was added. (Downstream service actions may still use `(supabase as any)` casts consistent with departments.ts, but the types must exist for the RPC + reads.)

    Make `tests/phase26.5/schema-contract.spec.ts` LIVE (replace the Plan-01 stub body): read the migration file text and assert (a) `create extension if not exists vector` present; (b) each of the 5 table names appears in a `create table`; (c) each table has exactly one `for select` policy using `current_organisation_id()`; (d) grep the policy bodies contain NO `from public.sops` (source-contract guard against 42P17); (e) `block_agent_metadata` references `sop_section_blocks` (D-02); (f) `match_sop_agent_metadata` contains `embedding <=> query_embedding` and `organisation_id = p_organisation_id`. Filter comment lines before counting (`grep -v '^--'` equivalent) so header prose never self-invalidates a count.
  </action>
  <verify>
    <automated>npx playwright test --project=phase26.5 tests/phase26.5/schema-contract.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `schema-contract.spec.ts` passes and asserts all 6 shape checks above
    - Migration file contains `create extension if not exists vector`, all 5 `create table` statements, and `using hnsw (embedding vector_cosine_ops)` twice
    - Grep of every `create policy` body in 00040 returns ZERO `from public.sops`
    - `block_agent_metadata` FK: grep returns `references public.sop_section_blocks`
    - `npx tsc --noEmit` clean after database.types.ts extension
  </acceptance_criteria>
  <done>00040 migration + database.types extension + live schema-contract spec complete; no cross-table RLS policy.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db push + post-push existence verification (D-01/D-03)</name>
  <files>scripts/apply-phase26.5-migration.mjs</files>
  <read_first>
    - CLAUDE.md 2026-06-15 learning (PGRST205 schema-cache-stale vs 42P01 table-absent; verify via `to_regclass` through the Supabase Management API, not an immediate `.from(table).select()` probe)
    - scripts/ (any existing apply-phaseNN-migration.mjs, e.g. the Phase 25 apply script pattern, for SUPABASE_ACCESS_TOKEN + Management API query shape)
  </read_first>
  <action>
    This is the MANDATORY schema-push task — the phase cannot pass verification without it (build + tsc pass without the push because types are hand-extended, creating a false-positive state).

    Run `supabase db push` (SUPABASE_ACCESS_TOKEN is already in `.env.local` from prior phases — set it in the process env for the non-TTY push, consistent with the 13-04 auto-completed push precedent).

    Create `scripts/apply-phase26.5-migration.mjs` that, AFTER the push, verifies each of the 5 new tables plus the `vector` extension and the `match_sop_agent_metadata` function exist by querying `select to_regclass('public.<table>')` (and `select extname from pg_extension where extname='vector'`) through the Supabase Management API `POST /v1/projects/{ref}/database/query` (raw SQL, bypasses PostgREST cache). Treat a `to_regclass` NULL as genuine absence (42P01-class) and a PostgREST `PGRST205` from any probe as schema-cache staleness — issue `NOTIFY pgrst, 'reload schema';` via the same Management API and re-probe. Print a clear PASS/FAIL line per object.
  </action>
  <verify>
    <automated>node scripts/apply-phase26.5-migration.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes without error
    - `scripts/apply-phase26.5-migration.mjs` reports all 5 tables + `vector` extension + `match_sop_agent_metadata` present via `to_regclass`/`pg_extension`
    - The script distinguishes PGRST205 (reload + retry) from 42P01 (real failure) — grep the script for both codes
  </acceptance_criteria>
  <done>Migration applied to live DB; all objects verified present via Management API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → new tables | Org members must only read their own org's rows |
| service role → new tables | Synthesis/proposal writes bypass RLS and must self-enforce org-scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-02-01 | Information Disclosure | RLS SELECT policies on 5 tables | mitigate | Each policy filters `organisation_id = current_organisation_id()` directly; cross-org reads impossible |
| T-26.5-02-02 | Denial of Service | New RLS policies referencing sops | mitigate | ZERO cross-table `exists(select ... from sops)` in any policy — 42P17 recursion avoided (schema-contract spec asserts absence) |
| T-26.5-02-03 | Elevation of Privilege | match_sop_agent_metadata SECURITY DEFINER | mitigate | Function body filters `organisation_id = p_organisation_id`; SECURITY DEFINER bypasses RLS so the in-body filter is the enforcement |
| T-26.5-02-04 | Tampering | Post-migration false-negative (PGRST205) | accept/mitigate | Verify existence via `to_regclass` through Management API, not `.from().select()` (CLAUDE.md 2026-06-15) — a documented false-negative class, not an attacker path |
</threat_model>

<verification>
- schema-contract.spec.ts green
- `node scripts/apply-phase26.5-migration.mjs` confirms all objects present
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- 5 append-only org-scoped tables + pgvector + HNSW + similarity RPC live in the DB
- Zero cross-table RLS policy (42P17-safe)
- database.types.ts extended and typechecks
- Migration verified via Management API to_regclass
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-02-SUMMARY.md` when done.
</output>
