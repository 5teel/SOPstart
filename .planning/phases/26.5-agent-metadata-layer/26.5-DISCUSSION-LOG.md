# Phase 26.5: Agent Metadata Layer - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-04
**Phase:** 26.5-agent-metadata-layer
**Areas discussed:** Storage model & granularity, Memory & learning proposals, ⚇ Agent layer surfacing, Phase payoff

---

## Storage model & granularity

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated tables | Org-scoped sop_agent_metadata + block_agent_metadata (junction-keyed); pgvector-ready; agent writes off the autosave path | ✓ |
| Ride on layout_data props | Uses the D-02 round-trip hook; bloats autosave; embeddings can't live in JSON | |
| Hybrid | Tags on props, heavy data in tables | |

**User's choice:** Dedicated tables

| Option | Description | Selected |
|--------|-------------|----------|
| SOP + block both | Per-SOP + per-block rows keyed by sop_section_blocks junction id | ✓ |
| SOP-level only | One row per SOP; block grain later | |
| You decide | Planner picks | |

**User's choice:** SOP + block both

| Option | Description | Selected |
|--------|-------------|----------|
| OpenAI 3-small + pgvector | Zero new deps; sufficient behind approval gates | |
| OpenAI 3-large + pgvector | Middle ground; still 8k chunking | |
| Voyage-3 + pgvector | Retrieval-specialised, hard-negative trained, 32k context; new dep + Railway key | ✓ |

**User's choice:** Voyage-3 + pgvector
**Notes:** User probed the trade-off twice ("Explain why Voyage-3 has better retrieval quality? The margin for error is incredibly low when working with SOPs"). Decision made after a mechanics deep-dive: contrastive training objectives, hard-negative mining (near-duplicate SOP corpus — LOTO Furnace A vs B), domain corpora, 32k vs 8k context/chunking. Counterweight acknowledged (retrieval is candidate-generation behind X-03 approval + verify + publish gates; corpus is small) — user chose the quality option anyway. Do not silently downgrade.

| Option | Description | Selected |
|--------|-------------|----------|
| On publish | Stable content, no autosave churn | ✓ |
| On publish + manual refresh | Adds admin-invokable refresh for long-lived drafts | |
| On every autosave (debounced) | Always fresh; external API on hot path | |

**User's choice:** On publish

---

## Memory & learning proposals

| Option | Description | Selected |
|--------|-------------|----------|
| Per-SOP + org-wide | Observations per SOP + org-level conventions | ✓ |
| Per-SOP only | Simpler; org patterns via cross-SOP queries later | |
| Per-block level too | Finest grain via junction ids | |

**User's choice:** Per-SOP + org-wide

| Option | Description | Selected |
|--------|-------------|----------|
| Completion data | sop_completions + ack traces | ✓ |
| AI-reviewer + verify history | Recurring flags / send-backs | ✓ |
| Voice Q&A transcripts | Worker questions reveal unclear steps | ✓ |
| Admin edit patterns | What admins correct after AI drafts | ✓ |

**User's choice:** All four (multiSelect)

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse X-03 approval path | gateWrite → pending_approval → admin review | |
| New proposals table + review UI | Parallel approval system | |
| X-03 path + evidence extension | Reuse gating; extend record with structured evidence (source memory rows, signal counts) | ✓ |

**User's choice:** X-03 path + evidence extension

| Option | Description | Selected |
|--------|-------------|----------|
| Free append-only writes | Memory = observations, not content; only derived proposals gated | ✓ |
| Stake-gated like content | All agent writes through gateWrite | |
| Free writes + retention cap | Append-only + TTL/compaction | |

**User's choice:** Free append-only writes

---

## ⚇ Agent layer surfacing

| Option | Description | Selected |
|--------|-------------|----------|
| Builder toggle, admin-only | Sketch's agentview toggle over the canvas | |
| Separate route | Dedicated /admin page; loses spatial context | |
| Toggle + org-level dashboard | Builder toggle PLUS org-wide agent activity view | ✓ |

**User's choice:** Toggle + org-level dashboard

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only + proposal actions | Inspect + approve/decline; no hand-editing (regenerates on publish) | ✓ |
| Editable tags/entities | Needs admin-pinned flags to survive regeneration | |
| Fully editable | Memory loses audit integrity | |

**User's choice:** Read-only + proposal actions

| Option | Description | Selected |
|--------|-------------|----------|
| Proposals queue + activity feed | Actionable first, observability second | ✓ |
| Proposals + cross-SOP graph view | Adds graph viz — real scope | |
| Minimal: proposals queue only | Smallest actionable slice | |

**User's choice:** Proposals queue + activity feed

| Option | Description | Selected |
|--------|-------------|----------|
| Agent assessment per SOP | Machine-maintained fresh/drifting/needs-review from signals | ✓ |
| Proposal lifecycle state only | Just pending/approved/declined | |
| You decide | Planner interprets | |

**User's choice:** Agent assessment per SOP

---

## Phase payoff — what ships visibly

| Option | Description | Selected |
|--------|-------------|----------|
| One flagship capability | Infra + one end-to-end proof (e.g. cross-SOP summarise) | |
| Infrastructure + surfacing only | Capabilities come in conversational phases | ✓ |
| Multiple capabilities | Full "layer delivers" story; big scope | |

**User's choice:** Infrastructure + surfacing only

| Option | Description | Selected |
|--------|-------------|----------|
| Scheduled synthesis job | Periodic/on-publish job populates memory/assessments/proposals | ✓ |
| Plumbing + manual trigger only | Admin-invoked "analyse now" | |
| Write paths only | Nothing populates until later phases (dead UI) | |

**User's choice:** Scheduled synthesis job

| Option | Description | Selected |
|--------|-------------|----------|
| Backfill all published | One-off batch job; layer useful day one | ✓ |
| Forward-only | Layer starts near-empty | |
| Backfill on-demand per SOP | Manual gradual coverage | |

**User's choice:** Backfill all published

| Option | Description | Selected |
|--------|-------------|----------|
| Claude Haiku 4.5 | Anthropic SDK in project; synthesis is Haiku-grade; shared constant + env override | ✓ |
| Claude Sonnet 5 | Stronger reasoning, costlier | |
| You decide | Per sub-task split | |

**User's choice:** Claude Haiku 4.5

---

## Claude's Discretion

- Table/schema design (names, columns, compaction, retention)
- Scheduler mechanics on Railway (no cron infra; single-process constraint)
- Cross-SOP link semantics + graphify build-time vs runtime-DB relationship
- Per-org cost guardrails on synthesis/embedding calls
- Exact assessment states + thresholds
- Assessment badge placement beyond panel + dashboard

## Deferred Ideas

- Flagship agent capabilities (cross-SOP summarise, related-SOPs, drift-alert notifications) → conversational phases
- Cross-SOP graph visualisation in the dashboard
- Editable/pinnable metadata surviving regeneration
- Provider re-embed migration (only if Voyage disappoints or worker-facing cross-SOP Q&A raises stakes)
