---
phase: 26.5-agent-metadata-layer
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - src/lib/agent-layer/model-constants.ts
  - src/lib/agent-layer/voyage-client.ts
  - tests/phase26.5/schema-contract.spec.ts
  - tests/phase26.5/synthesis-pipeline.spec.ts
  - tests/phase26.5/proposal-evidence.spec.ts
  - tests/phase26.5/signal-readers.spec.ts
  - tests/phase26.5/voice-qa-persistence.spec.ts
  - tests/phase26.5/synthesis-sweep-auth.spec.ts
  - tests/phase26.5/backfill-coverage.spec.ts
  - tests/phase26.5/agent-panel-readonly.spec.ts
  - tests/phase26.5/agent-dashboard.spec.ts
autonomous: false
requirements: [D-03, D-16]
user_setup:
  - service: voyage
    why: "Embedding generation for the agent-metadata layer (D-03)"
    env_vars:
      - name: VOYAGE_API_KEY
        source: "Voyage AI dashboard -> API keys (confirmed on Railway in Plan 06 before deploy)"
      - name: VOYAGE_EMBED_MODEL
        source: "Optional override; defaults to voyage-3.5 in code"
      - name: SYNTHESIS_MODEL
        source: "Optional override; defaults to claude-haiku-4-5-20251001 in code"

must_haves:
  truths:
    - "phase26.5 Playwright project is discoverable via --list (D-03/D-16 test infra)"
    - "voyageai is installed only after a human legitimacy verification (D-03)"
    - "Embedding + synthesis model IDs live behind env-overridable shared constants (D-03/D-16)"
    - "getVoyageClient() is a lazy singleton that never constructs at module load"
  artifacts:
    - path: "src/lib/agent-layer/model-constants.ts"
      provides: "EMBED_MODEL + SYNTHESIS_MODEL env-overridable constants (D-03/D-16)"
      contains: "EMBED_MODEL"
    - path: "src/lib/agent-layer/voyage-client.ts"
      provides: "Lazy VoyageAIClient singleton (D-03)"
      contains: "getVoyageClient"
    - path: "playwright.config.ts"
      provides: "phase26.5 project registration"
      contains: "phase26.5"
  key_links:
    - from: "tests/phase26.5/*.spec.ts"
      to: "playwright.config.ts phase26.5 project"
      via: "testMatch regex"
      pattern: "phase26\\.5"
---

<objective>
Lay the phase foundation with ZERO database dependency: register the `phase26.5` Playwright project, drop Wave-0 stub specs for every downstream test, install the `voyageai` SDK behind a mandatory legitimacy gate, and create the two shared building blocks every later plan imports — the lazy Voyage client and the env-overridable model constants (D-03, D-16).

Purpose: Everything downstream (schema, synthesis, UI) depends on the test harness existing and on `EMBED_MODEL`/`SYNTHESIS_MODEL` being single-source constants (CLAUDE.md 2026-06-02 model-rot learning). Getting the `voyageai` install gated and the specs registered up front prevents the 2026-05-25 unregistered-spec trap and the [ASSUMED]-package supply-chain risk.

Output: Registered Playwright project, 9 stub spec files, installed `voyageai`, `src/lib/agent-layer/model-constants.ts`, `src/lib/agent-layer/voyage-client.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-agent-metadata-layer/26.5-CONTEXT.md
@.planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md
@.planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md
@src/lib/parsers/verify-sop.ts
</context>

<artifacts_produced>
New symbols/files this plan creates (exclude from source-drift acknowledgement):
- File `src/lib/agent-layer/model-constants.ts` — exports `EMBED_MODEL`, `SYNTHESIS_MODEL`
- File `src/lib/agent-layer/voyage-client.ts` — exports `getVoyageClient`
- Playwright project `phase26.5` in `playwright.config.ts`
- 9 stub spec files under `tests/phase26.5/` (schema-contract, synthesis-pipeline, proposal-evidence, signal-readers, voice-qa-persistence, synthesis-sweep-auth, backfill-coverage, agent-panel-readonly, agent-dashboard)
- New npm dependency `voyageai`
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [GATE] Verify voyageai package legitimacy before install (D-03)</name>
  <read_first>
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Package Legitimacy Audit (the [ASSUMED] disposition — slopcheck could not reach the registry in the research sandbox)
  </read_first>
  <action>
    Do NOT run `npm install voyageai` in this task. Pause and present the legitimacy evidence (below) to the human for confirmation — this is a blocking human gate because `voyageai` is [ASSUMED] (slopcheck could not reach the registry during research). Only after the human types "approved" does Task 2 perform the install.
  </action>
  <what-built>
    Nothing is installed yet. This gate exists because RESEARCH.md marked `voyageai` as `[ASSUMED]` — slopcheck could not verify it (registry unreachable in the research sandbox), so per the Package Legitimacy Gate protocol a human must confirm legitimacy before `npm install`.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/voyageai
    2. Confirm: publisher/maintainer is `voyage-ai` (email voyageai.com domain); repository is `github.com/voyage-ai/typescript-sdk`; latest version is a real recent release (~0.4.0); the package has a multi-version history (not a brand-new single-version package).
    3. Confirm there is NO suspicious `postinstall`/`preinstall` script in the package's `package.json` on npm.
    4. Cross-check the repo `github.com/voyage-ai/typescript-sdk` is the official Voyage AI org and its README shows `npm i voyageai`.
  </how-to-verify>
  <acceptance_criteria>
    - npmjs.com/package/voyageai shows maintainer `voyage-ai` and repo `github.com/voyage-ai/typescript-sdk`
    - No install-time scripts flagged
    - Simon types "approved" (or names a blocker)
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow the install in Task 2, or describe what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install voyageai + shared model constants + lazy Voyage client (D-03, D-16)</name>
  <files>package.json, src/lib/agent-layer/model-constants.ts, src/lib/agent-layer/voyage-client.ts</files>
  <read_first>
    - src/lib/parsers/verify-sop.ts (lines 1-25 getAnthropic lazy singleton; line ~102 VERIFY_MODEL env-override constant — copy BOTH patterns exactly)
    - .planning/phases/26.5-agent-metadata-layer/26.5-PATTERNS.md § voyage-client.ts + model-constants.ts (lazy-init + constant shape)
  </read_first>
  <action>
    Run `npm install voyageai` (adds it to `dependencies`, exact-pinned consistent with the Phase 26-01 dep-pinning convention).

    Create `src/lib/agent-layer/model-constants.ts` exporting two env-overridable constants matching the `VERIFY_MODEL` pattern in verify-sop.ts: `EMBED_MODEL = process.env.VOYAGE_EMBED_MODEL ?? 'voyage-3.5'` and `SYNTHESIS_MODEL = process.env.SYNTHESIS_MODEL ?? 'claude-haiku-4-5-20251001'`. Add a comment on `EMBED_MODEL` stating: default is `voyage-3.5` (current-generation, retrieval-specialised, SAME price/context as voyage-3 per D-03 — provider unchanged; set `VOYAGE_EMBED_MODEL=voyage-3` to pin the bare model). Add a comment on `SYNTHESIS_MODEL` citing D-16 + CLAUDE.md 2026-06-02 model-rot learning. Do NOT hardcode either literal string anywhere else in the phase.

    Create `src/lib/agent-layer/voyage-client.ts` with a lazy singleton `getVoyageClient(): VoyageAIClient` using the `let voyage: VoyageAIClient | null = null; if (!voyage) { voyage = new VoyageAIClient({ apiKey: process.env.VOYAGE_API_KEY }) }` shape — imported from `voyageai`. Never construct at module top level (Next.js static-analysis safety when the key is unset locally). Add a header comment referencing the getAnthropic() analog.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "agent-layer/(model-constants|voyage-client)" || echo "no type errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `voyageai`
    - `src/lib/agent-layer/model-constants.ts` exports `EMBED_MODEL` and `SYNTHESIS_MODEL`; grep for `voyage-3.5` and `claude-haiku-4-5-20251001` each returns exactly one hit (the constant default)
    - `src/lib/agent-layer/voyage-client.ts` exports `getVoyageClient` and does NOT call `new VoyageAIClient` at module top level (only inside the `if (!voyage)` guard)
    - `npx tsc --noEmit` reports no errors originating from the two new files
  </acceptance_criteria>
  <done>voyageai installed; EMBED_MODEL/SYNTHESIS_MODEL constants and lazy getVoyageClient exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: Register phase26.5 Playwright project + Wave-0 stub specs</name>
  <files>playwright.config.ts, tests/phase26.5/schema-contract.spec.ts, tests/phase26.5/synthesis-pipeline.spec.ts, tests/phase26.5/proposal-evidence.spec.ts, tests/phase26.5/signal-readers.spec.ts, tests/phase26.5/voice-qa-persistence.spec.ts, tests/phase26.5/synthesis-sweep-auth.spec.ts, tests/phase26.5/backfill-coverage.spec.ts, tests/phase26.5/agent-panel-readonly.spec.ts, tests/phase26.5/agent-dashboard.spec.ts</files>
  <read_first>
    - playwright.config.ts (the existing `phase26` project entry near line 214-230 — copy its shape verbatim, renaming)
    - tests/phase26/block-registry.spec.ts (one existing phase26 spec, to match the source-contract + test.fixme stub convention)
    - .planning/phases/26.5-agent-metadata-layer/26.5-RESEARCH.md § Validation Architecture (the 9 Wave-0 gap files + which D-IDs each covers)
  </read_first>
  <action>
    Add a new `phase26.5` project to `playwright.config.ts` with `testMatch: /tests\/phase26\.5\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`, mirroring the deliberately-broad `phase26` entry so later plans drop specs in `tests/phase26.5/` with no further config edit (CLAUDE.md 2026-05-25).

    Create all 9 stub specs under `tests/phase26.5/`. Each stub is a real Playwright file that RUNS but asserts nothing yet — use `test.fixme(...)` with a title naming the D-IDs it will cover, OR a live source-contract test guarded by `fs.existsSync` that passes vacuously today (follow the phase22 `fs.existsSync + test.skip` convention to avoid dynamic-`import('@/...')` failures — CLAUDE.md 2026-06-24). Files and their target coverage: `schema-contract.spec.ts` (D-01/D-02), `synthesis-pipeline.spec.ts` (D-03/D-04), `proposal-evidence.spec.ts` (D-07/D-08), `signal-readers.spec.ts` (D-06), `voice-qa-persistence.spec.ts` (D-06 voice write, Pitfall 1), `synthesis-sweep-auth.spec.ts` (D-14), `backfill-coverage.spec.ts` (D-15), `agent-panel-readonly.spec.ts` (D-09/D-10), `agent-dashboard.spec.ts` (D-11). Use `[\s\S]` never the `/s` regex flag in any spec (CLAUDE.md 2026-06-02 TS target).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase26.5 2>&1 | grep -E "phase26\.5" | head</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase26.5` lists all 9 spec files (none silently dropped)
    - `playwright.config.ts` contains `name: 'phase26.5'` with the `/tests\/phase26\.5\/.*\.(spec|test)\.ts$/` regex
    - Running `npx playwright test --project=phase26.5` exits 0 (stubs pass/skip, none fail)
    - No spec uses the `/s` regex flag
  </acceptance_criteria>
  <done>phase26.5 project registered and discoverable; 9 stub specs run green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package `voyageai` enters the supply chain |
| env → server | `VOYAGE_API_KEY` read server-side only, never bundled to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-01-SC | Tampering | `voyageai` npm install | mitigate | Blocking human legitimacy checkpoint (Task 1) before install; [ASSUMED] per RESEARCH audit — never auto-approved |
| T-26.5-01-02 | Information Disclosure | `voyage-client.ts` | mitigate | Client constructed server-side only; `VOYAGE_API_KEY` never `NEXT_PUBLIC_*`; lazy-init avoids leaking key presence at build |
</threat_model>

<verification>
- `npx playwright test --list --project=phase26.5` shows 9 specs
- `npx tsc --noEmit` clean for the two new lib files
- `voyageai` present in package.json only after the legitimacy gate passed
</verification>

<success_criteria>
- phase26.5 Playwright project registered and discoverable
- 9 Wave-0 stub specs run green
- voyageai installed post-gate
- EMBED_MODEL/SYNTHESIS_MODEL constants + lazy getVoyageClient created and typecheck
</success_criteria>

<output>
Create `.planning/phases/26.5-agent-metadata-layer/26.5-01-SUMMARY.md` when done.
</output>
