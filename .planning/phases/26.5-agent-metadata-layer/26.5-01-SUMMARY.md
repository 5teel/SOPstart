---
phase: 26.5-agent-metadata-layer
plan: 1
subsystem: agent-layer foundation
tags: [voyageai, embeddings, playwright, nyquist-wave-0, model-constants]
requires: []
provides:
  - "phase26.5 Playwright project (broad tests/phase26.5/** testMatch)"
  - "9 Wave-0 stub specs covering D-01..D-16"
  - "EMBED_MODEL + SYNTHESIS_MODEL env-overridable constants"
  - "getVoyageClient() lazy singleton"
  - "voyageai 0.4.0 dependency (post human legitimacy gate)"
affects: [package.json, playwright.config.ts]
tech-stack:
  added: ["voyageai 0.4.0 (exact-pinned)"]
  patterns:
    - "lazy external-client singleton (getAnthropic analog)"
    - "env-overridable model constants (VERIFY_MODEL analog)"
    - "fs.existsSync-guarded Wave-0 source-contract stubs (phase22 convention)"
key-files:
  created:
    - src/lib/agent-layer/model-constants.ts
    - src/lib/agent-layer/voyage-client.ts
    - tests/phase26.5/schema-contract.spec.ts
    - tests/phase26.5/synthesis-pipeline.spec.ts
    - tests/phase26.5/proposal-evidence.spec.ts
    - tests/phase26.5/signal-readers.spec.ts
    - tests/phase26.5/voice-qa-persistence.spec.ts
    - tests/phase26.5/synthesis-sweep-auth.spec.ts
    - tests/phase26.5/backfill-coverage.spec.ts
    - tests/phase26.5/agent-panel-readonly.spec.ts
    - tests/phase26.5/agent-dashboard.spec.ts
  modified:
    - package.json
    - playwright.config.ts
decisions:
  - "voyageai 0.4.0 install approved by Simon at the Task-1 blocking human gate (live npm view corroboration: maintainer voyage-ai, repo voyage-ai/typescript-sdk, no postinstall script, MIT)"
  - "EMBED_MODEL defaults to voyage-3.5 (current-gen, same price/context as voyage-3 per D-03); VOYAGE_EMBED_MODEL env pins voyage-3 if wanted"
  - "Wave-0 stubs use fs.existsSync/content-level guards + test.fixme (not dynamic import) — phase22 convention, avoids Playwright ESM import failures"
metrics:
  duration: "~12m"
  completed: "2026-07-04"
---

# Phase 26.5 Plan 01: Agent Layer Foundation Summary

**One-liner:** voyageai 0.4.0 installed post-legitimacy-gate; EMBED_MODEL/SYNTHESIS_MODEL shared constants + lazy getVoyageClient() singleton; phase26.5 Playwright project registered with 9 Wave-0 stub specs (29 tests, all skip green).

## What Was Built

**Task 1 (checkpoint:human-verify):** voyageai legitimacy gate. Live `npm view` from this machine corroborated: version 0.4.0, maintainer `voyage-ai <zoltan@voyageai.com>`, repo `github.com/voyage-ai/typescript-sdk`, homepage matches, NO postinstall/preinstall script, MIT license, official registry tarball. Simon typed "approved".

**Task 2:** `npm install voyageai --save-exact` → `"voyageai": "0.4.0"` in dependencies (Phase 26-01 exact-pin convention). Created:
- `src/lib/agent-layer/model-constants.ts` — `EMBED_MODEL` (`process.env.VOYAGE_EMBED_MODEL ?? 'voyage-3.5'`) and `SYNTHESIS_MODEL` (`process.env.SYNTHESIS_MODEL ?? 'claude-haiku-4-5-20251001'`), each literal appears exactly once (grep-verified), comments cite D-03/D-16 + the 2026-06-02 model-rot learning.
- `src/lib/agent-layer/voyage-client.ts` — `getVoyageClient()` lazy singleton, `new VoyageAIClient` only inside the `if (!voyage)` guard, never at module load (getAnthropic analog cited in header).

**Task 3:** `phase26.5` project added to `playwright.config.ts` (`testMatch: /tests\/phase26\.5\/.*\.(spec|test)\.ts$/`, chromium, mirrors the deliberately-broad phase26 entry). 9 stub specs created under `tests/phase26.5/`, each a mix of fs.existsSync/content-guarded source-contract tests (skip today, go live when the target artifact ships) and `test.fixme` runtime stubs naming their D-IDs. No spec uses the `/s` regex flag.

## Verification

- `npx playwright test --list --project=phase26.5` → 29 tests in 9 files (all 9 spec files discovered, none dropped)
- `npx playwright test --project=phase26.5` → exit 0, 29 skipped (green)
- `npx tsc --noEmit` → zero errors from `agent-layer/*` or `tests/phase26.5/*` or `playwright.config.ts`
- `grep -c "voyage-3\.5" model-constants.ts` → 1; `grep -c "claude-haiku-4-5-20251001"` → 1

## Deviations from Plan

**1. [Rule 1 - Bug] Comment reworded to keep the voyage-3.5 grep count at exactly one**
- **Found during:** Task 2
- **Issue:** The plan's requested comment text ("default is voyage-3.5…") would put a second `voyage-3.5` literal in the file, breaking the plan's own acceptance criterion ("grep returns exactly one hit — the constant default").
- **Fix:** Comment says "the current-generation 3.5 model" instead; the criterion holds.
- **Files modified:** src/lib/agent-layer/model-constants.ts
- **Commit:** 8e95e4c

## Known Stubs

All 9 `tests/phase26.5/*.spec.ts` files are intentional Wave-0 stubs (this plan's explicit deliverable): guarded source-contract tests that skip until later plans (26.5-02..08) create the migration, synthesis pipeline, signal readers, voice-QA write path, sweep route, backfill script, panel components, and dashboard page. Not blockers — they are the Nyquist harness.

## Task Commits

| Task | Name | Commit |
|------|------|--------|
| 1 | [GATE] voyageai legitimacy verification | (no code — human approved) |
| 2 | Install voyageai + model constants + lazy Voyage client | 8e95e4c |
| 3 | Register phase26.5 project + 9 Wave-0 stub specs | 6558b64 |

## Threat Flags

None — no new network endpoints, auth paths, or schema changes beyond the plan's threat model (T-26.5-01-SC mitigated by the executed human gate; T-26.5-01-02 mitigated by lazy server-side-only client construction, key never NEXT_PUBLIC_*).

## Self-Check: PASSED

- src/lib/agent-layer/model-constants.ts + voyage-client.ts exist
- 9 spec files in tests/phase26.5/
- Commits 8e95e4c + 6558b64 present in git log
