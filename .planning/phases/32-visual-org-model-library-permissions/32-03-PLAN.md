---
phase: 32-visual-org-model-library-permissions
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - scripts/assert-phase32-day-one-equivalence.ts
autonomous: false
requirements: [SC-2, SC-6]
user_setup:
  - service: supabase
    why: "Schema push to the live Postgres — types come from config, so build/tsc pass WITHOUT the push, creating a false-positive verification state"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: ".env.local (already present from Phase 13/25/26 pushes) — required for non-TTY `npx supabase db push`"
must_haves:
  truths:
    - "All 7 new tables + departments.area_id exist in the live DB (to_regclass, not PostgREST REST)"
    - "sop_departments row content is byte-identical before and after the migration (day-one cutover safety, D-03)"
    - "sops_visible_by_person_grant policy and sop_in_user_person_grants() function exist in the live catalog"
  artifacts:
    - path: "scripts/assert-phase32-day-one-equivalence.ts"
      provides: "pre/post sop_departments snapshot compare + table-existence via to_regclass, PGRST205-tolerant"
      contains: "to_regclass"
  key_links:
    - from: "scripts/assert-phase32-day-one-equivalence.ts"
      to: "live Postgres"
      via: "Management API raw SQL (bypasses PostgREST schema cache)"
      pattern: "to_regclass"
---

<objective>
[BLOCKING] Apply migrations 00046 + 00047 to the live Supabase database, then prove: (a) all 7 new tables + `departments.area_id` exist, (b) `sop_departments` is byte-identical pre/post migration (day-one cutover safety D-03), (c) the D-13 person-grant RLS arm (`sops_visible_by_person_grant` policy + `sop_in_user_person_grants()` function) exists in the live catalog.

Purpose: build + tsc pass WITHOUT a push because types come from config, not the live DB — that is a false-positive. This plan is the mandatory schema-push gate before any dependent server-action/UI plan (32-04+) can be truthfully verified.
Output: applied schema + a runnable assertion script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md
@supabase/migrations/00046_org_model_schema.sql
@supabase/migrations/00047_org_model_data.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the day-one equivalence + table-existence assertion script</name>
  <read_first>
    - .planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md (Pitfall 5 — PostgREST PGRST205 schema-cache staleness; use to_regclass raw SQL via Management API)
    - scripts (grep for a prior post-migration assertion script that reads via the Supabase Management API / to_regclass, e.g. an apply-phase25-migrations style script, to copy the connection idiom)
    - CLAUDE.md §Learnings [2026-06-15] (PGRST205 vs 42P01), [2026-05-08] anon key name NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
  </read_first>
  <files>scripts/assert-phase32-day-one-equivalence.ts</files>
  <action>
    Standalone `tsx` script with two modes controlled by an argv flag: `--capture` (snapshot every `sop_departments` row to a temp JSON in the OS temp dir, ordered by sop_id,department_id) and `--verify` (re-read `sop_departments`, assert row set is byte-identical to the captured snapshot; then assert table existence for `areas, roles, role_members, collections, sop_collections, access_grants, sop_access_people` and column `departments.area_id` via `to_regclass('public.<t>')` raw SQL through the Supabase Management API — NOT the PostgREST REST client — so PGRST205 schema-cache staleness cannot cause a false MISSING; also assert `sop_in_user_person_grants` exists via `to_regprocedure` and `sops_visible_by_person_grant` exists via `pg_policies`). Print PASS/FAIL per check; exit non-zero on any FAIL. Retry the existence probes once after `NOTIFY pgrst, 'reload schema'` if a probe returns empty.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "assert-phase32-day-one-equivalence" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Script compiles under `npx tsc --noEmit` (0 errors referencing it)
    - Contains `to_regclass` and a Management API raw-SQL call (not `.from('...').select()` for existence checks)
    - `--capture` and `--verify` modes both implemented; `--verify` exits non-zero on any mismatch or missing object
    - Reads the anon/service key by the project's real env var names ([2026-05-08])
  </acceptance_criteria>
  <done>Script exists, compiles, capture+verify modes implemented, PGRST205-tolerant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Capture snapshot, push schema, verify day-one equivalence</name>
  <read_first>
    - CLAUDE.md §Learnings [2026-06-15] (schema-cache staleness), [2026-07-05] (SECURITY DEFINER lockdown)
  </read_first>
  <files>supabase/migrations/00046_org_model_schema.sql, supabase/migrations/00047_org_model_data.sql (applied, not edited)</files>
  <action>
    Run three commands in strict order, automatically: (1) `npx tsx scripts/assert-phase32-day-one-equivalence.ts --capture` to snapshot pre-migration `sop_departments`; (2) `npx supabase db push` with `SUPABASE_ACCESS_TOKEN` set (non-TTY) to apply 00046 + 00047 to the live DB; (3) `npx tsx scripts/assert-phase32-day-one-equivalence.ts --verify` to assert all 7 tables + `departments.area_id` exist (to_regclass), `sop_departments` is byte-identical to the capture, and `sops_visible_by_person_grant` + `sop_in_user_person_grants` exist. If `db push` fails on a missing `SUPABASE_ACCESS_TOKEN`, set it from `.env.local` and retry. If an existence probe reports MISSING right after push, re-run `--verify` (PGRST205 schema-cache staleness) before concluding failure. This is a blocking checkpoint because it mutates the live production schema.
  </action>
  <what-built>
    An automated sequence: (1) `npx tsx scripts/assert-phase32-day-one-equivalence.ts --capture` (pre-migration sop_departments snapshot), (2) `npx supabase db push` (applies 00046 + 00047 with `SUPABASE_ACCESS_TOKEN` set for non-TTY), (3) `npx tsx scripts/assert-phase32-day-one-equivalence.ts --verify` (table existence + person-grant arm + sop_departments unchanged). The executor runs all three automatically; this checkpoint exists only to confirm the live push succeeded and the equivalence assertion passed on the real DB.
  </what-built>
  <how-to-verify>
    1. Confirm the executor's `--capture` ran BEFORE `db push` (snapshot must predate the migration)
    2. Confirm `npx supabase db push` reported 00046 and 00047 applied (no error)
    3. Confirm `--verify` printed PASS for all 8 existence checks + the sop_departments byte-identical check + the sops_visible_by_person_grant / sop_in_user_person_grants checks
    4. If `db push` fails on `SUPABASE_ACCESS_TOKEN` missing, set it from .env.local and retry
    5. If any existence probe reports MISSING immediately after push, re-run `--verify` (PGRST205 schema-cache staleness — CLAUDE.md 2026-06-15) before concluding failure
  </how-to-verify>
  <verify>
    <automated>npx tsx scripts/assert-phase32-day-one-equivalence.ts --verify</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push` applied both migrations to the live DB
    - `--verify` exits 0: all 7 tables + area_id exist, sop_departments byte-identical, person-grant policy + function present
  </acceptance_criteria>
  <done>Schema live on the real DB; day-one equivalence + person-grant arm confirmed by `--verify`.</done>
  <resume-signal>Type "approved" once db push succeeded and --verify is green, or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live worker read path | The new person-grant arm changes what workers can SELECT; day-one seed must not widen visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Tampering | day-one visibility regression | mitigate | `--capture`/`--verify` proves `sop_departments` byte-identical across the migration (D-03) |
| T-32-03-02 | Information Disclosure | false-negative table probe (PGRST205) | mitigate | Existence via `to_regclass` Management API raw SQL + NOTIFY reload retry, not PostgREST REST |
| T-32-03-03 | Elevation of Privilege | person-grant arm live | mitigate | `--verify` confirms `sop_in_user_person_grants` exists and is the self-scoping definition from 00046 |
</threat_model>

<verification>
- `npx tsx scripts/assert-phase32-day-one-equivalence.ts --verify` exits 0 against the live DB
- `npx supabase db push` clean
</verification>

<success_criteria>
Schema live; day-one equivalence proven; person-grant arm present. Dependent plans (32-04+) can now verify truthfully.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-03-SUMMARY.md` when done.
</output>
