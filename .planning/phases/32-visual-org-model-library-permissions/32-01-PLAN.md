---
phase: 32-visual-org-model-library-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase32/org-chart-build.spec.ts
  - tests/phase32/resolve-access.spec.ts
  - tests/phase32/wiring-at-scale.spec.ts
  - tests/phase32/library-filter-deeplink.spec.ts
  - tests/phase32/wire-up-mode.spec.ts
  - tests/phase32/banner-slot-stability.spec.ts
  - tests/phase32/grants-org-isolation.spec.ts
  - tests/phase32/person-grant-rls.spec.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "npx playwright test --list --project=phase32 lists all 8 stub specs"
    - "The phase32 project runs green (all stubs test.fixme or vacuous) at phase head"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase32 project with broad tests/phase32/** testMatch"
      contains: "phase32"
    - path: "tests/phase32/grants-org-isolation.spec.ts"
      provides: "org-isolation stub (flipped live in 32-05)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase32/*.spec.ts"
      via: "testMatch regex"
      pattern: "tests\\\\/phase32"
---

<objective>
Wave 0 Nyquist harness for Phase 32: register a `phase32` Playwright project and drop one test.fixme stub spec per success criterion (SC-1..SC-6) plus the two project-learning-mandated runtime guards (grants org-isolation, person-grant RLS arm). Later plans flip each stub live in the plan that ships the code it tests.

Purpose: CLAUDE.md [2026-05-25] — a spec not in any project regex NEVER runs. Establish the single registration point before any Phase 32 code exists so no later plan has to touch playwright.config.ts.
Output: `phase32` project + 8 stub specs, all discoverable via `--list`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-visual-org-model-library-permissions/32-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols/files this phase introduces (exclude from source-drift acknowledgement across all Phase 32 plans):
- Tables: `areas`, `roles`, `role_members`, `collections`, `sop_collections`, `access_grants`, `sop_access_people`; column `departments.area_id`
- Enum: `grant_subject_type`
- SQL fns/policies: `sop_in_user_person_grants()`, `sops_visible_by_person_grant`
- Server actions: `src/actions/org-model.ts`, `src/actions/grants.ts`
- Lib: `src/lib/org-model/resolve-access.ts`, `src/lib/org-model/auto-layout.ts`
- Components: `src/components/admin/org-model/{OrgChartCanvas,OrgColumnsBoard,ViewToggle}.tsx`, `src/components/admin/wiring/{WiringPatchBay,SelectionStrip}.tsx`
- Routes changed: `/admin/team` (becomes org chart), `/admin/sops?view=access` (new arm)
- Scripts: `scripts/assert-phase32-day-one-equivalence.ts`
- Playwright project: `phase32`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register the phase32 Playwright project</name>
  <read_first>
    - playwright.config.ts (read the phase30 project block, lines ~300-319, as the exact analog)
    - .planning/phases/32-visual-org-model-library-permissions/32-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <files>playwright.config.ts</files>
  <action>
    Add one new project object to the `projects` array named `phase32`, mirroring the `phase30` block verbatim: `testDir: '.'`, `testMatch: /tests\/phase32\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`. Add a header comment mirroring phase30's (single registration point, CLAUDE.md 2026-05-25 reference, verify command `npx playwright test --list --project=phase32`). Do NOT modify any existing project. Project name is `phase32` (not `phase32-stubs`) to match the phase26/28/29/30 bare-name convention and the 32-PATTERNS.md analog.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase32 2>&1 | grep -c "tests/phase32" || true</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` contains `name: 'phase32'` with `testMatch: /tests\/phase32\/.*\.(spec|test)\.ts$/`
    - `npx playwright test --list --project=phase32` exits 0 and lists the 8 stub files from Task 2 (zero discovered = FAIL)
    - No existing project block is modified (git diff shows only an added block)
  </acceptance_criteria>
  <done>The phase32 project is registered and discovers every tests/phase32/*.spec.ts file.</done>
</task>

<task type="auto">
  <name>Task 2: Create 8 stub specs (one per SC + 2 guards)</name>
  <read_first>
    - tests/phase30 (read any one existing stub spec for the test.fixme + real-path-constant shape)
    - .planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md (§ Validation Architecture — the Requirement→Test Map naming each spec's behavior)
  </read_first>
  <files>tests/phase32/org-chart-build.spec.ts, tests/phase32/resolve-access.spec.ts, tests/phase32/wiring-at-scale.spec.ts, tests/phase32/library-filter-deeplink.spec.ts, tests/phase32/wire-up-mode.spec.ts, tests/phase32/banner-slot-stability.spec.ts, tests/phase32/grants-org-isolation.spec.ts, tests/phase32/person-grant-rls.spec.ts</files>
  <action>
    Create the 8 stub spec files. Each uses `test.fixme(...)` (or a vacuously-passing `fs.existsSync` guard for source-contract specs) with a descriptive title matching its SC row, and declares the REAL path constant of the artifact it will assert on later (so the flip in the owning plan is a one-line change from fixme to live). Mapping: org-chart-build→SC-1 (/admin/team org chart + Columns toggle + vacancy chips), resolve-access→SC-2 (`src/lib/org-model/resolve-access.ts` 5-level union), wiring-at-scale→SC-3 (WiringPatchBay grouped/focus at 15×20), library-filter-deeplink→SC-4 (`/admin/sops?departments=…|collection=…`), wire-up-mode→SC-5 (connect mode + blast radius), banner-slot-stability→SC-6 (SelectionStrip fixed 48px slot, getBoundingClientRect().top pixel-identical), grants-org-isolation→cross-tenant write blocked on access_grants/sop_collections/role_members (flipped LIVE in 32-05, NOT test.fixme per [2026-06-15]), person-grant-rls→D-13 person grant makes an SOP visible via the new RLS arm (flipped in 32-05). Add a top-of-file comment in each naming the plan that flips it live.
  </action>
  <verify>
    <automated>npx playwright test --project=phase32 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist under `tests/phase32/`
    - `npx playwright test --project=phase32` exits 0 (all fixme/vacuous — no red at phase head)
    - Each spec file names its owning-plan flip point in a comment (32-05, 32-06, 32-07, 32-08, 32-09 as appropriate)
    - grants-org-isolation.spec.ts and person-grant-rls.spec.ts contain a comment marking them as [2026-06-15]-mandated real runtime inserts to be flipped live in 32-05 (not permanent fixme)
  </acceptance_criteria>
  <done>All 8 stubs exist, discoverable, green-at-head.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness only | No runtime surface; config + fixme specs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Repudiation | unregistered spec | mitigate | Verify discoverability with `--list --project=phase32` (CLAUDE.md 2026-05-25) — a spec that never runs silently stops guarding |
| T-32-01-SC | Tampering | npm installs | accept | Zero new dependencies this phase (32-RESEARCH Package Legitimacy Audit) — no install task |
</threat_model>

<verification>
- `npx playwright test --list --project=phase32` lists 8 files
- `npx playwright test --project=phase32` green
</verification>

<success_criteria>
phase32 project registered; 8 stubs discoverable and green-at-head.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-01-SUMMARY.md` when done.
</output>
