---
phase: 32-visual-org-model-library-permissions
plan: 07
type: execute
wave: 5
depends_on: ["32-06", "32-04"]
files_modified:
  - src/components/admin/org-model/OrgColumnsBoard.tsx
  - src/app/(protected)/admin/team/page.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
  - tests/phase32/org-chart-build.spec.ts
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "/admin/team renders the org model: Node Chart default with a ⊞ Chart / ▤ Columns in-page toggle"
    - "Columns view shows departments as columns, roles as cards, people (named + vacancy) as chips, absorbing the old member list"
    - "AdminNav stays 5 tabs; Team tab still lands /admin/team (UX-02 preserved, D-08)"
    - "journeys.ts reflects the org-model Team semantics — /pathways shows 0 not-mapped for /admin/team"
  artifacts:
    - path: "src/app/(protected)/admin/team/page.tsx"
      provides: "org-model surface (chart + columns) at /admin/team (D-08)"
    - path: "src/components/admin/org-model/OrgColumnsBoard.tsx"
      provides: "Columns alt view"
  key_links:
    - from: "src/app/(protected)/admin/team/page.tsx"
      to: "listOrgTree + OrgChartCanvas + OrgColumnsBoard + ViewToggle"
      via: "server-fetch tree, client view switch"
      pattern: "listOrgTree"
---

<objective>
Make `/admin/team` the org model (D-08): server-fetch the org tree, render the Node Chart (32-06) by default with a ⊞ Chart / ▤ Columns in-page toggle, and build the `OrgColumnsBoard` alt view (departments as columns, roles as cards, people + vacancies as chips) that absorbs the current member list. Update `journeys.ts` (and `uat/tests.ts` if team-review-worthy) in the SAME change (D-10) so `/pathways` shows 0 not-mapped.

Purpose: the Team tab BECOMES the org — AdminNav stays 5 tabs (UX-02). Columns wins on raw entry speed, chart on comprehension; the toggle resolves the tension (org-model-views.md). The existing member roster is absorbed into Columns, not deleted.
Output: Columns board + rewritten team page + journeys/uat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/skills/sketch-findings-SOPstart/references/org-model-views.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@src/app/(protected)/admin/team/page.tsx
@src/components/admin/AdminNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrgColumnsBoard (Columns alt view)</name>
  <read_first>
    - .claude/skills/sketch-findings-SOPstart/references/org-model-views.md (§ Column Builder — horizontal overflow-x flex board, flex: 0 0 250px per department column, roles as cards, people as chips; add-in-place dashed ghosts, no modal)
    - src/components/admin/org-model/OrgChartCanvas.tsx (reuse the person-chip / vacancy chip / capacity-count JSX so both views share styling)
    - src/app/(protected)/admin/team/page.tsx (the current RoleAssignmentTable member roster being absorbed — reuse its member data source getTeamMembersWithEmails via listOrgTree)
  </read_first>
  <files>src/components/admin/org-model/OrgColumnsBoard.tsx</files>
  <action>
    `'use client'` component taking the same `OrgTree` prop as OrgChartCanvas. Horizontal `overflow-x` flex board, one `flex: 0 0 250px` column per department, roles as cards inside, people (named) + vacancies (dashed chip) as chips with capacity counts. Add-in-place dashed ghost affordances (`+ Add role`, `+ person`, `+ ADD DEPARTMENT`) wired to the 32-04 actions (createRole/assignRoleMembers/createDepartment). No modal for structure building. Reuse the person-chip/vacancy/capacity JSX from OrgChartCanvas (share, don't fork). The old member roster (role assignment) is represented here — a person's org-role edit path stays reachable (keep RoleAssignmentTable's role-edit affordance available from the columns view or a sub-panel; do not delete the role-edit capability).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "OrgColumnsBoard" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `OrgColumnsBoard.tsx` starts `'use client'`, renders one column per department (`flex: 0 0 250px`)
    - Renders named + vacancy chips with capacity counts (shares the chip JSX with OrgChartCanvas)
    - Org-role edit capability remains reachable (not silently dropped when absorbing the roster)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Columns view renders depts/roles/people/vacancies and preserves role editing.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /admin/team + journeys + uat</name>
  <read_first>
    - src/app/(protected)/admin/team/page.tsx (current file — getSessionContext guard, listDepartments fetch, AdminNav active="team")
    - src/lib/journeys/journeys.ts (lines ~228-248 admin journey — the `team` step at route /admin/team to extend; the "All screens" coverage is read live from the route tree)
    - src/lib/uat/tests.ts (shape of a UAT entry, if adding a team-review item)
    - CLAUDE.md §Pathways Map Maintenance (D-10 — journeys.ts updated in the SAME commit as the flow change; /pathways must show 0 not-mapped)
  </read_first>
  <files>src/app/(protected)/admin/team/page.tsx, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts, tests/phase32/org-chart-build.spec.ts</files>
  <action>
    Rewrite `team/page.tsx` (server component): keep the `getSessionContext()` + admin/safety_manager role guard verbatim, replace the department/RoleAssignmentTable body with `listOrgTree()` (32-04) and render a client shell that mounts `ViewToggle` (⊞ Chart default / ▤ Columns) switching between `OrgChartCanvas` and `OrgColumnsBoard`. Keep `AdminNav active="team"` (5-tab lock, UX-02). Update `journeys.ts`: extend the existing `team` step detail to describe the org-model surface (Node Chart default + Columns toggle, roles + vacancies) — do NOT add a new route (still /admin/team). Add a UAT entry in `uat/tests.ts` for the org-chart build flow if team-review-worthy. Flip `tests/phase32/org-chart-build.spec.ts` live (SC-1: assert /admin/team renders the chart, the toggle switches to Columns, vacancy chips present) — degrade to source-contract if chromium binary unavailable (Rule-3), keeping a live assertion that the page imports OrgChartCanvas + ViewToggle + OrgColumnsBoard.
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/org-chart-build.spec.ts --project=phase32 2>&1 | tail -5; grep -c "OrgChartCanvas\|ViewToggle\|listOrgTree" "src/app/(protected)/admin/team/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `team/page.tsx` keeps the getSessionContext admin/safety_manager guard and `AdminNav active="team"`
    - Page fetches `listOrgTree()` and mounts a ViewToggle switching OrgChartCanvas ↔ OrgColumnsBoard
    - `journeys.ts` `team` step describes the org-model surface; route stays `/admin/team` (no new not-mapped screen)
    - `org-chart-build.spec.ts` flipped live (no permanent test.fixme) and passes
  </acceptance_criteria>
  <done>/admin/team is the org model; journeys/uat updated in the same change; SC-1 spec live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/team route | Admin/safety_manager only; guard preserved from current page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07-01 | Elevation of Privilege | team page auth | mitigate | getSessionContext admin/safety_manager guard kept verbatim; presentation-only rewrite |
| T-32-07-02 | Repudiation | stale pathways map | mitigate | journeys.ts updated in the same change (D-10); /pathways 0 not-mapped verified |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx playwright test --project=phase32` green
- /pathways "All screens" shows 0 not-mapped for /admin/team
</verification>

<success_criteria>
/admin/team is the org model (chart default + columns toggle), member roster absorbed, journeys/uat current.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-07-SUMMARY.md` when done.
</output>
