---
phase: 32-visual-org-model-library-permissions
plan: 04
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - src/types/org-model.ts
  - src/lib/org-model/resolve-access.ts
  - src/actions/org-model.ts
  - src/lib/org-model/__tests__/resolve-access.test.ts
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "resolveEffectiveAccess unions grants up the 5-level chain org→area→department→role→person with direct/inherited/personal vocabulary"
    - "listOrgTree returns the caller's org as areas → departments → roles → people (named + vacancy) with capacity counts"
    - "createArea/updateArea/createRole/updateRole/assignRoleMembers write with org self-enforcement, never trusting client ids"
  artifacts:
    - path: "src/lib/org-model/resolve-access.ts"
      provides: "pure 5-level union resolver shared by every view"
      exports: ["resolveEffectiveAccess"]
    - path: "src/actions/org-model.ts"
      provides: "areas/roles/role_members CRUD + listOrgTree reader"
      exports: ["listOrgTree", "createArea", "createRole", "assignRoleMembers"]
    - path: "src/types/org-model.ts"
      provides: "Area/DeptRole/OrgTree/EffectiveAccess types (no bare `role` naming)"
  key_links:
    - from: "src/actions/org-model.ts"
      to: "createAdminClient + callerOrgId"
      via: "org self-enforcement on every write (Phase 25 pattern)"
      pattern: "createAdminClient"
---

<objective>
Build the org-model data layer: the pure 5-level access resolver, the org-model TypeScript types, and the `org-model.ts` server actions (areas + roles + role_members CRUD + `listOrgTree` reader). This is the API/backend tier every Phase 32 view consumes — one resolver, one tree reader, no view-private state.

Purpose: RESEARCH Pattern 2 — `resolveEffectiveAccess()` is ONE function reused by chart badges, wiring, matrix, blast-radius, and library-filter counts; computing it per-view is exactly the drift class the phase warns about. `org-model.ts` mirrors `departments.ts` verbatim (admin-client writes + org self-enforcement).
Output: types + resolver + actions + a real unit test for the resolver.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@.planning/phases/32-visual-org-model-library-permissions/32-PATTERNS.md
@src/actions/departments.ts
@.claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure 5-level resolver + org-model types</name>
  <read_first>
    - .claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md (§ Inheritance resolution JS block — the literal reference algorithm; § Access-state vocabulary direct/inherited/personal)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-06 five-level chain; D-11 additive-only)
    - src/types/sop.ts (grep for `Role` before naming — Pitfall 2: never name a type bare `Role`; use `DeptRole`)
  </read_first>
  <files>src/types/org-model.ts, src/lib/org-model/resolve-access.ts, src/lib/org-model/__tests__/resolve-access.test.ts</files>
  <behavior>
    - Grant on 'org' subject → every unit inherits that collection, labelled inherited-via 'org'
    - Grant on 'department' → that dept has it direct; its roles + people inherit it labelled inherited-via that department
    - Personal grant on a 'person' the dept does NOT otherwise have → that person has it as `personal`, dept unaffected (the Priya scenario)
    - Effective access of a unit = union over its ancestor chain; direct beats inherited beats absent
    - Same collection granted at two ancestors → resolves once, source = nearest ancestor
  </behavior>
  <action>
    In `src/types/org-model.ts` define: `SubjectType = 'org'|'area'|'department'|'role'|'person'`; `Area`, `DeptRole` (id, organisationId, departmentId, name, budgetedCount, filledCount), `OrgPerson` (id|null for vacancy, name, isVacancy), `OrgTree` (org → areas → departments → roles → people), `AccessGrant` ({subjectType, subjectId, collectionId}), and `EffectiveAccess` (`{ direct: Set<collectionId>, inherited: Record<collectionId, ancestorUnitId>, personal: Set<collectionId> }`). NEVER a bare `Role` type — use `DeptRole` (Pitfall 2). In `resolve-access.ts` export `resolveEffectiveAccess(chain, grantsByUnit)` (pure, no I/O) translating the permission-wiring-views.md `direct`/`inherited` block, extended to 5 chain levels per D-06, adding the `personal` bucket for person-level grants. Additive-only (D-11): no exclusion/negative-grant logic.
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/resolve-access.spec.ts --project=phase32 2>&1 | tail -5 || npx tsc --noEmit 2>&1 | grep -c resolve-access</automated>
  </verify>
  <acceptance_criteria>
    - `resolveEffectiveAccess` is a pure export (no `createClient`/`fetch`/`import 'server-only'`)
    - Unit test in `src/lib/org-model/__tests__/resolve-access.test.ts` covers all 5 behavior cases incl. the Priya personal-grant case; passes
    - No `Role` type declared in org-model.ts (grep confirms `DeptRole`)
    - `EffectiveAccess` distinguishes `direct` vs `inherited` (with source) vs `personal`
  </acceptance_criteria>
  <done>Resolver + types exist, pure, unit-tested for the 5-level union incl. personal grants.</done>
</task>

<task type="auto">
  <name>Task 2: org-model.ts server actions (areas/roles/role_members CRUD + listOrgTree)</name>
  <read_first>
    - src/actions/departments.ts (THE template — requireAdmin/AdminCtx lines 34-46, Zod enum input lines 56-82, orgScopedDeptIds+callerOrgId lines 335-361, replace-semantics assigner lines 367-418, discriminated-union returns)
    - src/types/org-model.ts (from Task 1 — the shapes to return)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-05 vacancies=budgeted−filled, D-07 role_members alongside member_departments, NOT derived)
  </read_first>
  <files>src/actions/org-model.ts</files>
  <action>
    `'use server'` module, every export async, `{ data } | { error }` discriminated union (never throw). Use `(supabase as any)` casts for the new tables (Phase 25 idiom — not in database.types.ts). Reuse `requireAdminContext()`, and copy `callerOrgId`/an `orgScopedIds` generalization for org self-enforcement. Exports:
    - `listOrgTree()` → assembles the caller's org into `OrgTree` (areas incl. null-area depts, roles per dept, role_members resolved to people via a members+email fetch like listDepartments, vacancies computed as budgetedCount − filledCount, rendered as `isVacancy` people). Independent reads via `Promise.all` ([2026-07-13] no serial waterfall).
    - `createArea`/`updateArea`/`archiveArea` (colour via `z.enum` of the DEPT_COLOURS set — never free string, V5).
    - `createRole`/`updateRole`/`archiveRole` (Zod: name, budgetedCount int ≥ 0, departmentId verified in caller org before write).
    - `assignRoleMembers(roleId, memberIds[])` — replace-semantics junction write via `createAdminClient()`: verify role ∈ caller org (roles→departments→org join) AND filter memberIds to org members, then delete+insert `role_members` (mirrors assignMemberDepartments). MUST NOT touch `member_departments` (D-07). MUST NOT write `organisation_members.role` (Pitfall 2 — job role ≠ org privilege role).
    - `setDepartmentArea(departmentId, areaId|null)` — verify both belong to caller org, then update `departments.area_id`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "src/actions/org-model" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `org-model.ts` starts with `'use server'` and every value export is async (no sync value export — [2026-06-27])
    - `assignRoleMembers` uses `createAdminClient()` for the write AND verifies role∈org + filters memberIds to org before insert (grep both)
    - No write path references `member_departments` or `organisation_members` `.update(` with a `role` field (D-07 / Pitfall 2)
    - `listOrgTree` computes vacancies as budgetedCount − filled and marks them `isVacancy`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>org-model.ts ships CRUD + listOrgTree with org self-enforcement, no role/dept cross-wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → new junctions | Service-role writes bypass RLS; org scope enforced in the action. |
| job role vs org privilege role | `role_members` (new) must never influence `organisation_members.role`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Tampering | assignRoleMembers cross-tenant | mitigate | Verify role∈callerOrg + filter memberIds to org members before insert ([2026-06-15]) |
| T-32-04-02 | Elevation of Privilege | job-role ↔ org-privilege confusion | mitigate | No code path lets a role_members write set organisation_members.role or JWT claims (Pitfall 2) |
| T-32-04-03 | Input Validation | free-string colour/subject | mitigate | `z.enum` on colour; strict typed inputs (V5) |
| T-32-04-04 | Information Disclosure | resolver drift across views | mitigate | ONE pure resolveEffectiveAccess reused everywhere (RESEARCH Pattern 2) |
</threat_model>

<verification>
- resolver unit test green
- `npx tsc --noEmit` clean
- `npx playwright test --project=phase32` green
</verification>

<success_criteria>
Resolver + types + org-model actions complete; org self-enforcement on every write; resolver behavior proven by unit test.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-04-SUMMARY.md` when done.
</output>
