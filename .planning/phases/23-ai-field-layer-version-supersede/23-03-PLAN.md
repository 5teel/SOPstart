---
phase: 23-ai-field-layer-version-supersede
plan: 03
type: execute
wave: 2
depends_on: ["23-00", "23-01"]
files_modified:
  - src/actions/versioning.ts
  - src/lib/builder/__tests__/clone-restore.test.ts
autonomous: true
requirements: [AFL-VER-01, AFL-VER-03]
must_haves:
  truths:
    - "cloneSopAsDraft deep-copies a published SOP's sections + steps + block junctions into a NEW draft SOP"
    - "The new draft continues the version lineage (parent_sop_id chain, version = max+1) and is created status='uploading' then flipped to 'draft' only after all copies succeed"
    - "Publishing the cloned draft supersedes the prior published version (superseded_by set on publish, NOT on clone)"
    - "restoreVersionAsNew creates a NEW current version copying an OLD version's content — history is append-only; no old row is rewritten/reactivated in place"
    - "A failed section copy deletes the partial draft (cascade) so no orphan/0-section drafts appear"
  artifacts:
    - path: "src/actions/versioning.ts"
      provides: "cloneSopAsDraft + restoreVersionAsNew server actions"
      exports: ["cloneSopAsDraft", "restoreVersionAsNew"]
    - path: "src/lib/builder/__tests__/clone-restore.test.ts"
      provides: "Unit assertions on lineage continuation + append-only invariant"
  key_links:
    - from: "src/actions/versioning.ts cloneSopAsDraft"
      to: "sops + sop_sections + sop_steps + sop_section_blocks"
      via: "admin client deep-copy with status sentinel"
      pattern: "sop_section_blocks"
    - from: "src/actions/versioning.ts restoreVersionAsNew"
      to: "cloneSopAsDraft"
      via: "restore = clone-an-old-version forward (append-only)"
      pattern: "cloneSopAsDraft"
---

<objective>
Add the two version-supersede server actions to `versioning.ts`: `cloneSopAsDraft` (D-05 edit-into-draft clone — the primary supersede entry point) and `restoreVersionAsNew` (D-06 restore-as-new-version, append-only). Both extend the existing `uploadNewVersion` patterns (auth guard, JWT role check, version lineage, admin client, status sentinel). Publishing the cloned draft supersedes the prior version via the EXISTING publish path — clone itself never sets superseded_by.

Purpose: AFL-VER-01 (formal supersede flow) + AFL-VER-03 (one-click restore-as-new). These are the data-layer actions the 23-05 UI wires. The append-only invariant (D-06, STATE.md) is non-negotiable: restore never mutates or reactivates an old row.
Output: 2 server actions + lineage/append-only unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-field-layer-version-supersede/23-CONTEXT.md
@.planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md
@.planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md
@src/actions/versioning.ts
</context>

<artifacts_this_phase_produces>
**Server actions (in versioning.ts):**
- `cloneSopAsDraft(publishedSopId): Promise<{success:true; newDraftId:string} | {success:false; error:string}>`
- `restoreVersionAsNew(oldVersionSopId): Promise<{success:true; newDraftId:string} | {success:false; error:string}>`
**Unit test:** `src/lib/builder/__tests__/clone-restore.test.ts` (phase21.5-unit project — already registered to src/lib/builder/__tests__)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: cloneSopAsDraft — deep-copy published SOP into a new draft</name>
  <files>src/actions/versioning.ts</files>
  <read_first>
    - src/actions/versioning.ts (the file being modified — `uploadNewVersion` lines 11-119 is the exact analog for auth guard, JWT role check, parent_sop_id lineage, version=max+1, admin client, status='uploading' sentinel)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § "src/actions/versioning.ts — extend with cloneSopAsDraft + restoreVersionAsNew" (lines 247-308 — full skeleton incl. cleanup-on-failure)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Pattern 3: clone-to-draft Supersede" (lines 330-340), § Pitfall 2 (orphan sections — near-atomic via status sentinel + batch insert + delete-on-failure), § Pitfall 7 (do NOT re-copy storage objects; sop_images by reference), § anti-patterns (do not set superseded_by on clone)
    - Find the existing publish action and the deep structure of sop_sections → sop_steps → sop_section_blocks (the copy scope; blocks themselves are library items referenced by FK, NOT copied)
    - CLAUDE.md § Learnings 2026-06-15 (admin client + self-enforce org-scope)
  </read_first>
  <action>
    Add `cloneSopAsDraft(publishedSopId)` to versioning.ts. Auth: `createClient()` → getUser → JWT role guard `['admin','safety_manager']` (copy uploadNewVersion lines 23-30). Fetch the source SOP (verify it belongs to the caller's org via the JWT organisation_id — self-enforce org-scope since the copy uses admin client). Compute `newParentId = oldSop.parent_sop_id ?? oldSop.id`, `newVersion = oldSop.version + 1`. With `createAdminClient()`: (1) insert the new sops row `status:'uploading'` (sentinel), copying title/source metadata, `version:newVersion`, `parent_sop_id:newParentId`; (2) batch-copy sop_sections (re-map section ids, preserve section_kind_id/sort_order/layout_data), then sop_steps for each section, then sop_section_blocks junction rows (re-point sop_section_id to new section ids; do NOT copy the blocks themselves — they are shared library items, FK only); copy sop_images by reference per Pitfall 7/A3 (new section/step FK, SAME storage_path — never re-upload). Prefer batch `insert([...rows])`. (3) On ANY copy failure: `admin.from('sops').delete().eq('id', newSop.id)` (cascade deletes partial children) and return error. (4) On full success: `update({status:'draft'})`. NEVER set `superseded_by` on the old SOP here — that only happens on publish (D-05). Return `{success:true, newDraftId}`.
  </action>
  <verify>
    <automated>grep -n "cloneSopAsDraft\|status: 'draft'\|superseded_by" src/actions/versioning.ts | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `export async function cloneSopAsDraft` present in versioning.ts
    - Copies sop_sections, sop_steps, AND sop_section_blocks (grep all three table names within the function body)
    - Creates the new SOP with status sentinel ('uploading') and flips to 'draft' only after copies succeed
    - On failure path: `admin.from('sops').delete()` cleanup is present (Pitfall 2)
    - The function body does NOT set `superseded_by` on the source SOP (grep: no `.update({ superseded_by` inside cloneSopAsDraft — supersede happens on publish)
    - Org-scope self-enforced (source SOP organisation_id checked against JWT organisation_id before copy)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>cloneSopAsDraft deep-copies content into a new draft; near-atomic; no superseded_by mutation on clone.</done>
</task>

<task type="auto">
  <name>Task 2: restoreVersionAsNew — append-only restore-as-new-version</name>
  <files>src/actions/versioning.ts</files>
  <read_first>
    - src/actions/versioning.ts (cloneSopAsDraft from Task 1 — restore reuses its deep-copy mechanism)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § restoreVersionAsNew note (line 308 — "calls cloneSopAsDraft with an old version's ID; never mutates superseded_by on old rows")
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § anti-patterns "Do not mutate history in the restore flow (D-06)" (line 379)
    - 23-CONTEXT.md D-06 (restore = restore-as-new-version; history append-only; nothing rewritten/reactivated in place)
  </read_first>
  <action>
    Add `restoreVersionAsNew(oldVersionSopId)` to versioning.ts. It restores an OLD (possibly superseded) version by copying its content FORWARD into a new draft — implement by delegating to `cloneSopAsDraft(oldVersionSopId)` (the deep-copy + lineage continuation is identical; the source is just an old version rather than the current published one). The new draft then follows the normal publish path to become current, which supersedes the prior current version. CRITICAL (D-06): never `UPDATE sops SET superseded_by = NULL` or otherwise reactivate the old row in place; never point superseded_by backward. Return `{success:true, newDraftId}`. Add a code comment citing D-06 append-only invariant. Admin-only role guard same as clone.
  </action>
  <verify>
    <automated>grep -n "restoreVersionAsNew\|cloneSopAsDraft" src/actions/versioning.ts | head</automated>
  </verify>
  <acceptance_criteria>
    - `export async function restoreVersionAsNew` present
    - It delegates to / reuses cloneSopAsDraft's deep-copy (grep shows cloneSopAsDraft call OR shared helper)
    - No statement reactivates an old row: grep within restoreVersionAsNew shows NO `superseded_by: null`, no `status: 'published'` on an old id, no UPDATE of an old version row's content
    - Admin-only role guard present
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>restoreVersionAsNew copies an old version forward into a new draft; append-only invariant preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lineage + append-only unit tests</name>
  <files>src/lib/builder/__tests__/clone-restore.test.ts</files>
  <read_first>
    - src/lib/builder/__tests__/*.test.ts (existing phase21.5-unit tests — analog structure; this dir is registered as phase21.5-unit in playwright.config.ts so static @/ imports resolve)
    - tests/phase23/version-supersede.spec.ts (the 23-00 source-contract stub — these unit tests complement it with behavioral assertions)
    - .planning/phases/23-ai-field-layer-version-supersede/23-VALIDATION.md § Per-Task Verification Map (AFL-VER-01 clone, AFL-VER-03 restore)
  </read_first>
  <behavior>
    - "lineage continuation": given a source SOP with version N and parent P, the computed new version is N+1 and the new parent_sop_id resolves to P (or source.id when source has no parent). Test the pure lineage-computation helper (extract `computeNextVersionLineage(oldSop)` from cloneSopAsDraft into a pure exported helper so it is unit-testable without a DB).
    - "append-only invariant (source-contract)": read versioning.ts source and assert restoreVersionAsNew contains no `superseded_by: null` and no reactivation of an old row (mirrors the 23-00 stub but as a focused unit assertion).
    Because deep-copy needs a live DB, the runtime insert is covered by the 23-07 phase UAT; this unit test covers the PURE lineage logic + the append-only source contract (no DB).
  </behavior>
  <action>
    Extract a pure exported helper `computeNextVersionLineage(oldSop: {id; version; parent_sop_id}): {newVersion; newParentId}` from cloneSopAsDraft (Task 1 should use it) so it is unit-testable. Create `src/lib/builder/__tests__/clone-restore.test.ts` with static `@/actions/versioning` imports asserting the lineage helper (N→N+1, parent resolution) and a source-contract read asserting restoreVersionAsNew's append-only invariant. Use static imports (phase21.5-unit testDir resolves @/), never dynamic import (CLAUDE.md 2026-06-24).
  </action>
  <verify>
    <automated>npx playwright test --project=phase21.5-unit -g "clone-restore\|lineage" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - clone-restore.test.ts exists and runs under phase21.5-unit (discoverable via --list)
    - lineage helper test passes (N→N+1; parent resolution for both has-parent and is-root cases)
    - append-only source-contract assertion passes
    - Uses static `@/` imports (grep: no `await import(`)
  </acceptance_criteria>
  <done>Lineage + append-only unit tests GREEN under phase21.5-unit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → DB | cloneSopAsDraft/restoreVersionAsNew write via service-role (bypasses RLS) — org-scope must be self-enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | Clone a SOP from another org via admin client | mitigate | Source SOP organisation_id checked against JWT organisation_id before copy (self-enforced org-scope, CLAUDE.md 2026-06-15). |
| T-23-03-02 | Elevation of Privilege | Non-admin triggers a clone/restore | mitigate | JWT role guard `['admin','safety_manager']` on both actions (uploadNewVersion analog). |
| T-23-03-03 | Tampering | Restore rewrites/reactivates history (violates append-only) | mitigate | D-06 invariant enforced: restore copies forward only; unit + source-contract assert no superseded_by:null / no old-row reactivation. |
| T-23-03-04 | Integrity | Partial draft (orphan sections) on copy failure | mitigate | status sentinel + delete-on-failure cleanup (Pitfall 2). |
| T-23-03-SC | Tampering | npm installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx playwright test --project=phase21.5-unit -g "clone-restore\|lineage"` GREEN.
- `npx playwright test --project=phase23-stubs -g "AFL-VER-01\|AFL-VER-03"` passes (source-contract stubs now find the exports).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- cloneSopAsDraft deep-copies sections+steps+junctions into a near-atomic new draft (AFL-VER-01).
- restoreVersionAsNew is append-only restore-as-new (AFL-VER-03, D-06).
- Clone never sets superseded_by (supersede is the publish path).
- Lineage + append-only invariants unit-covered.
</success_criteria>

<output>
Create `.planning/phases/23-ai-field-layer-version-supersede/23-03-SUMMARY.md` when done.
</output>
