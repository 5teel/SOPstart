---
phase: 23-ai-field-layer-version-supersede
plan: 07
type: execute
wave: 4
depends_on: ["23-04", "23-05", "23-06"]
files_modified:
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
autonomous: false
requirements: [AFL-AI-01, AFL-AI-02, AFL-AI-03, AFL-VER-01, AFL-VER-02, AFL-VER-03, AFL-VER-04, AFL-VER-05]
must_haves:
  truths:
    - "journeys.ts reflects the new roster kiosk login, version supersede/diff/restore, and worker+supervisor sign-off flows (CLAUDE.md same-change rule)"
    - "The /pathways 'All screens' view shows 0 not-mapped for every new route (/login/kiosk, /admin/sops/[sopId]/versions/diff)"
    - "uat/tests.ts has team-review entries for the phase's human-UAT items"
    - "The full Playwright suite is green and tsc is clean across the whole phase"
    - "First Load JS for /sops/[sopId] stays within the established bundle tolerance (the badge is a small addition; AI-field backbone is server/API only)"
  artifacts:
    - path: "src/lib/journeys/journeys.ts"
      provides: "Roster login + version/diff/restore + sign-off pathways"
      contains: "kiosk"
    - path: "src/lib/uat/tests.ts"
      provides: "Phase 23 team-review UAT entries"
  key_links:
    - from: "src/lib/journeys/journeys.ts"
      to: "/login/kiosk + /admin/sops/[sopId]/versions/diff"
      via: "Journey steps with real routes (coverage + deep-link)"
      pattern: "versions/diff|login/kiosk"
---

<objective>
Close the phase: update `src/lib/journeys/journeys.ts` (the /pathways single source of truth) and `src/lib/uat/tests.ts` (the /uat team-review hub) to reflect every new route and flow shipped this phase — roster kiosk login, version supersede/diff/restore, and the worker+supervisor sign-off chain — per the CLAUDE.md same-change rule. Then run the full phase verification gate (whole Playwright suite + tsc + bundle check) and confirm 0 not-mapped screens.

Purpose: CLAUDE.md project rule — "a new route or changed flow is not done until journeys.ts reflects it" (and uat/tests.ts when worth team review). This plan also serves as the phase-wide green gate before /gsd-verify-work.
Output: journeys.ts + uat/tests.ts updates + a final full-suite verification + 0-not-mapped confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-field-layer-version-supersede/23-CONTEXT.md
@src/lib/journeys/journeys.ts
@src/lib/uat/tests.ts
</context>

<artifacts_this_phase_produces>
**journeys.ts:** new/updated Journey entries for (a) worker roster kiosk login → complete SOP → sign-off; (b) admin version supersede (edit-into-draft clone → publish supersedes); (c) admin version diff/compare; (d) admin restore-as-new-version; (e) supervisor counter-sign.
**uat/tests.ts:** team-review entries for the 3 manual-only verifications (roster login + RLS isolation, inline AI proposal Accept/Reject, updated-since badge timing).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update journeys.ts for all new routes + flows (same-change rule)</name>
  <files>src/lib/journeys/journeys.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts (the file being modified — Journey/JourneyStep types, JOURNEY_GROUPS, existing worker + admin journeys; route values must be REAL paths for coverage + deep-link)
    - CLAUDE.md § "Pathways Map Maintenance (journeys.ts) — GSD triggers" (the /pathways All-screens view flags any screen no journey covers; new routes must be added in the same change)
    - 23-CONTEXT.md (the full flow set: roster login, clone-to-draft supersede, diff, restore-as-new, worker+supervisor sign-off)
    - The new routes shipped this phase: `/login/kiosk` (23-06), `/admin/sops/[sopId]/versions/diff` (23-05); plus changed flows on the existing versions page + activity sign-off + worker SOP card badge
  </read_first>
  <action>
    Add/extend Journey entries so the /pathways map reflects this phase's real flows: (1) Worker (kiosk) — `/login/kiosk` name-select → `/sops/[sopId]` → walkthrough → complete (worker signature). (2) Admin — version supersede: versions page → "Edit into new version" (clone) → `/admin/sops/builder/[sopId]` → publish (supersedes prior). (3) Admin — compare: versions page → `/admin/sops/[sopId]/versions/diff?a=&b=`. (4) Admin — restore-as-new from versions page. (5) Supervisor — counter-sign at `/activity/[completionId]`. (6) Worker — updated-since badge on `/sops`. Use real `route` values so coverage ticks off and deep-links work. Cover BOTH new routes (`/login/kiosk`, `versions/diff`) so the All-screens view shows them mapped.
  </action>
  <verify>
    <automated>grep -n "login/kiosk\|versions/diff" src/lib/journeys/journeys.ts</automated>
  </verify>
  <acceptance_criteria>
    - journeys.ts contains a step with route `/login/kiosk` and a step with route `/admin/sops/[sopId]/versions/diff`
    - Journeys cover clone-supersede, diff/compare, restore-as-new, worker self-sign, and supervisor counter-sign flows
    - All added `route` values are real paths (no placeholders)
    - `npx tsc --noEmit` clean (journeys.ts is typed config)
  </acceptance_criteria>
  <done>journeys.ts reflects every new route + flow; both new routes are mappable on /pathways.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 23 team-review entries to uat/tests.ts</name>
  <files>src/lib/uat/tests.ts</files>
  <read_first>
    - src/lib/uat/tests.ts (the file being modified — existing UAT test entry shape; the /uat page renders entirely from this config; responses stored in Supabase, AI reads GET /api/uat/export)
    - .planning/phases/23-ai-field-layer-version-supersede/23-VALIDATION.md § "Manual-Only Verifications" (the 3 items: roster login + RLS isolation; inline AI proposal Accept/Reject; updated-since badge timing)
    - CLAUDE.md § Pathways/UAT maintenance (update uat/tests.ts when a change is worth team review)
  </read_first>
  <action>
    Add team-review UAT entries mirroring the 3 manual-only verifications from 23-VALIDATION.md: (1) Roster name-select kiosk login establishes an org-scoped session + signature attribution + RLS isolation (AFL-VER-05/D-11). (2) Inline AI proposal Accept/Reject diff at a published-SOP field (AFL-AI-02/D-03). (3) "Updated since last completion" badge on the SOP card + walkthrough entry (AFL-VER-04). Follow the existing entry shape exactly (id, title, steps, route deep-links). These feed the /uat hub for team feedback.
  </action>
  <verify>
    <automated>grep -ni "roster\|kiosk\|proposal\|updated since\|AFL-" src/lib/uat/tests.ts | head</automated>
  </verify>
  <acceptance_criteria>
    - uat/tests.ts has ≥3 new entries covering the roster login, inline AI proposal Accept/Reject, and updated-since badge verifications
    - Each entry follows the existing tests.ts entry shape (renders on /uat without error)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Phase 23 manual verifications are surfaced on the /uat team-review hub.</done>
</task>

<task type="auto">
  <name>Task 3: Full-phase verification gate (suite + tsc + bundle)</name>
  <files>src/lib/journeys/journeys.ts</files>
  <read_first>
    - .planning/phases/23-ai-field-layer-version-supersede/23-VALIDATION.md § "Validation Sign-Off" (full suite green before /gsd-verify-work)
    - .bundle-baseline.json + scripts/check-bundle-size.ts (the established /sops/[sopId] bundle gate from Phase 15/24; tolerance ±2 KB from baseline)
    - CLAUDE.md § Learnings 2026-05-25 (verify every phase23 spec is registered + runs) + 2026-06-02 (run full `npx tsc --noEmit`, not just next build, as the post-merge gate)
  </read_first>
  <action>
    Run the phase-wide green gate: (1) `npx playwright test --list --project=phase23-stubs` and `--project=phase23-unit` both discover their files (registration sanity — CLAUDE.md 2026-05-25). (2) `npm run test` full suite green (or document any pre-existing unrelated reds). (3) `npx tsc --noEmit` clean. (4) Bundle check: run `npx tsx scripts/check-bundle-size.ts` (authoritative exit code per CLAUDE.md 2026-06-02 — npm run build masks postbuild exit codes); confirm /sops/[sopId] First Load JS is within tolerance (the X-03 backbone is server/API-only; the only client addition is the small SopLibraryCard badge). If the badge legitimately pushes the bundle past tolerance, re-baseline with a one-line justification (Phase 15-04 precedent) rather than removing the feature. (5) Confirm /pathways shows 0 not-mapped for the 2 new routes (grep journeys.ts as a proxy, or open the page). This task makes a trivial journeys.ts touch only if a coverage gap is found; otherwise it is verification-only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test --project=phase23-stubs --project=phase23-unit 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both phase23 projects discover their files via --list (non-zero)
    - `npx tsc --noEmit` exits clean
    - `npm run test` full suite green (pre-existing unrelated reds, if any, documented in SUMMARY)
    - Bundle gate passes (or re-baselined with justification per Phase 15-04 precedent)
    - 0 not-mapped for /login/kiosk + /admin/sops/[sopId]/versions/diff on /pathways
  </acceptance_criteria>
  <done>Whole phase is green: suite + tsc + bundle + pathways coverage all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final phase sign-off — pathways + uat + AI-field backbone smoke on sopstart.com</name>
  <what-built>The complete Phase 23 surface on the live deploy: /pathways map with the new flows mapped (0 not-mapped), /uat team-review entries, and the X-03 AI-field backbone (read/write APIs reachable for the future v5.0 agent — no user-facing surface this phase per D-04).</what-built>
  <how-to-verify>
    1. Deploy to sopstart.com (commit + push master).
    2. Open /pathways → confirm the new worker-kiosk, version-supersede/diff/restore, and sign-off journeys appear and the "All screens" view shows 0 not-mapped (no flagged new route).
    3. Open /uat → confirm the 3 new Phase 23 team-review entries render and are answerable.
    4. AI-field backbone smoke: confirm `GET /api/ai-fields/read?fieldId=sop.title&...` returns the current value for a registered field (org-scoped), and that NO user-facing AI command surface exists (D-04 — Cmd+K stays removed; this is backbone-only).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (unmapped routes, uat rendering, read-API behavior, any accidental user-facing AI surface).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public read of /pathways + /uat config | journeys.ts + uat/tests.ts are static config rendered to authed users; no secrets; no new runtime trust boundary. |
| agent → GET /api/ai-fields/read | Smoke-tested here; full mitigation in 23-02 (org-scoped session client). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-01 | Repudiation | Shipped route not reflected in journeys.ts (stale pathways map) | mitigate | Task 1 adds both new routes; Task 3 confirms 0 not-mapped (CLAUDE.md same-change rule). |
| T-23-07-02 | Information Disclosure | A user-facing AI surface accidentally ships (D-04 violation) | mitigate | Task 4 smoke confirms backbone-only (no command surface; Cmd+K stays removed). |
| T-23-07-03 | Tampering | Phase ships with an unregistered/never-run spec (false green) | mitigate | Task 3 verifies both phase23 projects discover files via --list (CLAUDE.md 2026-05-25). |
| T-23-07-SC | Tampering | npm installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `grep "login/kiosk\|versions/diff" src/lib/journeys/journeys.ts` returns both routes.
- `npx tsc --noEmit` clean; `npm run test` green.
- `npx tsx scripts/check-bundle-size.ts` passes (or re-baselined).
- Blocking human sign-off on sopstart.com (pathways + uat + read-API smoke).
</verification>

<success_criteria>
- journeys.ts + uat/tests.ts reflect every new route + flow (same-change rule).
- 0 not-mapped on /pathways for the 2 new routes.
- Whole phase green: suite + tsc + bundle.
- D-04 backbone-only confirmed (no user-facing AI surface).
</success_criteria>

<output>
Create `.planning/phases/23-ai-field-layer-version-supersede/23-07-SUMMARY.md` when done.
</output>
