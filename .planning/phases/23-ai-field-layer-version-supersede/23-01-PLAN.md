---
phase: 23-ai-field-layer-version-supersede
plan: 01
type: execute
wave: 1
depends_on: ["23-00"]
files_modified:
  - supabase/migrations/00038_phase23_schema.sql
  - src/types/database.types.ts
  - scripts/apply-phase23-migration.mjs
autonomous: true
requirements: [AFL-VER-05, AFL-AI-02]
must_haves:
  truths:
    - "sop_completions has a nullable roster_worker_id column (FK to auth.users)"
    - "sop_completion_signatures table exists, append-only (no authenticated INSERT/UPDATE/DELETE policy)"
    - "ai_field_proposals table exists, org-scoped SELECT only, no authenticated write policy"
    - "All three new RLS-enabled objects use current_organisation_id() directly with NO cross-table join to sops/sop_completions (no 42P17 recursion)"
    - "Migration is applied to the live DB and table existence is verified via to_regclass (not just PostgREST schema cache)"
  artifacts:
    - path: "supabase/migrations/00038_phase23_schema.sql"
      provides: "roster_worker_id column + sop_completion_signatures + ai_field_proposals"
      contains: "sop_completion_signatures"
      min_lines: 60
    - path: "src/types/database.types.ts"
      provides: "TypeScript types for new table/column (manually extended)"
      contains: "sop_completion_signatures"
    - path: "scripts/apply-phase23-migration.mjs"
      provides: "Schema push + to_regclass existence verification"
  key_links:
    - from: "supabase/migrations/00038_phase23_schema.sql"
      to: "auth.users"
      via: "roster_worker_id + roster_user_id FK references"
      pattern: "references auth\\.users"
    - from: "scripts/apply-phase23-migration.mjs"
      to: "to_regclass"
      via: "Management API raw SQL existence check"
      pattern: "to_regclass"
---

<objective>
Add migration 00038: the `roster_worker_id` attribution column on `sop_completions` (D-11), the append-only `sop_completion_signatures` table (AFL-VER-05 worker+supervisor sign-off chain), and the `ai_field_proposals` table (AFL-AI-02 high-stake pending approvals). Then run the [BLOCKING] schema push to the live DB and verify table existence via `to_regclass` (PostgREST schema-cache-safe). Extend database.types.ts manually (type regeneration is unavailable in this environment — established Phase 3/4/6/25 pattern).

Purpose: All downstream plans (23-04 writes ai_field_proposals, 23-05 reads superseded versions, 23-06 writes signatures + roster_worker_id) read or write these objects. The schema MUST exist on the live DB before verification — build/type checks pass without the push because types come from config, not the live DB, creating a false-positive verification state (per <schema_push_requirement>).
Output: 1 migration file + database.types.ts extension + apply-and-verify script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md
@.planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md
@supabase/migrations/00035_departments_schema.sql
@supabase/migrations/00010_completion_schema.sql
</context>

<artifacts_this_phase_produces>
**New table:** `public.sop_completion_signatures` (id, organisation_id, completion_id, role CHECK in ('worker','supervisor'), roster_user_id, signed_at)
**New table:** `public.ai_field_proposals` (id, organisation_id, field_id, field_label, context jsonb, current_value jsonb, proposed_value jsonb, status CHECK in ('pending','applied','rejected'), sop_version int nullable, created_at)
**New column:** `public.sop_completions.roster_worker_id uuid REFERENCES auth.users(id)` + `idx_completions_roster_worker`
**New script:** `scripts/apply-phase23-migration.mjs`
**RLS policies:** `sop_completion_signatures_org_read`, `ai_field_proposals_org_read` (org-scoped SELECT only; writes via service-role server actions)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 00038 (roster column + signatures + ai_field_proposals)</name>
  <files>supabase/migrations/00038_phase23_schema.sql</files>
  <read_first>
    - supabase/migrations/00035_departments_schema.sql (exact migration structure analog: begin/commit, ALTER TABLE ADD COLUMN IF NOT EXISTS, org-scoped table with current_organisation_id() SELECT policy)
    - supabase/migrations/00010_completion_schema.sql (append-only completion table — no UPDATE/DELETE policy pattern; worker_id = auth.uid() INSERT policy that roster_worker_id must NOT collide with)
    - supabase/migrations/00031_fix_sops_sub_trades_rls_recursion.sql (42P17 recursion-avoidance pattern — using(true) / no cross-table join)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § "supabase/migrations/00038_phase23_schema.sql" (lines 32-96 — exact DDL skeleton incl. comments)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "D-11 Deep-Dive / Schema additions needed" (lines 428-445) and § "Common Pitfalls" Pitfall 1 (lines 490-498)
    - CLAUDE.md § Learnings 2026-05-13 (RLS recursion), 2026-06-15 (junction/no-write-policy tables need service-role), 2026-05-08 (SQL function rename — N/A here, no function rename, but informs comment convention)
  </read_first>
  <action>
    Create `supabase/migrations/00038_phase23_schema.sql` wrapped in `begin; ... commit;`.
    (1) `alter table public.sop_completions add column if not exists roster_worker_id uuid references auth.users(id);` + `create index if not exists idx_completions_roster_worker on public.sop_completions(roster_worker_id);` + a `comment on column` citing "Phase 23 D-11: roster attribution FK, distinct from worker_id (kiosk uid for RLS), NULL for pre-Phase-23 completions."
    (2) `create table if not exists public.sop_completion_signatures` with columns: id uuid PK default gen_random_uuid(); organisation_id uuid not null references organisations(id) on delete cascade; completion_id uuid not null references sop_completions(id); role text not null check (role in ('worker','supervisor')); roster_user_id uuid not null references auth.users(id); signed_at timestamptz not null default now(). Enable RLS. Add ONLY a SELECT policy `sop_completion_signatures_org_read using (organisation_id = public.current_organisation_id())`. NO authenticated INSERT/UPDATE/DELETE policy (writes via createAdminClient in recordSignature — AFL-VER-05). Add `comment on table` citing CLAUDE.md 2026-06-15.
    (3) `create table if not exists public.ai_field_proposals` with columns: id uuid PK default gen_random_uuid(); organisation_id uuid not null references organisations(id) on delete cascade; field_id text not null; field_label text not null; context jsonb not null default '{}'::jsonb; current_value jsonb; proposed_value jsonb; status text not null default 'pending' check (status in ('pending','applied','rejected')); sop_version int; created_at timestamptz not null default now(). Add `idx_ai_field_proposals_org_status on (organisation_id, status)`. Enable RLS. Add ONLY a SELECT policy `ai_field_proposals_org_read using (organisation_id = public.current_organisation_id())`. NO authenticated write policy. Add a comment: "CRITICAL: do NOT reference public.sops from this policy — 42P17 recursion risk (CLAUDE.md 2026-05-13; RESEARCH Pitfall 1)."
    Every RLS SELECT policy uses `current_organisation_id()` DIRECTLY — zero cross-table EXISTS/join to sops or sop_completions.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/00038_phase23_schema.sql | grep -c "current_organisation_id()"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `add column if not exists roster_worker_id`, `create table if not exists public.sop_completion_signatures`, `create table if not exists public.ai_field_proposals`
    - Both new tables have `enable row level security` and exactly one SELECT policy each (grep `for select` returns exactly 2 across the two new tables)
    - Neither new table's RLS policy references `public.sops` or `public.sop_completions` in a subquery (grep within policy bodies returns 0 — avoids 42P17 per CLAUDE.md 2026-05-13)
    - roster_worker_id references auth.users(id) and is nullable (no NOT NULL)
    - `role` CHECK is `in ('worker', 'supervisor')`; `status` CHECK is `in ('pending', 'applied', 'rejected')`
    - Migration is wrapped in begin;/commit;
  </acceptance_criteria>
  <done>Migration 00038 written; all three schema additions present with recursion-safe org-scoped RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Extend database.types.ts for new table + column</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - src/types/database.types.ts (the file being modified — find the existing sop_completions Row/Insert/Update block and the departments table block from Phase 25 as the manual-extension analog)
    - .planning/STATE.md decisions: "database.types.ts manually extended … type regeneration not available in this environment" (Phase 3/4/6/25 precedent) and Phase 25 "supabase as any cast used for new tables not yet in database.types.ts"
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Runtime State Inventory"
  </read_first>
  <action>
    Manually extend src/types/database.types.ts: add `roster_worker_id: string | null` to the sop_completions Row type and `roster_worker_id?: string | null` to its Insert/Update types. Add full `sop_completion_signatures` and `ai_field_proposals` table type blocks (Row/Insert/Update) matching the migration columns. Follow the existing manual-extension shape used for departments/sub-trades. If full typing proves noisy, the consuming server actions may use the established `supabase as any` cast pattern (STATE.md Phase 25) — but the table type blocks SHOULD be added so call sites are type-checked where practical.
  </action>
  <verify>
    <automated>grep -c "sop_completion_signatures\|ai_field_proposals\|roster_worker_id" src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - database.types.ts contains `roster_worker_id`, `sop_completion_signatures`, and `ai_field_proposals` identifiers
    - `npx tsc --noEmit` passes (no new type errors introduced)
  </acceptance_criteria>
  <done>Types extended; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply migration 00038 to live DB + to_regclass verification</name>
  <files>scripts/apply-phase23-migration.mjs</files>
  <read_first>
    - scripts/apply-phase25-migrations.mjs (analog: supabase db push + post-push existence assertions; note its PGRST205 vs 42P01 bug — CLAUDE.md 2026-06-15 PostgREST schema-cache learning)
    - CLAUDE.md § Learnings 2026-06-15 (PostgREST schema-cache staleness: PGRST205 ≠ 42P01; verify existence via to_regclass through the Management API, NOT the supabase-js REST client; also "NOTIFY pgrst, 'reload schema'")
    - CLAUDE.md § Learnings 2026-04-04 (Railway/env) and 2026-05-08 (anon key is NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Open Questions #2" (roster_worker_id index)
    - <schema_push_requirement> in the planning context (non-TTY: SUPABASE_ACCESS_TOKEN; build/type checks pass WITHOUT push → false positive)
  </read_first>
  <action>
    Create `scripts/apply-phase23-migration.mjs` that: (1) runs `supabase db push` (non-TTY via SUPABASE_ACCESS_TOKEN env var already in .env.local from prior phases); (2) AFTER push, verifies existence of `public.sop_completion_signatures`, `public.ai_field_proposals`, and the `sop_completions.roster_worker_id` column via the Supabase Management API raw SQL endpoint using `select to_regclass('public.<table>')` and `information_schema.columns` for the column — NOT the supabase-js `.from().select()` REST client (which hits the stale PostgREST cache and returns PGRST205 false-misses); (3) issues `NOTIFY pgrst, 'reload schema';` via the same Management API to clear the cache; (4) treats PGRST205 distinctly from 42P01 if any REST probe is used as a secondary check. Run the script (this is the BLOCKING push — it MUST execute against the live DB, not be deferred). If SUPABASE_ACCESS_TOKEN is absent or the push errors with an auth/connectivity failure that the agent cannot resolve, STOP and surface a checkpoint to the operator rather than marking the task done.
  </action>
  <verify>
    <automated>node scripts/apply-phase23-migration.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` reports success (or "up to date" on idempotent re-run)
    - Script output confirms `to_regclass('public.sop_completion_signatures')` is non-null, `to_regclass('public.ai_field_proposals')` is non-null, and `sop_completions.roster_worker_id` appears in information_schema.columns — verified via Management API raw SQL, NOT the PostgREST REST client
    - Script distinguishes PGRST205 (cache stale) from 42P01 (genuinely absent) if it uses any REST probe (CLAUDE.md 2026-06-15)
    - `NOTIFY pgrst, 'reload schema'` is issued post-push
  </acceptance_criteria>
  <done>Migration applied to live DB; all three objects confirmed present via to_regclass; schema cache reloaded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DB (RLS) | The kiosk session and individual workers cross into Postgres; RLS must org-scope all reads of the new tables. |
| service-role → DB | recordSignature / approval writes use createAdminClient (bypasses RLS) — org-scoping enforced in the action, not the DB policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Information Disclosure | ai_field_proposals cross-org read | mitigate | SELECT policy `organisation_id = current_organisation_id()` only; no cross-table join (Pitfall 1). |
| T-23-01-02 | Tampering | Worker self-INSERTs a forged signature directly | mitigate | No authenticated INSERT policy on sop_completion_signatures; writes only via service-role server action (23-06). |
| T-23-01-03 | Denial of Service | RLS 42P17 infinite recursion breaks unrelated queries (CLAUDE.md 2026-05-13) | mitigate | New policies use current_organisation_id() directly; zero cross-table EXISTS to sops/sop_completions. Task 1 verify grep gate. |
| T-23-01-04 | Tampering | False-green verification because schema not pushed to live DB | mitigate | [BLOCKING] Task 3 pushes + to_regclass-verifies via Management API (not stale PostgREST cache). |
| T-23-01-SC | Tampering | npm installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `grep current_organisation_id() supabase/migrations/00038_phase23_schema.sql` shows direct org-scope on both new tables.
- `node scripts/apply-phase23-migration.mjs` confirms to_regclass non-null for both tables + column present.
- `npx tsc --noEmit` clean after database.types.ts extension.
</verification>

<success_criteria>
- Migration 00038 created with recursion-safe org-scoped RLS on both new tables.
- roster_worker_id added nullable to sop_completions (back-compat, no row migration).
- Schema applied to live DB and verified via to_regclass.
- database.types.ts extended; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/23-ai-field-layer-version-supersede/23-01-SUMMARY.md` when done.
</output>
