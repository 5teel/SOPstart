---
phase: 23-ai-field-layer-version-supersede
plan: 02
type: execute
wave: 2
depends_on: ["23-00", "23-01"]
files_modified:
  - src/lib/validators/ai-fields.ts
  - src/lib/ai-fields/registry.ts
  - src/lib/ai-fields/registrations/index.ts
  - src/lib/ai-fields/index.ts
  - src/app/api/ai-fields/read/route.ts
  - src/lib/ai-fields/__tests__/registry.test.ts
autonomous: true
requirements: [AFL-AI-01, AFL-AI-03]
must_haves:
  truths:
    - "registerField/getField/getAllFields round-trip works on a module-level Map (no React)"
    - "A registered field's read() returns the current value via the existing data path"
    - "GET /api/ai-fields/read?fieldId=... returns the current value for a registered field, org-scoped"
    - "At least 2 real fields (e.g. sop.title low-stake, a published-SOP content field high-stake) are registered via the unified mechanism"
    - "The registry is designed so a v5.0 conversational agent can drive it programmatically (no UI coupling)"
  artifacts:
    - path: "src/lib/ai-fields/registry.ts"
      provides: "FieldDescriptor registry: registerField, getField, getAllFields + types"
      exports: ["registerField", "getField", "getAllFields"]
      min_lines: 40
    - path: "src/lib/validators/ai-fields.ts"
      provides: "Zod schemas: FieldContextSchema, AiWriteRequestSchema, ProposalStatusSchema"
      contains: "AiWriteRequestSchema"
    - path: "src/lib/ai-fields/registrations/index.ts"
      provides: "Barrel that imports every field registration (imported by API routes)"
    - path: "src/app/api/ai-fields/read/route.ts"
      provides: "GET read API for any registered field"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/ai-fields/read/route.ts"
      to: "src/lib/ai-fields/registry.ts"
      via: "getField(fieldId).read(context)"
      pattern: "getField\\("
    - from: "src/app/api/ai-fields/read/route.ts"
      to: "src/lib/ai-fields/registrations/index.ts"
      via: "import to populate the registry before serving"
      pattern: "ai-fields/registrations"
---

<objective>
Build the X-03 AI field-layer BACKBONE foundation: the unified typed field-descriptor registry (AFL-AI-03), the Zod validator surface, and the AI read API (AFL-AI-01). This is the v5.0-consumable mechanism — every editable field registers via one shared `registerField()` call with `read()`/`write()` callbacks; the registry is a plain module-level Map with no React dependency so server actions, API routes, and the future conversational agent can all drive it. NO user-facing surface ships (D-04, Cmd+K removed).

Purpose: AFL-AI-03 unified registry is the single mechanism the whole AI write layer (23-04) and v5.0 build on. AFL-AI-01 read API proves a field's current value is fetchable. Per D-04 the registry must be programmatically driveable with no UI coupling.
Output: registry module + validators + read route + ≥2 registered fields + green unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-field-layer-version-supersede/23-CONTEXT.md
@.planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md
@.planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md
@src/lib/validators/completions.ts
@src/lib/builder/block-type-labels.ts
</context>

<artifacts_this_phase_produces>
**Module:** `src/lib/ai-fields/registry.ts` — `registerField<T>(descriptor)`, `getField(id)`, `getAllFields()`; types `FieldDescriptor<T>`, `FieldContext`, `WriteResult`, `StakeLevel`
**Module:** `src/lib/ai-fields/index.ts` (barrel)
**Module:** `src/lib/ai-fields/registrations/index.ts` (registration barrel imported by API routes — Pitfall 3 fix)
**Validators:** `src/lib/validators/ai-fields.ts` — `StakeLevelSchema`, `FieldContextSchema`, `AiWriteRequestSchema`, `ProposalStatusSchema`
**Route:** `GET /api/ai-fields/read`
**≥2 field registrations** (e.g. `sop.title`, one published-SOP content field) co-located near their existing server actions
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Field registry module + validators + index barrels</name>
  <files>src/lib/validators/ai-fields.ts, src/lib/ai-fields/registry.ts, src/lib/ai-fields/index.ts, src/lib/ai-fields/__tests__/registry.test.ts</files>
  <read_first>
    - src/lib/ai-fields/__tests__/registry.test.ts (the RED contract scaffold from 23-00 — this task turns it GREEN)
    - src/lib/builder/block-type-labels.ts (analog: module-level typed const map + lookup fns + barrel; header-comment + fallback patterns)
    - src/lib/validators/completions.ts (analog: Zod schema file structure, z.enum, z.infer types, header comment)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Pattern 1: Typed Field Descriptor Registry" (lines 188-269) and § "Pitfall 3" (registry populated at module load — Node module cache + idempotent guard)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § registry.ts (lines 100-153) and § ai-fields.ts (lines 199-243)
    - 23-CONTEXT.md D-01/D-02/D-04 (stake levels; backbone-only; v5.0-consumable)
  </read_first>
  <behavior>
    - registerField + getField round-trip: registering {id:'test.field', label, stakeLevel:'low', read} then getField('test.field') returns it (matches 23-00 unit test).
    - Idempotent re-registration: registering same id twice is a no-op; getAllFields() count unchanged (HMR/module-reload safe — RESEARCH Pitfall 3).
    - read() returns current value: getField(id).read(ctx) resolves the registered value.
    - getAllFields() lists all registered descriptors.
    Turn the 23-00 test.fixme cases GREEN (remove fixme markers once registry.ts exists).
  </behavior>
  <action>
    Create `src/lib/validators/ai-fields.ts` exporting `StakeLevelSchema = z.enum(['low','high'])`, `FieldContextSchema` (organisationId uuid, optional sopId/sectionId/stepId/memberId uuid, optional `sopIsPublished: z.boolean()` — A6 mitigation flag passed from the API route), `AiWriteRequestSchema` (fieldId string.min(1), context FieldContextSchema, newValue z.unknown()), `ProposalStatusSchema = z.enum(['pending','applied','rejected'])`, and inferred types. Import schemas from validators — never inline Zod in modules.
    Create `src/lib/ai-fields/registry.ts`: module-level `const registry = new Map<string, FieldDescriptor>()`; `FieldDescriptor<T>` interface (id, label, stakeLevel, read: (ctx: FieldContext) => Promise<T>, write?: (ctx, newValue) => Promise<WriteResult>); `WriteResult = {outcome:'applied';value} | {outcome:'pending_approval';proposalId}`; `registerField` with idempotent `if (registry.has(id)) return` guard; `getField`, `getAllFields`. BACKEND/SERVER-ONLY header comment ("Do NOT import from client components") and a v5.0-consumable note (D-04). Re-export FieldContext type from the validators module to keep one source of truth.
    Create `src/lib/ai-fields/index.ts` barrel re-exporting registry + types.
    Update `src/lib/ai-fields/__tests__/registry.test.ts` to remove test.fixme markers so the 4 behaviors run live and GREEN.
  </action>
  <verify>
    <automated>npx playwright test --project=phase23-unit -g registry</automated>
  </verify>
  <acceptance_criteria>
    - registry.ts exports `registerField`, `getField`, `getAllFields` and types `FieldDescriptor`, `FieldContext`, `WriteResult`, `StakeLevel`
    - registry uses a module-level `new Map` (NOT a React context — RESEARCH anti-pattern)
    - registerField is idempotent (re-registration guarded by `registry.has`)
    - validators/ai-fields.ts exports `AiWriteRequestSchema`, `FieldContextSchema`, `ProposalStatusSchema`, `StakeLevelSchema`
    - `npx playwright test --project=phase23-unit -g registry` is GREEN (the 4 behaviors pass, no fixme)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Registry + validators created; 23-00 unit suite GREEN; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Register ≥2 real fields + registration barrel</name>
  <files>src/lib/ai-fields/registrations/index.ts, src/lib/ai-fields/registry.ts</files>
  <read_first>
    - src/actions/sops.ts (find an existing low-stake mutation, e.g. SOP title update, to wire `sop.title` write() to — the registration must CALL the existing server action, not write to Supabase directly; RESEARCH anti-pattern)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Registration site example" (lines 249-269) and § "Pitfall 3" (barrel imported by API routes so the Map is populated before requests)
    - 23-CONTEXT.md D-02 (published-SOP content/metadata/assignments + member roles = high-stake; everything else = low-stake)
  </read_first>
  <action>
    Create `src/lib/ai-fields/registrations/index.ts` — the single barrel that imports/executes all field registrations (so importing this one file populates the registry; RESEARCH Pitfall 3 / A4). Register at minimum: (1) `sop.title` — `stakeLevel:'low'`, `read` returns the current SOP title via an existing getter, `write` CALLS the existing SOP-title server action (no Supabase bypass); (2) one published-SOP content field (e.g. a section title or a step text) marked so it resolves high-stake — either `stakeLevel:'high'` or low-stake-but-flagged-high-when-published (D-02). Each `read`/`write` is a pure function of its `context` arg (no captured mutable state — RESEARCH anti-pattern). Add a header comment mapping each field to D-02 stake tier.
  </action>
  <verify>
    <automated>npx playwright test --project=phase23-unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - registrations/index.ts registers ≥2 fields including `sop.title`
    - Each field's `write` (where present) calls an existing `src/actions/` server action (grep shows an import from `@/actions/...`), never `createClient`/`createAdminClient` directly in the descriptor
    - Importing registrations/index.ts populates the registry (getAllFields() length ≥2 after import — assert in a unit test or read-route smoke)
    - At least one field maps to the D-02 high-stake tier (published SOP content) and one to low-stake (draft/title)
  </acceptance_criteria>
  <done>≥2 fields registered via the unified mechanism through the barrel; no Supabase bypass.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/ai-fields/read route</name>
  <files>src/app/api/ai-fields/read/route.ts</files>
  <read_first>
    - src/app/api/voice/query/route.ts or another existing API route under src/app/api/ (analog: createClient() session, JWT org-claim extraction, RLS-respecting reads, JSON response shape)
    - src/actions/completions.ts lines 32-38 (JWT claim extraction pattern: organisation_id from session access_token)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Pitfall 3" (the route MUST import the registrations barrel so the registry is populated) and § Architecture (read route resolves descriptor → read())
    - 23-CONTEXT.md D-04 (backbone-only; designed for v5.0 agent to call programmatically)
  </read_first>
  <action>
    Create `GET /api/ai-fields/read`. At the top, `import '@/lib/ai-fields/registrations'` (side-effect import — populates the Map before any request; RESEARCH Pitfall 3/A4). Parse `fieldId` from query params; resolve `getField(fieldId)` — if undefined return 404 with `{error:'Unknown field'}`. Build `FieldContext` from query params + the org-scoped JWT claim (organisation_id from the session token — RLS-respecting `createClient()`, NOT admin client; reads stay org-scoped). Call `descriptor.read(context)` and return `{ fieldId, value }` JSON. Use the RLS-respecting session client so cross-org reads are impossible. No write path in this route.
  </action>
  <verify>
    <automated>grep -n "getField\|ai-fields/registrations\|createClient" "src/app/api/ai-fields/read/route.ts"</automated>
  </verify>
  <acceptance_criteria>
    - route.ts exports `GET`
    - route.ts imports `@/lib/ai-fields/registrations` (side-effect, populates registry — Pitfall 3)
    - route resolves the field via `getField(fieldId)` and returns 404 for unknown fields
    - route uses RLS-respecting `createClient()` (NOT createAdminClient) and derives organisationId from the JWT claim
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Read API serves any registered field's current value, org-scoped; registry populated via barrel import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/agent → GET /api/ai-fields/read | Untrusted fieldId + context params cross into the read API. |
| read API → DB | Reads must stay org-scoped via RLS (session client). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | read API leaks another org's field value | mitigate | RLS-respecting createClient(); organisationId from JWT claim, not client-supplied; descriptor.read() runs under session RLS. |
| T-23-02-02 | Tampering | read route accidentally exposes a write path | mitigate | Route exports GET only; no mutation; write lives in 23-04 behind gateWrite. |
| T-23-02-03 | Spoofing | unknown/arbitrary fieldId | mitigate | getField() returns undefined → 404; registry is a closed allow-list of registered descriptors. |
| T-23-02-04 | Tampering | descriptor.write bypasses existing server action (silent business-rule skip) | mitigate | Task 2 acceptance: write() must import+call an existing @/actions server action; grep gate forbids direct Supabase in descriptors (CLAUDE.md 2026-06-15). |
| T-23-02-SC | Tampering | npm installs | accept | No package installs (RESEARCH § Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx playwright test --project=phase23-unit` GREEN (registry round-trip, idempotent, read, getAllFields).
- `grep getField src/app/api/ai-fields/read/route.ts` confirms registry resolution.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Unified registry (Map + registerField/getField/getAllFields) with no React dependency (AFL-AI-03).
- Read API returns current value for any registered field, org-scoped (AFL-AI-01).
- ≥2 real fields registered through the single mechanism; write descriptors call existing server actions.
- Registry is programmatically driveable for v5.0 (D-04) — no UI coupling.
</success_criteria>

<output>
Create `.planning/phases/23-ai-field-layer-version-supersede/23-02-SUMMARY.md` when done.
</output>
