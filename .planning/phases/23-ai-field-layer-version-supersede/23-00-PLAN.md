---
phase: 23-ai-field-layer-version-supersede
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase23/ai-field-registry.spec.ts
  - tests/phase23/version-supersede.spec.ts
  - tests/phase23/version-indicator.spec.ts
  - tests/phase23/completion-roster.spec.ts
  - src/lib/ai-fields/__tests__/registry.test.ts
autonomous: true
requirements: [AFL-AI-01, AFL-AI-02, AFL-AI-03, AFL-VER-01, AFL-VER-02, AFL-VER-03, AFL-VER-04, AFL-VER-05]
must_haves:
  truths:
    - "Running `npx playwright test --list --project=phase23-stubs` lists all 4 tests/phase23/*.spec.ts files"
    - "Running `npx playwright test --list --project=phase23-unit` lists the registry unit test"
    - "Each AFL-* requirement has at least one source-contract assertion gated in phase23-stubs"
    - "The registry unit test uses STATIC @/ imports (no dynamic import('@/...'))"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase23-stubs + phase23-unit project registration"
      contains: "phase23-stubs"
    - path: "tests/phase23/ai-field-registry.spec.ts"
      provides: "AFL-AI-01/02/03 source-contract stubs"
    - path: "tests/phase23/version-supersede.spec.ts"
      provides: "AFL-VER-01/02/03 source-contract stubs"
    - path: "tests/phase23/version-indicator.spec.ts"
      provides: "AFL-VER-04 source-contract stub"
    - path: "tests/phase23/completion-roster.spec.ts"
      provides: "AFL-VER-05 + D-11 source-contract stubs"
    - path: "src/lib/ai-fields/__tests__/registry.test.ts"
      provides: "AFL-AI-03 behavioral unit scaffold (static imports)"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase23/*.spec.ts"
      via: "phase23-stubs project testMatch regex"
      pattern: "tests\\\\/phase23"
    - from: "playwright.config.ts"
      to: "src/lib/ai-fields/__tests__/"
      via: "phase23-unit project testDir"
      pattern: "phase23-unit"
---

<objective>
Establish the Wave 0 test surface for Phase 23: register the `phase23-stubs` and `phase23-unit` Playwright projects and write source-contract stub specs that gate every AFL-* requirement (plus D-11) BEFORE any production code ships. This is the Nyquist Wave-0 harness — each requirement gets at least one automated assertion so downstream waves cannot ship a green-but-broken feature.

Purpose: CLAUDE.md 2026-05-25 learning — a spec file not in any project regex NEVER runs. CLAUDE.md 2026-06-05 learning — source-contract tests must assert HANDLERS are wired, not merely that a string appears. This plan creates the registered, discoverable test inventory the whole phase executes against.
Output: 2 new Playwright project entries + 4 source-contract spec files + 1 unit test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ai-field-layer-version-supersede/23-VALIDATION.md
@.planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This Wave-0 plan creates the test scaffold only. The symbols asserted by these stubs are CREATED in later plans (they will be RED/test.fixme until then). Newly-created symbols across the phase (the drift-verification pass must treat these as new, NOT pre-existing):

**Playwright projects:** `phase23-stubs`, `phase23-unit`
**Server actions (created in 23-02/03/04/06):** `cloneSopAsDraft`, `restoreVersionAsNew`, `recordSignature`, `applyAiWrite`, `acceptProposal`, `rejectProposal`
**Field registry module (23-02):** `src/lib/ai-fields/registry.ts` exporting `registerField`, `getField`, `getAllFields`; types `FieldDescriptor`, `FieldContext`, `WriteResult`, `StakeLevel`
**Approval module (23-04):** `src/lib/ai-fields/approval.ts` exporting `gateWrite`
**Validators (23-02):** `src/lib/validators/ai-fields.ts` — `FieldContextSchema`, `AiWriteRequestSchema`, `ProposalStatusSchema`, `AcceptProposalSchema`, `RejectProposalSchema`
**Tables/columns (23-01):** `ai_field_proposals` table, `sop_completion_signatures` table, `sop_completions.roster_worker_id` column
**Routes (23-06/05):** `/login/kiosk` (RosterSelector), `/admin/sops/[sopId]/versions/diff`
**Playwright project names already exist; new ones added here are the two above.**
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Register phase23-stubs + phase23-unit Playwright projects</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (the file being modified — copy the `phase22-stubs` entry at lines 159-180 and `phase15-unit` entry at lines 60-64 verbatim as structural analogs)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § "playwright.config.ts — extend with phase23 project entries" (lines 539-569 give the exact entries to append)
    - .planning/phases/23-ai-field-layer-version-supersede/23-VALIDATION.md § "Wave 0 Requirements"
    - CLAUDE.md § Learnings 2026-05-25 (unregistered-spec) and 2026-04-24 (Playwright dynamic-import fails outside testDir)
  </read_first>
  <action>
    Append two project entries to the `projects` array in playwright.config.ts, copying the `phase22-stubs` block structure exactly. Entry 1 `phase23-stubs`: `testDir: '.'`, `testMatch: /tests\/phase23\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`. Entry 2 `phase23-unit`: `testDir: './src/lib/ai-fields/__tests__'`, `testMatch: /.*\.test\.ts$/` (no browserName — pure module unit project, mirrors phase15-unit). Add a header comment block on `phase23-stubs` listing the 4 registered spec files and the registration-verify command, mirroring the phase22-stubs comment. The phase23-unit comment must cite CLAUDE.md 2026-04-24 (dynamic-import-in-Playwright fails outside testDir — use static @/ imports here).
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase23-stubs 2>&1 | grep -c "phase23"</automated>
    <automated>npx playwright test --list --project=phase23-unit 2>&1 | grep -c "registry"</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts contains the literal strings `phase23-stubs` and `phase23-unit`
    - `npx playwright test --list --project=phase23-stubs` returns a non-zero count of discovered tests under tests/phase23/ (zero = registration failed per CLAUDE.md 2026-05-25)
    - `npx playwright test --list --project=phase23-unit` discovers the registry unit test
    - phase23-unit entry uses `testDir: './src/lib/ai-fields/__tests__'` (NOT testDir: '.') so static @/ imports resolve through Playwright's TS compiler
  </acceptance_criteria>
  <done>Both projects are registered and both `--list` commands discover their files (non-zero counts).</done>
</task>

<task type="auto">
  <name>Task 2: Write 4 source-contract stub specs gating every AFL-* requirement + D-11</name>
  <files>tests/phase23/ai-field-registry.spec.ts, tests/phase23/version-supersede.spec.ts, tests/phase23/version-indicator.spec.ts, tests/phase23/completion-roster.spec.ts</files>
  <read_first>
    - tests/phase22/voice-modal.spec.ts and tests/phase22/voice-safety-gate.spec.ts (analog source-contract stub structure — fs.readFileSync + toContain, [\s\S] not /s flag per CLAUDE.md 2026-06-02)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § "tests/phase23/*.spec.ts (5 source-contract stub files)" (lines 507-535 — shows the wiring-not-just-presence assertion pattern)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Validation Architecture / Phase Requirements → Test Map" (lines 690-704)
    - 23-CONTEXT.md D-01..D-11 (each stub cites the requirement + decision it gates)
  </read_first>
  <action>
    Create 4 spec files. Each test uses `fs.readFileSync(path.join(process.cwd(), '<file>'), 'utf-8')` then `expect(src).toContain(...)`. Guard each assertion that targets a not-yet-created file with `fs.existsSync(...)` + `test.skip()` so Wave-0 is green-when-absent and live-when-present (CLAUDE.md 2026-06-24 phase22 guard pattern). Use `[\s\S]` never the `/s` flag (CLAUDE.md 2026-06-02 TS-target).
    • ai-field-registry.spec.ts: AFL-AI-01 (registry.ts exports `read` on FieldDescriptor), AFL-AI-02 (approval.ts exports `gateWrite`; assert it returns `pending_approval` branch token AND `applied` branch token both present in source), AFL-AI-03 (registry.ts exports `registerField`, `getField`, `getAllFields`).
    • version-supersede.spec.ts: AFL-VER-01 (`export async function cloneSopAsDraft` in versioning.ts AND `cloneSopAsDraft(` CALLED in versions/page.tsx — wiring not just presence), AFL-VER-02 (versions/diff/page.tsx imports `diffBlockContent` from `@/lib/builder/diff-block-content`), AFL-VER-03 (`export async function restoreVersionAsNew` in versioning.ts AND it must NOT contain any `.update({ superseded_by` mutation of an OLD row — assert append-only by checking restore copies forward).
    • version-indicator.spec.ts: AFL-VER-04 (SopLibraryCard.tsx contains `data-updated-badge` attribute AND the badge derives from a published_at-vs-completion comparison prop, not a hardcoded literal).
    • completion-roster.spec.ts: AFL-VER-05 (completions.ts `submitCompletion` writes `roster_worker_id` AND validates org-membership via organisation_members before the write — assert both tokens present; `recordSignature` exported and uses `createAdminClient`), D-11 (`/login/kiosk/page.tsx` route file exists and renders a roster selector; assert the migration 00038 file contains `roster_worker_id` and `sop_completion_signatures`).
    Add a literal `D-01`..`D-11` citation comment to the relevant tests so the decision-coverage gate can read D-NN tokens from this scaffold.
  </action>
  <verify>
    <automated>npx playwright test --project=phase23-stubs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 4 spec files exist under tests/phase23/
    - `npx playwright test --project=phase23-stubs` runs without uncaught errors (passing or skipped — never errored). Tests targeting unbuilt files are test.skip via fs.existsSync guard.
    - Each spec contains the requirement ID it gates as a test title substring (grep `AFL-AI-01`, `AFL-VER-01` etc. each return ≥1 hit, filtered: `grep -v '^#' | grep -c`)
    - version-supersede.spec.ts asserts `cloneSopAsDraft(` is CALLED in versions/page.tsx (wiring), not merely exported (CLAUDE.md 2026-06-05)
    - No file uses the regex `/s` flag (use `[\s\S]`)
    - D-NN citation comments present (D-01 through D-11 appear across the 4 files)
  </acceptance_criteria>
  <done>4 stub specs created and discoverable; phase23-stubs run completes with no errored tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold the field registry behavioral unit test (static imports)</name>
  <files>src/lib/ai-fields/__tests__/registry.test.ts</files>
  <read_first>
    - src/lib/voice/__tests__/intent-classifier.test.ts (phase15-unit analog — static `@/` imports resolve through Playwright TS compiler when under a testDir-scoped project; CLAUDE.md 2026-06-24)
    - .planning/phases/23-ai-field-layer-version-supersede/23-RESEARCH.md § "Pattern 1: Typed Field Descriptor Registry" (lines 188-247 — the registerField/getField/getAllFields contract this test asserts)
    - .planning/phases/23-ai-field-layer-version-supersede/23-PATTERNS.md § "src/lib/ai-fields/__tests__/registry.test.ts"
  </read_first>
  <behavior>
    - Test "registry round-trip": registerField({id:'test.field', label:'Test', stakeLevel:'low', read: async () => 'v'}) then getField('test.field') returns the descriptor with matching id+label.
    - Test "registry idempotent": registering the same id twice does not throw and getAllFields() count does not increase on the second call (HMR-safe guard per RESEARCH Pitfall 3).
    - Test "read returns current value": getField('test.field').read({organisationId:'org-uuid'}) resolves to 'v'.
    - Test "getAllFields lists registered": after registering ≥1 field, getAllFields() includes that descriptor.
    Mark tests `test.fixme` (or guard with a try/catch import) ONLY if `src/lib/ai-fields/registry.ts` does not yet exist — Wave 0 ships the scaffold; 23-02 turns it green. Use STATIC `import { registerField, getField, getAllFields } from '@/lib/ai-fields/registry'` (never dynamic import — CLAUDE.md 2026-06-24).
  </behavior>
  <action>
    Create the unit test with static `@/lib/ai-fields/registry` imports. Because the module does not exist until 23-02, wrap the suite so it is green-when-absent: prefer `test.fixme` markers on each case with a comment "GREEN in 23-02 when registry.ts ships". Do NOT use `await import('@/...')` (fails in Playwright Node runner per CLAUDE.md 2026-06-24) — static top-level import under the phase23-unit testDir is the correct pattern. The four behaviors above are the RED contract that 23-02 must satisfy.
  </action>
  <verify>
    <automated>npx playwright test --project=phase23-unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/ai-fields/__tests__/registry.test.ts exists with static `@/lib/ai-fields/registry` import (grep confirms no `await import(`)
    - `npx playwright test --project=phase23-unit` runs without module-resolution errors (test.fixme cases are skipped cleanly, not errored)
    - The 4 named behaviors are present as test titles (round-trip, idempotent, read, getAllFields)
  </acceptance_criteria>
  <done>Unit scaffold discoverable under phase23-unit; suite runs clean (skipped, not errored) pre-23-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test infra) | This plan adds only test files + config; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-00-01 | Tampering | Unregistered spec file silently never runs (false-green gate) | mitigate | Task 1 verify gate counts `--list` output; zero = fail. CLAUDE.md 2026-05-25. |
| T-23-00-02 | Repudiation | Source-contract test asserts string presence but not wiring (dead feature passes) | mitigate | Task 2 asserts handlers are CALLED (cloneSopAsDraft(), data-updated-badge derived). CLAUDE.md 2026-06-05. |
| T-23-00-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH § Package Legitimacy Audit: none). No install task exists. |
</threat_model>

<verification>
- `npx playwright test --list --project=phase23-stubs` lists ≥4 spec files.
- `npx playwright test --list --project=phase23-unit` lists the registry test.
- `npx playwright test --project=phase23-stubs --project=phase23-unit` completes with no errored tests (skips allowed).
</verification>

<success_criteria>
- 2 Playwright projects registered and discoverable.
- 4 source-contract stub specs + 1 unit scaffold created.
- Every AFL-* requirement (8) and D-11 has ≥1 gating assertion.
- Wiring assertions (not just presence) used for interactive affordances.
</success_criteria>

<output>
Create `.planning/phases/23-ai-field-layer-version-supersede/23-00-SUMMARY.md` when done.
</output>
