---
phase: 46-capability-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase46/capability-matrix-doc.spec.ts
  - tests/phase46/sop-edit-guard-wiring.spec.ts
  - tests/phase46/sop-edit-owner-access.spec.ts
autonomous: true
requirements: [CAP-01, CAP-02]

must_haves:
  truths:
    - "`npx playwright test --list --project=phase46` lists all three tests/phase46 spec files (a spec in no project regex never runs — CLAUDE.md 2026-05-25)"
    - "The doc spec fails if CAPABILITY-MATRIX.md is missing a required role, capability row, legend marker, or the CLAUDE.md pointer"
    - "The wiring spec fails if requireSopEditAccess is defined but not called inside each enumerated write function"
    - "The live probe spec creates an ephemeral org/owner/peer/admin and asserts owner-edit, non-owner-denied, admin-regression, cross-org-denied, and publish-still-denied against real RLS"
    - "Every assertion that cannot pass before plans 46-02/46-03 land is marked test.fixme naming the plan that activates it — nothing is silently skipped"
  artifacts:
    - path: "playwright.config.ts"
      provides: "phase46 project registration, broad testMatch on tests/phase46/**"
      contains: "tests\\\\/phase46"
    - path: "tests/phase46/capability-matrix-doc.spec.ts"
      provides: "CAP-01 source-contract gate"
      min_lines: 60
    - path: "tests/phase46/sop-edit-guard-wiring.spec.ts"
      provides: "CAP-02 guard-exists-AND-is-called gate"
      min_lines: 70
    - path: "tests/phase46/sop-edit-owner-access.spec.ts"
      provides: "CAP-02 live positive/negative/regression/isolation probes"
      min_lines: 150
  key_links:
    - from: "playwright.config.ts"
      to: "tests/phase46/*.spec.ts"
      via: "project testMatch regex"
      pattern: "tests\\\\/phase46"
    - from: "tests/phase46/sop-edit-owner-access.spec.ts"
      to: "live Supabase (ephemeral org)"
      via: "service-role client + magic-link minted user tokens"
      pattern: "generateLink"
---

<objective>
Wave 0 for Phase 46: build the feedback harness before any behaviour changes. Register a `phase46` Playwright project and write the three specs that gate CAP-01 (matrix document) and CAP-02 (sign-off authority carries edit rights), including the live-Supabase probe set that proves owner-edit positively AND non-owner-edit negatively per role (CLAUDE.md 2026-07-20 — one probe per policy is not coverage).

Purpose: nothing in this phase can be declared done without an automated gate that fails when it regresses. The specs also freeze the exact contract (required matrix rows, required guarded functions) that plans 46-02 and 46-03 must satisfy — they are the spec, not a reflection of it.
Output: 1 config edit + 3 spec files, all discoverable via `--list`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-capability-matrix/46-CONTEXT.md
@.planning/phases/46-capability-matrix/46-RESEARCH.md
@.planning/phases/46-capability-matrix/46-PATTERNS.md
@.planning/phases/46-capability-matrix/46-VALIDATION.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created or modified across all three plans of Phase 46 (this plan creates the first four):

| Artifact | Plan | Kind |
|---|---|---|
| `playwright.config.ts` → `phase46` project | 46-01 | config |
| `tests/phase46/capability-matrix-doc.spec.ts` | 46-01 | spec |
| `tests/phase46/sop-edit-guard-wiring.spec.ts` | 46-01 | spec |
| `tests/phase46/sop-edit-owner-access.spec.ts` | 46-01 | spec |
| `.planning/codebase/CAPABILITY-MATRIX.md` | 46-02 | doc |
| `CLAUDE.md` → `## Capability Matrix` section | 46-02 | doc |
| `requireSopEditAccess(target)` + `SopEditContext` + `SopEditTarget` in `src/lib/auth/guards.ts` | 46-03 | function/types |
| `supabase/migrations/00063_sop_content_owner_edit.sql` | 46-03 | migration |
| `scripts/apply-phase46-migration.mjs` | 46-03 | script |

<tasks>

<task type="auto">
  <name>Task 1: Register the phase46 Playwright project and write the CAP-01 doc gate</name>
  <files>playwright.config.ts, tests/phase46/capability-matrix-doc.spec.ts</files>
  <read_first>
    - playwright.config.ts (lines 400-500 — the phase33/phase34/phase35/phase36 project entries; copy the block shape and the explanatory comment convention verbatim)
    - tests/lint/rls-org-scope.spec.ts (the file-parsing + substring-assertion shape this spec mirrors)
    - .planning/phases/46-capability-matrix/46-PATTERNS.md § `playwright.config.ts` and § `tests/phase46/capability-matrix-doc.spec.ts`
    - CLAUDE.md (the `## Pathways Map Maintenance` section — the doc-pointer convention 46-02 will mirror)
  </read_first>
  <action>
    Add one project entry to `playwright.config.ts` after the existing `phase40` entry: `name: 'phase46'`, `testDir: '.'`, `testMatch: /tests\/phase46\/.*\.(spec|test)\.ts$/`, `use: { browserName: 'chromium' }`. Keep the deliberately-broad testMatch so later plans drop specs into `tests/phase46/` with no further config edit. Carry the sibling entries' comment convention: name the CLAUDE.md 2026-05-25 learning, list the Wave-0 stub files and which plan activates each.

    Create `tests/phase46/capability-matrix-doc.spec.ts` — a source-contract spec (no browser, no Supabase) that reads `.planning/codebase/CAPABILITY-MATRIX.md` and `CLAUDE.md` with `fs.readFileSync` from `process.cwd()`. Normalise CRLF to LF before matching (CLAUDE.md 2026-07-18 worktree line-ending class). Assert:
    (a) the matrix file exists;
    (b) it contains each org role token: `worker`, `supervisor`, `admin`, `safety_manager`;
    (c) it contains `platform_admin` (the orthogonal Potenco axis, documented as a footnote not a peer row);
    (d) it contains all three legend markers as literal strings: `shipped-and-enforced`, `shipped-but-unenforced`, `planned`;
    (e) it contains both channel headings required by D1 (obligation ≠ access): `Access channel` and `Obligation channel`;
    (f) it contains every capability row label from this exact list, asserted one `expect` per label so a failure names the missing row: `Read SOP`, `Walk SOP`, `Self-add SOP`, `Record completion`, `Sign off completion`, `Record observation`, `Create SOP`, `Edit SOP content`, `Verify blocks`, `Publish SOP`, `Delete SOP`, `Version history`, `Governance queue`, `Approval chains`, `Manage team`, `Manage departments`, `Manage blocks library`, `AI settings`, `Training matrix`, `Assessor governance`, `Export training records`, `Own profile`;
    (g) it contains the CAP-02 ownership overlay tokens `sign-off authority` and `owner_user_id`;
    (h) it contains forward-reference phase markers `Phase 44a`, `Phase 44b`, `Phase 45`, `Phase 47`, `Phase 48`;
    (i) `CLAUDE.md` contains the literal path `.planning/codebase/CAPABILITY-MATRIX.md`.
    Mark each `test()` in this file with `test.fixme` plus the comment `// activated by plan 46-02`; plan 46-02's final task removes those markers. Do not use `test.skip` — a skip reads as green and hides the gap.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase46 2>&1 | grep -c "capability-matrix-doc"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase46` lists `tests/phase46/capability-matrix-doc.spec.ts` (zero hits = the spec is unregistered and would never run)
    - `playwright.config.ts` contains the literal `tests\/phase46` inside a project entry named `phase46`
    - `npx playwright test --project=phase46` exits 0 with every test reported as fixme/expected-failure, not passed
    - Every required row label from list (f) appears in its own `expect(...)` call in the spec (grep the spec: `grep -v '^\s*//' tests/phase46/capability-matrix-doc.spec.ts | grep -c "Export training records"` returns 1)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The phase46 project is registered and discoverable, and the CAP-01 gate exists with all 22 capability rows and both D1 channel headings pinned, fixme-marked pending 46-02.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CAP-02 guard-wiring source-contract gate</name>
  <files>tests/phase46/sop-edit-guard-wiring.spec.ts</files>
  <read_first>
    - src/lib/auth/guards.ts (current state — only `requireAdminContext` exists)
    - src/actions/sections.ts (the 4 target functions and their current guards)
    - src/actions/sop-section-blocks.ts (the `requireAdmin()` helper and every call site; note `verifyBlock`/`unverifyBlock`/`acceptBlockUpdate`/`declineBlockUpdate` stay admin-only)
    - src/app/api/sops/[sopId]/sections/[sectionId]/route.ts (currently zero app-level guard)
    - .planning/phases/46-capability-matrix/46-RESEARCH.md § SOP Edit Path Inventory and § Common Pitfalls
    - CLAUDE.md § Learnings entries 2026-06-05 (grep proves presence, not wiring) and 2026-07-13 (source-contract guards must be repointed when code moves)
  </read_first>
  <action>
    Create `tests/phase46/sop-edit-guard-wiring.spec.ts`. It must assert the guard is CALLED, not merely that a token appears somewhere in a file (CLAUDE.md 2026-06-05: a file-level grep passes on a dead feature).

    Implement a helper `fnBody(source, exportedName)` that slices the file text from the first index of `export async function <exportedName>` to the next occurrence of `\nexport ` after that index (or end-of-file), after CRLF normalisation. Throw a descriptive error if the anchor is not found — a renamed function must turn this spec RED, not silently pass an empty slice.

    Positive wiring assertions — each of these (file, function) slices must contain `requireSopEditAccess(`:
    - `src/actions/sections.ts` → `createSection`, `reorderSections`, `updateSectionLayout`, `updateSectionTitle`
    - `src/actions/sop-section-blocks.ts` → `addBlockToSection`, `removeBlockFromSection`, `setPinMode`, `reorderSectionBlocks`
    - `src/app/api/sops/[sopId]/sections/[sectionId]/route.ts` → the `PATCH` export (anchor on `export async function PATCH`)

    Negative / scope-containment assertions (these prove CAP-02 did not widen past "edit" — RESEARCH Pitfall 4). Each of these slices must NOT contain `requireSopEditAccess(` and must still contain `requireAdmin(`:
    - `src/actions/sop-section-blocks.ts` → `verifyBlock`, `unverifyBlock`, `acceptBlockUpdate`, `declineBlockUpdate`

    Guard-shape assertions on `src/lib/auth/guards.ts`:
    - exports `requireSopEditAccess`
    - the `requireSopEditAccess` slice contains `.eq('organisation_id', organisationId)` — the org filter is sourced from the session, never from the fetched row (CLAUDE.md 2026-06-15/26, 2026-07-28)
    - the slice contains `owner_user_id`
    - the slice contains `createAdminClient(` (the resolution fetch bypasses RLS deliberately and self-enforces org scope)
    - `requireAdminContext` still exists and is still exported (CAP-02 extends, it does not replace — other admin-only actions keep using it)

    Also assert `src/actions/sop-section-blocks.ts` still contains `data.serviceRole` and that the `addBlockToSection` slice guards the parser bypass before the user path (the serviceRole branch must not be gated by `requireSopEditAccess`) — RESEARCH Pitfall 3 flags this as a different trust boundary.

    Mark every `test()` in this file `test.fixme` with the comment `// activated by plan 46-03`.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase46 2>&1 | grep -c "sop-edit-guard-wiring"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase46` lists `tests/phase46/sop-edit-guard-wiring.spec.ts`
    - The spec contains a `fnBody` helper that throws when the `export async function <name>` anchor is absent (grep the spec for `throw new Error` inside the helper)
    - The spec asserts all 9 positive (file, function) pairs and all 4 negative pairs listed in the action
    - `npx playwright test --project=phase46` exits 0 with the new tests reported as fixme
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>A wiring gate exists that goes RED if the guard is defined-but-unused, if any enumerated write path loses it, if a target function is renamed, or if CAP-02 leaks into the publish/verify surface.</done>
</task>

<task type="auto">
  <name>Task 3: Write the CAP-02 live RLS probe set (ephemeral org, positive + negative per role)</name>
  <files>tests/phase46/sop-edit-owner-access.spec.ts</files>
  <read_first>
    - tests/phase34/observation-read-role-scope.spec.ts (full — env loader lines 49-85, fixtures lines 87-124, teardown lines 126-135, probe style lines 141+; copy the fixture and teardown code verbatim, there is no shared test-utils module for this pattern)
    - supabase/migrations/00003_sop_schema.sql lines 100-165 (the three policies this phase extends, and the read policies that stay unchanged)
    - supabase/migrations/00001_foundation_schema.sql lines 40-90 (`current_user_role()` reads the JWT `user_role` claim minted by the access-token hook — the ephemeral member's token carries it only because the membership row is inserted before the token is minted)
    - .planning/phases/46-capability-matrix/46-PATTERNS.md § `tests/phase46/sop-edit-owner-access.spec.ts`
    - CLAUDE.md § Learnings 2026-07-20 (positive AND negative probes per role) and 2026-08-04 (permissive policies OR-combine)
  </read_first>
  <action>
    Create `tests/phase46/sop-edit-owner-access.spec.ts` mirroring `tests/phase34/observation-read-role-scope.spec.ts`: same `loadEnv()`, `SUPABASE_URL`/`SERVICE_KEY`/`ANON_KEY`/`LIVE_ENV_READY` constants (`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` is this project's anon key name — CLAUDE.md 2026-05-08), same `serviceClient()`, `mintAccessToken()`, `asUserClient()`, same `cleanupOrgIds`/`cleanupUserIds` arrays and `test.afterAll` teardown.

    Extend the fixtures: `createEphemeralMember(admin, orgId, role)` role union becomes `'worker' | 'supervisor' | 'admin'`; `createEphemeralSop(admin, orgId, uploaderId, ownerUserId)` sets `owner_user_id` explicitly (do not rely on the Phase 28 `default_sop_owner` trigger) with `status: 'draft'`, `version: 1`, `source_file_path`/`source_file_type`/`source_file_name` as in the phase34 fixture. Add `createEphemeralSection(admin, sopId)` inserting into `sop_sections` (`section_type: 'procedure'`, a known `title`, `sort_order: 10`, `approved: false`) and `createEphemeralStep(admin, sectionId)` inserting into `sop_steps` (`step_number: 1`, a known `text`). Use unique email prefixes `p46-owner-…@example-phase46-test.invalid`.

    CRITICAL assertion shape: an RLS-denied UPDATE through PostgREST does not error — it silently affects zero rows. Every probe must therefore re-read the row with the SERVICE client after the attempted write and compare the persisted value, not just inspect the update response. Assert both directions explicitly.

    Probe set (each its own `test()`, each with a name stating role + relation + expected outcome):
    1. POSITIVE — org role `worker`, `owner_user_id` = self: updating their SOP's `sop_sections.title` persists.
    2. POSITIVE — same owner: updating `sop_steps.text` on a step under their SOP persists (proves `admins_can_manage_steps` was extended, not just the sections policy).
    3. NEGATIVE — a second `worker` in the SAME org who is not the owner: the same `sop_sections.title` update does NOT persist.
    4. NEGATIVE — a `supervisor` in the same org who is not the owner: does NOT persist (supervisor is not an edit role; sign-off-on-completions is a different noun — RESEARCH § Sign-off Authority).
    5. REGRESSION — an `admin` in the same org who is not the owner: DOES persist (universal admin edit unchanged).
    6. CROSS-ORG ISOLATION — the owner of org A attempting to update a section of org B's SOP: does NOT persist (the owner arm lives INSIDE the org-scope AND, never as a sibling policy — CLAUDE.md 2026-08-04).
    7. SCOPE CONTAINMENT — the owner (role `worker`) attempting to update their own SOP's `sops.status` to `published`: does NOT persist. CAP-02 grants content edit only; publish/delete/version/owner-reassignment stay admin-gated (RESEARCH Pitfall 4, assumption A2).

    Every test gets `test.skip(!LIVE_ENV_READY, …)` exactly as phase34 does, AND `test.fixme` with the comment `// activated by plan 46-03 after migration 00063 is applied`. Add a top-of-file comment recording that probes 3-7 must ALSO stay red-if-broken after CAP-02 ships — they are the negative half of the 2026-07-20 rule.
  </action>
  <verify>
    <automated>npx playwright test --list --project=phase46 2>&1 | grep -c "sop-edit-owner-access"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase46` lists `tests/phase46/sop-edit-owner-access.spec.ts` and the project lists 3 spec files total
    - The spec defines all 7 probes named in the action, each as a separate `test()`
    - Every probe re-reads the target row with the service client after the write attempt (grep the spec: at least 7 occurrences of a service-client re-read after an update; no probe asserts only on the update response object)
    - `npx playwright test --project=phase46` exits 0 with all tests fixme (no failures, no unexpected passes)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The live probe set exists and encodes owner-can, non-owner-cannot, supervisor-cannot, admin-still-can, cross-org-cannot, and owner-cannot-publish — fixme-marked pending 46-03's migration apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → live Supabase | The probe spec authenticates as real ephemeral users with real JWTs and exercises real RLS; a wrong assertion shape (checking the update response instead of the persisted row) would certify a hole as closed |
| spec file → playwright.config.ts | An unregistered spec silently never runs — the gate exists on disk but not in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-01 | Repudiation | `tests/phase46/*.spec.ts` | mitigate | Broad `tests/phase46/**` testMatch registered in `playwright.config.ts`; task verify greps `--list` output per CLAUDE.md 2026-05-25 |
| T-46-01-02 | Tampering | live probe assertions | mitigate | Every probe re-reads the row with the service-role client after the write attempt; an RLS-denied UPDATE returns success with 0 rows affected and would otherwise read as a pass |
| T-46-01-03 | Information Disclosure | ephemeral test users/orgs left on prod Supabase | mitigate | `test.afterAll` deletes every created org and auth user (phase34 teardown copied verbatim); emails use the `.invalid` TLD |
| T-46-01-04 | Elevation of Privilege | fixme markers left behind after activation | mitigate | Plans 46-02/46-03 each carry an acceptance criterion asserting zero `test.fixme` occurrences remain in the specs they activate |
</threat_model>

<verification>
- `npx playwright test --list --project=phase46` lists exactly 3 spec files
- `npx playwright test --project=phase46` exits 0 (all fixme, zero failures, zero unexpected passes)
- `npx tsc --noEmit` passes
- `npx playwright test --project=phase15-stubs` still green (no cross-project regression from the config edit)
</verification>

<success_criteria>
The phase46 project is registered and discoverable; three specs encode the full CAP-01 and CAP-02 contract; every not-yet-satisfiable assertion is fixme-marked with the activating plan named in a comment.
</success_criteria>

<output>
Create `.planning/phases/46-capability-matrix/46-01-SUMMARY.md` when done
</output>
