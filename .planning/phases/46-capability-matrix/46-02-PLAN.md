---
phase: 46-capability-matrix
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - .planning/codebase/CAPABILITY-MATRIX.md
  - CLAUDE.md
  - tests/phase46/capability-matrix-doc.spec.ts
autonomous: true
requirements: [CAP-01]

must_haves:
  truths:
    - "A reader can open one file and answer 'can a supervisor do X?' for every capability the app surfaces"
    - "The document distinguishes the access channel (can they reach it) from the obligation channel (are they required to do it) — they are never described as one thing"
    - "Planned capabilities are marked with their phase and are visually distinct from shipped ones"
    - "Any shipped-but-unenforced cell found while writing the matrix is recorded as a finding, not silently fixed"
    - "CLAUDE.md points at the matrix as the authority, with a maintenance trigger so it does not rot"
  artifacts:
    - path: ".planning/codebase/CAPABILITY-MATRIX.md"
      provides: "The role × capability authority document"
      min_lines: 120
      contains: "Obligation channel"
    - path: "CLAUDE.md"
      provides: "Pointer to the matrix as the single authority + maintenance trigger"
      contains: ".planning/codebase/CAPABILITY-MATRIX.md"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/codebase/CAPABILITY-MATRIX.md"
      via: "documented path reference in a `## Capability Matrix` section"
      pattern: "codebase/CAPABILITY-MATRIX\\.md"
    - from: ".planning/codebase/CAPABILITY-MATRIX.md"
      to: "src/lib/auth/guards.ts, supabase/migrations"
      via: "Enforcement-point index column naming where each gate lives"
      pattern: "guards\\.ts"
---

<objective>
CAP-01: write the one role × capability matrix that is the single reference for who can see and do what, and wire CLAUDE.md to it as the authority.

Purpose: today the answer to "who can do X" is spread across RLS policies in six migrations, `requireAdminContext()` call sites, and ad-hoc role checks — which is exactly how the 00061/00062 cross-tenant holes went unnoticed. One table, one place, with the enforcement point named per row, makes the next gap visible on read instead of on incident.
Output: `.planning/codebase/CAPABILITY-MATRIX.md` + a `## Capability Matrix` section in CLAUDE.md + the CAP-01 gate flipped live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-capability-matrix/46-CONTEXT.md
@.planning/phases/46-capability-matrix/46-RESEARCH.md
@.planning/phases/46-capability-matrix/46-PATTERNS.md
@tests/phase46/capability-matrix-doc.spec.ts
</context>

## Artifacts this phase produces

| Artifact | Plan | Kind |
|---|---|---|
| `.planning/codebase/CAPABILITY-MATRIX.md` | 46-02 (this plan) | doc |
| `CLAUDE.md` → `## Capability Matrix` section | 46-02 (this plan) | doc |
| `playwright.config.ts` → `phase46` project · 3 specs in `tests/phase46/` | 46-01 | config/spec |
| `requireSopEditAccess(target)` in `src/lib/auth/guards.ts` · migration `00063_sop_content_owner_edit.sql` · `scripts/apply-phase46-migration.mjs` | 46-03 | code/migration/script |

<tasks>

<task type="auto">
  <name>Task 1: Write the capability matrix document</name>
  <files>.planning/codebase/CAPABILITY-MATRIX.md</files>
  <read_first>
    - tests/phase46/capability-matrix-doc.spec.ts (the spec IS the contract — every required row label, legend marker, channel heading and phase reference it asserts must appear verbatim)
    - .planning/phases/46-capability-matrix/46-RESEARCH.md § Role Inventory, § Capability Inventory Sweep, § SOP Edit Path Inventory
    - .planning/codebase/CONVENTIONS.md (house table/doc style for `.planning/codebase/` documents)
    - src/types/auth.ts (the canonical org-role union)
    - src/lib/auth/guards.ts and src/lib/auth/platform-admin-guard.ts (where the gates actually live — the enforcement-point column cites these)
  </read_first>
  <action>
    Create `.planning/codebase/CAPABILITY-MATRIX.md` with these sections, in order:

    1. Header: title, `Last updated: 2026-08-25`, and a one-line authority statement — this document is the single reference for who can see and do what; when a capability gate changes, this file changes in the same commit.

    2. `## Two channels (D1: obligation ≠ access)` — define `Access channel` (whether a role can reach and act on a surface; enforced by RLS + server-action guards, shipped today) and `Obligation channel` (whether a role is *required* to complete a SOP; a manager-set record shipping in Phase 44a, today implied by assignment). State explicitly that the matrix below is the ACCESS channel, and that obligation is tracked separately and must never be inferred from access.

    3. `## Legend` — the three cell markers as literal strings the spec pins: `shipped-and-enforced`, `shipped-but-unenforced`, `planned` (each with a short symbol, e.g. ✅ / ⚠ / 🕒, and `—` for not-applicable).

    4. `## Matrix (access channel)` — one markdown table. Columns: `Capability` | `worker` | `supervisor` | `admin` | `safety_manager` | `SOP owner (any role)` | `Enforced at`. Rows, using these exact labels: Read SOP, Walk SOP, Self-add SOP, Record completion, Sign off completion, Record observation, Create SOP, Edit SOP content, Verify blocks, Publish SOP, Delete SOP, Version history, Governance queue, Approval chains, Manage team, Manage departments, Manage blocks library, AI settings, Training matrix, Assessor governance, Export training records, Own profile. Fill every cell from RESEARCH § Capability Inventory Sweep; the `Enforced at` column names the concrete gate (e.g. `RLS admins_can_manage_sections` + `requireSopEditAccess` in `src/lib/auth/guards.ts`, or `requireAdminContext()` in `src/lib/auth/guards.ts`, or `platform-admin-guard.ts`). Where a row's gate is RLS-only or app-guard-only, say which — that asymmetry is what made the legacy PATCH route invisible.

    5. `## Sign-off authority and edit rights (CAP-02)` — state the mapping in force: sign-off authority on a SOP = `sops.owner_user_id` (Phase 28 single accountable owner), and that per the locked decision an owner gets CONTENT edit rights on that SOP (sections, steps, images, layout_data, block junctions) while publish, verify-blocks, delete, version-supersede and owner-reassignment stay admin/safety_manager-only. Flag assumption A1 in bold as an open confirmation for Simon: if "sign-off authority" was meant as approval-chain approvers (Phase 29 `approval_chains`/`sop_approvals`), this row changes from one-owner-per-SOP to category-scoped multi-approver and the predicate swaps — one line of code, but the matrix semantics change.

    6. `## Roles that are not rows` — `platform_admin` (Potenco-level, `platform_admins` table, gates `/admin/global-blocks` only; orthogonal to org roles, and per CLAUDE.md § Ownership never conflated with Summit Insights) and dept-scoped job roles (Phase 32 `roles` table — an org-chart entity, not a capability gate anywhere in code today; becomes a visibility axis in Phase 44b).

    7. `## Planned capabilities` — rows for the not-yet-shipped capabilities with their phase marker: obligation record (Phase 44a), role-ladder visibility (Phase 44b), view-as-role (Phase 45), edit history read-only to everyone with SOP access (Phase 47, D5), worker feedback with moderated removals (Phase 48, D4). Each marked `planned`, never mixed into the shipped cells.

    8. `## Findings — shipped-but-unenforced` — record every gap observed while writing this document, with file path and current state. At minimum: the legacy `PATCH /api/sops/[sopId]/sections/[sectionId]` route carried no app-level guard (RLS-only) until Phase 46 plan 03; `createSection` in `src/actions/sections.ts` was RLS-only; `sop_section_blocks` RLS is `using(true)` by the 00030/00031 recursion-avoidance design so its app-level guard is the ONLY enforcement. Do not fix anything here beyond what 46-03 covers — record it.

    9. `## Maintenance` — the GSD trigger list mirroring CLAUDE.md § Pathways Map Maintenance: update this file in the same commit as any change to an RLS policy, a `require*` guard, or a role check; verify at `/gsd-code-review` and `/gsd-verify-work`.

    Every cell claim must be traceable to a file — do not assert a capability from memory. Where RESEARCH's sweep is ambiguous for a role, grep the action/route before writing the cell.
  </action>
  <verify>
    <automated>npx playwright test tests/phase46/capability-matrix-doc.spec.ts --project=phase46</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/CAPABILITY-MATRIX.md` exists and contains all 22 capability row labels, all 4 org roles, `platform_admin`, all three legend markers, both `Access channel` and `Obligation channel`, `sign-off authority`, `owner_user_id`, and `Phase 44a`/`Phase 44b`/`Phase 45`/`Phase 47`/`Phase 48`
    - The matrix table has an `Enforced at` column and every shipped row names a concrete file or policy in it (`grep -c "guards.ts\|RLS \|platform-admin-guard" .planning/codebase/CAPABILITY-MATRIX.md` ≥ 15)
    - The `## Findings — shipped-but-unenforced` section is present and non-empty (contains at least the PATCH route and `createSection` entries)
    - Assumption A1 is stated in the CAP-02 section as an open confirmation, not as settled fact
    - The doc spec still reports fixme at this point (it is activated in Task 2) — no assertion changes in this task
  </acceptance_criteria>
  <done>One document answers "who can do what" for every capability the app surfaces, names the enforcement point per row, keeps access and obligation separate, and records the unenforced gaps it found.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CLAUDE.md to the matrix and activate the CAP-01 gate</name>
  <files>CLAUDE.md, tests/phase46/capability-matrix-doc.spec.ts</files>
  <read_first>
    - CLAUDE.md (full — specifically `## Auto-load routing` and `## Pathways Map Maintenance (journeys.ts) — GSD triggers`, the convention this section mirrors)
    - .planning/codebase/CAPABILITY-MATRIX.md (written in Task 1)
    - tests/phase46/capability-matrix-doc.spec.ts (remove the fixme markers here)
  </read_first>
  <action>
    Add a `## Capability Matrix` section to `CLAUDE.md`, placed immediately before `## Pathways Map Maintenance (journeys.ts) — GSD triggers`. Contents: one sentence naming `.planning/codebase/CAPABILITY-MATRIX.md` as the single authority on who can see and do what (roles × capabilities, access channel), one sentence stating that obligation is a separate channel tracked by the Phase 44a record and must never be inferred from access, and a three-bullet maintenance trigger: (1) any change to an RLS policy, a `require*` guard in `src/lib/auth/guards.ts`, or a role check in `src/actions/*` updates the matrix in the same commit; (2) `/gsd-plan-phase` adds the matrix edit as an explicit task when a phase changes a capability gate; (3) `/gsd-code-review` and `/gsd-verify-work` confirm the matrix matches the gates the phase touched. Keep it to ~8 lines — CLAUDE.md is already long and every line competes for attention.

    Then remove every `test.fixme` marker from `tests/phase46/capability-matrix-doc.spec.ts` (leave the assertions untouched) and run the spec. If any assertion fails, fix the DOCUMENT to satisfy the spec — do not weaken the spec. If an assertion is genuinely wrong (a row label the app does not have), correct it and record why in the plan summary.

    Mutation-proof the gate before finishing: temporarily delete one required row label from the matrix document, confirm the spec goes RED naming that label, restore it, confirm GREEN. A gate that has never been observed failing is not known to be a gate (CLAUDE.md 2026-05-25 / 2026-06-05).
  </action>
  <verify>
    <automated>npx playwright test --project=phase46 --grep-invert "live"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CAPABILITY-MATRIX.md" CLAUDE.md` returns ≥ 1 and the reference sits in a `## Capability Matrix` section
    - `grep -v '^\s*//' tests/phase46/capability-matrix-doc.spec.ts | grep -c "test.fixme"` returns 0
    - `npx playwright test tests/phase46/capability-matrix-doc.spec.ts --project=phase46` passes with every test reported as passed (not fixme, not skipped)
    - The mutation check was performed: the plan summary records which row label was removed and that the spec named it in the failure output
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>CLAUDE.md names the matrix as the authority with a maintenance trigger, and the CAP-01 gate is live and mutation-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document → reader | The matrix is a security-relevant claim surface: a wrong "shipped-and-enforced" cell tells a future planner a gate exists that does not, which is worse than no document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Spoofing (of assurance) | `.planning/codebase/CAPABILITY-MATRIX.md` cells | mitigate | Every shipped row must name its enforcement point in the `Enforced at` column, traceable to a file; cells are written from greps of the actual gate, not from memory |
| T-46-02-02 | Repudiation | unenforced gaps found during the sweep | mitigate | `## Findings — shipped-but-unenforced` section is mandatory and asserted non-empty; gaps are recorded rather than silently absorbed |
| T-46-02-03 | Tampering (doc rot) | matrix drifting from code | accept + mitigate | Cannot be fully mechanised this phase; mitigated by the CLAUDE.md maintenance trigger and the doc-spec's pinned row set, which goes RED if a row label is deleted |
| T-46-02-04 | Information Disclosure | matrix describing internal gates | accept | The document lives in `.planning/` inside a private repo; it names policy names and file paths already visible in the codebase |
</threat_model>

<verification>
- `npx playwright test --project=phase46 --grep-invert "live"` — the CAP-01 spec passes, the CAP-02 specs remain fixme (46-03 activates them)
- `npx tsc --noEmit` passes
- `npm run build` passes (CLAUDE.md/doc edits cannot break it, but the phase gate requires a real `next build` — CLAUDE.md 2026-06-27)
</verification>

<success_criteria>
CAP-01 satisfied: one in-repo matrix covers every role × every capability the app surfaces, separates access from obligation, marks planned capabilities with their phase, records the unenforced gaps it found, and is referenced from CLAUDE.md as the authority — with an automated gate that fails if a row disappears.
</success_criteria>

<output>
Create `.planning/phases/46-capability-matrix/46-02-SUMMARY.md` when done
</output>
