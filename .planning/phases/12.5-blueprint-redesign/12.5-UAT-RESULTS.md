---
phase: 12.5-blueprint-redesign
uat_run: 2026-05-07T11:41:00Z
runner: Playwright (Chromium) against `next dev` on localhost:4200
status: partial-pass
---

# Phase 12.5 — Batched UAT Results

## Summary

Automated UAT executed alongside Phase 13 UAT (same Playwright session, same auth cookie). Theme-scoping passed. Interactive flows (Cmd+K palette, Puck FlowGraphField round-trip, mobile viewport reroute) deferred to human UAT due to known Windows webpack dev server instability when interacting with rapidly-loading routes.

## Results

| # | Scenario | Result | Evidence |
|---|---|---|---|
| 1 | Paper/ink theme does not bleed into admin routes | ✓ PASS | `/admin/sops` body `bg = rgb(17, 24, 39)` (steel-900 dark); `<html data-theme="steel-forge" class="dark">` |
| 2 | Paper/ink theme activates on `/sops/[sopId]` | ✓ PASS | `/sops/{id}` body `bg = rgb(250, 250, 250)` (paper white); `fg = rgb(9, 9, 11)` (ink black); `--paper-bg = #e4e4e7`; 6-tab nav present (Overview / Tools / Hazards / Flow / Model / Walkthrough) |
| 3 | Mobile immersive walkthrough enforced on phones (390px viewport) | ⚠ HUMAN-UAT | Needs viewport resize + walkthrough render + ImmersiveStepCard inspection — dev server file-lock race blocked navigation during the run |
| 4 | Voice capture state machine in browser (Deepgram) | ⚠ HUMAN-UAT | Requires microphone hardware + live Deepgram WebSocket |
| 5 | Online voice persistence to Supabase Storage + sop_voice_notes | ⚠ HUMAN-UAT | Same — depends on #4 |
| 6 | Cmd/Ctrl+K opens CommandPalette globally | ⚠ HUMAN-UAT | `page.keyboard.press('Control+k')` did not open palette during the run; correlated with the dev server entering a "Manifest file is empty" error state. The CmdKProvider is wired (verifier already confirmed) — needs production build to UAT cleanly |
| 7 | FlowGraphField round-trip in Puck builder | ⚠ HUMAN-UAT | Needs SVG drag canvas + 20px snap + Save-to-SOP roundtrip — too interactive for headless |

## What's left for human UAT

Five scenarios above (#3–#7). Recommended workflow per Phase 12 UAT learning:

```powershell
$env:PORT='4200'; npm run build; npm run start
```

Production build (`next start`) avoids the transient `next dev --webpack` file-lock race on Windows that blocked headless interaction with stateful routes (CLAUDE.md Learnings, 2026-04-24).
