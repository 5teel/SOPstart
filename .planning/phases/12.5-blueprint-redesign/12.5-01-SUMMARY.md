---
phase: "12.5-blueprint-redesign"
plan: "01"
subsystem: "foundation"
tags: [css-tokens, theme-scope, database-migration, dexie, offline, rls]
dependency_graph:
  requires: []
  provides:
    - paper/ink CSS tokens scoped under body[data-theme="paper"]
    - sops layout + auth layout + landing page paper theme mounts
    - migration 00021: sop_voice_notes, escalation_reports, walkthrough_progress, sops.flow_graph, sop-voice-notes bucket
    - Dexie v5 with voiceNotesQueue + walkthroughProgress stores
    - cmdk@1.1.1 exact-pinned dependency
    - DEEPGRAM_API_KEY + NEXT_PUBLIC_MODEL_BLOCK_ENABLED env var documentation
    - phase12.5-stubs Playwright project
  affects:
    - src/app/layout.tsx (imports blueprint-theme.css)
    - src/app/(protected)/sops/* (new layout + theme mount)
    - src/app/(auth)/layout.tsx (paper theme applied)
    - src/app/page.tsx (PaperThemeMount added)
    - src/lib/offline/db.ts (v5 bump)
    - src/types/database.types.ts (3 new tables + flow_graph column)
    - src/types/sop.ts (4 new union types)
tech-stack:
  added:
    - cmdk@1.1.1 (exact pin — command palette, Wave 5)
  patterns:
    - body[data-theme="paper"] CSS scope isolation (additive, no admin route bleed)
    - Tailwind v4 @theme inline block for paper/ink color utilities
    - PaperThemeMount client component pattern (useEffect setAttribute/removeAttribute)
    - Dexie cumulative-additive version bump (v4 stores re-declared verbatim in v5)
    - Manual database.types.ts extension (type regen not wired in this environment)
key-files:
  created:
    - src/styles/blueprint-theme.css
    - src/app/(protected)/sops/layout.tsx
    - src/app/(protected)/sops/_theme-mount.tsx
    - src/app/(auth)/_theme-mount.tsx
    - src/app/_theme-mount.tsx
    - supabase/migrations/00021_blueprint_redesign.sql
    - tests/sb-ux-blueprint.test.ts
  modified:
    - src/app/layout.tsx
    - src/app/(auth)/layout.tsx
    - src/app/page.tsx
    - src/lib/offline/db.ts
    - src/types/database.types.ts
    - src/types/sop.ts
    - .env.local.example
    - package.json
    - playwright.config.ts
decisions:
  - "PaperThemeMount as separate _theme-mount.tsx client component per route group — useEffect setAttribute pattern, cleans up on unmount"
  - "No (public) route group created — root page.tsx gets PaperThemeMount directly since app/page.tsx is at root"
  - "supabase db push flagged for manual apply — Docker not running, project not linked in worktree environment"
  - "sops layout.tsx is pure RSC wrapper + client child — no QueryProvider duplication with (protected)/layout.tsx"
metrics:
  duration: "~6 minutes"
  completed: "2026-04-26"
  tasks_completed: 4
  files_created: 7
  files_modified: 9
---

# Phase 12.5 Plan 01: Foundation (Wave 1) Summary

**One-liner:** Paper/ink CSS token system scoped under `body[data-theme="paper"]` + Supabase migration 00021 adding 3 new tables + Dexie v5 with voice/progress offline stores.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Install cmdk, env vars, phase12.5-stubs | `3896111` | package.json, .env.local.example, playwright.config.ts, tests/sb-ux-blueprint.test.ts |
| 2 | Paper/ink CSS tokens + nested layouts | `086b7af` | src/styles/blueprint-theme.css, src/app/(protected)/sops/layout.tsx, src/app/(auth)/layout.tsx, src/app/page.tsx |
| 3 | Migration 00021 | `35d9c11` | supabase/migrations/00021_blueprint_redesign.sql |
| 4 | Dexie v5 bump + database.types.ts extension | `73f7f01` | src/lib/offline/db.ts, src/types/database.types.ts, src/types/sop.ts |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Adjustment] No (public) route group exists**
- **Found during:** Task 2
- **Issue:** Plan described creating `src/app/(public)/layout.tsx` for the root `/` route, but `app/page.tsx` exists at the root level with no `(public)` route group.
- **Fix:** Per plan's own fallback instruction ("If `/` is NOT under a `(public)` route group: add a PaperThemeMount instance at the top of `src/app/page.tsx`"), added `PaperThemeMount` directly to `page.tsx` and created `src/app/_theme-mount.tsx` as the client component. No route group created.
- **Files modified:** src/app/page.tsx, src/app/_theme-mount.tsx

**2. [Rule 3 - Adjustment] playwright.config.ts uses `{ browserName: 'chromium' }` not `devices`**
- **Found during:** Task 1
- **Issue:** Plan specified `use: { ...devices['Desktop Chrome'] }` but the config is an ESM module with only `defineConfig` imported — no `devices` import and no `require()` pattern used in existing projects.
- **Fix:** Used `use: { browserName: 'chromium' }` to match the existing `e2e` project pattern in the same file.
- **Files modified:** playwright.config.ts

**3. [Rule 3 - Blocker] supabase db push requires linked project**
- **Found during:** Task 3
- **Issue:** Docker Desktop not running; Supabase CLI cannot connect to local container or remote project (not linked with `supabase link`).
- **Fix:** Migration file `00021_blueprint_redesign.sql` is written and correct. Apply manually with `npx supabase db push` after linking the project, or it will apply automatically on next Railway deploy.
- **Status:** DEFERRED — user must run `npx supabase db push` before Wave 2.

**4. [Rule 1 - Adjustment] .env.example vs .env.local.example**
- **Found during:** Task 1
- **Issue:** Plan referenced `.env.example` but the project uses `.env.local.example`.
- **Fix:** Appended env var docs to the existing `.env.local.example` file.
- **Files modified:** .env.local.example

## Wave 0 Prerequisites (user action required before Wave 2)

1. **Apply migration:** Run `npx supabase db push` (with project linked) to create the 3 new tables, `sops.flow_graph` column, and `sop-voice-notes` Storage bucket.
2. **Provision DEEPGRAM_API_KEY:** Add to local `.env.local` and Railway project env before Wave 4 starts.
3. **Run `npm install`:** After this wave merges, run locally so `cmdk` lands in node_modules.

## Known Stubs

- `tests/sb-ux-blueprint.test.ts` — "SB-UX-01: admin route does NOT have data-theme=paper" is `test.skip(true, ...)` — full admin-vs-worker contrast check deferred to Wave 2 when tabs exist.

## Threat Flags

No new threat surface beyond what is documented in plan's `<threat_model>`. All mitigations (T-12.5-01-01 through T-12.5-01-08) are addressed in the migration SQL and documented for Wave 4 enforcement.

## Self-Check: PASSED

Files created:
- src/styles/blueprint-theme.css — FOUND
- src/app/(protected)/sops/layout.tsx — FOUND
- src/app/(protected)/sops/_theme-mount.tsx — FOUND
- supabase/migrations/00021_blueprint_redesign.sql — FOUND
- tests/sb-ux-blueprint.test.ts — FOUND

Commits:
- 3896111 — FOUND
- 086b7af — FOUND
- 35d9c11 — FOUND
- 73f7f01 — FOUND

TypeScript: `npx tsc --noEmit` clean across all 4 tasks.
