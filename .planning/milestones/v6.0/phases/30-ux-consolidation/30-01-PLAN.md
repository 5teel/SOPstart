---
phase: 30-ux-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/phase30/role-homes.spec.ts
  - tests/phase30/admin-nav.spec.ts
  - tests/phase30/create-entry.spec.ts
  - tests/phase30/tab-merge.spec.ts
  - tests/phase30/list-rows.spec.ts
  - tests/phase30/plain-language.spec.ts
  - tests/phase30/governance-fold.spec.ts
  - tests/phase30/dead-weight.spec.ts
  - tests/integration/scp-source-viewer.test.ts
  - tests/integration/scp-parse-pipeline.test.ts
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx
  - scripts/check-bundle-size.ts
autonomous: true
requirements: [UX-05, UX-08]
must_haves:
  truths:
    - "`npx playwright test --list --project=phase30` lists all 8 phase30 spec files (zero discovered = FAIL)"
    - "`npx playwright test --project=phase21-stubs` is fully green (the 2 pre-existing scp-source-viewer failures are cleared)"
    - "BuilderWithSourceViewer.tsx no longer exists and no src file imports or comment-references it"
    - "The current worker /sops/[sopId] First Load JS baseline is recorded for post-merge comparison"
  artifacts:
    - path: "tests/phase30"
      provides: "Nyquist harness — 8 source-contract spec stubs, one per UX requirement, registered before any Phase 30 code ships"
    - path: "playwright.config.ts"
      provides: "phase30 project registration (broad tests/phase30/** testMatch)"
  key_links:
    - from: "tests/integration/scp-source-viewer.test.ts"
      to: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx"
      via: "readFileSync path constant repointed off deleted BuilderWithSourceViewer.tsx"
      pattern: "BuilderStageShell"
---

<objective>
Wave-0 test harness for Phase 30: register a single broad `phase30` Playwright project, create one source-contract spec stub per UX requirement (they assert eventual contracts and start as `test.fixme` or current-state-tolerant so the suite is green now and each later plan fills its own spec), fix the 2 pre-existing `scp-source-viewer.test.ts` failures on master, delete the orphan `BuilderWithSourceViewer.tsx` (UX-08) and repoint the scp specs that read it, and record the worker-route bundle baseline for the UX-05 tab-merge comparison.

Purpose: Every later plan gates against `--project=phase30`; without this scaffold and a clean baseline, verification cannot distinguish Phase 30 regressions from pre-existing debt (RESEARCH Pitfall 7).
Output: `tests/phase30/` (8 specs) + phase30 project + cleared scp failures + BuilderWithSourceViewer gone + recorded bundle baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-VALIDATION.md
</context>

<artifacts_produced>
- `phase30` Playwright project (config entry, broad `tests/phase30/**` testMatch).
- `tests/phase30/{role-homes,admin-nav,create-entry,tab-merge,list-rows,plain-language,governance-fold,dead-weight}.spec.ts` (8 source-contract stubs).
- DELETED: `src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx` (legacy, no real importer).
- Repointed spec path constants in `scp-source-viewer.test.ts` + `scp-parse-pipeline.test.ts` (→ `BuilderStageShell.tsx` / the shell chain file that actually carries each token); cleaned 2 stale src comment references (BuilderClient.tsx, useSelectionSync.tsx) + the stale filename in `scripts/check-bundle-size.ts` message.
- Recorded bundle baseline note (worker `/sops/[sopId]` First Load JS, currently 1057 KB per `.bundle-baseline.json`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Register phase30 Playwright project + create 8 source-contract spec stubs</name>
  <files>playwright.config.ts, tests/phase30/role-homes.spec.ts, tests/phase30/admin-nav.spec.ts, tests/phase30/create-entry.spec.ts, tests/phase30/tab-merge.spec.ts, tests/phase30/list-rows.spec.ts, tests/phase30/plain-language.spec.ts, tests/phase30/governance-fold.spec.ts, tests/phase30/dead-weight.spec.ts</files>
  <read_first>
    - playwright.config.ts (the `phase28` and `phase29` project entries, lines 258-299 — copy their broad-testMatch idiom EXACTLY: `{ name: 'phase30', testDir: '.', testMatch: /tests\/phase30\/.*\.(spec|test)\.ts$/, use: { browserName: 'chromium' } }`)
    - tests/phase28/governance-queue.spec.ts (the `path.join(ROOT, ...)` + `fs.readFileSync` + `toContain` source-contract idiom to mirror)
    - 30-RESEARCH.md § "Phase Requirements → Test Map" (the per-requirement behaviour each spec eventually asserts) and § "Test Repointing Inventory"
    - CLAUDE.md learning 2026-05-25 (unregistered specs never run — verify with `--list`)
  </read_first>
  <action>
    Add ONE project to `playwright.config.ts` mirroring the phase28/29 entries. Create the 8 stub specs under `tests/phase30/`, one per requirement (role-homes=UX-01, admin-nav=UX-02, create-entry=UX-04, tab-merge=UX-05, list-rows=UX-06, plain-language=UX-07, governance-fold=UX-03, dead-weight=UX-08). Each stub uses the phase28 `readFileSync` source-contract idiom and states the eventual assertions from RESEARCH § Test Map, wrapped in `test.fixme(...)` (or an assertion true at Phase-30-head) so the suite is GREEN now and each later plan flips its own `fixme`→live. Do NOT assert future contracts as live yet.
  </action>
  <acceptance_criteria>
    - `npx playwright test --list --project=phase30` lists all 8 spec files (grep the output for each filename; zero discovered = FAIL).
    - `npx playwright test --project=phase30` exits 0.
    - `playwright.config.ts` contains exactly one `name: 'phase30'` entry with testMatch `tests\/phase30\/`.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --list --project=phase30 && npx playwright test --project=phase30</automated>
  </verify>
  <done>phase30 project registered and green; 8 stubs discoverable.</done>
</task>

<task type="auto">
  <name>Task 2: Delete BuilderWithSourceViewer, repoint scp specs, clear pre-existing failures, record bundle baseline</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx, tests/integration/scp-source-viewer.test.ts, tests/integration/scp-parse-pipeline.test.ts, scripts/check-bundle-size.ts</files>
  <read_first>
    - tests/integration/scp-source-viewer.test.ts (SCP-VIEWER-01 asserts builder page contains 'BuilderWithSourceViewer' — stale since Phase 26; SCP-VIEWER-03 asserts registerBlockClickHandler in BuilderClient; plus existsSync/readFile on BuilderWithSourceViewer.tsx)
    - tests/integration/scp-parse-pipeline.test.ts (SCP-PARSE-03 readFile on BuilderWithSourceViewer.tsx)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx and its import chain (grep for the tokens each spec needs: `SourceViewerPane`, `dynamic(`, `showPane`, `ai_prompt` — RESEARCH Assumption A2: some live in the chain, e.g. src/components/admin/source-viewer/, not the shell file; find the real file per token)
    - The 2 stale src comment refs to clean: BuilderClient.tsx line ~41, useSelectionSync.tsx line ~24; and scripts/check-bundle-size.ts line ~277 (message string names the deleted file)
    - 30-RESEARCH.md § "Test Repointing Inventory" (scp rows) + § "State of the Art" (Puck→bespoke context explaining the stale assertions)
    - CLAUDE.md learning 2026-06-08 (dead refs after deletion) + 2026-07-03 (BuilderStageShell superseded BuilderWithSourceViewer)
  </read_first>
  <action>
    Confirm `BuilderWithSourceViewer.tsx` has ZERO real IMPORTs (grep `import.*BuilderWithSourceViewer` across `src` — expect none; the file is legacy), then delete it. Remove the 2 stale comment references in `BuilderClient.tsx` + `useSelectionSync.tsx` and fix the stale filename in the `scripts/check-bundle-size.ts` message so `grep -rn "BuilderWithSourceViewer" src` == 0. For each scp spec, repoint the `existsSync`/`readFileSync` path constant off the deleted file onto the builder-shell-chain file that actually contains the asserted token (verify per token by grep — do NOT assume all four tokens live in `BuilderStageShell.tsx`). Fix the 2 stale assertions: SCP-VIEWER-01 (assert the real current builder page/shell token) and SCP-VIEWER-03 (repoint to the file that now owns the block-click handler wiring). Record the current worker `/sops/[sopId]` First Load JS baseline value from `.bundle-baseline.json` (1057 KB) into the SUMMARY as the pre-merge reference — do NOT hand-edit `.bundle-baseline.json`. Leave the negative assertion in `tests/builder/builder-review-flow.spec.ts` (page.tsx must NOT import BuilderWithSourceViewer) — it stays valid.
  </action>
  <acceptance_criteria>
    - `grep -rn "BuilderWithSourceViewer" src` returns 0 hits (file gone + 2 comments cleaned; scripts/tests may retain a descriptive/negative reference).
    - `[ ! -f "src/app/(protected)/admin/sops/builder/[sopId]/BuilderWithSourceViewer.tsx" ]` is true.
    - `npx playwright test --project=phase21-stubs` exits 0 (was 2 failed / 3 passed on master — now fully green).
    - Neither scp spec references `BuilderWithSourceViewer` in a `readFileSync`/`existsSync` path.
    - SUMMARY records the pre-merge worker-route First Load JS baseline (1057 KB).
    - `npm run test` (full suite) run once after the scp fix; SUMMARY records the full-suite result (pass/fail counts + names of any remaining pre-existing failures/fixmes) as the Phase-30 regression BASELINE that 30-08's final gate compares against ("no NEW failures").
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --project=phase21-stubs</automated>
  </verify>
  <done>BuilderWithSourceViewer deleted; scp specs repointed and green; stale refs cleaned; bundle baseline recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Test scaffold + dead-code deletion; no runtime trust boundary crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering | scp spec repoint targeting wrong file | mitigate | Per-token grep verifies the repoint target actually contains the asserted token before committing |
| T-30-01-02 | Availability | deleting BuilderWithSourceViewer breaks the live source viewer | mitigate | Confirmed no real import (grep `import.*BuilderWithSourceViewer` == 0 in src); SourceViewerPane is dynamic-imported via the BuilderStageShell chain, not the deleted legacy copy (RESEARCH A2) — `npm run build` + phase21-stubs green prove it |
| T-30-01-SC | Tampering | npm installs | accept | No package installs this phase — N/A (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx playwright test --list --project=phase30` shows 8 specs.
- `npx playwright test --project=phase30 --project=phase21-stubs` green.
- `npx tsc --noEmit` clean; `npm run build` succeeds (proves the source viewer still mounts via the shell chain).
</verification>

<success_criteria>
phase30 harness registered and green; BuilderWithSourceViewer removed with scp specs repointed and stale refs cleaned; the 2 pre-existing scp failures cleared; bundle baseline recorded for UX-05.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-01-SUMMARY.md` when done.
</output>
