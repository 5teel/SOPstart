---
phase: 30-ux-consolidation
plan: 04
type: execute
wave: 4
depends_on: [30-02, 30-03]
files_modified:
  - src/components/layout/TopHeader.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase30/dead-weight.spec.ts
autonomous: true
requirements: [UX-01, UX-02, UX-08]
must_haves:
  truths:
    - "The account menu shows a single 'Admin' link (to /admin/sops), gated to admin/safety_manager — the 4-item ADMIN_LINKS list is gone"
    - "No primary-nav item points at /dashboard; the brand/logo link goes to the viewer's role home"
    - "The fake notifications bell (linked to /sops) is deleted from TopHeader; NotificationBadge stays where BottomTabBar uses it legitimately"
    - "/pathways and /uat are moved out of primary nav into the account menu (pages kept — team tooling)"
  artifacts:
    - path: "src/components/layout/TopHeader.tsx"
      provides: "Consolidated global header — one Admin link, no Dashboard, no fake bell, Pathways/UAT in account menu"
  key_links:
    - from: "src/components/layout/TopHeader.tsx"
      to: "roleHome(role)"
      via: "brand link + account 'Admin' link resolve destinations"
      pattern: "roleHome|/admin/sops"
---

<objective>
Consolidate the global header (UX-02 account menu + UX-01 nav landing + UX-08 dead weight). Collapse the 4-item `ADMIN_LINKS` to one "Admin" link → /admin/sops (admin/safety_manager only); remove "Dashboard" from `BASE_LINKS` and point the brand link at `roleHome(role)`; delete the fake notifications bell (links to /sops, no screen exists); move Pathways + UAT out of primary nav into the account menu (keep the pages). Verify BottomTabBar carries no /dashboard link.

Purpose: One door to admin, no dead Dashboard item, no placebo bell, internal tooling out of a factory worker's 5-item nav.
Output: TopHeader consolidated; dead-weight spec covers the bell + pathways move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
@src/components/layout/TopHeader.tsx
@src/lib/auth/role-home.ts
</context>

<artifacts_produced>
- TopHeader: `ADMIN_LINKS` collapsed to one `{ label: 'Admin', href: '/admin/sops' }` gated by `isAdminRole(role)`; `BASE_LINKS` loses Dashboard/Pathways/UAT; brand `href` → `roleHome(role)`; `isActive` special-case for `/dashboard` removed; notifications bell block (TopHeader:258-278) + its `NotificationBadge` import removed from the header.
- Pathways + UAT rendered as account-menu items (TopHeader menuitem pattern ~306-324).
- BottomTabBar verified free of `/dashboard`.
- journeys.ts nav-model entries reflect the header change.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Collapse account menu, remove Dashboard nav + fake bell, move Pathways/UAT</name>
  <files>src/components/layout/TopHeader.tsx, src/components/layout/BottomTabBar.tsx</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 1" (TopHeader BASE_LINKS[0]=Dashboard line 147, brand href line 225, isActive special-case 169) + § "Current Wiring 7" (notifications bell 258-278, NotificationBadge import line 7 — bell is fake, NotificationBadge stays for BottomTabBar) + § "Dead-Href Sweep A"
    - 30-PATTERNS.md § "src/components/admin/AdminNav.tsx" final para (BASE_LINKS 146-152 loses Dashboard/Pathways/Feedback; account-menu menuitem pattern 306-324; ADMIN_LINKS 157-162 → one Admin link; isAdminRole 164-166)
    - src/lib/auth/role-home.ts (from 30-02 — the brand-link destination)
    - CLAUDE.md memory "No Cmd+K" + learning 2026-06-08 (dead hrefs)
  </read_first>
  <action>
    In TopHeader: replace the `ADMIN_LINKS` array with a single `{ label: 'Admin', href: '/admin/sops' }` still gated by `isAdminRole(role)`. Remove `Dashboard` from `BASE_LINKS` and drop the `/dashboard` special-case in `isActive`; set the brand/logo `href` to `roleHome(role)` (TopHeader already has role). Delete the notifications bell block and its `NotificationBadge` import from TopHeader (leave `NotificationBadge` file + its BottomTabBar usage intact). Move Pathways + UAT from `BASE_LINKS` into the account-menu block using the existing menuitem markup. Grep BottomTabBar for `/dashboard` and repoint any hit to a real home (likely none — confirm).
  </action>
  <acceptance_criteria>
    - `grep -n "ADMIN_LINKS" src/components/layout/TopHeader.tsx` shows a single-item list (or the array is replaced by one Admin link); grep confirms exactly one admin href `/admin/sops`.
    - `grep -rn "/dashboard" src/components/layout/TopHeader.tsx src/components/layout/BottomTabBar.tsx` returns 0 hits.
    - Notifications bell removed: `grep -n "aria-label=\"Notifications\"" src/components/layout/TopHeader.tsx` returns 0 hits; `NotificationBadge` still imported by BottomTabBar (grep it present there).
    - `/pathways` and `/uat` appear inside the account-menu block, not `BASE_LINKS`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>One Admin link, no Dashboard nav, no fake bell, Pathways/UAT in account menu, BottomTabBar clean.</done>
</task>

<task type="auto">
  <name>Task 2: journeys nav model + dead-weight spec (bell + pathways move)</name>
  <files>src/lib/journeys/journeys.ts, tests/phase30/dead-weight.spec.ts</files>
  <read_first>
    - src/lib/journeys/journeys.ts (nav entries touching Dashboard/Pathways/UAT + brand)
    - 30-RESEARCH.md § "journeys.ts Impact" (which entries move) + § "Validation Architecture" UX-08 row
    - tests/phase30/dead-weight.spec.ts (the stub created in 30-01 — flip the TopHeader-related assertions live)
  </read_first>
  <action>
    Update journeys.ts so the nav model reflects the header: no Dashboard nav item, Pathways/UAT reachable via the account menu, brand→role home. Extend `dead-weight.spec.ts` (live, not fixme) to assert: TopHeader has no `aria-label="Notifications"` bell, no `/dashboard` string, exactly one admin href, and Pathways/UAT strings live in the account-menu region. Assert handler/href WIRING per CLAUDE.md 2026-06-05 (the Admin link's href resolves to /admin/sops, not just that the label appears).
  </action>
  <acceptance_criteria>
    - `npx playwright test tests/phase30/dead-weight.spec.ts --project=phase30` exits 0 with the TopHeader assertions live.
    - journeys.ts carries no Dashboard primary-nav entry.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/dead-weight.spec.ts --project=phase30</automated>
  </verify>
  <done>journeys nav model consistent; dead-weight spec asserts the TopHeader cleanup with wiring checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| non-admin viewer → account menu | Admin link visibility gated by isAdminRole(role) — visibility only, not access |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Elevation | account-menu Admin link | mitigate | Link gated by `isAdminRole(role)` exactly as ADMIN_LINKS was; the real gate is each admin page's server guard (unchanged) — a hidden link is not an access control |
| T-30-04-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `grep -rn "/dashboard" src/components/layout` == 0.
- `npx playwright test --project=phase30` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
UX-02/UX-01/UX-08 header slice: one Admin link, no Dashboard nav, brand→role home, fake bell deleted, Pathways/UAT relocated, journeys consistent.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-04-SUMMARY.md` when done.
</output>
