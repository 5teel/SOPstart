---
phase: 30-ux-consolidation
plan: 07
type: execute
wave: 7
depends_on: [30-05]
files_modified:
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
  - src/components/admin/ai-reviewer/FlagBadge.tsx
  - src/components/layout/OnlineStatusBanner.tsx
  - tests/phase30/list-rows.spec.ts
  - tests/phase30/plain-language.spec.ts
autonomous: true
requirements: [UX-06, UX-07]
must_haves:
  truths:
    - "The builder shell header hosts a labelled action menu for the per-SOP actions (Assign · Versions · Video · QR, plus Delete for drafts) — each is a labelled control, not an icon-only affordance"
    - "Builder stage labels read Check / Edit / Send to workers (display only; the BuilderStage 'build'|'review'|'publish' union + routing are unchanged)"
    - "AI reviewer flag titles are plain outcomes (Made-up content / Missing content / Picture not linked to its step / Table may be scrambled / Wording changed) with human step names — never 'block N'"
    - "Publish surface states reversibility ('You can unpublish or edit later'); the offline pill is plain-language"
  artifacts:
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx"
      provides: "Labelled per-SOP action menu (the 5 row actions' new home)"
    - path: "src/components/admin/ai-reviewer/FlagBadge.tsx"
      provides: "KIND_LABEL plain-title map for reviewer flags"
  key_links:
    - from: "BuilderStageShell action menu"
      to: "/admin/sops/[sopId]/assign | /versions | /video | /qr"
      via: "labelled Links (href wiring asserted, not just tokens)"
      pattern: "/admin/sops/.*(assign|versions|video|qr)"
    - from: "FlagBadge.tsx"
      to: "KIND_LABEL[flag.kind]"
      via: "plain title rendered from the reviewer kind, human step name for anchor"
      pattern: "KIND_LABEL"
---

<objective>
UX-06 (builder half) + UX-07 plain language. Give the 5 per-SOP actions a labelled home in the BuilderStageShell header (a labelled action menu — orchestrator decision #2), so the one-line rows in 30-08 can drop their icon-only actions. Apply the plain-language pass: builder stage labels → Check / Edit / Send to workers (display only), AI reviewer flag titles → plain outcomes with human step names (never "block N"), publish reversibility copy, plain offline pill. Tokenise the inline styles on any surface touched here.

Purpose: Actions get a discoverable labelled home (fixes usability-lab F-09 WCAG) and the app speaks human, not engineer.
Output: builder action menu + plain-language labels; list-rows + plain-language specs live for the builder half.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
</context>

<artifacts_produced>
- `BuilderStageShell.tsx`: labelled action menu in the header (next to the existing /admin/sops back-link) with Assign→`/admin/sops/[sopId]/assign`, Versions→`/versions`, Video→`/video`, QR→`/qr`, and Delete (drafts) — labelled controls with persistent labels/aria.
- `BuilderStageStepper.tsx`: StageChip display labels `Build→Edit`, `Review & verify→Check`, `Publish→Send to workers` (union + routing unchanged).
- `PublishStage.tsx`: "You can unpublish or edit later" near the publish button; inline `style={{}}` blocks replaced with CSS-var token classes.
- `FlagBadge.tsx`: `KIND_LABEL: Record<ReviewerFlagKind,string>` plain-title map; human step/section names instead of raw block ids.
- `OnlineStatusBanner.tsx`: plain-language offline copy.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Labelled per-SOP action menu in the builder shell header</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx, tests/phase30/list-rows.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "UX-06" (the 5 icon-only actions Pencil/Users/History/Video/QrCode at admin/sops/page.tsx:306-350 + their destinations; builder shell owns the back-link at BuilderClient.tsx:205; Delete must survive for drafts) + Open Question 2 (menu in shell header reachable in every stage — recommended)
    - 30-PATTERNS.md § "Builder labelled-action menu" (actions move here WITH labels; keep title/aria strings as visible labels) + § "Server-action mutation from client row" (useTransition + router.refresh for Delete)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx (header/back-link region) + the existing DeleteSopButton component
    - CLAUDE.md learning 2026-06-05 (assert the HANDLER/href wiring, not just token presence)
  </read_first>
  <action>
    Add a labelled action menu to the BuilderStageShell header: labelled Links Assign/Versions/Video/QR to the existing `/admin/sops/[sopId]/{assign,versions,video,qr}` routes, plus a Delete control (reuse `DeleteSopButton`) shown for drafts. CSS-var tokens only; each control has a visible label (not icon-only). Do NOT reimplement the destination routes — they already exist. Flip the `list-rows.spec.ts` stub's builder-half assertions live: the shell contains labelled actions wired to the 4 hrefs + Delete for drafts (assert the hrefs/handler, not just labels).
  </action>
  <acceptance_criteria>
    - BuilderStageShell contains labelled controls whose hrefs match `/admin/sops/.*(assign|versions|video|qr)` (grep all 4) and a Delete control for drafts.
    - Each action control has a visible text label or persistent aria-label (grep: no icon-only `evidence-btn` without label in the new menu).
    - `npx playwright test tests/phase30/list-rows.spec.ts --project=phase30` exits 0 for the builder-half assertions (row-simplification assertions arrive in 30-08).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/list-rows.spec.ts --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>The 5 per-SOP actions have a labelled home in the builder shell header, wired to their existing routes.</done>
</task>

<task type="auto">
  <name>Task 2: Plain-language stage labels + publish reversibility + tokenise PublishStage</name>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx, src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx</files>
  <read_first>
    - 30-RESEARCH.md § "Plain-Language Sources" (BuilderStageStepper.tsx:203-211 two StageChip arrays — Build→Edit, Review&verify→Check, Publish→Send to workers, union UNCHANGED; PublishStage reversibility + 9 inline style blocks to tokenise)
    - 30-PATTERNS.md § "Plain-language pass (UX-07) + tokenisation" (anti-pattern PublishStage:85-113 inline styles → token classes text-[var(--ink-900)] etc.)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageStepper.tsx + PublishStage.tsx
  </read_first>
  <action>
    Rename the StageChip DISPLAY labels only: Build→Edit, "Review & verify"→Check, Publish→"Send to workers". Do NOT touch the `BuilderStage` `'build'|'review'|'publish'` union, routing, or state keys. In PublishStage add "You can unpublish or edit later" near the publish button and replace the inline `style={{}}` blocks with CSS-var token classes (paper/ink + semantic accents).
  </action>
  <acceptance_criteria>
    - BuilderStageStepper display labels are Edit/Check/Send to workers (grep the three strings); the union `'build'|'review'|'publish'` is unchanged (grep still present).
    - PublishStage contains "You can unpublish or edit later"; `grep -c "style={{" src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx` is materially reduced (target 0 for the tokenised blocks).
    - `npm run build` succeeds (routing/state unchanged proven by clean build).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Stage chips read Check/Edit/Send to workers; publish states reversibility; PublishStage tokenised.</done>
</task>

<task type="auto">
  <name>Task 3: Plain AI-reviewer flag titles (no "block N") + offline pill + plain-language spec</name>
  <files>src/components/admin/ai-reviewer/FlagBadge.tsx, src/components/layout/OnlineStatusBanner.tsx, tests/phase30/plain-language.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Plain-Language Sources" (FlagBadge KIND_LABEL map values; ReviewerFlagKind enumerated in src/lib/parsers/ai-reviewer/types.ts:19-24; UI-side mapping ONLY — do NOT touch job prompts; "block N" ban across FlagBadge/ReviewerFlagsPanel/AdversarialFlagBanner; OnlineStatusBanner.tsx:24 offline copy)
    - src/lib/parsers/ai-reviewer/types.ts (the 5 kind strings: hallucination/omission/anchoring/table_fidelity/terminology)
    - src/components/admin/ai-reviewer/FlagBadge.tsx (current raw flag.kind title) + grep "Hallucination"/"block" across src/components/admin/ai-reviewer/
    - tests/phase30/plain-language.spec.ts (stub from 30-01 — flip live)
  </read_first>
  <action>
    Add `KIND_LABEL: Record<ReviewerFlagKind,string>` in FlagBadge: hallucination→'Made-up content', omission→'Missing content', anchoring→'Picture not linked to its step', table_fidelity→'Table may be scrambled', terminology→'Wording changed'. Render the plain title + human step/section name (never raw "block N") in FlagBadge and any sibling flag display (ReviewerFlagsPanel/AdversarialFlagBanner if they show titles/ids). Do NOT edit the reviewer job prompts or the stored `kind` strings. Plain-language the offline pill in OnlineStatusBanner (e.g. "No internet — your work is saved on this device"). Flip `plain-language.spec.ts` live: KIND_LABEL has the 5 titles, no "block N" string in flag UI, offline pill copy present, stage labels Check/Edit/Send to workers, publish reversibility present.
  </action>
  <acceptance_criteria>
    - FlagBadge contains `KIND_LABEL` with all 5 plain titles (grep each); `grep -rn "block N\|block \${" src/components/admin/ai-reviewer` returns 0 flag-title hits.
    - OnlineStatusBanner offline copy is the plain-language variant (grep the new string; old "Offline — changes saved locally" gone).
    - `npx playwright test tests/phase30/plain-language.spec.ts --project=phase30` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/plain-language.spec.ts --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>Reviewer flags speak plain outcomes with human step names; offline pill plain-languaged; plain-language spec live-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | Label/render-layer changes + labelled links to already-guarded routes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Tampering | reviewer flag titles are stored data | mitigate | KIND_LABEL is a UI-side map only; the stored `kind` strings + job prompts are untouched (RESEARCH: editing prompts risks the reviewer contract) — build proves no type/enum change |
| T-30-07-02 | Elevation | builder action-menu Delete | mitigate | Delete reuses the existing `DeleteSopButton` server-action path (org-scoped); no new deletion path added |
| T-30-07-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `npm run build` clean (union/routing/enum unchanged).
- `npx playwright test --project=phase30` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
UX-06 (builder half): 5 per-SOP actions labelled in the builder shell. UX-07: Check/Edit/Send-to-workers stage labels, plain reviewer flag titles with human step names, publish reversibility, plain offline pill, PublishStage tokenised.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-07-SUMMARY.md` when done.
</output>
