---
phase: 30-ux-consolidation
plan: 03
type: execute
wave: 3
depends_on: [30-01]
files_modified:
  - src/components/admin/AdminNav.tsx
  - src/app/(protected)/admin/settings/page.tsx
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/admin/governance/page.tsx
  - src/app/(protected)/admin/blocks/page.tsx
  - src/app/(protected)/admin/team/page.tsx
  - src/app/(protected)/admin/departments/page.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase30/admin-nav.spec.ts
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "One shared <AdminNav> renders the identical 5-item admin menu (SOPs · Governance · Blocks · Team · Settings) on every admin page"
    - "The 5 copy-pasted inline sub-navs are gone — zero `aria-label=\"Admin sections\"` inline nav blocks remain outside AdminNav"
    - "An /admin/settings surface exists grouping AI Settings, Departments, and the agent layer (currently-orphan /admin/agent) under one reachable home"
    - "AdminNav does NOT replace any per-page auth guard — every admin page keeps its own role gate verbatim"
  artifacts:
    - path: "src/components/admin/AdminNav.tsx"
      provides: "Shared admin sub-nav, active-state aware"
      exports: ["AdminNav"]
    - path: "src/app/(protected)/admin/settings/page.tsx"
      provides: "Admin Settings hub (AI Settings + Departments + agent-layer links)"
  key_links:
    - from: "src/app/(protected)/admin/*/page.tsx"
      to: "src/components/admin/AdminNav.tsx"
      via: "each admin page renders <AdminNav active=…/> in place of its inline sub-nav"
      pattern: "AdminNav"
---

<objective>
UX-02 One admin nav. Create a single `<AdminNav>` (SOPs · Governance · Blocks · Team · Settings) and mount it on all 5 admin pages, deleting the 5 disagreeing copy-pasted inline sub-navs. Create the `/admin/settings` surface (no route existed) grouping AI Settings, Departments, and the orphan agent layer (`/admin/agent`) under one home. The "Governance" nav item deep-links `/admin/sops?view=attention` (orchestrator decision #1) — the governance fold itself lands in 30-08. Per-page auth guards stay verbatim (AdminNav is presentation only).

Purpose: Kill duplicate #4 (3 disagreeing admin menus) and duplicate #7's cousin — one menu, one settings home, every admin surface consistent.
Output: AdminNav component, /admin/settings page, 5 sub-navs replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
@src/app/(protected)/admin/ai-settings/page.tsx
</context>

<artifacts_produced>
- `src/components/admin/AdminNav.tsx` — 5-item nav using the `.tab` idiom (`className="tab" data-active`), active state via an `active` prop OR `usePathname()`+isActive helper (RESEARCH/PATTERNS give both; pick the simpler for server-rendered pages).
- `src/app/(protected)/admin/settings/page.tsx` — Settings hub modelled on the `ai-settings/page.tsx` shell (61-line cleanest admin shell), keeping the `['admin','safety_manager']` guard; links to AI Settings, Departments, and the agent layer (gives `/admin/agent` its first inbound link).
- 5 admin pages mount `<AdminNav active="…" />`; inline sub-navs deleted.
- journeys.ts gains the `/admin/settings` screen and points the Governance nav item at `/admin/sops?view=attention`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: AdminNav component + /admin/settings hub</name>
  <files>src/components/admin/AdminNav.tsx, src/app/(protected)/admin/settings/page.tsx</files>
  <read_first>
    - 30-PATTERNS.md § "src/components/admin/AdminNav.tsx" (exact nav markup from admin/sops/page.tsx:154-171 + the isActive helper from TopHeader.tsx:168-171) and § "src/app/(protected)/admin/settings/page.tsx" (the ai-settings shell 14-31, 51-60)
    - src/app/(protected)/admin/ai-settings/page.tsx (the guard block + server-page shell to copy)
    - 30-RESEARCH.md § "Current Wiring 2" (the 5 sub-nav copies + line numbers; no /admin/settings route exists) + § "Architectural Responsibility Map"
  </read_first>
  <action>
    Build `AdminNav` with 5 `.tab` Links: SOPs→/admin/sops, Governance→/admin/sops?view=attention, Blocks→/admin/blocks, Team→/admin/team, Settings→/admin/settings. Active state from an `active` prop (server pages pass their key) or `usePathname()`. CSS-var tokens only. Build `/admin/settings/page.tsx` from the ai-settings shell: SAME guard block verbatim (`['admin','safety_manager']` → redirect), render `<AdminNav active="settings" />` + a tokenised section list linking to `/admin/ai-settings`, `/admin/departments`, and `/admin/agent`. Do NOT relocate ApprovalChainPanel here yet — that arrives with the governance fold in 30-08.
  </action>
  <acceptance_criteria>
    - `AdminNav.tsx` renders exactly 5 nav items (grep the 5 hrefs).
    - `/admin/settings/page.tsx` contains the `['admin', 'safety_manager']` guard string and links to `/admin/ai-settings`, `/admin/departments`, `/admin/agent`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AdminNav + Settings hub compile; Settings retains admin guard and homes the agent orphan.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the 5 inline sub-navs with AdminNav + journeys + spec</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/app/(protected)/admin/governance/page.tsx, src/app/(protected)/admin/blocks/page.tsx, src/app/(protected)/admin/team/page.tsx, src/app/(protected)/admin/departments/page.tsx, src/lib/journeys/journeys.ts, tests/phase30/admin-nav.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 2" table (exact sub-nav line ranges per page: sops 155-171, governance 103-109, blocks ~100-118, team ~53-74, departments ~96-132)
    - 30-PATTERNS.md § "Sub-nav deletion sweep" (grep `aria-label="Admin sections"` — 5 sites) + § "journeys.ts same-change rule"
    - src/lib/journeys/journeys.ts (admin entries; add /admin/settings screen)
    - CLAUDE.md Pathways Map Maintenance rule (journeys.ts updated in the SAME commit as route/nav change)
  </read_first>
  <action>
    In each of the 5 admin pages, delete the inline sub-nav block and render `<AdminNav active="…" />` (each passes its own key: sops/blocks/team/departments→their tab; governance page passes `sops` since Governance is a view of SOPs). Preserve the `team/page.tsx` inline `href="/dashboard"` handling per decision #5 (guard forwards). Add the `/admin/settings` screen to journeys.ts and confirm every admin route stays mapped. Flip `admin-nav.spec.ts` to live: assert AdminNav has 5 items, each of the 5 pages imports/renders `AdminNav`, and zero `aria-label="Admin sections"` inline navs remain.
  </action>
  <acceptance_criteria>
    - `grep -rn 'aria-label="Admin sections"' src/app` returns 0 hits (all 5 inline copies gone).
    - Each of the 5 admin pages contains `AdminNav` (grep each file).
    - `journeys.ts` contains an `/admin/settings` route entry.
    - `npx playwright test tests/phase30/admin-nav.spec.ts --project=phase30` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/admin-nav.spec.ts --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>One AdminNav on all 5 admin pages; inline sub-navs gone; journeys maps /admin/settings; spec live-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| non-admin → admin page | each admin page's own guard is the boundary; AdminNav is presentation only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Elevation | replacing per-page sub-nav | mitigate | AdminNav swap touches ONLY the nav JSX; every page's `['admin','safety_manager']` guard block stays verbatim — admin-nav.spec asserts each page still holds its guard string |
| T-30-03-02 | Information disclosure | /admin/settings new route | mitigate | Settings page copies the ai-settings guard verbatim; no new data fetch beyond links |
| T-30-03-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `grep -rn 'aria-label="Admin sections"' src/app` == 0.
- `npx playwright test --project=phase30` green.
- `npx tsc --noEmit` clean; /pathways still maps all admin routes (final 0-not-mapped sweep in 30-08).
</verification>

<success_criteria>
UX-02: one AdminNav on every admin page, 5 inline sub-navs deleted, /admin/settings exists and homes the agent orphan, guards untouched.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-03-SUMMARY.md` when done.
</output>
