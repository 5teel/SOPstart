---
phase: 30-ux-consolidation
plan: 05
type: execute
wave: 5
depends_on: [30-03]
files_modified:
  - src/app/(protected)/admin/sops/new/page.tsx
  - src/app/(protected)/admin/sops/page.tsx
  - src/lib/journeys/journeys.ts
  - tests/phase30/create-entry.spec.ts
autonomous: true
requirements: [UX-04]
must_haves:
  truths:
    - "There is exactly ONE 'New SOP' entry point on /admin/sops → the method picker at /admin/sops/new"
    - "The method picker offers 4 options with Upload FIRST: Upload a document · Talk it through · Describe it · Start blank"
    - "The 4 header create-buttons and the empty-state create-buttons on /admin/sops are gone (replaced by the single New SOP button)"
    - "The 3 intake destinations still work: /admin/sops/upload, /admin/sops/new/ai (and ?mode=voice), /admin/sops/new/blank"
  artifacts:
    - path: "src/app/(protected)/admin/sops/new/page.tsx"
      provides: "Single method-picker screen, Upload-first, 4 tiles"
  key_links:
    - from: "src/app/(protected)/admin/sops/page.tsx"
      to: "/admin/sops/new"
      via: "single 'New SOP' button href"
      pattern: "/admin/sops/new"
    - from: "src/app/(protected)/admin/sops/new/page.tsx"
      to: "/admin/sops/upload | /admin/sops/new/ai | /admin/sops/new/blank"
      via: "4 tiles, Upload first"
      pattern: "/admin/sops/(upload|new/ai|new/blank)"
---

<objective>
UX-04 One create entry. Build the method-picker at `/admin/sops/new` (4 tiles, Upload FIRST per Visy: Upload a document · Talk it through `?mode=voice` · Describe it · Start blank) and reduce `/admin/sops` to a single "New SOP" button pointing at it. Delete the 4 duplicated header create-buttons and the empty-state create-buttons. The 3 existing intake routes remain as destinations. The worker "Create SOP" tab removal (also UX-04) lives in the worker-detail plan 30-06 since it shares `sops/page.tsx`.

Purpose: Collapse 8 create entry points to 1 → 1 picker → 3 real routes.
Output: method picker + one New SOP button; scattered create buttons gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-ux-consolidation/30-CONTEXT.md
@.planning/phases/30-ux-consolidation/30-RESEARCH.md
@.planning/phases/30-ux-consolidation/30-PATTERNS.md
</context>

<artifacts_produced>
- `src/app/(protected)/admin/sops/new/page.tsx` — server page, admin guard, `<AdminNav active="sops" />`, 4 `DashTile`-style tiles (lifted from the deleted dashboard grid) in Upload-first order: `/admin/sops/upload`, `/admin/sops/new/ai?mode=voice`, `/admin/sops/new/ai`, `/admin/sops/new/blank`.
- `/admin/sops/page.tsx` header + empty-state: single "New SOP" button → `/admin/sops/new`; the 4+4 duplicated create buttons deleted.
- journeys.ts: new `/admin/sops/new` method-picker screen mapped; the 3 destinations remain.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build the method-picker page</name>
  <files>src/app/(protected)/admin/sops/new/page.tsx</files>
  <read_first>
    - 30-PATTERNS.md § "src/app/(protected)/admin/sops/new/page.tsx" (the DashTile tile pattern dashboard/page.tsx:114-125 + tile order + `?mode=voice` honoured by AiDraftTabs.tsx:27)
    - 30-RESEARCH.md § "Current Wiring 4" (the 8 entries + which 3 destinations remain)
    - src/app/(protected)/admin/sops/new/ai/page.tsx (server-page shell + guard to mirror) and src/app/(protected)/admin/ai-settings/page.tsx (guard block)
  </read_first>
  <action>
    Create `/admin/sops/new/page.tsx` as a server page with the verbatim admin guard, `<AdminNav active="sops" />`, and 4 tiles using the lifted DashTile markup (CSS-var tokens). Order (Upload first): "Upload a document"→/admin/sops/upload; "Talk it through"→/admin/sops/new/ai?mode=voice; "Describe it"→/admin/sops/new/ai; "Start blank"→/admin/sops/new/blank. Each tile: eyebrow + title + one-line description, keyboard-focusable.
  </action>
  <acceptance_criteria>
    - Page contains the 4 destination hrefs in Upload-first order (grep order: upload appears before new/ai and new/blank).
    - Page contains the `['admin', 'safety_manager']` guard string.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Method picker renders 4 Upload-first tiles behind the admin guard.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce /admin/sops to one New SOP button + journeys + spec</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/lib/journeys/journeys.ts, tests/phase30/create-entry.spec.ts</files>
  <read_first>
    - 30-RESEARCH.md § "Current Wiring 4" (header buttons admin/sops/page.tsx:127-151; empty-state 222-230) + § "Dead-Href Sweep E"
    - 30-PATTERNS.md § "Buttons to delete and repoint" + § "journeys.ts same-change rule"
    - src/app/(protected)/admin/sops/page.tsx (current header + empty-state create JSX)
    - tests/phase30/create-entry.spec.ts (stub from 30-01 — flip live)
  </read_first>
  <action>
    Replace the 4 header create-buttons (127-151) and the empty-state create-buttons (222-230) with a single "New SOP" button → `/admin/sops/new`. Grep the page for any remaining direct link to `/admin/sops/upload`, `/admin/sops/new/ai`, `/admin/sops/new/blank` and remove (those now live only in the picker). Add the `/admin/sops/new` screen to journeys.ts. Flip `create-entry.spec.ts` live: assert exactly one New-SOP entry on /admin/sops, picker has 4 tiles Upload-first, and a grep-sweep shows no stray intake hrefs outside the picker + nav.
  </action>
  <acceptance_criteria>
    - `admin/sops/page.tsx` contains exactly one `/admin/sops/new` link and zero direct `/admin/sops/upload`|`/admin/sops/new/ai`|`/admin/sops/new/blank` hrefs.
    - journeys.ts maps `/admin/sops/new`.
    - `npx playwright test tests/phase30/create-entry.spec.ts --project=phase30` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/phase30/create-entry.spec.ts --project=phase30 && npx tsc --noEmit</automated>
  </verify>
  <done>One New SOP button → picker; scattered create buttons gone; journeys maps the picker; spec live-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| non-admin → /admin/sops/new | picker page guard is the boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Elevation | new picker route | mitigate | Picker copies the admin guard verbatim; tiles are Links to already-guarded intake routes — no new server action or data path |
| T-30-05-SC | Tampering | npm installs | accept | No package installs — N/A |
</threat_model>

<verification>
- `npx playwright test --project=phase30` green.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
UX-04: one New SOP button → Upload-first method picker → 3 existing intake routes; 8 scattered entries collapsed.
</success_criteria>

<output>
Create `.planning/phases/30-ux-consolidation/30-05-SUMMARY.md` when done.
</output>
