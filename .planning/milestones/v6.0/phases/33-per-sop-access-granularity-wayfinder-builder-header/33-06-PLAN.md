---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 06
type: execute
wave: 3
depends_on: ["33-01"]
files_modified:
  - src/components/admin/wiring/WiringPatchBay.tsx
  - tests/phase32/wiring-at-scale.spec.ts
  - tests/phase33/teams-ladder.spec.ts
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "Teams column renders site → area → department → role → person as expandable, selectable tiers (mirrors OrgTree from /admin/team)"
    - "Vacancy chips render dashed and are NOT clickable (no user id to grant)"
    - "Person chains route through their role (org→area?→dept→role→person); legacy person-grant subjects not in any role keep the flat org→person chain"
    - "leftEndpoint generalizes to nearest-collapsed-ancestor so collapsed dept/role wires anchor correctly"
  artifacts:
    - path: "src/components/admin/wiring/WiringPatchBay.tsx"
      provides: "full org ladder in the teams column (expandedDepts/expandedRoles + role/person rows)"
      contains: "expandedRoles"
  key_links:
    - from: "role/person rows"
      to: "pending (connect mode)"
      via: "handleLeftClick(id, 'role' | 'person') — existing mechanics, new rows only"
      pattern: "handleLeftClick"
    - from: "trace/blast-radius display"
      to: "resolveEffectiveAccess"
      via: "the ONE shared resolver (Phase 32 rule; wiring-at-scale pins the import + call)"
      pattern: "resolveEffectiveAccess"
---

<objective>
Extend the WiringPatchBay teams column to the full org ladder (SC-1, locked decision: README §Decisions 1): departments grow a twist revealing role rows, roles reveal person rows, vacancies render dashed/non-interactive — mirroring the exact expand machinery the area tier already has. Runs parallel to 33-05 (disjoint files).

Purpose: OrgTree already carries the whole ladder (areas[].departments[].roles[].people[] incl. vacancies) and the access page already fetches it via listOrgTree() — this is a rendering + chains/peopleIndex extension, no new fetches. Connect-mode needs no new mechanics: handleLeftClick already flows role/person subjects into pending.
Output: ladder tiers live; wiring-at-scale pins repointed; teams-ladder spec live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@sketches/access-hierarchy/README.md
@sketches/access-hierarchy/index.html
@src/components/admin/wiring/WiringPatchBay.tsx
@src/types/org-model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full org ladder in the teams column</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 3 — the exact extension map: expandedDepts/expandedRoles, chains growth, peopleIndex role arm, leftEndpoint nearest-collapsed-ancestor)
    - src/components/admin/wiring/WiringPatchBay.tsx (the expandedAreas/toggleArea machinery to mirror; person jacks L575-589; chains memo; peopleIndex; leftEndpoint)
    - sketches/access-hierarchy/index.html (concept A tab — tier indentation, dashed person rows, twist affordances)
  </read_first>
  <files>src/components/admin/wiring/WiringPatchBay.tsx</files>
  <action>
    Mirror the existing area machinery exactly: add `expandedDepts: Set<string>` and `expandedRoles: Set<string>` beside expandedAreas; dept rows get a ▸/▾ twist revealing role rows (name + filled/budgeted count), role rows reveal person rows. Vacancy chips (p.isVacancy) render dashed and are NOT clickable — no id to grant. Extend the chains memo with role chains (org→area?→dept→role) and person chains routed through their role (…→role→person); KEEP the flat org→person chain for legacy person-grant subjects not present in any role. peopleIndex grows role→members entries from role.people. Generalize leftEndpoint from "area collapsed ⇒ anchor at area" to nearest-collapsed-ancestor via a parent-chain lookup (dept collapsed ⇒ role/person wires anchor at dept, etc.). handleLeftClick(id, 'role' | 'person') already flows into pending with the right subjectType — add the rows, not new mechanics. Search (matchIds) extends to role/person names with auto-expand of their ancestors (same effect as the existing area auto-expand). Do NOT touch resolveEffectiveAccess usage, the D-11 comment, or add any revokeGrant import (all pinned). No copy/jargon changes in this plan — SC-5 copy lands in 33-09 to avoid double-editing pinned literals.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "expandedRoles\|expandedDepts\|isVacancy" src/components/admin/wiring/WiringPatchBay.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dept + role tiers expand/collapse; person rows render dashed for vacancies and are non-interactive
    - chains contains role + through-role person chains; legacy flat person chains preserved
    - leftEndpoint anchors wires at the nearest collapsed ancestor
    - `resolveEffectiveAccess(chain, grantsByUnit)` call and D-11 comment unchanged; no revokeGrant reference
  </acceptance_criteria>
  <done>Full ladder renders and is selectable/grantable; wire anchoring correct at every collapse level.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint wiring-at-scale pins + flip teams-ladder spec</name>
  <read_first>
    - tests/phase32/wiring-at-scale.spec.ts (~15 exact source-line pins: toggleArea, expandedAreas, leftEndpoint body line, resolver call, personal-edge push, stroke/dasharray lines, count-badge templates, absence-of-revokeGrant, D-11 presence)
    - tests/phase33/teams-ladder.spec.ts (Wave-0 stub)
  </read_first>
  <files>tests/phase32/wiring-at-scale.spec.ts, tests/phase33/teams-ladder.spec.ts</files>
  <action>
    Same-commit repoint ([2026-07-13] class): update any wiring-at-scale pin whose exact source line changed (most likely the leftEndpoint body line; keep the toggleArea/expandedAreas/resolver/D-11/no-revokeGrant pins as-is if untouched — verify each against the new source rather than blanket-rewriting). Flip teams-ladder.spec.ts live: source-contract asserts expandedDepts/expandedRoles sets exist, role + person row rendering with dashed vacancy treatment, through-role person chains, nearest-collapsed-ancestor leftEndpoint, and search auto-expand covering role/person names; browser half stays an honest test.fixme (Railway-only UAT convention).
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/wiring-at-scale.spec.ts tests/phase33/teams-ladder.spec.ts --project=phase32 --project=phase33 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - wiring-at-scale.spec.ts green against the new source (no stale-red guard left behind)
    - teams-ladder.spec.ts flipped live and green
  </acceptance_criteria>
  <done>SC-1 spec live; no stale pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render-only extension | No new write path; ladder rows feed the EXISTING pending/createGrant flow (org-enforced server-side in grants.ts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06-01 | Elevation of Privilege | vacancy rows granted access | mitigate | Vacancy chips non-clickable (no id); server-side subject verification in createGrant remains the real gate |
| T-33-06-02 | Repudiation | stale wiring-at-scale pins ([2026-07-13]) | mitigate | Same-commit repoint; spec runs in Task 2 verify |
| T-33-06-03 | Denial of Service | wire spaghetti at 15-dept scale with person rows | mitigate | Nearest-collapsed-ancestor anchoring + shipped quiet-by-default/focus-only drawing bound worst case to one unit's wire set |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- phase32 wiring-at-scale + phase33 teams-ladder green
</verification>

<success_criteria>
SC-1 shipped: full ladder tiers expandable/selectable, vacancies honest, wires anchored correctly.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-06-SUMMARY.md` when done.
</output>
