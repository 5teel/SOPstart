---
phase: 33-per-sop-access-granularity-wayfinder-builder-header
plan: 07
type: execute
wave: 4
depends_on: ["33-05"]
files_modified:
  - src/actions/departments.ts
  - src/actions/sops.ts
  - src/app/api/sops/ai-prompt/route.ts
  - tests/phase33/sop-grant-materialization.spec.ts
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "assignSopDepartments writes SOP-target department grants (replace semantics) then materializes — it never writes sop_departments directly"
    - "createSopFromWizard and the ai-prompt route funnel hand-picked departments through the same grant path — zero direct sop_departments inserts remain in runtime code"
    - "A hand-picked SOP is overridden-from-birth (locked default, research Open Question 1 option i) and survives sibling-collection re-materialization — the 32-VERIFICATION silent-drop hole is closed"
    - "Clearing all hand-picked departments makes the SOP re-follow its collection (emergent)"
  artifacts:
    - path: "src/actions/departments.ts"
      provides: "assignSopDepartments rewired through SOP-target dept-subject grants + materialize"
      contains: "createGrant"
  key_links:
    - from: "assignSopDepartments"
      to: "access_grants (sop_id arm)"
      via: "replace-semantics: delete the SOP's dept-subject SOP-target grants, insert the new set, materialize"
      pattern: "sop_id"
    - from: "createSopFromWizard / ai-prompt route"
      to: "assignSopDepartments (or the shared grant helper it extracts)"
      via: "one write path — sop_departments is 100% derived on every path"
      pattern: "assignSopDepartments"
---

<objective>
Close the Phase-25 dual-write hole flagged in 32-VERIFICATION Anti-Patterns (SC-3/SC-4 integrity): `assignSopDepartments`, `createSopFromWizard` (sops.ts ~L556) and the ai-prompt route (~L84) still insert `sop_departments` directly with NO access_grants row, so the next unrelated materialize on that collection silently drops them. Rewire all three through SOP-target department grants — `sop_departments` becomes 100% derived on every path.

Purpose: research Open Question 1 default ADOPTED (option i): a SOP whose departments were hand-picked is overridden-from-birth — semantically truthful ("you chose who sees this"), stable against sibling wiring, and it kills the WR-02 widening class. The alternative (seeding collection grants at publish) reintroduces exactly that class.
Output: three rewired write paths + live runtime proof of the from-birth override + silent-drop closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md
@.planning/phases/32-visual-org-model-library-permissions/32-VERIFICATION.md
@src/actions/departments.ts
@src/actions/sops.ts
@src/app/api/sops/ai-prompt/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the three sop_departments write paths through SOP-target grants</name>
  <read_first>
    - .planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-RESEARCH.md (§ Pattern 2 #7 — the closure recommendation + the "overridden from birth" consequence to surface honestly in the SUMMARY)
    - src/actions/departments.ts (assignSopDepartments ~L484 — the replace-semantics body + org-scope guards to preserve; callers: SopDepartmentEditor on /admin/sops rows, DepartmentPicker sop-mode)
    - src/actions/sops.ts (createSopFromWizard direct sop_departments insert ~L556) and src/app/api/sops/ai-prompt/route.ts (~L84 direct insert)
    - src/actions/grants.ts (the 33-05 createGrant/materializeSopAccess surface being consumed)
  </read_first>
  <files>src/actions/departments.ts, src/actions/sops.ts, src/app/api/sops/ai-prompt/route.ts</files>
  <action>
    Rewire assignSopDepartments: instead of replace-writing sop_departments directly, (1) verify SOP + department ids ∈ callerOrg (keep the existing guards — service-role writes MUST self-enforce per [2026-06-15]); (2) delete the SOP's existing dept-subject SOP-target grants; (3) insert one dept-subject SOP-target grant per picked department (subject_type 'department', sop_id set, collection_id null); (4) call materializeSopAccess for that SOP — sop_departments now derives. Empty pick-set = delete-all grants + materialize (SOP re-follows its collection, emergent). Keep allDepartments handling coherent: allDepartments=true clears the SOP's dept-subject SOP-target grants and sets sops.all_departments=true (the 00035 arm) — note 33-05's override rule forces it false only when SOP-target grants EXIST, so the two states cannot conflict. createSopFromWizard + ai-prompt route: replace their direct `.from('sop_departments').insert(...)` blocks with a call to assignSopDepartments (or a thin shared internal helper extracted from it if request-scope prevents the server-action call from the route) — one write path, no forked logic. Do not change the callers' signatures (WizardClient/PromptClient/SopDepartmentEditor/DepartmentPicker pass departmentIds as today).
  </action>
  <verify>
    <automated>grep -rn "from('sop_departments').insert\|from(\"sop_departments\").insert" src/ --include=*.ts --include=*.tsx | grep -v "grants.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero direct sop_departments inserts outside grants.ts materialization (grep gate = 0)
    - assignSopDepartments writes dept-subject SOP-target grants with replace semantics + materializes; org self-enforcement preserved on every path
    - Wizard + ai-prompt creation paths funnel through the same helper; `npx tsc --noEmit` clean and `npm run build` succeeds (sops.ts/departments.ts are 'use server' — [2026-06-27])
  </acceptance_criteria>
  <done>sop_departments is 100% derived; hand-picked SOPs are overridden-from-birth by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Live runtime proof — from-birth override + silent-drop closure</name>
  <read_first>
    - tests/phase33/sop-grant-materialization.spec.ts (the 33-05 live ephemeral-org harness to extend — reuse its helpers/fixtures, do not fork)
  </read_first>
  <files>tests/phase33/sop-grant-materialization.spec.ts</files>
  <action>
    Extend the live spec with the closure scenarios (ephemeral org, junction-truth assertions only — Pitfall 6): (1) from-birth: call the rewired assignSopDepartments for a new SOP with 2 hand-picked departments → assert dept-subject SOP-target grant rows exist AND sop_departments contains exactly those 2 rows (derived, not direct); (2) silent-drop closure (the exact 32-VERIFICATION hole): with that SOP in a collection carrying a dept-subject collection grant for a DIFFERENT department, run materializeCollectionAccess on the collection (the "unrelated sibling wiring" trigger) → assert the hand-picked SOP's sop_departments still shows ONLY its hand-picked set (override held; before this phase it would have been replaced); (3) clear the picks (empty set) → assert the SOP now follows the collection (dept from the collection grant appears). Cross-org: assignSopDepartments with another org's SOP id → rejected, zero grant rows.
  </action>
  <verify>
    <automated>npx playwright test tests/phase33/sop-grant-materialization.spec.ts --project=phase33 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - From-birth override + sibling-materialize survival + clear-and-refollow all proven live against ephemeral orgs
    - Cross-org rejection asserted with zero rows written
  </acceptance_criteria>
  <done>The 32-VERIFICATION dual-write anti-pattern is closed and proven, not just claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| creation-time dept picks → grants | New admin-client grant-write path from wizard/ai-prompt flows (Pitfall 1 class). |
| materialize fanout | The only writer of sop_departments — a missed path = stale visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07-01 | Elevation of Privilege | cross-tenant dept/SOP ids into assignSopDepartments | mitigate | Preserve per-path org self-enforcement ([2026-06-15]) + live rejection test |
| T-33-07-02 | Tampering | silent dept-assignment drop on sibling materialize | mitigate | Closure by construction (grants are the source of truth) + the explicit live regression scenario |
| T-33-07-03 | Tampering | forked write logic drifting between wizard/route/editor | mitigate | One shared helper; grep gate proves zero direct inserts remain |
</threat_model>

<verification>
- grep gate: zero direct sop_departments inserts outside grants.ts
- `npx tsc --noEmit` + `npm run build` clean
- phase33 live spec green
</verification>

<success_criteria>
All department-assignment paths flow through SOP-target grants; from-birth override + silent-drop closure proven live.
</success_criteria>

<output>
Create `.planning/phases/33-per-sop-access-granularity-wayfinder-builder-header/33-07-SUMMARY.md` when done.
</output>
