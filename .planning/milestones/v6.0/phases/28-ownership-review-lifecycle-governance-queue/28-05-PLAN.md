---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 05
type: execute
wave: 3
depends_on: ["28-03"]
files_modified:
  - src/app/(protected)/admin/sops/page.tsx
  - src/components/admin/sops/LibraryReviewCell.tsx
  - src/components/admin/governance/GovernanceWidget.tsx
  - src/components/sop/tabs/OverviewTab.tsx
  - src/types/sop.ts
  - tests/phase28/library-and-worker.spec.ts
autonomous: true
requirements: [OWN-01, OWN-04, REV-02, REV-03, REV-04, GQ-04]
must_haves:
  truths:
    - "Admin library shows each SOP's owner + an overdue badge when review_due_at < now (REV-02/OWN-01 display)"
    - "Admin library has an 'Owned by me' filter (?owner=me) returning only the caller's owned SOPs (OWN-04)"
    - "Admin library row has a one-click Confirm current button wired to confirmSopCurrent (REV-04)"
    - "Admin /admin/sops header shows a governance widget with Overdue/Unowned/Due-soon counts deep-linking to /admin/governance?filter= (GQ-04)"
    - "Worker OverviewTab shows ONE passive 'Current as of <NZ date>' caption and NO badge/warning/gate on review state (REV-03/D28-07)"
  artifacts:
    - path: "src/components/admin/governance/GovernanceWidget.tsx"
      provides: "Counts card (overdue/unowned/due_soon) with deep links"
      contains: "/admin/governance?filter="
    - path: "src/components/admin/sops/LibraryReviewCell.tsx"
      provides: "Owner label + overdue badge + wired Confirm-current button"
      contains: "confirmSopCurrent"
    - path: "src/components/sop/tabs/OverviewTab.tsx"
      provides: "Passive 'Current as of' worker caption (no gating)"
      contains: "Current as of"
    - path: "src/types/sop.ts"
      provides: "Sop domain type extended with owner_user_id/review_due_at/last_reviewed_at"
      contains: "last_reviewed_at"
  key_links:
    - from: "src/app/(protected)/admin/sops/page.tsx"
      to: "owner_user_id filter"
      via: "?owner=me → .eq('owner_user_id', user.id)"
      pattern: "owner_user_id"
    - from: "src/components/admin/governance/GovernanceWidget.tsx"
      to: "/admin/governance?filter="
      via: "deep-link counts"
      pattern: "/admin/governance\\?filter="
    - from: "src/components/admin/sops/LibraryReviewCell.tsx"
      to: "src/actions/governance.ts::confirmSopCurrent"
      via: "wired onClick"
      pattern: "confirmSopCurrent"
---

<objective>
Surface governance where admins already work (the SOP library) and give workers the single passive
currency line — nothing more. Admin library gains an owner column, an overdue badge, an inline
Confirm-current button, and an "Owned by me" filter; the admin home header gains a governance
counts widget; the worker overview gains ONE "Current as of <date>" line.

Purpose: OWN-01 (owner visible), OWN-04 ("My SOPs" = owner filter, no new page), REV-02 (overdue
state in admin library only — worker access NEVER gated), REV-03 (worker passive caption), REV-04
(one-click confirm from the library), GQ-04 (dashboard widget). NORTH STAR: worker surface is one
passive line, zero interaction cost, no conditional/badge/blocking (D28-07 hard rule).
Output: admin library governance additions + widget + worker caption + type extension + specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-RESEARCH.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-PATTERNS.md
@src/app/(protected)/admin/sops/page.tsx
@src/components/sop/tabs/OverviewTab.tsx
@src/actions/governance.ts
@src/actions/auth.ts
@src/types/sop.ts
</context>

## Artifacts this phase produces (this plan)

- **Component:** `src/components/admin/governance/GovernanceWidget.tsx` (server component: counts overdue/unowned/due_soon from `listGovernanceQueue`, deep links to `/admin/governance?filter=`)
- **Component:** `src/components/admin/sops/LibraryReviewCell.tsx` (client: owner label + overdue badge + wired Confirm-current button)
- **Modified:** `admin/sops/page.tsx` (select `owner_user_id, review_due_at`; owner emails via `getTeamMembersWithEmails`; `?owner=me` filter + chip; widget in header; LibraryReviewCell per row)
- **Modified:** `OverviewTab.tsx` (one MetaRow "Current as of")
- **Modified:** `src/types/sop.ts` (Sop + SopWithSections: `owner_user_id`, `review_due_at`, `last_reviewed_at`, `last_reviewed_by` all `string | null`)
- **Spec:** `tests/phase28/library-and-worker.spec.ts`

<tasks>

<task type="auto">
  <name>Task 1: Admin library — owner column, overdue badge, confirm-current, owner=me filter</name>
  <files>src/app/(protected)/admin/sops/page.tsx, src/components/admin/sops/LibraryReviewCell.tsx, src/types/sop.ts</files>
  <read_first>
    - src/app/(protected)/admin/sops/page.tsx (the sops select column list, STATUS_TABS chip loop, en-NZ formatDate, blueprint-frame row markup, existing per-row action column)
    - src/actions/auth.ts::getTeamMembersWithEmails (owner label/email source — reuse, no new member query)
    - src/actions/governance.ts::confirmSopCurrent (the action the button calls)
    - src/types/sop.ts (Sop + SopWithSections blocks — mirror the prior additive field style)
    - 28-CONTEXT.md D28-08 (owner filter = chip, not a new page), D28-07 (overdue badge admin-library-ONLY)
  </read_first>
  <action>
    Extend the sops select in `page.tsx` to include `owner_user_id, review_due_at`. Read `?owner=` from searchParams; when `owner === 'me'`, add `.eq('owner_user_id', user.id)` to the query (OWN-04/D28-08). Add an "Owned by me" chip next to STATUS_TABS linking to `/admin/sops?owner=me` with `data-active` when the param is set. Fetch owner display labels once via `getTeamMembersWithEmails()` into a `Record<user_id, email|role>` map (reuse — do not add a new member query).
    Create `LibraryReviewCell.tsx` (client component) rendered per row: shows the owner label (email/role, or "No owner" when `owner_user_id` is null); shows an overdue badge (`review overdue`, red-ish paper/ink pill) ONLY when `review_due_at && new Date(review_due_at) < new Date()`, else a muted "Review due <NZ date>" caption; and a small "Confirm current" button wired to `confirmSopCurrent(sopId)` (real call → refresh; REV-04/GQ-02 wiring rule). Place the cell in the existing row block (near the department editor). This badge/grey-out lives ONLY here in the admin library — never on any worker route (D28-07/REV-02).
    Extend `src/types/sop.ts`: add `owner_user_id: string | null`, `review_due_at: string | null`, `last_reviewed_at: string | null`, `last_reviewed_by: string | null` to the `Sop` interface (and thus `SopWithSections`) — mirrors the prior `parent_sop_id`/`superseded_by` additive extension.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=fs.readFileSync('src/app/(protected)/admin/sops/page.tsx','utf8'); const c=fs.readFileSync('src/components/admin/sops/LibraryReviewCell.tsx','utf8'); const t=fs.readFileSync('src/types/sop.ts','utf8'); if(!/owner=='?me'?|owner === 'me'/.test(p)&&!p.includes(\"owner\")){console.error('page must handle ?owner=me');process.exit(1)} if(!p.includes('owner_user_id')||!p.includes('review_due_at')){console.error('page select must include owner+review columns');process.exit(1)} if(!/confirmSopCurrent\(/.test(c)){console.error('LibraryReviewCell must WIRE confirmSopCurrent');process.exit(1)} if(!t.includes('last_reviewed_at')){console.error('Sop type must add last_reviewed_at');process.exit(1)} console.log('library OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` selects `owner_user_id`+`review_due_at`, supports `?owner=me` (adds `.eq('owner_user_id', user.id)`), and shows an "Owned by me" chip.
    - `LibraryReviewCell` shows owner label + overdue badge (only when past due) + a wired `confirmSopCurrent(` button.
    - `Sop` type extended with the 4 fields; `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Admin library shows owner + overdue state + one-click confirm + owner=me filter; type extended; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Governance dashboard widget + worker currency caption</name>
  <files>src/components/admin/governance/GovernanceWidget.tsx, src/app/(protected)/admin/sops/page.tsx, src/components/sop/tabs/OverviewTab.tsx</files>
  <read_first>
    - src/actions/governance.ts::listGovernanceQueue (counts source)
    - src/app/(protected)/admin/sops/page.tsx header block (where the widget mounts)
    - src/components/sop/tabs/OverviewTab.tsx (MetaRow helper + "SOP Details" blueprint-frame — add ONE MetaRow, same visual weight as Author/Category)
    - 28-CONTEXT.md D28-09 (widget on /admin/sops header) + D28-07 (worker: ONE passive line, no badge/warning/gate)
    - 28-PATTERNS.md § OverviewTab (exact MetaRow usage + the D28-07 anti-pattern)
  </read_first>
  <action>
    Create `GovernanceWidget.tsx` (server component): call `listGovernanceQueue()`, count rows whose flags include `overdue`, `unowned`, `due_soon`; render a compact paper/ink counts card with three deep links — `/admin/governance?filter=overdue`, `?filter=unowned`, `?filter=due_soon` (GQ-04/D28-09). If the query errors or all counts are 0, render nothing or a muted "All current" state. Mount `<GovernanceWidget/>` in the `/admin/sops` header area (next to the LIBRARY pill / action buttons).
    In `OverviewTab.tsx` add exactly ONE `<MetaRow label="Current as of" value={formatNzDate(sop.last_reviewed_at ?? sop.published_at)} />` inside the existing "SOP Details" block (use the same en-NZ date format helper the file/library uses; add a tiny local `formatNzDate` if none is imported). HARD RULE (D28-07): no badge, no colour-coded warning, no `if (review_due_at < now)` branch, no blocking anywhere in this worker-facing file — plain informational text only, identical visual weight to `Author`/`Category`. Do not import governance actions here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const w=fs.readFileSync('src/components/admin/governance/GovernanceWidget.tsx','utf8'); const p=fs.readFileSync('src/app/(protected)/admin/sops/page.tsx','utf8'); const o=fs.readFileSync('src/components/sop/tabs/OverviewTab.tsx','utf8'); if(!/\/admin\/governance\?filter=/.test(w)){console.error('widget must deep-link ?filter=');process.exit(1)} if(!w.includes('listGovernanceQueue')){console.error('widget must count from listGovernanceQueue');process.exit(1)} if(!p.includes('GovernanceWidget')){console.error('widget must mount on /admin/sops');process.exit(1)} if(!o.includes('Current as of')){console.error('OverviewTab must add currency caption');process.exit(1)} if(/review_due_at\s*[<>]|if\s*\(.*review_due_at/.test(o)){console.error('D28-07: OverviewTab must NOT gate on review_due_at');process.exit(1)} console.log('widget/caption OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Widget counts overdue/unowned/due_soon from `listGovernanceQueue` and deep-links each to `/admin/governance?filter=`; mounted on `/admin/sops` header.
    - `OverviewTab` renders ONE "Current as of" MetaRow and contains NO `review_due_at` conditional/gate (verify command fails if present).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Dashboard widget live on admin home; worker sees one passive currency line, no gating.</done>
</task>

<task type="auto">
  <name>Task 3: Source-contract specs — library, worker no-gate, widget</name>
  <files>tests/phase28/library-and-worker.spec.ts</files>
  <read_first>
    - tests/phase28/governance-actions.spec.ts (spec style; phase28 project already registered)
    - src/app/(protected)/sops/[sopId]/walkthrough (worker walkthrough route files — the no-gate assertion targets)
    - CLAUDE.md Learnings 2026-06-05 (assert wired call) + 2026-05-25 (phase28 regex already matches tests/phase28/*)
  </read_first>
  <action>
    Create `tests/phase28/library-and-worker.spec.ts` (source-contract): assert (a) `admin/sops/page.tsx` handles `?owner=me` with `.eq('owner_user_id'` (OWN-04); (b) `LibraryReviewCell.tsx` wires `confirmSopCurrent(` and renders the overdue badge only under a `review_due_at < now` guard (REV-02/REV-04); (c) `GovernanceWidget.tsx` deep-links `/admin/governance?filter=` for overdue/unowned/due_soon and counts from `listGovernanceQueue` (GQ-04); (d) `OverviewTab.tsx` contains "Current as of" AND contains NO `review_due_at` conditional/redirect (REV-03/D28-07 worker no-gate); (e) worker walkthrough route + `sops/[sopId]/page.tsx` contain no `review_due_at`/`owner_user_id` gating branch (REV-02 no-block). Use `fs.readFileSync` + `toContain`/regex; `[\s\S]` not `/s`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase28 -g "owner=me|worker route no gate|currency caption|dashboard widget|library"</automated>
  </verify>
  <acceptance_criteria>
    - Spec asserts the wired `confirmSopCurrent(` call and the widget deep-link filter params.
    - Spec asserts the worker OverviewTab + walkthrough + sop detail contain no review-state gate (REV-02/D28-07).
    - Runs under `--project=phase28` and is green.
  </acceptance_criteria>
  <done>Library + worker-no-gate + widget source-contract specs green under phase28 project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/sops?owner=me | Filter must scope to the caller's own owned SOPs within the already-RLS-scoped org query |
| worker browser → /sops/[sopId] | Worker route must NOT branch on review/owner state — availability is never governance-gated (D28-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Information Disclosure | admin/sops ?owner=me | mitigate | Filter rides the existing admin/org RLS-scoped query; `owner=me` adds `.eq('owner_user_id', user.id)` only |
| T-28-05-02 | Denial of Service (availability) | worker OverviewTab / walkthrough | mitigate | Hard no-gate rule (D28-07): source-contract test asserts no `review_due_at` conditional on worker routes — governance never blocks read/walkthrough |
| T-28-05-SC | Tampering | package installs | accept | Zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase28 --project=phase28-unit` green.
- `npx tsc --noEmit` green.
- Worker routes contain no review-state gate (asserted by spec).
</verification>

<success_criteria>
- Admin library shows owner + overdue + one-click confirm + owner=me filter; header widget deep-links to the queue.
- Worker sees one passive "Current as of" line; no worker-facing gate anywhere.
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-05-SUMMARY.md` when done.
</output>
