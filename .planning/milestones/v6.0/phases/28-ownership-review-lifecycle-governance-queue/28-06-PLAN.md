---
phase: 28-ownership-review-lifecycle-governance-queue
plan: 06
type: execute
wave: 4
depends_on: ["28-04", "28-05"]
files_modified: []
autonomous: true
requirements: [GQ-01, GQ-04, REV-02]
must_haves:
  truths:
    - "Full phase28 + phase28-unit suites pass after both Wave-3 plans merge (integration proof)"
    - "npx tsc --noEmit and npm run build both pass (phase touches src/actions/* — 2026-06-27 mandates a real next build, not just tsc)"
    - "/pathways maps /admin/governance (journeys.ts coverage — asserted by grep)"
    - "Worker bundle delta within ±2 KB tolerance (governance is admin-only; no worker-route weight added beyond one caption)"
  artifacts:
    - path: ".planning/phases/28-ownership-review-lifecycle-governance-queue/28-06-SUMMARY.md"
      provides: "Phase-gate evidence (suite output, build result, pathways coverage)"
      contains: "PASS"
  key_links:
    - from: "npm run build"
      to: "src/actions/governance.ts"
      via: "next build server-action async-export enforcement (2026-06-27)"
      pattern: "governance"
---

<objective>
Final integration gate for Phase 28. After the two parallel Wave-3 plans merge, run the full
suite + `tsc` + `npm run build` together (only meaningful once all surfaces are on one tree),
confirm `/pathways` maps the new route, and fix-forward any integration break. This exists because
the phase touches `src/actions/*` — the 2026-06-27 learning is explicit that `tsc` green is
necessary but NOT sufficient; a sync export in a `'use server'` file only fails at `next build`.

Purpose: prove GQ-01/GQ-04/REV-02 hold end-to-end across the admin library, the governance queue,
and the worker no-gate surface, on a single merged tree. This phase runs autonomous (CONTEXT: grey
areas resolved by Claude; Simon reviews the whole phase on completion) — post-deploy UAT happens on
sopstart.com per the Railway-only-testing convention, not via local browser steps here.
Output: green gate evidence in 28-06-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-VALIDATION.md
@playwright.config.ts
@src/actions/governance.ts
@src/lib/journeys/journeys.ts
</context>

## Artifacts this phase produces (this plan)

- **Evidence:** `28-06-SUMMARY.md` recording suite/build/pathways results
- **Fix-forward:** any minimal integration fix surfaced by the merged-tree gate (record files touched in the SUMMARY)

<tasks>

<task type="auto">
  <name>Task 1: Full merged-tree gate — suite + tsc + build</name>
  <files>(fix-forward only — record any files touched in SUMMARY)</files>
  <read_first>
    - .planning/phases/28-ownership-review-lifecycle-governance-queue/28-VALIDATION.md § Sampling Rate + Conventions (binding)
    - CLAUDE.md Learnings 2026-06-27 ('use server' sync export breaks next build; run npm run build as the final gate) + 2026-06-02 (next build typecheck scope ≠ tsc scope)
  </read_first>
  <action>
    On the merged tree run, in order: `npx playwright test --project=phase28-unit --project=phase28`, then `npx tsc --noEmit`, then `npm run build`. All three MUST pass. If `npm run build` fails with "Server Actions must be async functions", locate the offending sync `export function` in `src/actions/governance.ts` and move it to `src/lib/governance/*` (2026-06-27 — should already be there; this is the safety net). If a spec is red on the merged tree that was green in isolation, fix-forward minimally (do not weaken the assertion). Record the exact commands + outcomes in the SUMMARY.
    If the repo tracks a bundle baseline (`.bundle-baseline.json` / `scripts/check-bundle-size.ts`), run the bundle check and confirm the worker route delta is within ±2 KB — governance is admin-only, so the worker bundle should be effectively unchanged (one caption line only).
  </action>
  <verify>
    <automated>npx playwright test --project=phase28-unit --project=phase28 && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --project=phase28-unit --project=phase28` fully green.
    - `npx tsc --noEmit` clean; `npm run build` completes with no "Server Actions must be async" (or any) error.
    - Worker bundle delta within ±2 KB (if a baseline gate exists).
  </acceptance_criteria>
  <done>All three gates green on the merged tree; any fix-forward recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Pathways coverage + phase artifact audit</name>
  <files>(verification only — record outcomes in SUMMARY)</files>
  <read_first>
    - src/lib/journeys/journeys.ts (the /admin/governance step added in 28-04)
    - CLAUDE.md § Pathways Map Maintenance (a new route is not "done" until journeys.ts reflects it)
  </read_first>
  <action>
    Confirm `/admin/governance` is mapped: grep `src/lib/journeys/journeys.ts` for `route: '/admin/governance'` (must be present — the /pathways "All screens" panel derives coverage from this). Then audit that every phase-28 requirement surface exists on the merged tree: migration 00043 applied, `src/actions/governance.ts` exports the four actions, `src/app/(protected)/admin/governance/page.tsx` present, `GovernanceWidget` mounted on `/admin/sops`, and the worker OverviewTab caption present with no review-state gate. Record a PASS/FAIL line per requirement (OWN-01..04, REV-01..04, GQ-01..04) in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=fs.readFileSync('src/lib/journeys/journeys.ts','utf8'); if(!/route:\s*'\/admin\/governance'/.test(j)){console.error('journeys.ts missing /admin/governance route (pathways coverage)');process.exit(1)} const need=['src/actions/governance.ts','src/app/(protected)/admin/governance/page.tsx','src/components/admin/governance/GovernanceWidget.tsx','supabase/migrations/00043_ownership_review_governance.sql']; const miss=need.filter(p=>!fs.existsSync(p)); if(miss.length){console.error('MISSING artifacts',miss);process.exit(1)} console.log('pathways + artifacts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `journeys.ts` maps `route: '/admin/governance'` (0 not-mapped on /pathways for it).
    - All phase-28 core artifacts exist on the merged tree; per-requirement PASS/FAIL recorded in SUMMARY.
  </acceptance_criteria>
  <done>Pathways coverage confirmed; 12-requirement artifact audit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → src/actions/* | next build enforces async-only server-action exports (a sync export would ship broken) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Denial of Service (broken deploy) | src/actions/governance.ts | mitigate | `npm run build` gate catches any sync-export-in-'use server' before Railway deploy (2026-06-27) |
| T-28-06-SC | Tampering | package installs | accept | Zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase28-unit --project=phase28` green.
- `npx tsc --noEmit` + `npm run build` green.
- `journeys.ts` maps `/admin/governance` (asserted).
</verification>

<success_criteria>
- Full phase suite + tsc + build green on the merged tree.
- New route mapped; worker bundle within tolerance; 12-requirement audit recorded.
</success_criteria>

<output>
Create `.planning/phases/28-ownership-review-lifecycle-governance-queue/28-06-SUMMARY.md` when done.
</output>
