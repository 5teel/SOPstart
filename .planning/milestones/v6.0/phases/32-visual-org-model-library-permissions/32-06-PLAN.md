---
phase: 32-visual-org-model-library-permissions
plan: 06
type: execute
wave: 4
depends_on: ["32-04"]
files_modified:
  - src/lib/org-model/auto-layout.ts
  - src/lib/org-model/__tests__/auto-layout.test.ts
  - src/components/admin/org-model/OrgChartCanvas.tsx
  - src/components/admin/org-model/ViewToggle.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "auto-layout deterministically positions org→area→department→role nodes as a leveled tree (no library)"
    - "OrgChartCanvas renders absolutely-positioned nodes on 20px grid paper with an SVG bezier underlay, vacancy chips dashed, role capacity counts shown"
    - "ViewToggle renders the ⊞ Chart / ▤ Columns segmented control per sketch CSS"
  artifacts:
    - path: "src/lib/org-model/auto-layout.ts"
      provides: "pure leveled-tree layout → {x,y,width,height} per node"
      exports: ["layoutOrgTree"]
    - path: "src/components/admin/org-model/OrgChartCanvas.tsx"
      provides: "Node Chart view (client)"
    - path: "src/components/admin/org-model/ViewToggle.tsx"
      provides: "shared segmented view switcher"
  key_links:
    - from: "OrgChartCanvas"
      to: "layoutOrgTree + SVG underlay"
      via: "getBoundingClientRect bezier connectors (FlowGraphCanvas pattern)"
      pattern: "getBoundingClientRect"
---

<objective>
Build the Node Chart view (D-08 default): a pure leveled-tree auto-layout function, the `OrgChartCanvas` client component (absolutely-positioned nodes + one SVG bezier underlay on 20px grid paper, the shipped Flow-tab visual language), and the shared `ViewToggle` segmented control. Vacancies render as first-class dashed chips with role capacity counts (D-05). This plan is chart rendering only; the Columns view + `/admin/team` page wiring is 32-07.

Purpose: sketch 001 (org-model-views.md) is the literal spec — translate its `.node`/`.person-chip.vacant`/`.view-toggle` CSS, don't redesign. RESEARCH Open Question 3: write a DEDICATED leveled-tree layout (strict depth = column/row) rather than reusing FlowGraphCanvas's step-graph `layoutFromPositions`, but copy its pattern (pure TS, bounding-box sizing, SVG underlay).
Output: layout fn + chart canvas + view toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/skills/sketch-findings-SOPstart/references/org-model-views.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@src/components/sop/flow/FlowGraphCanvas.tsx
@src/types/org-model.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure leveled-tree auto-layout</name>
  <read_first>
    - src/components/sop/flow/FlowGraphCanvas.tsx (layoutFromPositions lines ~52-70 — the pure "structured data → {x,y,width,height} + canvas width/height" pattern to mirror, NOT reuse)
    - .claude/skills/sketch-findings-SOPstart/references/org-model-views.md (§ What to Avoid: hand-positioned nodes need an auto-layout pass; strict depth-level tree)
    - src/types/org-model.ts (OrgTree shape from 32-04)
  </read_first>
  <files>src/lib/org-model/auto-layout.ts, src/lib/org-model/__tests__/auto-layout.test.ts</files>
  <behavior>
    - org root at depth 0, areas depth 1, departments depth 2, roles depth 3 (people render inside role nodes, not as separate layout nodes)
    - siblings at a depth are evenly spaced; parent centered over its children span
    - output is deterministic (same input → same coordinates) and includes total canvas width/height
    - depts with null area attach directly under org root (no synthetic area)
  </behavior>
  <action>
    Export `layoutOrgTree(tree: OrgTree)` (pure, no DOM/no I/O) returning `{ placed: Map<nodeId, {x,y,width,height}>, width, height }`. Column-or-row-per-depth leveled layout with even sibling spacing and parent-centering over child span; fixed node width/gap constants. Handle null-area departments by parenting them to the org root. Keep it short — a leveled tree is simpler than the FlowGraphCanvas step-graph.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "auto-layout" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `layoutOrgTree` is a pure export (no `getBoundingClientRect`, no React import)
    - Unit test asserts determinism (two calls, identical Map) + null-area dept parenting + even sibling spacing
    - Test passes
  </acceptance_criteria>
  <done>Deterministic leveled-tree layout with a passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: OrgChartCanvas + ViewToggle components</name>
  <read_first>
    - src/components/sop/flow/FlowGraphCanvas.tsx (client-component structure: absolutely-positioned nodes, `<svg>` underlay, fit state via useState, path.setAttribute('d', cubic bezier) recomputed on resize)
    - .claude/skills/sketch-findings-SOPstart/references/org-model-views.md (§ CSS Patterns — `.node`, `.node.org-root`, `.person-chip`, `.person-chip.vacant`, `.view-toggle` — translate these EXACTLY; § Role capacity counts 3/4)
    - src/styles/blueprint-theme.css (grep to confirm every var(--…) token referenced is declared — [2026-07-14] no bare undefined var(--x))
  </read_first>
  <files>src/components/admin/org-model/OrgChartCanvas.tsx, src/components/admin/org-model/ViewToggle.tsx</files>
  <action>
    `OrgChartCanvas` (`'use client'`): renders `layoutOrgTree(tree)` output as absolutely-positioned `.node` divs on a `bg-grid` (20px grid paper) relative canvas, with one `<svg>` underlay drawing cubic-bezier parent→child connectors computed from `getBoundingClientRect()` (FlowGraphCanvas idiom), redrawn on resize. Role nodes show `filled/budgeted` capacity counts (e.g. 3/4); people render as `.person-chip` inside role nodes; vacancies as `.person-chip.vacant` dashed chips with a dashed `+` avatar (NOT styled as an error — org-model-views.md). Org root node uses `.node.org-root`. Add-affordances are dashed ghosts (`+ Add role`, `+ ADD DEPARTMENT`) wired to the 32-04 create actions. `ViewToggle` (`'use client'`): a reusable segmented control taking `options` + `value` + `onChange` (or href per option), rendering the `.view-toggle`/`button.on` CSS from the sketch — used for both ⊞ Chart / ▤ Columns (here) and ⌇/▦/◉ (wiring, 32-08). Confirm every `var(--token)` referenced is declared in blueprint-theme.css.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "org-model/OrgChartCanvas\|org-model/ViewToggle" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `OrgChartCanvas.tsx` and `ViewToggle.tsx` start with `'use client'`
    - OrgChartCanvas contains `getBoundingClientRect` and `setAttribute('d'` (bezier underlay) and renders `.person-chip.vacant` for `isVacancy` people
    - Role nodes render a `filled/budgeted` capacity count
    - Every `var(--…)` token used resolves to a declaration in `src/styles/blueprint-theme.css` (grep each; [2026-07-14])
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Node Chart canvas + shared view toggle render structure, vacancies, and capacity counts to the sketch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client component | Presentational over server-fetched org-scoped tree; no direct DB writes except via 32-04 actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06-01 | Information Disclosure | invisible control (undefined CSS token) | mitigate | Confirm every `var(--x)` is declared in blueprint-theme.css; manual visual check post-deploy ([2026-07-14] — no automated gate catches this) |
| T-32-06-02 | Tampering | add-affordance write | accept | Writes route through 32-04 org-scoped actions; component only invokes them |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- auto-layout unit test green
- `npx playwright test --project=phase32` green
</verification>

<success_criteria>
Deterministic org-chart layout + Node Chart canvas + shared ViewToggle, built to the sketch, vacancies/capacity first-class.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-06-SUMMARY.md` when done.
</output>
