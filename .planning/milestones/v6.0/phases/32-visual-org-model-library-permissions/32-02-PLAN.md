---
phase: 32-visual-org-model-library-permissions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00046_org_model_schema.sql
  - supabase/migrations/00047_org_model_data.sql
autonomous: true
requirements: [SC-1, SC-2, SC-6]
must_haves:
  truths:
    - "areas, roles, role_members, collections, sop_collections, access_grants, sop_access_people tables are defined"
    - "departments.area_id nullable FK column is defined"
    - "grant_subject_type enum has exactly org|area|department|role|person"
    - "sops_visible_by_person_grant additive OR policy exists; sops_visible_by_department and sops_visible_by_sub_trade are byte-untouched"
    - "seed migration creates one collection per distinct sops.category per org, backfills sop_collections, seeds dept-level access_grants from sop_departments (D-03)"
    - "seed migration does NOT write to sop_departments (day-one cutover safety)"
  artifacts:
    - path: "supabase/migrations/00046_org_model_schema.sql"
      provides: "7 new tables + area_id column + enum + D-13 helper + additive RLS arm"
      contains: "create policy \"sops_visible_by_person_grant\""
    - path: "supabase/migrations/00047_org_model_data.sql"
      provides: "collections/sop_collections/access_grants seed (day-one equivalence)"
      contains: "sop_departments"
  key_links:
    - from: "access_grants"
      to: "collections"
      via: "collection_id FK"
      pattern: "references public.collections"
    - from: "sop_in_user_person_grants"
      to: "sop_access_people"
      via: "SECURITY DEFINER self-scoping query on auth.uid()"
      pattern: "member_id = auth.uid\\(\\)"
---

<objective>
Write both Phase 32 migrations: 00046 (schema — 7 new tables + `departments.area_id` + `grant_subject_type` enum + the D-13 person/role-grant SECURITY DEFINER helper + one additive RLS arm on `sops`) and 00047 (data — seed collections from `sops.category`, backfill `sop_collections`, seed dept-level `access_grants` from `sop_departments` for day-one equivalence D-03). Files only — the [BLOCKING] `db push` and assertions live in 32-03.

Purpose: This is the schema foundation. It copies the Phase 25 migration 00035/00036 pattern verbatim (org-scoped entity tables + `using(true)` junctions + additive-OR `sops` policy via a SECURITY DEFINER helper — no recursion). D-13 resolves the Pitfall-1 role/person enforcement gap with ONE new narrow additive arm; shipped policies stay byte-untouched.
Output: two migration files, unpushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@.planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md
@supabase/migrations/00035_departments_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 00046 schema migration (tables, enum, D-13 RLS arm)</name>
  <read_first>
    - supabase/migrations/00035_departments_schema.sql (THE template — copy the org-scoped entity table shape lines 27-95, the `using(true)` junction shape lines 97-142, the member-self-read junction lines 149-176, the SECURITY DEFINER helper + additive `sops` policy lines 178-252)
    - .planning/phases/32-visual-org-model-library-permissions/32-PATTERNS.md (§ New SECURITY DEFINER RLS arm — exact template + "do not touch sops_visible_by_department/sub_trade")
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-04, D-05, D-06, D-13)
  </read_first>
  <files>supabase/migrations/00046_org_model_schema.sql</files>
  <action>
    Single `begin;`/`commit;` migration, all pure-additive. Create these objects with the Phase 25 idioms:

    Enum: `grant_subject_type` = ('org','area','department','role','person').

    Entity tables (departments-shape: org FK ON DELETE CASCADE, `enable row level security`, `<t>_org_read` using `organisation_id = public.current_organisation_id()`, `<t>_admin_insert/_update/_delete` gated on `organisation_members.role in ('admin','safety_manager')`, org index):
    - `areas` (id, organisation_id, name text, colour text default '#3b82f6', sort int default 0, created_at, updated_at)
    - `roles` (id, organisation_id, department_id uuid not null references departments(id) on delete cascade, name text, budgeted_count int not null default 1, sort int default 0, created_at, updated_at) — dept-scoped job role, D-05
    - `collections` (id, organisation_id, name text, colour text default '#3b82f6', sort int default 0, created_at, updated_at, `unique (organisation_id, name)` — required for 00047 ON CONFLICT, D-01)

    Add `departments.area_id uuid references public.areas(id) on delete set null` (D-04, nullable, additive `alter table ... add column if not exists`).

    Junction tables (using(true) SELECT, NO authenticated write policy, two indexes each):
    - `role_members` (role_id references roles on delete cascade, member_id references auth.users on delete cascade, assigned_at, assigned_by references auth.users, pk(role_id, member_id)) — D-05/D-07
    - `sop_collections` (sop_id references sops on delete cascade, collection_id references collections on delete cascade, pk(sop_id, collection_id)) — D-01

    `access_grants` (id, organisation_id references organisations on delete cascade, subject_type grant_subject_type not null, subject_id uuid null, collection_id uuid not null references collections on delete cascade, granted_by uuid references auth.users, created_at) — ORG-SCOPED SELECT policy `organisation_id = public.current_organisation_id()` (NOT using(true) — it carries organisation_id; no auth write policy, admin-client writes; index on organisation_id and collection_id). D-11 additive-only.

    D-13 person/role materialized junction + RLS arm (copy 00035 lines 184-252 exactly, substituting names):
    - `sop_access_people` (sop_id references sops on delete cascade, member_id references auth.users on delete cascade, pk(sop_id, member_id)) — SELECT policy `member_id = auth.uid() OR admin-in-org` (mirror member_departments_self_read); no auth write policy (materialize writes via admin client). Index on sop_id and member_id.
    - `sop_in_user_person_grants(p_sop_id uuid) returns boolean language sql stable security definer set search_path = public` = `exists(select 1 from sop_access_people where sop_id = p_sop_id and member_id = auth.uid())` — SELF-SCOPING via auth.uid(), NEVER a caller-supplied id ([2026-07-05]). Add the [2026-05-08] rename-tripwire comment.
    - `create policy "sops_visible_by_person_grant" on public.sops for select to authenticated using (sop_in_user_person_grants(sops.id))` — additive OR arm. DO NOT modify or drop `sops_visible_by_department` or `sops_visible_by_sub_trade`.

    Every junction SELECT policy uses `using(true)` or a self/org predicate — NEVER reference sops/departments/roles/collections from inside a junction's own policy (42P17 recursion trap, [2026-05-13]).
  </action>
  <verify>
    <automated>grep -c "sops_visible_by_person_grant\|grant_subject_type\|sop_access_people\|create table if not exists public.access_grants" supabase/migrations/00046_org_model_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - File contains `create type public.grant_subject_type as enum ('org', 'area', 'department', 'role', 'person')` (exact 5 values)
    - `create table if not exists public.areas`, `.roles`, `.role_members`, `.collections`, `.sop_collections`, `.access_grants`, `.sop_access_people` all present
    - `alter table public.departments add column if not exists area_id uuid references public.areas(id) on delete set null`
    - `collections` has `unique (organisation_id, name)`
    - `sop_in_user_person_grants` body contains `member_id = auth.uid()` and NO function parameter is used as an org/subject filter
    - `sops_visible_by_person_grant` policy exists; file contains NO `drop policy`/`alter policy` and no reference to `sops_visible_by_department` or `sops_visible_by_sub_trade` other than in comments
    - No junction SELECT policy body references `public.sops`, `public.departments`, `public.roles`, or `public.collections` (grep for recursion trap returns only comment lines)
  </acceptance_criteria>
  <done>00046 defines all schema objects, additive-only, recursion-safe, D-13 arm self-scoping.</done>
</task>

<task type="auto">
  <name>Task 2: Write 00047 data seed migration (day-one equivalence D-03)</name>
  <read_first>
    - supabase/migrations/00036_departments_data.sql (idempotent per-org seed + ON CONFLICT DO NOTHING + RAISE EXCEPTION assertion pattern)
    - .planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md (§ Code Examples — "Grant seed from existing sop_departments" — steps A/B/C + the day-one assertion note)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>supabase/migrations/00047_org_model_data.sql</files>
  <action>
    Single `begin;`/`commit;`. Idempotent (safe to re-run):
    - Step A: `insert into public.collections (organisation_id, name, colour, sort) select distinct s.organisation_id, s.category, '#3b82f6', 0 from public.sops s where s.category is not null and s.organisation_id is not null on conflict (organisation_id, name) do nothing` — one collection per distinct `sops.category` per org (D-01; collections seeded FROM category but category is NOT replaced — Pitfall 3).
    - Step B: `insert into public.sop_collections (sop_id, collection_id) select s.id, c.id from public.sops s join public.collections c on c.organisation_id = s.organisation_id and c.name = s.category where s.category is not null on conflict do nothing`.
    - Step C: seed one `access_grants` row per (department, collection) pair that already exists via `sop_departments` today — `insert into public.access_grants (organisation_id, subject_type, subject_id, collection_id, granted_by) select distinct d.organisation_id, 'department'::public.grant_subject_type, sd.department_id, sc.collection_id, null from public.sop_departments sd join public.departments d on d.id = sd.department_id join public.sop_collections sc on sc.sop_id = sd.sop_id on conflict do nothing` (D-03).
    - CRITICAL: do NOT write to `sop_departments` or `sop_access_people` in this migration — leave existing worker-visibility rows byte-untouched (that IS the day-one cutover guarantee; materialize faithfulness is proven separately in 32-05).
    - Final `do $$ begin ... end $$` assertion mirroring 00036: RAISE EXCEPTION if any org with sops has zero collections, or if any sop with a non-null category has zero sop_collections rows.
  </action>
  <verify>
    <automated>grep -c "insert into public.collections\|insert into public.sop_collections\|insert into public.access_grants\|raise exception" supabase/migrations/00047_org_model_data.sql</automated>
  </verify>
  <acceptance_criteria>
    - File contains steps A, B, C exactly as described with `on conflict do nothing`
    - Step C inserts `subject_type = 'department'` grants derived from `sop_departments` join `sop_collections`
    - File contains NO `insert into public.sop_departments`, NO `update public.sop_departments`, NO write to `sop_access_people`
    - File ends with a `raise exception` idempotent-assertion DO block (00036 pattern)
    - `sops.category` is only READ, never updated (Pitfall 3 — grep shows no `update public.sops set category`)
  </acceptance_criteria>
  <done>00047 seeds collections/sop_collections/dept-grants for day-one equivalence without touching sop_departments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → sops read path | The new `sops_visible_by_person_grant` arm widens SOP visibility; must only widen via self-scoped auth.uid(). |
| junction SELECT policies | `using(true)` exposes UUID-pair rows to any authenticated user (established Phase 25 tradeoff). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-01 | Elevation of Privilege | `sop_in_user_person_grants()` | mitigate | SECURITY DEFINER self-scopes on `auth.uid()`; takes only `p_sop_id`, never an org/subject id ([2026-07-05]) |
| T-32-02-02 | Denial of Service | new junction SELECT policies | mitigate | `using(true)`/self predicates only — no cross-table EXISTS on sops/departments (42P17 avoidance, [2026-05-13]) |
| T-32-02-03 | Tampering | `access_grants` cross-tenant read | mitigate | Org-scoped SELECT (`organisation_id = current_organisation_id()`), not `using(true)` — grants reveal who-sees-what, tighter than a UUID-pair junction |
| T-32-02-04 | Information Disclosure | `sop_access_people` enumeration | mitigate | SELECT policy = `member_id = auth.uid()` OR same-org admin (mirrors member_departments) |
| T-32-02-05 | Tampering | day-one visibility regression | mitigate | Seed writes zero rows to `sop_departments`/`sop_access_people`; assertion in 32-03 confirms cutover equivalence |
</threat_model>

<verification>
- grep gates above pass
- `npx tsc --noEmit` unaffected (SQL only)
- Actual apply + assertions run in 32-03
</verification>

<success_criteria>
Both migrations written, additive-only, recursion-safe, day-one-equivalent by construction.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-02-SUMMARY.md` when done.
</output>
