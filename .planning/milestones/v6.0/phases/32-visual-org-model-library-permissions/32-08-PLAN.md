---
phase: 32-visual-org-model-library-permissions
plan: 08
type: execute
wave: 5
depends_on: ["32-05", "32-06"]
files_modified:
  - src/components/admin/wiring/SelectionStrip.tsx
  - src/components/admin/wiring/WiringPatchBay.tsx
  - tests/phase32/wiring-at-scale.spec.ts
  - tests/phase32/banner-slot-stability.spec.ts
  - tests/phase32/wire-up-mode.spec.ts
autonomous: true
requirements: [SC-2, SC-3, SC-5, SC-6]
must_haves:
  truths:
    - "WiringPatchBay renders grouped org-units × collections with expand-in-place, quiet-by-default focus (no wires until search/click), and count badges (survives ~15×20)"
    - "clicking a unit traces its access via the shared resolver — lit/dim, direct=solid, personal=dashed wire"
    - "SelectionStrip is a permanently-reserved fixed 48px slot that swaps content/class, never mounts/unmounts (graph never reflows on click)"
    - "wire-up mode: a NEW·UNWIRED SOP enters connect mode; each toggle draws/removes a live wire; a blast-radius banner counts PEOPLE; ✓ Done writes grants via createGrant"
  artifacts:
    - path: "src/components/admin/wiring/WiringPatchBay.tsx"
      provides: "D-hybrid wiring view (grouped, focus, trace, wire-up)"
    - path: "src/components/admin/wiring/SelectionStrip.tsx"
      provides: "fixed-height banner slot (idle/selection/wiring states)"
  key_links:
    - from: "WiringPatchBay trace + blast radius"
      to: "resolveEffectiveAccess"
      via: "the ONE shared resolver (no per-view recompute)"
      pattern: "resolveEffectiveAccess"
    - from: "wire-up ✓ Done"
      to: "createGrant"
      via: "grant write on confirm"
      pattern: "createGrant"
---

<objective>
Build the D-hybrid wiring surface: `SelectionStrip` (the fixed-height banner slot) and `WiringPatchBay` (grouped org-units ↔ collections with expand-in-place, quiet-by-default focus, trace-on-click, and wire-up connect mode with a live people blast-radius). Translates sketch 003 (the 711-line winner-D reference impl) — grouping, focus, wire-up, slot stability — into React. This is the component layer; the `?view=access` page arm + library deep-links + publish CTA are 32-09.

Purpose: sketch 003 is the SHIPPING form of the wiring view, validated at real Visy scale (15×20). The four pillars (grouped, focus, viz-as-filter, wire-up) and the fixed-slot layout rule are locked design. Trace + blast-radius both call the ONE `resolveEffectiveAccess` (no per-view drift). Wire-up is the surface's most important job — permission CREATION (D-12).
Output: SelectionStrip + WiringPatchBay + 3 live specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md
@.planning/sketches/003-wiring-at-scale/index.html
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@src/lib/org-model/resolve-access.ts
@src/actions/grants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SelectionStrip fixed-slot banner + banner-slot-stability spec</name>
  <read_first>
    - .claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md (§ Layout stability rule — 48px overflow:hidden, swap content+state class, NEVER mount/unmount; idle copy doubles as onboarding)
    - .planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md (§ Code Examples — Fixed-height banner slot tsx; Anti-Patterns "Mount-on-select banners")
    - tests/phase32/banner-slot-stability.spec.ts (Wave-0 stub to flip)
  </read_first>
  <files>src/components/admin/wiring/SelectionStrip.tsx, tests/phase32/banner-slot-stability.spec.ts</files>
  <action>
    `'use client'` `SelectionStrip` taking a `state` prop (`'idle' | 'selection' | 'wiring'`) + content props (people count, grant count, unit/SOP label). ALWAYS render a single `h-[48px] overflow-hidden` slot with `data-state`; swap INNER content + class by state — never conditionally render the slot div itself. Idle copy is the onboarding line "Select anything to trace who it reaches · click the NEW SOP to wire it up." Selection copy: "Visible to <b>{count}</b> people via {grantCount} grants" (headline nowrap + ellipsis). Wiring copy: live blast-radius. Flip `banner-slot-stability.spec.ts` live (SC-6): assert the slot's `getBoundingClientRect().top` (or a mounted container's) is pixel-identical across state transitions — the exact method sketch 003 used; degrade to a source-contract assertion that the component has no `state === 'x' ? null :` mount/unmount branch if chromium is unavailable (Rule-3).
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/banner-slot-stability.spec.ts --project=phase32 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SelectionStrip.tsx` renders exactly one `h-[48px]` slot unconditionally; grep shows NO `? null :` / conditional slot-div mount
    - Idle state renders the onboarding copy; selection state renders "…people via…grants"
    - `banner-slot-stability.spec.ts` flipped live and passes (pixel-identical top, or documented Rule-3 source-contract fallback)
  </acceptance_criteria>
  <done>Fixed-slot banner that never reflows the graph; SC-6 spec live.</done>
</task>

<task type="auto">
  <name>Task 2: WiringPatchBay grouped/focus/trace + wire-up mode + specs</name>
  <read_first>
    - .planning/sketches/003-wiring-at-scale/index.html (the working reference impl — grouping/expand-in-place, focus quiet-by-default, trace-on-click, wire-up connect mode, blast radius; translate the vanilla JS, don't redesign)
    - .claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md (§ trace-on-click JS, § wire drawing getBoundingClientRect + bezier, § the four pillars, § Rest state land-quiet)
    - src/lib/org-model/resolve-access.ts (resolveEffectiveAccess — the ONE resolver for trace + blast radius)
    - src/actions/grants.ts (createGrant/revokeGrant/listGrants — wire-up ✓ Done + trace data)
    - src/components/admin/org-model/ViewToggle.tsx (reuse for the ⌇ Wiring / ▦ Matrix / ◉ Illuminate toggle segmented control; Matrix/Illuminate may render minimal for v1 — Wiring is the shipping default)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-11 additive-only/revoke-at-source, D-12 wire-up entry, blast-radius unit is PEOPLE)
    - tests/phase32/wiring-at-scale.spec.ts, tests/phase32/wire-up-mode.spec.ts (Wave-0 stubs)
  </read_first>
  <files>src/components/admin/wiring/WiringPatchBay.tsx, tests/phase32/wiring-at-scale.spec.ts, tests/phase32/wire-up-mode.spec.ts</files>
  <action>
    `'use client'` `WiringPatchBay` taking the org tree + collections + grants (server-fetched by 32-09). Implement the four pillars from sketch 003: (1) GROUPED — org units under area headers, collections under domain headers; groups collapse to single jacks with aggregated wires + count badges, expand in place; a wire anchors at the dept if its group is expanded else at the group jack. (2) FOCUS — quiet by default (NO wires until search/click); focusing draws only that unit's wires and dims the rest (`.lit`/`.dim`); search auto-expands matching groups. (3) TRACE — click a unit → `resolveEffectiveAccess` computes lit set; wires drawn from `getBoundingClientRect()` cubic beziers (direct=solid green, personal=dashed), redrawn on resize + view switch. (4) WIRE-UP MODE (D-12) — a SOP passed as `NEW·UNWIRED` pins atop the collection column with `0 grants`; clicking it enters connect mode where left-side org units become grant toggles, each click draws/removes a live wire (personal grants dashed) and updates the SelectionStrip blast-radius (people count via resolveEffectiveAccess over the toggled grants — PEOPLE, not SOPs); `✓ Done` writes the accumulated grants via `createGrant`. Revoke is at-source only (D-11 additive-only — no in-place inherited-revoke). Rest state lands quiet (only fresh wiring visible). Reuse `ViewToggle` for the ⌇/▦/◉ lens switch. Flip `wiring-at-scale.spec.ts` (SC-3: seed ~15 depts × ~20 collections fixture, assert grouped 6×6 overview + expand-in-place + focus draws only focused wires) and `wire-up-mode.spec.ts` (SC-5: enter connect mode on a NEW SOP, toggle grants, assert live wires + people blast-radius count updates + ✓ Done calls createGrant) live; degrade the visual portions to source-contract (Rule-3) if chromium unavailable but keep the createGrant-on-Done wiring assertion.
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/wiring-at-scale.spec.ts tests/phase32/wire-up-mode.spec.ts --project=phase32 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `WiringPatchBay.tsx` imports `resolveEffectiveAccess` (trace + blast radius) and `createGrant` (wire-up Done) — grep both
    - Quiet-by-default: no wires drawn until focus/search (grep the guard); groups collapse to jacks with count badges
    - Wire-up mode updates the SelectionStrip with a PEOPLE blast-radius count and writes grants via createGrant on ✓ Done
    - No in-place inherited-revoke affordance (D-11 — grep confirms revoke routes to source)
    - `wiring-at-scale.spec.ts` and `wire-up-mode.spec.ts` flipped live and pass
  </acceptance_criteria>
  <done>Grouped/focus/trace patch bay + wire-up connect mode with people blast-radius; SC-3/SC-5 specs live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire-up ✓ Done → grant write | Client accumulates toggles; the server action re-validates every subject/collection ∈ org. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08-01 | Tampering | wire-up grant write | mitigate | ✓ Done calls createGrant, which self-enforces org scope (32-05) — client toggles are never trusted directly |
| T-32-08-02 | Information Disclosure | blast-radius miscount lying about reach | mitigate | People count derived from the SAME resolveEffectiveAccess the RLS materialization uses — display and enforcement can't disagree (RESEARCH Pattern 2) |
| T-32-08-03 | Denial of Service | graph reflow / wire spaghetti at scale | mitigate | Fixed 48px slot (SC-6) + quiet-by-default focus + group collapse (sketch 003) — validated at 15×20 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx playwright test --project=phase32` green (wiring-at-scale + banner-slot + wire-up live)
</verification>

<success_criteria>
D-hybrid wiring surface built to sketch 003: grouped/focus/trace + wire-up with people blast-radius + fixed slot. SC-2/3/5/6 satisfied at the component layer.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-08-SUMMARY.md` when done.
</output>
