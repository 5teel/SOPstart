---
phase: 32-visual-org-model-library-permissions
plan: 09
type: execute
wave: 6
depends_on: ["32-08", "32-07"]
files_modified:
  - src/app/(protected)/admin/sops/page.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
  - src/lib/journeys/journeys.ts
  - src/lib/uat/tests.ts
  - tests/phase32/library-filter-deeplink.spec.ts
autonomous: true
requirements: [SC-4, SC-5]
must_haves:
  truths:
    - "/admin/sops?view=access renders the WiringPatchBay over the org tree + collections + grants (third view beside Needs attention)"
    - "focusing a unit deep-links /admin/sops?departments=<id> or ?collection=<id>; the library list server-filters to matching SOPs with an 'Open in library (N)' count"
    - "a newly published SOP shows a 'Wire up access' CTA landing on ?view=access&sop=<id> pinned NEW·UNWIRED (D-12)"
    - "journeys.ts + uat reflect ?view=access — /pathways shows 0 not-mapped"
  artifacts:
    - path: "src/app/(protected)/admin/sops/page.tsx"
      provides: "?view=access arm + ?departments=/?collection= server-side library filter"
      contains: "view === 'access'"
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx"
      provides: "post-publish Wire up access CTA"
  key_links:
    - from: "src/app/(protected)/admin/sops/page.tsx (view=access)"
      to: "WiringPatchBay + listOrgTree + listGrants"
      via: "server-fetch, Promise.all"
      pattern: "WiringPatchBay"
    - from: "PublishStage Wire up access CTA"
      to: "/admin/sops?view=access&sop="
      via: "Link href"
      pattern: "view=access"
---

<objective>
Land the wiring surface on `/admin/sops?view=access` (D-09, a third view beside "Needs attention" using the same `?view=` fold idiom), wire the viz-as-library-filter deep-links (`?departments=<id>` / `?collection=<id>` server-filter the SOP list with an "Open in library (N)" count, SC-4), add the post-publish "Wire up access" CTA (D-12), and update journeys/uat. End with the phase gate.

Purpose: the library page already server-filters by query params; the wiring viz is a filter front-end for it (permission-wiring-views.md pillar 3). Wire-up mode triggers from BOTH the post-publish CTA and organically from the access view (D-12). This closes SC-4 + SC-5's UI entry and finalizes the phase.
Output: access view arm + deep-link filter + publish CTA + journeys/uat + phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/skills/sketch-findings-SOPstart/references/permission-wiring-views.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@src/app/(protected)/admin/sops/page.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
@src/components/admin/wiring/WiringPatchBay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ?view=access arm + library-filter deep-links</name>
  <read_first>
    - src/app/(protected)/admin/sops/page.tsx (the `isAttentionView`/`?view=` idiom lines 61-69, 194-228; the Promise.all fetch lines 102-106; the SOP list query built conditionally lines 73-90 — extend it, don't fork it)
    - src/components/admin/wiring/WiringPatchBay.tsx (props it needs: org tree, collections, grants)
    - src/actions/org-model.ts (listOrgTree), src/actions/grants.ts (listGrants)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-09 third view, D-12b organic wire-up entry)
    - tests/phase32/library-filter-deeplink.spec.ts (Wave-0 stub)
  </read_first>
  <files>src/app/(protected)/admin/sops/page.tsx</files>
  <action>
    Extend the sops page searchParams type with `view?: 'attention'|'access'`, `departments?: string`, `collection?: string`, `sop?: string`. Add `const isAccessView = params.view === 'access'` following the `isAttentionView` branch style. When `isAccessView`: server-fetch `listOrgTree()` + `listGrants()` + collections (via a listCollections helper or a direct org-scoped select) in the SAME `Promise.all`, and render `<WiringPatchBay ... newSopId={params.sop} />` (the `sop` param pins that SOP as NEW·UNWIRED for organic wire-up, D-12b). Add "Access" to the view tabs/nav so AdminNav/tabs highlight correctly (mirror the attention-view highlight handling). Library-filter deep-links (SC-4): when `params.departments` or `params.collection` is present (and NOT in access view), resolve the matching SOP id set server-side (`sop_departments` for a department id; `sop_collections` for a collection id) and `.in('id', ids)` on the SOP query, and render an "Open in library (N)" / filtered-count header. Keep all reads concurrent (no serial waterfall, [2026-07-13]). Flip `library-filter-deeplink.spec.ts` live (SC-4: assert `?departments=<id>` yields the server-filtered SOP set + the count; assert the WiringPatchBay focus link builds that URL) — degrade visual portions to source-contract if chromium unavailable (Rule-3) keeping the server-filter assertion.
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/library-filter-deeplink.spec.ts --project=phase32 2>&1 | tail -5; grep -c "isAccessView\|WiringPatchBay\|departments\b" "src/app/(protected)/admin/sops/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` contains `const isAccessView = params.view === 'access'` and renders `<WiringPatchBay` in that arm
    - `?departments=<id>` and `?collection=<id>` server-filter the SOP list via `sop_departments`/`sop_collections` id resolution + `.in('id', …)`
    - listOrgTree + listGrants + collections fetched inside the existing `Promise.all` (no new serial await)
    - `library-filter-deeplink.spec.ts` flipped live and passes
  </acceptance_criteria>
  <done>Access view renders the patch bay; deep-link filters work server-side; SC-4 spec live.</done>
</task>

<task type="auto">
  <name>Task 2: Publish CTA + journeys/uat + phase gate</name>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx (props + render; grep for where PublishStage is rendered to find the parent shell that holds sopId + publish-success state)
    - src/lib/journeys/journeys.ts (admin journey lines ~228-248; the publish action step ~362; add the ?view=access screen + the wire-up flow; keep route values real for coverage)
    - src/lib/uat/tests.ts (add a wire-up-access review item if team-review-worthy)
    - CLAUDE.md §Pathways Map Maintenance (D-10) and §Learnings [2026-06-27] (next build enforces server-action constraints tsc misses)
  </read_first>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx, src/lib/journeys/journeys.ts, src/lib/uat/tests.ts</files>
  <action>
    Add an optional `wireUpHref?: string` prop to `PublishStage` and render a "Wire up access" CTA (link/button) after a successful publish, pointing to `/admin/sops?view=access&sop={sopId}` (D-12a — lands the SOP pinned NEW·UNWIRED). Supply the href from PublishStage's parent shell (it already knows sopId). Do not fork the publish pipeline — this is an additive CTA on the existing published state. Update `journeys.ts`: add a `/admin/sops?view=access` screen to the admin journey (org-model access view), and a wire-up-access flow step (post-publish CTA → access view → connect mode → ✓ Done); keep the `route` values real so `/pathways` coverage + deep-links work. Add a UAT entry for the wire-up flow if worthwhile. Then run the phase gate: full `npm run test`, `npx tsc --noEmit`, `npm run build` (next build — [2026-06-27], since this touches server-action call paths), and confirm `/pathways` "All screens" shows 0 not-mapped for `/admin/team` and `/admin/sops?view=access`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `PublishStage.tsx` renders a "Wire up access" CTA with href containing `view=access` and the sopId (only after publish success)
    - `journeys.ts` has a `/admin/sops?view=access` screen + a wire-up flow; grep confirms the route string present
    - `npx tsc --noEmit` clean AND `npm run build` (next build) succeeds
    - `npm run test` full suite green (phase32 + no regressions)
    - `/pathways` shows 0 not-mapped for /admin/team and /admin/sops?view=access
  </acceptance_criteria>
  <done>Post-publish wire-up CTA + organic entry both live; journeys/uat current; phase gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/sops?view=access | Admin/safety_manager only (existing page guard); reads org-scoped tree + grants. |
| ?departments/?collection filter | Client-supplied ids resolved to SOP set through org-scoped junction reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09-01 | Elevation of Privilege | access view auth | mitigate | Reuses the sops page's existing admin/safety_manager redirect guard (unchanged) |
| T-32-09-02 | Information Disclosure | deep-link filter leaking cross-org SOPs | mitigate | Filter resolves ids through org-scoped `sop_departments`/`sop_collections` reads under the session RLS client — cross-org ids yield empty |
| T-32-09-03 | Repudiation | stale pathways / build-only server-action break | mitigate | journeys/uat updated same change (D-10); `next build` run as final gate ([2026-06-27]) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` (next build) succeeds
- `npm run test` full suite green
- `/pathways` 0 not-mapped for /admin/team + /admin/sops?view=access
</verification>

<success_criteria>
Access view + deep-link filter + wire-up CTA shipped; journeys/uat current; phase gate green. SC-1..SC-6 all satisfied across the phase.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-09-SUMMARY.md` when done.
</output>
