---
phase: 32-visual-org-model-library-permissions
plan: 05
type: execute
wave: 4
depends_on: ["32-04"]
files_modified:
  - src/actions/grants.ts
  - tests/phase32/grants-org-isolation.spec.ts
  - tests/phase32/person-grant-rls.spec.ts
autonomous: true
requirements: [SC-2, SC-5]
must_haves:
  truths:
    - "createGrant/revokeGrant write access_grants with org self-enforcement on subject_id + collection_id"
    - "materializeSopAccess recomputes and replace-writes sop_departments (org/area/dept grants) AND sop_access_people (role/person grants) for a SOP"
    - "materialize is faithful: re-running it against the seeded day-one grants reproduces sop_departments byte-identical (D-03)"
    - "a person-level grant makes a target SOP visible to that person via sops_visible_by_person_grant, without widening their department"
    - "cross-tenant grant writes are rejected (real runtime insert, not test.fixme)"
  artifacts:
    - path: "src/actions/grants.ts"
      provides: "grant CRUD + materialization fanout into sop_departments + sop_access_people"
      exports: ["createGrant", "revokeGrant", "listGrants", "materializeSopAccess", "materializeCollectionAccess"]
  key_links:
    - from: "src/actions/grants.ts materializeSopAccess"
      to: "resolveEffectiveAccess"
      via: "shared resolver (no per-view recompute)"
      pattern: "resolveEffectiveAccess"
    - from: "materializeSopAccess"
      to: "sop_departments + sop_access_people"
      via: "replace-semantics delete+insert via admin client"
      pattern: "sop_access_people"
---

<objective>
Build `grants.ts`: access-grant CRUD (create/revoke/list) plus the materialization fanout that turns grants into worker-visibility rows. `materializeSopAccess(sopId)` and `materializeCollectionAccess(collectionId)` resolve effective access via the shared resolver and replace-write BOTH derived junctions — `sop_departments` (org/area/department grants) and `sop_access_people` (role/person grants, D-13). This is the security-critical write path; it ships with REAL runtime tests (org-isolation, person-grant RLS, materialize faithfulness), not fixme stubs.

Purpose: RESEARCH "Don't Hand-Roll" — a single materialize pair called from every grant/collection write is the only defence against stale, silently-wrong `sop_departments`. D-13 role/person enforcement lands here (materialize populates `sop_access_people`, the additive RLS arm reads it).
Output: grants.ts + 3 live runtime specs flipped from Wave-0 stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md
@.planning/phases/32-visual-org-model-library-permissions/32-PATTERNS.md
@src/actions/departments.ts
@src/actions/org-model.ts
@src/lib/org-model/resolve-access.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: grants.ts CRUD + materialization fanout</name>
  <read_first>
    - src/actions/departments.ts (assignSopDepartments lines 484-540 — the exact replace-semantics delete+insert template; orgScopedDeptIds/callerOrgId lines 335-361)
    - src/lib/org-model/resolve-access.ts (resolveEffectiveAccess signature from 32-04)
    - src/actions/org-model.ts (listOrgTree / grant-read helpers to reuse)
    - .planning/phases/32-visual-org-model-library-permissions/32-RESEARCH.md (Pattern 1 createGrant; § System Architecture — materialize called on grant create/delete + sop↔collection change + wire-up confirm)
    - .planning/phases/32-visual-org-model-library-permissions/32-CONTEXT.md (D-02 materialize onto sop_departments, D-06 5-level, D-11 additive-only, D-13 person/role arm)
  </read_first>
  <files>src/actions/grants.ts</files>
  <action>
    `'use server'`, every export async, `{ data } | { error }`. Use `(supabase as any)` casts and `createAdminClient()` writes with `callerOrgId` self-enforcement. Exports:
    - `listGrants()` → all `access_grants` for the caller's org (admin client, org-filtered).
    - `createGrant({ subjectType, subjectId, collectionId })` — Zod `subjectType: z.enum(['org','area','department','role','person'])` (never free string, V5). subjectId null ONLY for 'org'. Verify subjectId belongs to caller org (per-type: areas/departments/roles/organisation_members; 'org' → the caller's own org id) AND collectionId ∈ caller org, BEFORE insert. After insert, call `materializeCollectionAccess(collectionId)`.
    - `revokeGrant(grantId)` — verify grant ∈ caller org, delete, then `materializeCollectionAccess(collectionId)`. Additive-only model (D-11): revoke deletes the source grant; no negative/exclusion rows.
    - `materializeSopAccess(sopId)` — verify sop ∈ org; gather the SOP's collections (`sop_collections`) and all `access_grants` on those collections; resolve via `resolveEffectiveAccess` to (a) a department-id set from org/area/department grants (area→its departments via `departments.area_id`; org→all org departments) and (b) a member-id set from role grants (role→role_members) + person grants. Replace-write `sop_departments` (delete by sop_id, insert the dept set) AND `sop_access_people` (delete by sop_id, insert the member set). Never write when the set is unchanged is NOT required — replace-semantics is fine.
    - `materializeCollectionAccess(collectionId)` — verify ∈ org, then call `materializeSopAccess` for every sop in that collection (`sop_collections`).
    All writes org-self-enforced; never trust client ids (Pitfall 4 / [2026-06-15]).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "src/actions/grants" || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - `grants.ts` starts `'use server'`, all value exports async ([2026-06-27])
    - `createGrant`/`revokeGrant` verify subject/collection ∈ callerOrg before any write (grep the org-scope guard)
    - `materializeSopAccess` imports and calls `resolveEffectiveAccess` (grep) and replace-writes BOTH `sop_departments` and `sop_access_people`
    - `createGrant`, `revokeGrant`, and any sop↔collection change path all funnel through `materializeCollectionAccess`/`materializeSopAccess` (no orphan write path — grep the call sites)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>grants.ts ships CRUD + faithful materialization into both derived junctions.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the 3 runtime guards live (org-isolation, person-grant RLS, materialize faithfulness)</name>
  <read_first>
    - tests/phase32/grants-org-isolation.spec.ts, tests/phase32/person-grant-rls.spec.ts (Wave-0 stubs to flip)
    - CLAUDE.md §Learnings [2026-06-15] (real runtime insert as authenticated admin, NOT test.fixme), [2026-05-08] magic-link cookie session install for Playwright, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
    - .planning/phases/32-visual-org-model-library-permissions/32-VALIDATION.md (Per-Task Verification Map rows for org-isolation + person-grant + day-one)
  </read_first>
  <files>tests/phase32/grants-org-isolation.spec.ts, tests/phase32/person-grant-rls.spec.ts</files>
  <action>
    Flip both stubs to REAL runtime tests (remove test.fixme):
    - `grants-org-isolation.spec.ts`: as an authenticated admin of org A, attempt `createGrant` with a `subjectId`/`collectionId` belonging to org B → assert error/rejection and assert NO `access_grants` row was written for the cross-org pair. Repeat the cross-tenant assertion for a `role_members` and a `sop_collections` write via their owning actions. (These are the [2026-06-15]-mandated real inserts — a green source-contract grep is not sufficient.)
    - `person-grant-rls.spec.ts`: seed a person-level grant (person P, collection C) via `createGrant`, run `materializeSopAccess` for a SOP in C, then assert P can SELECT that SOP (RLS-respecting client authenticated as P) via the new arm, while a co-worker in P's department who has no dept/role/person grant to C canNOT — proving the arm widens to the person, not the department (the Priya scenario).
    Also add/keep a materialize-faithfulness assertion (may live here or in resolve-access.spec): after materialize against the day-one seeded grants, `sop_departments` for a sampled SOP is unchanged (D-03). If the local environment lacks a chromium binary, degrade the SELECT-visibility portion to a source-contract + a service-role SQL assertion (Rule-3 trade-off, documented) but keep the cross-tenant write rejection as a real insert.
  </action>
  <verify>
    <automated>npx playwright test tests/phase32/grants-org-isolation.spec.ts tests/phase32/person-grant-rls.spec.ts --project=phase32 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Neither spec contains `test.fixme` for the cross-tenant write case (real insert attempted)
    - grants-org-isolation asserts a cross-org `createGrant` writes ZERO rows
    - person-grant-rls asserts the granted person CAN see the SOP and a same-dept non-grantee CANNOT (or the documented Rule-3 service-role SQL fallback)
    - Both specs pass under `--project=phase32`
  </acceptance_criteria>
  <done>Org-isolation + person-grant enforcement + materialize faithfulness proven by live runtime tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → grant write | Attacker may pass another org's subject_id/collection_id. |
| grant → worker visibility | Materialization is the ONLY thing that turns a grant into a readable SOP; a missed call = silent stale visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | Tampering / EoP | cross-tenant grant write | mitigate | Verify subject_id + collection_id ∈ callerOrg before insert; real runtime cross-tenant test asserts zero rows ([2026-06-15]) |
| T-32-05-02 | Information Disclosure | over-broad person grant materialization | mitigate | person/role grants materialize to `sop_access_people` (member-scoped), NOT to the whole department in `sop_departments` (Pitfall 1 / D-13) |
| T-32-05-03 | Tampering | stale sop_departments from missed materialize | mitigate | Single materialize pair funnels every grant/collection write; no orphan write path (grep call sites) |
| T-32-05-04 | Repudiation | day-one visibility drift | mitigate | Materialize-faithfulness test: re-materializing day-one grants leaves sop_departments byte-identical (D-03) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx playwright test --project=phase32` green (org-isolation + person-grant live)
</verification>

<success_criteria>
Grant CRUD + faithful bi-junction materialization; role/person enforcement proven; cross-tenant writes blocked by real tests.
</success_criteria>

<output>
Create `.planning/phases/32-visual-org-model-library-permissions/32-05-SUMMARY.md` when done.
</output>
