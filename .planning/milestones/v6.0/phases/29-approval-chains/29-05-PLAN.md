---
phase: 29-approval-chains
plan: 05
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - src/components/admin/governance/GovernanceQueueRow.tsx
  - src/components/admin/governance/GovernanceFilterChips.tsx
  - src/components/admin/governance/GovernanceWidget.tsx
  - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx
  - tests/phase29/queue-approve-action.spec.ts
  - tests/phase29/version-history-approvals.spec.ts
autonomous: true
requirements: [APR-03, APR-04, APR-05]
user_setup: []

must_haves:
  truths:
    - "A governance queue row that is awaiting_approval AND where the caller is the next approver shows a one-click Approve action (top priority over unowned/stale_role)"
    - "Every admin sees an 'Awaiting approval' chip/filter + dashboard-widget count for pending rows (glanceable, GQ-04)"
    - "The version-history surface renders sop_approvals rows grouped by version (who, when, action, step label)"
  artifacts:
    - path: "src/components/admin/governance/GovernanceQueueRow.tsx"
      provides: "awaiting_approval Approve branch wired to approveStep"
      contains: "approveStep"
    - path: "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"
      provides: "approval history grouped by version"
      contains: "getApprovalHistory"
  key_links:
    - from: "src/components/admin/governance/GovernanceQueueRow.tsx"
      to: "src/actions/approvals.ts"
      via: "Approve button calls approveStep"
      pattern: "approveStep\\("
    - from: "src/app/(protected)/admin/sops/[sopId]/versions/page.tsx"
      to: "src/actions/approvals.ts"
      via: "getApprovalHistory fetch for the version lineage"
      pattern: "getApprovalHistory\\("
---

<objective>
Complete the queue + history surfaces: the governance-queue row's one-click Approve action (top-priority when the caller is the next approver), the `awaiting_approval` chip + dashboard-widget count, and the read-only approval history on the version-history page.

Purpose: APR-03's queue-side one-click approve, APR-04's queue-visible pending state (and GQ-04's widget count), and APR-05's version-history approval log — all additive to existing surfaces, no new routes.
Output: `GovernanceQueueRow`/`GovernanceFilterChips`/`GovernanceWidget` extended; `versions/page.tsx` renders `sop_approvals` grouped by version.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/phases/29-approval-chains/29-PATTERNS.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-04-SUMMARY.md

# Analog files
@src/components/admin/governance/GovernanceQueueRow.tsx
@src/components/admin/governance/GovernanceFilterChips.tsx
@src/components/admin/governance/GovernanceWidget.tsx
@src/app/(protected)/admin/sops/[sopId]/versions/page.tsx
@src/actions/approvals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue row Approve branch + awaiting_approval chip + widget count</name>
  <read_first>
    - src/components/admin/governance/GovernanceQueueRow.tsx (the if/else-if action-priority chain + FLAG_STYLE/FLAG_LABEL maps; existing handleConfirmCurrent useTransition shape)
    - src/components/admin/governance/GovernanceFilterChips.tsx (CHIPS array shape)
    - src/components/admin/governance/GovernanceWidget.tsx (counts object + Link chips)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 7 and § 8
    - src/actions/approvals.ts (approveStep)
  </read_first>
  <files>src/components/admin/governance/GovernanceQueueRow.tsx, src/components/admin/governance/GovernanceFilterChips.tsx, src/components/admin/governance/GovernanceWidget.tsx</files>
  <action>
    In `GovernanceQueueRow.tsx`: prepend ONE new branch to the existing if/else-if action chain (29-PATTERNS.md §7): `row.flags.includes('awaiting_approval') && row.isCallerNextApprover` → a one-click "Approve" button calling `approveStep(row.id)` inside a `useTransition` (mirror `handleConfirmCurrent`), surfacing `{ error }` inline and `router.refresh()` on success. Priority order becomes: awaiting_approval+next → unowned → stale_role → Confirm current. Add `awaiting_approval` entries to `FLAG_STYLE` (`'bg-[var(--accent-signoff)]/20 text-[var(--accent-signoff)]'`) and `FLAG_LABEL` (`'Awaiting approval'`). All onClick handlers WIRED (2026-06-05 — no empty handler).
    In `GovernanceFilterChips.tsx`: add `{ label: 'Awaiting approval', value: 'awaiting_approval' }` to the CHIPS array (and the `GovernanceFilter` type union + the page's `counts` record already extend via the flag — coordinate the type). Note: `/admin/governance/page.tsx`'s `counts` and `visibleRows` filter read `GovernanceFilter` — add `awaiting_approval` to that union too (small edit in the chips file's exported type; the page already maps over flags generically for counts — verify it compiles).
    In `GovernanceWidget.tsx`: add `awaiting_approval: 0` to the `counts` object, increment it in the row loop, and add one `<Link href="/admin/governance?filter=awaiting_approval">` count chip (same styling as the existing overdue/unowned chips) — satisfies GQ-04's "awaiting approval" count.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Queue row's Approve branch is top-priority and calls approveStep(row.id) wired in a transition.
    - FLAG_STYLE/FLAG_LABEL + filter chip + widget count all carry awaiting_approval.
    - `npx tsc --noEmit` clean (GovernanceFilter union extended).
  </acceptance_criteria>
  <done>The governance queue and dashboard widget surface pending approvals; the next approver approves in one click inline.</done>
</task>

<task type="auto">
  <name>Task 2: Version-history approval log (grouped by version)</name>
  <read_first>
    - src/app/(protected)/admin/sops/[sopId]/versions/page.tsx (client component: getVersionHistory in useEffect ~line 138; versions.map render ~line 384)
    - src/actions/approvals.ts (getApprovalHistory — returns rows with resolved approver + step labels)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 9
  </read_first>
  <files>src/app/(protected)/admin/sops/[sopId]/versions/page.tsx</files>
  <action>
    In the versions page (client component), in the same `useEffect` that loads `getVersionHistory(sopId)`, also fetch `getApprovalHistory(versionIds)` for every version id in the lineage and store in state. Under each version row in the `versions.map` render, render its approval rows (filter by `sop_id === ver.id`) grouped read-only: `{approverLabel} — {action} — {formatted NZ date}` plus the resolved step label. Step labels + approver labels come already-resolved from `getApprovalHistory` (which reuses `getOrgMembers()` and each version's `approval_snapshot[step_index].label` server-side — do NOT hand-roll a second member query or add a label column). Read-only addition (D29-06) — no new buttons, no mutation. Use existing paper/ink row styling. Dates in NZ format (metric/NZ convention).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Page fetches getApprovalHistory(versionIds) and renders approval rows grouped under each version.
    - Approver + step labels resolved via getApprovalHistory (no second member query, no label column).
    - Read-only; NZ date format; `npx tsc --noEmit` + `npm run build` clean.
  </acceptance_criteria>
  <done>Version history shows who approved which version, when, and at which step — read-only.</done>
</task>

<task type="auto">
  <name>Task 3: Source-contract specs (queue Approve wiring + version history)</name>
  <read_first>
    - tests/phase28/governance-queue.spec.ts (wired-handler source-contract shape)
    - .planning/phases/29-approval-chains/29-RESEARCH.md § Validation → Test Map (APR-03/APR-05 rows)
  </read_first>
  <files>tests/phase29/queue-approve-action.spec.ts, tests/phase29/version-history-approvals.spec.ts</files>
  <action>
    Create `tests/phase29/queue-approve-action.spec.ts` (phase29) — source-contract: GovernanceQueueRow's Approve branch condition is `awaiting_approval && isCallerNextApprover`, positioned BEFORE the unowned/stale_role branches, and its onClick calls `approveStep(row.id)` (wired, not bare); FilterChips CHIPS includes `awaiting_approval`; GovernanceWidget counts + Links `awaiting_approval`.
    Create `tests/phase29/version-history-approvals.spec.ts` (phase29) — source-contract: versions page calls `getApprovalHistory(`, filters approval rows by `ver.id`, and renders approver/action/date (read-only — assert no mutation action call in the approval-render block).
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "awaiting_approval priority|version history approvals"</automated>
  </verify>
  <acceptance_criteria>
    - Both specs green: Approve wiring + priority order proven; version-history read-only render proven.
  </acceptance_criteria>
  <done>Queue Approve wiring and version-history approval log are proven at the source-contract level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approver browser → approveStep (queue inline) | button visibility is UX only; server re-checks the approver |
| admin browser → version-history read | approval rows are org-scoped by RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Elevation of Privilege | queue Approve for wrong caller | mitigate | isCallerNextApprover hides the button; approveStep re-runs stepMatchesCaller server-side (29-02) |
| T-29-05-02 | Information Disclosure | version-history approval rows cross-org | mitigate | sop_approvals SELECT RLS `organisation_id = current_organisation_id()` (29-01); getApprovalHistory requireAdmin |
| T-29-05-SC | Tampering | npm installs | accept | zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase29` green.
- `npx tsc --noEmit` + `npm run build` clean; worker bundle Δ0 (admin-only surfaces).
- phase28 queue/widget regression green (additive edits only).
</verification>

<success_criteria>
The governance queue offers one-click Approve to the next approver, every admin sees the awaiting-approval count/filter, and version history renders the read-only approval log.
</success_criteria>

<artifacts_produced>
- `src/components/admin/governance/GovernanceQueueRow.tsx` (Approve branch)
- `src/components/admin/governance/GovernanceFilterChips.tsx` (awaiting_approval chip + union)
- `src/components/admin/governance/GovernanceWidget.tsx` (awaiting_approval count + link)
- `src/app/(protected)/admin/sops/[sopId]/versions/page.tsx` (approval history)
- `tests/phase29/queue-approve-action.spec.ts`, `tests/phase29/version-history-approvals.spec.ts`
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-05-SUMMARY.md` when done.
</output>
