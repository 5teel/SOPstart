---
phase: 29-approval-chains
plan: 06
type: execute
wave: 4
depends_on: [29-01, 29-02, 29-03, 29-04, 29-05]
files_modified:
  - tests/phase29/phase-gate.spec.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03, APR-04, APR-05]
user_setup: []

must_haves:
  truths:
    - "The full phase29 + phase29-unit suite passes on the merged tree"
    - "phase28 + phase28-unit regression stays green (no governance/publish regression)"
    - "npx tsc --noEmit clean AND npm run build clean (2026-06-27 async-only-export gate — publish route + src/actions touched)"
    - "All 5 APR requirements are audited PASS with implementation evidence"
  artifacts:
    - path: "tests/phase29/phase-gate.spec.ts"
      provides: "5-requirement audit as a runnable source-contract manifest"
  key_links:
    - from: "tests/phase29/phase-gate.spec.ts"
      to: "APR-01..05"
      via: "one assertion group per requirement mapped to its proving artifact"
      pattern: "APR-0"
---

<objective>
Merged-tree gate: run the full phase29 suite plus the phase28 regression, `tsc`, and a real `next build`, and audit all five APR requirements against their shipped artifacts.

Purpose: Waves 1-3 land on separate concerns; this proves they compose without regression and that every requirement is genuinely covered (not just planned). The publish route + src/actions changes make a full `npm run build` mandatory (2026-06-27 async-only-export learning).
Output: `tests/phase29/phase-gate.spec.ts` (the 5-req audit) + a green full gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-VALIDATION.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-requirement audit manifest spec</name>
  <read_first>
    - .planning/phases/29-approval-chains/29-RESEARCH.md § Validation → Phase Requirements → Test Map (the per-req proving artifacts)
    - .planning/REQUIREMENTS.md (v6.0 APR-01..05 text)
    - the phase29 specs created in Waves 1-3 (to reference their proving assertions, not duplicate them)
  </read_first>
  <files>tests/phase29/phase-gate.spec.ts</files>
  <action>
    Create `tests/phase29/phase-gate.spec.ts` (phase29) with one `test()` per requirement, each a source-contract assertion tying the requirement to its shipped artifact (a manifest, not a re-implementation of the wave specs):
    - APR-01: `00045_approval_chains.sql` uses `current_organisation_id` (never `app_metadata`); publish route no-chain path calls `performPublish(`; ApprovalChainEditor calls `setApprovalChain(`.
    - APR-02: migration has the partial unique index `where action = 'approved'`; publish route sets `approval_snapshot` on request-publish; requestChanges does NOT null `approval_snapshot` (grep the update sets only `approval_state`).
    - APR-03: GovernanceQueueRow Approve branch calls `approveStep(`; ApprovalChainPanel Approve/Request-changes wired to callbacks.
    - APR-04: approveStep final-step calls `performPublish(` with `approvalState: 'approved'`; resolveNextStepIndex counts approved-only; listGovernanceQueue computes isCallerNextApprover.
    - APR-05: versions page calls `getApprovalHistory(` and renders rows grouped by version.
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "APR-0"</automated>
  </verify>
  <acceptance_criteria>
    - Five audit tests (APR-01..05) present and green, each pointing at a real artifact.
  </acceptance_criteria>
  <done>Every APR requirement has a runnable audit assertion mapped to its shipped code.</done>
</task>

<task type="auto">
  <name>Task 2: Full merged-tree gate (phase29 + phase28 regression + tsc + build)</name>
  <read_first>
    - .planning/phases/29-approval-chains/29-VALIDATION.md § Sampling Rate + Final gates
    - CLAUDE.md Learnings (2026-06-27 async-only-export; 2026-05-25 spec-registration; bundle-gate convention)
  </read_first>
  <files>tests/phase29/phase-gate.spec.ts</files>
  <action>
    Run the full validation gate on the merged tree and fix-forward any failure surfaced (record fixes in the SUMMARY):
    (1) `npx playwright test --project=phase29-unit --project=phase29` — full phase29 suite green (live-DB cross-org + partial-index double-click cases remain `test.fixme` per Railway-only-testing convention; confirm they are listed, not failing).
    (2) `npx playwright test --project=phase28-unit --project=phase28` — regression green (classify.ts + governance.ts + publish route were modified additively).
    (3) `npx tsc --noEmit` clean.
    (4) `npm run build` clean (full next build — publish route + src/actions/* touched; a pure/sync export in a 'use server' file breaks build even when tsc passes). Confirm the worker `/sops/[sopId]` bundle gate is Δ0 (this phase touches only admin-facing + server surfaces).
    (5) Confirm no new routes were added ⇒ `journeys.ts` needs no edit and `/pathways` shows 0 not-mapped for the phase's surfaces (all reused: /admin/governance, builder, versions).
  </action>
  <verify>
    <automated>npx playwright test --project=phase29-unit --project=phase29 --project=phase28-unit --project=phase28 && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - phase29 + phase29-unit + phase28 + phase28-unit all green (fixme cases listed, not failing).
    - `npx tsc --noEmit` + `npm run build` both clean.
    - Worker bundle gate Δ0; no new routes (journeys.ts unchanged, verified).
  </acceptance_criteria>
  <done>The merged tree passes the full suite, regression, tsc, and a real production build, with all 5 requirements audited PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification harness → merged tree | the gate must exercise real build + regression, not just greps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Repudiation | green stubs masking a broken build | mitigate | mandatory `npm run build` (not just tsc) per 2026-06-27; phase28 regression included |
| T-29-06-SC | Tampering | npm installs | accept | zero new dependencies |
</threat_model>

<verification>
- Full suite + regression + tsc + build all green.
- Live-DB cross-org isolation + partial-index double-click carried as documented `test.fixme` UAT (Railway-only-testing).
</verification>

<success_criteria>
Phase 29 composes cleanly: full suite green, phase28 regression green, tsc + next build clean, all 5 APR requirements audited PASS, no worker-bundle or route regressions.
</success_criteria>

<artifacts_produced>
- `tests/phase29/phase-gate.spec.ts` (5-requirement audit manifest)
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-06-SUMMARY.md` when done.
</output>
