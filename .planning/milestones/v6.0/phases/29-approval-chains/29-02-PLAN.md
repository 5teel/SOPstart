---
phase: 29-approval-chains
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/actions/approvals.ts
  - src/actions/governance.ts
  - src/app/api/sops/[sopId]/publish/route.ts
  - src/lib/governance/classify.ts
  - src/lib/governance/__tests__/classify.test.ts
  - tests/phase29/approval-actions.spec.ts
  - tests/phase29/publish-chain-gate.spec.ts
  - tests/phase29/queue-classifier.spec.ts
autonomous: true
requirements: [APR-01, APR-02, APR-03, APR-04]
user_setup: []

must_haves:
  truths:
    - "Admin can save a 1-4 step approval chain per category (org-scoped, service-role write, org from JWT only)"
    - "Requesting publish on a chained-category SOP sets sops.approval_state='pending' + snapshots the current chain steps, WITHOUT publishing"
    - "The final-step approve calls the SAME performPublish() the no-chain route path calls (not a duplicated inline status flip)"
    - "Governance queue rows carry an 'awaiting_approval' flag when approval_state='pending', and isCallerNextApprover is computed per row"
    - "requestChanges clears approval_state to NULL, keeps approval_snapshot, and records a changes_requested row with a required comment"
  artifacts:
    - path: "src/actions/approvals.ts"
      provides: "chain CRUD + approve/request-changes + status/history reads"
      exports: ["setApprovalChain", "approveStep", "requestChanges", "getApprovalStatus", "getApprovalHistory", "getApprovalChains"]
    - path: "src/lib/governance/classify.ts"
      provides: "awaiting_approval flag"
      contains: "awaiting_approval"
  key_links:
    - from: "src/actions/approvals.ts"
      to: "src/lib/governance/publish-core.ts"
      via: "approveStep final step calls performPublish"
      pattern: "performPublish\\("
    - from: "src/app/api/sops/[sopId]/publish/route.ts"
      to: "approval_chains"
      via: "chain-gate lookup before performPublish"
      pattern: "approval_chains"
    - from: "src/actions/approvals.ts"
      to: "src/lib/governance/approvals.ts"
      via: "stepMatchesCaller / resolveNextStepIndex gate before insert"
      pattern: "stepMatchesCaller\\(|resolveNextStepIndex\\("
---

<objective>
Build the backend behavior: chain CRUD (`setApprovalChain`), the one-click `approveStep`/`requestChanges` actions, the `getApprovalStatus`/`getApprovalHistory` reads, the publish-route chain-gate that diverts chained-category SOPs into `pending_approval`, and the `awaiting_approval` governance-queue classification.

Purpose: This is the whole approval state machine. It reuses `performPublish()` (29-01) as the ONLY publish path, the pure resolver (29-01) as the ONLY "who's next" logic, and mirrors `governance.ts`/`sop_review_events` write patterns exactly — no parallel pipeline (D29-03).
Output: `src/actions/approvals.ts`; `governance.ts` exports `requireAdmin` (with role) + `listGovernanceQueue` computes `isCallerNextApprover`; publish route chain-gate; `classify.ts` awaiting_approval flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/phases/29-approval-chains/29-PATTERNS.md
@.planning/phases/28-ownership-review-lifecycle-governance-queue/28-03-SUMMARY.md

# Analog files
@src/actions/governance.ts
@src/lib/governance/classify.ts
@src/lib/governance/approvals.ts
@src/lib/governance/publish-core.ts
@src/app/api/sops/[sopId]/publish/route.ts
@src/actions/completions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/actions/approvals.ts + export requireAdmin(with role) from governance.ts</name>
  <read_first>
    - src/actions/governance.ts (requireAdmin at ~line 53 — currently private, AdminCtx = {userId, organisationId}, reads role from claims['user_role'] at ~line 59 but does not return it; setReviewCadence service-role write shape; getOrgMembers reuse)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 3 (full approveStep / requestChanges / setApprovalChain bodies)
    - src/actions/completions.ts (23505 idempotency idiom)
    - src/lib/governance/approvals.ts + publish-core.ts (the two modules this file wires)
    - src/lib/validators/approvals.ts (chain validation)
  </read_first>
  <files>src/actions/governance.ts, src/actions/approvals.ts</files>
  <action>
    In `governance.ts`: change `requireAdmin` from private to `export`, extend `AdminCtx` to `{ userId; organisationId; role: AppRole }`, and return `role: claims['user_role'] as AppRole` (the claim key is `user_role`, already read at line ~59 — just include it in the return). Import `AppRole` from `@/types/auth`. This is additive — existing destructures of `{ userId, organisationId }` are unaffected.
    Create `src/actions/approvals.ts` ('use server') per 29-PATTERNS.md §3:
    - `setApprovalChain(category, steps)` — `requireAdmin()`, Zod-validate via `approvalChainSchema` (1-4 steps, exactly one of role/userId, role ∈ admin|safety_manager per Pitfall 3), then UPSERT `approval_chains` via `createAdminClient()` with `organisation_id: ctx.organisationId` (JWT-derived ONLY, never a parameter — T-29-02-01), `onConflict: 'organisation_id,category'`. Mirror setReviewCadence exactly.
    - `getApprovalChains()` — `requireAdmin()`, read all `approval_chains` rows for the org (plain session client, org-scoped RLS).
    - `approveStep(sopId, comment?)` — plain session client (Pitfall 1/3: caller is always admin/safety_manager, `admins_can_update_sops` covers the sops write). Load sop (`id, version, category, approval_state, approval_snapshot`); reject if approval_state !== 'pending'. Read approved-only rows (`.eq('action','approved')`), build the Set, `resolveNextStepIndex`; reject if -1 ("Chain already complete"). `stepMatchesCaller(steps[nextIndex], { userId: ctx.userId, role: ctx.role })` — reject "Not your turn" if false (server-side EoP gate — never trust a client flag, T-29-02-02). INSERT the sop_approvals row (action 'approved'); on error code 23505 return `{ success: true }` (idempotent double-click). If `nextIndex === steps.length - 1`, call `performPublish(supabase, { sopId, organisationId: ctx.organisationId, userId: ctx.userId, approvalState: 'approved' })` and propagate its error.
    - `requestChanges(sopId, comment)` — require non-empty `comment.trim()` (locked in CONTEXT specifics), same requireAdmin + next-approver gate, INSERT a `changes_requested` sop_approvals row at nextIndex, then UPDATE `sops` SET `approval_state: null` — leave `approval_snapshot` in place (Pattern 2 / A3). Do NOT publish.
    - `getApprovalStatus(sopId)` — `requireAdmin()`; return `{ state, steps, approvals, nextStepIndex, isCallerNextApprover }` computed from the sop row's approval_snapshot + its approved rows (reuse resolveNextStepIndex + stepMatchesCaller). Used by the builder page (29-04).
    - `getApprovalHistory(versionIds: string[])` — `requireAdmin()`; read sop_approvals `.in('sop_id', versionIds)`, resolve approver labels via `getOrgMembers()` (third call site — reuse, do not hand-roll) and step labels from each version's `approval_snapshot[step_index].label`; return rows grouped-ready. Used by the versions page (29-05).
    Use `parseJwtPayload` (never raw atob) anywhere a JWT is read; use `(supabase as any)` for approval_chains/sop_approvals casts. File header must list every export + which client each uses + why (governance.ts discipline). Avoid the literal substring `createAdminClient` in any comment inside a plain-session function body (28-03 false-match learning).
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "performPublish shared|approval actions" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `requireAdmin` is exported and returns `role`; existing phase28 governance specs still pass.
    - approvals.ts exports setApprovalChain, getApprovalChains, approveStep, requestChanges, getApprovalStatus, getApprovalHistory.
    - approveStep's final-step branch calls `performPublish(` with `approvalState: 'approved'` (not an inline status flip).
    - setApprovalChain sources organisation_id from ctx only; requestChanges rejects empty comment.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The full approval action layer exists, wired to the pure resolver and the shared performPublish, with idempotent-approve + server-side next-approver gating.</done>
</task>

<task type="auto">
  <name>Task 2: Publish-route chain-gate (pending_approval divert + snapshot)</name>
  <read_first>
    - src/app/api/sops/[sopId]/publish/route.ts (post-29-01: step 1 auth+org, then delegates to performPublish)
    - .planning/phases/29-approval-chains/29-RESEARCH.md § "Publish route — chain gate inserted before the existing (unmoved) logic" code example
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 4
  </read_first>
  <files>src/app/api/sops/[sopId]/publish/route.ts, tests/phase29/publish-chain-gate.spec.ts</files>
  <action>
    Insert the chain-gate BETWEEN step 1 (auth+org) and the `performPublish()` call. First load the sop's `category, approval_state` (a lightweight select; the route already resolves organisationId). Look up `approval_chains` for `(organisationId, category)` via `(supabase as any).from('approval_chains').select('steps').eq('organisation_id', organisationId).eq('category', category ?? '').maybeSingle()`.
    - If `chainRow?.steps?.length > 0` AND `approval_state === 'pending'` → return `{ success: true, pendingApproval: true, alreadyPending: true }` (no-op, idempotent).
    - If `chainRow?.steps?.length > 0` AND not yet pending → UPDATE `sops` SET `approval_state: 'pending', approval_snapshot: chainRow.steps` `.eq('id', sopId).eq('status','draft').is('approval_state', null).select('id')`; if 0 rows return the alreadyPending shape, else return `{ success: true, pendingApproval: true }`. This OVERWRITES approval_snapshot on each fresh request-publish (APR-02 semantics).
    - If NO chain → fall through to the EXISTING `performPublish()` call, byte-identical.
    LOCKED ORDERING (D29-03, plan-checker Blocker 1 fix — do NOT deviate): BEFORE the chain-gate can set approval_state='pending', call `assertPublishGates(supabase, sopId)` (exported from publish-core.ts by 29-01). If `ok: false`, return the existing 400 response shape immediately — an SOP with unverified blocks/unapproved sections can NEVER enter pending_approval, chain or no chain. performPublish still calls assertPublishGates internally, so gates ALSO re-run at final approval (re-validates content edited mid-approval — Pitfall 6 closed both ways). One shared function, two call points — no duplicated gate logic.
    ALSO (plan-checker Blocker 2 fix): UPDATE `tests/phase29/publish-core-extraction.spec.ts` in this task — REMOVE the 29-01 assertion that the route "does NOT branch on approval_chains" (true only pre-29-02) and REPLACE with the post-chain-gate contract: route references `approval_chains`, and `assertPublishGates(` appears in route source BEFORE the `approval_state: 'pending'` write (assert source index ordering).
    Create `tests/phase29/publish-chain-gate.spec.ts` (phase29) — source-contract: route references `approval_chains`, sets `approval_state: 'pending'` + `approval_snapshot`, has the alreadyPending short-circuit, gate-before-divert source ordering, and the no-chain branch still calls `performPublish(`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "chain gate|pending" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Route diverts chained-category SOPs to approval_state='pending' + snapshot, without publishing.
    - `assertPublishGates(` called BEFORE the pending divert in route source (unverified SOPs 400, never enter pending — locked D29-03).
    - publish-core-extraction.spec.ts updated to the post-chain-gate contract (old "no approval_chains branch" assertion removed — would fail Wave-4 gate otherwise).
    - alreadyPending idempotent short-circuit present.
    - No-chain branch still calls performPublish (byte-identical path preserved).
    - `npx tsc --noEmit` + `npm run build` clean.
  </acceptance_criteria>
  <done>Publish route is the single entry: chained categories enter pending; no-chain categories publish exactly as before.</done>
</task>

<task type="auto">
  <name>Task 3: classify.ts awaiting_approval flag + listGovernanceQueue isCallerNextApprover</name>
  <read_first>
    - src/lib/governance/classify.ts (GovernanceFlag union, GovernanceInput, classifyGovernanceRow)
    - src/lib/governance/__tests__/classify.test.ts (existing phase28-unit cases to extend, not break)
    - src/actions/governance.ts (listGovernanceQueue composition — where per-row inputs are built)
    - .planning/phases/29-approval-chains/29-RESEARCH.md § Pattern 6
  </read_first>
  <files>src/lib/governance/classify.ts, src/lib/governance/__tests__/classify.test.ts, src/actions/governance.ts, tests/phase29/queue-classifier.spec.ts</files>
  <action>
    In `classify.ts`: extend `GovernanceFlag` with `'awaiting_approval'`; add `hasPendingApproval: boolean` (and keep `isCallerNextApprover` OUT of the pure classifier — it is a per-viewer concern surfaced on the GovernanceRow, not a flag) to `GovernanceInput`; in `classifyGovernanceRow`, `if (input.hasPendingApproval) flags.push('awaiting_approval')`. The flag is visible to EVERY admin (informational, GQ-01 glanceable surface), independent of who can act.
    In `governance.ts` `listGovernanceQueue`: include `approval_state, approval_snapshot, version` in the sops select; set `hasPendingApproval = approval_state === 'pending'`; compute per-row `isCallerNextApprover` by reading that SOP's approved sop_approvals rows (batch the query across the pending rows, or per-row — keep it simple), `resolveNextStepIndex` over the snapshot, then `stepMatchesCaller(snapshot[nextIndex], { userId: ctx.userId, role: ctx.role })`. Add `isCallerNextApprover: boolean` to the `GovernanceRow` type. Only pending rows need the extra approvals query — skip it for non-pending rows.
    Extend `src/lib/governance/__tests__/classify.test.ts` (phase28-unit) with an awaiting_approval case — do NOT break existing cases.
    Create `tests/phase29/queue-classifier.spec.ts` (phase29) — source-contract: classify.ts pushes awaiting_approval on hasPendingApproval; listGovernanceQueue selects approval_state and computes isCallerNextApprover via stepMatchesCaller.
  </action>
  <verify>
    <automated>npx playwright test --project=phase29-unit --project=phase29 --project=phase28-unit -g "awaiting_approval|classifier|governance" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - classify.ts emits awaiting_approval when hasPendingApproval; existing phase28 classify cases still green.
    - GovernanceRow carries isCallerNextApprover; listGovernanceQueue computes it via stepMatchesCaller for pending rows only.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The governance queue data layer flags pending-approval SOPs and knows, per viewer, who may approve next.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approver client → approveStep/requestChanges | client cannot be trusted to declare "I am the next approver" |
| admin client → setApprovalChain | org id must not be caller-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | setApprovalChain (cross-org write) | mitigate | organisation_id sourced ONLY from parseJwtPayload/ctx, never a parameter (mirrors setReviewCadence) |
| T-29-02-02 | Elevation of Privilege | approveStep step-skip / not-your-turn | mitigate | server-side stepMatchesCaller() + resolveNextStepIndex() gate BEFORE insert; never trust a client next-approver flag |
| T-29-02-03 | Tampering | sops writes by approver | mitigate | plain session client rides admins_can_update_sops; NO new createAdminClient() touching sops (Pitfall 1) |
| T-29-02-04 | Tampering | raw atob JWT decode reintroduced | mitigate | parseJwtPayload from @/lib/supabase/jwt exclusively (governance.ts pattern) |
| T-29-02-SC | Tampering | npm installs | accept | zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase29 --project=phase29-unit` green.
- phase28 + phase28-unit regression green (classify.ts + governance.ts modified additively).
- `npx tsc --noEmit` + `npm run build` clean.
- Cross-org isolation for setApprovalChain / approveStep / requestChanges carried as `test.fixme` runtime (Railway-only-testing convention, phase27/28 precedent).
</verification>

<success_criteria>
Chain CRUD, one-click approve/request-changes, the pending divert, the auto-publish on final approval, and the queue awaiting_approval classification all work through the single performPublish + single pure-resolver path.
</success_criteria>

<artifacts_produced>
- `src/actions/approvals.ts` (setApprovalChain, getApprovalChains, approveStep, requestChanges, getApprovalStatus, getApprovalHistory)
- `src/actions/governance.ts` (requireAdmin exported with role; listGovernanceQueue computes isCallerNextApprover; GovernanceRow extended)
- `src/app/api/sops/[sopId]/publish/route.ts` (chain-gate divert)
- `src/lib/governance/classify.ts` (awaiting_approval flag)
- `tests/phase29/approval-actions.spec.ts`, `tests/phase29/publish-chain-gate.spec.ts`, `tests/phase29/queue-classifier.spec.ts`
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-02-SUMMARY.md` when done.
</output>
