---
phase: 29-approval-chains
plan: 04
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - src/components/admin/governance/ApprovalChainPanel.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
  - src/app/(protected)/admin/sops/builder/[sopId]/page.tsx
  - tests/phase29/publish-stage-approval.spec.ts
autonomous: true
requirements: [APR-03, APR-04]
user_setup: []

must_haves:
  truths:
    - "When an SOP is approval_state='pending', the builder Publish stage shows the chain (step list + who's next); no chain SOPs show the unchanged Publish button"
    - "The matching next approver sees one-click Approve + Request-changes (comment required) in the Publish stage"
    - "Approve calls approveStep; Request-changes calls requestChanges; both refresh so state updates"
    - "Requesting publish on a chained SOP shows the pending chain (handlePublish handles the pendingApproval response)"
  artifacts:
    - path: "src/components/admin/governance/ApprovalChainPanel.tsx"
      provides: "presentational pending-chain display + Approve/Request-changes buttons"
      min_lines: 40
    - path: "src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx"
      provides: "conditional ApprovalChainPanel mount on approvalStatus.state==='pending'"
      contains: "ApprovalChainPanel"
  key_links:
    - from: "src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx"
      to: "src/actions/approvals.ts"
      via: "onApprove/onRequestChanges wired to approveStep/requestChanges"
      pattern: "approveStep\\(|requestChanges\\("
    - from: "src/app/(protected)/admin/sops/builder/[sopId]/page.tsx"
      to: "src/actions/approvals.ts"
      via: "getApprovalStatus fetch threaded to the shell"
      pattern: "getApprovalStatus\\("
---

<objective>
Surface the pending chain in the builder Publish stage: `ApprovalChainPanel` (presentational) mounted in `PublishStage` when `approvalStatus.state === 'pending'`, with one-click Approve / Request-changes wired through `BuilderStageShell` to the `approveStep`/`requestChanges` actions.

Purpose: APR-03/APR-04's SOP-side approval surface — one-click approve from where the admin already is, and the "who's next" pending display. No approval console (D29-05).
Output: `ApprovalChainPanel.tsx`; `PublishStage` extended with an optional `approvalStatus` prop; `BuilderStageShell` threads it + owns the approve/request-changes handlers; `page.tsx` computes `getApprovalStatus`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-approval-chains/29-RESEARCH.md
@.planning/phases/29-approval-chains/29-PATTERNS.md

# Analog files
@src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx
@src/app/(protected)/admin/sops/builder/[sopId]/page.tsx
@src/actions/approvals.ts
@.claude/skills/sketch-findings-SOPstart/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ApprovalChainPanel.tsx — presentational pending-chain display</name>
  <read_first>
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 6 (controlled/presentational contract, prop shape)
    - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx (the "props in, callbacks out, no fetch" contract to mirror)
    - .claude/skills/sketch-findings-SOPstart/SKILL.md (paper/ink tokens; `--accent-signoff` for sign-off/approval, `evidence-btn`, `mono`, `blueprint-frame`)
  </read_first>
  <files>src/components/admin/governance/ApprovalChainPanel.tsx</files>
  <action>
    Create a purely presentational client component `ApprovalChainPanel` with props `{ steps: ChainStep[]; approvals: ApprovalRow[]; nextStepIndex: number; canAct: boolean; onApprove: (comment?: string) => void; onRequestChanges: (comment: string) => void; pending?: boolean; error?: string | null }` — NO fetch/Supabase/action calls inside (identical contract to PublishStage). Render:
    - The ordered step list; each step shows its label + role/member target + a state chip (approved / current "who's next" / waiting) derived from `approvals` and `nextStepIndex`.
    - When `canAct` (caller is the next approver): a one-click "Approve" button (`onApprove`) and a "Request changes" control with a REQUIRED one-line comment input (`onRequestChanges(comment)` — disabled until non-empty; the comment is the accuracy signal back to the owner per CONTEXT specifics). Buttons render ONLY when `canAct`.
    - Surface `error` inline; show a busy state via `pending`.
    Use `--accent-signoff` tinting and paper/ink tokens. Import `ChainStep` from `@/lib/governance/approvals`; define/collocate the `ApprovalRow` render type.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Component is presentational (no fetch/action import); Approve/Request-changes only render when canAct.
    - Request-changes requires a non-empty comment before firing.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>A reusable pending-chain panel exists with props-in/callbacks-out, matching PublishStage's contract.</done>
</task>

<task type="auto">
  <name>Task 2: Thread approvalStatus through page → shell → PublishStage; wire handlers</name>
  <read_first>
    - src/app/(protected)/admin/sops/builder/[sopId]/page.tsx (RSC role-guarded fetch; where it renders BuilderStageShell and what props it passes)
    - src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx (handlePublish at ~line 111 handling the publish POST; PublishStage mount at ~line 312)
    - src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx (PublishStageProps)
    - src/actions/approvals.ts (getApprovalStatus, approveStep, requestChanges)
    - .planning/phases/29-approval-chains/29-PATTERNS.md § 6
  </read_first>
  <files>src/app/(protected)/admin/sops/builder/[sopId]/page.tsx, src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx, src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx</files>
  <action>
    In `page.tsx`: after the existing role-guarded SOP fetch, call `getApprovalStatus(sopId)` and pass the result as a new `approvalStatus` prop to `BuilderStageShell` (the select already covers the new columns via `select('*')`).
    In `PublishStage.tsx`: add an optional `approvalStatus?: { state: 'pending'|'approved'|null; steps: ChainStep[]; approvals: ApprovalRow[]; nextStepIndex: number; isCallerNextApprover: boolean }` prop plus `onApproveStep?` / `onRequestChanges?` callbacks. When `approvalStatus?.state === 'pending'`, render `<ApprovalChainPanel ... />` alongside the existing progress block; otherwise render nothing new (the existing "Publish SOP" button behavior is completely unchanged for no-chain SOPs — D29-03).
    In `BuilderStageShell.tsx`: accept the `approvalStatus` prop, thread it to `PublishStage`, and add `handleApprove`/`handleRequestChanges` (mirroring `handlePublish`'s useCallback+useTransition+router.refresh shape) that call `approveStep(sopId, comment?)` / `requestChanges(sopId, comment)` and surface errors. Extend `handlePublish`'s `res.ok` branch to detect `{ pendingApproval: true }` in the response body and `router.refresh()` (which re-fetches the now-pending SOP so the panel appears) — do NOT treat pendingApproval as an error.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx calls getApprovalStatus and passes approvalStatus to the shell.
    - PublishStage mounts ApprovalChainPanel only when state==='pending'; no-chain publish button unchanged.
    - BuilderStageShell wires onApprove→approveStep and onRequestChanges→requestChanges; handlePublish handles pendingApproval.
    - `npx tsc --noEmit` + `npm run build` clean; worker bundle Δ0 (admin-only surface).
  </acceptance_criteria>
  <done>The builder Publish stage shows the pending chain and lets the matching approver act in one click, without touching the no-chain publish flow.</done>
</task>

<task type="auto">
  <name>Task 3: Source-contract specs (wired handlers, conditional mount)</name>
  <read_first>
    - tests/phase28/governance-queue.spec.ts (wired-handler source-contract shape, 2026-06-05 rule)
    - .planning/phases/29-approval-chains/29-RESEARCH.md § Validation → Test Map (APR-03/APR-04 rows)
  </read_first>
  <files>tests/phase29/publish-stage-approval.spec.ts</files>
  <action>
    Create `tests/phase29/publish-stage-approval.spec.ts` (phase29) — source-contract, asserting REAL wiring not bare prop presence (2026-06-05): PublishStage renders `<ApprovalChainPanel` gated on `approvalStatus?.state === 'pending'`; ApprovalChainPanel's Approve button `onClick` references the `onApprove` callback and Request-changes references `onRequestChanges` with a comment guard; BuilderStageShell's handlers call `approveStep(` and `requestChanges(`; page.tsx calls `getApprovalStatus(`; handlePublish branch references `pendingApproval`.
  </action>
  <verify>
    <automated>npx playwright test --project=phase29 -g "ApprovalChainPanel mount|approve wired|request changes"</automated>
  </verify>
  <acceptance_criteria>
    - Spec asserts conditional mount + wired onApprove/onRequestChanges + action call sites + pendingApproval handling.
    - Spec green under the phase29 project.
  </acceptance_criteria>
  <done>The builder approval surface is proven wired end-to-end at the source-contract level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approver browser → approveStep/requestChanges | UI shows Approve only to the next approver, but the SERVER re-checks — UI gating is not the security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Elevation of Privilege | client renders Approve for wrong caller | mitigate | isCallerNextApprover only hides the button; approveStep re-runs stepMatchesCaller server-side (29-02) — the real gate |
| T-29-04-02 | Information Disclosure | non-admin reaching builder | mitigate | /admin/sops/builder/[sopId] already hard-redirects non-admin/safety_manager (existing guard, unchanged) |
| T-29-04-SC | Tampering | npm installs | accept | zero new dependencies |
</threat_model>

<verification>
- `npx playwright test --project=phase29` green.
- `npx tsc --noEmit` + `npm run build` clean; worker bundle Δ0.
- No no-chain publish regression (button behavior unchanged when approvalStatus is null).
</verification>

<success_criteria>
The builder Publish stage displays the pending chain and the one-click Approve / Request-changes controls for the matching approver, with the no-chain publish path untouched.
</success_criteria>

<artifacts_produced>
- `src/components/admin/governance/ApprovalChainPanel.tsx`
- `src/app/(protected)/admin/sops/builder/[sopId]/PublishStage.tsx` (approvalStatus prop + panel mount)
- `src/app/(protected)/admin/sops/builder/[sopId]/BuilderStageShell.tsx` (approve/request-changes handlers + pendingApproval handling)
- `src/app/(protected)/admin/sops/builder/[sopId]/page.tsx` (getApprovalStatus)
- `tests/phase29/publish-stage-approval.spec.ts`
</artifacts_produced>

<output>
Create `.planning/phases/29-approval-chains/29-04-SUMMARY.md` when done.
</output>
