---
context: deliberation
phase: v9.0-scoping
task: 6
total_tasks: 11
status: in_progress
last_updated: 2026-08-08T03:21:07.720Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> in this session. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Obligation is not access** — `src/actions/competency.ts:332-343` derives `requiredSopIds` from `sop_departments` ∪ `sop_access_people`, so the training matrix treats *can see* as *must do*. Any plan that widens visibility (the role ladder is exactly that) inflates every worker's matrix as a side effect. — Structural mitigation: Phase **44a must land before 44b**. The matrix must read a manager-set obligation record, not the access tables, before any inheritance ships. Do not re-merge these into one phase.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Deletion guard asserts file absence, not reference absence | `tests/phase30/dead-weight.spec.ts` had a test named "*/sops/[sopId]/walkthrough route is deleted*" that checked `fs.existsSync` on the directory and passed — while the worker Miller pane's primary CTA linked to that exact dead URL and 404'd in production. Internal hrefs are not type-checked, so tsc, `next build` and the full suite were all green. | blocking | Every `existsSync(...).toBe(false)` must be paired with a `src/` sweep for references to the deleted thing. Added in `09c1b7b`, mutation-proven both ways. Anchor such regexes narrowly (`/sops/` here) or import paths like `@/stores/walkthrough` become false positives. |
| Source-contract assertion pinned a variable name a refactor renamed | The same spec asserted the literal `sopDeptMap[sop.id]`; the library-tab merge renamed that predicate to `deptMatches(sopId)`, so the guard was about to go stale-red on a live filter. | advisory | Repoint source-contract assertions in the SAME commit that renames what they grep. Prefer asserting the behaviour plus that the caller still calls it, over pinning one token. |
| A component used far outside the size it was built for | `DepartmentSidebar` is a `w-[240px] h-screen sticky` aside; it was being rendered inside a 150px Miller column and hung below the scope list as its own full-height panel. Every gate was green — only a screenshot of the deployed page revealed it. | advisory | After any UI change, LOOK at the deployed page (CLAUDE.md 2026-07-14). Before reusing a component in a new container, read its root element's own sizing/positioning classes. |

<current_state>
No code work is in flight. All three `/sops` commits from this session are pushed,
Railway-deployed, and visually verified on sopstart.com at 1440px.

The pause is **mid-conversation on a decision sequence**. Simon is answering
decisions D1–D5 one at a time from a published artifact. D1 and D2 are locked.
**D3 has been asked and is awaiting his answer.**

Decision artifact (live, private):
https://claude.ai/code/artifact/6de44250-2870-4e43-9790-7710fd37d912
</current_state>

<completed_work>

Shipped this session (all pushed to master):

- `cbce023` — `/sops` merged "Your SOPs" and "SOP Library" into one Miller
  surface; the library became two scopes (Everything, Not added yet). Deleted
  `LibrarySection` (~130 lines of duplicate list renderer).
- `09c1b7b` — fixed "Start walkthrough" 404 (`/walkthrough` route was deleted in
  Phase 30; correct URL is `?tab=walk`), plus the reference-sweep guard above.
- `085251d` — logged the deleted-route dead-href learning in CLAUDE.md.
- `82fec78` — rebuilt the `/sops` desktop layout as sketch 005 variant C's Miller
  frame (one bordered 168/1fr/248 grid, sticky mono column headers, flush
  hairline rows, ink-fill selection). Fixed the `DepartmentSidebar` mis-sizing.
  Bundle: −11 KB on `/sops/[sopId]`.

Scoping work (not yet written to ROADMAP.md):

- Derived phases 44–50 for v9.0 from the 2026-08-04 Joe + Simon UAT session.
- Found the obligation-vs-access coupling (see BLOCKING CONSTRAINTS) and split
  Phase 44 into 44a/44b as a result.
- Published the D1–D5 decision artifact.
- D1 answered (C), D2 answered (B).
</completed_work>

<remaining_work>

1. **D3** — asked, awaiting answer. *Are the standard start/end steps copied or
   linked?* A = copied at creation (simple; change a hazard and you edit it 36
   times, drift invisible). **B = linked to one library source** (edit once,
   every SOP updates; needs an explicit override path; interacts with versioning
   and the publish gate). Recommendation is B. Follow-on if B is chosen: when a
   linked hazard changes, does every published SOP silently update or does it
   need re-verification?
2. **D4** — not yet asked. *Who can remove a worker's feedback once posted?*
   A = nobody (append-only), B = the author, **C = sign-off manager + admin,
   removals logged** (recommended — matches who already receives the email).
3. **D5** — not yet asked. *Who sees the SOP edit history?* A = admins and
   sign-off authority only, **B = everyone with SOP access, read-only**
   (recommended — consistent with the feedback decision).
4. Update the published artifact to mark D1–D5 settled (same file path
   republishes to the same URL).
5. Write v9.0 (44a, 44b, 45–50) into `ROADMAP.md` with requirement IDs. **Simon
   has not yet said to do this** — he gated it on reviewing the phase list first.
</remaining_work>

<decisions_made>

- **Inheritance is visibility-only.** The role ladder never confers training
  obligation; obligation is set by higher-level managers (model TBD in Joe's
  update). *Simon, 2026-08-05.*
- **Self-add = bookmark, never obligation.** Already true by accident — the
  matrix reads different tables than `sop_assignments` — but nothing guarantees
  it, so 44a needs a test pinning it. *Simon, 2026-08-05.*
- **D1 = C.** Split Phase 44 into 44a (obligation gets its own record, matrix
  repointed onto it) then 44b (role ladder). Backfill obligation = today's
  access, so no existing org's matrix moves on day one. *Simon, 2026-08-06.*
- **D2 = B.** Run 44a and 46 alongside v8.0 Phase 41 — they touch no routes so
  they cannot collide with the surface merge. 45 (view-as) still waits for 41.
  *Simon, 2026-08-06.*
- Chose linked-not-copied as the **recommendation** for D3 because Joe framing
  the start/end steps as generic across all hot end SOPs is a governance
  statement, not a convenience one. Not yet ratified.
</decisions_made>

<blockers>

- **Joe's hot end role hierarchy + ~12 SOPs per role** — due end of week
  (w/c 2026-08-04). This data *is* the first content of 44a's obligation record.
  Blocks detailed planning of 44a and 44b. No workaround.
- **Joe's worker-feedback spec** — blocks Phase 48. Partial workaround: decide
  D4 and put it to Joe as a proposal rather than an open question.
- **Joe's colour-coded generics + generic start/end steps** — blocks Phase 49.
- **Joe's annotated proofread (what is irrelevant to the operator role, with
  reasons)** — blocks Phase 50, which is gated on v8.0 Phase 42 regardless.
- **No outbound email exists in this codebase.** Phase 48 needs a provider, a
  sending domain on sopstart.com, and bounce handling. Scope it in, don't
  discover it mid-phase.
</blockers>

## Required Reading (in order)

1. `src/actions/competency.ts:325-345` — the `requiredSopIds` derivation. This is
   the blocking constraint above; read the code before planning 44a or 44b.
2. `src/lib/competency/matrix.ts` (header comment) — deliberately locks
   requirements to the already-materialized `sop_departments`/`sop_access_people`
   output. 44a changes this contract, so the comment must change with it.
3. `supabase/migrations/00046_org_model_schema.sql:102-160` — the `roles` table.
   Dept-scoped, has a `sort` int, has **no** parent/inheritance column. 44b adds
   `inherits_from_role_id`; do NOT overload `sort`, which is display order.
4. `CLAUDE.md` § Learnings, entries dated 2026-08-04 — the two RLS holes fixed
   the same day as the UAT session. Directly relevant to the library-discrepancy
   verification below.
5. `.planning/ROADMAP.md` § v8.0 — phases 41–43 are still open and 41 is running
   in parallel with 44a/46 per D2.
6. `.planning/research/customer-interviews/` — primary-source field research;
   CLAUDE.md requires consulting it before spec/discuss/plan on any new phase.

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Treating "the route directory is deleted" as proof nothing
  links to it → pair every deletion assertion with a reference sweep of `src/`.
- [ANTI-PATTERN]: Planning the role ladder as a single phase → the matrix reads
  the access tables the ladder widens; 44a must repoint it first.
- [ANTI-PATTERN]: Declaring UI work done on green tests → screenshot the
  deployed page.

## Infrastructure State

- **Railway / sopstart.com**: deployed and healthy at `82fec78`. `/sops`
  verified in-browser at 1440px, both scope groups and both detail-pane states.
- **Working tree**: clean of session work — everything is committed and pushed.
  The many untracked files in `git status` (`.agents/`, `*.png` sketches,
  `SOPstart - Raw SOPs/`, etc.) are pre-existing and unrelated.
- **Background processes**: none running.
- **Migrations**: live through `00062`. Nothing pending.

<context>
The session started as a UI simplification request for `/sops` and ended as
milestone scoping. The connective tissue is worth keeping in mind: the `/sops`
Miller surface that shipped today is *already the right shape* for the v9.0
permission model — "All yours" is the obligation set and "Everything / Not added
yet" is the inherited-visible set. That is why 44b needs no new worker UI, and
it is a genuine piece of luck rather than planning.

The scoping frame throughout has been: read the UAT transcript for what it
implies structurally, check each implication against the actual schema before
proposing a phase, and separate things the session *settled* from things it
*left open*. That is what surfaced the obligation-vs-access coupling — it was
not in the transcript at all, it came from tracing "cumulative SOPs" into
`competency.ts`.

Simon is working through decisions one at a time in chat, terse answers
("C", "B"). Keep offering one decision per turn with the options as a compact
table and a marked recommendation. Do not batch them.
</context>

<next_action>
Ask Simon for the **D3** answer — standard start/end steps copied vs linked
(recommendation: B, linked). Present as a compact table, one decision only.
Then D4, then D5, then offer to write v9.0 into ROADMAP.md.
</next_action>
