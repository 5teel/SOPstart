---
phase: 12.5-blueprint-redesign
verified: 2026-04-26T14:30:00Z
status: human_needed
score: 10/10
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 7/10
  gaps_closed:
    - "Online voice capture persists transcript + blob to sop_voice_notes via saveVoiceNote (SC-5)"
    - "FlowTab renders SVG node graph with derivation and D-16 JSONB override (SC-8)"
    - "Flow graph editable inside Puck builder via FlowGraphField + updateSopFlowGraph server action (SC-9)"
    - "CommandPalette with Cmd/Ctrl+K globally across /sops routes (SC-6)"
    - "/api/sops/[sopId]/ask streaming AI endpoint (SC-6 prerequisite)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Paper/ink theme does not bleed into admin routes — open /admin/sops, confirm dark steel-900 theme; open /sops/[sopId], confirm paper/ink white activates"
    expected: "Admin routes dark; worker /sops/* routes switch to paper/ink on load; theme reverts on browser back to admin"
    why_human: "CSS scope applied by useEffect on mount/unmount — requires live browser to confirm no bleed"
  - test: "Mobile immersive walkthrough enforced on phones — open /sops/[sopId]?tab=walkthrough on a 390px viewport, confirm only ImmersiveStepCard renders, list mode hidden"
    expected: "No list view on <=430px regardless of stored walkthrough-mode; ViewModeToggle hidden below 430px"
    why_human: "CSS-only viewport routing via .hide-below-430 classes requires live viewport testing"
  - test: "Voice capture state machine in browser — click Record on a VoiceNoteBlock step, confirm idle -> listening -> transcribing -> captured transitions"
    expected: "Transcript appears in block; Deepgram WebSocket connects; state machine progresses"
    why_human: "Requires live Deepgram connection and microphone access"
  - test: "Online voice persistence — record a voice note while online; verify row inserted in sop_voice_notes table in Supabase and audio blob uploaded to sop-voice-notes Storage bucket"
    expected: "sop_voice_notes row with audioStoragePath present; blob accessible in Storage"
    why_human: "Requires Supabase access + live Deepgram connection to verify end-to-end flow"
  - test: "Cmd/Ctrl+K opens CommandPalette on /sops/[sopId] — press Cmd+K, confirm palette opens showing JUMP TO STEP steps, ASK AI input, TOOLS & HAZARDS sections"
    expected: "Palette opens; JUMP TO STEP shows step list; ASK AI streams response; TOOLS & HAZARDS shows relevant sections"
    why_human: "Requires browser interaction to test keyboard shortcut, streaming response, and cmdk UI"
  - test: "FlowGraphEditor in Puck builder — navigate to /admin/sops/builder/[sopId], open the root panel, add 2 nodes, draw an edge, click Save to SOP; navigate to /sops/[sopId]?tab=flow and confirm SVG graph renders the saved nodes"
    expected: "Admin saves flow graph; worker FlowTab reads sops.flow_graph JSONB and renders it (D-16 override precedence)"
    why_human: "End-to-end write/read path through Supabase JSONB column — requires live browser + Supabase"
---

# Phase 12.5: Blueprint Redesign — Re-Verification Report

**Phase Goal:** Close all Wave 5 gaps — wire online voice persistence, replace FlowTab stub with SVG renderer, implement FlowGraphField Puck editor, implement CommandPalette with streaming AI ask endpoint.
**Verified:** 2026-04-26T14:30:00Z
**Status:** human_needed
**Re-verification:** Yes — after Wave 5 gap closure (Plans 05 and 06)

## Re-verification Summary

Previous verification (2026-04-26T12:00:00Z) found 3 complete gaps (SC-6 CommandPalette, SC-8 FlowTab SVG, SC-9 Puck editor) and 1 partial gap (SC-5 voice online persistence). Plans 05 and 06 targeted all four gaps. All automated checks now pass. Six human-verification items remain from the full phase (carried forward from initial verification plus two new items for the Wave 5 features).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Online voice transcript + blob persisted to sop_voice_notes via saveVoiceNote (VoiceNoteBlock) | ✓ VERIFIED | `handleCaptured` in VoiceNoteBlock.tsx: JWT decode → orgId → Storage upload to `sop-voice-notes` → `saveVoiceNote` call. `onCaptured` prop wired in VoiceCaptureControl JSX |
| 2 | Online measurement blob + transcript persisted via saveVoiceNote (blockType=measurement) | ✓ VERIFIED | MeasurementBlock.tsx: identical `handleCaptured` pattern; `blockType: 'measurement'`; `onCaptured={handleCaptured}` on VoiceCaptureControl |
| 3 | FlowTab renders SVG graph — rect nodes, line edges, accent colors per type | ✓ VERIFIED | FlowTab.tsx (163 lines): `FlowCanvas` renders `<svg>` with `<rect>` (160x48) + `<text>` nodes, `<line>` edges from bottom-centre to top-centre. `NODE_COLORS` and `EDGE_COLORS` maps with CSS var fallbacks. Empty state renders `<p>` |
| 4 | FlowTab checks sop.flow_graph first (D-16 override); falls back to deriveFlowGraph with warn-once | ✓ VERIFIED | Lines 140-153 of FlowTab.tsx: `if (sop.flow_graph != null)` → `FlowGraphSchema.safeParse` → success uses parsed data; failure triggers `warnedRef.current` guard then uses `derivedGraph`. `useMemo` keyed on `sop.id, sop.updated_at` |
| 5 | FlowGraphSchema matches D-14 Zod shape with blockProps as z.record(z.string(), z.unknown()) | ✓ VERIFIED | flow-graph.ts (21 lines): `version: z.literal(1)`, `nodes` array with all 7 type enum values, `edges` array with 4 kind values. `blockProps: z.record(z.string(), z.unknown()).optional()` — Zod v4 fix applied |
| 6 | Admin Puck builder has FlowGraphField panel; admin can add nodes, draw edges, save to sops.flow_graph | ✓ VERIFIED | `flow-graph-field.tsx` (471 lines): 7-type node palette, SVG drag canvas with 20px grid snap via `getScreenCTM().inverse()`, edge mode toggle, right props panel, "Save to SOP" button calling `updateSopFlowGraph`. `FlowGraphField` reads sopId via `useParams`. Registered in `puck-config.tsx` at `root.fields.flowGraph` (lines 198-207) |
| 7 | updateSopFlowGraph validates FlowGraphSchema, enforces 256KB MAX_BYTES, rejects non-admin/safety_manager | ✓ VERIFIED | `flow-graph.ts` action: `Input.safeParse` → `Buffer.byteLength` check → `supabase.auth.getUser()` → role extraction from `user_metadata` + `app_metadata` fallback → `['admin', 'safety_manager'].includes(role)` gate → `supabase.from('sops').update({flow_graph: ...})` |
| 8 | Cmd/Ctrl+K on any /sops route opens CommandPalette globally via CmdKProvider in sops layout | ✓ VERIFIED | `CmdKProvider.tsx`: `usePathname` + `/\/sops\/([^/]+)/` regex → `useSopDetail(sopId ?? '')` (enabled: !!sopId guard in hook). `handleKeyDown` checks `(e.metaKey\|\|e.ctrlKey) && e.key==='k'` + `document.querySelector('[aria-modal="true"]')` guard. `sops/layout.tsx` wraps `{children}` with `<CmdKProvider>` |
| 9 | CommandPalette shows JUMP TO STEP, ASK AI streaming, TOOLS & HAZARDS groups | ✓ VERIFIED | `CommandPalette.tsx` (161 lines): 3 `Command.Group` headings (JUMP TO STEP, ASK AI, TOOLS & HAZARDS). JUMP TO STEP: `sop.sop_sections.flatMap(s=>s.sop_steps).slice(0,20)` → onSelect pushes `?tab=walkthrough&step={id}`. ASK AI: `fetch /api/sops/${sop.id}/ask` with `sopId: sop.id` in body, `ReadableStream.getReader()` loop. TOOLS & HAZARDS: filters by section_type + warning/caution |
| 10 | /api/sops/[sopId]/ask is auth-gated, rejects body.sopId !== path.sopId, returns streaming response | ✓ VERIFIED | `ask/route.ts`: `supabase.auth.getUser()` → 401 if no user. `body.sopId && body.sopId !== sopId` → 400 'SOP mismatch'. `ReadableStream` with `for await (const event of stream)` enqueuing `text_delta` chunks. No `NEXT_PUBLIC_ANTHROPIC` prefix |

**Score:** 10/10 truths verified

### Gaps Closed vs Previous Verification

| Gap | Previous Status | Current Status |
|-----|----------------|----------------|
| SC-5: Online voice cloud persistence | ✗ PARTIAL (saveVoiceNote never called) | ✓ CLOSED |
| SC-6: CommandPalette + Cmd/Ctrl+K | ✗ FAILED (component absent) | ✓ CLOSED |
| SC-8: FlowTab SVG renderer | ✗ FAILED (18-line stub) | ✓ CLOSED |
| SC-9: Puck FlowGraphField editor | ✗ FAILED (no implementation) | ✓ CLOSED |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/sop/VoiceCaptureControl.tsx` | onCaptured optional callback, fires in online path after onTranscript | ✓ VERIFIED | Lines 48, 59, 106-107: interface + destructure + `else if (result.transcript && onCaptured)` call |
| `src/components/sop/blocks/VoiceNoteBlock.tsx` | handleCaptured + onCaptured wired | ✓ VERIFIED | Lines 7, 41: import + call; `onCaptured={handleCaptured}` on VoiceCaptureControl |
| `src/components/sop/blocks/MeasurementBlock.tsx` | handleCaptured + onCaptured wired (blockType=measurement) | ✓ VERIFIED | Lines 7, 61: import + call; `blockType: 'measurement'` |
| `src/lib/validators/flow-graph.ts` | FlowGraphSchema Zod validator + FlowGraph type | ✓ VERIFIED | 21 lines; all D-14 fields present; Zod v4 `z.record(z.string(), z.unknown())` fix applied |
| `src/lib/sop/flow-graph.ts` | deriveFlowGraph function | ✓ VERIFIED | 60 lines; `allSteps = sop_sections.flatMap(s => s.sop_steps)`; BLOCK_TYPE_TO_NODE map; sequential edges |
| `src/components/sop/tabs/FlowTab.tsx` | SVG FlowCanvas + D-15 useMemo + D-16 override | ✓ VERIFIED | 163 lines; `useMemo(() => deriveFlowGraph(sop), [sop.id, sop.updated_at])`; D-16 logic; FlowCanvas with nodes/edges |
| `src/lib/builder/flow-graph-field.tsx` | FlowGraphField Puck field + FlowGraphEditor SVG canvas | ✓ VERIFIED | 471 lines; palette, drag SVG, edge mode, props panel, Save button |
| `src/actions/flow-graph.ts` | updateSopFlowGraph server action | ✓ VERIFIED | 42 lines; FlowGraphSchema validation, MAX_BYTES, role gate, Supabase update |
| `src/lib/builder/puck-config.tsx` | root.fields.flowGraph custom field | ✓ VERIFIED | Lines 3, 198-207: FlowGraphField import + root field registration |
| `src/components/sop/CommandPalette.tsx` | cmdk palette with 3 groups | ✓ VERIFIED | 161 lines; `from 'cmdk'`; 3 Command.Group headings; streaming ASK AI |
| `src/app/api/sops/[sopId]/ask/route.ts` | Streaming AI endpoint | ✓ VERIFIED | 96 lines; auth gate, cross-SOP guard, Anthropic stream, ReadableStream |
| `src/app/(protected)/sops/CmdKProvider.tsx` | CmdKProvider client component | ✓ VERIFIED | 41 lines; usePathname regex, useSopDetail, Cmd+K handler, aria-modal guard |
| `src/app/(protected)/sops/layout.tsx` | CmdKProvider wrapping children | ✓ VERIFIED | 12 lines; `<CmdKProvider>` wraps `<PaperThemeMount />` + `{children}` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `VoiceNoteBlock.tsx` | `saveVoiceNote` server action | `handleCaptured` → Storage upload → `saveVoiceNote` | ✓ WIRED | `onCaptured={handleCaptured}` on VoiceCaptureControl; Storage path `{orgId}/voice/{sopId}/{uuid}.{ext}` |
| `MeasurementBlock.tsx` | `saveVoiceNote` server action | `handleCaptured` → Storage upload → `saveVoiceNote` | ✓ WIRED | Same pattern; `blockType: 'measurement'`; `language: 'en-NZ'` |
| `VoiceCaptureControl.tsx` | `onCaptured` callback | `else if (result.transcript && onCaptured)` in handleStop | ✓ WIRED | Fires after `onTranscript` in online path; offline path unchanged (Dexie queue) |
| `FlowTab.tsx` | `deriveFlowGraph` | `useMemo` + D-16 fallback branch | ✓ WIRED | `derivedGraph` used in both null-flow_graph and invalid-flow_graph branches |
| `FlowTab.tsx` | `sop.flow_graph` JSONB | `FlowGraphSchema.safeParse(sop.flow_graph)` | ✓ WIRED | D-16 override precedence: JSONB first, derived fallback |
| `puck-config.tsx` | `FlowGraphField` | `root.fields.flowGraph` custom field render | ✓ WIRED | `type: 'custom'`, render calls `<FlowGraphField value={value} onChange={onChange} />` |
| `FlowGraphEditor` | `updateSopFlowGraph` | "Save to SOP" button `handleSave` | ✓ WIRED | `await updateSopFlowGraph({ sopId, graph: localGraph })` in `handleSave` |
| `sops/layout.tsx` | `CommandPalette` | `CmdKProvider` wrapping children | ✓ WIRED | CmdKProvider imported and used; renders CommandPalette when `sop && sopId` |
| `CommandPalette.tsx` | `/api/sops/[sopId]/ask` | `fetch POST` with `sopId: sop.id` in body | ✓ WIRED | `fetch(\`/api/sops/${sop.id}/ask\`)` with `{ query: aiQuery, sopId: sop.id }` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `VoiceNoteBlock.tsx` | `saved` (UI state) | `onTranscript` callback (Deepgram) | Yes — real transcript | ✓ FLOWING |
| `VoiceNoteBlock.tsx` | `sop_voice_notes` row | `handleCaptured` → Storage → `saveVoiceNote` | Yes — real DB insert | ✓ FLOWING (code path; live verification needed) |
| `FlowTab.tsx` | `graph` (FlowGraph) | `deriveFlowGraph(sop)` from `sop.sop_sections[].sop_steps[]` OR `sop.flow_graph` JSONB | Yes — real SOP data | ✓ FLOWING |
| `CmdKProvider.tsx` | `sop` (SopWithSections) | `useSopDetail(sopId)` → TanStack Query → Supabase | Yes — real SOP from DB | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Evidence | Status |
|----------|----------|--------|
| VoiceCaptureControl fires onCaptured in online path | `else if (result.transcript && onCaptured)` branch at line 106 | ✓ PASS (code verified) |
| VoiceNoteBlock imports saveVoiceNote and calls it in handleCaptured | Lines 7, 41 of VoiceNoteBlock.tsx | ✓ PASS (code verified) |
| FlowTab stub text "Wave 5 wires FlowCanvas here" is gone | FlowTab.tsx is 163 lines; no stub text | ✓ PASS |
| FlowTab useMemo keyed on sop.id + sop.updated_at | Line 137: `useMemo(() => deriveFlowGraph(sop), [sop.id, sop.updated_at])` | ✓ PASS |
| updateSopFlowGraph has MAX_BYTES guard | Line 6 of flow-graph.ts: `const MAX_BYTES = 256 * 1024` | ✓ PASS |
| updateSopFlowGraph rejects non-admin/safety_manager | Line 31: `if (!['admin', 'safety_manager'].includes(role))` | ✓ PASS |
| Ask route returns 401 without auth | Line 21: `if (!user) return new Response('Unauthorized', { status: 401 })` | ✓ PASS |
| Ask route cross-SOP guard active | Lines 28-30: `if (body.sopId && body.sopId !== sopId)` → 400 | ✓ PASS |
| No NEXT_PUBLIC_ANTHROPIC in ask route | Anthropic SDK reads `ANTHROPIC_API_KEY` server-side; no NEXT_PUBLIC_ prefix in file | ✓ PASS |
| CmdKProvider mounted in layout not page | sops/layout.tsx wraps children with CmdKProvider; CommandPalette absent from sops/[sopId]/page.tsx | ✓ PASS |
| useSopDetail empty-string guard | Hook has `enabled: !!sopId` — empty string disables query; no spurious fetch when not on SOP route | ✓ PASS |
| CommandPalette includes sopId in ASK AI POST body | Line 31: `body: JSON.stringify({ query: aiQuery, sopId: sop.id })` | ✓ PASS |
| CmdKProvider aria-modal guard prevents opening over EscalationFormModal | Line 18: `document.querySelector('[aria-modal="true"]')` check | ✓ PASS |
| 3 Command.Group headings present | Lines 74, 98, 132: JUMP TO STEP, ASK AI, TOOLS & HAZARDS | ✓ PASS |

### Requirements Coverage

Note: SB-UX-06, SB-UX-07, SB-UX-09, SB-UX-10 are phase-internal requirement IDs not present in `.planning/REQUIREMENTS.md` (which covers SB-AUTH, SB-SECT, SB-LAYOUT, SB-BLOCK, SB-ANNOT, SB-COLLAB, SB-INFRA). Status reflects the phase SPEC/CONTEXT definitions.

| Requirement | Source Plan | Description | Status | Evidence |
|------------|-------------|-------------|--------|----------|
| SB-UX-06 | 12.5-05 | Server-side voice transcription with cloud persistence in online path | ✓ SATISFIED | VoiceNoteBlock + MeasurementBlock both call saveVoiceNote via handleCaptured; Storage upload path mirrors voice-queue.ts pattern |
| SB-UX-07 | 12.5-06 | Cmd/Ctrl+K command palette globally across /sops routes | ✓ SATISFIED | CmdKProvider in sops/layout.tsx; 3 result groups; streaming ASK AI; aria-modal guard |
| SB-UX-09 | 12.5-05 | Flow tab renders SVG graph via derivation + explicit JSONB override (D-16) | ✓ SATISFIED | FlowCanvas SVG renderer with rect nodes, line edges, accent colors; deriveFlowGraph + D-16 override precedence; useMemo D-15 |
| SB-UX-10 | 12.5-06 | Flow graph editable inside Puck builder | ✓ SATISFIED | FlowGraphField in puck-config.tsx root.fields; FlowGraphEditor SVG canvas; updateSopFlowGraph server action with role gate + size cap |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/(protected)/sops/CmdKProvider.tsx` | 12 | `useSopDetail(sopId ?? '')` — passes empty string when not on a SOP detail route | Info | Intentional: hook has `enabled: !!sopId` so empty string disables the query. Not a stub — correct pattern. |

No blockers or warnings found. The empty-string sopId pattern is a valid guard (TanStack Query `enabled: false` when sopId is falsy).

### Human Verification Required

#### 1. Paper/ink theme does not bleed into admin routes

**Test:** Log in as admin, navigate to `/admin/sops`. Confirm dark steel-900 theme (NOT paper/ink). Then navigate to `/sops` and open any published SOP. Confirm paper/ink white background activates.
**Expected:** Admin routes stay dark; worker `/sops/*` routes switch to paper/ink on load; theme reverts on browser back to admin.
**Why human:** CSS scope applied by `useEffect` on mount/unmount — requires live browser to confirm no bleed.

#### 2. Mobile immersive walkthrough enforced on phones

**Test:** Open `/sops/[sopId]?tab=walkthrough` on a 390px viewport (real phone or Chrome DevTools). Confirm only `ImmersiveStepCard` renders. Toggle `ViewModeToggle` from a desktop viewport and confirm list mode appears; confirm toggle is hidden below 430px.
**Expected:** No list view on <=430px regardless of stored `safestart.walkthrough-mode`; toggle visible only above 430px.
**Why human:** CSS-only viewport routing via `.hide-below-430` and `.immersive-only-below-430` classes requires live viewport testing.

#### 3. Voice capture state machine in browser

**Test:** Navigate to a walkthrough step containing a VoiceNoteBlock. Click Record. Confirm state transitions: idle → listening → Stop → transcribing → captured (transcript shown).
**Expected:** Deepgram WebSocket connects; state machine progresses correctly; transcript appears in block.
**Why human:** Requires live Deepgram connection and microphone access.

#### 4. Online voice persistence end-to-end

**Test:** While online, record a voice note in VoiceNoteBlock. After the transcript appears, check Supabase: confirm a row was inserted in `sop_voice_notes` with a valid `audio_storage_path`, and the corresponding blob is accessible in the `sop-voice-notes` Storage bucket.
**Expected:** `sop_voice_notes` row present with org-scoped path `{orgId}/voice/{sopId}/{uuid}.{ext}`; blob uploaded to Storage.
**Why human:** End-to-end path through JWT decode → Storage upload → server action insert — requires live Supabase + Deepgram.

#### 5. CommandPalette opens and streams AI response

**Test:** Navigate to `/sops/[sopId]`. Press Cmd+K (Mac) or Ctrl+K (Windows). Confirm palette opens. Verify JUMP TO STEP shows steps from the current SOP (up to 20). In ASK AI, type a question and press Enter — confirm streaming response appears. Verify TOOLS & HAZARDS shows relevant sections.
**Expected:** Palette opens on keyboard shortcut; three groups populated from current SOP; ASK AI streams text response from `/api/sops/[sopId]/ask`; palette closes on Escape.
**Why human:** Requires browser interaction for keyboard shortcut, streaming render, and cmdk UI behaviour.

#### 6. FlowGraphEditor write + FlowTab read

**Test:** In `/admin/sops/builder/[sopId]`, open the Puck root panel ("Page" panel). Add 2 nodes via the palette, draw a sequential edge, click "Save to SOP". Navigate to `/sops/[sopId]?tab=flow`. Confirm the SVG graph renders the saved 2 nodes and 1 edge (D-16 override precedence — explicit JSONB used instead of derived).
**Expected:** FlowTab reads `sops.flow_graph` JSONB and renders it; derived graph is NOT used when explicit graph is present and valid.
**Why human:** End-to-end write path (FlowGraphEditor → updateSopFlowGraph → Supabase) and read path (FlowTab D-16 → SVG render) requires live browser + Supabase.

---

_Verified: 2026-04-26T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
