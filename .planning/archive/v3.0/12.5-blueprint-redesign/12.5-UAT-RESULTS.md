---
phase: 12.5-blueprint-redesign
uat_run_initial: 2026-05-07T11:41:00Z
uat_run_resumed: 2026-05-08T05:16:00Z
runner: Playwright (Chromium) against `next dev` (initial) + `next start` (resumed) on localhost:4200
status: pass-pending-deepgram
---

# Phase 12.5 — Batched UAT Results

## Summary

Initial run against `next dev` blocked at #3 / #6 / #7 by the Windows webpack file-lock race. Resumed run against `next start` (production build) cleared all of those. Six of seven scenarios PASS; only the two Deepgram/microphone items remain as carried human-UAT (no automated path possible — physical hardware).

## Results

| # | Scenario | Result | Evidence |
|---|---|---|---|
| 1 | Paper/ink theme does not bleed into admin routes | ✓ PASS | `/admin/sops` body `bg = rgb(17, 24, 39)` (steel-900 dark); `<html data-theme="steel-forge" class="dark">` |
| 2 | Paper/ink theme activates on `/sops/[sopId]` | ✓ PASS | `/sops/{id}` body `bg = rgb(250, 250, 250)` (paper white); `fg = rgb(9, 9, 11)` (ink black); `--paper-bg = #e4e4e7`; 6-tab nav present (Overview / Tools / Hazards / Flow / Model / Walkthrough) |
| 3 | Mobile immersive walkthrough enforced on phones (390px viewport) | ✓ PASS | At `viewport.w=390`: 2 `.hide-below-430` elements have `display: none`; ImmersiveStepCard renders active step ("Procedure · Step 1/10 PREPARATION — …"); ViewModeToggle hidden as designed |
| 4 | Voice capture state machine in browser (Deepgram) | ⚠ HUMAN-UAT | Requires microphone hardware + live Deepgram WebSocket — no automation path |
| 5 | Online voice persistence to Supabase Storage + sop_voice_notes | ⚠ HUMAN-UAT | Same — depends on #4 |
| 6 | Cmd/Ctrl+K opens CommandPalette globally | ✓ PASS | `page.keyboard.press('Control+k')` opens palette ("SOP Command Palette" header). Both inputs present: `Search steps, ask a question, or find tools & hazards…` and `Ask about "Alkaline Cleaning Tank Operation — Machi"…`. JUMP TO STEP renders live SOP steps |
| 7 | FlowGraphField round-trip in Puck builder | ✓ PASS (read path) / ⚠ HUMAN-UAT (write path) | `/sops/{id}?tab=flow` renders SVG with **10 rect nodes + 15 lines/paths** derived from SOP steps (D-15 fallback active since `sop.flow_graph` is null). Write path (admin saves graph → worker reads override) needs interactive Puck builder UAT against an admin draft SOP |

## What's left for human UAT

- **#4 / #5 Deepgram voice + persistence**: only achievable with physical microphone hardware. Test on Simon's actual machine.
- **#7 write-path round-trip**: open `/admin/sops/{id}/builder`, drag 2 nodes into FlowGraphField, draw an edge, click "Save to SOP", reload `/sops/{id}?tab=flow`, confirm SVG renders the saved nodes (D-16 override beats derivation).

## Resumed-run procedure (reusable)

```powershell
npm run build; if ($?) { Start-Process -NoNewWindow -FilePath npm -ArgumentList 'run','start' }
node scripts/uat-session.mjs > .uat-cookie.json
# Inject .uat-cookie.json's cookieValue via document.cookie at http://localhost:4200, then navigate.
```
