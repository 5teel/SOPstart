---
phase: 15-manufacturing-line-mode
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/capture-bundle-baseline.ts
  - scripts/check-bundle-size.ts
  - .bundle-baseline.json
  - tests/integration/desktop-walkthrough-layout.spec.ts
  - tests/integration/sequential-ack.spec.ts
  - tests/integration/voice-qa-happy-path.spec.ts
  - tests/integration/voice-grounding-scope.spec.ts
  - tests/integration/sub-trade-rls-backward-compat.spec.ts
  - tests/e2e/sub-trade-assignment.spec.ts
  - tests/fixtures/anthropic-voice-mock.ts
  - tests/fixtures/visy-enf4-03-031.sql
  - tests/lint/no-static-desktop-import.spec.ts
autonomous: true
requirements: [SB-LINE-06]
must_haves:
  truths:
    - "A pre-Phase-15 First Load JS baseline number exists on disk for `/sops/[sopId]/page` so Wave 4 has something to compare against"
    - "Every Wave 1-4 spec file referenced in 15-VALIDATION.md exists as a skipped scaffold so executors never write code without a failing test"
    - "A grep-based lint test prevents any future static import of `DesktopWalkthrough` or `WalkthroughVoiceModal`"
  artifacts:
    - path: ".bundle-baseline.json"
      provides: "Pre-Phase-15 First Load JS measurement keyed by route"
      contains: "/sops/[sopId]/page"
    - path: "scripts/capture-bundle-baseline.ts"
      provides: "One-shot capture script writing .bundle-baseline.json"
    - path: "scripts/check-bundle-size.ts"
      provides: "CI gate comparing post-build size to baseline, ≤2 KB tolerance, also asserts DesktopWalkthrough chunk exists"
    - path: "tests/integration/desktop-walkthrough-layout.spec.ts"
      provides: "Failing scaffold for SB-LINE-01 (desktop ≥24px + mobile non-regression)"
    - path: "tests/integration/sequential-ack.spec.ts"
      provides: "Failing scaffold for SB-LINE-02 (sequential gate + forward-jump redirect)"
    - path: "tests/integration/voice-qa-happy-path.spec.ts"
      provides: "Failing scaffold for SB-LINE-03 (mocked Anthropic, modal a11y)"
    - path: "tests/integration/voice-grounding-scope.spec.ts"
      provides: "Failing scaffold for SB-LINE-04 (2-SOP fixture, not-in-this-procedure)"
    - path: "tests/integration/sub-trade-rls-backward-compat.spec.ts"
      provides: "Failing scaffold for SB-LINE-05 (empty sops_sub_trades = visible to all)"
    - path: "tests/e2e/sub-trade-assignment.spec.ts"
      provides: "Failing scaffold for SB-LINE-05 (admin assigns + worker visibility)"
    - path: "tests/fixtures/anthropic-voice-mock.ts"
      provides: "Canned answer + verifier responses for voice tests"
    - path: "tests/fixtures/visy-enf4-03-031.sql"
      provides: "Seed SQL for the demo Visy SOP"
    - path: "tests/lint/no-static-desktop-import.spec.ts"
      provides: "Grep lint preventing static import of DesktopWalkthrough / WalkthroughVoiceModal outside WalkthroughSwitcher.tsx"
  key_links:
    - from: "scripts/check-bundle-size.ts"
      to: ".bundle-baseline.json"
      via: "fs.readFileSync"
      pattern: "BASELINE_FILE = '\\.bundle-baseline\\.json'"
    - from: "scripts/check-bundle-size.ts"
      to: ".next/app-build-manifest.json"
      via: "fs.readFileSync after next build"
      pattern: "app-build-manifest\\.json"
---

<objective>
Capture the pre-Phase-15 mobile-route bundle baseline AND scaffold every Playwright/test fixture file referenced in `15-VALIDATION.md`. Without this wave: (1) Wave 4's bundle-isolation CI gate has nothing to compare against, and (2) Wave 1-3 executors write production code without a failing test waiting (Nyquist violation per `15-VALIDATION.md` § Wave 0 Requirements).

Purpose: lock the SB-LINE-06 reference point + satisfy Nyquist Rule (every `<verify>` block downstream has a real `<automated>` command path).

Output: 1 JSON baseline file + 2 build scripts + 8 test scaffolds + 1 lint test, all committed before any other Phase 15 work begins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-manufacturing-line-mode/15-SPEC.md
@.planning/phases/15-manufacturing-line-mode/15-RESEARCH.md
@.planning/phases/15-manufacturing-line-mode/15-PATTERNS.md
@.planning/phases/15-manufacturing-line-mode/15-VALIDATION.md
@CLAUDE.md
@playwright.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-Phase-15 First Load JS baseline for /sops/[sopId]</name>
  <files>scripts/capture-bundle-baseline.ts, .bundle-baseline.json</files>
  <read_first>
    - `package.json` (verify `next build` command and existing scripts/ structure)
    - `15-RESEARCH.md` lines 1010-1047 (sister script `check-bundle-size.ts` — invert for baseline)
    - `15-PATTERNS.md` lines 726-734 (script pattern notes — runs after `next build`, keys baseline by route → KB)
  </read_first>
  <action>
    Create `scripts/capture-bundle-baseline.ts` per **D-02 / SB-LINE-06**. The script:
    1. Reads `.next/app-build-manifest.json` (produced by `npm run build`)
    2. For route `/sops/[sopId]/page`, sums byte size of every chunk listed via `fs.statSync` over `path.join('.next', chunkPath)`
    3. Writes `.bundle-baseline.json` with shape `{ "capturedAt": "<iso>", "routes": { "/sops/[sopId]/page": <KB-integer> } }`
    4. Logs `Baseline captured: /sops/[sopId]/page = <N> KB`

    Then execute the script (this task includes running it):
    ```powershell
    npm run build; if ($?) { npx tsx scripts/capture-bundle-baseline.ts }
    ```

    Commit BOTH `scripts/capture-bundle-baseline.ts` AND `.bundle-baseline.json` so the baseline number is permanent in git. This is the irreversible reference point for Wave 4's check.

    **DO NOT** edit any Phase 15 source files before running this — the baseline must measure Phase-14-head, not a partially-modified tree.
  </action>
  <acceptance_criteria>
    - `Test-Path scripts/capture-bundle-baseline.ts` returns True
    - `Test-Path .bundle-baseline.json` returns True
    - `(Get-Content .bundle-baseline.json | ConvertFrom-Json).routes.'/sops/[sopId]/page'` is an integer > 0
    - `(Get-Content .bundle-baseline.json | Select-String 'capturedAt').Count` ≥ 1
    - `Select-String -Path scripts/capture-bundle-baseline.ts -Pattern 'app-build-manifest\.json'` returns at least one match
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "Test-Path .bundle-baseline.json -PathType Leaf; (Get-Content .bundle-baseline.json -Raw | ConvertFrom-Json).routes.'/sops/[sopId]/page' -gt 0"</automated>
  </verify>
  <done>`.bundle-baseline.json` exists with a positive integer KB value keyed under `routes['/sops/[sopId]/page']`, captured against Phase-14-head before any Phase 15 file is created.</done>
</task>

<task type="auto">
  <name>Task 2: Write bundle-isolation CI gate (check-bundle-size.ts)</name>
  <files>scripts/check-bundle-size.ts, package.json</files>
  <read_first>
    - `15-RESEARCH.md` lines 1010-1047 (full sketch — copy verbatim)
    - `15-PATTERNS.md` lines 726-734 (wiring + assertion list)
    - `package.json` (locate existing `prebuild` / `postbuild` script slots)
  </read_first>
  <action>
    Create `scripts/check-bundle-size.ts` per **D-02 / SB-LINE-06 / RESEARCH.md § Code Examples**. The script asserts:
    1. `.next/app-build-manifest.json` contains chunks for `/sops/[sopId]/page` summing to ≤ baseline KB + 2 KB tolerance (read `TOLERANCE_KB = 2`)
    2. JSON.stringify of the manifest includes the string `DesktopWalkthrough` AT LEAST as a chunk filename (proves dynamic-import emitted a separate chunk per Pitfall 5)
    3. JSON.stringify of the manifest includes the string `WalkthroughVoiceModal`
    On failure, log the actual delta + which chunks were found, then `process.exit(1)`.

    Wire into `package.json`:
    - Add `"postbuild": "tsx scripts/check-bundle-size.ts"` so `npm run build` automatically gates on bundle size.
    - DO NOT touch existing scripts.

    **Important:** at this point the script is expected to PASS (no Phase 15 code exists yet, so delta = 0; the DesktopWalkthrough chunk assertion will fail until Wave 2 — adjust the script to no-op the DesktopWalkthrough/WalkthroughVoiceModal assertion if `process.env.PHASE_15_WAVE === '0'` OR if both chunks are absent AND delta ≤ 0; document this Wave-0 carve-out at the top of the file). The carve-out auto-deactivates once Wave 2 ships DesktopWalkthrough.

    Simpler pattern: gate the two chunk-existence assertions on `process.env.WAVE_4_GATE !== 'false'` and document. Wave 4 plan will set this env var to enforce. Choose whichever is clearer in the executor's hands — leave a `// TODO(wave-4): enforce chunk assertions` comment if deferred.
  </action>
  <acceptance_criteria>
    - `Test-Path scripts/check-bundle-size.ts` returns True
    - `Select-String -Path scripts/check-bundle-size.ts -Pattern 'TOLERANCE_KB'` returns at least one match
    - `Select-String -Path scripts/check-bundle-size.ts -Pattern 'DesktopWalkthrough'` returns at least one match
    - `Select-String -Path scripts/check-bundle-size.ts -Pattern 'WalkthroughVoiceModal'` returns at least one match
    - `Select-String -Path package.json -Pattern '"postbuild"'` returns at least one match
    - `npm run build` exits 0 (no Phase 15 source exists; delta should be 0)
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "if (-not (Test-Path scripts/check-bundle-size.ts)) { exit 1 }; (Select-String -Path scripts/check-bundle-size.ts -Pattern 'TOLERANCE_KB','DesktopWalkthrough','WalkthroughVoiceModal' | Measure-Object).Count -ge 3"</automated>
  </verify>
  <done>`check-bundle-size.ts` exists, references baseline + manifest paths + DesktopWalkthrough + WalkthroughVoiceModal strings, and is wired into `npm run build` via `postbuild` script. Wave-0 carve-out documented inline.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold all Playwright test files + fixtures + lint test referenced in 15-VALIDATION.md</name>
  <files>tests/integration/desktop-walkthrough-layout.spec.ts, tests/integration/sequential-ack.spec.ts, tests/integration/voice-qa-happy-path.spec.ts, tests/integration/voice-grounding-scope.spec.ts, tests/integration/sub-trade-rls-backward-compat.spec.ts, tests/e2e/sub-trade-assignment.spec.ts, tests/fixtures/anthropic-voice-mock.ts, tests/fixtures/visy-enf4-03-031.sql, tests/lint/no-static-desktop-import.spec.ts</files>
  <read_first>
    - `15-VALIDATION.md` § Wave 0 Requirements (full file — 9 listed scaffolds)
    - `15-RESEARCH.md` § Validation Architecture lines 540-578 (test→requirement map)
    - `playwright.config.ts` (use existing test runner setup; no new config)
    - One existing integration test under `tests/integration/` to mirror imports + describe shape — recommend the most recent one referenced by `npm run test:integration`
  </read_first>
  <action>
    Create each test file as a **`test.fixme`** (Playwright) or `test.skip` scaffold so it compiles + appears in `playwright test --list` but does NOT block CI. Each scaffold MUST:
    1. Reference the corresponding SB-LINE-XX requirement in a top-level comment
    2. List the expected assertions as `// TODO:` comments (so Wave 1-4 executors fill them in)
    3. Use Playwright's existing fixtures + cookie-auth pattern from CLAUDE.md learning 2026-04-24 (the `sb-{projectRef}-auth-token` cookie format)

    Specific scaffolds:

    **`tests/integration/desktop-walkthrough-layout.spec.ts`** (SB-LINE-01):
    - `test.fixme('renders DesktopWalkthrough with computed font-size >= 24px at 1920×1080')`
    - `test.fixme('renders MobileWalkthrough byte-identical to Phase 12.5 at 390×844')`

    **`tests/integration/sequential-ack.spec.ts`** (SB-LINE-02):
    - `test.fixme('Next button advances 1→2→3→4→5 only via sequential acknowledgement')`
    - `test.fixme('deep-link to ?step=4 without acking 2/3 redirects to highest acknowledged step')`

    **`tests/integration/voice-qa-happy-path.spec.ts`** (SB-LINE-03):
    - `test.fixme('mic button opens voice modal; transcription appears within 3s')`
    - `test.fixme('answer renders with citation chip clicking which scrolls walkthrough to section')`
    - `test.fixme('ESC closes modal; focus returns to mic button')`
    - Imports `tests/fixtures/anthropic-voice-mock.ts` for canned responses

    **`tests/integration/voice-grounding-scope.spec.ts`** (SB-LINE-04):
    - `test.fixme('question whose answer exists in a DIFFERENT SOP returns "I cant find that in this procedure"')`
    - `test.fixme('verifier flagged claim renders with yellow Verification badge and original claim highlighted')`
    - Requires 2-SOP fixture (seed both via Supabase admin client in beforeAll)

    **`tests/integration/sub-trade-rls-backward-compat.spec.ts`** (SB-LINE-05 backward compat):
    - `test.fixme('worker with no sub_trade rows can read SOP with empty sops_sub_trades (backward compat)')`
    - `test.fixme('worker with fitter tag can read SOP tagged [fitter]; worker without cannot')`

    **`tests/e2e/sub-trade-assignment.spec.ts`** (SB-LINE-05 admin flow):
    - `test.fixme('admin assigns [fitter, sparky] to worker via team page; persists in users_sub_trades')`
    - `test.fixme('admin assigns SOP to [fitter] via assign page; persists in sops_sub_trades')`

    **`tests/fixtures/anthropic-voice-mock.ts`**:
    - Export `mockAnswerCall(answer: string, citations: string[])` returning a fake Anthropic SDK response shape with `cache_creation_input_tokens: 6000` (Q1) and `cache_read_input_tokens: 6000` (Q2)
    - Export `mockVerifierCall(flags: VerificationFlag[])` returning JSON-stringified array
    - Mirror Phase 14's `tests/fixtures/anthropic-mock.ts` shape (read that file first to match conventions)

    **`tests/fixtures/visy-enf4-03-031.sql`**:
    - Minimal SQL seed: insert one `sops` row titled "ENF4-03-031 Blank Side Hanger", with 3 sections (Overview, Hazards [containing "heat-resistant gloves"], Steps with at least 3 sop_steps)
    - Use placeholder UUIDs declared as `\set sop_id '...'` at the top so the file is idempotent
    - Header comment: "Seed fixture for Phase 15 voice grounding tests + final Visy demo prep"

    **`tests/lint/no-static-desktop-import.spec.ts`** (Pitfall 5 — guards SB-LINE-06):
    - `test('no static import of DesktopWalkthrough outside WalkthroughSwitcher.tsx')` — uses Node `fs.readdirSync` over `src/**` to grep for `from .*DesktopWalkthrough` matches; passes if the ONLY match is in `src/components/sop/walkthrough/WalkthroughSwitcher.tsx` AND the import is via `next/dynamic`, fails otherwise
    - Same test for `WalkthroughVoiceModal`
    - This test runs LIVE (not fixme) — initially passes (no matches at all in Phase-14-head); Wave 2 will create the dynamic import; the test catches any regression where someone later adds a static `import { DesktopWalkthrough }` outside the switcher

    All scaffolds MUST compile under `tsc --noEmit` and appear in `npx playwright test --list`. None should fail (skipped/fixme = grey, not red).
  </action>
  <acceptance_criteria>
    - All 9 files in `<files>` exist (verify with `Get-ChildItem tests/integration/desktop-walkthrough-layout.spec.ts, tests/integration/sequential-ack.spec.ts, tests/integration/voice-qa-happy-path.spec.ts, tests/integration/voice-grounding-scope.spec.ts, tests/integration/sub-trade-rls-backward-compat.spec.ts, tests/e2e/sub-trade-assignment.spec.ts, tests/fixtures/anthropic-voice-mock.ts, tests/fixtures/visy-enf4-03-031.sql, tests/lint/no-static-desktop-import.spec.ts | Measure-Object | %{$_.Count -eq 9}`)
    - `npx playwright test --list 2>&1 | Select-String 'desktop-walkthrough-layout|sequential-ack|voice-qa-happy-path|voice-grounding-scope|sub-trade-rls-backward-compat|sub-trade-assignment' | Measure-Object | %{$_.Count -ge 6}` shows all six spec files listed
    - `Select-String -Path tests/integration/voice-qa-happy-path.spec.ts -Pattern 'anthropic-voice-mock'` returns at least one match (fixture wired)
    - `Select-String -Path tests/fixtures/visy-enf4-03-031.sql -Pattern 'heat-resistant gloves'` returns at least one match (so SB-LINE-04 demo question has a target)
    - `Select-String -Path tests/lint/no-static-desktop-import.spec.ts -Pattern 'DesktopWalkthrough','WalkthroughVoiceModal' | Measure-Object | %{$_.Count -ge 2}` — both names grep'd
    - `npx playwright test tests/lint/no-static-desktop-import.spec.ts` PASSES (no static imports exist yet)
    - `npm run lint` passes with all new files included
  </acceptance_criteria>
  <verify>
    <automated>npm run lint; if ($?) { npx playwright test tests/lint/no-static-desktop-import.spec.ts -x }</automated>
  </verify>
  <done>All 9 Wave-0 test/fixture/lint files exist and compile. Skipped/fixme tests appear in `--list`. The no-static-desktop-import lint test PASSES against Phase-14-head. Wave 1-4 executors now have a failing scaffold per requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → repo | CI script reads build manifest and writes to repo — assert script never executes user-controlled paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-00-01 | Tampering | `.bundle-baseline.json` | accept | File in repo + git history; PR review surfaces tampering. Baseline change requires explicit commit by maintainer. |
| T-15-00-02 | Information Disclosure | `tests/fixtures/visy-enf4-03-031.sql` | mitigate | Seed file contains illustrative Visy SOP content only — no real customer secrets. Confirmed with Simon during discuss-phase. |

(Wave 0 is scaffolding only; full security threat register lives in Wave 3 plan where the voice route is built.)
</threat_model>

<verification>
- `Test-Path .bundle-baseline.json` returns True with positive `routes['/sops/[sopId]/page']` integer
- `npm run build` exits 0 (postbuild bundle check passes against same-build baseline = 0 delta)
- `npx playwright test --list` shows all six SB-LINE-XX spec files
- `npx playwright test tests/lint/no-static-desktop-import.spec.ts -x` PASSES
- `npm run lint` PASSES
</verification>

<success_criteria>
- Baseline KB number committed to git so it cannot drift mid-phase
- Every requirement ID (SB-LINE-01..06) has at least one test scaffold ready for Wave 1-4 executors to fill in
- Static-import-leak guard is LIVE (Wave 2 will trigger it if someone forgets `next/dynamic`)
- Wave 0 completes in one sitting; Wave 1 can start immediately after
</success_criteria>

<output>
After completion, create `.planning/phases/15-manufacturing-line-mode/15-00-SUMMARY.md` recording:
- The exact baseline KB number captured for `/sops/[sopId]/page`
- List of scaffolded test files + which `test.fixme` blocks each carries
- Any Wave-0 carve-outs left in `check-bundle-size.ts` for Wave 4 to remove
</output>
