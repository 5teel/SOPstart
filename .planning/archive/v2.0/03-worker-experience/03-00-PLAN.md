---
phase: 03-worker-experience
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/offline-sync.test.ts
  - tests/walkthrough.test.ts
  - tests/quick-ref.test.ts
  - tests/sop-library.test.ts
  - tests/sop-assignment.test.ts
  - tests/sop-versioning.test.ts
  - playwright.config.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - WORK-07
  - WORK-08
  - WORK-09
  - WORK-10
  - MGMT-01
  - MGMT-02
  - MGMT-03
  - MGMT-04
  - MGMT-05
  - MGMT-06
  - MGMT-07
must_haves:
  truths:
    - "Every Phase 3 requirement has a test stub file"
    - "All stub tests are marked test.fixme and produce a clear inventory without failing CI"
    - "Playwright config has a phase3-stubs project matching the new test files"
  artifacts:
    - path: "tests/offline-sync.test.ts"
      provides: "Stubs for WORK-07 and WORK-08"
    - path: "tests/walkthrough.test.ts"
      provides: "Stubs for WORK-01, WORK-02, WORK-05, WORK-06, WORK-09, WORK-10"
    - path: "tests/quick-ref.test.ts"
      provides: "Stubs for WORK-03, WORK-04"
    - path: "tests/sop-library.test.ts"
      provides: "Stubs for MGMT-02, MGMT-03, MGMT-04"
    - path: "tests/sop-assignment.test.ts"
      provides: "Stubs for MGMT-01"
    - path: "tests/sop-versioning.test.ts"
      provides: "Stubs for MGMT-05, MGMT-06, MGMT-07"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/*.test.ts"
      via: "phase3-stubs project testMatch regex"
      pattern: "phase3-stubs"
---

<objective>
Create Playwright test stub files for all 17 Phase 3 requirements, following the established `test.fixme` pattern from Phases 1 and 2.

Purpose: Every requirement gets a test skeleton before implementation begins, providing a clear inventory and Nyquist-compliant verification targets.
Output: 6 test files with `test.fixme` stubs, updated Playwright config with a `phase3-stubs` project.
</objective>

<execution_context>
@C:/Users/Simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-worker-experience/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 6 Playwright test stub files</name>
  <files>
    tests/offline-sync.test.ts,
    tests/walkthrough.test.ts,
    tests/quick-ref.test.ts,
    tests/sop-library.test.ts,
    tests/sop-assignment.test.ts,
    tests/sop-versioning.test.ts
  </files>
  <read_first>
    tests/sop-upload.test.ts,
    tests/sop-review.test.ts,
    .planning/phases/03-worker-experience/03-VALIDATION.md
  </read_first>
  <action>
Create 6 test files following the exact `test.fixme` pattern used in Phase 2 stubs (read sop-upload.test.ts for the pattern). Each test uses `import { test, expect } from '@playwright/test'` and every test case is `test.fixme(...)`.

**tests/offline-sync.test.ts:**
- `test.fixme('WORK-07: worker can access cached SOPs without internet connection', ...)`
- `test.fixme('WORK-08: data entered offline syncs automatically when connectivity returns', ...)`

**tests/walkthrough.test.ts:**
- `test.fixme('WORK-01: worker can walk through SOP step-by-step with progress indication', ...)`
- `test.fixme('WORK-02: worker can navigate back to previous steps during walkthrough', ...)`
- `test.fixme('WORK-05: hazard and PPE information displayed before procedure steps', ...)`
- `test.fixme('WORK-06: images display inline within SOP steps with zoom capability', ...)`
- `test.fixme('WORK-09: all primary actions use 72px+ tap targets', ...)`
- `test.fixme('WORK-10: walkthrough uses full-screen interface optimised for one-handed use', ...)`

**tests/quick-ref.test.ts:**
- `test.fixme('WORK-03: worker can view SOP sections via tabbed quick-reference mode', ...)`
- `test.fixme('WORK-04: worker can jump directly to any section without walking through steps', ...)`

**tests/sop-library.test.ts:**
- `test.fixme('MGMT-02: worker sees assigned SOPs first when browsing library', ...)`
- `test.fixme('MGMT-03: worker can search SOP library by title and content', ...)`
- `test.fixme('MGMT-04: worker can browse SOPs by category or department', ...)`

**tests/sop-assignment.test.ts:**
- `test.fixme('MGMT-01: admin can assign SOPs to specific roles or individual workers', ...)`

**tests/sop-versioning.test.ts:**
- `test.fixme('MGMT-05: admin can update SOP by uploading a new version', ...)`
- `test.fixme('MGMT-06: previous SOP versions retained and linked to historical completions', ...)`
- `test.fixme('MGMT-07: workers notified when assigned SOP has been updated', ...)`

Each test body should contain a comment describing the test intent (e.g., `// Navigate to walkthrough page, verify step counter shows "Step 1 of N", mark steps complete, verify progress updates`). This provides executor context when tests are activated later.
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx playwright test --list 2>&1 | grep -c "fixme"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "test.fixme" tests/offline-sync.test.ts returns 2
    - grep -c "test.fixme" tests/walkthrough.test.ts returns 6
    - grep -c "test.fixme" tests/quick-ref.test.ts returns 2
    - grep -c "test.fixme" tests/sop-library.test.ts returns 3
    - grep -c "test.fixme" tests/sop-assignment.test.ts returns 1
    - grep -c "test.fixme" tests/sop-versioning.test.ts returns 3
    - Each test.fixme name starts with its requirement ID (e.g., "WORK-01:", "MGMT-03:")
  </acceptance_criteria>
  <done>17 test stubs across 6 files, all using test.fixme, all referencing their requirement ID</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright config with phase3-stubs project</name>
  <files>playwright.config.ts</files>
  <read_first>playwright.config.ts</read_first>
  <action>
Add a new project entry to the `projects` array in `playwright.config.ts`:

```typescript
{
  name: 'phase3-stubs',
  testMatch: /offline-sync|walkthrough|quick-ref|sop-library|sop-assignment|sop-versioning/,
},
```

This follows the exact pattern used for `phase2-stubs` (which matches `sop-upload|sop-parsing|sop-review`). The regex must match all 6 new test file names. Place the new entry after the existing `phase2-stubs` project.
  </action>
  <verify>
    <automated>cd "C:/Development/SOP Assistant" && npx playwright test --project=phase3-stubs --list 2>&1 | head -25</automated>
  </verify>
  <acceptance_criteria>
    - grep "phase3-stubs" playwright.config.ts returns a match
    - npx playwright test --project=phase3-stubs --list shows all 17 test stubs
    - npx playwright test --project=phase3-stubs exits with code 0 (fixme tests are skipped, not failed)
  </acceptance_criteria>
  <done>Playwright discovers all 17 Phase 3 test stubs under the phase3-stubs project and exits cleanly</done>
</task>

</tasks>

<verification>
- `npx playwright test --project=phase3-stubs` exits 0 (all tests are fixme = skipped)
- All 17 requirement IDs (WORK-01 through WORK-10, MGMT-01 through MGMT-07) appear in test names
</verification>

<success_criteria>
- 6 test files created in tests/ directory
- 17 test.fixme stubs covering every Phase 3 requirement
- Playwright config updated with phase3-stubs project
- CI remains green (fixme tests are skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-experience/03-00-SUMMARY.md`
</output>
